---
phase: 68-init-wizard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/cli/init.rs
  - tests/integration.rs
autonomous: true
requirements:
  - INIT-02
  - INIT-03
must_haves:
  truths:
    - "run_wizard 接受 &mut impl BufRead + &mut impl Write，使用全 Enter 输入时返回 sqllogs/Csv/outputs/sqllog.csv 默认值"
    - "run_wizard 在导出格式输入无效时最多重试 3 次后返回 Err(Error::Config(ConfigError::InvalidValue))"
    - "run_wizard 接受 'sqlite' 时继续询问 sqlite_db 和 sqlite_table 两步"
    - "apply_wizard_answers_to_template 输出 CSV 模式时含 inputs/csv.file 替换后的值，[exporter.csv] 段未被注释，[exporter.sqlite] 段保持原模板注释状态"
    - "apply_wizard_answers_to_template 输出 SQLite 模式时 [exporter.sqlite] 段被激活（去掉 # 前缀），database_url/table_name 替换为用户值，[exporter.csv] 段每行加 # 前缀注释掉"
    - "apply_wizard_answers_to_template 输出可被 toml::from_str::<Config>() 成功解析并通过 Config::validate()"
    - "write_config_file 私有函数从 handle_init 提取，handle_init 改造后函数体仍 ≤ 40 行"
  artifacts:
    - path: "src/cli/init.rs"
      provides: "WizardAnswers / ExporterChoice / run_wizard / apply_wizard_answers_to_template / write_config_file"
      contains: "pub fn run_wizard"
    - path: "src/cli/init.rs"
      provides: "WizardAnswers / ExporterChoice / run_wizard / apply_wizard_answers_to_template / write_config_file"
      contains: "fn apply_wizard_answers_to_template"
    - path: "src/cli/init.rs"
      provides: "WizardAnswers / ExporterChoice / run_wizard / apply_wizard_answers_to_template / write_config_file"
      contains: "fn write_config_file"
    - path: "src/cli/init.rs"
      provides: "新增 5+ 个 #[cfg(test)] 单元测试覆盖向导核心路径"
      contains: "test_wizard_all_defaults"
  key_links:
    - from: "run_wizard"
      to: "WizardAnswers"
      via: "返回值"
      pattern: "Result<WizardAnswers>"
    - from: "apply_wizard_answers_to_template"
      to: "CONFIG_TEMPLATE_EN"
      via: "str::replace 链式调用"
      pattern: "CONFIG_TEMPLATE_EN.*\\.replace"
    - from: "handle_init"
      to: "write_config_file"
      via: "私有函数调用（重构后）"
      pattern: "write_config_file\\(path"
---

<objective>
在 `src/cli/init.rs` 中新增向导核心逻辑（`WizardAnswers` / `ExporterChoice` / `run_wizard` / `apply_wizard_answers_to_template`），并将 `handle_init` 现有文件写入逻辑提取为 `write_config_file` 私有函数，供本 Plan 与 Plan 02 的 `handle_init_interactive` 共用。新增 5+ 个单元测试覆盖向导默认路径、自定义 csv 路径、sqlite 路径、无效格式重试、模板替换正确性。

Purpose: 把可测试的核心向导算法与 CLI 接入解耦——本 Plan 提供"算法 + 模板替换 + 写入复用"，Plan 02 只做"flag + dispatch + e2e"。这样 INIT-02 / INIT-03 的全部单元级保证在本 Plan 内完成。
Output: src/cli/init.rs 新增类型与函数 + handle_init 重构 + 单元测试块。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/68-init-wizard/68-CONTEXT.md
@.planning/phases/68-init-wizard/68-RESEARCH.md
@.planning/phases/68-init-wizard/68-PATTERNS.md
@.planning/phases/68-init-wizard/68-UI-SPEC.md
@src/cli/init.rs
@src/error.rs

<interfaces>
<!-- 关键现有类型，executor 应直接使用而非探索 -->

From src/error.rs:
- pub type Result<T> = std::result::Result<T, Error>;
- pub enum Error { Io(#[from] io::Error), Config(#[from] ConfigError), File(FileError), ... }
- pub enum ConfigError { InvalidValue { field: String, value: String, reason: String }, ... }
- pub enum FileError { AlreadyExists { path: PathBuf }, WriteFailed { path, reason }, CreateDirectoryFailed { path, reason } }

From src/cli/init.rs (现状):
- pub fn handle_init(output_path: &str, force: bool) -> Result<()>
- const CONFIG_TEMPLATE_EN: &str  (内含 inputs = ["sqllogs"] / file = "outputs/sqllog.csv" / # [exporter.sqlite] 等替换目标)

新增导出（本 Plan 添加）:
- pub enum ExporterChoice { Csv, Sqlite }
- pub struct WizardAnswers { pub inputs: String, pub exporter: ExporterChoice, pub csv_file: Option<String>, pub sqlite_db: Option<String>, pub sqlite_table: Option<String> }
- pub fn run_wizard(reader: &mut impl BufRead, writer: &mut impl Write) -> Result<WizardAnswers>
- fn apply_wizard_answers_to_template(answers: &WizardAnswers) -> String  (私有)
- fn write_config_file(path: &Path, content: &str, force: bool) -> Result<()>  (私有，从 handle_init 提取)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 提取 write_config_file 私有函数并改造 handle_init</name>
  <files>src/cli/init.rs</files>
  <read_first>
    - src/cli/init.rs  (handle_init 完整实现位于第 1-59 行；CONFIG_TEMPLATE_EN 位于第 63-152 行)
    - src/error.rs  (Error / FileError 变体定义)
    - .planning/phases/68-init-wizard/68-PATTERNS.md  ("write_config_file 私有函数" 章节给出精确签名与提取范围)
    - tests/integration.rs  (第 156-212 行现有 handle_init 测试，必须在重构后仍全部通过)
  </read_first>
  <behavior>
    - 现有 4 个 handle_init 测试（test_handle_init_creates_config_file / test_handle_init_fails_if_exists_without_force / test_handle_init_force_overwrites_existing / test_handle_init_en_template）继续通过
    - handle_init 与 write_config_file 函数体均 ≤ 40 行（per CLAUDE.md）
    - write_config_file 接受 (path: &Path, content: &str, force: bool)，返回 Result<()>，包含：文件存在检查、--force 行为、目录创建、fs::write、AlreadyExists/WriteFailed/CreateDirectoryFailed 错误映射、覆盖/创建的 info! 日志
    - handle_init 改造后委托 write_config_file 完成写入，并保留原有 "Next steps" 三行 info! 输出
  </behavior>
  <action>
    在 src/cli/init.rs 中新增私有函数 `fn write_config_file(path: &Path, content: &str, force: bool) -> Result<()>`，将当前 handle_init 第 12-51 行（file_existed 计算 → fs::write → file_existed ? overwritten : generated 日志）原样迁入此函数（per D-10）。函数顶部保留 "Preparing to generate configuration file" 的 info! 日志（即把 handle_init 第 10 行也迁入），保证日志顺序不变。

    然后改造 handle_init 函数本体：先 `let path = Path::new(output_path);`，再 `let content = CONFIG_TEMPLATE_EN;`，调用 `write_config_file(path, content, force)?`，最后保留第 53-57 行原有 "Next steps" 三行 info! 输出。删除原 handle_init 中已迁入 write_config_file 的代码（包括 debug! 提示日志），avoid 重复输出。

    保留 use 语句不变（log::{debug, error, info, warn} 仍需 debug！调用如果留在 write_config_file 中，否则删除未用 import 以免 clippy unused-imports 警告）。

    write_config_file 中的错误返回继续使用 `Error::File(FileError::AlreadyExists { path: path.to_path_buf() })` 等模式（per PATTERNS.md "write_config_file 私有函数" 章节展示的精确代码片段）。
  </action>
  <verify>
    <automated>cargo test --test integration test_handle_init -- --exact-match</automated>
  </verify>
  <acceptance_criteria>
    - src/cli/init.rs 包含 `fn write_config_file(path: &Path, content: &str, force: bool) -> Result<()>`
    - handle_init 函数体（fn handle_init 到下一个顶层 fn/const 之前）行数 ≤ 40 行（验证：`awk '/^pub fn handle_init\(/,/^}/' src/cli/init.rs | wc -l` 结果 ≤ 42 含签名和闭花括号）
    - write_config_file 函数体行数 ≤ 40 行（同上 awk 验证）
    - `cargo test --test integration test_handle_init_creates_config_file -- --exact-match` 退出 0
    - `cargo test --test integration test_handle_init_fails_if_exists_without_force -- --exact-match` 退出 0
    - `cargo test --test integration test_handle_init_force_overwrites_existing -- --exact-match` 退出 0
    - `cargo test --test integration test_handle_init_en_template -- --exact-match` 退出 0
    - `cargo clippy --all-targets -- -D warnings` 退出 0（无 unused imports / dead code 警告）
    - `cargo fmt --check` 退出 0
  </acceptance_criteria>
  <done>handle_init 重构为委托 write_config_file 的瘦壳函数，私有 write_config_file 承担文件 I/O，所有现有 handle_init 测试继续通过，cargo clippy/fmt 三道门禁绿灯。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 新增 WizardAnswers / ExporterChoice / run_wizard 与 5 个单元测试</name>
  <files>src/cli/init.rs</files>
  <read_first>
    - src/cli/init.rs  (Task 1 重构后状态)
    - src/error.rs  (ConfigError::InvalidValue 字段：field/value/reason；Error::Io 通过 From<io::Error> 自动转换)
    - .planning/phases/68-init-wizard/68-CONTEXT.md  (D-04 精确提示串、D-07 函数签名、D-11/D-12/D-13 测试策略)
    - .planning/phases/68-init-wizard/68-UI-SPEC.md  ("Wizard Steps Contract" 与 "Copywriting Contract" 给出每步精确字符串)
    - .planning/phases/68-init-wizard/68-RESEARCH.md  ("Code Examples" 章节给出 run_wizard 骨架与测试模板)
    - .planning/phases/68-init-wizard/68-PATTERNS.md  ("run_wizard 函数签名与 IO 模式" 章节)
  </read_first>
  <behavior>
    - test_wizard_all_defaults：输入 `b"\n\n\n"`（3 个 Enter），返回 WizardAnswers { inputs: "sqllogs", exporter: ExporterChoice::Csv, csv_file: Some("outputs/sqllog.csv"), sqlite_db: None, sqlite_table: None }
    - test_wizard_custom_csv_path：输入 `b"my/logs\ncsv\nmy_out/result.csv\n"`，返回 inputs=="my/logs"，exporter==Csv，csv_file==Some("my_out/result.csv")
    - test_wizard_sqlite_path：输入 `b"\nsqlite\nmy.db\nmy_table\n"`，返回 exporter==Sqlite，sqlite_db==Some("my.db")，sqlite_table==Some("my_table")，csv_file==None
    - test_wizard_sqlite_defaults：输入 `b"\nsqlite\n\n\n"`，返回 sqlite_db==Some("export/sqllog2db.db")，sqlite_table==Some("sqllog_records")
    - test_wizard_invalid_format_three_times_returns_err：输入 `b"\nbad\nbad\nbad\n"`，返回 Err，且 matches!(err, Error::Config(ConfigError::InvalidValue { field, .. }) if field == "exporter")
    - test_wizard_writer_receives_prompts：输入 `b"\n\n\n"`，writer (Vec<u8>) 内容包含 "SQL log 输入目录" 与 "导出格式 (csv/sqlite)" 与 "CSV 输出文件路径"
  </behavior>
  <action>
    在 src/cli/init.rs 顶部 use 区追加：`use crate::error::ConfigError;` 和 `use std::io::{BufRead, Write};`。

    在 CONFIG_TEMPLATE_EN 常量定义之前（即 handle_init 之后），新增公开类型（per D-07 + UI-SPEC Data Model Contract）：
    - `pub enum ExporterChoice { Csv, Sqlite }`（派生 Debug + PartialEq + Eq 便于测试 matches! 与 assert_eq!）
    - `pub struct WizardAnswers { pub inputs: String, pub exporter: ExporterChoice, pub csv_file: Option<String>, pub sqlite_db: Option<String>, pub sqlite_table: Option<String> }`（派生 Debug + PartialEq + Eq）

    新增 `pub fn run_wizard(reader: &mut impl BufRead, writer: &mut impl Write) -> Result<WizardAnswers>`（per D-07/D-11）。函数体顺序：

    Step 1 (inputs) — 使用 UI-SPEC 精确字符串 `SQL log 输入目录（可以是目录、文件或 glob 模式）[default: sqllogs]: ` 通过 `write!` + `writer.flush()?` 输出，`reader.read_line(&mut buf)?` 读入，`.trim()` 后判空：空则取 "sqllogs"，否则取 trim 结果。读入失败由 `?` 自动转 Error::Io。

    Step 2 (exporter，最多 3 次循环) — 提示串 `导出格式 (csv/sqlite) [default: csv]: `；trim 后：空 → Csv 跳出循环；"csv" → Csv 跳出；"sqlite" → Sqlite 跳出；其他 → 输出 `无效格式"{user_input}"，请输入 csv 或 sqlite: ` 重新读入。3 次后仍无效返回 `Err(Error::Config(ConfigError::InvalidValue { field: "exporter".to_owned(), value: <最后一次原始输入 trim 后>.to_owned(), reason: "must be 'csv' or 'sqlite'".to_owned() }))`（per RESEARCH Discretion + PATTERNS "导出格式验证错误类型"）。提示重发的精确字符串见 UI-SPEC "Error Messages Contract"。

    Step 3a (CSV 模式) — 仅当 exporter == Csv：提示 `CSV 输出文件路径 [default: outputs/sqllog.csv]: `，空 → "outputs/sqllog.csv"，否则取输入。写入 csv_file = Some(...)。sqlite_db / sqlite_table 设为 None。

    Step 3b/3c (SQLite 模式) — 仅当 exporter == Sqlite：先提示 `SQLite 数据库路径 [default: export/sqllog2db.db]: `，空 → "export/sqllog2db.db"，存 sqlite_db。再提示 `表名（仅含字母/数字/下划线）[default: sqllog_records]: `，空 → "sqllog_records"，存 sqlite_table。csv_file 设为 None。

    每次 read_line 前 `buf.clear()`，避免上次输入残留。

    在 src/cli/init.rs 文件底部 `#[cfg(test)] mod tests { use super::*; ... }` 中追加上述 6 个单元测试。所有测试使用 `std::io::Cursor::new(input.as_ref())` 作为 reader，`Vec::<u8>::new()` 作为 writer（per D-11/D-13）。

    不在 run_wizard 中调用 log::* —— 提示文本仅走 writer（per UI-SPEC "log::info! / log::warn! 输出约定"）。
  </action>
  <verify>
    <automated>cargo test --test integration test_wizard -- --nocapture && cargo test --lib wizard -- --nocapture; cargo clippy --all-targets -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    - src/cli/init.rs 包含 `pub fn run_wizard(reader: &mut impl BufRead, writer: &mut impl Write) -> Result<WizardAnswers>`
    - src/cli/init.rs 包含 `pub enum ExporterChoice` 且至少有 Csv / Sqlite 两个变体
    - src/cli/init.rs 包含 `pub struct WizardAnswers` 且字段名严格为 inputs / exporter / csv_file / sqlite_db / sqlite_table
    - `grep -c "fn test_wizard_" src/cli/init.rs` ≥ 6
    - `cargo test --lib wizard` 退出 0（6 个测试全通过）
    - test_wizard_invalid_format_three_times_returns_err 通过：断言 err matches Error::Config(ConfigError::InvalidValue { field, .. }) 且 field == "exporter"
    - run_wizard 提示文本逐字符匹配 UI-SPEC：grep 验证 `grep -F 'SQL log 输入目录（可以是目录、文件或 glob 模式）[default: sqllogs]: ' src/cli/init.rs` 返回 1 行
    - grep 验证 `grep -F '导出格式 (csv/sqlite) [default: csv]: ' src/cli/init.rs` 返回 1 行
    - grep 验证 `grep -F 'CSV 输出文件路径 [default: outputs/sqllog.csv]: ' src/cli/init.rs` 返回 1 行
    - grep 验证 `grep -F 'SQLite 数据库路径 [default: export/sqllog2db.db]: ' src/cli/init.rs` 返回 1 行
    - grep 验证 `grep -F '表名（仅含字母/数字/下划线）[default: sqllog_records]: ' src/cli/init.rs` 返回 1 行
    - `cargo clippy --all-targets -- -D warnings` 退出 0
    - run_wizard 函数体 ≤ 40 行（如超过则拆分私有辅助函数 prompt_step(reader, writer, prompt, default) 等）
  </acceptance_criteria>
  <done>WizardAnswers / ExporterChoice / run_wizard 实现完成，6 个单元测试全部通过，提示文本与 UI-SPEC 逐字匹配，clippy 零警告。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 实现 apply_wizard_answers_to_template + 模板替换正确性测试</name>
  <files>src/cli/init.rs</files>
  <read_first>
    - src/cli/init.rs  (Task 2 后状态，特别是 CONFIG_TEMPLATE_EN 完整内容)
    - src/config/mod.rs  (Config::from_str / toml::from_str::<Config> 入口确认)
    - .planning/phases/68-init-wizard/68-CONTEXT.md  (D-08/D-09 替换策略)
    - .planning/phases/68-init-wizard/68-PATTERNS.md  ("apply_wizard_answers_to_template 字符串替换模式" 表格)
    - .planning/phases/68-init-wizard/68-UI-SPEC.md  ("Template Substitution Contract" 章节)
    - .planning/phases/68-init-wizard/68-RESEARCH.md  ("Pitfall 1 / Pitfall 4" 关于 logging.file 冲突与 SQLite 段激活精确格式)
  </read_first>
  <behavior>
    - test_apply_csv_default：WizardAnswers { inputs: "sqllogs", exporter: Csv, csv_file: Some("outputs/sqllog.csv"), .. None } → 输出字符串 == CONFIG_TEMPLATE_EN（零替换路径，全默认）
    - test_apply_csv_custom：WizardAnswers { inputs: "my/dir", exporter: Csv, csv_file: Some("out/r.csv"), .. } → 输出包含 `inputs = ["my/dir"]` 与 `file = "out/r.csv"`，且 [exporter.csv] 段未被注释（即包含未注释的 `[exporter.csv]` 行），[exporter.sqlite] 段保持注释（即包含 `# [exporter.sqlite]`）
    - test_apply_sqlite：WizardAnswers { inputs: "x", exporter: Sqlite, sqlite_db: Some("d.db"), sqlite_table: Some("t"), csv_file: None } → 输出包含未注释的 `[exporter.sqlite]`、`database_url = "d.db"`、`table_name = "t"`；同时 `[exporter.csv]` 行变为 `# [exporter.csv]`，且原 `file = "outputs/sqllog.csv"` 变为 `# file = "outputs/sqllog.csv"`
    - test_apply_does_not_corrupt_logging_file：输出永远包含未变更的 `file = "logs/sqllog2db.log"` 行（验证 Pitfall 1 未发生）
    - test_apply_output_parses_as_config_csv：CSV 模式输出 `toml::from_str::<crate::config::Config>(&output)` 成功且 Config::validate() Ok
    - test_apply_output_parses_as_config_sqlite：SQLite 模式输出同上
  </behavior>
  <action>
    在 src/cli/init.rs 中新增私有函数 `fn apply_wizard_answers_to_template(answers: &WizardAnswers) -> String`。

    实现步骤（per D-08/D-09 + PATTERNS 替换表）：

    1. 从 `let mut content = CONFIG_TEMPLATE_EN.to_string();` 起。

    2. 替换 inputs：`content = content.replace(r#"inputs = ["sqllogs"]"#, &format!(r#"inputs = ["{}"]"#, answers.inputs));`。

    3. 分支处理 exporter：

       - 若 `answers.exporter == ExporterChoice::Csv`：
         - 仅替换 `file = "outputs/sqllog.csv"` 为 `file = "{user_csv}"`（仅当 csv_file 非默认时实质改变；默认时 replace 自身无副作用）。
         - 不对 [exporter.sqlite] / [exporter.csv] 段做任何注释切换。

       - 若 `answers.exporter == ExporterChoice::Sqlite`：
         - 步骤 a：注释掉 CSV 段。精确匹配 `[exporter.csv]` 整段文本块（从 `# Option 1: CSV export (default)\n[exporter.csv]\n# CSV output file path\nfile = "outputs/sqllog.csv"\n# Drop and recreate the file before writing (true/false)\noverwrite = true\n# Append to existing CSV file instead of overwriting (true/false)\nappend = false\n` 替换为 `# Option 1: CSV export (default)\n# [exporter.csv]\n# CSV output file path\n# file = "outputs/sqllog.csv"\n# Drop and recreate the file before writing (true/false)\n# overwrite = true\n# Append to existing CSV file instead of overwriting (true/false)\n# append = false\n`）—— 即在 `[exporter.csv]` / `file =` / `overwrite =` / `append =` 4 行各加前缀 `# `。建议用 4 次 `.replace()` 链式精确替换以避免误伤注释行。
         - 步骤 b：激活 sqlite 段。
           - `.replace("# [exporter.sqlite]", "[exporter.sqlite]")`
           - `.replace(r#"# database_url = "export/sqllog2db.db""#, &format!(r#"database_url = "{}""#, answers.sqlite_db.as_deref().unwrap_or("export/sqllog2db.db")))`
           - `.replace(r#"# table_name = "sqllog_records""#, &format!(r#"table_name = "{}""#, answers.sqlite_table.as_deref().unwrap_or("sqllog_records")))`
           - `.replace("# overwrite = true\n# append = false", "overwrite = true\nappend = false")` —— sqlite 段尾部两行去掉 `# ` 前缀。注意该字符串在模板末尾唯一（CSV 段的 overwrite/append 不带 `# ` 前缀，per src/cli/init.rs 第 138-140 行）。

    4. 返回 content。

    在 #[cfg(test)] mod tests 中追加 6 个测试（per "behavior"）。test_apply_output_parses_as_config_* 通过 `let cfg: crate::config::Config = toml::from_str(&content).expect(...)` 后 `cfg.validate().expect(...)` 双重断言。

    apply_wizard_answers_to_template 函数体若超过 40 行，按 CSV 路径 / SQLite 路径拆分为 fn apply_csv_substitutions(content, answers) 与 fn apply_sqlite_substitutions(content, answers)。
  </action>
  <verify>
    <automated>cargo test --lib apply_ && cargo clippy --all-targets -- -D warnings && cargo fmt --check</automated>
  </verify>
  <acceptance_criteria>
    - src/cli/init.rs 包含 `fn apply_wizard_answers_to_template(answers: &WizardAnswers) -> String`
    - `grep -c "fn test_apply_" src/cli/init.rs` ≥ 6
    - `cargo test --lib apply_` 退出 0（6 个测试全通过）
    - test_apply_csv_default 通过：输出与 CONFIG_TEMPLATE_EN 字符串相等（保证默认 CSV 路径零副作用）
    - test_apply_sqlite 通过：输出可被 `toml::from_str::<crate::config::Config>` 成功解析且 Config::validate() 返回 Ok
    - test_apply_does_not_corrupt_logging_file 通过：输出永远 contains `file = "logs/sqllog2db.log"`（不带 `# ` 前缀）
    - apply_wizard_answers_to_template 函数体 ≤ 40 行（若拆分则每个辅助函数也 ≤ 40 行）
    - `cargo clippy --all-targets -- -D warnings` 退出 0
    - `cargo fmt --check` 退出 0
  </acceptance_criteria>
  <done>apply_wizard_answers_to_template 实现完成，CSV 默认零副作用、CSV 自定义路径正确替换、SQLite 段正确激活、logging 段不被误改，6 个测试全通过，clippy/fmt 双门禁绿灯。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stdin → run_wizard | 用户键盘输入（任意字符串）跨入向导，包括 inputs 路径、格式选择、CSV/SQLite 路径与表名 |
| WizardAnswers → CONFIG_TEMPLATE_EN | 用户输入值经 str::replace 嵌入到将被 toml::from_str 解析的字符串 |
| 生成 content → fs::write | 嵌入用户输入后的 TOML 内容写入磁盘 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-68-01 | Tampering | run_wizard Step 2 (exporter 选择) | mitigate | 白名单枚举：只接受空 / "csv" / "sqlite"，其他输入最多 3 次重试后返回 Error::Config(ConfigError::InvalidValue)。不允许任意字符串通过此步骤进入模板替换 |
| T-68-02 | Tampering | apply_wizard_answers_to_template (用户输入嵌入 TOML 字符串) | mitigate | 用户输入仅嵌入到带双引号的 TOML 字符串字段（inputs / file / database_url / table_name）。如果用户输入含双引号或换行符，会被 TOML 解析拒绝；test_apply_output_parses_as_config_* 单元测试通过 toml::from_str + Config::validate 双重验证拦截损坏模板。不引入任何 shell/SQL 上下文，无注入面。inputs 路径不做存在性验证（per UI-SPEC，与非交互式 init 行为一致） |
| T-68-03 | Information Disclosure | run_wizard 提示输出 | accept | 提示文本不含敏感信息（无凭证、无文件内容），输出到本地 stdout，无网络外泄面 |
| T-68-04 | Denial of Service | Step 2 无效格式循环 | mitigate | 硬上限 3 次（per CONTEXT Discretion），避免无限循环。test_wizard_invalid_format_three_times_returns_err 单元测试守护此行为 |
| T-68-SC | Tampering | npm/pip/cargo installs | mitigate | 本 Plan 不引入任何新 crate（per D-06），无安装步骤，无 slopcheck 风险面 |
</threat_model>

<verification>
- `cargo test --test integration test_handle_init` 全部通过（重构无回归）
- `cargo test --lib wizard` 通过（向导单元测试）
- `cargo test --lib apply_` 通过（模板替换单元测试）
- `cargo clippy --all-targets -- -D warnings` 零警告
- `cargo fmt --check` 零差异
- grep 验证 UI-SPEC 精确提示串逐字符匹配（5 处）
- handle_init / write_config_file / run_wizard / apply_wizard_answers_to_template 函数体均 ≤ 40 行
</verification>

<success_criteria>
- src/cli/init.rs 公开 run_wizard、WizardAnswers、ExporterChoice，可被外部模块（main.rs / tests）使用
- 私有 write_config_file 与 apply_wizard_answers_to_template 可被 handle_init 和（后续 Plan 02）handle_init_interactive 共用
- 全部 5 类 D-12 测试场景覆盖（csv 默认、自定义 csv、sqlite、无效格式重试、空 inputs 默认）
- SQLite 模式生成的 TOML 可被 Config::from_str + Config::validate 双重通过（INIT-03 单元级保证）
- 重构后 handle_init 函数体仍 ≤ 40 行（符合 CLAUDE.md）
- 三道质量门禁（cargo test / clippy / fmt）全绿
</success_criteria>

<output>
Create `.planning/phases/68-init-wizard/68-01-SUMMARY.md` when done. Must list:
- 实际新增公开类型与函数签名（WizardAnswers / ExporterChoice / run_wizard）
- 重构后 handle_init / write_config_file 行数
- 单元测试清单（>= 12 个新测试名）
- 任何在实现过程中发现的模板字符串边界条件（特别是 SQLite 段激活的精确多行替换确认）
</output>
