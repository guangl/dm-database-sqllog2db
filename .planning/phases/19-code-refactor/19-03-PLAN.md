---
phase: 19-code-refactor
plan: 03
type: execute
wave: 2
depends_on:
  - 19-01
  - 19-02
files_modified:
  - src/exporter/mod.rs
  - src/exporter/projection.rs
  - src/exporter/csv.rs
  - src/exporter/csv/mod.rs
  - src/exporter/csv/writer.rs
  - src/exporter/csv/companion.rs
  - src/exporter/sqlite.rs
  - src/exporter/sqlite/mod.rs
  - src/exporter/sqlite/sql_builder.rs
  - src/exporter/sqlite/write.rs
  - src/lib.rs
autonomous: true
requirements:
  - REFACTOR-01
  - REFACTOR-02
  - REFACTOR-03
  - REFACTOR-04
tags:
  - rust
  - refactor
  - exporter
  - trait-cleanup
  - projection

must_haves:
  truths:
    - "src/exporter/csv.rs 和 src/exporter/sqlite.rs 已被替换为 src/exporter/csv/ 和 src/exporter/sqlite/ 目录子模块"
    - "新建 src/exporter/projection.rs，包含 pub(crate) fn projected_field_names(ordered_indices: &[usize]) -> Vec<&'static str>"
    - "sqlite/sql_builder.rs::build_insert_sql 和 build_create_sql 调用 projection::projected_field_names，删除原内联 cols 映射逻辑"
    - "DryRunExporter struct 已删除，整合为 ExporterKind::DryRun { stats: ExportStats } struct variant（D-08）"
    - "Exporter trait 的 export_one_normalized / export_one_preparsed 默认实现中冗余 match 分支已清理（D-07）"
    - "src/exporter/ 目录下每个 .rs 子文件行数 ≤ 300"
    - "ExporterManager / ExporterKind / Exporter trait / CsvExporter / SqliteExporter 等的可见性已按 D-10 收紧"
    - "cargo test 全部通过，cargo clippy --all-targets -- -D warnings 零警告，无性能回归"
  artifacts:
    - path: "src/exporter/projection.rs"
      provides: "字段投影共用辅助函数 projected_field_names"
      contains: "fn projected_field_names"
    - path: "src/exporter/csv/mod.rs"
      provides: "CsvExporter struct + new/from_config + Exporter impl"
      contains: "struct CsvExporter"
    - path: "src/exporter/csv/writer.rs"
      provides: "热路径 write_record_preparsed 静态方法 + write_csv_escaped"
      contains: "fn write_record_preparsed"
    - path: "src/exporter/csv/companion.rs"
      provides: "write_companion_rows + format_companion_row"
      contains: "fn write_companion_rows"
    - path: "src/exporter/sqlite/mod.rs"
      provides: "SqliteExporter struct + new/from_config + Exporter impl + conn_ref 辅助"
      contains: "struct SqliteExporter"
    - path: "src/exporter/sqlite/sql_builder.rs"
      provides: "build_insert_sql / build_create_sql"
      contains: "fn build_insert_sql"
    - path: "src/exporter/sqlite/write.rs"
      provides: "do_insert_preparsed + write_template_stats"
      contains: "fn do_insert_preparsed"
    - path: "src/exporter/mod.rs"
      provides: "Exporter trait + ExporterKind（含 DryRun 整合）+ ExporterManager + ExportStats"
      max_lines: 600
  key_links:
    - from: "src/exporter/sqlite/sql_builder.rs"
      to: "src/exporter/projection.rs"
      via: "use super::super::projection::projected_field_names"
      pattern: "projection::projected_field_names"
    - from: "src/exporter/mod.rs"
      to: "ExporterKind::DryRun { stats }"
      via: "struct variant"
      pattern: "DryRun\\s*\\{"
---

<objective>
完成 exporter 子系统的结构重构：
1. 拆分 csv.rs (1260 行) → csv/{mod.rs, writer.rs, companion.rs}
2. 拆分 sqlite.rs (1302 行) → sqlite/{mod.rs, sql_builder.rs, write.rs}
3. 新建 projection.rs，提取 CSV/SQLite 共用字段映射
4. 整合 DryRunExporter 进 ExporterKind（D-08）
5. 清理 Exporter trait 默认实现冗余 match（D-07）
6. 评估并按需精简 exporter/mod.rs（756 行 → 目标 ≤ 600）
7. 全面收紧本模块所有 pub 可见性（D-10）

Purpose: 覆盖 REFACTOR-01（拆分 csv/sqlite）、REFACTOR-02（projection 共用层）、REFACTOR-03（trait 统一 + DryRun 整合）、REFACTOR-04（exporter 模块可见性收紧）。是 Phase 19 的核心 Plan。

Output: 重构后的 src/exporter/ 子树，所有现有测试通过，性能基准无回归。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/19-code-refactor/19-CONTEXT.md
@.planning/phases/19-code-refactor/19-RESEARCH.md
@.planning/phases/19-code-refactor/19-PATTERNS.md
@.planning/codebase/CONCERNS.md
@src/exporter/mod.rs
@src/exporter/csv.rs
@src/exporter/sqlite.rs
@src/pipeline/mod.rs
@src/lib.rs

<interfaces>
<!-- 必须保持稳定的外部 API 与签名 -->

外部消费者（src/cli/run.rs, tests/integration.rs 等）依赖路径：

```
crate::exporter::ExporterManager           // src/cli/run.rs 主调用方
crate::exporter::ExporterManager::dry_run()
crate::exporter::ExporterManager::from_csv(...)
crate::exporter::ExporterManager::from_sqlite(...)
crate::exporter::ExporterManager::from_config(...)
crate::exporter::CsvExporter               // 当前 lib.rs pub use（评估是否收紧）
crate::exporter::SqliteExporter            // 当前 lib.rs pub use（评估是否收紧）
crate::exporter::Exporter                  // trait，当前 lib.rs pub use
crate::exporter::ExportStats               // 多处引用
```

CsvExporter 关键静态方法签名（**严禁修改**，拆分到 writer.rs 时逐字搬移）：

```rust
pub(crate) fn write_record_preparsed(
    itoa_buf: &mut itoa::Buffer,
    line_buf: &mut Vec<u8>,
    sqllog: &Sqllog<'_>,
    meta: &MetaParts<'_>,
    pm: &PerformanceMetrics<'_>,
    normalized: Option<&str>,
    field_mask: FieldMask,
    ordered_indices: &[usize],
    include_performance_metrics: bool,
) -> Result<()>
```

SqliteExporter 关键热路径函数签名（**严禁修改**）：

```rust
fn do_insert_preparsed(
    stmt: &mut rusqlite::CachedStatement<'_>,
    sqllog: &Sqllog<'_>,
    meta: &MetaParts<'_>,
    pm: &PerformanceMetrics<'_>,
    normalized_sql: Option<&str>,
    field_mask: crate::pipeline::FieldMask,
    ordered_indices: &[usize],
) -> std::result::Result<(), rusqlite::Error>
```

```rust
fn build_insert_sql(table_name: &str, ordered_indices: &[usize]) -> String
fn build_create_sql(table_name: &str, ordered_indices: &[usize]) -> String
```

新建 projection.rs 内容（来自 D-05 + PATTERNS.md）：

```rust
// src/exporter/projection.rs
use crate::pipeline::FIELD_NAMES;

pub(crate) fn projected_field_names(ordered_indices: &[usize]) -> Vec<&'static str> {
    ordered_indices.iter().map(|&i| FIELD_NAMES[i]).collect()
}
```

DryRunExporter 整合目标（D-08 + PATTERNS.md "DryRunExporter 整合模式"）：

```rust
// Before
pub enum ExporterKind {
    Csv(CsvExporter),
    Sqlite(SqliteExporter),
    DryRun(DryRunExporter),
}
pub struct DryRunExporter { stats: ExportStats }
impl Exporter for DryRunExporter { ... }

// After
pub(crate) enum ExporterKind {
    Csv(CsvExporter),
    Sqlite(SqliteExporter),
    DryRun { stats: ExportStats },  // struct variant
}
// 删除独立 DryRunExporter struct 和 impl Exporter for DryRunExporter
// ExporterKind 的 match arm 直接处理 DryRun { stats } 各方法
```

ExporterManager::dry_run() 公开签名保持不变：

```rust
pub(crate) fn dry_run() -> Self { ... }   // 内部构造 ExporterKind::DryRun { stats: ExportStats::default() }
```
</interfaces>

<visibility_rules>
按 D-10 + D-11 + integration test 实际引用情况：

- `pub trait Exporter` → `pub(crate) trait Exporter`（无跨 crate 实现者，lib.rs `pub use exporter::Exporter` 同步改 `pub(crate) use`）
- `pub enum ExporterKind` → `pub(crate) enum ExporterKind`
- `pub struct ExportStats` → `pub(crate) struct ExportStats`，所有字段同步
- `pub struct CsvExporter` → `pub(crate) struct CsvExporter`（lib.rs `pub use ... CsvExporter` 同步收紧；grep 验证 integration test 无直接引用）
- `pub struct SqliteExporter` → `pub(crate) struct SqliteExporter`（同上）
- `pub struct ExporterManager` → `pub(crate)`，所有方法同步
- `CsvExporter::new / from_config` → `pub(crate)`
- `SqliteExporter::new / from_config` → `pub(crate)`
- `ExportStats::new / record_success / total / failed` 等方法 → `pub(crate)`
- DryRunExporter（删除前）的所有 pub 项一并删除

收紧 lib.rs 中 `pub use exporter::{CsvExporter, Exporter, SqliteExporter};`：
- 先 grep 确认 `tests/integration.rs` 不直接引用这 3 个类型（命令：`grep -E "(CsvExporter|SqliteExporter|::Exporter\b)" tests/integration.rs`）
- 如确认无引用 → 改为 `pub(crate) use exporter::{CsvExporter, Exporter, SqliteExporter};`
- 如有引用 → 该类型保持 pub，其余收紧
</visibility_rules>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 新建 projection.rs 并拆分 sqlite.rs（含 sql_builder + write + conn_ref 修复）</name>
  <files>
    src/exporter/projection.rs (new),
    src/exporter/sqlite.rs (delete),
    src/exporter/sqlite/mod.rs (new),
    src/exporter/sqlite/sql_builder.rs (new),
    src/exporter/sqlite/write.rs (new),
    src/exporter/mod.rs
  </files>
  <read_first>
    src/exporter/sqlite.rs,
    src/exporter/mod.rs,
    src/pipeline/mod.rs,
    .planning/phases/19-code-refactor/19-PATTERNS.md,
    .planning/codebase/CONCERNS.md
  </read_first>
  <action>
    1) 新建 `src/exporter/projection.rs`：

       ```rust
       use crate::pipeline::FIELD_NAMES;

       pub(crate) fn projected_field_names(ordered_indices: &[usize]) -> Vec<&'static str> {
           ordered_indices.iter().map(|&i| FIELD_NAMES[i]).collect()
       }
       ```

       预计 ~10 行，含可选的 `#[cfg(test)] mod tests` 测一两个基本映射用例。

    2) 在 src/exporter/mod.rs 中添加 `pub(crate) mod projection;` 声明。

    3) 删除 `src/exporter/sqlite.rs`（其内容将搬到子模块）。

    4) 新建 `src/exporter/sqlite/mod.rs`：从原 sqlite.rs 第 1-128 行迁移：
       - `use` 区
       - `pub(crate) struct SqliteExporter` 定义（原 pub → pub(crate)；`pub(super)` 字段保持 `pub(super)`，但因为已是子模块路径 `exporter/sqlite/mod.rs`，`pub(super)` 实际指向 `exporter/`，与原意保持一致）
       - `initialize_pragmas` 私有函数
       - `Debug impl for SqliteExporter`
       - `impl SqliteExporter { pub(crate) fn new, pub(crate) fn from_config, fn db_err, fn conn_ref }`
       - **新增 `conn_ref` 辅助函数**（修复 CONCERNS.md tech debt，CONTEXT.md Claude's Discretion 允许）：
         ```rust
         fn conn_ref(&self) -> crate::error::Result<&rusqlite::Connection> {
             self.conn.as_ref().ok_or_else(|| Self::db_err("not initialized"))
         }
         ```
         然后将本文件内 `self.conn.as_ref().unwrap()` 替换为 `self.conn_ref()?`（行 231/236/243 等）。**只替换 `unwrap()` 模式，不修改任何业务逻辑分支**。
       - `impl Exporter for SqliteExporter`（原方法体保留，可见性 trait 方法不变；调用 do_insert_preparsed/build_insert_sql 等改为 `use super::write::*; use super::sql_builder::*;`）
       - `#[cfg(test)] mod tests`（迁移测 SqliteExporter::new / from_config / Debug / initialize_pragmas 等的测试）

       注意：`pub(super) ordered_indices: Vec<usize>` 在原文件第 21 行——拆分后 mod.rs 内的 `pub(super)` 等价于"对 src/exporter/ 内部可见"，仍允许 mod.rs::SqliteExporter 与 src/exporter/projection.rs 跨子模块共享视图。

    5) 新建 `src/exporter/sqlite/sql_builder.rs`：从原 sqlite.rs 第 71-115 行迁移 build_insert_sql / build_create_sql：
       - `use super::super::projection::projected_field_names;`
       - `use crate::pipeline::FIELD_NAMES;`
       - 函数 `fn build_insert_sql(table_name: &str, ordered_indices: &[usize]) -> String`
         - 全量路径（`ordered_indices.len() == FIELD_NAMES.len()`）保持原 fast-path（不调用 projection）
         - 投影路径：原内联 cols 构造 `ordered_indices.iter().map(|&i| FIELD_NAMES[i]).collect::<Vec<_>>().join(", ")` **改为** `projected_field_names(ordered_indices).join(", ")`
       - 函数 `fn build_create_sql(table_name: &str, ordered_indices: &[usize]) -> String`
         - 此函数构造 `"field_name TYPE"` 对——**不能直接用 projected_field_names**，因为还要拼接类型；但 cols 名部分仍可用 `projected_field_names(ordered_indices)`（D-06：共用层仅做 indices→名称映射）
         - 具体改造：`ordered_indices.iter().zip(projected_field_names(ordered_indices).iter()).map(|(&i, &name)| format!("{} {}", name, COL_TYPES[i])).collect::<Vec<_>>().join(", ")`
         - 若上面写法不简洁，保留原 ordered_indices.iter() 迭代但 cols 数组用 `projected_field_names` 预生成

    6) 新建 `src/exporter/sqlite/write.rs`：从原 sqlite.rs 第 137-490 行迁移：
       - `do_insert_preparsed`（签名严格不变，见 interfaces 块）
       - `write_template_stats`（签名按 Phase 18 已改为 `(csv_path: Option<&str>, sqlite_table: Option<&str>)`，逐字搬移）
       - 其他 write_batch / batch_commit_if_needed 辅助函数
       - 私有 `fn db_err`（若与 mod.rs 中重复，留 mod.rs 一份，write.rs 通过 `use super::db_err` 引入；或保持各自一份私有，按 grep 实际情况决定）
       - `#[cfg(test)] mod tests`（迁移 do_insert_preparsed / write_template_stats 等测试）

    7) 行数检查：
       - sqlite/mod.rs ≤ 300（含 ~200 行业务 + 50 行测试）
       - sqlite/sql_builder.rs ≤ 200
       - sqlite/write.rs：原 do_insert + 大量测试，可能接近 300。若超 300，把 do_insert_preparsed 测试单独剪到 sqlite/mod.rs 末尾，或新建 sqlite/write/tests.rs（仅当 wc -l 超 300 时）

    8) `cargo build && cargo test --lib`。
  </action>
  <verify>
    <automated>cargo build 2>&amp;1 | grep error | head -10; cargo test --lib 2>&amp;1 | tail -10; wc -l src/exporter/sqlite/*.rs src/exporter/projection.rs; ! test -f src/exporter/sqlite.rs &amp;&amp; echo "SQLITE_FILE_REMOVED"</automated>
  </verify>
  <acceptance_criteria>
    - src/exporter/sqlite.rs 不存在
    - src/exporter/projection.rs 存在且包含 `fn projected_field_names`
    - src/exporter/sqlite/mod.rs 存在，包含 `pub(crate) struct SqliteExporter` 与 `fn conn_ref`
    - src/exporter/sqlite/sql_builder.rs 存在，包含 `fn build_insert_sql` 和 `fn build_create_sql`
    - src/exporter/sqlite/sql_builder.rs 包含 `projection::projected_field_names` 调用（grep 命中）
    - src/exporter/sqlite/write.rs 存在，包含 `fn do_insert_preparsed`
    - `grep -nE "self\.conn\.as_ref\(\)\.unwrap\(\)" src/exporter/sqlite/` 应为空（unwrap 已被 conn_ref 替代）
    - `wc -l src/exporter/sqlite/*.rs` 每个文件 ≤ 300
    - `cargo build` 退出码 0，`cargo test --lib` 通过
  </acceptance_criteria>
  <done>sqlite.rs 完整拆分为 3 个文件 + projection.rs，conn_ref tech debt 修复，编译测试通过。</done>
</task>

<task type="auto">
  <name>Task 2: 拆分 csv.rs（mod.rs + writer.rs + companion.rs）</name>
  <files>
    src/exporter/csv.rs (delete),
    src/exporter/csv/mod.rs (new),
    src/exporter/csv/writer.rs (new),
    src/exporter/csv/companion.rs (new),
    src/exporter/mod.rs
  </files>
  <read_first>
    src/exporter/csv.rs,
    src/exporter/mod.rs,
    src/exporter/sqlite/mod.rs,
    src/exporter/projection.rs,
    .planning/phases/19-code-refactor/19-PATTERNS.md
  </read_first>
  <action>
    1) 删除 `src/exporter/csv.rs`。

    2) 新建 `src/exporter/csv/mod.rs`：从原 csv.rs 第 95-152 行 + Exporter impl 区域迁移：
       - `use` 区（保留 std::path::Path, std::io::BufWriter, std::fs::File 等）
       - 顶部 `mod writer; mod companion;` 子模块声明
       - `pub(crate) use companion::write_companion_rows;`（companion.rs 中的导出，因为 CSV 配套 stats 输出需被 mod.rs 内调用，且也可能被父 exporter/mod.rs 用到）
       - `pub(crate) struct CsvExporter { ... }` 定义（原 pub → pub(crate)；字段保留原可见性 `pub(crate)` 或 `pub(super)` 与原文件一致；Phase 14 后 csv.rs 中 `ordered_indices: pub(crate)` 保持）
       - `impl CsvExporter`:
         - `pub(crate) fn new(path: impl AsRef<Path>) -> Self`
         - `pub(crate) fn from_config(config: &config::CsvExporterConfig) -> Self`
       - `impl Exporter for CsvExporter`（delegate 到 writer::write_record_preparsed 等；`use super::writer::*;`）
       - `#[cfg(test)] mod tests`（迁移测 CsvExporter::new / from_config 的测试）

    3) 新建 `src/exporter/csv/writer.rs`：从原 csv.rs 第 14-22 行 + 第 154-418 行迁移：
       - `use super::CsvExporter;` 或如不需要 self 则不引入
       - `use crate::error::{Error, ExportError, Result};`
       - `use crate::pipeline::FieldMask;`
       - `use dm_database_parser_sqllog::{MetaParts, PerformanceMetrics, Sqllog};`
       - `use std::io::Write; use std::path::Path;`
       - `#[inline] fn write_csv_escaped(buf: &mut Vec<u8>, bytes: &[u8])`（私有）
       - 静态方法 `pub(crate) fn write_record_preparsed(...)`（**签名严格保持，逐字搬移**，见 interfaces 块）
       - 因为是 inherent 静态方法而非 `impl CsvExporter` 上的方法 → 必须用 `impl CsvExporter { pub(crate) fn write_record_preparsed(...) ... }` 包裹放在 writer.rs（Rust 允许同一 struct 有多个 impl 块在不同文件，前提是同 crate 同模块层级）。

       关键模式：
       ```rust
       // src/exporter/csv/writer.rs
       use super::CsvExporter;

       impl CsvExporter {
           #[inline]
           pub(crate) fn write_record_preparsed(
               itoa_buf: &mut itoa::Buffer,
               line_buf: &mut Vec<u8>,
               sqllog: &Sqllog<'_>,
               // ... 完整 9 参数
           ) -> Result<()> {
               // 原函数体逐字搬移
           }
       }

       #[inline]
       fn write_csv_escaped(buf: &mut Vec<u8>, bytes: &[u8]) { ... }
       ```

    4) 新建 `src/exporter/csv/companion.rs`：从原 csv.rs 第 25-93 行迁移：
       - `use crate::error::{Error, ExportError, Result};`
       - `use std::fs::File; use std::io::{BufWriter, Write}; use std::path::Path;`
       - `use crate::pipeline::TemplateStats;`（按实际类型导入）
       - `fn ensure_parent_dir(path: &Path) -> std::io::Result<()>`（私有，若仅 companion.rs 使用）
       - `#[inline] fn io_err(path: &Path, reason: String) -> Error`（私有）
       - `fn format_companion_row(line_buf: &mut Vec<u8>, itoa_buf: &mut itoa::Buffer, s: &TemplateStats)`（私有）
       - `pub(crate) fn write_companion_rows(path: &Path, stats: &[TemplateStats]) -> Result<()>`（保持原签名）
       - `#[cfg(test)] mod tests`

    5) **避免热路径回归**（D-06 + Pitfall #1）：
       - writer.rs::write_record_preparsed **绝不调用** projection::projected_field_names（每条记录调用会 Vec 分配，热路径性能回归）
       - writer.rs 仍按原代码 match ordered_indices 索引逐字段写入
       - projection 仅用于 sqlite/sql_builder.rs 初始化路径

    6) 行数检查：
       - csv/mod.rs ≤ 250
       - csv/writer.rs ≤ 300（含 ~200 业务 + 50 测试）
       - csv/companion.rs ≤ 200

    7) `cargo build && cargo test --lib`。
  </action>
  <verify>
    <automated>cargo build 2>&amp;1 | grep error | head -10; cargo test --lib 2>&amp;1 | tail -10; wc -l src/exporter/csv/*.rs; ! test -f src/exporter/csv.rs &amp;&amp; echo "CSV_FILE_REMOVED"</automated>
  </verify>
  <acceptance_criteria>
    - src/exporter/csv.rs 不存在
    - src/exporter/csv/mod.rs 存在，包含 `pub(crate) struct CsvExporter` 与 `impl Exporter for CsvExporter`
    - src/exporter/csv/writer.rs 存在，包含 `pub(crate) fn write_record_preparsed`（在 `impl CsvExporter` 块内）
    - src/exporter/csv/companion.rs 存在，包含 `pub(crate) fn write_companion_rows`
    - `grep -E "projection::projected_field_names" src/exporter/csv/writer.rs` 应为空（热路径不调用）
    - `wc -l src/exporter/csv/*.rs` 每个文件 ≤ 300
    - `cargo build` 退出码 0，`cargo test --lib` 通过
  </acceptance_criteria>
  <done>csv.rs 完整拆分为 3 个文件，热路径性能保持，编译测试通过。</done>
</task>

<task type="auto">
  <name>Task 3: 整合 DryRunExporter 进 ExporterKind 并清理 Exporter trait 冗余 match（D-07/D-08）</name>
  <files>
    src/exporter/mod.rs,
    src/lib.rs
  </files>
  <read_first>
    src/exporter/mod.rs,
    src/exporter/csv/mod.rs,
    src/exporter/sqlite/mod.rs,
    src/lib.rs,
    tests/integration.rs,
    .planning/phases/19-code-refactor/19-PATTERNS.md
  </read_first>
  <action>
    1) **D-08：DryRunExporter 整合**

       1a) 在 src/exporter/mod.rs 中：
       - 将 `enum ExporterKind` 的 `DryRun(DryRunExporter)` variant 改为 `DryRun { stats: ExportStats }`（struct variant）
       - 删除 `pub struct DryRunExporter { stats: ExportStats }` 定义（行 174）
       - 删除 `impl Exporter for DryRunExporter { ... }` 块（行 178-222）
       - 修改 `ExporterManager::dry_run()`：从 `exporter: ExporterKind::DryRun(DryRunExporter::default())` 改为 `exporter: ExporterKind::DryRun { stats: ExportStats::default() }`

       1b) 在 `impl Exporter for ExporterKind` 或 ExporterKind 自身的方法中，处理 DryRun variant：
       - 原来通过 `Self::DryRun(e) => e.export_one_preparsed(...)` 委托给 DryRunExporter
       - 整合后改为内联：`Self::DryRun { stats } => { stats.exported += 1; Ok(()) }` 等
       - 具体每个方法（export, export_one_normalized, export_one_preparsed, flush, finalize, stats 等）的 DryRun 分支按原 DryRunExporter impl 中的逻辑内联——大多数是 no-op 或仅累加 stats 字段

       1c) 更新 src/exporter/mod.rs 中所有测试（行 521-735 区域）：
       - `let mut e = DryRunExporter::default();` → `let mut e = ExporterKind::DryRun { stats: ExportStats::default() };`
       - 涉及方法调用如 `e.export_one_preparsed(...)` 改为通过 ExporterKind 调用（同名方法存在）
       - `DryRunExporter { stats }` 字面构造 → `ExporterKind::DryRun { stats }`
       - 注意：原测试若直接访问 `e.stats` 字段，整合后需通过 ExporterKind 的 `stats()` 访问器或 match 解构。如果无访问器，新增 `pub(crate) fn stats(&self) -> &ExportStats { match self { Self::Csv(e) => e.stats(), ... Self::DryRun { stats } => stats } }`

    2) **D-07：清理 export_one_normalized / export_one_preparsed / ExporterKind 默认实现冗余 match**

       2a) 检查 `impl Exporter for ExporterKind` 或 ExporterKind inherent 方法中的 match 分支。识别"冗余"模式：
       - 所有 arm 都委托同名方法 → 可保留（这是必要的 dispatch）
       - 某 arm 与默认实现重复 → 删除该 arm，依赖 trait 默认实现
       - 某 arm 内嵌逻辑能用更简单的转发实现 → 简化

       2b) 检查 trait 定义（src/exporter/mod.rs 第 12-65 行）：
       - 是否有方法的默认实现与所有实现者重复 → 简化默认实现
       - 是否有方法仅 1-2 实现者需要特化 → 评估默认实现是否合适

       2c) 具体清理标准（保守原则）：
       - **仅在能用更简单表达替代时清理**，不为清理而清理
       - 若 match 三个 arm 都委托同名 trait 方法 → 保留（这是 enum dispatch 的本质）
       - 若某 arm 是 no-op 且 trait 有默认 no-op 实现 → 可删除该 arm

       2d) 由于 D-07 表述较抽象，本 Task 至少完成以下具体动作：
       - 删除 DryRunExporter 后，原 `Self::DryRun(e) => e.method(...)` 简化为 `Self::DryRun { .. } => Ok(())` 或 `Self::DryRun { stats } => { /* 内联 */ Ok(()) }`
       - 检查每个 trait 方法的 ExporterKind dispatch，确认无形参丢弃或多余转换

    3) **lib.rs pub use 收紧**：
       - 检查 `grep -E "(CsvExporter|SqliteExporter|::Exporter\b)" tests/integration.rs`
       - 若 integration test 无引用 → 改 `pub use exporter::{CsvExporter, Exporter, SqliteExporter};` 为 `pub(crate) use exporter::{CsvExporter, Exporter, SqliteExporter};`
       - 若有引用 → 该类型保持 pub，其他收紧

    4) **mod.rs 行数评估**（Open Question #2）：
       - 删除 DryRunExporter 后 `wc -l src/exporter/mod.rs`
       - 原 756 行 - DryRunExporter (~50 行) - 部分测试调整 ≈ 700 行
       - 若仍 > 600 → 进一步拆分 ExporterManager 或工具函数到 `src/exporter/manager.rs`
       - 目标 ≤ 600（CONTEXT.md 提到"评估是否拆分"，不是硬约束 300，因为 trait + enum + manager 是一个紧密耦合的单元）

       具体拆分策略（若超 600 行）：
       - 将 `ExportStats` 移到 `src/exporter/stats.rs`
       - 将 `ExporterManager` 移到 `src/exporter/manager.rs`
       - mod.rs 仅保留 trait + ExporterKind + 子模块声明 + re-export

    5) `cargo build && cargo test`（含 integration test）。

    6) `cargo clippy --all-targets -- -D warnings`。
  </action>
  <verify>
    <automated>cargo clippy --all-targets -- -D warnings 2>&amp;1 | tail -10; cargo test 2>&amp;1 | tail -10; grep -c "DryRunExporter" src/exporter/mod.rs; wc -l src/exporter/mod.rs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "DryRunExporter" src/exporter/mod.rs` 输出 0（DryRunExporter struct 与 impl 已删除）
    - src/exporter/mod.rs 包含 `DryRun { stats: ExportStats }` 字样（struct variant 形式）
    - src/exporter/mod.rs 包含 `ExporterKind::DryRun { stats: ExportStats::default() }` 在 ExporterManager::dry_run() 内
    - tests/integration.rs 编译通过（`cargo test` 不报 `DryRunExporter` 相关错误）
    - `cargo test` 全部通过
    - `cargo clippy --all-targets -- -D warnings` 退出码 0
    - `wc -l src/exporter/mod.rs` ≤ 600（若超过，需进一步拆分如 stats.rs / manager.rs）
    - src/lib.rs 中 `pub use exporter::{CsvExporter, Exporter, SqliteExporter}` 已根据 integration test 实际引用情况评估并收紧（若无引用则改 pub(crate) use；若有则保留并在注释说明）
  </acceptance_criteria>
  <done>DryRunExporter 整合完成，trait 冗余 match 清理，mod.rs 精简达标，所有测试通过。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Exporter trait → 文件系统 | CSV/SQLite exporter 写入用户提供的文件路径；本 Plan 不改变写入语义 |
| ExporterKind dispatch | enum variant 整合后所有路径必须仍正确 dispatch 到对应实现 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-03-01 | Tampering | DryRunExporter 整合期间分支遗漏 | mitigate | Task 3 内联所有 DryRun 方法时逐方法比对原 impl Exporter for DryRunExporter，确保 stats 累加语义一致；现有 exporter/mod.rs 测试是回归网 |
| T-19-03-02 | Denial of Service | csv/writer.rs 热路径引入 Vec 分配 | mitigate | Action 明确禁止在 write_record_preparsed 中调用 projection::projected_field_names；Task 2 acceptance criteria 含 grep 验证 |
| T-19-03-03 | Information Disclosure | pub → pub(crate) 收紧 | accept | 收紧降低暴露面，无新风险 |
| T-19-03-04 | Tampering | conn_ref 替换 unwrap 时遗漏分支 | mitigate | 仅替换 `self.conn.as_ref().unwrap()` 这一精确模式（grep 定位），不修改业务逻辑分支；现有 sqlite 测试覆盖错误路径 |
| T-19-03-SC | Tampering | npm/pip/cargo installs | N/A | 本 Plan 不引入新依赖，跳过包合法性门禁 |
</threat_model>

<verification>
- `cargo build` 通过
- `cargo test` 全部通过（含 integration test）
- `cargo clippy --all-targets -- -D warnings` 零警告
- `wc -l src/exporter/projection.rs src/exporter/csv/*.rs src/exporter/sqlite/*.rs` 每文件 ≤ 300
- `wc -l src/exporter/mod.rs` ≤ 600
- `grep -c DryRunExporter src/` 整体应大幅下降（理想为 0，除非 mod.rs 注释保留历史说明）
- `grep -nE "self\.conn\.as_ref\(\)\.unwrap\(\)" src/exporter/sqlite/` 为空
- `grep -nE "projection::projected_field_names" src/exporter/csv/writer.rs` 为空（热路径未调用）
- `grep -nE "projection::projected_field_names" src/exporter/sqlite/sql_builder.rs` 至少 1 次命中（初始化路径已调用）
- 性能基准：若项目有 criterion bench，运行 `cargo bench --bench <name>`，确认 throughput 与重构前相比偏差 ≤ 5%
</verification>

<success_criteria>
- src/exporter/csv.rs 和 src/exporter/sqlite.rs 已被目录子模块替代
- src/exporter/projection.rs 创建并被 sqlite/sql_builder.rs 调用
- DryRunExporter struct 完全删除，整合为 ExporterKind::DryRun struct variant
- Exporter trait 默认实现冗余 match 已清理
- 所有 exporter 子模块 ≤ 300 行（mod.rs ≤ 600）
- 整个 exporter 子系统可见性按 D-10 收紧
- REFACTOR-01/02/03/04 共四条需求在本 Plan 部分或完整覆盖
</success_criteria>

<output>
Create `.planning/phases/19-code-refactor/19-03-SUMMARY.md` when done
</output>
