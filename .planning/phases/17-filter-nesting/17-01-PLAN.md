---
phase: 17-filter-nesting
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/filters.rs
autonomous: true
requirements:
  - CONFIG-01
  - CONFIG-02
  - CONFIG-05
tags:
  - serde
  - toml
  - config-refactor
  - backward-compat

must_haves:
  truths:
    - "新格式 [features.filter.include] 子表（users/ips/sessions/threads/statements/apps/tags/start_ts/end_ts/trxids）可被 toml::from_str 正确反序列化为 IncludeFilters"
    - "新格式 [features.filter.exclude] 子表（users/ips/sessions/threads/statements/apps/tags）可被反序列化为 ExcludeFilters"
    - "旧格式扁平字段（usernames / exclude_usernames / client_ips / sess_ids / thrd_ids / statements / appnames / tags / exclude_* / start_ts / end_ts / trxids）仍能 parse，并自动映射到 include/exclude 子结构"
    - "SqlFilters 接受新字段名 includes / excludes，旧字段名 include_patterns / exclude_patterns 通过 alias 继续可用"
    - "FiltersFeature.has_filters / has_transaction_filters / merge_found_trxids 在新结构下保持原有语义"
    - "CompiledMetaFilters::try_from_include_exclude 入参为 (&IncludeFilters, &ExcludeFilters)，返回字段与旧 try_from_meta 等价"
  artifacts:
    - path: "src/features/filters.rs"
      provides: "新 IncludeFilters / ExcludeFilters / RawFiltersFeature struct + 手写 FiltersFeature::Deserialize impl + 重命名后的 CompiledMetaFilters::try_from_include_exclude + SqlFilters.includes/excludes 字段"
      contains: "pub struct IncludeFilters"
  key_links:
    - from: "src/features/filters.rs::FiltersFeature::deserialize"
      to: "src/features/filters.rs::RawFiltersFeature"
      via: "RawFiltersFeature::deserialize(d)? + From<RawFiltersFeature> for FiltersFeature"
      pattern: "impl<'de> Deserialize<'de> for FiltersFeature"
    - from: "src/features/filters.rs::CompiledMetaFilters::try_from_include_exclude"
      to: "IncludeFilters + ExcludeFilters"
      via: "compile_patterns calls with 'features.filters.include.X' / 'features.filters.exclude.X' 字段路径字符串"
      pattern: "try_from_include_exclude"
---

<objective>
将 `FiltersFeature` 中扁平的 `MetaFilters`（通过 `#[serde(flatten)]` 合并）拆分为显式的 `include` / `exclude` 嵌套子结构，同时通过手写 `Deserialize` impl + 中间 `RawFiltersFeature` 结构体保持旧扁平格式向后兼容。

Purpose: 让用户用 `[features.filter.include]` / `[features.filter.exclude]` 子表表达过滤意图，配置层次更清晰；旧 config 文件无需用户改动即可继续 parse。
Output: `src/features/filters.rs` 内含新结构体、手写 Deserialize、重命名后的 Compiled 入口、SqlFilters 字段重命名，所有现有 unit test 适配新字段路径。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-filter-nesting/17-CONTEXT.md
@.planning/phases/17-filter-nesting/17-RESEARCH.md
@.planning/phases/17-filter-nesting/17-PATTERNS.md
@src/features/filters.rs

<interfaces>
当前 (重构前) 关键类型和方法签名（来自 src/features/filters.rs）：

```
pub struct FiltersFeature {
    pub enable: bool,
    #[serde(flatten)] pub meta: MetaFilters,    // 重构后移除 flatten，替换为 include + exclude
    #[serde(default)] pub indicators: IndicatorFilters,
    #[serde(default)] pub sql: SqlFilters,
    #[serde(default)] pub record_sql: SqlFilters,
}

pub struct MetaFilters {
    pub start_ts, end_ts: Option<String>,
    pub sess_ids, thrd_ids, usernames, statements, appnames, client_ips, tags: Option<Vec<String>>,
    pub trxids: Option<TrxidSet>,   // 自定义 deserialize_with = "vec_to_hashset"
    pub exclude_usernames, exclude_client_ips, exclude_sess_ids,
        exclude_thrd_ids, exclude_statements, exclude_appnames, exclude_tags: Option<Vec<String>>,
}

pub struct SqlFilters {
    pub include_patterns: Option<Vec<String>>,   // 重构后：includes + alias = "include_patterns"
    pub exclude_patterns: Option<Vec<String>>,   // 重构后：excludes + alias = "exclude_patterns"
}

impl FiltersFeature {
    pub fn has_filters(&self) -> bool { ... 引用 self.meta.* }
    pub fn has_transaction_filters(&self) -> bool { ... 不引用 meta }
    pub fn merge_found_trxids(&mut self, trxids: Vec<CompactString>) { self.meta.trxids... }
}

impl MetaFilters {
    pub fn has_filters(&self) -> bool { ... }   // 重构后拆为 IncludeFilters::has_filters + ExcludeFilters::has_filters
}

impl CompiledMetaFilters {
    pub fn try_from_meta(meta: &MetaFilters) -> Result<Self> { ... }
    // 重构后：pub fn try_from_include_exclude(include: &IncludeFilters, exclude: &ExcludeFilters) -> Result<Self>
}

helper fns（保留不动，在同 module 内）:
fn vec_to_hashset (Option<Vec<String>> → Option<TrxidSet>)
fn vec_to_i64_hashset (Option<Vec<i64>> → Option<AHashSet<i64>>)
fn compile_patterns (field: &str, patterns: Option<&[String]>) -> Result<Option<Vec<Regex>>>
fn match_any_regex (patterns: Option<&[Regex]>, val: &str) -> bool
```

重构后目标接口（执行者必须严格按此实现）：

```
pub struct FiltersFeature {
    pub enable: bool,
    pub include: IncludeFilters,
    pub exclude: ExcludeFilters,
    pub indicators: IndicatorFilters,
    pub sql: SqlFilters,
    pub record_sql: SqlFilters,
}
// 手写 impl<'de> Deserialize<'de> for FiltersFeature
// 通过私有 RawFiltersFeature（同 module）二段反序列化，兼容旧扁平字段

#[derive(Debug, Deserialize, Clone, Default)]
pub struct IncludeFilters {
    pub users, ips, sessions, threads, statements, apps, tags: Option<Vec<String>>,
    pub start_ts, end_ts: Option<String>,
    #[serde(default, deserialize_with = "vec_to_hashset")]
    pub trxids: Option<TrxidSet>,
}
impl IncludeFilters { pub fn has_filters(&self) -> bool { ... } }

#[derive(Debug, Deserialize, Clone, Default)]
pub struct ExcludeFilters {
    pub users, ips, sessions, threads, statements, apps, tags: Option<Vec<String>>,
}
impl ExcludeFilters { pub fn has_filters(&self) -> bool { ... } }

#[derive(Debug, Deserialize, Clone, Default)]
pub struct SqlFilters {
    #[serde(default, alias = "include_patterns")] pub includes: Option<Vec<String>>,
    #[serde(default, alias = "exclude_patterns")] pub excludes: Option<Vec<String>>,
}

impl CompiledMetaFilters {
    pub fn try_from_include_exclude(
        include: &IncludeFilters,
        exclude: &ExcludeFilters,
    ) -> crate::error::Result<Self> { ... }
}
```

注意：`MetaFilters` 整个 struct 在 Phase 17 完成后**不再公开使用**。可选两种处理：
(a) 删除 `pub struct MetaFilters` 定义（推荐，避免歧义），或
(b) 保留但标 `#[deprecated]` + 仅作为内部 alias。
本计划选 (a)——完全删除 `MetaFilters` 公开定义。`RawFiltersFeature` 是私有 helper，包含全部旧字段（带 `#[serde(default)]`）和新子表字段（`Option<IncludeFilters>` / `Option<ExcludeFilters>`）。
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — 新增 parse 测试 (RED)</name>
  <files>src/features/filters.rs</files>
  <read_first>
    - src/features/filters.rs (当前 FiltersFeature / MetaFilters / SqlFilters 定义、`#[cfg(test)] mod tests` 全部测试)
    - .planning/phases/17-filter-nesting/17-RESEARCH.md (Validation Architecture → Phase Requirements → Test Map + Wave 0 Gaps 章节)
    - .planning/phases/17-filter-nesting/17-CONTEXT.md (`<specifics>` 章节中新旧格式 TOML 示例)
  </read_first>
  <behavior>
    - test_new_nested_format_include: 新格式 TOML `[features.filter.include]` 含 users/ips/sessions/threads/statements/apps/tags/start_ts/end_ts/trxids，解析后 `filters.include.users == Some(vec!["user1".into()])`，`filters.include.ips == Some(vec!["192.168.1.1".into()])`，`filters.include.trxids` 是 Some 且 len ≥ 1。
    - test_new_nested_format_exclude: 新格式 `[features.filter.exclude]` 含 users/ips/sessions/threads/statements/apps/tags，解析后 `filters.exclude.users == Some(vec!["admin".into()])` 等。
    - test_backward_compat_flat_format: 旧扁平 TOML 同时包含 `usernames = ["user1"]`、`exclude_usernames = ["admin"]`、`client_ips = [...]`、`exclude_client_ips = [...]`、`sess_ids = [...]`、`thrd_ids = [...]`、`exclude_sess_ids = [...]`、`exclude_thrd_ids = [...]`、`statements = [...]`、`exclude_statements = [...]`、`appnames = [...]`、`exclude_appnames = [...]`、`tags = [...]`、`exclude_tags = [...]`、`start_ts`、`end_ts`、`trxids = ["123"]`，解析后字段被映射到 `filters.include.users` / `filters.exclude.users` / `filters.include.ips` / `filters.exclude.ips` 等，所有 include 类字段进入 `filters.include`，所有 exclude_* 字段进入 `filters.exclude`，`trxids` 进入 `filters.include.trxids` 且长度为 1。
    - test_sql_filters_alias_backward_compat: 旧 `[features.filter.sql]` 含 `include_patterns = ["SELECT"]` / `exclude_patterns = ["DROP"]`，解析后 `filters.sql.includes == Some(vec!["SELECT".into()])` 且 `filters.sql.excludes == Some(vec!["DROP".into()])`。
    - test_sql_filters_new_field_names: 新格式 `[features.filter.sql]` 含 `includes = [...]` / `excludes = [...]`，解析后字段值正确。
    - test_filters_toml_deserialization_with_trxids_and_exec_ids (现存测试更新): 第 1253-1273 行原测试中 `filters.meta.trxids` 改为 `filters.include.trxids`；其他断言保持不变。
  </behavior>
  <action>
    在 `src/features/filters.rs` 末尾的 `mod tests` 中新增 5 个 `#[test]` 函数（`test_new_nested_format_include`、`test_new_nested_format_exclude`、`test_backward_compat_flat_format`、`test_sql_filters_alias_backward_compat`、`test_sql_filters_new_field_names`），覆盖 `<behavior>` 中列出的全部断言。
    构造 toml 字符串均使用 `r#"..."#` 多行 raw string；解析入口统一为 `let cfg: crate::config::Config = toml::from_str(toml).unwrap(); let filters = cfg.features.filters.unwrap();`（参照现有 `test_filters_toml_deserialization_with_trxids_and_exec_ids` 模式）。
    断言对象字段路径需引用尚未实现的 `filters.include.*` / `filters.exclude.*` / `filters.sql.includes` / `filters.sql.excludes` —— 此时本任务结束后这些测试**必须编译失败或运行失败**（RED 阶段）。
    现有的 `test_filters_toml_deserialization_with_trxids_and_exec_ids` 中两行 `filters.meta.trxids` 改为 `filters.include.trxids`（其余两行 `filters.indicators.exec_ids` 不动）。
    不要在本任务修改任何 src struct 定义；本任务只追加测试 + 微调 1 处现有 trxids 访问路径。
    使用 `git commit -m "test(17-01): add failing parse tests for nested filters (RED)"` 在本任务结束后提交（Co-Authored-By 行保留）。
  </action>
  <verify>
    <automated>cargo test --lib features::filters::tests::test_new_nested_format_include features::filters::tests::test_new_nested_format_exclude features::filters::tests::test_backward_compat_flat_format features::filters::tests::test_sql_filters_alias_backward_compat features::filters::tests::test_sql_filters_new_field_names 2>&1 | grep -E "(FAILED|error\[E)" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build` 失败或 `cargo test --lib features::filters::tests` 中新增 5 个测试 FAILED（RED 状态确认）。失败原因必须包含 `no field 'include'` 或 `no field 'exclude'` 或 `no field 'includes'` 等编译/字段错误，而非 panic in test logic。
    - `src/features/filters.rs` 包含 `fn test_new_nested_format_include`、`fn test_new_nested_format_exclude`、`fn test_backward_compat_flat_format`、`fn test_sql_filters_alias_backward_compat`、`fn test_sql_filters_new_field_names` 五个函数定义（grep `^\s*fn test_(new_nested_format_include|new_nested_format_exclude|backward_compat_flat_format|sql_filters_alias_backward_compat|sql_filters_new_field_names)` 命中 5 行）。
    - 现存测试 `test_filters_toml_deserialization_with_trxids_and_exec_ids` 中字段访问从 `filters.meta.trxids` 改为 `filters.include.trxids`（grep `filters\.meta\.trxids` 在该测试函数内为 0 命中）。
    - git log 最新一条 commit 信息匹配 `test(17-01): add failing parse tests for nested filters (RED)`。
  </acceptance_criteria>
  <done>5 个新增测试函数存在；编译/运行确认 RED；现存测试微调完成；commit 已创建。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 实现新结构 + 手写 Deserialize + Compiled 入口重命名 + 现有测试适配 (GREEN)</name>
  <files>src/features/filters.rs</files>
  <read_first>
    - src/features/filters.rs (Task 1 产生的状态：新测试已存在但编译失败)
    - .planning/phases/17-filter-nesting/17-RESEARCH.md (Architecture Patterns → 中间 raw struct 模式 + SqlFilters 字段名重命名 + FiltersFeature 新结构 三段完整代码骨架，Common Pitfalls 6 条全部 mitigation)
    - .planning/phases/17-filter-nesting/17-PATTERNS.md (模式 1–9 全部，关键字段路径映射表)
    - .planning/phases/17-filter-nesting/17-CONTEXT.md (Locked Decisions D-01 至 D-06)
  </read_first>
  <behavior>
    - Task 1 中 5 个新增测试 + 现存 `test_filters_toml_deserialization_with_trxids_and_exec_ids` 全部通过（GREEN）。
    - 所有 `MetaFilters` 已有 unit test（test_meta_has_filters_with_exclude_usernames、test_t1_meta_has_filters_*、test_meta_has_filters_with_exclude、test_meta_has_filters_all_none 等）转换为针对 `IncludeFilters` / `ExcludeFilters` 的等价测试，或被新的 `IncludeFilters::has_filters` / `ExcludeFilters::has_filters` 测试取代。
    - `cargo test --lib features::filters` 全绿。
    - `cargo clippy --all-targets -- -D warnings` 零警告。
  </behavior>
  <action>
    在 `src/features/filters.rs` 内按以下次序改动（顺序不可乱）：

    1) 顶部 `use` 已含 `serde::{Deserialize, Deserializer}`，按需追加 `serde::de::Error as _` 仅当手写 Deserialize 需要返回自定义错误时（中间 raw struct 方案不需要，省略）。

    2) 删除 `pub struct MetaFilters { ... }`（第 60-81 行整个 struct 定义）和 `impl MetaFilters { has_filters / should_keep / match_exact / match_substring ... }`（第 182-255 行整块）。`match_exact` / `match_substring` 若仅被 `MetaFilters::should_keep` 使用则一并删除；若被其他模块引用则保留并改 module-level free function。基于现有代码仅 `MetaFilters` 自身使用，可整块删除。

    3) 新增 `pub struct IncludeFilters` 和 `pub struct ExcludeFilters`（derive: `Debug, Deserialize, Clone, Default`，每个字段加 `#[serde(default)]`）。`IncludeFilters` 含字段：`users / ips / sessions / threads / statements / apps / tags`（全部 `Option<Vec<String>>`）、`start_ts / end_ts`（`Option<String>`）、`trxids`（`Option<TrxidSet>` + `#[serde(default, deserialize_with = "vec_to_hashset")]`）。`ExcludeFilters` 含字段：`users / ips / sessions / threads / statements / apps / tags`（全部 `Option<Vec<String>>`）。
       对二者实现 `pub fn has_filters(&self) -> bool`，使用 `is_some_and(|v| !v.is_empty())` 模式（参照 `MetaFilters::has_filters` 的原写法）；`IncludeFilters::has_filters` 额外检查 `start_ts.is_some() || end_ts.is_some() || trxids.as_ref().is_some_and(|s| !s.is_empty())`。

    4) 重写 `pub struct FiltersFeature`：移除 `#[derive(Deserialize)]`（改为手写），保留 `Debug, Clone, Default`；字段改为 `pub enable: bool, pub include: IncludeFilters, pub exclude: ExcludeFilters, pub indicators: IndicatorFilters, pub sql: SqlFilters, pub record_sql: SqlFilters`（去除 `#[serde(flatten)]` 和 `meta` 字段）。

    5) 在同 module 内新增私有 `struct RawFiltersFeature`（`#[derive(Debug, Deserialize)]`），字段顺序无关，所有字段必须加 `#[serde(default)]`：
       - 新格式：`enable: bool`、`include: Option<IncludeFilters>`、`exclude: Option<ExcludeFilters>`、`indicators: IndicatorFilters`、`sql: SqlFilters`、`record_sql: SqlFilters`
       - 旧扁平字段（17 个，与 RESEARCH.md 完整列出一致）：`usernames / client_ips / sess_ids / thrd_ids / statements / appnames / tags`（全 `Option<Vec<String>>`）、`start_ts / end_ts`（`Option<String>`）、`trxids: Option<TrxidSet>`（必须保留 `#[serde(default, deserialize_with = "vec_to_hashset")]`）、`exclude_usernames / exclude_client_ips / exclude_sess_ids / exclude_thrd_ids / exclude_statements / exclude_appnames / exclude_tags`（全 `Option<Vec<String>>`）

    6) 实现 `impl<'de> Deserialize<'de> for FiltersFeature`：内部调用 `RawFiltersFeature::deserialize(d)?` 然后 `Ok(FiltersFeature::from(raw))`。

    7) 实现 `impl From<RawFiltersFeature> for FiltersFeature`：
       - `include = raw.include.unwrap_or_else(|| IncludeFilters { users: raw.usernames, ips: raw.client_ips, sessions: raw.sess_ids, threads: raw.thrd_ids, statements: raw.statements, apps: raw.appnames, tags: raw.tags, start_ts: raw.start_ts, end_ts: raw.end_ts, trxids: raw.trxids })`
       - `exclude = raw.exclude.unwrap_or_else(|| ExcludeFilters { users: raw.exclude_usernames, ips: raw.exclude_client_ips, sessions: raw.exclude_sess_ids, threads: raw.exclude_thrd_ids, statements: raw.exclude_statements, apps: raw.exclude_appnames, tags: raw.exclude_tags })`
       - 其余 `enable / indicators / sql / record_sql` 直接 move。
       - 优先级：**新格式优先**（`raw.include`/`raw.exclude` 为 `Some` 时忽略旧扁平字段，避免歧义）。

    8) 更新 `impl FiltersFeature`：
       - `has_filters`: 改为 `self.enable && (self.include.has_filters() || self.exclude.has_filters() || self.indicators.has_filters() || self.sql.has_filters() || self.record_sql.has_filters())`（不再单独检查 start_ts/end_ts，因为它们已并入 `IncludeFilters::has_filters`）。
       - `has_transaction_filters`: 字段路径不涉及，保持不变。
       - `merge_found_trxids`: `self.meta.trxids` → `self.include.trxids`。
       - 删除已 `#[deprecated]` 的 `should_keep` 方法及其测试（搜索 `#[deprecated]` 和 `#[allow(deprecated)]` 标记）；该方法在热路径已不用，且 OR-semantics 与 `CompiledMetaFilters::should_keep` 矛盾，移除清理代码。

    9) 重命名 `SqlFilters` 字段：`include_patterns` → `includes`（加 `#[serde(default, alias = "include_patterns")]`）；`exclude_patterns` → `excludes`（加 `#[serde(default, alias = "exclude_patterns")]`）。更新 `impl SqlFilters { has_filters / matches }`：方法体中 `self.include_patterns` / `self.exclude_patterns` 改为 `self.includes` / `self.excludes`。
       同步更新所有现有 `SqlFilters { include_patterns: ..., exclude_patterns: ... }` 字面构造点（测试代码 `test_sql_filters_include_pattern` / `test_sql_filters_exclude_pattern` / `test_sql_filters_include_and_exclude` / `test_sql_filters_empty_include_patterns_with_exclude` / `test_handle_validate_with_filters_all_fields` 等）改为新字段名。注意 `tests/integration.rs` 内的 `SqlFilters { ... }` 构造由 Plan 02 处理，本任务范围仅 `src/features/filters.rs` 内。

    10) 重命名 `CompiledMetaFilters::try_from_meta(&MetaFilters)` 为 `try_from_include_exclude(&IncludeFilters, &ExcludeFilters)`。方法体内：
        - include 字段使用 `compile_patterns("features.filters.include.users", include.users.as_deref())?` 等（路径字符串使用新嵌套路径，覆盖 7 个 include 字段 + 1 个 trxids 不编译只克隆）。
        - exclude 字段：`exclude_usernames: compile_patterns("features.filters.exclude.users", exclude.users.as_deref())?` 等（7 个 exclude 字段）。
        - `trxids: include.trxids.clone()`。
        - struct 字段名（`usernames` / `client_ips` / `sess_ids` / `thrd_ids` / `statements` / `appnames` / `tags` / `trxids` / `exclude_usernames` 等 15 个）保持不变（这是热路径已编译字段名，与配置字段名无关，避免破坏 `CompiledMetaFilters` 的使用者）。

    11) 更新 `CompiledSqlFilters::try_from_sql_filters`：`sf.include_patterns.as_deref()` 改为 `sf.includes.as_deref()`，`sf.exclude_patterns.as_deref()` 改为 `sf.excludes.as_deref()`。`compile_patterns` 的 field 路径字符串保持 `"features.filters.record_sql.include_patterns"` / `"features.filters.record_sql.exclude_patterns"`（错误消息向用户暴露的字段路径，按用户决策 D-03 改名为 `"features.filters.record_sql.includes"` / `"features.filters.record_sql.excludes"` —— 此处采用新路径以保持错误消息与配置字段名一致）。

    12) 更新 `mod tests` 中所有现存测试：
        - `make_feature` 工厂函数：`meta: MetaFilters::default()` 行替换为 `include: IncludeFilters::default(), exclude: ExcludeFilters::default()`。
        - 所有 `f.meta.usernames` → `f.include.users`；`f.meta.client_ips` → `f.include.ips`；`f.meta.sess_ids` → `f.include.sessions`；`f.meta.thrd_ids` → `f.include.threads`；`f.meta.statements` → `f.include.statements`；`f.meta.appnames` → `f.include.apps`；`f.meta.tags` → `f.include.tags`；`f.meta.start_ts` → `f.include.start_ts`；`f.meta.end_ts` → `f.include.end_ts`；`f.meta.trxids` → `f.include.trxids`。
        - 所有 `f.meta.exclude_usernames` → `f.exclude.users`；`f.meta.exclude_client_ips` → `f.exclude.ips`；`f.meta.exclude_sess_ids` → `f.exclude.sessions`；`f.meta.exclude_thrd_ids` → `f.exclude.threads`；`f.meta.exclude_statements` → `f.exclude.statements`；`f.meta.exclude_appnames` → `f.exclude.apps`；`f.meta.exclude_tags` → `f.exclude.tags`。
        - `make_compiled_meta` 测试 helper：原构造 `MetaFilters { usernames, client_ips, ..Default::default() }` + `CompiledMetaFilters::try_from_meta(&meta).unwrap()` 改为构造 `IncludeFilters { users, ips, ..Default::default() }` + `ExcludeFilters::default()` + `try_from_include_exclude(&include, &exclude).unwrap()`。同理 `make_compiled_with_exclude`。
        - 删除 `#[allow(deprecated)]` 注解（如果跟随 `should_keep` 移除）。删除使用 `FiltersFeature::should_keep` 的测试（`test_should_keep_no_filters_passes_all` 等约 10 个测试），因为该方法已删除；保留的功能性等价测试由 `CompiledMetaFilters::should_keep` 路径已存在的 test_compiled_meta_* 系列覆盖。
        - test_t1_meta_has_filters_* 与 test_meta_has_filters_* 系列：将 `MetaFilters { ... }.has_filters()` 改为对 `IncludeFilters` 或 `ExcludeFilters` 的等价断言（按字段语义分配——exclude_usernames 测 ExcludeFilters，usernames 测 IncludeFilters）。
        - `test_exclude_invalid_regex_validate_fails` 与 `test_t1_compiled_from_meta_*` 系列：将 `try_from_meta(&MetaFilters { ... })` 改为 `try_from_include_exclude(&IncludeFilters { ... }, &ExcludeFilters { users: Some(vec!["[invalid".into()]), ..Default::default() })` 等价构造。

    13) 提交：`git commit -m "feat(17-01): nest filters into include/exclude sub-tables with backward-compat deserialize (GREEN)"`（Co-Authored-By 行保留）。

    注意事项：
    - 不要删除或修改 `vec_to_hashset` / `vec_to_i64_hashset` / `compile_patterns` / `match_any_regex` helper 函数。
    - 不要修改 `CompiledMetaFilters` struct 字段定义（仅改其构造入口）。
    - 不要修改 `CompiledMetaFilters::has_filters` / `has_any_filters` / `should_keep` / `exclude_veto` / `include_and` 方法体（这些是热路径，操作的是已编译字段，不依赖配置字段名）。
    - 不要修改 `IndicatorFilters` struct 或其方法。
    - `src/config.rs` / `src/cli/run.rs` / `src/cli/init.rs` / `tests/integration.rs` 由 Plan 02 负责；本任务结束后 `cargo build --tests` 会因为这些文件仍引用 `filters.meta` / `try_from_meta` / `MetaFilters` 而失败，**这是预期的**。在 verify 阶段使用 `cargo test --lib features::filters` 而非 `cargo test`，仅验证 `filters.rs` 内部一致。
  </action>
  <verify>
    <automated>cargo test --lib features::filters 2>&1 | tail -20 && cargo clippy --lib -p dm-database-sqllog2db -- -D warnings 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --lib features::filters` 退出码为 0，新增 5 个测试全部 PASS，所有原有 `features::filters::tests::*` 测试 PASS（被删的 `should_keep` 相关测试不计）。
    - `cargo clippy --lib -p dm-database-sqllog2db -- -D warnings` 零警告（针对 lib 部分；tests/integration.rs 由 Plan 02 修复）。
    - `grep -c "pub struct IncludeFilters" src/features/filters.rs` ≥ 1。
    - `grep -c "pub struct ExcludeFilters" src/features/filters.rs` ≥ 1。
    - `grep -c "struct RawFiltersFeature" src/features/filters.rs` ≥ 1。
    - `grep -c "impl<'de> Deserialize<'de> for FiltersFeature" src/features/filters.rs` ≥ 1。
    - `grep -c "impl From<RawFiltersFeature> for FiltersFeature" src/features/filters.rs` ≥ 1。
    - `grep -c "fn try_from_include_exclude" src/features/filters.rs` ≥ 1。
    - `grep -E "^\s*pub\s+(struct\s+)?MetaFilters" src/features/filters.rs | grep -v "//"` 0 命中（MetaFilters 已删除）。
    - `grep -E "fn try_from_meta\b" src/features/filters.rs | grep -v "//"` 0 命中。
    - `grep -E "include_patterns:|exclude_patterns:" src/features/filters.rs | grep -v "alias" | grep -v "//"` 仅保留 `CompiledSqlFilters` struct 内的字段定义 + 其字面构造（CompiledSqlFilters 字段未改名）；`SqlFilters` 字面构造中 `include_patterns:` / `exclude_patterns:` 应为 0 命中。
    - `grep -E '#\[serde\(default, alias = "include_patterns"\)\]' src/features/filters.rs` ≥ 1 命中。
    - `grep -E '#\[serde\(default, alias = "exclude_patterns"\)\]' src/features/filters.rs` ≥ 1 命中。
    - git log 最新一条 commit 信息匹配 `feat(17-01): nest filters into include/exclude sub-tables with backward-compat deserialize (GREEN)`。
  </acceptance_criteria>
  <done>filters.rs 内重构完整；5 个新测试 GREEN；原 unit tests 适配新字段；lib 部分 clippy 零警告；commit 已创建。整体 cargo build/test 失败是预期（待 Plan 02 修复 caller）。</done>
</task>

</tasks>

<verification>
- 新增 IncludeFilters / ExcludeFilters / RawFiltersFeature 三个 struct 存在
- 手写 FiltersFeature::Deserialize 通过 RawFiltersFeature 路径
- CompiledMetaFilters::try_from_include_exclude 取代 try_from_meta
- SqlFilters 字段重命名为 includes / excludes 并通过 alias 兼容旧名
- features::filters 全部单元测试通过（cargo test --lib features::filters）
- 新格式 + 旧格式 parse 测试均 PASS
- lib clippy 零警告
</verification>

<success_criteria>
- `cargo test --lib features::filters` 全绿
- `cargo clippy --lib -p dm-database-sqllog2db -- -D warnings` 零警告
- 新格式与旧格式 TOML 解析结果在 IncludeFilters / ExcludeFilters 层语义一致
- 整体 `cargo test` 仍失败属预期（callers 由 Plan 02 修复）
</success_criteria>

<output>
Create `.planning/phases/17-filter-nesting/17-01-SUMMARY.md` when done
</output>
