---
phase: 34-audit-gap-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/config/mod.rs
  - src/config/validate.rs
autonomous: true
requirements: [RM-05, RM-08]
user_setup: []
must_haves:
  truths:
    - "[template] TOML 配置段被显式拒绝，并给出清晰的废弃错误消息"
    - "现有合法配置（无 [template] 段）的 validate() 通过不受影响"
    - "cargo build --release + cargo clippy + cargo test + cargo fmt 全部通过"
  artifacts:
    - path: "src/config/mod.rs"
      provides: "Config 结构体增加 template_deprecated 字段"
      contains: "template_deprecated"
    - path: "src/config/validate.rs"
      provides: "[template] 段被拒绝的检查逻辑"
      contains: "template_deprecated"
  key_links:
    - from: "src/config/mod.rs"
      to: "src/config/validate.rs"
      via: "validate_and_compile() 中检查 template_deprecated"
    - from: "[template] TOML 段"
      to: "serde 反序列化"
      via: "rename 将 [template] 映射到 template_deprecated 字段"
---

<objective>
添加 [template] 配置段的显式拒绝逻辑

Purpose: 关闭审计缺口 INT-02 — Phase 30 移除模板分析后，`[template]` 配置段被 serde 静默忽略，与 `[pipeline]` 的显式拒绝行为不一致。用户升级后如有遗留 [template] 配置不会收到任何警告。
Output:
- Config 结构体增加 `template_deprecated: Option<toml::Value>` 字段（类似 pipeline_deprecated 模式）
- validate_and_compile() 检测到 [template] 段时返回清晰的错误消息
- 现有测试更新 + 新测试覆盖 [template] 拒绝行为
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@$HOME/.claude/get-shit-done/references/mandatory-initial-read.md

@/Users/guang/Projects/sqllog2db/.planning/ROADMAP.md
@/Users/guang/Projects/sqllog2db/.planning/STATE.md
@/Users/guang/Projects/sqllog2db/.planning/phases/34-audit-gap-closure/34-CONTEXT.md
@/Users/guang/Projects/sqllog2db/.planning/v1.7-MILESTONE-AUDIT.md

<interfaces>
From src/config/mod.rs — current Config struct (参考 pipeline_deprecated 模式):
```rust
#[derive(Debug, Deserialize, Clone, Default)]
pub struct Config {
    // ... 现有字段 ...
    #[doc(hidden)]
    #[serde(rename = "pipeline", default)]
    pub pipeline_deprecated: Option<toml::Value>,
}
```

From src/config/validate.rs — validate_and_compile() 中 pipeline 拒绝逻辑（参考模式）:
```rust
if self.pipeline_deprecated.is_some() {
    return Err(Error::Config(ConfigError::InvalidValue {
        field: "[pipeline]".to_string(),
        value: String::new(),
        reason: PIPELINE_MIGRATION_HINT.to_string(),
    }));
}
```

当前验证状态（plan 执行前确认基线）:
- cargo build: 通过
- cargo test: 604 测试全通过（275+293+36）
- cargo clippy --all-targets -- -D warnings: 通过
- cargo fmt --check: 通过
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 为 Config 添加 template_deprecated 字段及拒绝逻辑</name>
  <read_first>
    - /Users/guang/Projects/sqllog2db/src/config/mod.rs (Config 结构体, 了解 pipeline_deprecated 的 serde 模式)
    - /Users/guang/Projects/sqllog2db/src/config/validate.rs (validate_and_compile, 参考 pipeline 拒绝逻辑)
  </read_first>
  <files>
    - src/config/mod.rs
    - src/config/validate.rs
  </files>
  <action>
    1. 在 src/config/mod.rs 中，Config 结构体增加 `template_deprecated` 字段。模式完全参照 `pipeline_deprecated`:
       - 字段类型: `Option<toml::Value>`
       - serde rename: `"template"` (TOML 中的 `[template]` 表会被捕获到此字段)
       - serde default: 使用 `#[serde(default)]` 使缺少 [template] 时为 None
       - 标注 `#[doc(hidden)]` 禁止用户直接访问
       - 位置: 在 `pipeline_deprecated` 字段之后
    2. 在 src/config/validate.rs 的 `validate_and_compile()` 方法中，在 `pipeline_deprecated` 检查之后（第 27-33 行附近）添加 `template_deprecated` 检查:
       - 如果 `self.template_deprecated.is_some()`, 返回 `Error::Config(ConfigError::InvalidValue { ... })`
       - field 字符串: `"[template]"`
       - value: 空字符串 `String::new()`
       - reason: `"配置段 [template] 已废弃，请移除此配置段"`
       - 不要使用 PIPELINE_MIGRATION_HINT 作为 reason — [template] 与 [pipeline] 的迁移路径不同，[template] 功能已完全移除，仅需告知用户移除即可
    3. 注意: handle 上报 `[template]` 键在 apply_one 中已被 `_ => return Err(unknown())` 捕获，不需要额外修改 apply_one.rs
    4. **不修改任何其他文件** — 只改 mod.rs 结构体和 validate.rs 的验证逻辑
  </action>
  <acceptance_criteria>
    - src/config/mod.rs: Config 结构体包含 `template_deprecated: Option<toml::Value>` 字段，serde rename 为 "template"，serde default 为 None
    - src/config/mod.rs: `cargo build` 通过，无编译错误
    - src/config/validate.rs: `validate_and_compile()` 包含 template_deprecated 检查，当 [template] 存在时返回包含 "[template]" 和 "已废弃" 的错误消息
  </acceptance_criteria>
  <verify>
    <automated>cargo build</automated>
  </verify>
  <done>Config 结构体增加 template_deprecated 字段，validate_and_compile 返回 [template] 已废弃错误</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: 更新测试 — 移除 [template] 引用 + 添加拒绝测试</name>
  <read_first>
    - /Users/guang/Projects/sqllog2db/src/config/validate.rs (第 444-460 行的 test_validate_new_top_level_format_passes 测试)
  </read_first>
  <files>
    - src/config/validate.rs
  </files>
  <action>
    1. 修改 `test_validate_new_top_level_format_passes` 测试（当前第 444-460 行）:
       - 从 TOML 字符串中移除 `[template]\nenable = true\n` 两行
       - 保持其他所有字段不变（[sqllog], [filter], [output], [exporter.csv]）
       - 断言 `cfg.validate().is_ok()` 不变 — 移除了 [template] 后，配置应该仍然通过验证
    2. 添加新测试 `test_validate_rejects_template_section`:
       - TOML 包含 [template] 段（任意内容，如 `enable = true`）
       - 断言 `cfg.validate()` 返回 Err
       - 断言错误消息包含 `"[template]"` 和 `"已废弃"` 两个关键词
    3. 注意: 新测试在文件末尾附近添加，与现有的旧路径检测测试放在一起
  </action>
  <acceptance_criteria>
    - test_validate_new_top_level_format_passes: TOML 字符串中无 [template] 行，测试通过
    - test_validate_rejects_template_section: 验证 [template] 被拒绝，错误消息包含 "[template]" 和 "已废弃"
    - `cargo test` 全部通过（604+1 或 604+2 取决于测试组织形式）
  </acceptance_criteria>
  <verify>
    <automated>cargo test -- config::validate::tests 2>&1 | grep -E "ok|FAILED"</automated>
  </verify>
  <done>所有测试通过，[template] 拒绝行为被测试覆盖</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: 全链路验证 — build + test + clippy + fmt</name>
  <read_first>
    - (无额外需要读取的文件)
  </read_first>
  <files>
    - (无需修改文件)
  </files>
  <action>
    运行以下四个命令，确保全部通过:
    1. `cargo build --release` — 确认 release 编译成功
    2. `cargo test` — 确认所有测试通过
    3. `cargo clippy --all-targets -- -D warnings` — 确认零警告
    4. `cargo fmt --check` — 确认格式合规
  </action>
  <acceptance_criteria>
    - cargo build --release: 成功，无错误
    - cargo test: 全部通过
    - cargo clippy --all-targets -- -D warnings: 无警告
    - cargo fmt --check: 无输出（格式合规）
  </acceptance_criteria>
  <verify>
    <automated>cargo build --release && cargo test && cargo clippy --all-targets -- -D warnings && cargo fmt --check</automated>
  </verify>
  <done>四个门禁全部通过</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TOML 配置文件 → Config serde 反序列化 | 用户提供的外部配置文件进入代码信任边界 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-01 | Spoofing | Config 反序列化 | mitigate | serde 通过 rename 捕获废弃 [template] 段，validate 返回明确错误消息 — 防止旧配置被静默忽略导致用户误以为模板功能仍在运行 |
| T-34-SC | Tampering | 依赖安装 | accept | 本次 plan 不修改 Cargo.toml，无新依赖引入 |
</threat_model>

<verification>
执行顺序: Task 1 → Task 2 → Task 3（串行，每步依赖上一步完成）

验证命令:
```bash
cargo build --release && cargo test && cargo clippy --all-targets -- -D warnings && cargo fmt --check
```
</verification>

<success_criteria>
- [template] TOML 段被显式拒绝，错误消息清晰（D-04, D-06）
- 测试覆盖 [template] 拒绝行为（D-05）
- `cargo build --release` + `cargo test` + `cargo clippy --all-targets -- -D warnings` + `cargo fmt --check` 全部通过（D-13）
</success_criteria>

<output>
创建 `.planning/phases/34-audit-gap-closure/34-01-SUMMARY.md`
</output>
