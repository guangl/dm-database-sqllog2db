---
phase: 01-watch
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/config/mod.rs
  - src/cli/run/mod.rs
  - src/cli/watch/mod.rs
autonomous: true
requirements:
  - WATCH-07
  - WATCH-08
  - WATCH-09
must_haves:
  truths:
    - "watch 触发 CSV exporter 时新增记录追加到现有 CSV 文件，多次触发后文件含累计记录与单一 header"
    - "watch 期间产生的所有 parse error 追加写入 error log，不覆盖前次触发的历史错误"
    - "watch 进程被 SIGINT/Ctrl+C 中断后 handle_watch 返回 Err(Error::Interrupted)，main.rs 走 exit(130) 分支"
    - "run 子命令的 write_error_log 行为保持覆盖写不变（append_error_log 默认 false）"
  artifacts:
    - path: "src/config/mod.rs"
      provides: "Config.append_error_log #[serde(skip)] pub(crate) bool 字段"
      contains: "append_error_log"
    - path: "src/cli/run/mod.rs"
      provides: "write_error_log 根据 cfg.append_error_log 选择 OpenOptions::append 或 truncate"
      contains: "cfg.append_error_log"
    - path: "src/cli/watch/mod.rs"
      provides: "trigger_full_file 与 build_incremental_cfg 注入 CSV append + append_error_log；handle_watch 尾部 interrupted 检查"
      contains: "csv_cfg.append = true"
  key_links:
    - from: "src/cli/watch/mod.rs::trigger_full_file"
      to: "src/config/mod.rs::Config.append_error_log"
      via: "tmp_cfg.append_error_log = true 注入"
      pattern: "tmp_cfg\\.append_error_log\\s*=\\s*true"
    - from: "src/cli/watch/mod.rs::build_incremental_cfg"
      to: "src/config/exporter.rs::CsvExporterConfig.append"
      via: "csv_cfg.append = true; csv_cfg.overwrite = false 注入"
      pattern: "csv_cfg\\.append\\s*=\\s*true"
    - from: "src/cli/run/mod.rs::write_error_log"
      to: "std::fs::OpenOptions"
      via: "条件分支选择 append vs truncate 模式"
      pattern: "OpenOptions::new"
    - from: "src/cli/watch/mod.rs::handle_watch"
      to: "src/error.rs::Error::Interrupted"
      via: "interrupted.load(Ordering::Acquire) 后 return Err"
      pattern: "Error::Interrupted"
---

<objective>
为 watch 子命令补齐三项功能短板（WATCH-07/08/09）：CSV 增量追加、error log 追加写入、Ctrl+C 退出码 130。

Purpose: 让 watch 长时间运行时不丢失累计数据（CSV 不被覆盖、error log 不丢历史），且 SIGINT 退出码与 run 子命令保持一致（130），消除用户从 run 切换到 watch 时的行为差异。
Output: Config 新增内部字段、watch 触发函数注入 append 标志、handle_watch 尾部检查中断、4 个新单元测试验证以上行为。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-watch/01-CONTEXT.md
@.planning/phases/01-watch/01-RESEARCH.md
@.planning/phases/01-watch/01-PATTERNS.md
@.planning/phases/01-watch/01-VALIDATION.md

<interfaces>
<!-- Extracted from current codebase so the executor does not need to grep -->

From src/config/mod.rs (current Config struct, lines 23-41):
```rust
#[derive(Debug, Deserialize, Clone, Default)]
pub struct Config {
    #[serde(default)]
    pub sqllog: SqllogConfig,
    #[serde(default)]
    pub logging: LoggingConfig,
    #[serde(default)]
    pub exporter: ExporterConfig,
    #[serde(default)]
    pub replace_parameters: Option<NormalizeConfig>,
    #[serde(default)]
    pub filter: Option<FiltersFeature>,
    #[serde(default)]
    pub output: Option<OutputConfig>,
    #[serde(default)]
    pub stats: StatsConfig,
    #[serde(default)]
    pub error: Option<ErrorLogConfig>,
}
```

From src/config/exporter.rs (CsvExporterConfig has both fields):
```rust
pub struct CsvExporterConfig {
    pub file: String,
    pub overwrite: bool,   // #[serde(default = "default_true")]
    pub append: bool,      // #[serde(default)]
    // ...
}
```

From src/cli/run/mod.rs (write_error_log signature & current body, lines 425-461):
```rust
fn write_error_log(cfg: &crate::config::Config, stats: &ErrorStats) {
    let Some(error_cfg) = cfg.error.as_ref() else { return; };
    if stats.parse_error_records.is_empty() { return; }
    use std::io::Write;
    let file = match std::fs::File::create(&error_cfg.file) {
        Ok(f) => f,
        Err(e) => { log::warn!("Failed to create error log {}: {e}", error_cfg.file); return; }
    };
    let mut writer = std::io::BufWriter::new(file);
    // ... writeln! loop + flush
}
```

From src/cli/watch/mod.rs (trigger_full_file lines 311-313, build_incremental_cfg lines 514-523, handle_watch lines 66-73):
```rust
// trigger_full_file body excerpt:
let mut tmp_cfg = cfg.clone();
tmp_cfg.sqllog.inputs = vec![path.to_string_lossy().into_owned()];
match crate::cli::run::handle_run(&tmp_cfg, quiet, verbose, interrupted, None) { ... }

// build_incremental_cfg full body:
fn build_incremental_cfg(cfg: &Config, tmp_file: &tempfile::NamedTempFile) -> Config {
    let mut tmp_cfg = cfg.clone();
    tmp_cfg.sqllog.inputs = vec![tmp_file.path().to_string_lossy().into_owned()];
    if let Some(ref mut sqlite_cfg) = tmp_cfg.exporter.sqlite {
        sqlite_cfg.append = true;
        sqlite_cfg.overwrite = false;
    }
    tmp_cfg
}

// handle_watch tail:
pb.finish_and_clear();
print_final_summary(&start, state.trigger_count(), state.total_stats().records_exported, quiet);
Ok(())
```

From src/error.rs (line 162-163):
```rust
#[error("Interrupted by user")]
Interrupted,
```

From src/main.rs (lines 113-115, already in place):
```rust
Err(e) if matches!(e, Error::Interrupted) => std::process::exit(130),
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 新增 Config.append_error_log 字段 + write_error_log 双分支</name>
  <files>src/config/mod.rs, src/cli/run/mod.rs</files>
  <read_first>
    - src/config/mod.rs (确认 Config 结构体完整字段顺序，新字段追加在 `error: Option&lt;ErrorLogConfig&gt;` 之后)
    - src/cli/run/mod.rs (line 423-461 write_error_log 当前实现)
    - .planning/phases/01-watch/01-RESEARCH.md (D-04/D-06 决策原文)
    - .planning/phases/01-watch/01-PATTERNS.md (Pattern 2: OpenOptions 双分支)
  </read_first>
  <behavior>
    - 行为 1（append=false，默认/run 路径）：write_error_log 以 truncate 模式打开 error_cfg.file，等价于现有 File::create 行为
    - 行为 2（append=true，watch 路径）：write_error_log 以 OpenOptions::create(true).append(true) 模式打开，已有内容保留
    - 行为 3：Config::default() 返回的实例 append_error_log == false
    - 行为 4：append_error_log 字段不参与 TOML 反序列化（#[serde(skip)]）—— 任意 TOML 配置文件解析后该字段保持 false
  </behavior>
  <action>
    实施 D-04 + D-06：

    步骤 1（src/config/mod.rs）：在 `Config` 结构体的最后一个字段 `pub error: Option&lt;ErrorLogConfig&gt;` 之后追加新字段：

    - 字段名：`append_error_log`
    - 类型：`bool`
    - 可见性：`pub(crate)`
    - 属性：`#[serde(skip)]`
    - 必须在字段上方写文档注释（doc comment），原文：`/// watch 触发时设为 true，使 write_error_log 以追加模式打开文件。run 路径默认 false（覆盖写）。`
    - 不要为该字段编写自定义 Default impl —— Rust `bool` 的 `Default::default()` 已经是 false，依赖 derive(Default) 自动保证

    步骤 2（src/cli/run/mod.rs，函数 `write_error_log`，line 423-461）：将文件打开块（line 433-439 的 `let file = match std::fs::File::create(...) { Ok(f) => f, Err(e) => { log::warn!(...); return; } };`）替换为条件分支：

    - 当 `cfg.append_error_log == true`：使用 `std::fs::OpenOptions::new().create(true).append(true).open(&error_cfg.file)`
    - 当 `cfg.append_error_log == false`：使用 `std::fs::OpenOptions::new().create(true).write(true).truncate(true).open(&error_cfg.file)`
    - 两个分支统一用一个 `let file = if cfg.append_error_log { ... } else { ... };` 表达式，紧接着保留原 `let file = match file { Ok(f) =&gt; f, Err(e) =&gt; { log::warn!("Failed to create error log {}: {e}", error_cfg.file); return; } };` 的 match 处理（per D-06）
    - **不要**修改 BufWriter / writeln! / flush 后续代码（line 440-461 保持原样）
    - **不要**新增 `use` 语句到模块顶部 —— `OpenOptions` 用 `std::fs::OpenOptions` 全路径直接调用，与现有 `std::fs::File::create` 风格一致
    - 同时更新 line 423-424 的函数文档注释：原文为 `/// 将解析错误记录批量写出到配置的 error log 文件（覆盖模式）。` —— 改为 `/// 将解析错误记录批量写出到配置的 error log 文件。cfg.append_error_log=true 时为追加模式（watch 触发），false 时为覆盖模式（run 子命令默认）。`

    步骤 3：本任务不修改 `write_error_log` 的调用点（call sites）—— 调用方传入的 `cfg` 自带 `append_error_log` 状态，无需额外参数。

    步骤 4（TDD 守护）：在 `src/cli/run/mod.rs` 的 `#[cfg(test)] mod tests;` 对应 tests 模块文件中追加单元测试 `test_write_error_log_run_still_truncates`（实现见 Task 3，本任务此处不需要写测试体，但确认现有 tests.rs 文件位置以便 Task 3 接续）。
  </action>
  <verify>
    <automated>cargo build --release 2&gt;&amp;1 | grep -v '^#' | (! grep -E 'error\[E[0-9]+\]') &amp;&amp; cargo test config::tests --lib -- --test-threads=1</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -n 'append_error_log' src/config/mod.rs` 返回 ≥ 2 行（一条字段定义、一条 doc comment）
    - Source assertion: `grep -n '#\[serde(skip)\]' src/config/mod.rs` 返回 ≥ 1 行
    - Source assertion: `grep -n 'pub(crate) append_error_log: bool' src/config/mod.rs` 返回 1 行
    - Source assertion: `grep -n 'cfg.append_error_log' src/cli/run/mod.rs` 返回 ≥ 1 行
    - Source assertion: `grep -nE 'OpenOptions::new\(\)' src/cli/run/mod.rs` 在 line 425-461 之间返回 ≥ 2 行（append 分支 + truncate 分支）
    - Source assertion: `grep -n 'std::fs::File::create(&error_cfg.file)' src/cli/run/mod.rs` 返回 0 行（旧调用已被替换）
    - Behavior assertion: `cargo build --release` 退出码 0，编译无 error
    - Behavior assertion: `cargo test config::tests --lib` 现有 config 模块测试全部通过（新字段不破坏现有测试）
    - Behavior assertion: Config::default() 返回值的 `append_error_log` 字段为 `false`（可在临时 dbg! 或后续 Task 3 测试中验证）
  </acceptance_criteria>
  <done>
    - Config 结构体新增 `pub(crate) append_error_log: bool` 字段，带 `#[serde(skip)]` 与 doc comment
    - write_error_log 文件打开段改为 if/else 双分支（append vs truncate），调用方接口不变
    - cargo build --release 通过，cargo test 现有测试不回归
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: watch 触发函数注入 append 标志 + handle_watch 退出码检查</name>
  <files>src/cli/watch/mod.rs</files>
  <read_first>
    - src/cli/watch/mod.rs (line 295-326 trigger_full_file、line 514-523 build_incremental_cfg、line 30-74 handle_watch)
    - src/error.rs (line 159-163 Error::Interrupted 变体确认)
    - .planning/phases/01-watch/01-CONTEXT.md (D-01/D-02/D-03/D-05/D-07/D-08 决策原文)
    - .planning/phases/01-watch/01-PATTERNS.md (修改点 1/2/3 注入位置)
  </read_first>
  <behavior>
    - 行为 1（WATCH-07 全量路径）：trigger_full_file 在调用 handle_run 前，tmp_cfg.exporter.csv (若 Some) 的 append 设为 true、overwrite 设为 false
    - 行为 2（WATCH-07 增量路径）：build_incremental_cfg 返回的 Config，tmp_cfg.exporter.csv (若 Some) 的 append 设为 true、overwrite 设为 false
    - 行为 3（WATCH-08）：trigger_full_file 与 build_incremental_cfg 中 tmp_cfg.append_error_log 设为 true
    - 行为 4（WATCH-09）：当 interrupted 为 true 时，handle_watch 在 print_final_summary 之后返回 Err(Error::Interrupted)；未被中断时仍返回 Ok(())
    - 行为 5（D-08 摘要保留）：interrupted 与否都先调用 print_final_summary，再判断返回值
  </behavior>
  <action>
    实施 D-01/D-02/D-03/D-05/D-07/D-08：

    步骤 1（trigger_full_file，line 311-313）：在 `tmp_cfg.sqllog.inputs = vec![path.to_string_lossy().into_owned()];` 之后、`match crate::cli::run::handle_run(...)` 之前插入：

    ```
    if let Some(ref mut csv_cfg) = tmp_cfg.exporter.csv {
        csv_cfg.append = true;
        csv_cfg.overwrite = false;
    }
    tmp_cfg.append_error_log = true;
    ```

    并在第一条 `if let` 上方加注释：`// WATCH-07 (D-01): 全量触发也强制 CSV 追加，避免每次触发覆盖既有数据`。在 `tmp_cfg.append_error_log = true;` 上方加注释：`// WATCH-08 (D-05): error log 追加模式，保留 watch 进程历史错误`。

    步骤 2（build_incremental_cfg，line 514-523）：在现有 SQLite append 注入块（`if let Some(ref mut sqlite_cfg) = ...`）**之后**、`tmp_cfg` 返回值之前追加同样两段：

    ```
    // WATCH-07 (D-01): 增量路径同样强制 CSV 追加
    if let Some(ref mut csv_cfg) = tmp_cfg.exporter.csv {
        csv_cfg.append = true;
        csv_cfg.overwrite = false;
    }
    // WATCH-08 (D-05): error log 追加模式
    tmp_cfg.append_error_log = true;
    ```

    步骤 3（handle_watch，line 66-73）：在 `print_final_summary(...)` 调用之后、`Ok(())` 之前插入：

    ```
    // WATCH-09 (D-07/D-08): 摘要打印后再检查中断标志，main.rs Err(Interrupted) 分支处理 exit(130)
    if interrupted.load(Ordering::Acquire) {
        return Err(Error::Interrupted);
    }
    ```

    `Ordering` 已在 line 16 导入（`use std::sync::atomic::Ordering;`），`Error` 已在 line 6 导入（`use crate::error::{Error, ErrorStats, Result};`），无需新增 `use`。

    步骤 4：不要修改 trigger_incremental（line 375-416）—— 它通过 build_incremental_cfg 间接获取 append 标志，无需再次注入。

    步骤 5（Claude 自由裁量）：考虑到 trigger_full_file 与 build_incremental_cfg 中注入逻辑完全相同（CSV append + append_error_log），可以提取一个 `fn force_append_for_watch_trigger(cfg: &mut Config)` 私有辅助函数放置在 `build_incremental_cfg` 之前，函数体为：

    ```
    /// watch 触发前强制 tmp_cfg 进入追加模式（per D-01/D-05），适用全量与增量路径。
    fn force_append_for_watch_trigger(cfg: &mut Config) {
        if let Some(ref mut csv_cfg) = cfg.exporter.csv {
            csv_cfg.append = true;
            csv_cfg.overwrite = false;
        }
        cfg.append_error_log = true;
    }
    ```

    然后在 trigger_full_file 与 build_incremental_cfg 中各调用一次 `force_append_for_watch_trigger(&mut tmp_cfg);`。SQLite append 注入（build_incremental_cfg 现有逻辑 line 518-521）保持原位不动 —— SQLite 仅增量路径需要，与 force_append_for_watch_trigger 抽象边界不重合。**优先采用此抽取方案以减少重复**，除非抽取后任一函数体超 40 行（按项目 CLAUDE.md 规则）—— 经核算 trigger_full_file 当前约 31 行，抽取后只新增 1 行调用，安全。

    步骤 6：不修改任何现有 tests 模块内的测试 —— Task 3 负责追加 4 个新测试。
  </action>
  <verify>
    <automated>cargo build --release 2&gt;&amp;1 | (! grep -E 'error\[E[0-9]+\]') &amp;&amp; cargo test --lib cli::watch::tests -- --test-threads=1</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -nE 'csv_cfg\.append\s*=\s*true' src/cli/watch/mod.rs` 返回 ≥ 1 行（若抽取辅助函数则为 1 行，否则 2 行）
    - Source assertion: `grep -nE 'csv_cfg\.overwrite\s*=\s*false' src/cli/watch/mod.rs` 返回 ≥ 1 行
    - Source assertion: `grep -nE '(tmp_cfg|cfg)\.append_error_log\s*=\s*true' src/cli/watch/mod.rs` 返回 ≥ 1 行
    - Source assertion: `grep -n 'return Err(Error::Interrupted)' src/cli/watch/mod.rs` 返回 ≥ 1 行（且必须在 handle_watch 函数体内 —— 通过 awk 段落检查或行号范围 30-80 验证）
    - Source assertion: handle_watch 中 `print_final_summary` 出现的行号小于 `return Err(Error::Interrupted)` 出现的行号（用 `grep -n 'print_final_summary\|return Err(Error::Interrupted)' src/cli/watch/mod.rs | head -3` 检查顺序，D-08 摘要先于退出）
    - Source assertion: `grep -c 'force_append_for_watch_trigger' src/cli/watch/mod.rs` （若选择抽取方案）≥ 3 次（定义 1 + 两处调用），或 0 次（未抽取）—— 两种方案任择其一
    - Behavior assertion: `cargo build --release` 退出码 0，无 error
    - Behavior assertion: `cargo clippy --all-targets -- -D warnings` 退出码 0
    - Behavior assertion: 现有 cli::watch::tests 单元测试（8 个）继续通过，无回归
  </acceptance_criteria>
  <done>
    - trigger_full_file 和 build_incremental_cfg 均强制 CSV append + append_error_log = true（直接内联或经 force_append_for_watch_trigger 抽取）
    - handle_watch 在 print_final_summary 之后检查 interrupted，true 时返回 Err(Error::Interrupted)
    - cargo build/clippy 全绿，现有测试不回归
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 新增 4 个 Wave 0 测试 + 质量门禁</name>
  <files>src/cli/watch/mod.rs, src/cli/run/tests.rs</files>
  <read_first>
    - src/cli/watch/mod.rs (现有 #[cfg(test)] mod tests 块，line 600+ 处的测试样式 — 仿照 test_interrupted_flag_exits_immediately 等)
    - src/cli/run/mod.rs (line 464 `#[cfg(test)] mod tests;` 指向 src/cli/run/tests.rs)
    - src/error.rs (ErrorStats / ParseErrorRecord / ErrorKind 结构 — 构造测试样本需要)
    - .planning/phases/01-watch/01-VALIDATION.md (Per-Task Verification Map: 4 个测试名)
    - .planning/phases/01-watch/01-RESEARCH.md (Wave 0 Gaps section 测试意图)
  </read_first>
  <behavior>
    - test_watch_csv_append（src/cli/watch/mod.rs::tests，集成风格单元测试）：
      * 创建 tempdir + 两个不同内容的源 .log 文件 A、B（每个含 1-2 条合法的 DM SQL 日志行）
      * 配置 Config：sqllog.inputs = [tempdir 路径]、exporter.csv = Some(file=output.csv, overwrite=true, append=false)、其余默认
      * 直接调用 `trigger_full_file(&A.path, &cfg, ...)` 然后 `trigger_full_file(&B.path, &cfg, ...)`（构造合法 WatchLoopState、ProgressBar::hidden、interrupted=false）
      * 断言 output.csv 行数 == A 记录数 + B 记录数 + 1（header）
      * 断言 output.csv 第一行（header）只出现一次（通过 `grep -c` 或字符串 splitn 检查）
    - test_watch_error_log_append（src/cli/watch/mod.rs::tests，集成风格单元测试）：
      * 创建 tempdir + 两个含解析失败行的源 .log 文件 A、B（每个含 1 条故意格式错误的行，例如 "garbage line"）
      * 配置 Config：error = Some(ErrorLogConfig{ file: "errors.log" })、其余照常
      * 两次连续调用 trigger_full_file
      * 断言 errors.log 内含来自 A 和 B 的两条错误（即包含两条 "garbage" 子串或同时含两个文件的行号信息）
    - test_write_error_log_run_still_truncates（src/cli/run/tests.rs，单元测试）：
      * 构造 Config 实例：append_error_log = false、error = Some(ErrorLogConfig{ file: tmpfile })、其余默认
      * 构造 ErrorStats，parse_error_records 含 1 条 ParseErrorRecord
      * 先用 std::fs::write(&tmpfile, b"OLD CONTENT\n") 预置文件内容
      * 调用 write_error_log(&cfg, &stats)
      * 断言 tmpfile 内容不再含 "OLD CONTENT"（被 truncate），但含新写入的 ERROR 行
    - test_handle_watch_returns_interrupted（src/cli/watch/mod.rs::tests，单元测试）：
      * 创建 tempdir 作为 watch 目录
      * Config: sqllog.inputs = [tempdir.path()]，其余默认
      * interrupted = Arc::new(AtomicBool::new(true))
      * 调用 handle_watch(&cfg, true, false, &interrupted)
      * 断言返回 Err(Error::Interrupted)（通过 matches!(result, Err(Error::Interrupted))）
  </behavior>
  <action>
    步骤 1（src/cli/watch/mod.rs::tests 模块，在 line 868 闭合大括号之前）追加以下 3 个测试（test_watch_csv_append、test_watch_error_log_append、test_handle_watch_returns_interrupted）：

    - 每个测试使用 `#[test]` 属性
    - 必须使用 `tempfile::TempDir` 或 `tempfile::NamedTempFile`（项目已有先例：line 647-650）
    - DM SQL 日志格式示例（参考 sqllogs/ 中的样本或 tests/integration.rs 中现有测试样本）—— 合法行示例：`2024-01-15 10:00:00.123 (EP[0] sess:0x123 thrd:456 user:SYSDBA trxid:1001 stmt:select_001 appname:test ip:127.0.0.1) [SEL] select * from t1; EXECTIME: 5(ms) ROWCOUNT: 10.`
    - ProgressBar 用 `indicatif::ProgressBar::hidden()`（line 819 现有模式）
    - WatchLoopState 用 `WatchLoopState::new(HashMap::new(), None)` 构造
    - 关键约束 1（CSV 测试）：必须验证 `trigger_full_file` 调用确实经过 Task 2 注入的 CSV append 路径 —— 即 cfg.exporter.csv.append 初始为 false（默认）、cfg.exporter.csv.overwrite 初始为 true（默认）。若 Task 2 未注入，第二次 trigger_full_file 会因 CSV truncate 覆盖第一次结果，测试失败。
    - 关键约束 2（error log 测试）：trigger_full_file 内部调用 handle_run，handle_run 内部调用 write_error_log（仅当有 parse_error_records 时）。若 Task 1 未实现 append 分支，第二次触发会 truncate 第一次错误，测试失败。
    - 关键约束 3（handle_watch 测试）：interrupted 初值为 true 时，handle_watch 进入 watch loop 立即在 line 172 退出循环，然后 print_final_summary，再到新增的 interrupted 检查 → 返回 Err。watch_dirs 必须非空（否则 line 39 提前返回 Io error）—— 用 tempdir 保证 dir 存在。

    步骤 2（src/cli/run/tests.rs）追加 test_write_error_log_run_still_truncates：

    - 引入 `use super::*;` 应已存在（确认 tests.rs 文件已有模块声明 `use super::*;`，若无则在测试函数所在 mod 头部添加）
    - 构造 ParseErrorRecord 时使用 `crate::error::ParseErrorRecord { line_number: 1, raw_truncated: "fake".to_string(), kind: crate::error::ErrorKind::ParseFailed }` 风格（确认 ParseErrorRecord 字段顺序后再写）—— 若字段不同，按 src/error.rs 实际定义调整
    - 使用 `tempfile::NamedTempFile` 创建临时 error log 文件路径
    - **重要**：write_error_log 是 src/cli/run/mod.rs 内的私有函数（`fn write_error_log`），tests.rs 通过 `use super::*;` 访问。若现有 tests.rs 已有 `use super::*;`，无需再加；若没有，添加到 tests.rs 顶部。

    步骤 3：在每个新测试上方加 `// WATCH-07` / `// WATCH-08` / `// WATCH-09` 注释标明追踪需求。

    步骤 4：运行 `cargo fmt`，确保格式合规。运行 `cargo clippy --all-targets -- -D warnings`，修复任何 clippy 警告（项目要求 -D warnings）。运行 `cargo test --lib && cargo test`，全部通过。

    步骤 5：本任务不修改 tests/integration.rs（避免与 Phase 2 计划重叠 —— QUAL-02 才是集成测试补全）。所有 4 个测试均放 src/ 内的单元测试模块。
  </action>
  <verify>
    <automated>cargo fmt --check &amp;&amp; cargo clippy --all-targets -- -D warnings &amp;&amp; cargo test test_watch_csv_append test_watch_error_log_append test_write_error_log_run_still_truncates test_handle_watch_returns_interrupted -- --test-threads=1</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -nE 'fn test_watch_csv_append' src/cli/watch/mod.rs` 返回 1 行
    - Source assertion: `grep -nE 'fn test_watch_error_log_append' src/cli/watch/mod.rs` 返回 1 行
    - Source assertion: `grep -nE 'fn test_handle_watch_returns_interrupted' src/cli/watch/mod.rs` 返回 1 行
    - Source assertion: `grep -nE 'fn test_write_error_log_run_still_truncates' src/cli/run/tests.rs` 返回 1 行
    - Test-command assertion: `cargo test test_watch_csv_append -- --test-threads=1` 退出码 0
    - Test-command assertion: `cargo test test_watch_error_log_append -- --test-threads=1` 退出码 0
    - Test-command assertion: `cargo test test_write_error_log_run_still_truncates -- --test-threads=1` 退出码 0
    - Test-command assertion: `cargo test test_handle_watch_returns_interrupted -- --test-threads=1` 退出码 0
    - Test-command assertion: `cargo test` 全套通过（无回归）
    - Test-command assertion: `cargo clippy --all-targets -- -D warnings` 退出码 0
    - Test-command assertion: `cargo fmt --check` 退出码 0
    - Behavior assertion: test_watch_csv_append 在 Task 2 注入被移除时应失败（防回归契约 —— 如果第二次 trigger_full_file 覆盖第一次内容，CSV 行数 < 期望值）
    - Behavior assertion: test_write_error_log_run_still_truncates 在 Task 1 OpenOptions 分支被反转时应失败（防回归契约 —— append_error_log=false 应 truncate）
  </acceptance_criteria>
  <done>
    - 4 个新测试函数在指定文件存在并按预期断言
    - cargo test、cargo clippy --all-targets -- -D warnings、cargo fmt --check 全套绿色
    - 4 个测试均验证 Task 1 + Task 2 的预期行为，且在实现回归时会失败
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 文件系统 → write_error_log | OpenOptions 调用接收 cfg.error.file 路径（来自用户 TOML 配置），文件路径已在 Phase 39 验证流程中校验非恶意输入 |
| SIGINT → handle_watch | 操作系统信号 → AtomicBool（已由 Phase 69 设置），handle_watch 仅读取布尔值，无信号处理代码注入风险 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | write_error_log OpenOptions 路径 | accept | 路径在 Phase 39/47 validate 已检查，append 模式不放大攻击面（追加 vs 截断都依赖同一路径）|
| T-01-02 | Denial of Service | error log 追加模式无限增长 | accept | watch 长进程可能让 error log 无限制增长；DOC-04 (Phase 3) 已计划告知用户配置日志轮转工具（logrotate）。本 phase 不实现内置 rotation（Out of Scope per REQUIREMENTS.md：deferred）|
| T-01-03 | Information Disclosure | append 模式保留旧错误内容 | accept | 错误日志本就含 SQL 内容（与单次写一致），追加模式不增加敏感数据暴露面 |

本 phase 不引入新外部包，无 Package Legitimacy Gate 需求（Cargo.toml 不变）。
</threat_model>

<verification>
- 编译通过：`cargo build --release` 退出码 0
- 测试全绿：`cargo test` 通过（含 4 个新测试 + 现有 ~880 测试）
- 静态分析无警告：`cargo clippy --all-targets -- -D warnings` 退出码 0
- 格式合规：`cargo fmt --check` 退出码 0
- 手动验证（per VALIDATION.md Manual-Only）：`cargo run -- watch -c config.toml` → Ctrl+C → `echo $?` 输出 130
</verification>

<success_criteria>
- WATCH-07：watch 触发 CSV exporter 后多次触发，output.csv 行数累计，header 仅一行（test_watch_csv_append 绿）
- WATCH-08：watch 多次触发产生 parse error 后，error log 含所有历史错误（test_watch_error_log_append 绿）；run 子命令 write_error_log 仍 truncate（test_write_error_log_run_still_truncates 绿）
- WATCH-09：interrupted=true 时 handle_watch 返回 Err(Error::Interrupted)（test_handle_watch_returns_interrupted 绿）；手动 Ctrl+C 退出码 130
- 三道质量门禁全绿：cargo build --release、cargo clippy --all-targets -- -D warnings、cargo fmt --check、cargo test
- 不引入新 Cargo 依赖（Cargo.toml diff 为空）
</success_criteria>

<output>
Create `.planning/phases/01-watch/01-01-SUMMARY.md` when done
</output>
