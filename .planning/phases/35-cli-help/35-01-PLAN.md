---
phase: 35
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - src/cli/opts.rs
requirements:
  - UX-03
autonomous: true
must_haves:
  truths:
    - "`sqllog2db --help` prints 4 DaMeng scenario examples (basic export, SQL indicators filter, config generation, config validation)"
    - "`sqllog2db run --help` prints run-specific examples (custom config path, stdin pipe placeholder)"
    - "`sqllog2db init --help` prints init example with --force override"
    - "`sqllog2db validate --help` prints validate example"
    - "Each subcommand has long_about text more detailed than current about"
    - "Key args (--config, --output) have help text explaining format/usage"
  artifacts:
    - "src/cli/opts.rs — modified with after_help, long_about, help attributes"
  key_links: []
---

# Plan: CLI --help 增强

**Phase:** 35
**Requirements:** UX-03
**Goal:** 通过 clap `after_help` 在 `--help` 输出添加达梦场景的实用示例，提升新用户上手体验

## Tasks

### Wave 1 (all modify `src/cli/opts.rs`, sequential)

---

<task type="auto">
<files>src/cli/opts.rs</files>
<read_first>
- src/cli/opts.rs — current Cli struct clap derive configuration
- .planning/phases/35-cli-help/35-CONTEXT.md — decisions D-01 through D-10
</read_first>
<action>
Add `after_help` attribute to the `Cli` struct's `#[command()]` attribute.

The `after_help` string contains 4 examples in cargo/crates.io style (no "$" prefix, short description above each example):
  1. Basic export: `sqllog2db run -c config.toml` — simplest full export flow
  2. SQL indicators filter: run with `[pipeline.filters.indicators]` configured
  3. Config generation: `sqllog2db init -o config.toml` — generate config file
  4. Config validation: `sqllog2db validate -c config.toml` — verify config correctness

Format constraints:
- Use clap's `after_help` attribute (string literal or const str)
- Cargo help style: under an "EXAMPLES" heading, each command preceded by an indented description line
- All text in English (D-05)
- Do NOT add `value_hint` (D-09)
</action>
<verify>
<automated>
cargo run -- --help 2>&1 | grep -c "EXAMPLES"
cargo run -- --help 2>&1 | grep -c "sqllog2db run"
cargo run -- --help 2>&1 | grep -c "sqllog2db init"
cargo run -- --help 2>&1 | grep -c "sqllog2db validate"
cargo clippy --all-targets -- -D warnings
</automated>
</verify>
<done>
- `sqllog2db --help` output ends with "EXAMPLES" section containing all 4 examples
- Examples use cargo style: description line + indented command, no "$ " prefix
- `cargo clippy --all-targets -- -D warnings` produces no new warnings
</done>
</task>

---

<task type="auto">
<files>src/cli/opts.rs</files>
<read_first>
- src/cli/opts.rs — Commands enum definition (Run, Init, Validate variants)
- .planning/phases/35-cli-help/35-CONTEXT.md — D-02 (per-subcommand examples), D-04, D-10 (Phase 37 placeholder)
</read_first>
<action>
Add `after_help` and `long_about` to the three Commands enum variants:

Run:
  - `after_help` with 2 examples:
    a) Custom --config path: `sqllog2db run -c /path/to/config.toml`
    b) Placeholder: "Pipe log data via stdin (requires --input flag)." with `// TODO(Phase 37): replace with actual stdin pipe example` comment above
  - Expand doc comment to `long_about` describing Run's input/output/filter pipeline

Init:
  - `after_help` with 1 example: `sqllog2db init -o myconfig.toml --force` explaining force overwrite
  - `long_about` describing config generation behavior

Validate:
  - `after_help` with 1 example: `sqllog2db validate -c config.toml` noting valid config message
  - `long_about` describing validation behavior

Format: cargo style, no "$ " prefix, English (D-05), no --input flag in Run examples
</action>
<verify>
<automated>
cargo run -- run --help 2>&1 | grep -c "config"
cargo run -- init --help 2>&1 | grep -c "force"
cargo run -- validate --help 2>&1 | grep -c "config.toml"
grep -c "TODO(Phase 37)" src/cli/opts.rs
cargo clippy --all-targets -- -D warnings
</automated>
</verify>
<done>
- `sqllog2db run --help` shows Run-specific examples (custom config path, stdin placeholder)
- `sqllog2db init --help` shows init example with --force override
- `sqllog2db validate --help` shows validate example
- `src/cli/opts.rs` contains `// TODO(Phase 37):` comment marking stdin example placeholder
- Each subcommand has `long_about` text more detailed than current about
- `cargo clippy --all-targets -- -D warnings` produces no new warnings
</done>
</task>

---

<task type="auto">
<files>src/cli/opts.rs</files>
<read_first>
- src/cli/opts.rs — all current `#[arg()]` attributes
- .planning/phases/35-cli-help/35-CONTEXT.md — D-08 (arg help), D-11 (config section reference)
</read_first>
<action>
Add `help` text to key arguments:

  1. Run.config: `#[arg(help = "TOML configuration file path. See [csv], [sqlite], [pipeline] sections.")]`
  2. Init.output: `#[arg(help = "Path for the generated default configuration file.")]`
  3. Validate.config: `#[arg(help = "TOML configuration file path to validate.")]`
  4. Cli.verbose: `#[arg(help = "-v for debug logging, -vv for trace logging.")]`

Add config section reference:
  - In `sqllog2db run --help` long_about or after_help tail, add:
    "Configuration file sections: [csv] / [sqlite] for output, [pipeline] for filters (include, exclude, indicators, sql)."
  - Use D-11's three main sections: csv, sqlite, pipeline
  - Mention pipeline filters: include, exclude, indicators, sql
</action>
<verify>
<automated>
cargo run -- --help 2>&1 | grep -c "TOML configuration"
cargo run -- --help 2>&1 | grep -c "debug logging"
cargo run -- init --help 2>&1 | grep -c "generated default"
cargo run -- run --help 2>&1 | grep -c "csv.*sqlite.*pipeline"
cargo run -- run --help 2>&1 | grep -c "include.*exclude.*indicators.*sql"
cargo clippy --all-targets -- -D warnings
cargo test
</automated>
</verify>
<done>
- `sqllog2db --help` shows `--config` arg with TOML format and config section reference
- `sqllog2db --help` shows `-v` arg with debug/trace level description
- `sqllog2db init --help` shows `--output` arg with config generation description
- `sqllog2db run --help` after_help or long_about tail shows `[csv] / [sqlite] / [pipeline]` section reference
- Config section reference lists include/exclude/indicators/sql filters for pipeline
- `cargo clippy --all-targets -- -D warnings` produces no new warnings
- `cargo test` all pass
</done>
</task>

---

## Verification

Phase-level verification after all tasks complete:

| # | Check | Method |
|---|-------|--------|
| 1 | `sqllog2db --help` shows 4 examples | `cargo run -- --help` |
| 2 | `sqllog2db run --help` shows run examples | `cargo run -- run --help` |
| 3 | `sqllog2db init --help` shows init example | `cargo run -- init --help` |
| 4 | `sqllog2db validate --help` shows validate example | `cargo run -- validate --help` |
| 5 | Cargo-style indent, no "$ " prefix | Visual inspection |
| 6 | `cargo clippy --all-targets -- -D warnings` passes | CLI |
| 7 | `cargo test` all pass | CLI |
| 8 | `TODO(Phase 37)` comment exists | `grep 'TODO(Phase 37)' src/cli/opts.rs` |
| 9 | Config section reference in run --help | `cargo run -- run --help` output contains csv/sqlite/pipeline |

## Undo Plan

```bash
git checkout -- src/cli/opts.rs
```
