---
phase: 43-parser-api-filter
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pipeline/filters/mod.rs
  - src/cli/run/prescan.rs
  - src/pipeline/mod.rs
  - src/cli/run/filter_processor.rs
  - src/cli/run/processor.rs
  - src/exporter/mod.rs
  - src/exporter/sqlite/write.rs
  - src/exporter/csv/writer.rs
autonomous: true
requirements: [PARSER-02]
must_haves:
  truths:
    - "D-01: IndicatorFilters::matches 签名接受 row_count: u32，prescan.rs 调用方无需 i64::from(result.rowcount)，冗余手动映射代码已删除"
    - "D-02: prescan.rs 的 collect 变通注释不再提及 'v1.1.0'，准确说明 LogIterator 未实现 rayon trait 是版本无关的事实"
    - "源码中不再存在 'v1.1.0' 字样（除 CHANGELOG/historical doc 外）"
    - "cargo test 全部通过，无任何回归"
    - "cargo clippy --all-targets -- -D warnings 通过"
  artifacts:
    - path: "src/pipeline/filters/mod.rs"
      provides: "IndicatorFilters::matches 新签名 (row_count: u32)"
      contains: "row_count: u32"
    - path: "src/cli/run/prescan.rs"
      provides: "无 i64::from 转换的 matches 调用，更新后的 collect 注释"
      contains: "filters.indicators.matches(result.exec_id, result.exectime, result.rowcount)"
  key_links:
    - from: "src/cli/run/prescan.rs"
      to: "src/pipeline/filters/mod.rs::IndicatorFilters::matches"
      via: "直接调用，类型 u32 直传"
      pattern: "filters\\.indicators\\.matches\\(result\\.exec_id, result\\.exectime, result\\.rowcount\\)"
---

<objective>
利用 dm-database-parser-sqllog 2.0.0 的字段类型信息（rowcount: u32 已确认），消除项目中残留的 `i64::from(result.rowcount)` 变通转换；同时更新 prescan.rs 中过时的 "v1.1.0" 注释，使其准确反映 2.0.0 的实际情况。

Purpose: 删除冗余的手动类型映射代码，使过滤路径签名与 parser 库的实际类型对齐（PARSER-02 / D-01 / D-02）。
Output: IndicatorFilters::matches 签名调整 + 调用点更新 + 注释清理。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/43-parser-api-filter/43-CONTEXT.md
@.planning/phases/43-parser-api-filter/43-RESEARCH.md
@.planning/phases/43-parser-api-filter/43-VALIDATION.md

# 必读源文件（理解现状）
@src/pipeline/filters/mod.rs
@src/cli/run/prescan.rs

<interfaces>
<!-- 关键签名（重构前）来自 src/pipeline/filters/mod.rs -->
当前 IndicatorFilters::matches 签名:
  pub fn matches(&self, exec_id: i64, runtime_ms: f32, row_count: i64) -> bool

当前 prescan.rs 第 31-35 行调用:
  filters.indicators.matches(
      result.exec_id,
      result.exectime,
      i64::from(result.rowcount),  // 需要消除此转换
  )

当前 prescan.rs 第 24 行注释:
  // 收集到 Vec 再并行处理（LogParser 未实现 rayon 的 IntoParallelRefIterator，需先 collect 到 Vec 再并行）

研究员已确认 2.0.0 Sqllog 字段类型:
  - exec_id: i64
  - exectime: f32
  - rowcount: u32  ← 与 IndicatorFilters.min_row_count: Option<u32> 一致
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 调整 IndicatorFilters::matches 签名为 row_count: u32</name>
  <files>src/pipeline/filters/mod.rs</files>
  <read_first>
    - src/pipeline/filters/mod.rs（理解 IndicatorFilters::matches 当前实现，确认 min_row_count: Option<u32> 的存储类型）
    - .planning/phases/43-parser-api-filter/43-RESEARCH.md（Code Examples 节给出了修改前后对比）
  </read_first>
  <behavior>
    - Test 1: `IndicatorFilters { min_row_count: Some(100), .. }.matches(0, 0.0_f32, 100_u32)` 返回 true
    - Test 2: `IndicatorFilters { min_row_count: Some(100), .. }.matches(0, 0.0_f32, 99_u32)` 返回 false
    - Test 3: `IndicatorFilters { exec_ids: Some(set with 42), .. }.matches(42, 0.0_f32, 0_u32)` 返回 true
    - Test 4: `IndicatorFilters::default().matches(1, 9999.0_f32, 9999_u32)` 返回 false（has_filters() = false 短路）
    - 现有 4 个 indicator_matches_* 测试用 u32 字面量调整后全部继续通过
  </behavior>
  <action>
    将 IndicatorFilters::matches 的第三个参数从 `row_count: i64` 改为 `row_count: u32`。函数体内将 `if row_count >= i64::from(min_r)` 改为 `if row_count >= min_r`（min_r 已是 u32）。同步更新模块内 #[cfg(test)] tests 模块中的 4 个 indicator 测试（test_indicator_matches_exec_id、test_indicator_matches_min_runtime、test_indicator_matches_min_row_count、test_indicator_no_filters_always_false）的字面量从 `0` / `100` / `99` / `9999` 改为带类型后缀的 `0_u32` / `100_u32` / `99_u32` / `9999_u32`，使 Rust 类型推导明确。不修改其他逻辑（exec_id 仍为 i64，runtime_ms 仍为 f32，min_runtime_ms / min_row_count 字段类型不变）。
  </action>
  <verify>
    <automated>cargo test --lib -- pipeline::filters::tests::test_indicator</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "row_count: u32" src/pipeline/filters/mod.rs` 命中且为 IndicatorFilters::matches 的参数定义
    - `grep -c "i64::from(min_r)" src/pipeline/filters/mod.rs` 输出 0
    - `cargo test --lib -- pipeline::filters::tests::test_indicator` 4 个测试全部通过
    - Task 2 的 `cargo build --release` 将验证整体编译通过；执行者无需在本 task 单独运行 `cargo build`（Task 1 结束时 prescan.rs 尚未更新，全量编译会失败，属预期，由 Task 2 修复）
  </acceptance_criteria>
  <done>
    IndicatorFilters::matches 接受 u32 类型 row_count；模块内单元测试通过；编译错误集中在调用方（prescan.rs）。
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 更新 prescan.rs 调用点 + 修订过时注释 + 清理 v1.1.0 注释残留</name>
  <files>src/cli/run/prescan.rs, src/pipeline/mod.rs, src/cli/run/filter_processor.rs, src/cli/run/processor.rs, src/exporter/mod.rs, src/exporter/sqlite/write.rs, src/exporter/csv/writer.rs</files>
  <read_first>
    - src/cli/run/prescan.rs（理解 collect 注释上下文以及调用 matches 的位置）
    - src/pipeline/mod.rs（v1.1.0 注释位置：第 155 行附近）
    - src/cli/run/filter_processor.rs（v1.1.0 注释位置：第 53 行附近）
    - src/cli/run/processor.rs（v1.1.0 注释位置：第 68 行附近）
    - src/exporter/mod.rs（v1.1.0 注释位置：第 26 行附近）
    - src/exporter/sqlite/write.rs（v1.1.0 注释位置：第 5 行附近）
    - src/exporter/csv/writer.rs（v1.1.0 注释位置：第 211 行附近）
    - .planning/phases/43-parser-api-filter/43-RESEARCH.md（Pitfall 5 解释 collect 注释如何写）
  </read_first>
  <behavior>
    - Test 1: `cargo build --release` 编译成功无 warning
    - Test 2: `cargo test --lib -- filter` 50 个 filter 测试全部通过（基线保持）
    - Test 3: `cargo test` 全套通过（无功能回归）
    - Test 4: `cargo clippy --all-targets -- -D warnings` 通过
  </behavior>
  <action>
    第一步（prescan.rs 调用点）：将 prescan.rs::scan_log_file_for_matches 中 filters.indicators.matches 的第三个参数从 `i64::from(result.rowcount)` 改为 `result.rowcount`（直接传 u32，与 Task 1 的新签名匹配）。
    第二步（prescan.rs collect 注释，第 24 行）：将注释从 "收集到 Vec 再并行处理（LogParser 未实现 rayon 的 IntoParallelRefIterator，需先 collect 到 Vec 再并行）" 修订为版本无关的准确表述："收集到 Vec 再并行处理（LogIterator 未实现 rayon::IntoParallelIterator trait，需先 collect 到 Vec 才能用 par_iter）"。删除任何 "v1.1.0" 字样。
    第三步（清理过时 v1.1.0 字样，全部为注释，不改逻辑）：在以下 6 个位置将注释中的 "v1.1.0" 替换为 "parser 库"（去除版本号语义），或重写注释让其与版本无关：
      - src/pipeline/mod.rs:155 注释 "v1.1.0 后所有字段均为栈上数据" → "parser 库提供物化后的字段（栈上数据）"
      - src/cli/run/filter_processor.rs:53 注释 "v1.1.0 已物化所有元数据" → "parser 库已物化所有元数据字段"
      - src/cli/run/processor.rs:68 注释 "v1.1.0 所有字段已物化" → "parser 库已物化所有字段"
      - src/exporter/mod.rs:26 注释 "v1.1.0 后所有字段已物化在 Sqllog 上" → "parser 库已将所有字段物化到 Sqllog"
      - src/exporter/sqlite/write.rs:5 注释 "v1.1.0 所有字段已物化" → "parser 库已物化所有字段"
      - src/exporter/csv/writer.rs:211 注释 "v1.1.0 所有字段已物化" → "parser 库已物化所有字段"
    第四步：运行 `cargo fmt` 保证格式。
    不修改任何业务逻辑，仅修改注释和签名调用点。不删除 `// 收集到 Vec` 这一行（仍需保留并行解释），仅替换其内容。
  </action>
  <verify>
    <automated>cargo build --release 2>&1 | grep -E "warning|error" | grep -v "^$" || echo "OK no warnings/errors"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "i64::from(result.rowcount)" src/cli/run/prescan.rs` 输出 0
    - `grep -n "result.rowcount" src/cli/run/prescan.rs` 命中且无 i64::from 包裹
    - `grep -rn "v1\.1\.0" src/` 输出空（src/ 下不再有 v1.1.0 字样）
    - `grep -n "LogIterator 未实现 rayon" src/cli/run/prescan.rs` 命中（新注释存在）
    - `cargo build --release` 输出不含 `warning:` 或 `error:`
    - `cargo test --lib -- filter` 通过，至少 50 个 filter 测试 ok
    - `cargo test` 全套通过
    - `cargo clippy --all-targets -- -D warnings` 通过
    - `cargo fmt --check` 通过
  </acceptance_criteria>
  <done>
    所有调用点已切换到 u32；prescan.rs 注释准确反映 2.0.0 现状；源码中无 v1.1.0 残留；全套质量门禁通过。
  </done>
</task>

</tasks>

<verification>
全 Plan 完成后：
- `cargo build --release` 无 warning/error
- `cargo test` 全部通过（filter 模块 ≥ 50 个，整体 ≥ 487 个）
- `cargo clippy --all-targets -- -D warnings` 通过
- `cargo fmt --check` 通过
- `git diff --stat src/` 显示净行数变化（预期：删除约 1-2 行 i64::from + 注释微调）
</verification>

<success_criteria>
- PARSER-02 满足：IndicatorFilters::matches 不再要求调用方做 i64::from(u32) 的冗余类型转换
- D-01 / D-02 满足：删除变通注释和冗余映射代码
- D-08 满足：clippy + fmt 无新增警告
- 无功能回归：所有现有测试继续通过
</success_criteria>

<output>
Create `.planning/phases/43-parser-api-filter/43-01-SUMMARY.md` when done
</output>
