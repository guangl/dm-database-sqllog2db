---
phase: 24-svg
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - site/src/index.md
autonomous: true
requirements: [I18N-02, DESVG-02]

must_haves:
  truths:
    - 用户打开 GitHub Pages 落地页看到的是全中文内容
    - GitHub Pages 落地页中没有 SVG Gallery 段落
    - 落地页中的架构图、演示录像、链接等功能性内容完整保留
  artifacts:
    - path: "site/src/index.md"
      provides: "GitHub Pages 落地页（中文，去 SVG）"
      min_lines: 80
  key_links:
    - from: "site/src/index.md"
      to: "GitHub Pages 渲染"
      via: "mdBook build"
      pattern: "^Built with"
---

<objective>
将 GitHub Pages 落地页（site/src/index.md）翻译为全中文，移除其中完整的 SVG Gallery 段落（四类图表的嵌入式 SVG 代码）。

Purpose: 实现 GitHub Pages 落地页的中文化，DBA 通过项目展示页了解工具全貌时不再看到英文和 SVG 图表。
Output: 全中文的 site/src/index.md，无 SVG Gallery 残留。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@site/src/index.md

## 术语一致性约定

与 Plan 01（24-01-PLAN.md）共用相同的术语翻译表。关键对照：

| 英文 | 中文 |
|------|------|
| Dameng / DM Database | 达梦数据库 |
| pipeline | 处理管道 |
| exporter | 导出器 |
| filter | 过滤器 |
| template | 模板 |
| chart | 图表 |
| record | 记录 |
| fingerprint | 指纹 |
| aggregation | 聚合 |
| throughput | 吞吐量 |
| feature | 功能特性 |
| streaming | 流式处理 |
| binary | 二进制文件 |

## 需要执行的具体翻译规则

1. 所有正文、标题、列表、表格翻译为中文
2. 代码块、命令、API 引用、徽章行保持原样
3. 链接保持原样（内部相对路径和外部 URL 都不改）
4. 保持 Markdown 格式结构完整
5. asciinema-player 的 script/link 标签保持原样
6. Architecture 章节的 ASCII art 流程图保持原样（内容是对齐文本，不翻译）
</context>

<interfaces>
<!-- site/src/index.md 当前结构概览（已从上下文提取） -->
<!-- 约641行，包含如下主要章节（按顺序）：
 1. 项目标题 + 徽章行（lines 1-8）
 2. 英文摘要 + 分隔线（lines 9-12）
 3. ## Installation（lines 16-24）
 4. ## Feature Overview（lines 31-39，四个段落） ← 翻译并缩减为中文表述
 5. ## Architecture（lines 45-68，ASCII art 流程图 + 文字说明） ← 翻译文字部分，保留 ASCII art
 6. ## Performance（lines 73-81，基准测试表格）
 7. ## SVG Chart Gallery（lines 86-610）← 整个删除，用纯文字描述替代
 8. ## Demo（lines 616-625，asciinema-player）
 9. ## Links（lines 628-637）
 10. 尾部注释（line 640）
-->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 翻译 site/src/index.md 为中文并移除 SVG Gallery</name>
  <files>site/src/index.md</files>
  <read_first>site/src/index.md</read_first>
  <action>
    将 site/src/index.md 全部翻译为中文。具体变更包含四个层面：

    **1. 标题翻译：**
    - "High-performance SQL log parser for DaMeng Database" → 翻译为中文摘要
    - 所有章节标题翻译为中文：Installation → 安装、Feature Overview → 功能概览、Architecture → 架构、Performance → 性能、Demo → 演示、Links → 链接

    **2. Feature Overview 翻译：**
    四个段落的纯文本翻译：
    - "Parsing & Export" 段落 → 解析与导出
    - "Filtering & Field Control" 段落 → 过滤与字段控制
    - "Template Analysis & Charts" 段落 → 模板分析与图表
    - "Configuration & Performance" 段落 → 配置与性能

    **3. 完整移除 SVG Gallery 段落（DESVG-02）：**
    删除从 `<!-- SVG Gallery -->`（第84行）到下一个 `---` 分隔线或 `<!-- Demo -->` 之前的所有内容（第86-610行），包括：
    - "## SVG Chart Gallery" 标题
    - 四个 `<details>` 区块展开的全部 SVG 代码
    - 每个 detail 的 `<summary>` 和说明文字
    - 所有 `<svg>` 标签及其内容

    替换为纯文字描述（D-03）：
    "本工具内置 SQL 模板分析引擎，可自动生成四类 SVG 图表：频率柱状图（Top-N SQL 模板按执行次数排序）、延迟直方图（每类模板的执行时间分布）、趋势折线图（SQL 执行频率随时间变化）和用户饼图（按数据库用户的查询占比）。图表通过 sqllog2db stats 命令配合 --chart 参数生成，输出到配置的 charts/ 目录。"

    **4. 保留的内容：**
    - Architecture 章节的 ASCII art 流程图（从 `SQL Log Files (.log)` 开始的文本图）— 不翻译流程图内文字
    - asciinema-player 的 `<script>`、`<link>`、`<asciinema-player>` 标签
    - 所有 Markdown 链接和 Badge 行
    - Demo 章节的 asciicast 下载链接
    - 尾部 "Built with mdBook..." 注释翻译为中文（"由 mdBook 构建。图表由 plotters 渲染。通过 GitHub Actions 部署。"）

    **5. 链接章节调整：**
    翻译所有链接描述文字为中文。原本 README 的链接指向保持不变。将 "Technical reference and QuickStart" 等描述翻译为中文。
  </action>
  <verify>
    <automated>
      # SVG Gallery section header removed
      if grep -q '## SVG Chart Gallery' site/src/index.md; then echo "FAIL: SVG Gallery section remains"; fi

      # Embedded SVG code removed (check for the first SVG element)
      if grep -q '<svg width="1200" height="600"' site/src/index.md; then echo "FAIL: Embedded SVG remains"; fi

      # Chinese content present
      if ! grep -q '功能概览' site/src/index.md; then echo "FAIL: Chinese section header not found"; fi
    </automated>
  </verify>
  <acceptance_criteria>
    - site/src/index.md 中不包含 "SVG Chart Gallery" 标题
    - site/src/index.md 中不包含任何 `<svg` 标签
    - site/src/index.md 中不包含 "Four chart types are generated" 等英文段落
    - SVG Gallery 位置被纯文字描述替代
    - Architecture、Demo、Links 章节保留且功能完整
    - ASCII art 流程图保留原样
    - asciinema-player 的 script 和 link 标签保留原样
    - 所有正文内容为中文
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This is a documentation-only plan with no runtime code changes. No new trust boundaries are introduced.

| Boundary | Description |
|----------|-------------|
| No code boundary | site/src/index.md 为 mdBook 源文件，不涉及代码执行路径 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-02 | Tampering | site/src/index.md content | mitigate | 仅修改 site/src/index.md，保留所有外部 script/link 标签原样。不修改 mermaid/asciinema 配置 |
| T-24-SC | Tampering | no packages | accept | 本计划不涉及任何包管理操作 |
</threat_model>

<verification>

全自动验证步骤：

```bash
# 1. SVG Gallery 标题已移除
! grep -q '## SVG Chart Gallery' site/src/index.md

# 2. SVG 标签已移除
! grep -q '<svg ' site/src/index.md

# 3. 纯文字描述已添加
grep -q '频率柱状图' site/src/index.md

# 4. ASCII art 流程图保留
grep -q 'SQL Log Files (.log)' site/src/index.md

# 5. asciinema-player 标签保留
grep -q 'asciinema-player' site/src/index.md

# 6. 架构描述章节标题翻译
grep -q '## 架构' site/src/index.md

# 7. 功能概览标题翻译
grep -q '## 功能概览' site/src/index.md
```

</verification>

<success_criteria>
- site/src/index.md 全部为中文，无英文段落残留
- site/src/index.md 中无 SVG Gallery 段落
- ASCII art 流程图、Demo 录像、链接保持原样
- 纯文字描述替代了原本的 SVG Gallery
</success_criteria>
