---
phase: 71-mod-rs-mod-rs-pub-use
plan: 08
type: execute
wave: 2
depends_on: []
files_modified:
  - src/exporter/sqlite/mod.rs
  - src/exporter/sqlite/exporter.rs
  - src/exporter/sqlite/impls.rs
  - src/exporter/sqlite/pragma.rs
autonomous: true
requirements: []
must_haves:
  truths:
    - "src/exporter/sqlite/mod.rs 仅含 mod 声明与 pub use 重导出"
    - "SqliteExporter struct 字段及可见性保持不变（含 pub(super) normalize/field_mask/ordered_indices）"
    - "Exporter trait 实现行为完全不变（事务、batch_size、PRAGMAs）"
    - "cargo test 通过（含 sqlite/tests.rs 全部测试）"
    - "cargo clippy --all-targets -- -D warnings 通过"
  artifacts:
    - path: "src/exporter/sqlite/mod.rs"
      provides: "module declarations + pub use only"
    - path: "src/exporter/sqlite/pragma.rs"
      provides: "initialize_pragmas 私有 helper"
      contains: "fn initialize_pragmas"
    - path: "src/exporter/sqlite/exporter.rs"
      provides: "SqliteExporter struct + 构造方法 (new, from_config) + 内部 helper (db_err, conn_ref, set_wal_mode, batch_commit_if_needed, handle_delete_clear_result, prepare_target_table) + Debug impl"
      contains: "pub(crate) struct SqliteExporter"
    - path: "src/exporter/sqlite/impls.rs"
      provides: "impl Exporter for SqliteExporter 块"
      contains: "impl Exporter for SqliteExporter"
  key_links:
    - from: "src/exporter/sqlite/mod.rs"
      to: "exporter / impls / pragma"
      via: "mod 声明 + pub use"
      pattern: "pub(crate) use exporter::SqliteExporter"
    - from: "src/exporter/mod.rs"
      to: "crate::exporter::sqlite::SqliteExporter"
      via: "pub(crate) use sqlite::SqliteExporter"
      pattern: "pub(crate) use sqlite::SqliteExporter"
---

<objective>
重构 src/exporter/sqlite/mod.rs（249 行）：将 SqliteExporter struct 与内部 helper 方法、Debug、initialize_pragmas 拆到 exporter.rs（含构造/工具方法）+ pragma.rs（PRAGMA 配置常量），把 `impl Exporter for SqliteExporter` trait 实现块拆到 impls.rs，mod.rs 仅保留 mod 声明与 pub use 重导出。sql_builder.rs / write.rs / tests.rs 不动。

Purpose: sqlite 子模块入口清晰化，按"数据 + 内部工具 vs trait 行为 vs PRAGMA 配置"切分。
Output: 4 个文件，cargo test/clippy 全绿。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md

<interfaces>
<!-- 必须保持的可见性 -->

pub(crate)：
- `struct SqliteExporter` 含字段：
  - database_url: String
  - table_name: String
  - insert_sql: String
  - overwrite: bool
  - append: bool
  - conn: Option<Connection>
  - stats: ExportStats
  - row_count: usize
  - batch_size: usize
  - pub(super) normalize: bool
  - pub(super) field_mask: FieldMask
  - pub(super) ordered_indices: Vec<usize>
- `SqliteExporter::new(database_url, table_name, overwrite, append) -> Self`
- `SqliteExporter::from_config(config: &SqliteExporterConfig) -> Self`
- `SqliteExporter::set_wal_mode() -> Result<()>`

私有：
- `fn initialize_pragmas(conn: &Connection) -> rusqlite::Result<()>`
- `fn SqliteExporter::db_err(reason) -> Error`
- `fn SqliteExporter::conn_ref(&self) -> Result<&Connection>`
- `fn SqliteExporter::batch_commit_if_needed(&mut self) -> Result<()>`
- `fn SqliteExporter::handle_delete_clear_result(...)`（关联函数）
- `fn SqliteExporter::prepare_target_table(&self) -> Result<()>`
- `impl Debug for SqliteExporter`
- `impl Exporter for SqliteExporter`

子模块（不动）：
- `mod sql_builder;`（私有，被 impls.rs 间接使用）
- `mod write;`（私有，被 impls.rs 间接使用）
- `#[cfg(test)] mod tests;`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 拆分 sqlite/mod.rs 到 exporter.rs + impls.rs + pragma.rs</name>
  <files>src/exporter/sqlite/mod.rs, src/exporter/sqlite/exporter.rs, src/exporter/sqlite/impls.rs, src/exporter/sqlite/pragma.rs</files>
  <read_first>
    - src/exporter/sqlite/mod.rs（全文 249 行）
    - src/exporter/sqlite/sql_builder.rs（确认 build_create_sql/build_insert_sql 签名）
    - src/exporter/sqlite/write.rs（确认 do_insert_preparsed 签名）
    - src/exporter/sqlite/tests.rs（确认 use 路径，预期 use super::SqliteExporter; 等）
    - src/exporter/mod.rs（确认 `pub(crate) use sqlite::SqliteExporter;`）
  </read_first>
  <action>
    Step 1: 创建 `src/exporter/sqlite/pragma.rs`：
      - 移入 mod.rs 第 30-42 行：
        ```rust
        use rusqlite::Connection;

        pub(super) fn initialize_pragmas(conn: &Connection) -> std::result::Result<(), rusqlite::Error> {
            conn.execute_batch(
                "PRAGMA journal_mode = OFF;
                 PRAGMA synchronous = OFF;
                 PRAGMA cache_size = 1000000;
                 PRAGMA locking_mode = EXCLUSIVE;
                 PRAGMA temp_store = MEMORY;
                 PRAGMA mmap_size = 30000000000;
                 PRAGMA page_size = 65536;
                 PRAGMA threads = 4;",
            )?;
            Ok(())
        }
        ```
      - 改为 `pub(super) fn`（让 exporter.rs 内 initialize 调用）

    Step 2: 创建 `src/exporter/sqlite/exporter.rs`：
      - 移入 mod.rs 第 15-29 行 + 第 44-158 行：
        - `pub(crate) struct SqliteExporter` 完整定义（字段可见性保持）
        - `impl Debug for SqliteExporter`
        - `impl SqliteExporter { new, from_config, db_err, conn_ref, set_wal_mode, batch_commit_if_needed, handle_delete_clear_result, prepare_target_table }`
      - 顶部 use：
        ```rust
        use super::super::stats::ExportStats;
        use crate::error::{Error, ExportError, Result};
        use rusqlite::Connection;
        ```
        说明：ExportStats 通过 `super::super::ExportStats` 访问（exporter 父模块的 pub use stats::ExportStats）
      - 可见性细节：以下方法 impls.rs 也需要调用 → 必须升级为 `pub(super)`：
        - `db_err` → `pub(super) fn db_err`（impls.rs 多处用到 Self::db_err）
        - `conn_ref` → `pub(super) fn conn_ref`
        - `batch_commit_if_needed` → `pub(super) fn batch_commit_if_needed`
        - `prepare_target_table` → `pub(super) fn prepare_target_table`
        - 其他（handle_delete_clear_result, set_wal_mode）不需要被 impls.rs 调用，保持原可见性
        - SqliteExporter 内字段 `insert_sql` `conn` `stats` `row_count` `table_name` `database_url` `ordered_indices` 等被 impls.rs 直接访问 → 因为 impls.rs 是兄弟文件位于同一父模块下，且字段已有 `pub(super)` 标注的会被访问。需要把 `insert_sql` `conn` `stats` `row_count` `table_name` `database_url` 6 个字段也加 `pub(super)` 让 impls.rs 可访问；本来私有的字段如 `overwrite` `append` `batch_size` 因 impls.rs 不直接访问可保持私有。
        - **实际审计：impls.rs 实际访问的字段（grep impls.rs 找 self.field 用法）：**
          - `self.export_one_normalized(...)` → 方法
          - `self.export_one_preparsed(...)` → 方法
          - `self.conn` → 字段（在 export_one_preparsed 内）
          - `self.insert_sql` → 字段
          - `self.normalize` → 字段（已 pub(super)）
          - `self.field_mask` → 字段（已 pub(super)）
          - `self.ordered_indices` → 字段（已 pub(super)）
          - `self.stats.record_success()` → 字段
          - `self.batch_commit_if_needed()` → 方法
          - `self.conn` → finalize 中
          - `self.database_url, self.stats.exported, self.stats.failed` → finalize 中
          - `self.stats` → stats_snapshot
        - 因此需要把 `conn`, `insert_sql`, `stats`, `database_url` 这 4 个字段也加 `pub(super)`
        - 其余 `table_name` `overwrite` `append` `row_count` `batch_size` 由 exporter.rs 内的方法封装访问，impls.rs 不直接触碰

    Step 3: 创建 `src/exporter/sqlite/impls.rs`：
      - 移入 mod.rs 第 160-246 行 `impl Exporter for SqliteExporter`
      - 顶部 use：
        ```rust
        use super::exporter::SqliteExporter;
        use super::pragma::initialize_pragmas;
        use super::sql_builder::{build_create_sql, build_insert_sql};
        use super::write::do_insert_preparsed;
        use super::super::{Exporter, ExportStats};
        use crate::error::Result;
        use dm_database_parser_sqllog::Sqllog;
        use log::info;
        use rusqlite::Connection;
        use std::path::Path;
        ```

    Step 4: 改写 `src/exporter/sqlite/mod.rs` 为：
      ```rust
      //! SQLite exporter：struct 与构造在 exporter.rs，trait impl 在 impls.rs，PRAGMA 配置在 pragma.rs。

      mod exporter;
      mod impls;
      mod pragma;
      mod sql_builder;
      mod write;

      #[cfg(test)]
      mod tests;

      pub(crate) use exporter::SqliteExporter;
      ```
      仅注释、mod 声明、pub use。

    Step 5: 运行质量门禁：
      - `cargo build`
      - `cargo clippy --all-targets -- -D warnings`
      - `cargo test`
      - `cargo fmt`

    可见性回顾（汇总）：
      - SqliteExporter struct 仍 `pub(crate)`
      - 字段升级为 `pub(super)`：`database_url`、`insert_sql`、`conn`、`stats`、（保留 `normalize` `field_mask` `ordered_indices` 已有 pub(super)）
      - 方法升级为 `pub(super)`：`db_err`、`conn_ref`、`batch_commit_if_needed`、`prepare_target_table`
      - `set_wal_mode` 保持 `pub(crate)`（被 ExporterManager 调用）
      - `from_config`、`new` 保持 `pub(crate)`
      - `initialize_pragmas` 改为 `pub(super)`（被 impls.rs 调用）
  </action>
  <verify>
    <automated>cargo clippy --all-targets -- -D warnings && cargo test</automated>
    <grep-check>grep -v '^\s*//' src/exporter/sqlite/mod.rs | grep -vE '^\s*$' | grep -E '\bfn \w+|\bstruct \w+|\bimpl |\benum |\btrait ' && echo "FAIL: mod.rs 仍含实现" || echo "OK: mod.rs 仅声明"</grep-check>
  </verify>
  <acceptance_criteria>
    - cargo clippy --all-targets -- -D warnings 退出码 0
    - cargo test 退出码 0
    - src/exporter/sqlite/mod.rs 过滤注释/空行后只含 `mod`/`pub(crate) use`/属性，无 `fn`/`struct`/`impl`/`trait`/`enum`
    - SqliteExporter 在 crate::exporter::sqlite::SqliteExporter 和 crate::exporter::SqliteExporter 两条路径下都可达
    - sqlite/tests.rs 内容不变且测试全部通过
    - sql_builder.rs / write.rs 文件内容完全不变
    - 集成测试中的 SQLite 导出 + WAL 模式行为不变
  </acceptance_criteria>
  <done>
    mod.rs 简化到 ≤ 15 行（含注释），exporter.rs / impls.rs / pragma.rs 三个文件承接全部实现，质量门禁全绿。
  </done>
</task>

</tasks>

<verification>
- cargo clippy --all-targets -- -D warnings 通过
- cargo test 通过
- src/exporter/sqlite/mod.rs grep -E '\bfn |\bstruct |\bimpl |\btrait |\benum ' 无匹配
- src/exporter/sqlite/{sql_builder.rs,write.rs,tests.rs} 内容不变
</verification>

<success_criteria>
- mod.rs 仅含 mod 声明与 pub use（≤ 15 行）
- exporter.rs（数据 + 工具）+ impls.rs（trait 行为）+ pragma.rs（PRAGMA 配置）三文件承接全部实现
- 全套质量门禁通过
</success_criteria>

<output>
Create `.planning/phases/71-mod-rs-mod-rs-pub-use/71-08-SUMMARY.md` when done
</output>
