---
phase: 71-mod-rs-mod-rs-pub-use
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/cli/stats/mod.rs
  - src/cli/stats/handler.rs
  - src/cli/stats/tests.rs
autonomous: true
requirements: []
must_haves:
  truths:
    - "src/cli/stats/mod.rs 仅含 mod 声明与 pub use 重导出，无任何 fn/struct/impl 实现"
    - "cargo test 全部通过且 handle_stats 的所有原有公开行为不变"
    - "cargo clippy --all-targets -- -D warnings 通过，0 warnings"
  artifacts:
    - path: "src/cli/stats/mod.rs"
      provides: "module declarations + pub use re-exports only"
    - path: "src/cli/stats/handler.rs"
      provides: "handle_stats + merge_stats_options 函数实现"
      contains: "pub fn handle_stats"
    - path: "src/cli/stats/tests.rs"
      provides: "merge_stats_options / handle_stats 全部 8 个单元测试"
      contains: "#[cfg(test)]"
  key_links:
    - from: "src/cli/stats/mod.rs"
      to: "src/cli/stats/handler.rs"
      via: "mod handler; pub use handler::handle_stats"
      pattern: "pub use handler::handle_stats"
    - from: "main.rs (Stats 分支)"
      to: "crate::cli::stats::handle_stats"
      via: "pub use re-export"
      pattern: "cli::stats::handle_stats"
---

<objective>
重构 src/cli/stats/mod.rs（147 行）：将 `handle_stats` 与 `merge_stats_options` 函数实现从 mod.rs 拆分到 handler.rs，将 #[cfg(test)] mod tests 块拆分到 tests.rs，mod.rs 仅保留模块声明与 pub use。

Purpose: 让 mod.rs 回归"仅模块声明"角色，提升可读性与模块边界清晰度。stats 是最小最独立的 mod.rs，作为 Wave 1 入手最稳。
Output: 3 个文件（mod.rs 极简化 + handler.rs + tests.rs），cargo test/clippy 全绿。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md

<interfaces>
<!-- 当前 src/cli/stats/mod.rs 的公开 API 必须保持不变 -->

外部调用方（src/main.rs Stats 分支）使用：
```rust
crate::cli::stats::handle_stats(&cfg, top, from, to)
```

handle_stats 签名：
```rust
pub fn handle_stats(
    cfg: &Config,
    top: Option<u32>,
    from: Option<String>,
    to: Option<String>,
) -> Result<()>
```

merge_stats_options 是私有 fn（仅模块内部 + tests 使用）：
```rust
fn merge_stats_options(
    cfg: &Config,
    cli_top: Option<u32>,
    cli_from: Option<String>,
    cli_to: Option<String>,
) -> (u32, Option<String>, Option<String>)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 拆分 stats/mod.rs 到 handler.rs + tests.rs</name>
  <files>src/cli/stats/mod.rs, src/cli/stats/handler.rs, src/cli/stats/tests.rs</files>
  <read_first>
    - src/cli/stats/mod.rs（全文 147 行）
    - src/main.rs（grep 'cli::stats::' 找调用点，确认依赖的导出路径）
    - src/cli/mod.rs（确认 stats 子模块怎么声明）
  </read_first>
  <action>
    Step 1: 创建 `src/cli/stats/handler.rs`：
      - 把 mod.rs 第 1-41 行的所有 use 导入 + `merge_stats_options` 函数 + `handle_stats` 函数原样移到 handler.rs
      - `merge_stats_options` 保持 `fn`（私有，但需让同模块 tests 可见 → 使用 `pub(super) fn merge_stats_options`，因为 tests.rs 用 `use super::*;` 通过 mod.rs 再到 handler.rs 路径访问；或直接保留 `fn` 并在 tests.rs 中用 `use super::handler::merge_stats_options;`。任选其一保持简洁，建议改为 `pub(super) fn`）
      - `handle_stats` 保持 `pub fn`
      - 顶部 use 完整迁移：`use crate::config::Config; use crate::error::Result;`

    Step 2: 创建 `src/cli/stats/tests.rs`：
      - 把 mod.rs 第 43-147 行的 `#[cfg(test)] mod tests { ... }` 块内的内容原样迁移
      - 不要再嵌套 `mod tests`——tests.rs 作为整文件就是 tests 模块，顶部加 `#![cfg(test)]` 或在 mod.rs 用 `#[cfg(test)] mod tests;`
      - 文件顶部：`use super::handler::{handle_stats, merge_stats_options};` + `use crate::config::{Config, CsvExporterConfig, ExporterConfig, SqllogConfig};`
      - 保留全部 8 个测试函数（test_handle_stats_top_default_passes / test_handle_stats_top_nonzero_passes / test_handle_stats_cli_none_config_none_falls_back_to_20 / test_handle_stats_cli_top_overrides_config_top / test_handle_stats_config_top_used_when_cli_none / test_merge_stats_options_cli_top_priority / test_merge_stats_options_config_top_fallback / test_merge_stats_options_default_fallback）以及 make_test_config_with_log helper

    Step 3: 把 `src/cli/stats/mod.rs` 整文件重写为：
      ```rust
      //! Stats 子命令入口：合并 CLI 与配置选项后委托 crate::stats::run_stats。
      mod handler;
      #[cfg(test)]
      mod tests;

      pub use handler::handle_stats;
      ```
      仅这几行（含注释 + 模块声明 + pub use），无 use 导入、无 fn/struct/impl。

    Step 4: 运行验证序列（依序）：
      - `cargo build` 通过
      - `cargo clippy --all-targets -- -D warnings` 通过（0 warnings）
      - `cargo test` 通过（特别确认 cli::stats::tests 中 8 个 test 仍跑过）
      - `cargo fmt`
  </action>
  <verify>
    <automated>cargo clippy --all-targets -- -D warnings && cargo test --lib cli::stats && cargo test</automated>
    <grep-check>grep -v '^\s*//' src/cli/stats/mod.rs | grep -vE '^\s*$' | grep -E 'fn |struct |impl ' && echo "FAIL: mod.rs 仍含实现代码" || echo "OK: mod.rs 仅声明"</grep-check>
  </verify>
  <acceptance_criteria>
    - cargo clippy --all-targets -- -D warnings 退出码 0
    - cargo test 退出码 0
    - src/cli/stats/mod.rs 经过滤注释和空行后只含 `mod` 声明、`pub use` 重导出（无 `fn`/`struct`/`impl` 关键字）
    - 原 mod.rs 中 handle_stats 与 merge_stats_options 的全部行为保持不变
    - 8 个测试 + 1 个 helper 全部迁移到 tests.rs
    - main.rs 中 `cli::stats::handle_stats(...)` 调用路径无需修改
  </acceptance_criteria>
  <done>
    mod.rs ≤ 10 行（仅注释、mod 声明、pub use），handler.rs 含全部函数实现，tests.rs 含全部 8 个测试，三道质量门禁全绿。
  </done>
</task>

</tasks>

<verification>
- cargo clippy --all-targets -- -D warnings 通过
- cargo test 通过
- src/cli/stats/mod.rs grep -E 'fn |struct |impl ' 无匹配（仅 mod/pub use/注释）
</verification>

<success_criteria>
- mod.rs 仅含模块声明与 pub use（10 行以内）
- 8 个原 tests 与 2 个原函数全部迁移到 handler.rs + tests.rs
- 公开 API 路径 `crate::cli::stats::handle_stats` 与签名完全保持
- 全套 cargo 质量门禁通过
</success_criteria>

<output>
Create `.planning/phases/71-mod-rs-mod-rs-pub-use/71-01-SUMMARY.md` when done
</output>
