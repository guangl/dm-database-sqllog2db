---
phase: 32-cleanup-project-structure
plan: 03
type: execute
wave: 3
depends_on: [32-02]
files_modified:
  - tests/integration.rs
  - src/cli/run/tests.rs
  - src/cli/init.rs
  - src/cli/show_config.rs
autonomous: true
requirements: [RM-08]
must_haves:
  truths:
    - "tests/integration.rs 中无 stats/digest/resume 测试及对应 import"
    - "src/cli/run/tests.rs 中无 template stats 测试"
    - "init.rs 的 中英文模板字符串中无 [template]/[charts]/resume 配置段注释"
    - "show_config.rs 中无 template/charts 配置显示代码"
    - "find src -type d -empty 无输出"
    - "cargo build --release 编译成功"
    - "cargo clippy --all-targets -- -D warnings 无警告"
    - "cargo fmt --check 格式合规"
  artifacts:
    - path: "tests/integration.rs"
      provides: "stats/digest/resume 测试和 import 已删除"
    - path: "src/cli/init.rs"
      provides: "模板字符串中已删除 [template]/[charts]/resume 配置段注释"
    - path: "src/cli/show_config.rs"
      provides: "template/charts 显示代码已删除"
  key_links:
    - from: "编译器"
      to: "所有编辑后的文件"
      pattern: "cargo build + clippy + test + fmt"
    - from: "开发者"
      to: "项目结构"
      pattern: "find src -type d -empty"
---

<objective>
清理测试死代码和 init/show_config 模板残留，执行全链路验证。

**Purpose:** 删完所有死代码后做最终验证：从测试层清理调用已移除功能的测试用例，从 init 模板和 show_config 中清除已无意义的配置段注释和显示代码，然后全链路跑通 build/test/clippy/fmt。

**Output:** 4 个文件编辑 + 全链路验证通过。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/32-cleanup-project-structure/32-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 删除 tests/integration.rs 和 cli/run/tests.rs 中的死测试代码</name>
  <files>tests/integration.rs, src/cli/run/tests.rs</files>
  <action>
    **A. tests/integration.rs** (约 54KB, ~1100 行):
    - 删除文件头部导入: `use dm_database_sqllog2db::cli::digest::{SortBy, handle_digest};` 和 `use dm_database_sqllog2db::cli::stats::handle_stats;`
    - 删除完整 "Stats 测试" 章节（约 13 个测试函数）:
      `test_handle_stats_empty_dir`
      `test_handle_stats_with_log_files`
      `test_handle_stats_nonexistent_dir`
      `test_handle_stats_group_by_user`
      `test_handle_stats_group_by_multiple`
      `test_handle_stats_group_by_invalid`
      `test_handle_stats_top_slow`
      `test_handle_stats_bucket_hour`
      `test_handle_stats_bucket_minute`
      `test_handle_stats_bucket_invalid`
      `test_handle_stats_json_basic`
      `test_handle_stats_json_with_group_and_bucket`
      `test_handle_stats_verbose`
      `test_handle_stats_group_and_bucket_non_quiet`
    - 删除完整 "Digest 测试" 章节（约 8 个测试函数）:
      `test_handle_digest_empty_dir`
      `test_handle_digest_basic`
      `test_handle_digest_sort_exec`
      `test_handle_digest_top_n`
      `test_handle_digest_min_count`
      `test_handle_digest_json`
      `test_handle_digest_nonexistent_dir`
      `test_handle_digest_aggregates_same_fingerprint`
    - 删除完整 "Resume 测试" 章节（约 2 个测试函数）:
      `test_resume_skips_processed_files`
      `test_resume_reprocesses_changed_file`
    - 删除 `test_handle_run_parallel_csv_with_resume` 测试函数
    - 删除仅被已删测试使用的 helper 函数:
      `fn write_test_log_multi_ts(...)` (约 16 行，仅被 stats/bucket 测试使用)
      `fn make_stats_cfg(...)` (约 10 行，仅被 stats/digest 测试使用)
    - 确认删除后的语法正确性（无孤立 `mod tests` 区块）

    **B. src/cli/run/tests.rs:**
    - 删除以下 2 个模板相关测试函数:
      `test_no_template_stats_when_disabled`
      `test_template_stats_enabled_end_to_end_sequential`
    - 确认这些测试使用的 helper/fixture 没有被其他测试引用
  </action>
  <verify>
    <automated>cargo build 2>&1 | head -10 && cargo test 2>&1 | tail -20</automated>
  </verify>
  <done>
    - tests/integration.rs 无 stats/digest/resume 测试和对应 import
    - tests/integration.rs 无 write_test_log_multi_ts/make_stats_cfg 函数
    - src/cli/run/tests.rs 无 template stats 测试
    - cargo build 编译成功
    - cargo test 全部通过或仅因 Phase 33 范围外的失败
  </done>
</task>

<task type="auto">
  <name>Task 2: 删除 init.rs 和 show_config.rs 中的 template/charts/resume 遗留代码</name>
  <files>src/cli/init.rs, src/cli/show_config.rs</files>
  <action>
    **A. src/cli/init.rs** (约 10KB):
    两个模板字符串 `CONFIG_TEMPLATE_ZH` 和 `CONFIG_TEMPLATE_EN`。
    
    删除中英文模板中以下配置段注释（具体行号以实际代码为准，按标识符搜索）:
    - `[template]` 整个配置段注释（含 `# 模板分析`、`enabled = true`、`[template.report]` 等约 10 行）
    - `[charts]` 整个配置段注释（含 `# 图表配置`、`output_dir`、`top_n`、`[charts.trend]` 等约 8 行）
    - resume 相关注释（约 2-4 行，`# 断点续传` 等相关）
    - 确保删除后模板字符串语法仍然正确（连续字符串拼接、缩进对齐）

    **B. src/cli/show_config.rs** (约 11KB):
    - 在显示 Config 的代码中，删除:
      `if let Some(ta) = &cfg.template { ... }` 整块（约 20 行，输出 [template] 配置段）
      `if let Some(charts) = &cfg.charts { ... }` 整块（约 20 行，输出 [charts] 配置段）
    - 检查是否有 `cfg.resume` 或 `cfg.charts` 的其他引用，一并删除
    - 确保删除后显示逻辑连续（无空 `if` 块或语法错误）
  </action>
  <verify>
    <automated>cargo build 2>&1 | head -10</automated>
  </verify>
  <done>
    - init.rs 两个模板字符串中无 [template]/[charts]/resume 配置段
    - show_config.rs 无 template 和 charts 配置显示代码
    - cargo build 编译成功
  </done>
</task>

<task type="auto">
  <name>Task 3: 全链路验证 — 编译 + 测试 + clippy + fmt + 空目录检查</name>
  <files>N/A（验证任务）</files>
  <action>
    按顺序执行以下验证步骤，任一失败即停止并报告失败信息：

    1. **空目录检查:**
       ```bash
       find src -type d -empty 2>&1
       ```
       如果输出非空，删除空目录后重试。特别确认 `src/charts/` 目录（如果 Phase 28 已删除文件但未删目录）。

    2. **release 编译:**
       ```bash
       cargo build --release 2>&1
       ```
       预期: 0 错误，生成成功。

    3. **测试通过:**
       ```bash
       cargo test 2>&1
       ```
       预期: 所有测试通过（允许 Phase 33 范围外的已知失败，但不应有编译失败）。

    4. **Clippy 零警告:**
       ```bash
       cargo clippy --all-targets -- -D warnings 2>&1
       ```
       预期: 无任何 warning 或 error。

    5. **格式检查:**
       ```bash
       cargo fmt --check 2>&1
       ```
       如果格式不符合，运行 `cargo fmt` 修复并重新验证。

    6. **最终确认:**
       完成所有步骤后，运行:
       ```bash
       cargo build --release && cargo test && cargo clippy --all-targets -- -D warnings && cargo fmt --check && echo "ALL CHECKS PASSED" || echo "SOME CHECKS FAILED"
       ```
       输出 `ALL CHECKS PASSED` 即确认最终态满足 Phase 32 所有 success criteria。
  </action>
  <verify>
    <automated>
cargo build --release 2>&1 && cargo test 2>&1 && cargo clippy --all-targets -- -D warnings 2>&1 && cargo fmt --check 2>&1
    </automated>
  </verify>
  <done>
    - `cargo build --release` 成功
    - `cargo test` 全部通过
    - `cargo clippy --all-targets -- -D warnings` 无警告
    - `cargo fmt --check` 格式合规
    - `find src -type d -empty` 无空目录
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Phase 32 仅做代码结构清理，不涉及运行时信任边界变更。
</threat_model>

<verification>
```bash
cargo build --release 2>&1 && cargo test 2>&1 && cargo clippy --all-targets -- -D warnings 2>&1 && cargo fmt --check 2>&1 && find src -type d -empty
```
预期：四道验证全绿，空目录检查无输出。
</verification>

<success_criteria>
- [ ] `cargo build --release` 编译成功
- [ ] `cargo test` 全部测试通过
- [ ] `cargo clippy --all-targets -- -D warnings` 无警告
- [ ] `cargo fmt --check` 格式通过
- [ ] `find src -type d -empty` 无空目录
- [ ] tests/integration.rs 和 cli/run/tests.rs 无死测试代码
- [ ] init.rs 模板字符串无已移除功能配置段
- [ ] show_config.rs 无已移除功能显示代码
</success_criteria>

<output>
创建 `.planning/phases/32-cleanup-project-structure/32-03-SUMMARY.md` 记录操作内容和验证结果。
</output>
