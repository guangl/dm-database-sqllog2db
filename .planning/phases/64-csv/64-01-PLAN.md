---
phase: "64"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - PARALLEL-01
  - PARALLEL-02

must_haves:
  truths:
    - "cargo test 全部通过（774+ 测试，无回归）"
    - "cargo clippy --all-targets -- -D warnings 无警告"
    - "REQUIREMENTS.md 中 PARALLEL-02 描述与 temp-file 实际实现对齐，不再提 channel"
    - "SC1：多文件+CSV 自动走并行路径（use_csv_parallel 条件已在 mod.rs 实现）"
    - "SC4：单文件回退顺序路径（log_files.len() == 1 时 use_csv_parallel = false）"
  artifacts:
    - path: "src/cli/run/parallel.rs"
      provides: "process_csv_parallel 完整实现（已存在，验证不修改）"
      contains: "process_csv_parallel"
    - path: "src/cli/run/mod.rs"
      provides: "use_csv_parallel 切换条件（已存在）"
      contains: "use_csv_parallel"
    - path: ".planning/REQUIREMENTS.md"
      provides: "PARALLEL-02 更新后描述（写入不全量缓冲内存，temp-file 方案）"
      contains: "temp-file"
  key_links:
    - from: "src/cli/run/mod.rs"
      to: "src/cli/run/parallel.rs"
      via: "run_csv_parallel → process_csv_parallel"
      pattern: "process_csv_parallel"
---

<objective>
Phase 64 是验证阶段。CSV 多文件并行路径已在 v1.16.0 (Phase 59) 完整实现于
src/cli/run/parallel.rs，采用 temp-file 方案：每个 rayon 线程独立写临时 CSV，
最终按原始顺序拼接。

本计划的两项工作：
1. 运行质量门禁确认现有实现满足 SC1–SC4（无需任何代码改动）
2. 更新 REQUIREMENTS.md 中 PARALLEL-02 的描述，使其与 D-01 决策对齐
   （当前措辞"通过 channel"与实际 temp-file 实现不符）

Purpose: 让里程碑 v1.17 有正确的文档基础，Phase 65/66 可在准确的 requirement 描述下继续。
Output: 质量门禁全绿报告 + REQUIREMENTS.md PARALLEL-02 条目更新。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/64-csv/64-CONTEXT.md
@.planning/phases/64-csv/64-RESEARCH.md

<interfaces>
<!-- 已实现的核心接口，执行时无需探索代码库 -->

src/cli/run/mod.rs (已验证行 61-62):
  let use_csv_parallel =
      jobs > 1 && log_files.len() > 1 && !is_stdin_pipe && final_cfg.exporter.csv.is_some();

src/cli/run/parallel.rs (已验证行 266-276):
  pub(super) fn process_csv_parallel(
      log_files: &[PathBuf],
      cfg: &crate::config::Config,
      pipeline: &Pipeline,
      jobs: usize,
      interrupted: &Arc<AtomicBool>,
      do_normalize: bool,
      placeholder_override: Option<bool>,
      field_mask: FieldMask,
      ordered_indices: &[usize],
  ) -> Result<(Vec<(PathBuf, usize)>, usize, ErrorStats)>

已有测试（tests/integration.rs:486）:
  test_handle_run_parallel_csv_multiple_files — 3 个文件 × 10 条 = 30 行断言

已有测试（src/cli/run/tests.rs:103）:
  test_parallel_merge_consistent — 并行与顺序路径结果一致性
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 运行质量门禁并核对四条成功标准</name>
  <files>（无文件修改，仅执行验证命令）</files>
  <read_first>
    - src/cli/run/mod.rs — 核查 use_csv_parallel 条件（行 61-62）是否满足 SC1/SC4
    - src/cli/run/parallel.rs — 核查 write_records_to_csv 内的 Vec drop 时机是否满足 SC2
  </read_first>
  <action>
    执行以下验证步骤，逐一核查 SC1–SC4。

    步骤 1 — 运行完整测试套件：
      cargo test
    预期：所有测试通过，无 FAILED 行。记录测试数量。

    步骤 2 — 运行 clippy：
      cargo clippy --all-targets -- -D warnings
    预期：无警告，exit code 0。

    步骤 3 — 核查 SC1（多文件自动并行）：
    在 src/cli/run/mod.rs 确认 use_csv_parallel 条件为
    "jobs > 1 && log_files.len() > 1 && !is_stdin_pipe && csv.is_some()"。
    确认 test_handle_run_parallel_csv_multiple_files 通过（cargo test 输出含此测试名且为 ok）。

    步骤 4 — 核查 SC2（无全量内存缓冲）：
    在 src/cli/run/parallel.rs 的 run_parallel_tasks 中，确认 rows（Vec）在传入
    write_records_to_csv 时被 move 进函数，函数返回后 Vec 已释放。
    无需代码改动，仅记录分析结论。

    步骤 3 — 核查 SC3（峰值内存 ≤ 2× 单线程）：
    ROADMAP 未要求自动化内存基准测试，以理论分析代替：
    rayon work-stealing 保证任意时刻最多 jobs 个线程并行，
    对于 jobs = 2 时峰值内存 ≤ 2 × 单文件 Vec 大小，满足 SC3（≤ 2×）。
    记录此分析为理论满足。

    步骤 5 — 核查 SC4（单文件回退顺序路径）：
    确认 log_files.len() == 1 时 use_csv_parallel = false，走 run_sequential 分支。
    test_handle_run_real_csv_export（单文件测试）通过即为佐证。

    若任何步骤失败，记录具体错误信息，不得跳过。
  </action>
  <verify>
    <automated>cargo test 2>&1 | tail -5</automated>
    <automated>cargo clippy --all-targets -- -D warnings 2>&1 | grep -c "^error" || true</automated>
  </verify>
  <acceptance_criteria>
    - cargo test 最后输出含 "test result: ok" 且 failed 字段为 0
    - cargo clippy 输出不含任何 "error[" 行
    - SC1/SC2/SC3/SC4 均有核对记录（理论分析或测试验证）
  </acceptance_criteria>
  <done>质量门禁全绿，四条成功标准核查完毕，结论记录在本次执行输出中</done>
</task>

<task type="auto">
  <name>Task 2: 更新 REQUIREMENTS.md PARALLEL-02 描述与 temp-file 实现对齐</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md — 当前 PARALLEL-02 条目全文
    - .planning/phases/64-csv/64-CONTEXT.md — D-01 锁定决策（temp-file 方案权威来源）
  </read_first>
  <action>
    当前 REQUIREMENTS.md 中 PARALLEL-02 原文：
      "并行路径通过 channel 将各解析线程的记录传递给单一写入线程，写入不全量缓冲内存"

    依据 D-01（CONTEXT.md）：实际采用 temp-file 方案，channel 写入线程架构已被明确推迟。
    PARALLEL-02 的核心语义是"写入不全量缓冲内存"，temp-file 方案通过按文件 Vec drop 满足此要求。

    将 PARALLEL-02 条目更新为：
      "并行路径写入不全量缓冲内存：每个 rayon 线程将单文件记录收集到 Vec 后写入临时 CSV，
       写入完成后 Vec 立即释放；最终按原始顺序拼接为单一输出文件（temp-file 方案，per D-01）"

    同时在条目上方或下方保留对原始 channel 设计意图的一行注释，格式：
      <!-- 原始设计意图为 channel 写入线程；D-01 决策改为 temp-file 方案，channel 留后续里程碑 -->

    注意：不修改 PARALLEL-01、PARALLEL-03、PARALLEL-04、PARALLEL-05、IO-01、COMPAT-* 任何条目。
    不修改 Traceability 表格（PARALLEL-02 仍指向 Phase 64）。
  </action>
  <verify>
    <automated>grep -c "temp-file" /Users/guang/Projects/sqllog2db/.planning/REQUIREMENTS.md</automated>
    <automated>grep -c "channel" /Users/guang/Projects/sqllog2db/.planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - REQUIREMENTS.md 中 PARALLEL-02 条目不再包含"通过 channel 将各解析线程的记录传递给单一写入线程"措辞
    - PARALLEL-02 条目含"temp-file"或"临时 CSV"字样，且含"写入不全量缓冲内存"语义
    - "channel"仅出现在注释中（html 注释或说明性文字），不作为功能要求
    - 其他所有条目内容不变，cargo test 仍通过（文档改动不影响代码）
  </acceptance_criteria>
  <done>REQUIREMENTS.md PARALLEL-02 描述与 temp-file 实际实现对齐，D-01 决策有据可查</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 无新输入处理 | Phase 64 仅验证现有实现，不引入新的用户输入路径 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-64-01 | Tampering | 临时 CSV parts 目录 | accept | 已有 setup_parts_dir 含 PID 隔离（dir_name 含 process::id()），同机多实例安全 |
| T-64-02 | Information Disclosure | 临时文件残留 | accept | concat_csv_parts 写盘后删除 temp files；finalize_concat 无论成功与否删除 parts_dir；可接受 |
| T-64-SC | Tampering | 本计划无 npm/pip/cargo 安装 | accept | Phase 64 不引入任何新依赖，slopcheck 不适用 |
</threat_model>

<verification>
Phase 64 完成条件：
1. `cargo test` — 全部通过，与 v1.16.0 baseline（774 个测试）数量相同或更多，无 FAILED
2. `cargo clippy --all-targets -- -D warnings` — exit code 0，无任何 error 行
3. `.planning/REQUIREMENTS.md` — PARALLEL-02 条目已更新，"channel"不再作为功能要求
4. SC1-SC4 均有核查记录（cargo test 输出 + 代码审查结论）
</verification>

<success_criteria>
- cargo test 全绿（774+ 测试，0 FAILED）
- cargo clippy 无警告（0 error 行）
- REQUIREMENTS.md PARALLEL-02 与 temp-file 实现对齐，含 D-01 引用
- Phase 64 的 PARALLEL-01 和 PARALLEL-02 需求均被验证满足，有文档依据
</success_criteria>

<output>
执行完成后创建 .planning/phases/64-csv/64-01-SUMMARY.md

摘要应包含：
- cargo test 运行结果（测试数量、通过/失败）
- cargo clippy 运行结果（警告数）
- SC1-SC4 逐条核查结论（满足/不满足/理论满足）
- REQUIREMENTS.md 修改的 diff 摘要（原文 → 新文）
- 为 Phase 65 留下的已知事项（如单核 CI 的 jobs==1 行为、SC3 理论分析局限）
</output>
