---
phase: 30-remove-template-analysis
plan: 02
type: execute
wave: 2
depends_on: [30-01]
files_modified:
  - src/cli/run/mod.rs
  - src/cli/run/processor.rs
  - src/cli/run/parallel.rs
  - src/exporter/mod.rs
  - src/exporter/csv/mod.rs
  - src/exporter/sqlite/mod.rs
  - src/pipeline/aggregator.rs (delete)
  - src/pipeline/template_reporter.rs (delete)
  - src/exporter/csv/companion.rs (delete)
autonomous: true
requirements: [RM-05]
user_setup: []

must_haves:
  truths:
    - "aggregator.rs 和 template_reporter.rs 文件已删除"
    - "cli/run/mod.rs 不再包含 write_template_reports、do_template、template_agg 和相关调用"
    - "processor.rs 不再接收 aggregator 参数或包含模板聚合代码块"
    - "parallel.rs 不再包含 do_template 参数和 TemplateAggregator 类型"
    - "Exporter trait 不再包含 write_template_stats 方法"
    - "csv/mod.rs 不再包含 mod companion 和 write_template_stats"
    - "sqlite/mod.rs 不再包含 write_template_stats 方法"
    - "companion.rs 文件已删除"
  artifacts:
    - path: src/pipeline/aggregator.rs (MUST NOT exist)
    - path: src/pipeline/template_reporter.rs (MUST NOT exist)
    - path: src/exporter/csv/companion.rs (MUST NOT exist)
    - path: src/cli/run/mod.rs (MUST NOT contain "write_template_reports" or "do_template" or "TemplateAggregator")
    - path: src/cli/run/processor.rs (MUST NOT contain "TemplateAggregator" or "aggregator.observe" or "normalize_template")
    - path: src/cli/run/parallel.rs (MUST NOT contain "do_template" or "TemplateAggregator" or "merged_agg")
    - path: src/exporter/mod.rs (MUST NOT contain "write_template_stats")
    - path: src/exporter/csv/mod.rs (MUST NOT contain "write_template_stats" or "mod companion")
    - path: src/exporter/sqlite/mod.rs (MUST NOT contain "write_template_stats")
  key_links:
    - from: src/cli/run/mod.rs -> src/pipeline/aggregator.rs via "TemplateAggregator" — import and usage removed
    - from: src/cli/run/mod.rs -> src/pipeline/template_reporter.rs via "TemplateReporter" — import and usage removed
    - from: src/exporter/csv/mod.rs -> src/exporter/csv/companion.rs via "mod companion" — decoupled
    - from: src/cli/run/parallel.rs -> src/pipeline/aggregator.rs via "TemplateAggregator" — return type and merge removed
---

<objective>
移除模板分析运行时代码（RM-05：运行时清理）

**Purpose**: 删除 aggregator.rs 和 template_reporter.rs 两个核心文件；从 cli/run（mod.rs、processor.rs、parallel.rs）中移除所有模板聚合、报告相关的运行时代码；从 Exporter trait 和实现中移除 write_template_stats 方法。

**Output**: 运行时代码不再包含模板分析逻辑，热循环中 template_agg 相关代码完全移除，pipeline.is_empty() 快路径不受影响。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/30-remove-template-analysis/30-RESEARCH.md
@.planning/phases/30-remove-template-analysis/30-01-PLAN.md

<source_refs>
当前代码库中需要修改的文件映射（基于 research doc 的分析）：

### A. 删除文件（3 个）
- `src/pipeline/aggregator.rs` — 447 行，TemplateAggregator / TemplateStats / ChartEntry 及其测试
- `src/pipeline/template_reporter.rs` — 278 行，TemplateReporter (write_csv, write_sqlite) 及其测试
- `src/exporter/csv/companion.rs` — 99 行，write_companion_rows / write_template_stats / format_companion_row

### B. cli/run/mod.rs
- 行 6: `use crate::pipeline::template_reporter::TemplateReporter;`
- 行 7: `use crate::pipeline::{CompiledMetaFilters, CompiledSqlFilters, TemplateAggregator};`
- 行 8: `use crate::pipeline::{derive_template_report_paths, template_report_enabled};`
- 行 27-58: `fn write_template_reports()` 完整函数
- 行 130: `let do_template = final_cfg.template.as_ref().is_some_and(|t| t.enable);`
- 行 153-168: `process_csv_parallel()` 调用中的 do_template 参数和 parallel_agg 处理
- 行 171-181: 并行路径的 template_stats / write_template_reports 块
- 行 207: `let mut template_agg = do_template.then(TemplateAggregator::new);`
- 行 239: `template_agg.as_mut()` 参数传递给 process_log_file
- 行 257-261: `if let Some(ref agg) = template_agg { charts ... }` 块
- 行 266-271: `let template_stats = ...` 和 `write_template_reports()` 调用

### C. cli/run/processor.rs
- 行 5: `use crate::pipeline::{CompiledSqlFilters, Pipeline, TemplateAggregator};`
- 行 27: `mut aggregator: Option<&mut TemplateAggregator>,` 参数
- 行 91: `let pm = if include_pm || aggregator.is_some() {` 条件
- 行 131-161: 完整模板聚合代码块（`if let Some(ref mut agg) = aggregator { ... }`）

### D. cli/run/parallel.rs
- 行 5: `use crate::pipeline::{CompiledSqlFilters, FieldMask, Pipeline, TemplateAggregator};`
- 行 82: `do_template: bool,` 参数
- 行 87: `Option<TemplateAggregator>` 返回类型
- 行 130: `type TaskResult = Option<(PathBuf, PathBuf, usize, Option<TemplateAggregator>)>;`
- 行 168: `let mut task_agg = do_template.then(TemplateAggregator::new);`
- 行 180: `task_agg.as_mut()` 参数
- 行 186: `task_agg` 从四元组中移除
- 行 197-210: map-reduce 合并逻辑（merged_agg）

### E. exporter/mod.rs
- 行 54-63: Exporter trait 中 `fn write_template_stats` 默认实现
- 行 129-148: ExporterKind 中 `fn write_template_stats`
- 行 290-299: ExporterManager 中 `fn write_template_stats`

### F. exporter/csv/mod.rs
- 行 10: `mod companion;`
- 行 13: `pub(crate) use self::companion::write_companion_rows;`
- 行 243-251: `fn write_template_stats` 方法覆盖

### G. exporter/sqlite/mod.rs
- 行 250+: `fn write_template_stats` 方法覆盖
</source_refs>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 清理 cli/run 热循环 — mod.rs、processor.rs、parallel.rs</name>
  <files>
    src/cli/run/mod.rs
    src/cli/run/processor.rs
    src/cli/run/parallel.rs
  </files>
  <read_first>
    src/cli/run/mod.rs (lines 6-8 — imports; lines 27-58 — write_template_reports fn; lines 130, 153-181 — 并行路径; lines 207, 239, 257-271 — 顺序路径)
    src/cli/run/processor.rs (line 5 — import; line 27 — aggregator param; line 91 — condition; lines 131-161 — template aggregation block)
    src/cli/run/parallel.rs (line 5 — import; line 82 — do_template param; line 87 — return type; line 130 — TaskResult; lines 168, 180, 186, 197-210 — aggregator logic)
  </read_first>
  <action>
    按顺序执行，每步后运行 `cargo build` 验证编译。

    **Step 1: 清理 src/cli/run/mod.rs**

    1a) 删除导入：
    - 删除 line 6: `use crate::pipeline::template_reporter::TemplateReporter;`
    - 从 line 7 `use crate::pipeline::{CompiledMetaFilters, CompiledSqlFilters, TemplateAggregator};` 中删除 `TemplateAggregator,`
    - 删除 line 8: `use crate::pipeline::{derive_template_report_paths, template_report_enabled};`

    1b) 删除 `fn write_template_reports()` 完整函数（lines 27-58）

    1c) 并行路径删除：
    - 删除 line 130: `let do_template = final_cfg.template.as_ref().is_some_and(|t| t.enable);`
    - 修改 line 153: 从 `let (processed_files, parallel_skipped, parallel_agg) = process_csv_parallel(` 改为 `let (processed_files, parallel_skipped) = process_csv_parallel(`
    - 删除 line 163: `do_template,` 参数（process_csv_parallel 调用中的第 7 个参数）
    - 删除 lines 171-181（共 11 行）：完整删除：
      ```
      if let Some(ref agg) = parallel_agg {
          if let Some(charts_cfg) = final_cfg.charts.as_ref() {
              crate::charts::generate_charts(agg, charts_cfg)?;
          }
      }
      let template_stats = parallel_agg.map(TemplateAggregator::finalize);
      if let Some(ref stats) = template_stats {
          info!("Template analysis: {} unique templates", stats.len());

          write_template_reports(final_cfg, stats)?;
      }
      ```

    1d) 顺序路径删除：
    - 删除 line 207: `let mut template_agg = do_template.then(TemplateAggregator::new);`
    - 从 line 239 `template_agg.as_mut(),` 参数中删除整行（process_log_file 的第 7 个参数）
    - 删除 lines 257-261（共 5 行）：`if let Some(ref agg) = template_agg { if let Some(charts_cfg) = final_cfg.charts.as_ref() { crate::charts::generate_charts(agg, charts_cfg)?; } }`
    - 删除 lines 266-271（共 6 行）：`let template_stats = template_agg.map(TemplateAggregator::finalize); if let Some(ref stats) = template_stats { info!(...); write_template_reports(final_cfg, stats)?; }`

    **Step 2: 清理 src/cli/run/processor.rs**

    2a) 从 `use crate::pipeline::{CompiledSqlFilters, Pipeline, TemplateAggregator};`（line 5）中删除 `TemplateAggregator,`

    2b) 修改函数签名（line 27）：将 `mut aggregator: Option<&mut TemplateAggregator>,` 参数删除（从参数列表中移除整行）

    2c) 修改第 91 行的条件：将 `if include_pm || aggregator.is_some()` 改为 `if include_pm` — 移除了 aggregator.is_some() 条件，因为不再有模板聚合路径需要 exectime

    2d) 删除整个模板聚合块（lines 131-161）：`if let Some(ref mut agg) = aggregator { ... }` 的完整块（13 行，包括 `// 模板聚合` 注释到闭合 `}`）
    - 此块包含 `normalize_template(pm.sql.as_ref())` 调用和 `agg.observe(...)` 调用

    **Step 3: 清理 src/cli/run/parallel.rs**

    3a) 从 `use crate::pipeline::{CompiledSqlFilters, FieldMask, Pipeline, TemplateAggregator};`（line 5）中删除 `TemplateAggregator,`

    3b) 删除 `do_template: bool,` 参数（line 82）— 从函数签名参数列表中移除

    3c) 修改返回类型（line 87）：从 `Result<(Vec<(PathBuf, usize)>, usize, Option<TemplateAggregator>)>` 改为 `Result<(Vec<(PathBuf, usize)>, usize)>`

    3d) 修改 TaskResult 类型别名（line 130）：从 `type TaskResult = Option<(PathBuf, PathBuf, usize, Option<TemplateAggregator>)>;` 改为 `type TaskResult = Option<(PathBuf, PathBuf, usize)>;`

    3e) 删除或修改以下行：
    - 删除 line 168: `let mut task_agg = do_template.then(TemplateAggregator::new);`
    - 从 `process_log_file(...)` 调用（line 170-186）中删除 `task_agg.as_mut(),` 参数（从参数列表中移除整行）
    - 修改 line 189: `Ok(Some((file.clone(), temp_path, count, task_agg)))` → `Ok(Some((file.clone(), temp_path, count)))`

    3f) 删除 map-reduce 合并逻辑：
    - 删除 line 197: `let mut merged_agg: Option<TemplateAggregator> = None;`
    - 删除 lines 205-210（在 `match result` 的 `Ok(Some(...))` arm 内）：
      ```
      // map-reduce：将各 rayon task 的聚合器合并到主线程
      if let Some(task_agg) = task_agg {
          match &mut merged_agg {
              Some(base) => base.merge(task_agg),
              None => merged_agg = Some(task_agg),
          }
      }
      ```
    - 在 `match result` 中，将 `Ok(Some((orig, temp, count, task_agg)))` → `Ok(Some((orig, temp, count)))`
    - 将 `let mut parts_info: Vec<(PathBuf, PathBuf, usize)>`（line 196）保持不变 — task_agg 不再出现在四元组中

    3g) 修改函数返回语句（line 245-252）：从
    ```
    Ok((
        parts_info...,
        skipped,
        merged_agg,
    ))
    ```
    改为
    ```
    Ok((
        parts_info...,
        skipped,
    ))
    ```

    每步后运行 `cargo build` 验证编译。
  </action>
  <verify>
    <automated>
      grep -c 'TemplateAggregator' src/cli/run/mod.rs src/cli/run/processor.rs src/cli/run/parallel.rs; echo "expect 0"
      grep -c 'write_template_reports' src/cli/run/mod.rs; echo "expect 0"
      grep -c 'do_template' src/cli/run/mod.rs src/cli/run/parallel.rs; echo "expect 0"
      grep -c 'aggregator\.is_some' src/cli/run/processor.rs; echo "expect 0"
      grep -c 'merged_agg' src/cli/run/parallel.rs; echo "expect 0"
      cargo build 2>&1 | tail -5
    </automated>
  </verify>
  <done>cli/run/mod.rs、processor.rs、parallel.rs 中所有模板聚合/报告相关代码已移除，cargo build 编译通过。</done>
</task>

<task type="auto">
  <name>Task 2: 清理 Exporter 层和删除文件</name>
  <files>
    src/exporter/mod.rs
    src/exporter/csv/mod.rs
    src/exporter/sqlite/mod.rs
    src/pipeline/aggregator.rs (delete)
    src/pipeline/template_reporter.rs (delete)
    src/exporter/csv/companion.rs (delete)
  </files>
  <read_first>
    src/exporter/mod.rs (lines 54-63 — trait default impl; lines 129-148 — ExporterKind method; lines 290-299 — ExporterManager method)
    src/exporter/csv/mod.rs (lines 10, 13 — mod companion and use; lines 243-251 — write_template_stats override)
    src/exporter/sqlite/mod.rs (line 250+ — write_template_stats method)
    src/pipeline/aggregator.rs (entire file, 447 lines)
    src/pipeline/template_reporter.rs (entire file, 278 lines)
    src/exporter/csv/companion.rs (entire file, 99 lines)
  </read_first>
  <action>
    按顺序执行，每步后运行 `cargo build` 验证编译。

    **Step 1: 清理 src/exporter/mod.rs**

    1a) 从 `Exporter` trait 中删除 `fn write_template_stats` 默认实现（lines 48-63 — 从 `/// 将 SQL 模板聚合统计写入导出目标。` 注释到闭合 `}`）。删除整个方法。

    1b) 从 `ExporterKind` 中删除 `fn write_template_stats` 方法（lines 129-148 — 从 `#[inline]` 到方法末尾闭合 `}`）。删除整个方法。

    1c) 从 `ExporterManager` 中删除 `fn write_template_stats` 方法（lines 290-299 — 从 `#[allow(dead_code)]` 到方法末尾）。同时删除方法体前的 `#[allow(dead_code)]` 属性行。删除整个方法。

    **Step 2: 清理 src/exporter/csv/mod.rs**

    2a) 删除 line 10: `mod companion;`
    2b) 删除 line 13: `pub(crate) use self::companion::write_companion_rows;`
    2c) 删除 `fn write_template_stats` 方法覆盖（lines 243-251）— 从 `fn write_template_stats(...)` 到闭合 `}`

    **Step 3: 清理 src/exporter/sqlite/mod.rs**

    3a) 删除 `fn write_template_stats` 方法（line 250 开始，约 30 行）— 从 `fn write_template_stats(...)` 到方法末尾闭合 `}`。注意确认方法末尾位置。

    **Step 4: 删除 3 个文件**

    4a) `git rm src/pipeline/aggregator.rs` — 447 行，包含 TemplateAggregator 结构体、TemplateStats、ChartEntry、所有测试
    4b) `git rm src/pipeline/template_reporter.rs` — 278 行，包含 TemplateReporter（write_csv / write_sqlite）、测试
    4c) `git rm src/exporter/csv/companion.rs` — 99 行，包含 write_companion_rows / write_template_stats / format_companion_row

    注意：删除文件后，之前添加的 `mod companion;` 和 `pub(crate) mod template_reporter;` 等声明已在 Plan 01 或本任务的 Step 2 中移除。如果仍有残留，`cargo build` 会报 missing module 错误，需要返回检查并同步清理。

    每步后运行 `cargo build` 验证。

    最终运行 `cargo build` 确认无编译错误。
  </action>
  <verify>
    <automated>
      test ! -f src/pipeline/aggregator.rs; echo "aggregator deleted"
      test ! -f src/pipeline/template_reporter.rs; echo "template_reporter deleted"
      test ! -f src/exporter/csv/companion.rs; echo "companion deleted"
      grep -c 'write_template_stats' src/exporter/mod.rs src/exporter/csv/mod.rs src/exporter/sqlite/mod.rs; echo "expect 0"
      grep -c 'mod companion' src/exporter/csv/mod.rs; echo "expect 0"
      cargo build 2>&1 | tail -5
    </automated>
  </verify>
  <done>Exporter 层中 write_template_stats 方法已全部移除，3 个删除文件已 git rm 清理，cargo build 编译通过。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

本计划为移除操作，不引入新代码或新攻击面。核心关注点：热循环性能不受影响。

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-03 | Tampering | Hot loop modification | mitigate | `processor.rs` 中热循环仅移除 template 代码块，`if include_pm` 条件简化但不影响正常数据路径 |
| T-30-04 | Tampering | Parallel CSV map-reduce | mitigate | 从返回类型移除 `Option<TemplateAggregator>`，简化 `parts_info` 收集，合并逻辑完全移除 |
| T-30-SC | Tampering | File deletion | mitigate | `git rm` 确保文件从版本控制中删除；`cargo build` 验证无残留引用 |
</threat_model>

<verification>
1. `cargo build` 编译成功
2. `test ! -f src/pipeline/aggregator.rs` 文件已删除
3. `test ! -f src/pipeline/template_reporter.rs` 文件已删除
4. `test ! -f src/exporter/csv/companion.rs` 文件已删除
5. `grep -c 'TemplateAggregator' src/` 返回 0（无残留引用）
6. `grep -c 'write_template_stats' src/` 返回 0
</verification>

<success_criteria>
- aggregator.rs、template_reporter.rs、companion.rs 文件已删除
- cli/run 三个文件中所有模板聚合/报告代码已清理
- Exporter trait/kind/manager 中 write_template_stats 已移除
- csv 和 sqlite 导出器中 write_template_stats 已移除
- `cargo build` 编译通过
</success_criteria>
</task>
