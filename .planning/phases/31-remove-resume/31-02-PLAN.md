---
phase: 31-remove-resume
plan: 02
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - src/cli/run/tests.rs
  - tests/integration.rs
  - src/cli/init.rs
  - README.md
  - docs/architecture.md
autonomous: true
requirements:
  - RM-06
user_setup: []
must_haves:
  truths:
    - "run/tests.rs 中所有 handle_run 调用与新的 8 参数签名一致"
    - "integration.rs 中所有 handle_run 调用与新的 8 参数签名一致"
    - "integration.rs 中不再包含 resume 集成测试"
    - "init 模板不再包含 [resume] 配置段注释"
    - "README.md 不再提及断点续传功能"
    - "docs/architecture.md 不再提及 resume 模块"
    - "cargo test && cargo clippy && cargo fmt --check 全部通过"
  artifacts:
    - path: "src/cli/run/tests.rs"
      provides: "handle_run 单元测试"
      contains: "#[test]"
    - path: "tests/integration.rs"
      provides: "集成测试"
      not_contains: "resume"
    - path: "src/cli/init.rs"
      provides: "配置文件模板"
      not_contains: "resume"
    - path: "README.md"
      provides: "项目 README"
      not_contains: "断点续传"
    - path: "docs/architecture.md"
      provides: "架构文档"
      not_contains: "ResumeState"
  key_links:
    - from: "tests/integration.rs"
      to: "handle_run 调用签名"
      pattern: "handle_run\\(.*80,\\s*(true|false),\\s*(Some|None)"
    - from: "src/cli/run/tests.rs"
      to: "handle_run 调用签名"
      pattern: "handle_run\\(.*80,\\s*(true|false),\\s*(Some|None)"
---

<objective>
清理所有测试文件、初始化模板和文档中的 resume 引用，完成全链路验证。

**Purpose:** 确保 `cargo test` 通过，确保 init 模板不再显示 `[resume]` 段，确保文档不再提及断点续传功能。

**Output:** 干净、可编译、可测试、无警告的代码库。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@file:/Users/guang/Projects/sqllog2db/.planning/ROADMAP.md
@file:/Users/guang/Projects/sqllog2db/.planning/ROADMAP.md
@file:/Users/guang/Projects/sqllog2db/.planning/phases/31-remove-resume/31-RESEARCH.md
@file:/Users/guang/Projects/sqllog2db/.planning/phases/31-remove-resume/31-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 更新测试文件中的 handle_run 调用签名</name>
  <files>
    src/cli/run/tests.rs,
    tests/integration.rs
  </files>
  <action>
    **Plan 01 已将 handle_run 签名从 10 参数改为 8 参数**（删除了 `resume: bool, state_file_override: Option<&str>,`）。
    所有调用点需删除对应的参数对。

    ---

    编辑 `src/cli/run/tests.rs`：

    该文件有 5 处 handle_run 调用，均在函数签名的第 7-8 个参数位置传递 `false, None`。
    将每个调用中的 `false, None,` 删除（位于 `80,` 之后）。调用点：
    - 第 26-37 行：`...&Arc::new(AtomicBool::new(false)), 80, false, None, 1, None)` → `...&Arc::new(AtomicBool::new(false)), 80, 1, None)`
    - 第 79-90 行：同上
    - 第 124-135 行：同上
    - 第 186-197 行：同上
    - 第 231-242 行：同上

    注意保持缩进和对齐。

    ---

    编辑 `tests/integration.rs`（1710 行，最多变化的文件）：

    **a) 删除 3 个 resume 测试函数：**

    1. 删除 `test_resume_skips_processed_files`（第 216-287 行，72 行）：
       - 从 `// ── resume tests ──` 注释（第 216 行）开始删除
       - 删除到 `test_resume_reprocesses_changed_file` 之前（第 289 行之前）
       - 注意保留 `// ── handle_stats tests ──` 注释（第 345 行），但删除中间的 `// ── resume tests ──`
       - 如果 216 行注释和 345 行注释相连，删除 216-287 行（从注释到函数结束）

    2. 删除 `test_resume_reprocesses_changed_file`（第 289-343 行，55 行）：
       - 从第 289 行 `fn test_resume_reprocesses_changed_file` 开始删除
       - 到第 343 行 `}` 结束

    3. 删除 `test_handle_run_parallel_csv_with_resume`（第 1054-1108 行，55 行）：
       - 从第 1054 行 `fn test_handle_run_parallel_csv_with_resume` 开始删除
       - 到第 1108 行 `}` 结束
       - 注意保留 `// ── handle_init tests ──`（第 681 行之后），这个函数位于 test_handle_run_parallel_csv_multiple_files（line 1020）和 handle_init tests 之间

    **b) 更新非 resume 测试中所有 handle_run 调用：**

    共有约 20 处 handle_run 调用。每处在 `progress_interval` 参数后传递 `false, None`（resume=false, state_file=None）。
    将 `false, None,` 删除。

    具体调用位置（行号为近似值，请精确搜索 `handle_run(` 定位）：
    - line ~70：`handle_run(&cfg, None, true, true, &interrupted, 80, false, None, 1, None)`
    - line ~101：同上
    - line ~131：同上，`Some(5)` 的 limit
    - line ~157：同上
    - line ~197：同上，`AtomicBool::new(true)` 的 interrupted
    - line ~856：`false,` non-quiet
    - line ~892：传 `compiled_filters` 而非 None
    - line ~917：`Some(5), false,` limit + dry_run=true
    - line ~957：同上，transaction filter test
    - line ~993：同上，min_runtime filter test
    - line ~1035：`2` jobs (parallel CSV test)
    - line ~1140：perf baseline test
    - line ~1346：E2E filter pipeline test
    - line ~1395：E2E filter pipeline second run
    - line ~1435：E2E template normalization test
    - line ~1490：E2E field projection test
    - line ~1544：boundary empty log
    - line ~1595：boundary all filtered
    - line ~1645：boundary malformed line
    - line ~1687：boundary long SQL

    对所有调用统一执行：删除 `false, None,` 这对参数（位置在 `80,` 和 `1`/`2`/`compiled_filters` 之间）。
  </action>
  <verify>
    <automated>cargo build && cargo test 2>&1 | tail -30</automated>
  </verify>
  <done>
    run/tests.rs 和 integration.rs 中所有 handle_run 调用与新签名一致；
    3 个 resume 集成测试已删除；`cargo test` 全部通过。
  </done>
</task>

<task type="auto">
  <name>Task 2: 清理初始化模板和文档中的 resume 引用</name>
  <files>
    src/cli/init.rs,
    README.md,
    docs/architecture.md
  </files>
  <action>
    编辑 `src/cli/init.rs`：

    a) 删除中文配置模板中的 resume 注释块（第 158-161 行）：
       - `\t# ===================== 断点续传 =====================`
       - `\t# 使用 --resume 标志时，sqllog2db 会跳过已成功处理的文件（通过文件大小和修改时间判断）。`
       - `\t# [resume]`
       - `\t# state_file = ".sqllog2db_state.toml"`
       - 共 4 行，注意前面是 tab 缩进。删除后 `\t# ===================== 导出器配置 =====================` 成为下一个段落的开始。
       - 删除后注意空行调整：删除前段落后保留 1 个空行。

    b) 删除英文配置模板中的 resume 注释块（第 259-262 行）：
       - `\t# ===================== Resume / Checkpoint =====================`
       - `\t# With --resume, sqllog2db skips files already successfully processed`
       - `\t# (tracked by file size and modification time).`
       - `\t# [resume]`
       - `\t# state_file = ".sqllog2db_state.toml"`
       - 共 5 行，同样删除后保留 1 个空行。

    编辑 `README.md`：

    a) 删除第 25 行：
       - 删除 `- **断点续传支持**：可选的状态文件通过文件大小和修改时间跟踪已处理文件，实现增量导出，跳过后续运行中已完成的文件。`
       - 该行在 `### 解析与导出` 小节下，紧跟在 `- **优先级路由的 ExporterManager**...` 之后。
       - 删除后，`- **优先级路由的 ExporterManager**：`（第 24 行）直接成为该小节的最后一项。

    编辑 `docs/architecture.md`：

    a) 在支撑模块表格中（第 101-107 行），删除 resume 行：
       - 删除 `| 断点续传 | src/resume.rs | 文件指纹追踪、增量处理状态 |`
       - 表格共 4 行（header + 3 行数据），删除后保留 header 和其余 2 行数据
       - 注意保持表格 markdown 格式有效（可能需要调整分割线）

    b) 删除 ResumeState 章节（第 127-129 行）：
       - 从 `### ResumeState` 到 `增量导出。仅在 `[sqllog]` 配置中显式启用时生效。`
       - 共 3 行，包含标题、描述句和代码引用句
  </action>
  <verify>
    <automated>grep -rn "resume" src/cli/init.rs README.md docs/architecture.md && echo "FOUND (not OK)" || echo "ALL CLEAN"</automated>
  </verify>
  <done>
    init 模板中不再有 [resume] 注释块；README.md 不再提及断点续传；
    docs/architecture.md 不再有 resume 引用。
  </done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 3: 全链路编译验证</name>
  <files>无</files>
  <action>
    运行以下命令验证 Phase 31 所有变更正确：

    ```bash
    cargo build --release
    cargo test
    cargo clippy --all-targets -- -D warnings
    cargo fmt --check
    ```

    运行 `sqllog2db run --help` 验证：
    - 不再显示 `--resume` 选项
    - 不再显示 `--state-file` 选项

    运行 `sqllog2db init -o config.toml --force && cat config.toml` 验证：
    - 配置模板中不再包含 `[resume]` 段注释
  </action>
  <what-built>
    Plan 01: resume 源文件删除、模块声明清理、运行循环 resume_state 逻辑移除、CLI 参数清理

    Plan 02: 测试文件更新、集成测试 resume 测试删除、init 模板清理、文档清理
  </what-built>
  <how-to-verify>
    1. 运行 `cargo build --release` — 应成功编译
    2. 运行 `cargo test` — 应全部通过（不包含 resume 测试）
    3. 运行 `cargo clippy --all-targets -- -D warnings` — 应无警告
    4. 运行 `cargo fmt --check` — 应无格式问题
    5. 运行 `cargo run -- run --help` — 应不包含 `--resume` 和 `--state-file`
    6. 运行 `cargo run -- init -o /tmp/test_config.toml --force && cat /tmp/test_config.toml` — 不应包含 `[resume]`
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

无变化。纯删除操作，无新输入路径，无新攻击面。

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-01 | Tampering | npm/pip/cargo installs | mitigate | slopcheck + blocking human checkpoint for [ASSUMED]/[SUS] — 本阶段无新依赖，无需检查 |
</threat_model>

<verification>
```bash
cargo build --release
cargo test
cargo clippy --all-targets -- -D warnings
cargo fmt --check
```
</verification>

<success_criteria>
- [ ] `cargo build --release` 编译成功
- [ ] `cargo test` 全部通过
- [ ] `cargo clippy --all-targets -- -D warnings` 无警告
- [ ] `cargo fmt --check` 通过
- [ ] `sqllog2db run --help` 不再显示 `--resume` 和 `--state-file`
- [ ] init 生成的配置模板不再包含 `[resume]` 段注释
- [ ] README.md 和 docs/architecture.md 不再提及断点续传功能
- [ ] integration.rs 中不再有 resume 相关测试
</success_criteria>

<output>
创建 `.planning/phases/31-remove-resume/31-02-SUMMARY.md`
</output>
