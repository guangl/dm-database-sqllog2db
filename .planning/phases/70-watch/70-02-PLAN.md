---
phase: 70-watch
plan: 02
type: execute
wave: 2
depends_on:
  - 70-01
files_modified:
  - src/cli/watch/mod.rs
autonomous: true
requirements:
  - WATCH-03
  - WATCH-04
user_setup: []

must_haves:
  truths:
    - "WatchLoopState 携带 file_offsets: HashMap<PathBuf, u64> 和 sqlite_db_url: Option<String> 两个新字段"
    - "handle_watch 启动时若 cfg.exporter.sqlite 存在：调用 ensure_offset_table + load_offsets 预填 WatchLoopState.file_offsets"
    - "EventKind::Create(_) 路由到 trigger_full_file，处理后调用 save_offset 持久化文件当前大小"
    - "EventKind::Modify(Data(Content)) 路由到 trigger_incremental，仅处理 [start_offset, new_size) 区间"
    - "trigger_incremental 在 new_size <= start_offset 时立即 return Ok(())，不调用 handle_run、不增加 trigger_count"
    - "首次 Modify 但 file_offsets 无该路径记录时：start_offset = 当前文件大小，跳过处理，仅记录初始 offset"
    - "trigger_incremental 内 tmp_cfg.exporter.sqlite.append=true 且 overwrite=false（强制 append 模式）"
    - "handle_event 签名重构为接受 &mut WatchLoopState 单一可变状态参数"
  artifacts:
    - path: "src/cli/watch/mod.rs"
      provides: "trigger_full_file 和 trigger_incremental 两个 pub(crate) 可测试函数 + 扩展后的 WatchLoopState"
      contains: "pub(crate) fn trigger_incremental"
  key_links:
    - from: "src/cli/watch/mod.rs (trigger_incremental)"
      to: "tempfile::Builder"
      via: "Builder::new().prefix(\"sqllog2db-watch-\").suffix(\".log\").tempfile()"
      pattern: "sqllog2db-watch-"
    - from: "src/cli/watch/mod.rs (trigger_incremental)"
      to: "src/cli/run::handle_run"
      via: "tmp_cfg with sqlite.append=true"
      pattern: "append = true"
    - from: "src/cli/watch/mod.rs (handle_watch)"
      to: "offsets::load_offsets"
      via: "cfg.exporter.sqlite.as_ref().map(|s| &s.database_url)"
      pattern: "load_offsets"
---

<objective>
Wave 1 核心实现：扩展 WatchLoopState 携带 file_offsets 与 sqlite_db_url；重构 handle_event/process_log_path 路由为 Create→trigger_full_file 与 Modify→trigger_incremental；新增 trigger_incremental 函数实现 Seek+NamedTempFile 增量读取（per D-01）并持久化偏移（per D-07/D-08）。

Purpose: 实现 WATCH-03（追加内容增量处理）与 WATCH-04（SQLite 字节偏移去重）。增量路径强制 sqlite.append=true 避免清空表（per D-09）；启动时 load_offsets 实现跨重启恢复（per D-06）。
Output: 重构后的 src/cli/watch/mod.rs，trigger_full_file 与 trigger_incremental 为 pub(crate) 可独立测试，handle_event 接受 &mut WatchLoopState（参数列表简化）。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/70-watch/70-CONTEXT.md
@.planning/phases/70-watch/70-RESEARCH.md
@.planning/phases/70-watch/70-01-SUMMARY.md
@src/cli/watch/mod.rs
@src/cli/watch/offsets.rs
@src/cli/run/mod.rs

<interfaces>
<!-- Phase 70-01 created these — use directly. -->
From src/cli/watch/offsets.rs:
```
pub(super) fn ensure_offset_table(database_url: &str) -> rusqlite::Result<()>;
pub(super) fn load_offsets(database_url: &str) -> HashMap<PathBuf, u64>;
pub(super) fn save_offset(database_url: &str, path: &Path, offset: u64);
```

From src/cli/run/mod.rs:29:
```
pub fn handle_run(
    cfg: &Config,
    quiet: bool,
    verbose: bool,
    interrupted: &Arc<AtomicBool>,
    prescan_overrides: Option<...>,  // 已存在的可选参数
) -> Result<ErrorStats>;
```

From src/config/exporter.rs:
```
pub struct SqliteExporterConfig {
    pub database_url: String,
    pub table_name: String,
    pub overwrite: bool,
    pub append: bool,
    // ...
}
pub struct ExporterConfig {
    pub csv: Option<CsvExporterConfig>,
    pub sqlite: Option<SqliteExporterConfig>,
    // ...
}
```

From notify crate (Phase 69 已用):
EventKind::Create(_) — 新文件创建
EventKind::Modify(ModifyKind::Data(DataChange::Content)) — 内容追加
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 扩展 WatchLoopState + 重构 handle_event 签名 + handle_watch 启动时加载 offsets</name>
  <files>src/cli/watch/mod.rs</files>
  <action>
  对 src/cli/watch/mod.rs 进行三处结构化修改，全部围绕 WatchLoopState 重构：

  **修改 1 — WatchLoopState 结构（约第 87-104 行）**：

  per D-04 + D-12，在结构体中新增两个字段：
  ```
  struct WatchLoopState {
      last_trigger_at: Option<Instant>,
      last_status_refresh: Instant,
      debounce_map: HashMap<PathBuf, Instant>,
      total_stats: ErrorStats,
      trigger_count: u64,
      // Phase 70 新增（per D-12）：
      file_offsets: HashMap<PathBuf, u64>,
      sqlite_db_url: Option<String>,
  }
  ```

  修改 `impl WatchLoopState::new()` 签名为：
  ```
  fn new(
      init_offsets: HashMap<PathBuf, u64>,
      sqlite_db_url: Option<String>,
  ) -> Self
  ```
  body 中 `file_offsets: init_offsets, sqlite_db_url: sqlite_db_url,`。

  **修改 2 — handle_watch 启动加载 offsets（约第 28-62 行 handle_watch 函数体）**：

  在 `let watch_dirs = collect_watch_dirs(...)` 之后、`let pb = build_progress_bar(...)` 之前，插入以下逻辑（per D-06）：

  ```
  let sqlite_db_url: Option<String> = cfg.exporter.sqlite.as_ref()
      .map(|s| s.database_url.clone());
  let init_offsets = if let Some(ref url) = sqlite_db_url {
      if let Err(e) = offsets::ensure_offset_table(url) {
          warn!("watch: ensure_offset_table failed: {e}");
      }
      offsets::load_offsets(url)
  } else {
      HashMap::new()
  };
  ```

  然后将 `let mut state = WatchLoopState::new();` 改为 `let mut state = WatchLoopState::new(init_offsets, sqlite_db_url);`。
  调用顺序：ensure_offset_table 必须在 handle_run 之前——此时 SqliteExporter 尚未实例化，无 EXCLUSIVE 锁冲突（per Pitfall 4 + Open Question 1）。

  **修改 3 — handle_event 签名重构（约第 232-273 行）+ run_watch_loop 调用点（约 118-147 行）**：

  per D-09 + Research Open Question 2（CLAUDE.md ≤40 行），将 handle_event 从当前 9 参数 + 即将新增 2 参数（共 11）重构为 4 参数：
  ```
  fn handle_event(
      event: &notify::Event,
      cfg: &Config,
      quiet: bool,
      verbose: bool,
      interrupted: &Arc<std::sync::atomic::AtomicBool>,
      state: &mut WatchLoopState,
      pb: &ProgressBar,
  ) {
      // ... 函数体内通过 state.total_stats, state.trigger_count, state.last_trigger_at,
      //     state.debounce_map, state.file_offsets, state.sqlite_db_url 访问字段
  }
  ```

  同步修改 run_watch_loop 中 `handle_event(&event, ...)` 调用：将原先的 7 个 `&mut state.xxx` 字段引用替换为单个 `&mut state`，删除 `&mut state.total_stats, &mut state.trigger_count, &mut state.last_trigger_at, &mut state.debounce_map` 等参数。

  process_log_path 函数同样改造为接受 `&mut WatchLoopState` 而非分散的 5 个 `&mut` 字段（保持函数体 ≤40 行；若超出，拆分内部辅助）。

  **修改 4 — 单元测试 test_interrupted_flag_exits_immediately（约第 400-409 行）**：

  调用点从 `WatchLoopState::new()` 改为 `WatchLoopState::new(HashMap::new(), None)`（保持现有断言不变）。

  **变量命名**：使用 `database_url`, `initial_offsets`, `state_ref` 等描述性名称（per CLAUDE.md）。

  **不要修改的内容**：should_trigger、format_paths_display、build_progress_bar、render_active_status、refresh_active_status、collect_watch_dirs、format_elapsed_hms、print_final_summary、所有现有单元测试断言（除上述 new() 调用点）。
  </action>
  <verify>
    <automated>cargo build 2>&amp;1 | grep -E '(error|warning:)' | head -5 ; cargo test --lib cli::watch:: 2>&amp;1 | tail -5 | grep -E 'test result: ok'</automated>
  </verify>
  <done>WatchLoopState 含 file_offsets/sqlite_db_url 字段；handle_watch 启动时若有 SQLite 导出器，调用 ensure_offset_table + load_offsets；handle_event 与 process_log_path 重构为 &amp;mut WatchLoopState 单一参数；所有现有 watch 模块测试通过；cargo clippy 无警告。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 实现 trigger_full_file / trigger_incremental + Create/Modify 事件路由分支</name>
  <files>src/cli/watch/mod.rs</files>
  <behavior>
    - test_trigger_incremental_skips_if_no_new_bytes: 创建 100 字节文件，file_offsets 记录 100，调用 trigger_incremental → 返回 Ok(())，trigger_count 不增、total_stats.records_exported 不变、文件未被处理。
    - test_trigger_incremental_first_seen_records_baseline: file_offsets 无该 path 记录，文件已 100 字节，调用 trigger_incremental → 不处理（跳过），file_offsets[path] 被写入 100。
    - test_trigger_incremental_processes_new_bytes_only: 创建 50 字节起始内容，处理一次记录 offset=50，追加 30 字节，再次触发 → 临时文件包含且仅包含新增的 30 字节内容。
  </behavior>
  <action>
  在 src/cli/watch/mod.rs 内新增两个 `pub(crate)` 函数 + 修改事件路由 + 3 个新单元测试。

  **新增 use 语句**（文件顶部）：
  ```
  use std::fs;
  use std::io::{Read, Seek, SeekFrom, Write};
  ```

  **新增函数 1 — `pub(crate) fn trigger_full_file`**（替代当前 process_log_path 的 Create 分支语义，per D-03/D-10）：

  签名：
  ```
  pub(crate) fn trigger_full_file(
      path: &Path,
      cfg: &Config,
      quiet: bool,
      verbose: bool,
      interrupted: &Arc<std::sync::atomic::AtomicBool>,
      state: &mut WatchLoopState,
      pb: &ProgressBar,
  )
  ```

  body 实现（≤40 行；若超出，提取 `record_offset_after_trigger` 辅助）：
  1. `if !path.exists() { warn!(...); return; }`（与当前 process_log_path 行 287-292 一致）。
  2. 构造 tmp_cfg = cfg.clone()；`tmp_cfg.sqllog.inputs = vec![path.to_string_lossy().into_owned()];`
  3. **不修改 sqlite.append / overwrite**（per D-10：Create 路径保持用户 config 设置）。
  4. 调用 `crate::cli::run::handle_run(&tmp_cfg, quiet, verbose, interrupted, None)`：
     - Ok(stats) → `state.total_stats.merge(&stats); state.trigger_count += 1; state.last_trigger_at = Some(Instant::now());`
       然后 **handle_run 返回后**（SqliteExporter 已 drop，锁已释放，per Pitfall 4）：
       `let new_size = fs::metadata(path).map(|m| m.len()).unwrap_or(0);`
       `let canonical = path.canonicalize().unwrap_or_else(|_| path.to_path_buf());`（per CONTEXT specifics：路径规范化）
       `state.file_offsets.insert(canonical.clone(), new_size);`
       `if let Some(ref url) = state.sqlite_db_url { offsets::save_offset(url, &canonical, new_size); }`（per D-08）
       更新 pb.set_message（保持 process_log_path 行 305-310 的状态行刷新逻辑）。
     - Err(Interrupted) → `interrupted.store(true, Ordering::Release);`
     - Err(other) → `warn!("watch trigger error (full): {e}");`

  **新增函数 2 — `pub(crate) fn trigger_incremental`**（per D-01/D-02/D-04/D-07/D-09）：

  签名：
  ```
  pub(crate) fn trigger_incremental(
      path: &Path,
      cfg: &Config,
      quiet: bool,
      verbose: bool,
      interrupted: &Arc<std::sync::atomic::AtomicBool>,
      state: &mut WatchLoopState,
      pb: &ProgressBar,
  )
  ```

  body 实现 — **必须拆分为 ≤40 行的私有辅助函数**（建议拆 2-3 个）：

  1. **存在性检查**：`if !path.exists() { warn!(...); return; }`

  2. **路径规范化 + 查 offset**（per CONTEXT specifics + D-04）：
     ```
     let canonical = path.canonicalize().unwrap_or_else(|_| path.to_path_buf());
     let new_size = match fs::metadata(path) { Ok(m) => m.len(), Err(e) => { warn!("metadata: {e}"); return; } };
     let start_offset = match state.file_offsets.get(&canonical) {
         Some(&offset) => offset,
         None => {
             // D-04: 首次 Modify 无记录 → 记录基线 = 当前大小，跳过处理
             state.file_offsets.insert(canonical.clone(), new_size);
             if let Some(ref url) = state.sqlite_db_url {
                 offsets::save_offset(url, &canonical, new_size);
             }
             return;
         }
     };
     ```

  3. **无新字节快速跳过**（per D-02）：
     ```
     if new_size <= start_offset { return; }
     ```

  4. **Seek + read_to_end + 写临时文件**（per D-01 + RESEARCH Pattern 1）：
     提取为 `fn read_bytes_to_tempfile(path: &Path, start_offset: u64) -> std::io::Result<tempfile::NamedTempFile>`：
     ```
     use tempfile::Builder;
     let mut file = std::fs::File::open(path)?;
     file.seek(SeekFrom::Start(start_offset))?;
     let mut buffer = Vec::new();
     file.read_to_end(&mut buffer)?;
     let mut tmp = Builder::new()
         .prefix("sqllog2db-watch-")
         .suffix(".log")
         .tempfile()?;
     tmp.write_all(&buffer)?;
     tmp.flush()?;
     Ok(tmp)
     ```
     调用：`let tmp_file = match read_bytes_to_tempfile(path, start_offset) { Ok(f) => f, Err(e) => { warn!("read_bytes: {e}"); return; } };`
     **注意 Pitfall 3：tmp_file 必须持有至 handle_run 返回后才能 drop——使用显式 `let tmp_file = ...;` 不要 `let _ = ...`。**

  5. **构造 tmp_cfg 强制 append 模式**（per D-09 + Anti-Pattern：不可遗漏 append=true）：
     ```
     let mut tmp_cfg = cfg.clone();
     tmp_cfg.sqllog.inputs = vec![tmp_file.path().to_string_lossy().into_owned()];
     if let Some(ref mut sqlite_cfg) = tmp_cfg.exporter.sqlite {
         sqlite_cfg.append = true;
         sqlite_cfg.overwrite = false;
     }
     ```

  6. **调用 handle_run 并更新状态**：
     ```
     match crate::cli::run::handle_run(&tmp_cfg, quiet, verbose, interrupted, None) {
         Ok(stats) => {
             state.total_stats.merge(&stats);
             state.trigger_count += 1;
             state.last_trigger_at = Some(Instant::now());
             // 持久化新 offset（per D-07，handle_run 返回后 SqliteExporter 已 drop）
             state.file_offsets.insert(canonical.clone(), new_size);
             if let Some(ref url) = state.sqlite_db_url {
                 offsets::save_offset(url, &canonical, new_size);
             }
             // 状态行刷新
             let dir = path.parent().map(|p| p.display().to_string()).unwrap_or_default();
             pb.set_message(render_active_status(&dir, state.trigger_count, state.total_stats.records_exported, Duration::from_secs(0)));
         }
         Err(crate::error::Error::Interrupted) => interrupted.store(true, Ordering::Release),
         Err(e) => warn!("watch trigger error (incremental): {e}"),
     }
     // tmp_file 在此处 drop，临时文件自动删除
     ```

  **修改事件路由 — handle_event 函数体**（per D-03）：

  替换当前 handle_event 中的统一 `process_log_path(...)` 调用为按 EventKind 分支：

  ```
  let is_create = matches!(event.kind, EventKind::Create(_));
  let is_content_modify = matches!(
      event.kind,
      EventKind::Modify(ModifyKind::Data(DataChange::Content))
  );
  if !is_create && !is_content_modify { return; }
  let now = Instant::now();
  for path in &event.paths {
      if path.extension().is_none_or(|ext| ext != "log") { continue; }
      if !should_trigger(path, &mut state.debounce_map, now, DEBOUNCE_WINDOW) { continue; }
      if is_create {
          trigger_full_file(path, cfg, quiet, verbose, interrupted, state, pb);
      } else {
          trigger_incremental(path, cfg, quiet, verbose, interrupted, state, pb);
      }
  }
  ```

  **删除 process_log_path**（其语义已由 trigger_full_file 取代，per D-03）。删除旧 TODO 注释 `// TODO: track processed paths to prevent duplicate processing on log rotation (WR-03)`——Phase 70 通过 file_offsets 解决该问题。

  **新增 3 个单元测试**（在文件末尾 `mod tests` 块内）：

  使用 `tempfile::NamedTempFile` + `tempfile::TempDir` 构造测试 fixture。Config 构造使用 `Config::default()` 然后通过字段覆盖配置 inputs。注意：
  - sqlite_db_url 设为 None（避免实际 SQLite 交互——本任务测试 offset 跳过逻辑，handle_run 的 SQLite 行为由 Plan 03 集成测试覆盖）。
  - 但 trigger_incremental 在 sqlite_db_url=None 时不会调用 save_offset，从而避开 SQLite——这正是要测试的内存逻辑。
  - 对 test_trigger_incremental_processes_new_bytes_only：可断言 tmp_file 路径在 handle_run 调用前包含正确字节（提取 read_bytes_to_tempfile 为 pub(super)，单独测试该辅助）。如此可避免 handle_run 真实运行的开销。

  推荐测试方案：直接测试 `read_bytes_to_tempfile`（pub(super)）— 创建 100 字节文件，调用 `read_bytes_to_tempfile(path, 50)`，断言返回的 NamedTempFile 内容恰好是后 50 字节。同时测试 `read_bytes_to_tempfile(path, 100)` → 临时文件为空。

  对 skips_if_no_new_bytes 与 first_seen_records_baseline：构造伪 WatchLoopState（sqlite_db_url=None），调用 trigger_incremental，断言 state.file_offsets 状态变化与 trigger_count 不变。注意 trigger_incremental 在跳过路径下不应调用 handle_run，所以即使 cfg 无效也不会 panic——但需确保 quick-skip 分支不进入 handle_run。

  **变量命名**：`canonical_path`, `start_offset`, `new_size`, `tmp_file`, `tmp_cfg`, `sqlite_cfg`, `buffer`（不用 `c`, `o`, `n`, `t`, `b`）。
  </action>
  <verify>
    <automated>cargo test --lib cli::watch:: 2>&amp;1 | tail -5 | grep -E 'test result: ok\.' &amp;&amp; cargo clippy --all-targets -- -D warnings 2>&amp;1 | tail -3</automated>
  </verify>
  <done>trigger_full_file 和 trigger_incremental 均为 pub(crate) 函数；read_bytes_to_tempfile 为 pub(super) 辅助函数；handle_event 按 EventKind 分支路由；3 个新单元测试 + 既有 9 个 watch 模块单元测试均通过；cargo build --release 成功；cargo clippy --all-targets -- -D warnings + cargo fmt --check 通过；所有函数体 ≤40 行。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem→watch | path 来自 notify::Event；fs::metadata、fs::File::open 操作受 OS 权限保护 |
| watch→tempfile | OS 临时目录写入（NamedTempFile 使用不可预测路径，无路径遍历风险） |
| watch→sqlite | handle_run 内部 SqliteExporter + 独立 offsets::save_offset 连接 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-70-04 | Denial of Service | trigger_incremental 大 append 全量复制到 tempfile | accept | per CONTEXT Claude's Discretion：当前不做过早优化；若实际遇到性能问题，后续 patch 改为内存 buffer |
| T-70-05 | Tampering | tmp_cfg.sqlite.append 强制覆盖 | mitigate | per D-09：增量路径强制 append=true、overwrite=false 是正确性需求（不是漏洞），用户 config 的 overwrite 仅对 Create 路径生效 |
| T-70-06 | Information Disclosure | tempfile 临时文件残留 | mitigate | NamedTempFile drop 时自动删除；Pitfall 3 强制 tmp_file 持有到 handle_run 完成后 |
| T-70-07 | Tampering | file_offsets key 路径混淆 | mitigate | per CONTEXT specifics：path.canonicalize().unwrap_or_else(...) 规范化路径，避免相对/绝对路径不一致 |
</threat_model>

<verification>
- `cargo test --lib cli::watch::` 全部通过（既有 + 新增）
- `cargo build --release` 编译通过
- `cargo clippy --all-targets -- -D warnings` 无警告
- `cargo fmt --check` 通过
- grep 验证：`grep -c 'pub(crate) fn trigger_full_file\|pub(crate) fn trigger_incremental' src/cli/watch/mod.rs` == 2
- grep 验证 D-09：trigger_incremental 函数体内 `append = true` 与 `overwrite = false` 各出现 1 次
- grep 验证 D-02：trigger_incremental 函数体内 `new_size <= start_offset` 出现
</verification>

<success_criteria>
1. WatchLoopState 含 file_offsets 与 sqlite_db_url 字段
2. handle_watch 启动时若 cfg.exporter.sqlite 存在，调用 ensure_offset_table + load_offsets
3. EventKind::Create(_) → trigger_full_file（不强制 append，per D-10）
4. EventKind::Modify(Data(Content)) → trigger_incremental（强制 append=true，per D-09）
5. trigger_incremental 三种路径正确：first-seen 跳过+记录基线 / new_size <= start_offset 跳过 / 正常增量处理
6. trigger_incremental 通过 NamedTempFile 仅读取 [start_offset, new_size) 字节
7. handle_run 返回后才调用 save_offset（避开 SqliteExporter EXCLUSIVE 锁）
8. file_offsets 与 _watch_offsets 表均使用 canonicalize 后的路径
9. 全部函数体 ≤40 行；变量名为描述性词汇
</success_criteria>

<output>
Create `.planning/phases/70-watch/70-02-SUMMARY.md` when done
</output>
