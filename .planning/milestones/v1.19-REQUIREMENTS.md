# Requirements Archive: v1.19 — watch完善与文档对齐

**Archived:** 2026-06-07  
**Milestone:** watch完善与文档对齐  
**Status:** ✅ All requirements complete

---

## v1.19 Requirements

### watch 增强

- [x] **WATCH-07**: 用户可通过 watch 子命令使用 CSV 导出格式（增量追加到 CSV 文件）
  - **Outcome:** 已实现。`force_append_for_watch_trigger` 注入 `csv.append=true + overwrite=false`，多次触发后行数正确累计，header 仅出现一次。
- [x] **WATCH-08**: watch 长时间运行时 error log 以追加模式写入，不覆盖历史错误
  - **Outcome:** 已实现。`write_error_log` 改为 `OpenOptions` 双分支，`append_error_log=true` 时追加；run 路径截断模式防回归测试通过。
- [x] **WATCH-09**: watch 收到 Ctrl+C 时退出码为 130（与 `run` 保持一致）
  - **Outcome:** 已实现。`handle_watch` 中断时返回 `Err(Error::Interrupted)`，main.rs `exit(130)`；signal-aware 退出路径。

### 工程质量

- [x] **QUAL-01**: Phase 67/68/69 VALIDATION.md 草稿补全为正式文件，Phase 70 VALIDATION.md 新建
  - **Outcome:** 已完成。4 份 VALIDATION.md 以 `status: complete` 落地，Per-Task Verification Map 从各 SUMMARY 转录，全部 Sign-Off。
- [x] **QUAL-02**: watch 功能测试补充，整体行覆盖率达到 92%+
  - **Outcome:** 已达成。`cargo llvm-cov --summary-only` 报告 92.06% 行覆盖率（目标 ≥92%）。collector.rs + filter_processor.rs 新增 Group 1–4 单元测试。
- [x] **QUAL-03**: macOS FSEvents 限制的 `#[ignore]` 测试调研落地方案（跨平台或 mock 解决）
  - **Outcome:** 保留 `#[ignore]` + 文档化说明（D-01/D-02）。调研结论：FSEvents 需要实际文件系统事件，mock 注入需引入新依赖且与 notify crate 深度耦合；保留 `#[ignore]` 为最简可维护方案。

### 文档

- [x] **DOC-04**: README 补充 `watch` 用法、`init --interactive` 说明、进度选项
  - **Outcome:** 已完成。功能特性升级为 5 命令（含 watch），新增持续监听功能特性条目，快速入门追加 watch/init --interactive/quiet+verbose 三段示例。
- [x] **DOC-05**: `watch` / `validate` / `stats` 子命令 `--help` 补充示例和选项说明
  - **Outcome:** 已完成。Watch `after_help` 补充 `--quiet` 示例；Validate `after_help` 补充 `--verbose` 示例；Stats `after_help` 原有 3 个示例保留不变。

---

## Traceability

| Requirement | Phase | Status |
|-------------|-------|--------|
| WATCH-07 | Phase 1 | ✅ Complete |
| WATCH-08 | Phase 1 | ✅ Complete |
| WATCH-09 | Phase 1 | ✅ Complete |
| QUAL-02 | Phase 2 | ✅ Complete |
| QUAL-03 | Phase 2 | ✅ Complete |
| QUAL-01 | Phase 3 | ✅ Complete |
| DOC-04 | Phase 3 | ✅ Complete |
| DOC-05 | Phase 3 | ✅ Complete |

**Coverage: 8/8 — 100% complete**

---

*Requirements archived: 2026-06-07 after v1.19 milestone completion*
