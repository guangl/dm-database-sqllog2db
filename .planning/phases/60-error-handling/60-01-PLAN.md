---
phase: 60-error-handling
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/logging.rs
  - src/cli/run/parallel.rs
autonomous: true
requirements:
  - STRUCT-03
tags:
  - rust
  - error-handling
  - thiserror
  - refactor

must_haves:
  truths:
    - "src/logging.rs 第 60 行的 .unwrap() 在同一行末尾带 `// infallible: writing to a String never fails` 注释"
    - "src/cli/run/parallel.rs 第 87 行附近的 .expect(\"parallel CSV requires CSV exporter\") 在调用前有 `// infallible: process_csv_parallel is only called when CSV exporter is present` 注释"
    - "src/pipeline/normalizer.rs:310 与 :418 的 expect 调用保持不变（D-03）"
    - "三处 rayon ThreadPoolBuilder.map_err（parallel.rs / sqlite_parallel.rs / prescan.rs）保留原样（D-01：rayon::ThreadPoolBuildError 无 From<...> for Error）"
    - "所有携带 path/reason 字段的 .map_err（logging.rs:82,112；parser.rs:58,66,108；config/mod.rs:38,45；exporter/csv/mod.rs:95,116,134,207；exporter/sqlite/mod.rs:118,127；cli/init.rs:31,40）保留原样（D-01：From 无法注入上下文字段）"
    - "src/error.rs 不被修改（D-04：不引入新 Error 变体或 From impl）"
    - "cargo clippy --all-targets -- -D warnings 通过且无 unwrap_used / expect_used 报告"
    - "cargo test 全部通过（功能行为不变）"
    - "cargo fmt --check 通过"
  artifacts:
    - path: "src/logging.rs"
      provides: "format_utc_timestamp 中 write! 的 infallible 注释"
      contains: "// infallible: writing to a String never fails"
    - path: "src/cli/run/parallel.rs"
      provides: "process_csv_parallel 中 csv_cfg.expect 的 infallible 前置注释"
      contains: "// infallible: process_csv_parallel is only called when CSV exporter is present"
  key_links:
    - from: "src/logging.rs:60"
      to: "format_utc_timestamp 调用点"
      via: "write! 宏对 String 的写入"
      pattern: "\\.unwrap\\(\\); // infallible: writing to a String never fails"
    - from: "src/cli/run/parallel.rs:87"
      to: "process_csv_parallel 入口"
      via: "上游已校验 CSV exporter 存在"
      pattern: "// infallible: process_csv_parallel is only called when CSV exporter is present"
---

<objective>
为 Phase 60「错误处理路径统一」交付完整的代码审计与最小修改集。

目标：满足 STRUCT-03 与 ROADMAP Phase 60 的四条成功标准——
1. 生产代码中每个 unwrap/expect 均已有 infallible 注释或在测试代码中
2. From 自动转换路径保持集中在 src/error.rs，未携带上下文字段的 map_err 已审计
3. cargo clippy 与 cargo test 全部通过，无 unwrap_used / expect_used 警告
4. 功能行为完全不变

实现策略（来自 60-RESEARCH.md + 60-PATTERNS.md）：
- 仅 2 处生产代码 unwrap/expect 需要新增 infallible 注释（logging.rs:60、parallel.rs:87）
- 3 处 rayon map_err 已确认无法替换为 ?（rayon::ThreadPoolBuildError 无 From），保留
- 所有携带 path/reason 字段的 map_err 必须保留（D-01）
- src/error.rs 不修改（D-04）
- src/pipeline/normalizer.rs:310,418 保持不变（D-03）
- 测试代码中的 unwrap 保持不变（D-02）

Purpose: 让 grep 扫描结果可逐行解释——要么有 // infallible 注释，要么在测试代码中，要么在保留的 map_err closure 中携带上下文。
Output: 两处代码注释 + 一份审计兜底验证。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/60-error-handling/60-CONTEXT.md
@.planning/phases/60-error-handling/60-RESEARCH.md
@.planning/phases/60-error-handling/60-PATTERNS.md
@.planning/phases/60-error-handling/60-VALIDATION.md
@CLAUDE.md

<interfaces>
<!-- src/error.rs 现有 From 覆盖（只读参考，不修改） -->

Error 枚举（src/error.rs:70-89）通过 thiserror `#[from]` 已支持以下自动转换：
  - ConfigError → Error::Config
  - FileError   → Error::File
  - ParserError → Error::Parser
  - ExportError → Error::Export
  - io::Error   → Error::Io

不支持的源类型（关键判定依据）：
  - rayon::ThreadPoolBuildError → 无 From impl，必须保留 .map_err 经 std::io::Error::other(e) 中转
</interfaces>

<scope_anchors>
本 Plan 仅在以下文件添加 infallible 注释；其他文件读取仅用于审计验证，不修改：
  - src/logging.rs（行 60）
  - src/cli/run/parallel.rs（行 87 附近）

明确不修改：
  - src/error.rs（D-04）
  - src/pipeline/normalizer.rs（D-03）
  - 任何 tests.rs / #[cfg(test)] 块内的 unwrap/expect（D-02）
  - 三处 rayon map_err（parallel.rs:117-120、sqlite_parallel.rs:116-119、prescan.rs:114-117）（D-01）
  - 所有携带 path/reason 字段的 map_err closure（D-01）
</scope_anchors>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 为 logging.rs:60 的 write! unwrap 添加 infallible 注释</name>
  <files>src/logging.rs</files>
  <read_first>
    - src/logging.rs（行 30-65，确认 format_utc_timestamp 的实现与第 60 行 .unwrap() 当前形态）
    - .planning/phases/60-error-handling/60-CONTEXT.md（D-02、Claude's Discretion 段）
    - .planning/phases/60-error-handling/60-PATTERNS.md（Pattern A：infallible 注释格式）
  </read_first>
  <action>
    在 src/logging.rs 第 60 行 `.unwrap();` 末尾、同一行追加注释：
    `// infallible: writing to a String never fails`

    具体落点（per D-02 + Claude's Discretion）：当前第 60 行的 `.unwrap();` 改为
    `.unwrap(); // infallible: writing to a String never fails`

    不得调整：write! 宏的 fmt 字符串、buf 容量、函数签名、其他行的代码。
    不得新增：use 语句、helper 函数、log 语句。
    不得触碰：logging.rs:82 与 :112 的 .map_err（携带 path + reason 字段，per D-01 保留）。

    完成后立即运行 cargo fmt 保证格式正确。
  </action>
  <verify>
    <automated>grep -n '\.unwrap(); // infallible: writing to a String never fails' src/logging.rs</automated>
    <automated>cargo fmt --check</automated>
    <automated>cargo clippy --all-targets -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    - source: src/logging.rs 包含字面文本 `.unwrap(); // infallible: writing to a String never fails`
    - source: src/logging.rs 第 60 行（或重新格式化后对应行）以 `.unwrap();` 起、以注释 `// infallible: writing to a String never fails` 结
    - source: src/logging.rs 中 `.map_err(|e| {` 出现的次数与本 Plan 开始前一致（grep -c 不变）
    - cli: `cargo fmt --check` 退出码 0
    - cli: `cargo clippy --all-targets -- -D warnings` 退出码 0
    - behavior: format_utc_timestamp() 返回的字符串格式 `YYYY-MM-DD HH:MM:SS` 不变
  </acceptance_criteria>
  <done>logging.rs:60 的 unwrap 已带 infallible 注释，cargo fmt + clippy 通过，无其他改动。</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: 为 parallel.rs:87 的 expect 添加 infallible 前置注释</name>
  <files>src/cli/run/parallel.rs</files>
  <read_first>
    - src/cli/run/parallel.rs（行 75-130，确认 process_csv_parallel 入口、第 87 行的 .expect 调用位置以及第 117-120 行的 rayon map_err）
    - .planning/phases/60-error-handling/60-CONTEXT.md（D-01 与 D-02）
    - .planning/phases/60-error-handling/60-PATTERNS.md（Pattern A 的"前置注释块"形式 与 Pattern B：rayon map_err 保留模式）
  </read_first>
  <action>
    在 src/cli/run/parallel.rs 第 87 行 `.expect("parallel CSV requires CSV exporter");` 之前的一行
    插入注释行（per D-02 + Pattern A 第 2 种形式）：
    `// infallible: process_csv_parallel is only called when CSV exporter is present`

    插入后该 builder 链应保持为：
    第 83 行起 `let csv_cfg = cfg`
    第 84 行 `    .exporter`
    第 85 行 `    .csv`
    第 86 行 `    .as_ref()`
    新增的注释行 `    // infallible: process_csv_parallel is only called when CSV exporter is present`
    原第 87 行 `    .expect("parallel CSV requires CSV exporter");`

    注释行缩进与上下文匹配（method chain 上的 4 空格缩进）。

    严格保持不变（per D-01）：
    - 第 117-120 行的 `let pool = rayon::ThreadPoolBuilder::new()...build().map_err(|e| Error::Io(std::io::Error::other(e)))?;` 一行也不改
    - process_csv_parallel 中其它任何 .unwrap_or_default() / .unwrap_or(Path::new(".")) 不动（这些是 *_or 系列，不在 grep 范围）
    - sqlite_parallel.rs 与 prescan.rs 不修改

    完成后运行 cargo fmt 保证格式正确。
  </action>
  <verify>
    <automated>grep -n 'infallible: process_csv_parallel is only called when CSV exporter is present' src/cli/run/parallel.rs</automated>
    <automated>grep -c '\.map_err(|e| Error::Io(std::io::Error::other(e)))' src/cli/run/parallel.rs</automated>
    <automated>cargo fmt --check</automated>
    <automated>cargo clippy --all-targets -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    - source: src/cli/run/parallel.rs 包含字面注释 `// infallible: process_csv_parallel is only called when CSV exporter is present`
    - source: 该注释紧邻 `.expect("parallel CSV requires CSV exporter")` 的上一行
    - source: src/cli/run/parallel.rs 中 `.map_err(|e| Error::Io(std::io::Error::other(e)))` 出现次数与本 Plan 开始前一致（rayon 三处保留，grep -c 结果不变）
    - cli: `cargo fmt --check` 退出码 0
    - cli: `cargo clippy --all-targets -- -D warnings` 退出码 0
  </acceptance_criteria>
  <done>parallel.rs:87 的 expect 已加 infallible 前置注释，rayon map_err 与其他 map_err 全部保留原样，clippy 干净。</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: 全代码库 unwrap/expect/map_err 审计与 ROADMAP 成功标准兜底验证</name>
  <files>.planning/phases/60-error-handling/60-AUDIT.md</files>
  <read_first>
    - src/error.rs（确认无修改，per D-04）
    - src/pipeline/normalizer.rs（行 295-420，确认 :310 与 :418 的 expect 注释未被改动，per D-03）
    - src/cli/run/sqlite_parallel.rs（行 110-125，确认 rayon map_err 未被改动）
    - src/cli/run/prescan.rs（行 108-122，确认 rayon map_err 未被改动）
    - .planning/phases/60-error-handling/60-RESEARCH.md（Production unwrap/expect 完整审计 表）
    - .planning/phases/60-error-handling/60-VALIDATION.md（Sampling Rate 与 Manual-Only Verifications）
  </read_first>
  <action>
    本任务交付审计报告 .planning/phases/60-error-handling/60-AUDIT.md，并执行 ROADMAP Phase 60
    成功标准的兜底自动化验证。**不修改任何 src/ 下的代码**。

    步骤 1：运行下列命令并把原始输出整段保存到 60-AUDIT.md 的对应章节
      A. `grep -rn 'unwrap()\|expect(' src/ --include="*.rs"`（覆盖成功标准 1 的扫描命令）
      B. `grep -rn '\.map_err' src/ --include="*.rs"`
      C. `cargo clippy --all-targets -- -D warnings 2>&1`
      D. `cargo test 2>&1 | tail -40`
      E. `cargo fmt --check`（退出码 0 表示干净）

    步骤 2：对 A 的每一行结果分类标注，写入 60-AUDIT.md 的「unwrap/expect 分类表」：
      - production_commented：生产代码且已有 // infallible 注释或 /// # Panics 文档（如 logging.rs:60、parallel.rs:87、normalizer.rs:310/418、stats/normalize.rs:56）
      - test_code：位于 tests.rs 文件 或 #[cfg(test)] 块（per D-02 保留）
      - unwrap_or_family：误匹配的 unwrap_or / unwrap_or_default / unwrap_or_else（grep 命中但不属于成功标准 1）
      - **未注释生产代码 unwrap/expect 必须为 0**——若发现新增条目，列出并标记为「待处理」

    步骤 3：对 B 的每一行结果分类标注，写入 60-AUDIT.md 的「map_err 保留判定表」：
      - retained_with_context：closure 构造携带 path / reason / line_number 字段的变体（per D-01 保留）
      - retained_rayon：rayon::ThreadPoolBuildError 的 std::io::Error::other(e) 中转模式（per D-01 保留）
      - replaceable_with_question_mark：未列出（**审计结果应为 0**——若发现说明 D-01 判定遗漏，列出并标记为「待评估」）

    步骤 4：在 60-AUDIT.md 末尾写「ROADMAP Phase 60 成功标准核对」章节，逐条勾选：
      - [ ] 标准 1：grep 结果中每个 unwrap/expect 已有注释或在测试代码中（贴上证据计数）
      - [ ] 标准 2：所有 From 实现位于 src/error.rs（验证 `git diff --stat src/error.rs` 输出为空）
      - [ ] 标准 3：cargo clippy --all-targets -- -D warnings 通过；clippy 输出中不含 `unwrap_used` 或 `expect_used` 关键字
      - [ ] 标准 4：cargo test 全部通过

    步骤 5：若任意标准不达标，**不要伪造勾选**——保持未勾选状态并在文档末尾列出 BLOCKER 清单，留给后续 plan 或 gap closure。

    报告文件结构（最少 5 个 section）：
      # Phase 60 — 错误处理审计报告
      ## 1. grep 'unwrap()|expect(' 扫描结果分类
      ## 2. grep '.map_err' 扫描结果分类
      ## 3. cargo clippy 输出摘录（关键行）
      ## 4. cargo test 输出摘录（pass/fail 计数）
      ## 5. ROADMAP Phase 60 成功标准核对
  </action>
  <verify>
    <automated>test -f .planning/phases/60-error-handling/60-AUDIT.md</automated>
    <automated>grep -c '^## ' .planning/phases/60-error-handling/60-AUDIT.md</automated>
    <automated>grep -q 'ROADMAP Phase 60 成功标准核对' .planning/phases/60-error-handling/60-AUDIT.md</automated>
    <automated>cargo clippy --all-targets -- -D warnings</automated>
    <automated>cargo test</automated>
    <automated>cargo fmt --check</automated>
    <automated>git diff --stat src/error.rs | grep -c 'src/error.rs' || true</automated>
    <human-check>核对 60-AUDIT.md 的「unwrap/expect 分类表」中无 production_uncommented 条目，map_err 表中无 replaceable_with_question_mark 条目</human-check>
  </verify>
  <acceptance_criteria>
    - source: .planning/phases/60-error-handling/60-AUDIT.md 存在并包含至少 5 个 `## ` 二级标题
    - source: 60-AUDIT.md 含字面文本 "ROADMAP Phase 60 成功标准核对"
    - source: 60-AUDIT.md 含 grep 'unwrap()|expect(' 的原始输出且每行已标注分类（4 个分类标签之一）
    - source: 60-AUDIT.md 含 grep '.map_err' 的原始输出且每行已标注分类（3 个分类标签之一）
    - behavior: 60-AUDIT.md 报告的 production_uncommented unwrap/expect 数量为 0
    - behavior: 60-AUDIT.md 报告的 replaceable_with_question_mark map_err 数量为 0
    - cli: `cargo clippy --all-targets -- -D warnings` 退出码 0
    - cli: `cargo test` 退出码 0
    - cli: `cargo fmt --check` 退出码 0
    - cli: `git diff --stat src/error.rs` 输出为空（D-04：src/error.rs 未被修改）
    - cli: 在 cargo clippy 完整输出中（grep 自身命令除外），grep -v '^#' 后 `grep -c -E '\b(unwrap_used|expect_used)\b'` 结果为 0
  </acceptance_criteria>
  <done>60-AUDIT.md 已交付，ROADMAP Phase 60 四条成功标准全部勾选通过，src/error.rs 零 diff，cargo 全套工具链绿。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | 本 Plan 为纯代码审计 + 注释添加，无信任边界穿越；无外部输入、网络、磁盘写入路径变更 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-60-01 | Tampering | src/error.rs 意外被修改（违反 D-04） | mitigate | Task 3 acceptance_criteria 显式校验 `git diff --stat src/error.rs` 为空；checker 复核 |
| T-60-02 | Tampering | 携带上下文的 map_err 被误替换为 ?（违反 D-01，导致错误信息丢失路径/原因） | mitigate | Task 3 审计表中显式分类，replaceable_with_question_mark 数量必须为 0；e2e 测试（Phase 57 新增）会捕获错误消息变化 |
| T-60-03 | DoS | logging.rs format_utc_timestamp 因注释错位破坏函数主体 | accept | 注释仅在 .unwrap() 行尾，cargo build + cargo test 可立刻捕获；风险极低 |
| T-60-04 | InfoDisclosure | 审计报告 60-AUDIT.md 中可能泄露源码片段 | accept | 报告仅在 .planning/ 目录内、随项目仓库版本控制；无新增 PII 或机密 |
| T-60-SC | Tampering | npm/pip/cargo 等包管理器恶意包注入 | N/A | 本 Plan **不安装任何包**，Cargo.toml 不修改；Package Legitimacy Gate 不适用（researcher 已在 60-RESEARCH.md 跳过此节） |
</threat_model>

<verification>
Phase 60 整体验证（在所有任务完成后执行）：

1. `cargo fmt --check` — 格式干净
2. `cargo clippy --all-targets -- -D warnings` — 无警告，无 unwrap_used / expect_used 报告
3. `cargo test` — 全部测试通过（与 Phase 57 e2e 套件兼容）
4. `grep -rn 'unwrap()\|expect(' src/ --include="*.rs"` — 每行结果应可在 60-AUDIT.md 中找到对应分类
5. `git diff --stat src/error.rs` — 输出为空（D-04）
6. `git diff --stat src/pipeline/normalizer.rs` — 输出为空（D-03）
7. `git diff --stat src/cli/run/sqlite_parallel.rs src/cli/run/prescan.rs` — 输出为空（D-01 三处 rayon 保留）
</verification>

<success_criteria>
对应 ROADMAP Phase 60 的四条成功标准（goal-backward 派生）：

1. **每个 unwrap/expect 可解释**：grep 扫描结果中所有命中行属于以下之一——生产代码已带 infallible 注释 / 测试代码 / *_or 系列误匹配，0 例外（60-AUDIT.md 给出可审计的逐行分类）
2. **From 集中且 map_err 已审计**：src/error.rs 零 diff；所有保留的 .map_err 均携带不可由 From 自动填充的上下文字段（path / reason / line_number）或属于 rayon 错误的 std::io::Error::other 中转模式
3. **clippy + test 绿**：`cargo clippy --all-targets -- -D warnings` 退出码 0 且无 `unwrap_used` / `expect_used` 关键字；`cargo test` 退出码 0
4. **行为不变**：cargo test 既有套件（含 Phase 57 e2e）100% 通过；无任何 src/ 文件中的非注释代码字节变化（除两处新增的 // infallible 注释）
</success_criteria>

<output>
Create `.planning/phases/60-error-handling/60-01-SUMMARY.md` when done, including:
- 实际修改的两处注释证据（grep 输出）
- 60-AUDIT.md 中四条成功标准的勾选结果
- 三个保留判定（D-01/D-03/D-04）的零 diff 证明
- 任何待跟进项（理论上应为空）
</output>
