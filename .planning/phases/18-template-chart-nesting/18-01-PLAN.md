---
phase: 18-template-chart-nesting
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pipeline/mod.rs
  - src/config/mod.rs
autonomous: true
requirements:
  - CONFIG-03
  - CONFIG-04

must_haves:
  truths:
    - "用 [template] 顶层子表（含 enable / output_csv_path / output_sqlite_table）的 TOML 可被解析"
    - "用 [charts] 顶层子表（含 output_dir / top_n / *_bar / *_hist / *_line / *_pie）的 TOML 可被解析"
    - "用 [replace_parameters] 顶层子表（含 enable / placeholders）的 TOML 可被解析"
    - "用 [filter] / [filter.include] / [filter.exclude] / [filter.indicators] / [filter.sql] / [filter.record_sql] 顶层子表的 TOML 可被解析"
    - "用 [output.fields] 顶层子表（fields: Vec<String>）的 TOML 可被解析"
    - "含旧 [pipeline] 段（任意子表）的 TOML 在 validate() 阶段返回错误，错误消息列出五条迁移映射"
    - "PipelineConfig 不再有 filters / normalize / template_analysis / charts / fields 字段，已被废弃删除"
    - "validate_charts 跨字段依赖检查改为依赖 template.enable=true"
    - "apply_overrides 接受 template.* / charts.* / replace_parameters.* / filter.enable / output.fields 路径"
    - "cargo test --lib config:: 通过；cargo test --lib pipeline:: 通过；cargo clippy --all-targets -- -D warnings 零警告"
  artifacts:
    - path: "src/pipeline/mod.rs"
      provides: "TemplateConfig / ChartsConfig / NormalizeConfig / OutputConfig struct 顶层化"
      contains: "pub struct TemplateConfig"
    - path: "src/config/mod.rs"
      provides: "Config 顶层字段重组 + 旧路径检测 + validate_template/charts/output_fields"
      contains: "pub template: Option<TemplateConfig>"
  key_links:
    - from: "src/config/mod.rs Config"
      to: "src/pipeline/mod.rs TemplateConfig / ChartsConfig / NormalizeConfig / OutputConfig"
      via: "use crate::pipeline::{TemplateConfig, ChartsConfig, NormalizeConfig, OutputConfig}"
      pattern: "pub template: Option<TemplateConfig>"
    - from: "src/config/mod.rs validate()"
      to: "旧 [pipeline] 路径检测"
      via: "_pipeline_deprecated: Option<toml::Value> 字段"
      pattern: "_pipeline_deprecated"
---

<objective>
将 Phase 17 已完成的 `[pipeline.filters]` 嵌套结构 + 当前所有 `[pipeline.*]` 子表（normalize / template_analysis / charts / filters / fields）统一提升到 Config 顶层，彻底清空 `[pipeline]` 命名空间。

破坏性升级：不实现 serde alias 兼容层；validate() 阶段通过 `_pipeline_deprecated: Option<toml::Value>` 捕获旧 `[pipeline]` 段并返回明确迁移错误。

实现 D-01（5 个顶层路径）、D-03（template.enable 字段名 + 移除 enabled）、D-04（output_csv_path / output_sqlite_table 显式字段）、D-05（无 alias 兼容层）、D-06（旧路径检测）。

Purpose: 让 Config 顶层结构语义化，移除中间命名空间 `pipeline`；为 Plan 02 调用方迁移做好编译基础。
Output: src/pipeline/mod.rs 与 src/config/mod.rs 的结构性重构，能独立通过 `cargo build` 与该模块的单元测试，但顶层调用方（cli/run.rs 等）此时编译失败 — 由 Plan 02 修复。

注：CONTEXT.md `<decisions>` 段提到 `[features.*]` 是历史用词，当前命名空间实际为 `[pipeline.*]`，D-01 / D-06 等所有"旧路径"读作 `[pipeline.*]`。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/18-template-chart-nesting/18-CONTEXT.md
@.planning/phases/18-template-chart-nesting/18-PATTERNS.md
@.planning/phases/17-filter-nesting/17-CONTEXT.md

<interfaces>
现有 struct（即将被重构）— src/pipeline/mod.rs:

```rust
// 当前 NormalizeConfig（保留 struct，迁移路径）
pub struct NormalizeConfig {
    #[serde(default = "default_true")]
    pub enable: bool,
    #[serde(default)]
    pub placeholders: Vec<String>,
}

// 当前 TemplateAnalysisConfig（重命名 + 添加字段）
pub struct TemplateAnalysisConfig {
    #[serde(default)]
    pub enabled: bool,
}

// 当前 ChartsConfig（仅迁移路径，字段不变）
pub struct ChartsConfig {
    pub output_dir: String,
    #[serde(default = "default_top_n")]
    pub top_n: usize,
    #[serde(default = "default_true")]
    pub frequency_bar: bool,
    // latency_hist / trend_line / user_pie 同理
}

// 当前 PipelineConfig（即将退化）
pub struct PipelineConfig {
    pub filters: Option<FiltersFeature>,
    #[serde(alias = "replace_parameters")]
    pub normalize: Option<NormalizeConfig>,
    pub fields: Option<Vec<String>>,
    pub template_analysis: Option<TemplateAnalysisConfig>,
    pub charts: Option<ChartsConfig>,
}

// PipelineConfig 也实现了 field_mask() / ordered_field_indices()
impl PipelineConfig {
    pub fn field_mask(&self) -> FieldMask;
    pub fn ordered_field_indices(&self) -> Vec<usize>;
}
```

现有 Config struct — src/config/mod.rs:

```rust
pub struct Config {
    #[serde(default)] pub sqllog: SqllogConfig,
    #[serde(default)] pub logging: LoggingConfig,
    #[serde(default)] pub pipeline: PipelineConfig,
    #[serde(default)] pub exporter: ExporterConfig,
    #[serde(default)] pub resume: ResumeConfig,
}
```

错误类型 — src/error.rs:

```rust
pub enum ConfigError {
    InvalidValue { field: String, value: String, reason: String },
    // ...
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 重构 src/pipeline/mod.rs — 提升 struct 至顶层语义</name>
  <files>src/pipeline/mod.rs</files>
  <read_first>
    - src/pipeline/mod.rs（当前 NormalizeConfig / TemplateAnalysisConfig / ChartsConfig / PipelineConfig 与单元测试）
    - .planning/phases/18-template-chart-nesting/18-CONTEXT.md（D-03 字段名 / D-04 输出字段 / Phase 边界说明）
    - .planning/phases/18-template-chart-nesting/18-PATTERNS.md（NormalizeConfig 模式参考 lines 79-99）
  </read_first>
  <behavior>
    - 测试 1（已存在保留）：NormalizeConfig 默认 enable=true，placeholders 为空；placeholder_override 三种返回值（None / Some(false) / Some(true)）逻辑不变
    - 测试 2（新增）：TemplateConfig 默认值：enable=false, output_csv_path="", output_sqlite_table=""
    - 测试 3（新增）：TemplateConfig 从 toml 字符串 `enable = true\noutput_csv_path = "out.csv"\noutput_sqlite_table = "tpl"` 解析后字段对应
    - 测试 4（新增）：TemplateConfig 从空字符串解析得到默认值（serde(default) 生效）
    - 测试 5（已存在改名）：ChartsConfig 默认值与字段反序列化不变（output_dir="charts/", top_n=10, *_bar/_hist/_line/_pie=true）
    - 测试 6（新增）：OutputConfig 含 `fields: Option<Vec<String>>`，并暴露 `field_mask()` / `ordered_field_indices()` 方法（行为复用现 PipelineConfig 实现）
    - 测试 7（删除）：原 test_pipeline_config_default / test_template_analysis_config_default / test_template_analysis_config_deserialize_* 全部移除（TemplateAnalysisConfig / PipelineConfig 中相关字段已删除）
  </behavior>
  <action>
    第一步 — 重命名并扩展 TemplateConfig（替换原 TemplateAnalysisConfig，lines 129-135）：
    - 删除 `pub struct TemplateAnalysisConfig`（含其 `enabled: bool` 字段）
    - 新增 `pub struct TemplateConfig`，派生 `Debug, Deserialize, Clone, Default`
    - 字段（全部 `#[serde(default)]`）：
        * `pub enable: bool`（字段名由 enabled 改 enable，对齐 NormalizeConfig / FiltersFeature）— 实现 D-03
        * `pub output_csv_path: String`（默认空串，空串语义 = 不生成）— 实现 D-04
        * `pub output_sqlite_table: String`（默认空串，空串语义 = 不生成）— 实现 D-04
    - 模块顶层（紧邻 ChartsConfig）放置，文档注释写 `/// [template] 配置段`
    - 不要保留 enabled 字段或 alias（D-05 破坏性升级）

    第二步 — 保留 ChartsConfig 与 NormalizeConfig 不变（仅修改文档注释）：
    - ChartsConfig（lines 137-170）：把 doc 注释 `[pipeline.charts]` 改为 `[charts]`，struct / Default / 字段全部保持原样
    - NormalizeConfig（lines 80-99 + impl 100-119）：把 doc 注释 `[pipeline.normalize]` 改为 `[replace_parameters]`，结构与 placeholder_override 实现不变；将原 `#[serde(alias = "replace_parameters")]` alias 标记（位于 PipelineConfig 字段处）移除（顶层化后 alias 失去意义）

    第三步 — 新增 OutputConfig（紧邻 NormalizeConfig 上方）：
    - `pub struct OutputConfig`，派生 `Debug, Deserialize, Clone, Default`
    - 字段 `#[serde(default)] pub fields: Option<Vec<String>>`
    - impl 块复制 PipelineConfig 中 `field_mask()` 和 `ordered_field_indices()` 两个方法的方法体，保持签名 `pub fn field_mask(&self) -> FieldMask` 与 `pub fn ordered_field_indices(&self) -> Vec<usize>`

    第四步 — 删除 PipelineConfig 结构与 impl 块（lines 173-208）：
    - 完整删除 `pub struct PipelineConfig { ... }` 定义
    - 完整删除 `impl PipelineConfig { ... }` 块（含 field_mask / ordered_field_indices，已迁移到 OutputConfig）
    - 不要导出 PipelineConfig；如有 `pub use` 在 lib.rs / config/mod.rs，由下游 task 处理

    第五步 — 更新单元测试模块（lines 254-507）：
    - 删除 `test_pipeline_config_default`, `test_pipeline_config_default_charts_is_none`, `test_ordered_field_indices_*`（5 个），改为针对 `OutputConfig` 重写：方法名前缀 `test_output_config_*`
    - 删除 `test_template_analysis_config_default`, `test_template_analysis_config_deserialize_enabled_true`, `test_template_analysis_config_deserialize_empty_is_false`
    - 新增（参见 behavior）：
        * `test_template_config_default` — enable=false, output_csv_path="", output_sqlite_table=""
        * `test_template_config_deserialize_full` — 全字段解析
        * `test_template_config_deserialize_empty_is_default` — 空 toml → 默认值
        * `test_output_config_field_mask_default` / `test_output_config_field_mask_with_names`
        * `test_output_config_ordered_indices_preserves_user_order`
    - 保留 NormalizeConfig 的全部测试（test_placeholder_override_*, test_normalize_config_default, test_default_true_via_serde）
    - 保留 ChartsConfig 的全部测试（test_charts_config_*）
    - 保留 Pipeline / FieldMask / LogProcessor 测试不变

    第六步 — 导出顶层 struct：
    - 在 src/pipeline/mod.rs 顶部 `pub use` 区段确保 `TemplateConfig`, `ChartsConfig`, `NormalizeConfig`, `OutputConfig` 都可见（这些已在同模块定义，无需额外 use；但需删除 `pub use ... PipelineConfig` 相关导出，由 config/mod.rs 内 use 一致处理）
  </action>
  <verify>
    <automated>cargo test --lib -- pipeline:: 2>&amp;1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - 源断言：`grep -n "pub struct TemplateConfig" src/pipeline/mod.rs` 命中且 `grep -n "TemplateAnalysisConfig" src/pipeline/mod.rs` 不命中（旧 struct 已删除）
    - 源断言：`grep -n "pub struct PipelineConfig" src/pipeline/mod.rs` 不命中
    - 源断言：`grep -n "pub struct OutputConfig" src/pipeline/mod.rs` 命中
    - 源断言：`grep -nE 'pub +(enable|output_csv_path|output_sqlite_table) *:' src/pipeline/mod.rs` 同时命中三个字段
    - 源断言：TemplateConfig 字段 `enable` 而非 `enabled`（`grep -n 'pub enable: bool' src/pipeline/mod.rs` 命中，并位于 TemplateConfig 块内）
    - 测试命令：`cargo test --lib pipeline:: -- --nocapture` 退出码 0，且新增的 5 个 test_template_config_* / test_output_config_* 全部 PASSED
    - Clippy：本步骤完成后允许临时 clippy 警告（依赖文件由 Plan 02 修复）；但 src/pipeline/mod.rs 本身单独 `cargo clippy --lib -- -D warnings` 不必通过（跨文件引用尚未修正）
  </acceptance_criteria>
  <done>TemplateConfig / OutputConfig 已落地；TemplateAnalysisConfig / PipelineConfig 已删除；ChartsConfig 与 NormalizeConfig 内容不变；本模块单元测试通过。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 重构 src/config/mod.rs — 顶层字段 + 旧路径检测 + 校验方法</name>
  <files>src/config/mod.rs</files>
  <read_first>
    - src/config/mod.rs（当前 Config struct / validate / apply_one / validate_pipeline_* / 测试）
    - src/pipeline/mod.rs（Task 1 完成后的最新状态：TemplateConfig / OutputConfig / NormalizeConfig / ChartsConfig）
    - src/pipeline/filters.rs（FiltersFeature struct，path 从 pipeline.filters 改为 filter — 注意 Task 1 已删除 PipelineConfig.filters，本任务仅做顶层字段迁移）
    - src/error.rs（ConfigError::InvalidValue 构造模式）
    - .planning/phases/18-template-chart-nesting/18-CONTEXT.md（D-06 旧路径检测、specifics 段错误消息样本）
    - .planning/phases/18-template-chart-nesting/18-PATTERNS.md（validate_pipeline_charts / apply_one 模式参考）
  </read_first>
  <behavior>
    - 测试 1（新增）：含 `[pipeline]` 段（任意子表）的 toml 在 validate() 返回 Err；err.to_string() 同时包含 `[pipeline.template_analysis] → [template]`、`[pipeline.charts] → [charts]`、`[pipeline.normalize] → [replace_parameters]`、`[pipeline.filters.* ] → [filter.*]`、`[pipeline.fields] → [output.fields]` 五条迁移提示
    - 测试 2（新增）：新格式 toml（`[template]` enable=true + `[charts]` output_dir + `[filter]` enable=false + `[output]` fields）validate() 通过
    - 测试 3（新增）：`[charts]` 存在但 `[template].enable` 为 false 时，validate() 返回 Err，错误消息包含 `[charts]` 与 `[template]`
    - 测试 4（新增）：`[output] fields = ["unknown_field"]` 时 validate() 返回 Err，错误消息包含 `output.fields` 与 unknown_field 名
    - 测试 5（新增）：`apply_overrides` 接受 `template.enable=true` / `template.output_csv_path=/tmp/a.csv` / `template.output_sqlite_table=tpl` / `charts.output_dir=...` / `charts.top_n=...` / `charts.frequency_bar=false` / `replace_parameters.enable=false` / `filter.enable=true` / `output.fields=sql,ts` 路径
    - 测试 6（新增）：`apply_overrides` 拒绝旧 `pipeline.*` 路径（返回 InvalidValue with unknown config key）
    - 测试 7（删除）：原所有引用 `cfg.pipeline.*` / `[pipeline.*]` toml 的旧测试同步重写为新路径（test_validate_invalid_regex_in_filters / test_validate_new_nested_format_passes / test_validate_charts_* / test_validate_and_compile_* 等）
    - 测试 8（新增）：validate_and_compile() 在新格式 + filter.enable=true + 有效正则时返回 `Ok(Some(...))`；filter.enable=false 时返回 `Ok(None)`
  </behavior>
  <action>
    第一步 — Config struct 顶层化（替换 lines 17-29）：
    - 移除字段 `pub pipeline: PipelineConfig`
    - 新增顶层字段（全部 `#[serde(default)]`）按 D-01 顺序：
        * `pub replace_parameters: Option<NormalizeConfig>`
        * `pub template: Option<TemplateConfig>`
        * `pub filter: Option<FiltersFeature>`（注意：单数 filter，对齐 CONTEXT.md D-01）
        * `pub charts: Option<ChartsConfig>`
        * `pub output: Option<OutputConfig>`
    - 新增私有字段用于旧路径检测：`#[serde(rename = "pipeline", default)] _pipeline_deprecated: Option<toml::Value>`
    - 顶部 `use` 区段从 `pub use crate::pipeline::PipelineConfig` 改为 `use crate::pipeline::{ChartsConfig, FiltersFeature, NormalizeConfig, OutputConfig, TemplateConfig}`（pub use 不再需要，因为 PipelineConfig 已删除）

    第二步 — 实现旧路径检测（在 validate() 开头插入，位于 self.logging.validate() 之前）：
    - 当 `self._pipeline_deprecated.is_some()` 时返回：
        ```
        Err(Error::Config(ConfigError::InvalidValue {
            field: "[pipeline]".to_string(),
            value: String::new(),
            reason: "配置格式已升级，请迁移以下字段：\n  [pipeline.template_analysis] → [template]\n  [pipeline.charts] → [charts]\n  [pipeline.normalize] → [replace_parameters]\n  [pipeline.filters.*] → [filter.*]\n  [pipeline.fields] → [output.fields]\n详见 .planning/phases/18-template-chart-nesting/18-CONTEXT.md".to_string(),
        }))
        ```
    - 注意：reason 字符串中的 5 条迁移映射必须按此顺序出现，测试用例依赖此顺序

    第三步 — 替换 validate_pipeline_* 私有方法（lines 286-349）：
    - `validate_pipeline_filters` → `validate_filter`：从 `self.filter` 读取，调用 `CompiledMetaFilters::try_from_include_exclude` 与 `CompiledSqlFilters::try_from_sql_filters`，逻辑不变
    - `validate_pipeline_fields` → `validate_output_fields`：从 `self.output.as_ref().and_then(|o| o.fields.as_ref())` 读取；错误消息字段路径从 `pipeline.fields` 改为 `output.fields`
    - `validate_pipeline_charts` → `validate_charts`：跨字段依赖检查改为 `self.template.as_ref().is_some_and(|t| t.enable)`；错误消息字段路径从 `pipeline.charts` / `pipeline.charts.output_dir` / `pipeline.charts.top_n` 全部去除 `pipeline.` 前缀；reason 中提示文字 `[pipeline.charts]` → `[charts]`，`[pipeline.template_analysis] enabled = true` → `[template] enable = true`
    - validate() 调用顺序：旧路径检测 → logging → exporter → sqllog → validate_filter → validate_output_fields → validate_charts

    第四步 — 重写 validate_and_compile()（lines 61-92）：
    - 旧路径检测在最前
    - `self.pipeline.filters` 改为 `self.filter`
    - 跨字段编译逻辑保持不变（include / exclude / record_sql 编译路径相同）
    - 调用 validate_output_fields() / validate_charts() 替代原 pipeline 版本

    第五步 — 重写 apply_one() match 分支（lines 130-281）：
    - 删除分支 `"pipeline.filters.enable"` → 改为 `"filter.enable"`，写入 `self.filter.get_or_insert_with(Default::default).enable`
    - 删除分支 `"pipeline.normalize.enable"` → 改为 `"replace_parameters.enable"`，写入 `self.replace_parameters.get_or_insert_with(Default::default).enable`
    - 删除分支 `"pipeline.template_analysis.enabled"` → 改为 `"template.enable"`，写入 `self.template.get_or_insert_with(Default::default).enable`
    - 新增分支 `"template.output_csv_path"` / `"template.output_sqlite_table"`，写入对应字段（字符串赋值，无 trim 校验，空串语义有效）
    - 删除分支 `"pipeline.charts.*"`（output_dir / top_n / frequency_bar / latency_hist / trend_line / user_pie 共 6 个） → 改为 `"charts.*"`（路径前缀去掉 pipeline.），写入逻辑不变（含 output_dir 非空与 top_n>0 校验）
    - 新增分支 `"output.fields"`：value 用逗号切分为 `Vec<String>`，写入 `self.output.get_or_insert_with(Default::default).fields = Some(parsed)`
    - 移除分支 `"pipeline.fields"`（不再支持，作为 unknown key 处理）
    - 所有错误消息中的 `pipeline.charts.*` 字段路径同步去掉 `pipeline.` 前缀

    第六步 — 重写 tests 模块（lines 352-1106 全部更新，约 70+ 个测试用例）：
    - 顶部 `use` 改为：`use crate::pipeline::{ChartsConfig, FiltersFeature, NormalizeConfig, OutputConfig, TemplateConfig}`（删除 PipelineConfig / TemplateAnalysisConfig）
    - 所有 toml 字符串中：
        * `[pipeline.filters]` → `[filter]`
        * `[pipeline.filters.include]` → `[filter.include]`
        * `[pipeline.filters.exclude]` → `[filter.exclude]`
        * `[pipeline.filters.indicators]` → `[filter.indicators]`
        * `[pipeline.filters.sql]` → `[filter.sql]`
        * `[pipeline.filters.record_sql]` → `[filter.record_sql]`
        * `[pipeline.template_analysis]\nenabled = true` → `[template]\nenable = true`
        * `[pipeline.charts]` → `[charts]`
        * `[pipeline.normalize]` → `[replace_parameters]`
        * `[pipeline.fields]` → `[output.fields]`（注意：fields 是顶层 `[output]` 表内字段，不是单独表）
    - 所有 Rust 代码字段路径：
        * `cfg.pipeline.filters` → `cfg.filter`
        * `cfg.pipeline.normalize` → `cfg.replace_parameters`
        * `cfg.pipeline.template_analysis` → `cfg.template`
        * `cfg.pipeline.charts` → `cfg.charts`
        * `cfg.pipeline.fields` → `cfg.output.as_ref().and_then(|o| o.fields.as_ref())`（读取）；写入需 `cfg.output.get_or_insert_with(Default::default).fields = Some(...)`
        * `cfg.pipeline = PipelineConfig { ... }` 这种整体赋值改为分字段赋值（参见 lines 992-1086 的 charts/template 检验测试）
    - 错误消息断言（assert msg.contains）：
        * `"pipeline.charts"` → `"[charts]"` 或 `"charts"`（视上下文匹配新错误消息）
        * `"[pipeline.template_analysis]"` → `"[template]"`
        * `"pipeline.filters.include.users"` → `"filter.include.users"`（FiltersFeature 内部 compile_patterns 的 field 路径由 Plan 02 在 filters.rs 中同步更新；本任务测试可暂时改为 `"filter."` 或 `"include.users"` 通用断言以避免锁死）
        * `"pipeline.fields"` → `"output.fields"`
        * `"pipeline.charts.output_dir"` → `"charts.output_dir"`
        * `"pipeline.charts.top_n"` → `"charts.top_n"`
    - 新增测试（按 behavior 列举）：
        * `test_validate_legacy_pipeline_path_rejected` — 含 `[pipeline.template_analysis]` toml，validate() 返回 Err，err 消息包含 5 条迁移映射
        * `test_validate_new_top_level_format_passes` — 新格式 `[template]` + `[charts]` + `[filter]` 通过
        * `test_apply_one_template_enable` / `test_apply_one_template_output_csv_path` / `test_apply_one_template_output_sqlite_table`
        * `test_apply_one_replace_parameters_enable`
        * `test_apply_one_filter_enable`
        * `test_apply_one_charts_*`（output_dir / top_n / frequency_bar / latency_hist 已存在，路径改名）
        * `test_apply_one_output_fields`
        * `test_apply_one_rejects_legacy_pipeline_paths`（pipeline.filters.enable / pipeline.charts.output_dir 等返回 Err）

    第七步 — 同步外部引用：
    - 删除 `pub use crate::pipeline::PipelineConfig`（在 src/config/mod.rs 顶部）。后续 task 处理 lib.rs 等外部引用。
  </action>
  <verify>
    <automated>cargo test --lib config:: 2>&amp;1 | tail -50</automated>
  </verify>
  <acceptance_criteria>
    - 源断言：`grep -nE 'pub +(replace_parameters|template|filter|charts|output) *:' src/config/mod.rs` 命中 5 个顶层字段（在 Config struct 中）
    - 源断言：`grep -n '_pipeline_deprecated' src/config/mod.rs` 命中（字段定义 + validate() 检测两处）
    - 源断言：`grep -n 'pub pipeline:' src/config/mod.rs` 不命中
    - 源断言：`grep -n 'pub use crate::pipeline::PipelineConfig' src/config/mod.rs` 不命中
    - 行为断言（toml 测试）：`cargo test --lib config::tests::test_validate_legacy_pipeline_path_rejected` 退出码 0
    - 行为断言（toml 测试）：`cargo test --lib config::tests::test_validate_new_top_level_format_passes` 退出码 0
    - 行为断言：`cargo test --lib config::tests::test_validate_charts_requires_template_analysis` 退出码 0（重命名后仍通过，断言改为 template.enable）
    - 测试命令：`cargo test --lib -- config::` 退出码 0，无被忽略测试
    - 错误消息内容：`cargo test --lib config::tests::test_validate_legacy_pipeline_path_rejected -- --nocapture` 的 stderr/输出包含字符串 `[pipeline.template_analysis] → [template]`、`[pipeline.charts] → [charts]`、`[pipeline.normalize] → [replace_parameters]`、`[pipeline.filters.*] → [filter.*]`、`[pipeline.fields] → [output.fields]`
  </acceptance_criteria>
  <done>Config 顶层字段重组完成；旧 `[pipeline]` 段被 validate() 显式拒绝；validate_filter / validate_output_fields / validate_charts 三私有方法到位；apply_one 接受新路径键，拒绝旧路径键；config:: 单元测试全部通过；但 src/cli/*.rs 与 src/main.rs / tests/integration.rs 等下游因 cfg.pipeline.* 引用尚未迁移，cargo build 全局可能失败（预期，由 Plan 02 修复）。</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user → TOML 文件 | 用户提供的配置文件是外部输入；validate() 必须对结构合法性与字段语义负责 |
| 旧 user 配置 → 新 binary | 用户从 v1.3 升级到 v1.4 时，旧 `[pipeline.*]` 配置必须明确报错，不能静默忽略导致功能默默失效 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01 | Information Disclosure / Repudiation | Config::validate() 静默接受旧 `[pipeline]` 段 | mitigate | `_pipeline_deprecated: Option<toml::Value>` 字段捕获旧表，validate() 返回 InvalidValue with 5 条迁移映射，用户立刻看到错误而非误以为功能正常 |
| T-18-02 | Tampering | TOML 注入未知字段绕过验证 | accept | TOML 不支持执行语义；未知字段在 `#[serde(default)]` 下被忽略是 serde 标准行为，不构成攻击面 |
| T-18-03 | Denial of Service | TemplateConfig.output_csv_path 用户给出极端长度或非法路径 | accept | 由文件系统/底层 IO 处理；本 Phase 不引入路径校验（D-04 显式约定 空串=不生成；非空时由 exporter 创建文件，错误向用户回流） |
| T-18-04 | Elevation of Privilege | output_csv_path / output_sqlite_table 用户配置的路径越权访问 | accept | 由进程运行时权限决定；CLI 工具本身无 sudo / setuid，无升权场景 |
| T-18-SC | Tampering | 包管理（cargo） | n/a | 本 Plan 不新增 crate 依赖（toml::Value 已在 toml crate 中，已在 Cargo.toml） |

</threat_model>

<verification>
完成 Task 1 + Task 2 后，本 Plan 的局部验证：

1. `cargo test --lib pipeline::` 全通过
2. `cargo test --lib config::` 全通过
3. `grep -rn 'PipelineConfig\|TemplateAnalysisConfig' src/pipeline/mod.rs src/config/mod.rs` 全部不命中（这两个 struct 已被删除）
4. `grep -nE 'pub +(replace_parameters|template|filter|charts|output) *:' src/config/mod.rs` 命中 5 个

全局 `cargo build` / `cargo clippy` 在本 Plan 完成时**预期失败**（src/cli/*.rs、src/main.rs、tests/integration.rs 仍引用 `cfg.pipeline.*`），由 Plan 02 修复。这是受控的破坏性中间态，PLAN 02 必须在 Wave 2 内紧接完成。
</verification>

<success_criteria>
- src/pipeline/mod.rs：TemplateConfig（含 enable / output_csv_path / output_sqlite_table 三字段）+ OutputConfig（含 field_mask/ordered_field_indices 方法）落地；TemplateAnalysisConfig 与 PipelineConfig 删除；ChartsConfig 与 NormalizeConfig 内容不变
- src/config/mod.rs：Config 含 5 个顶层 Option<T> 字段（replace_parameters / template / filter / charts / output）+ 1 个 `_pipeline_deprecated` 私有字段；validate() 优先检测旧路径并返回带 5 条迁移映射的错误；validate_filter / validate_output_fields / validate_charts 三方法到位
- apply_one 接受 template.enable / template.output_csv_path / template.output_sqlite_table / replace_parameters.enable / filter.enable / charts.output_dir / charts.top_n / charts.frequency_bar / charts.latency_hist / charts.trend_line / charts.user_pie / output.fields 共 12 个新路径键
- apply_one 拒绝旧 pipeline.* 路径（返回 unknown config key 错误）
- cargo test --lib pipeline:: 与 cargo test --lib config:: 均全通过
- 旧路径检测的 reason 字符串包含 5 条迁移映射（按 D-01 顺序）
</success_criteria>

<output>
Create `.planning/phases/18-template-chart-nesting/18-01-SUMMARY.md` when done
</output>
