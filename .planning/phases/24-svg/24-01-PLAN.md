---
phase: 24-svg
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - site/book.toml
autonomous: true
requirements: [I18N-01, DESVG-01]

must_haves:
  truths:
    - 用户打开 README.md 看到的是全中文文档
    - README.md 中没有 SVG 截图或图表引用（仅保留文字描述）
    - mdBook 站点的语言声明为中文
  artifacts:
    - path: "README.md"
      provides: "项目主文档（中文，去 SVG）"
      min_lines: 150
    - path: "site/book.toml"
      provides: "mdBook 语言配置"
      contains: 'language = "zh"'
  key_links:
    - from: "site/book.toml"
      to: "language = \"zh\""
      via: "语言声明字段"
      pattern: 'language\s*=\s*"zh"'
---

<objective>
将 README.md 翻译为全中文版本，移除其中所有 SVG 图表引用（PNG 截图和 Gallery 链接），同时将 site/book.toml 的语言声明改为 "zh"。

Purpose: 实现第一个用户面文档的中文化及去 SVG 化，确保中文 DBA 能直接阅读项目主文档。
Output: 全中文的 README.md，mdBook 语言配置改为 "zh"。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@README.md
@site/book.toml

## 术语一致性约定

以下术语在此阶段所有文档中使用统一的中文翻译（本 Plan + Plan 02 + Plan 03 共用）：

| 英文术语 | 中文翻译 | 说明 |
|---------|---------|------|
| DaMeng / DM Database | 达梦数据库 | 专有名词，保持品牌名称 |
| pipeline | 处理管道 | 数据流中的处理阶段 |
| exporter | 导出器 | 数据导出模块 |
| filter / filtering | 过滤器 / 过滤 | 记录筛选逻辑 |
| template | 模板 | SQL 模板归一化 |
| chart | 图表 | SVG 图表类型 |
| record | 记录 | 单条 SQL 日志记录 |
| parsing / parser | 解析 / 解析器 | 日志解析过程 |
| CLI / command line | 命令行 | 命令行界面 |
| config / configuration | 配置 / 配置项 | TOML 配置文件 |
| feature | 功能特性 | 工具提供的功能 |
| streaming | 流式 | 流式处理模式 |
| fingerprint / normalization | 指纹 / 归一化 | SQL 模板指纹 |
| aggregation | 聚合 | 统计累积 |
| latency | 延迟 | 执行延迟 |
| throughput | 吞吐量 | 处理吞吐量 |
| binary | 二进制文件 | 编译产物 |
| dependency | 依赖 | 外部依赖 |

## 需要执行的具体翻译规则

1. 所有章节标题、正文、列表、表格翻译为中文
2. 代码块、命令、TOML 配置示例、环境变量名保持原样（不翻译）
3. 包名（dm-database-sqllog2db）、crates.io 徽章保持原样
4. 链接保持原样（内部相对路径和外部 URL 都不改）
5. 保持 Markdown 格式结构完整
</context>

<interfaces>
<!-- README.md 当前结构概览（已从上下文提取） -->
<!-- 约208行，包含如下主要章节（按顺序）：
 1. 项目标题 + 徽章行（lines 1-9）
 2. 英文简介段落（lines 10-14）
 3. ## Features
    - ### Parsing & Export
    - ### Filtering & Field Control
    - ### Template Analysis & Charts
    - ### Configuration & Performance
 4. ## Architecture（含 Mermaid 图 + 文字流程 + 关键模块）
 5. ## Installation
 6. ## QuickStart（内联快速命令）
 7. ## Configuration（内联 TOML 示例）
 8. ## Performance（基准测试表格）
 9. ## SVG Charts ← 需要移除的全部内容
 10. ## Error Handling
 11. ## Links
 12. ## License

site/book.toml 当前内容：language = "en"，需改为 "zh"
-->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 翻译 README.md 为中文并移除 SVG 图表引用</name>
  <files>README.md</files>
  <read_first>README.md</read_first>
  <action>
    将 README.md 全部翻译为中文。具体变更包含四个层面：

    **1. 内容翻译：**
    全文中文化，保留代码块、命令、URL、TOML 配置示例、徽章行、包名、crates.io 相关信息不翻译。
    遵循术语一致性约定中的翻译对照表。
    英文简介段落（第10-14行 "Parse Dameng database SQL logs..."）翻译为中文，保持技术语气。

    **2. 结构保留与调整：**
    保留所有 ## 和 ### 层级标题结构。
    Architecture 章节中的 Mermaid 代码块和下方的纯文本流程图都保留（它们不是 SVG 图表，是文本格式）。

    **3. SVG 移除（DESVG-01）：**
    - 删除整个 "## SVG Charts" 章节（包含 PNG 图片引用、标题文字、Gallery 链接）
    - 删除 "For additional chart samples from different datasets, see the Gallery" 这句话
    - 删除 `![Frequency Bar Chart](docs/images/frequency_bar.png)` 和 `![Latency Histogram](docs/images/latency_histogram.png)` 两行以及它们下方的说明文字
    - 不删除 src/charts/ 目录下的任何代码
    - 保留 Performance 章节的基准测试表格

    **4. 替换为纯文字描述（D-03）：**
    在原本 SVG Charts 的位置，添加一段纯文字描述：
    "本工具内置 SQL 模板分析引擎，可自动生成四类图表：频率柱状图（Top-N SQL 模板按执行次数排序）、延迟直方图（每类模板的执行时间分布）、趋势折线图（SQL 执行频率随时间变化）、用户饼图（按数据库用户的查询占比）。图表以 SVG 格式输出，通过 sqllog2db stats 命令配合 --chart 参数生成。"

    将 Links 章节中 "Contributing Guide _(Coming v1.6)_" 等暂译为中文 "（即将推出）"。
  </action>
  <verify>
    <automated>
      grep -c 'frequency_bar.png' README.md | xargs test 0 -eq || echo "FAIL: PNG reference remains"
      grep -c 'latency_histogram.png' README.md | xargs test 0 -eq || echo "FAIL: PNG reference remains"
      grep -c 'SVG Charts' README.md | xargs test 0 -eq || echo "FAIL: SVG Charts section remains"
      grep -c 'language = "en"' site/book.toml | xargs test 0 -eq || echo "FAIL: book.toml still has 'en'"
    </automated>
  </verify>
  <acceptance_criteria>
    - README.md 中不包含 `frequency_bar.png` 引用
    - README.md 中不包含 `latency_histogram.png` 引用
    - README.md 中不包含 "SVG Charts" 章节标题
    - README.md 中不包含 "For additional chart samples" 段落
    - README.md 中的 SVG Charts 位置被纯文字描述替代
    - README.md 全部正文内容为中文（代码块、命令、URL 除外）
    - 所有 Markdown 链接和目标路径保持原样不变
    - README.md 文件可正常渲染（无语法错误）
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: 修改 site/book.toml 语言声明</name>
  <files>site/book.toml</files>
  <read_first>site/book.toml</read_first>
  <action>
    将 site/book.toml 中 `language = "en"` 改为 `language = "zh"`（D-05）。

    仅修改这一行，保留所有其他配置不变：
    - `[book]` 块的 title、authors、description 保持英文（这些会在 Phase 26 多页站点重构时一并处理）
    - `[output.html]` 块的 theme、css、git-repository-url 保持不变
    - `[build]` 块的 create-missing 保持不变
  </action>
  <verify>
    <automated>
      grep -q 'language = "zh"' site/book.toml || echo "FAIL: book.toml language not updated"
    </automated>
  </verify>
  <acceptance_criteria>
    - site/book.toml 中 `language` 字段值为 `"zh"`
    - 其他配置项（title、authors、description、theme、css 等）保持不变
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This is a documentation-only plan with no runtime code changes. No new trust boundaries are introduced.

| Boundary | Description |
|----------|-------------|
| No code boundary | README.md 和 book.toml 均为文档文件，不涉及执行路径 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-01 | Tampering | README.md content | mitigate | 仅修改 README.md 和 book.toml，不修改 src/ 目录任何文件。翻译保留代码块和 URL 原样，不做内容注入 |
| T-24-SC | Tampering | no packages | accept | 本计划不涉及任何包管理操作 |
</threat_model>

<verification>

全自动验证步骤：

```bash
# 1. PNG 图片引用已移除
! grep -q 'frequency_bar.png' README.md
! grep -q 'latency_histogram.png' README.md

# 2. SVG Charts 章节已移除
! grep -q '## SVG Charts' README.md

# 3. 纯文字描述已添加
grep -q '频率柱状图' README.md

# 4. book.toml 语言已改
grep -q 'language = "zh"' site/book.toml

# 5. book.toml 其他配置不变
grep -q 'create-missing = false' site/book.toml

# 6. 链接保持原样
grep -q '\[GitHub Repository\](https://github.com/guangl/sqllog2db)' README.md
```

</verification>

<success_criteria>
- README.md 全部为中文，无英文段落残留
- README.md 中无 SVG 截图或图表引用（仅保留文字描述）
- site/book.toml 的 language 为 "zh"
- 代码块、命令、TOML 示例、URL、徽章行保持原样
</success_criteria>
