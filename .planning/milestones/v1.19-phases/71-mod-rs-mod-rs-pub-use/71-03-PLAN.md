---
phase: 71-mod-rs-mod-rs-pub-use
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pipeline/mod.rs
  - src/pipeline/field_mask.rs
  - src/pipeline/normalize_config.rs
  - src/pipeline/output_config.rs
  - src/pipeline/processor.rs
  - src/pipeline/tests.rs
autonomous: true
requirements: []
must_haves:
  truths:
    - "src/pipeline/mod.rs 仅含 mod 声明与 pub use 重导出，无 const/struct/impl/fn"
    - "FIELD_NAMES / FieldMask / NormalizeConfig / OutputConfig / Pipeline / LogProcessor / FiltersFeature 在原路径下仍可访问"
    - "compute_normalized 仍可通过 pub(crate) use 路径访问"
    - "cargo test 通过（含 12 个原 tests）"
    - "cargo clippy --all-targets -- -D warnings 通过"
  artifacts:
    - path: "src/pipeline/mod.rs"
      provides: "module declarations + pub use re-exports only"
    - path: "src/pipeline/field_mask.rs"
      provides: "FIELD_NAMES 常量 + FieldMask struct/impl"
      contains: "pub const FIELD_NAMES"
    - path: "src/pipeline/normalize_config.rs"
      provides: "NormalizeConfig struct + impl Default + impl + default_true 私有 helper"
      contains: "pub struct NormalizeConfig"
    - path: "src/pipeline/output_config.rs"
      provides: "OutputConfig struct + impl"
      contains: "pub struct OutputConfig"
    - path: "src/pipeline/processor.rs"
      provides: "LogProcessor trait + Pipeline struct + impl"
      contains: "pub trait LogProcessor"
    - path: "src/pipeline/tests.rs"
      provides: "原 12 个单元测试"
      contains: "#[test]"
  key_links:
    - from: "src/pipeline/mod.rs"
      to: "field_mask / normalize_config / output_config / processor"
      via: "mod 声明 + pub use"
      pattern: "pub use (field_mask|normalize_config|output_config|processor)"
    - from: "src/exporter/mod.rs"
      to: "crate::pipeline::FIELD_NAMES, FieldMask, OutputConfig"
      via: "原 use 路径不变"
      pattern: "crate::pipeline::(FIELD_NAMES|FieldMask|OutputConfig)"
---

<objective>
重构 src/pipeline/mod.rs（347 行）：将四类内容（FieldMask + FIELD_NAMES、NormalizeConfig、OutputConfig、LogProcessor + Pipeline）拆分到 4 个新文件，#[cfg(test)] mod tests 块拆分到 tests.rs，mod.rs 仅保留 mod 声明、pub mod、pub use 重导出。

Purpose: pipeline 是 hot path 模块，分文件后每个类型/概念独立可读。
Output: 6 个文件，cargo test/clippy 全绿，所有现有公开/crate 内引用路径不变。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md

<interfaces>
<!-- 必须保持的导出路径（其他模块大量使用） -->

公开导出（已被 cli/run、exporter、stats 等模块使用）：
- `crate::pipeline::FIELD_NAMES`（pub const &[&str]）
- `crate::pipeline::FieldMask`（pub struct, Copy）
- `crate::pipeline::FieldMask::ALL`（pub const Self）
- `crate::pipeline::FieldMask::from_names` / `includes_normalized_sql`（pub）
- `crate::pipeline::FieldMask::is_active`（pub(crate)）
- `crate::pipeline::NormalizeConfig`（pub struct）
- `crate::pipeline::NormalizeConfig::placeholder_override`（pub）
- `crate::pipeline::OutputConfig`（pub struct）
- `crate::pipeline::OutputConfig::field_mask` / `ordered_field_indices`（pub）
- `crate::pipeline::LogProcessor`（pub trait）
- `crate::pipeline::Pipeline`（pub struct）
- `crate::pipeline::FiltersFeature`（已 pub use 自 filters::FiltersFeature，保持不动）
- `crate::pipeline::normalizer::*` 子模块路径（保留 `pub mod normalizer;`）
- `crate::pipeline::compute_normalized`（pub(crate) use，保留）
- `crate::pipeline::normalizer::ParamBuffer`（外部通过 normalizer 子模块访问，保留）

私有 helper：
- `fn default_true() -> bool`（serde 默认值，仅 normalize_config 内部使用）

子模块（保持不动）：
- `pub mod filters;`（含 FiltersFeature）
- `pub mod normalizer;`（含 compute_normalized, ParamBuffer）

外部使用样例（grep 已验证）：
```rust
// src/exporter/mod.rs
crate::pipeline::FieldMask::ALL
crate::pipeline::OutputConfig::field_mask
crate::pipeline::FIELD_NAMES.len()
crate::pipeline::OutputConfig::ordered_field_indices
crate::pipeline::FiltersFeature::has_transaction_filters

// src/exporter/csv/mod.rs, sqlite/mod.rs
crate::pipeline::FieldMask
crate::pipeline::FIELD_NAMES
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 拆分 pipeline/mod.rs 到 5 个新文件</name>
  <files>src/pipeline/mod.rs, src/pipeline/field_mask.rs, src/pipeline/normalize_config.rs, src/pipeline/output_config.rs, src/pipeline/processor.rs, src/pipeline/tests.rs</files>
  <read_first>
    - src/pipeline/mod.rs（全文 347 行）
    - src/pipeline/normalizer.rs（确认 compute_normalized / ParamBuffer 的公开签名，不修改）
    - src/exporter/mod.rs（grep 'crate::pipeline::' 确认引用路径）
    - src/exporter/csv/mod.rs（grep 'crate::pipeline::' 确认引用路径）
    - src/exporter/sqlite/mod.rs（grep 'crate::pipeline::' 确认引用路径）
  </read_first>
  <action>
    Step 1: 创建 `src/pipeline/field_mask.rs`：
      - 移入 mod.rs 第 10-68 行的：
        - `pub const FIELD_NAMES: &[&str] = &[...]`（15 个字段名）
        - `pub struct FieldMask(pub u16)` + 所有方法（ALL 常量、from_names、is_active、includes_normalized_sql）
        - `impl Default for FieldMask`
      - 文件顶部不需要 use（这部分代码自包含）

    Step 2: 创建 `src/pipeline/normalize_config.rs`：
      - 移入 mod.rs 第 70-114 行的：
        - `pub struct NormalizeConfig { enable, placeholders }` + Deserialize derive
        - `impl Default for NormalizeConfig`
        - `impl NormalizeConfig { placeholder_override }`
        - 私有 helper `fn default_true() -> bool { true }`
      - 顶部 use：`use serde::Deserialize;`

    Step 3: 创建 `src/pipeline/output_config.rs`：
      - 移入 mod.rs 第 116-147 行的：
        - `pub struct OutputConfig { fields }` + Deserialize derive + Default derive
        - `impl OutputConfig { field_mask, ordered_field_indices }`
      - 顶部 use：`use serde::Deserialize; use super::field_mask::{FieldMask, FIELD_NAMES};`

    Step 4: 创建 `src/pipeline/processor.rs`：
      - 移入 mod.rs 第 149-187 行的：
        - `pub trait LogProcessor` (Send + Sync + Debug)
        - `#[derive(Debug, Default)] pub struct Pipeline { processors }`
        - `impl Pipeline { new, add, is_empty, run_with_meta }`
      - 顶部 use：`use dm_database_parser_sqllog::Sqllog;`

    Step 5: 创建 `src/pipeline/tests.rs`：
      - 移入 mod.rs 第 189-347 行 `#[cfg(test)] mod tests { ... }` 内的全部 12 个测试
      - 文件顶部不嵌套 mod tests，直接：`use super::*; use crate::pipeline::*;` 等按需补充
      - 保留：test_pipeline_empty / test_pipeline_add / test_placeholder_override_question / test_placeholder_override_colon / test_placeholder_override_auto / test_placeholder_override_both_is_auto / test_normalize_config_default / test_output_config_field_mask_default / test_output_config_field_mask_with_names / test_output_config_ordered_indices_preserves_user_order / test_output_config_ordered_indices_none_returns_all / test_output_config_ordered_indices_empty_equals_all / test_default_true_via_serde / test_process_with_meta_default_delegates_to_process

    Step 6: 改写 `src/pipeline/mod.rs` 为：
      ```rust
      //! Pipeline 模块：字段定义、过滤器、归一化、处理管线。

      pub mod filters;
      pub mod normalizer;

      mod field_mask;
      mod normalize_config;
      mod output_config;
      mod processor;

      #[cfg(test)]
      mod tests;

      pub use field_mask::{FIELD_NAMES, FieldMask};
      pub use filters::FiltersFeature;
      pub use normalize_config::NormalizeConfig;
      pub use output_config::OutputConfig;
      pub use processor::{LogProcessor, Pipeline};

      pub(crate) use normalizer::compute_normalized;
      ```
      仅注释 + mod 声明 + pub use 行。

    Step 7: 运行质量门禁：
      - `cargo build`
      - `cargo clippy --all-targets -- -D warnings`
      - `cargo test`
      - `cargo fmt`

    注意事项：
      - FieldMask 的 `is_active` 是 pub(crate)，迁移到 field_mask.rs 后仍是 pub(crate)（子模块可访问 crate-visibility）
      - FieldMask 的 `includes_normalized_sql` 是 pub
      - 不要修改 normalizer.rs 与 filters/ 目录任何文件
  </action>
  <verify>
    <automated>cargo clippy --all-targets -- -D warnings && cargo test</automated>
    <grep-check>grep -v '^\s*//' src/pipeline/mod.rs | grep -vE '^\s*$' | grep -E '\bfn \w+|\bstruct \w+|\bimpl |\btrait |\bconst ' && echo "FAIL: mod.rs 仍含实现" || echo "OK: mod.rs 仅声明"</grep-check>
  </verify>
  <acceptance_criteria>
    - cargo clippy --all-targets -- -D warnings 退出码 0
    - cargo test 退出码 0
    - src/pipeline/mod.rs 过滤注释/空行/属性后仅含 `mod`/`pub mod`/`pub use`/`pub(crate) use` 行；不含 `fn`/`struct`/`impl`/`trait`/`const`
    - crate::pipeline::FIELD_NAMES / FieldMask / NormalizeConfig / OutputConfig / LogProcessor / Pipeline / FiltersFeature / compute_normalized 全部可达
    - exporter/mod.rs、exporter/csv/mod.rs、exporter/sqlite/mod.rs、cli/run/mod.rs 中所有引用路径无需修改
    - 12+ 个原 tests 全部迁移到 tests.rs 且 cargo test 通过
  </acceptance_criteria>
  <done>
    mod.rs 简化到 ≤ 25 行（含注释），field_mask/normalize_config/output_config/processor/tests 五文件接管全部实现与测试，质量门禁全绿。
  </done>
</task>

</tasks>

<verification>
- cargo clippy --all-targets -- -D warnings 通过
- cargo test 通过
- src/pipeline/mod.rs grep -E '\bfn |\bstruct |\bimpl |\btrait |\bconst ' 无匹配
- grep -r 'crate::pipeline::FieldMask' src/ tests/ | wc -l 与重构前一致（无路径破坏）
</verification>

<success_criteria>
- mod.rs 仅含 mod 声明与 pub use（≤ 25 行）
- 四类内容按职责分到独立文件
- 全部公开路径保持向后兼容
- 全套 cargo 质量门禁通过
</success_criteria>

<output>
Create `.planning/phases/71-mod-rs-mod-rs-pub-use/71-03-SUMMARY.md` when done
</output>
