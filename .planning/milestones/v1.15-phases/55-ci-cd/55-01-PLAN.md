---
phase: 55-ci-cd
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/ci.yaml
  - .github/workflows/bench.yml
  - .github/workflows/lychee.yml
  - .github/workflows/pages.yml
autonomous: true
requirements:
  - CICD-01
tags:
  - ci-cd
  - github-actions
  - workflow

must_haves:
  truths:
    - "推送 PR 后 ci.yaml 的 test/lint/coverage 三个 job 在 ubuntu/windows/macos 上均使用 actions/checkout@v4 运行"
    - "bench.yml 使用 actions/checkout@v4 检出代码，使用 actions/upload-artifact@v4 上传 bench-results"
    - "lychee.yml 使用 actions/checkout@v4 检出代码"
    - "pages.yml 使用 actions/checkout@v4 检出代码"
    - "所有 4 个 workflow 文件中不再存在 actions/checkout@v6 或 actions/upload-artifact@v7 引用"
  artifacts:
    - path: ".github/workflows/ci.yaml"
      provides: "三平台 CI 测试，使用 checkout@v4"
      contains: "actions/checkout@v4"
    - path: ".github/workflows/bench.yml"
      provides: "Benchmark workflow，使用 checkout@v4 和 upload-artifact@v4"
      contains: "actions/upload-artifact@v4"
    - path: ".github/workflows/lychee.yml"
      provides: "Link check workflow，使用 checkout@v4"
      contains: "actions/checkout@v4"
    - path: ".github/workflows/pages.yml"
      provides: "GitHub Pages 部署，使用 checkout@v4"
      contains: "actions/checkout@v4"
  key_links:
    - from: ".github/workflows/ci.yaml"
      to: "actions/checkout@v4"
      via: "uses 指令"
      pattern: "actions/checkout@v4"
    - from: ".github/workflows/bench.yml"
      to: "actions/upload-artifact@v4"
      via: "uses 指令"
      pattern: "actions/upload-artifact@v4"
---

<objective>
将 4 个简单 workflow 文件（ci.yaml、bench.yml、lychee.yml、pages.yml）中的损坏 action 版本统一修复为锁定的 v4 版本，消除可能由版本不一致导致的 CI 失败。

Purpose: 实现 CONTEXT D-01/D-02 的版本统一决策，使 PR 推送时 CI 全绿（CICD-01）。
Output: 4 个 workflow 文件的 action 版本引用全部修正。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/55-ci-cd/55-CONTEXT.md
@.planning/phases/55-ci-cd/55-RESEARCH.md
@.planning/phases/55-ci-cd/55-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 修复 ci.yaml 三处 checkout 版本</name>
  <files>.github/workflows/ci.yaml</files>
  <read_first>
    - .github/workflows/ci.yaml （当前文件状态，确认 3 处 checkout 行号）
    - .planning/phases/55-ci-cd/55-CONTEXT.md （决策 D-01）
    - .planning/phases/55-ci-cd/55-PATTERNS.md （受影响行号汇总）
  </read_first>
  <action>
    将 .github/workflows/ci.yaml 第 21 行（test job）、第 46 行（lint job）、第 74 行（coverage job）三处 `uses: actions/checkout@v6` 全部替换为 `uses: actions/checkout@v4`，per D-01。不要修改任何其他行；不要修改 dtolnay/rust-toolchain@stable、Swatinem/rust-cache@v2、taiki-e/install-action@v2 这三个 action 的版本号（D-03 确认其版本正确）。保持文件其余内容（matrix 配置、step 顺序、shell 命令）完全不变。
  </action>
  <verify>
    <automated>grep -c 'actions/checkout@v4' .github/workflows/ci.yaml | grep -q '^3$' && ! grep -q 'actions/checkout@v6' .github/workflows/ci.yaml</automated>
  </verify>
  <acceptance_criteria>
    - 源断言：`grep -c 'actions/checkout@v4' .github/workflows/ci.yaml` 输出 `3`
    - 源断言：`grep 'actions/checkout@v6' .github/workflows/ci.yaml` 无任何匹配输出（退出码非 0）
    - 源断言：`grep -c 'dtolnay/rust-toolchain@stable' .github/workflows/ci.yaml` 输出 `3`（三个 job 各一处，未被改动）
    - 源断言：`grep -c 'Swatinem/rust-cache@v2' .github/workflows/ci.yaml` 输出 `3`
    - 源断言：`grep -c 'taiki-e/install-action@v2' .github/workflows/ci.yaml` 输出 `1`（coverage job 中安装 cargo-llvm-cov）
    - 行为断言：YAML 语法仍合法（无解析错误）
  </acceptance_criteria>
  <done>ci.yaml 中所有 checkout 引用为 @v4，其他 action 版本未被改动，YAML 语法合法。</done>
</task>

<task type="auto">
  <name>Task 2: 修复 bench.yml 的 checkout 和 upload-artifact 版本</name>
  <files>.github/workflows/bench.yml</files>
  <read_first>
    - .github/workflows/bench.yml （当前文件状态，确认 checkout 在第 20 行、upload-artifact 在第 37 行）
    - .planning/phases/55-ci-cd/55-CONTEXT.md （决策 D-01、D-02）
    - .planning/phases/55-ci-cd/55-PATTERNS.md （bench.yml 修复细节）
  </read_first>
  <action>
    在 .github/workflows/bench.yml 中执行两处版本修复，per D-01 与 D-02：第 20 行 `uses: actions/checkout@v6` → `uses: actions/checkout@v4`；第 37 行 `uses: actions/upload-artifact@v7` → `uses: actions/upload-artifact@v4`。不要修改 `continue-on-error: true`（属于 Phase 56 BENCH-01 处理范围之外的现状保留）、不要改动 artifact `name` 和 `path` 字段、不要改动 `retention-days: 60`、不要改动 `bash scripts/collect_bench_results.sh` 调用。dtolnay/rust-toolchain@stable 和 Swatinem/rust-cache@v2 保留不变（D-03）。
  </action>
  <verify>
    <automated>grep -q 'actions/checkout@v4' .github/workflows/bench.yml && grep -q 'actions/upload-artifact@v4' .github/workflows/bench.yml && ! grep -qE 'actions/checkout@v6|actions/upload-artifact@v7' .github/workflows/bench.yml</automated>
  </verify>
  <acceptance_criteria>
    - 源断言：`grep -c 'actions/checkout@v4' .github/workflows/bench.yml` 输出 `1`
    - 源断言：`grep -c 'actions/upload-artifact@v4' .github/workflows/bench.yml` 输出 `1`
    - 源断言：`grep -E 'actions/checkout@v6|actions/upload-artifact@v7' .github/workflows/bench.yml` 无匹配（退出码非 0）
    - 源断言：`grep -c 'continue-on-error: true' .github/workflows/bench.yml` 输出 `1`（保留原状）
    - 源断言：`grep -c 'retention-days: 60' .github/workflows/bench.yml` 输出 `1`（保留原状）
    - 行为断言：YAML 语法合法
  </acceptance_criteria>
  <done>bench.yml checkout 改为 @v4、upload-artifact 改为 @v4，其他配置保留不变。</done>
</task>

<task type="auto">
  <name>Task 3: 修复 lychee.yml 和 pages.yml 的 checkout 版本</name>
  <files>.github/workflows/lychee.yml, .github/workflows/pages.yml</files>
  <read_first>
    - .github/workflows/lychee.yml （当前文件状态，checkout 在第 27 行）
    - .github/workflows/pages.yml （当前文件状态，checkout 在第 22 行）
    - .planning/phases/55-ci-cd/55-CONTEXT.md （决策 D-01）
    - .planning/phases/55-ci-cd/55-PATTERNS.md （lychee.yml 和 pages.yml 修复细节）
  </read_first>
  <action>
    将 .github/workflows/lychee.yml 第 27 行 `uses: actions/checkout@v6` 替换为 `uses: actions/checkout@v4`，per D-01。将 .github/workflows/pages.yml 第 22 行 `uses: actions/checkout@v6` 替换为 `uses: actions/checkout@v4`，per D-01。不要修改 lychee.yml 中的 `actions/cache@v5`、`lycheeverse/lychee-action@v2` 版本（其版本正确）。不要修改 pages.yml 中的 `peaceiris/actions-mdbook@v2`、`peaceiris/actions-gh-pages@v4` 版本（其版本正确）。保留 mdbook-version、publish_dir、publish_branch、force_orphan 等字段原值。
  </action>
  <verify>
    <automated>grep -c 'actions/checkout@v4' .github/workflows/lychee.yml | grep -q '^1$' && grep -c 'actions/checkout@v4' .github/workflows/pages.yml | grep -q '^1$' && ! grep -q 'actions/checkout@v6' .github/workflows/lychee.yml && ! grep -q 'actions/checkout@v6' .github/workflows/pages.yml</automated>
  </verify>
  <acceptance_criteria>
    - 源断言：`grep -c 'actions/checkout@v4' .github/workflows/lychee.yml` 输出 `1`
    - 源断言：`grep -c 'actions/checkout@v4' .github/workflows/pages.yml` 输出 `1`
    - 源断言：`grep 'actions/checkout@v6' .github/workflows/lychee.yml` 无匹配（退出码非 0）
    - 源断言：`grep 'actions/checkout@v6' .github/workflows/pages.yml` 无匹配（退出码非 0）
    - 源断言：`grep -c 'actions/cache@v5' .github/workflows/lychee.yml` 输出 `1`（保留）
    - 源断言：`grep -c 'lycheeverse/lychee-action@v2' .github/workflows/lychee.yml` 输出 `1`（保留）
    - 源断言：`grep -c 'peaceiris/actions-mdbook@v2' .github/workflows/pages.yml` 输出 `1`（保留）
    - 源断言：`grep -c 'peaceiris/actions-gh-pages@v4' .github/workflows/pages.yml` 输出 `1`（保留）
    - 行为断言：两文件 YAML 语法合法
  </acceptance_criteria>
  <done>lychee.yml 和 pages.yml 的 checkout 均改为 @v4，其他 action 版本和参数保留不变。</done>
</task>

<task type="auto">
  <name>Task 4: 全仓库版本残留扫描与 cargo 质量门禁</name>
  <files>(无新文件修改，仅扫描验证)</files>
  <read_first>
    - .github/workflows/ci.yaml
    - .github/workflows/bench.yml
    - .github/workflows/lychee.yml
    - .github/workflows/pages.yml
  </read_first>
  <action>
    执行 4 个 workflow 文件的全文扫描，验证不存在任何 `@v6` 的 `actions/checkout` 引用、不存在 `@v7` 的 `actions/upload-artifact` 引用。然后运行 `cargo clippy --all-targets -- -D warnings` 和 `cargo fmt --all -- --check` 确认 Rust 代码侧无回归（本 plan 不改 Rust，仅作为门禁验证）。注：release.yaml 不在本 plan 扫描范围（由 Plan 02 处理）。
  </action>
  <verify>
    <automated>! grep -rE 'actions/checkout@v6' .github/workflows/ci.yaml .github/workflows/bench.yml .github/workflows/lychee.yml .github/workflows/pages.yml && ! grep -rE 'actions/upload-artifact@v7' .github/workflows/ci.yaml .github/workflows/bench.yml .github/workflows/lychee.yml .github/workflows/pages.yml && cargo clippy --all-targets -- -D warnings && cargo fmt --all -- --check</automated>
  </verify>
  <acceptance_criteria>
    - 行为断言：`grep -rE 'actions/checkout@v6' .github/workflows/ci.yaml .github/workflows/bench.yml .github/workflows/lychee.yml .github/workflows/pages.yml` 退出码非 0（无残留）
    - 行为断言：`grep -rE 'actions/upload-artifact@v7' .github/workflows/ci.yaml .github/workflows/bench.yml .github/workflows/lychee.yml .github/workflows/pages.yml` 退出码非 0（无残留）
    - 测试命令：`cargo clippy --all-targets -- -D warnings` 退出码 0（无警告）
    - 测试命令：`cargo fmt --all -- --check` 退出码 0（格式合规）
  </acceptance_criteria>
  <done>4 个 workflow 文件无任何 @v6 checkout 或 @v7 upload-artifact 残留，Rust 工具链质量门禁绿灯。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Git push → GitHub Actions Runner | 维护者推送代码触发 workflow 运行，runner 执行可信代码 |
| Runner → GitHub Marketplace Actions | runner 拉取 actions/checkout@v4 等第三方 action |
| Runner → GitHub Artifact Store | bench.yml 上传 benchmark JSON 产物 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-55-01 | Tampering | actions/checkout@v4 引用 | mitigate | 使用主版本 tag @v4（而非 @main），GitHub 官方维护，供应链风险低；CONTEXT D-03 确认锁定版本策略 |
| T-55-02 | Tampering | actions/upload-artifact@v4 引用 | mitigate | 使用主版本 tag @v4，GitHub 官方维护；上传产物范围限定为 `bench-results-*.json` 与 `retention-days: 60` |
| T-55-03 | Information Disclosure | bench.yml artifact 泄漏 | accept | benchmark JSON 仅含 microbench 性能数据，无 PII、无凭据；retention-days 60 与现状一致 |
| T-55-SC | Tampering | npm/pip/cargo installs | mitigate | 本 plan 仅修改 workflow YAML，不引入新的包管理器 install；RESEARCH 已审计 4 个 actions（均为 GitHub 官方/知名维护者） |
</threat_model>

<verification>
- 4 个 workflow 文件中 `actions/checkout` 引用全部为 `@v4`（共 6 处：ci.yaml × 3、bench.yml × 1、lychee.yml × 1、pages.yml × 1）
- bench.yml 中 `actions/upload-artifact` 引用为 `@v4`
- 不存在 `@v6` checkout 或 `@v7` upload-artifact 残留
- `cargo clippy --all-targets -- -D warnings` 通过
- `cargo fmt --all -- --check` 通过
- Plan 02 完成后，推送测试 PR 到 GitHub 观察 Actions tab，三平台 test/lint/coverage job 应全部绿灯（CICD-01 端到端验证）
</verification>

<success_criteria>
- 修复 4 个 workflow 文件中 6 处 action 版本引用
- 无任何意外引入的版本号变更（dtolnay/rust-toolchain@stable、Swatinem/rust-cache@v2、taiki-e/install-action@v2、actions/cache@v5、lycheeverse/lychee-action@v2、peaceiris/actions-mdbook@v2、peaceiris/actions-gh-pages@v4 全部保留原版本）
- Rust 工具链质量门禁通过
- 任务 1/2/3 各自的源断言全部通过
</success_criteria>

<output>
Create `.planning/phases/55-ci-cd/55-01-SUMMARY.md` when done.
</output>
