---
phase: 22-github-pages
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - site/src/index.md
autonomous: true
requirements:
  - PAGES-01
  - PAGES-03
  - PAGES-04
  - PAGES-05
  - SUPP-01

must_haves:
  truths:
    - "用户访问 guangl.github.io/sqllog2db/ 能看到 Hero 标题、安装命令、四组功能概览"
    - "用户能看到 Mermaid.js 架构/数据流图（日志文件 → Parser → Pipeline → ExporterManager → CSV/SQLite）"
    - "用户能看到性能基准表格（合成 CSV 5.2M/s + 真实文件 1.55M/s，标注测试环境）"
    - "用户能看到 4 张内嵌 SVG 图表 Gallery（频率柱状图、延迟直方图、趋势折线图、用户饼图），每张带可折叠说明"
    - "落地页内容与 README 互补非重复（Pages 侧重可视化展示，README 是文字参考源）"
  artifacts:
    - path: "site/src/index.md"
      provides: "完整单页落地页（Hero → Install → Features → Architecture → Performance → SVG Gallery → Links）"
      contains: "Mermaid flowchart + 性能表格 + SVG inline image + link index"
  key_links:
    - from: "site/src/index.md"
      to: "site/book.toml"
      via: "mdBook 构建"
      pattern: "book.toml 构建配置引用了 src/ 目录"
    - from: "site/src/index.md"
      to: "README.md"
      via: "内容互补策略"
      pattern: "Pages 侧重可视化展示，README 是文字参考"
---

<objective>
编写完整的落地页内容（site/src/index.md），包含 Hero 区域、安装命令、功能概览、Mermaid 架构图、性能基准表格、内嵌 SVG 图表 Gallery（4 张实际图表示例 + 可折叠交互说明）和链接索引。同时生成 4 张示例 SVG 图表。

Purpose: 提供用户可访问的 GitHub Pages 落地页，以可视化方式展示项目能力，与 README 最小骨架互补。落地页是项目在 Web 上的展示窗口。
Output:
  - site/src/index.md — 完整的单页 mdBook 落地页 Markdown
  - 4 张内嵌 SVG 图表示例（频率柱状图、延迟直方图、趋势折线图、用户饼图）
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
<interfaces>
<!-- Performance data from existing benchmarks -->
合成 CSV throughput: ~5.2M records/sec
真实文件 (1.1GB): ~1.55M records/sec
测试环境: Apple M-series NVMe SSD

<!-- CLI commands for chart generation (from src/charts/) -->
sqllog2db supports stats subcommand with SVG chart output for:
- frequency bar chart (SQL template frequency distribution)
- latency histogram (execution latency distribution)
- trend line chart (execution count over time)
- user pie chart (top users by execution count)

<!-- README content strategy reference -->
From Phase 21 CONTEXT.md D-01: README 精简为最小骨架
From 22-CONTEXT.md D-01: Pages 作为完整项目主页，README 是最简索引
From 22-CONTEXT.md D-02: 页面结构 Hero → Install → Features → Architecture → Performance → SVG Gallery → Links
</interfaces>

@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/22-github-pages/22-CONTEXT.md (D-01 至 D-06, D-09)
@README.md (在补充不重复的前提下参考内容布局)
@.github/workflows/ci.yaml (badge URL 参考)
</context>

<tasks>

<task type="auto">
  <name>Task 1: 生成 4 张示例 SVG 图表</name>
  <files>site/src/index.md（Task 2 写入，本任务只生成 SVG 内容供内嵌）</files>
  <read_first>
    @.planning/phases/22-github-pages/22-CONTEXT.md (D-06: SVG Gallery 内嵌 SVG + 可折叠说明)
    运行 `cargo run -- stats --help` 查看 stats 子命令的参数和 SVG 输出方式
    运行 `cargo run -- --help` 查看可用子命令
  </read_first>
  <action>
    生成 4 张 SVG 图表内容，用于内嵌到 index.md 的 SVG Gallery 部分。

    方法：
    1. 创建临时测试数据：一个简短的 .log 文件（约 50-100 行 SQL 记录），包含多种用户、时间分布和延迟特征
    2. 创建临时 config.toml：启用 template 和 charts 配置，输出到临时目录
    3. 使用 `cargo build --release` 构建二进制（如果尚未构建）
    4. 运行 `sqllog2db run -c temp_config.toml` 生成 CSV/SQLite 输出
    5. 运行 `sqllog2db stats <path/to/output>`（使用实际 CLI 参数）生成以下 4 张 SVG 图表：
       - frequency_bar.svg — SQL 模板频率柱状图（展示 TOP-N 模板分布）
       - latency_histogram.svg — 执行延迟直方图（展示延迟分布区间）
       - trend_line.svg — 执行量趋势折线图（按时间窗口统计）
       - user_pie.svg — 用户饼图（展示 TOP-N 用户执行占比）
    6. 提取每张 SVG 文件的原始 XML 内容（不含外层 HTML 包装），保存供 Task 2 使用

    备选方案（如果 stats 子命令 CLI 参数复杂或生成流程不适用于临时数据）：
    - 直接根据 src/charts/ 中 SVG 生成代码的输出格式，创建与真实图表内容和结构一致的示例 SVG
    - 确保示例数据在图表中产生有意义的视觉模式（柱状图有高矮差异、折线图有趋势变化、饼图有大小区分）
    - 每张 SVG 包含 axis labels、legend、title 等完整元素

    将 4 张 SVG 的原始 XML 内容存储为临时变量或临时文件，供 Task 2 内嵌到 index.md 中。
  </action>
  <acceptance_criteria>
    - 4 张 SVG 图表均存在（作为内嵌内容而非独立文件）
    - 每张 SVG 是有效的 XML（可用 xmllint --noout 验证或 grep 检查 <svg> 和 </svg> 标签）
    - 图表包含有意义的视觉模式（柱状图有差异、折线图有趋势等）
    - 图表包含完整的标签、标题和图例元素
    - 图表类型对应：频率柱状图、延迟直方图、趋势折线图、用户饼图
  </acceptance_criteria>
  <verify>
    <automated># Verify 4 SVG contents are available (stored for Task 2)
    # Check each SVG has valid svg tags
    for svg_name in frequency_bar latency_histogram trend_line user_pie; do
      # SVG content should be in a known temp location or variable
      if [ -f "/tmp/sqllog2db_svgs/${svg_name}.svg" ]; then
        grep -q '<svg' "/tmp/sqllog2db_svgs/${svg_name}.svg" || exit 1
      else
        exit 1
      fi
    done
    echo "All 4 SVGs verified"
    </automated>
  </verify>
  <done>4 张示例 SVG 图表已生成并准备好供内嵌到落地页</done>

</task>

<task type="auto">
  <name>Task 2: 编写 site/src/index.md 完整落地页</name>
  <files>site/src/index.md</files>
  <read_first>
    @.planning/phases/22-github-pages/22-CONTEXT.md (D-01 至 D-06, D-09 全部决策)
    @README.md (当前 README 内容，确保落地页互补不重复)
    Task 1 输出的 4 张 SVG 内容（内嵌到 Gallery 部分）
  </read_first>
  <action>
    编写 site/src/index.md，严格按 D-02 定义的 7 段结构。

    整体原则（D-01 / D-05 / PAGES-05）：
    - 落地页是"完整项目展示"，README 是最简索引
    - 落地页侧重"视觉化呈现"（架构图、性能表格、SVG Gallery），README 侧重"文字参考"
    - 避免复制 README 中大段文字描述，改为简洁要点+可视化元素
    - 页面使用英文（与 README 一致）

    各段具体要求：

    1. Hero 区域：
       - 页面顶部大标题 "sqllog2db" + 副标题 "High-performance SQL log parser for DaMeng Database"
       - 4-6 个 Badge 行（与 README badge 一致：CI status、crates.io version、license、release）
       - 一行简短描述（取自 Cargo.toml description）

    2. 安装命令（D-02 specific idea）：
       - 同时展示两种安装方式：`cargo install dm-database-sqllog2db` 和 `cargo build --release`
       - 使用 mdBook 代码块语法（```bash）

    3. 功能概览（D-02: 图标+描述）：
       - 使用 emoji 或 Unicode 符号作为图标前缀
       - 四组按领域分组，与 README 结构一致但更精简（各用 1-2 行要点）：
         - Parsing & Export: 解析达梦 SQL 日志，流式导出到 CSV/SQLite
         - Filtering & Field Control: 正则包含/排除过滤，自定义输出字段投影
         - Template Analysis & Charts: SQL 模板归一化，统计聚合，自动生成 SVG 图表
         - Configuration & Performance: TOML 配置，约 5.2M rec/s 吞吐量，恒定内存

    4. 架构图 — Mermaid.js（D-03 / PAGES-04）：
       - 使用 ```mermaid 围栏代码块
       - flowchart LR 方向
       - 节点序列：SQL Log Files (.log) → SqllogParser → dm-database-parser-sqllog (parse each line) → Pipeline (Filter / Template / Charts) → ExporterManager → CSV Exporter / SQLite Exporter
       - Pipeline 使用菱形节点 + 条件分支（empty → fast path, filters → processing）
       - 所有节点使用清晰标签，与 CLAUDE.md 架构保持一致

    5. 性能表格（PAGES-03 / D-02 specific idea）：
       - 表格包含列：Benchmark, Records/sec, Data Source, Hardware
       - 数据行：
         - CSV Synthetic: ~5,200,000 rec/s, Synthetic workload, M-series NVMe SSD
         - Real-world (1.1 GB): ~1,550,000 rec/s, Production .log file, M-series NVMe SSD
       - 脚注标注测试环境详情（Apple Silicon, macOS, 具体 SSD 型号摘要）
       - 使用 mdBook 表格语法，交替行背景色由 custom.css 处理

    6. SVG Gallery（SUPP-01 / D-06）：
       - 节标题 "SVG Chart Gallery" + 短说明（4 张图表类型和用途概述）
       - 每张图表使用 HTML ```<details><summary> 包装，实现"可折叠交互说明"（D-06 requirement）
       - 每张图表结构：
         - summary 标签：图表名称 + 图标（如 "📊 Frequency Bar Chart"）
         - description：简要说明图表含义（2-3 行）、生成方式（如 "Generated via `sqllog2db stats --chart frequency`"）、使用场景（如 "Identify the most executed SQL templates"）
         - SVG 内嵌：直接粘贴 Task 1 生成的原始 ```<svg> XML（不含外层文档包装）
       - 4 张图表按 Task 1 顺序排列：频率柱状图、延迟直方图、趋势折线图、用户饼图
       - details 标签使用 open 属性让第一张图表默认展开

    7. 链接索引（D-02）：
       - GitHub Repository - 指向 repo URL
       - crates.io - 指向 crate 页面
       - Documentation - 指向自身 Pages URL（避免循环引用，改为指向 GitHub README）
       - Changelog - 指向 CHANGELOG.md（GitHub 渲染）
       - License - 标注 Apache-2.0

    格式要求：
    - 使用中文注释（HTML 注释 ```<!-- -->）标记每个分段的开始和结束（便于后续维护）
    - 页面长度控制在 300-400 行（含 SVG XML），超过 400 行是正常的内嵌 SVG 导致
    - 如果 SVGs 占用大量行数，将每张 SVG 的 ```<svg> 至 ```</svg> 保持紧凑格式（移除多余换行）
    - SVG Gallery 部分使用 HTML 标签（details/summary/svg），其余部分使用标准 Markdown
    - 不包含 asciinema-player 嵌入（SUPP-04 在 Phase 23 实现）
    - 不包含 docs/quickstart.md 或 docs/config-reference.md 链接（Phase 23 创建）
  </action>
  <acceptance_criteria>
    - index.md 存在且包含全部 7 个分段（Hero / Install / Features / Architecture / Performance / SVG Gallery / Links）
    - Architecture 段包含有效的 ```mermaid 流程图代码块
    - Performance 段包含 2 行数据的表格（CSV Synthetic + Real-world）
    - SVG Gallery 段包含 4 个 ```<details> 块，每个含图表名称和一张内嵌 ```<svg>
    - Mermaid 语法和 SVG 标签语法正确（无截断或缺少闭合标签）
    - 内容与 README 互补不重复（不使用 README 的大段文字描述）
    - 页面不包含 asciinema-player 相关内容
  </acceptance_criteria>
  <verify>
    <automated>
      # Verify index.md exists and has all required sections
      test -f site/src/index.md

      # Verify Architecture section has Mermaid code block
      grep -q '```mermaid' site/src/index.md
      grep -q 'flowchart' site/src/index.md

      # Verify Performance section has table with benchmark data
      grep -q '5,200,000\|5.2M' site/src/index.md
      grep -q '1,550,000\|1.55M' site/src/index.md
      grep -q 'Records/sec\|rec/s' site/src/index.md

      # Verify SVG Gallery has 4 details blocks
      grep -c '<details' site/src/index.md | xargs test 4 -eq

      # Verify each details block has an SVG
      grep -c '<svg' site/src/index.md | xargs test 4 -eq

      # Verify Hero section exists
      grep -q 'sqllog2db' site/src/index.md
    </automated>
  </verify>
  <done>site/src/index.md 包含完整的 7 段落地页内容，可在 mdBook 中成功构建</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Phase 22 是纯文档阶段，无代码变更。无运行时信任边界。落地页内容仅包含静态 Markdown、Mermaid 图表和内嵌 SVG，不加载外部脚本。

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-03 | Spoofing | SVG/Chart Gallery 内容 | accept | 图表仅用于展示说明用途，无安全敏感操作；数据为示例虚构数据 |
| T-22-04 | Information Disclosure | Mermaid 图表中的架构信息 | accept | 架构信息与 README/CLAUDE.md 公开一致，无敏感细节 |
</threat_model>

<verification>
- `mdbook build site/` 命令执行成功（验证 mdBook 结构 + 内容语法均正确）
- 验证构建输出目录 site/book/ 存在且包含 index.html
- 验证构建输出 site/book/index.html 中包含 Mermaid 图表容器和 SVG 内容
</verification>

<success_criteria>
1. site/src/index.md 包含全部 7 段内容（Hero / Install / Features / Architecture / Performance / SVG Gallery / Links）
2. `mdbook build site/` 成功，构建输出到 site/book/
3. 落地页包含 Mermaid 架构图、2 行性能表格、4 张内嵌 SVG 图表 Gallery
4. 落地页内容与 README 互补：Pages 侧重视觉展示，README 是文字参考
</success_criteria>

<output>
Create `.planning/phases/22-github-pages/22-02-SUMMARY.md` when done
</output>
