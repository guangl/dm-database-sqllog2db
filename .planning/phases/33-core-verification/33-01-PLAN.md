---
phase: 33-core-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: [KEEP-06]
user_setup: []

must_haves:
  truths:
    - "`cargo check` passes with zero errors (debug build compilation check)"
    - "`cargo build --release` succeeds producing a working binary at target/release/sqllog2db"
    - "`cargo clippy --all-targets -- -D warnings` produces zero warnings"
    - "`cargo fmt --check` confirms all source files are correctly formatted"
  artifacts:
    - path: "target/debug/sqllog2db"
      provides: "Debug build binary from cargo check"
      exists: true
    - path: "target/release/sqllog2db"
      provides: "Release build binary"
      exists: true
  key_links:
    - from: "src/main.rs"
      to: "target/release/sqllog2db"
      via: "cargo build --release"
      pattern: "cargo build --release"
    - from: "cargo clippy"
      to: "all source files"
      via: "lint check"
      pattern: "cargo clippy --all-targets -- -D warnings"
---

<objective>
执行静态代码分析，验证精简后的代码库在 debug 和 release 模式下都能正常编译，且无任何 clippy 警告和格式问题。

Purpose: 确保 Phase 28-32 的移除操作未引入编译错误、lint 警告或格式破坏（KEEP-06）。
Output: 编译成功、clippy 零警告、fmt 通过。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/33-core-verification/33-CONTEXT.md

# 本计划引用的具体决策
# D-07: 构建验证含 debug check + release build 两者
# D-15: Plan 1 = 静态检查

# 项目质量标准（CLAUDE.md）
# - cargo clippy --all-targets -- -D warnings 必须零警告
# - cargo fmt 格式检查必须通过
</context>

<interfaces>
<!--
本计划不涉及代码修改，仅运行构建和 lint 命令。
需要了解的接口：无（纯 CLI 工具调用）。
-->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 执行 debug check 和 release build</name>
  <files></files>
  <read_first>
    <!-- 纯命令执行，无需读取源文件 -->
  </read_first>
  <action>
    执行两步构建验证：

    1. `cargo check` — 快速 debug 编译检查（验证所有代码无编译错误，无需生成完整二进制）
    2. `cargo build --release` — 优化 release 构建（LTO=fat, panic=abort, strip=symbols）

    两个命令都必须成功退出（exit code 0）。任何错误需要记录具体失败信息。
    不修改任何代码（Phase 33 是纯验证阶段）。

    D-07: 必须同时验证 debug check 和 release build。
  </action>
  <acceptance_criteria>
    - `cargo check` 退出码 0，无 error 输出
    - `cargo build --release` 退出码 0，成功生成 `target/release/sqllog2db`
    - `target/release/sqllog2db` 文件存在且可执行（`ls -la target/release/sqllog2db`）
    - release 构建在 `finishing` 行显示：`Finished \`release\` profile`
  </acceptance_criteria>
  <verify>
    <automated>cargo check && cargo build --release 2>&1 | grep -q "Finished.*release" && test -f target/release/sqllog2db</automated>
  </verify>
  <done>Release build 成功，二进制文件可用；debug check 无编译错误</done>
</task>

<task type="auto">
  <name>Task 2: 执行 clippy lint 检查和格式化验证</name>
  <files></files>
  <read_first>
    <!-- 纯命令执行，无需读取源文件 -->
  </read_first>
  <action>
    执行两步代码质量检查：

    1. `cargo clippy --all-targets -- -D warnings` — 以 deny 模式运行 clippy
       - `--all-targets` 包括 lib、bin、tests、benches 等所有目标
       - `-D warnings` 将所有 warnings 提升为 errors，确保零警告
    2. `cargo fmt --check` — 验证 Rust 代码格式一致
       - 无输出（干净）或仅报告未格式化文件

    两个命令都必须成功。若 clippy 产生警告，记录具体警告内容但不修改代码（Phase 33 不涉及代码修改）。
    若 fmt --check 报告未格式化文件，同样记录但不自动格式化。

    D-07: 构建验证含 debug check + release build 两者（Task 1 已完成此要求）。
  </action>
  <acceptance_criteria>
    - `cargo clippy --all-targets -- -D warnings` 退出码 0
    - `cargo fmt --check` 退出码 0（无格式问题）
  </acceptance_criteria>
  <verify>
    <automated>cargo clippy --all-targets -- -D warnings 2>&1 && cargo fmt --check 2>&1</automated>
  </verify>
  <done>Clippy 零警告，fmt 格式检查通过</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI tools → codebase | cargo check/build/clippy/fmt 读取并分析源代码 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-01 | Tampering | cargo build | accept | Release binary 未签名，但本项目为 CLI 工具，无供应链攻击面 |
| T-33-02 | Information Disclosure | cargo check | accept | 仅本地编译，无远程暴露风险 |
</threat_model>

<verification>
逐任务验证（见各 task 的 <verify> 字段）。全部 2 个 task 通过即 Plan 1 完成。
</verification>

<success_criteria>
- [ ] `cargo check` 成功（debug build 无编译错误）
- [ ] `cargo build --release` 成功生成 release 二进制
- [ ] `cargo clippy --all-targets -- -D warnings` 零警告
- [ ] `cargo fmt --check` 格式正确
- [ ] KEEP-06 静态部分已验证通过
</success_criteria>

<output>
创建 `.planning/phases/33-core-verification/33-01-SUMMARY.md` 记录验证结果
</output>
