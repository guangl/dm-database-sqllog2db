---
phase: 58-cli-run
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/cli/run/mod.rs
autonomous: true
requirements:
  - CLEAN-02

must_haves:
  truths:
    - "src/cli/run/mod.rs 中每个 fn 的函数体不超过 40 行（含 handle_run）"
    - "提取后的私有函数命名清晰反映单一职责，且不含 helper/util/misc 字样"
    - "Phase 57 新增 e2e 测试（run CSV/SQLite、init、stats from>to 等）在重构后全部通过"
    - "cargo clippy --all-targets -- -D warnings 通过，无新增警告"
    - "cargo fmt --check 通过，无格式问题"
  artifacts:
    - path: "src/cli/run/mod.rs"
      provides: "handle_run + 7 个私有辅助函数 (resolve_input_files, merge_trxid_prescan, make_progress_bar, run_csv_parallel, run_sqlite_parallel, run_sequential, print_run_summary)"
      contains: "fn resolve_input_files"
    - path: "src/cli/run/mod.rs"
      provides: "merge_trxid_prescan 私有函数 (返回 Option<Config>, 实现 D-03/D-04 模式)"
      contains: "fn merge_trxid_prescan"
    - path: "src/cli/run/mod.rs"
      provides: "run_sequential 私有函数 (≤40 行, 含 #[allow(clippy::too_many_arguments)])"
      contains: "fn run_sequential"
    - path: "src/cli/run/mod.rs"
      provides: "print_run_summary 私有函数 (摘要输出, 接收 elapsed 参数)"
      contains: "fn print_run_summary"
  key_links:
    - from: "handle_run"
      to: "resolve_input_files / merge_trxid_prescan / make_progress_bar / run_csv_parallel / run_sqlite_parallel / run_sequential / print_run_summary"
      via: "顶层编排调用提取函数"
      pattern: "handle_run\\s*\\("
    - from: "merge_trxid_prescan 调用方"
      to: "final_cfg 引用"
      via: "let merged = ...; let final_cfg: &Config = merged.as_ref().unwrap_or(cfg);"
      pattern: "merged.as_ref\\(\\).unwrap_or\\(cfg\\)"
    - from: "tests/integration.rs 端到端测试"
      to: "handle_run 行为"
      via: "Phase 57 e2e 测试作为重构安全网"
      pattern: "test_cli_run_csv_output|test_cli_run_sqlite_output|test_cli_init_"
---

<objective>
将 `src/cli/run/mod.rs` 中唯一的公共函数 `handle_run`（当前 234 行）拆分为 7 个私有辅助函数，使每个函数体不超过 40 行，行为完全不变。

Purpose: 满足 CLAUDE.md 的"函数不超过 40 行"硬约束（CLEAN-02），提升 cli/run 模块可读性；为后续维护提供清晰的语义边界。
Output: 重构后的 `src/cli/run/mod.rs`，所有函数 ≤40 行，Phase 57 的 e2e 测试套件全部绿灯。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/58-cli-run/58-CONTEXT.md
@.planning/phases/58-cli-run/58-RESEARCH.md
@.planning/phases/58-cli-run/58-PATTERNS.md

@src/cli/run/mod.rs
@tests/integration.rs

<interfaces>
<!-- 必须使用的现有类型签名（从代码直接读取确认） -->
<!-- 子模块函数签名（本 phase 不修改，但提取函数需调用） -->

From src/cli/run/processor.rs (process_log_file, 14 参数):
- fn process_log_file(
    file_path: &str,
    file_idx: usize,
    total_files: usize,
    exporter_manager: &mut ExporterManager,
    pipeline: &Pipeline,
    show_progress: bool,
    progress_bar: Option<&ProgressBar>,  // 注: 第 7 个参数实际是 Option<&ProgressBar>=None 在原代码
    interrupted: &Arc<AtomicBool>,
    do_normalize: bool,
    placeholder_override: Option<bool>,
    params_buffer: &mut ParamBuffer,
    ns_scratch: &mut Vec<u8>,
    is_last_file: bool,
    pb: Option<&ProgressBar>,
  ) -> Result<(usize, ErrorStats)>;

From src/cli/run/parallel.rs:
- fn process_csv_parallel(
    log_files: &[PathBuf],
    final_cfg: &Config,
    pipeline: &Pipeline,
    jobs: usize,
    show_progress: bool,
    interrupted: &Arc<AtomicBool>,
    do_normalize: bool,
    placeholder_override: Option<bool>,
    field_mask: FieldMask,
    ordered_indices: &[usize],
  ) -> Result<(Vec<(PathBuf, usize)>, usize, ErrorStats)>;

From src/cli/run/sqlite_parallel.rs:
- fn process_sqlite_parallel(...) -> Result<(Vec<(PathBuf, usize)>, usize, ErrorStats)>;
  // 签名与 process_csv_parallel 完全相同

From src/cli/run/prescan.rs:
- fn scan_for_trxids_by_transaction_filters(
    log_files: &[PathBuf],
    cfg: &Config,
    jobs: usize,
  ) -> Result<HashSet<String>>;  // 返回类型与 merge_found_trxids 入参兼容

From src/pipeline (existing exports, 已 use):
- crate::pipeline::FiltersFeature (trait, has_transaction_filters)
- crate::pipeline::FieldMask (含 ALL 常量 + includes_normalized_sql 方法)
- crate::pipeline::FIELD_NAMES
- crate::pipeline::OutputConfig (field_mask, ordered_field_indices)
- crate::pipeline::NormalizeConfig::placeholder_override -> Option<bool>
- crate::pipeline::normalizer::ParamBuffer

From src/error:
- enum Error { Interrupted, Export(ExportError), Parser(ParserError), ... }
- ErrorStats { total_errors, parse_errors, export_errors, fatal_error, ... }
- ErrorStats::default(), merge(&Self), has_errors(), has_fatal()

<!-- 目标新函数签名（D-01 + 研究报告新增并行提取） -->

Target signatures (NEW, all private to src/cli/run/mod.rs):

```
fn resolve_input_files(cfg: &Config) -> Result<(Vec<PathBuf>, bool)>;
//   返回 (log_files, is_stdin_pipe)

fn merge_trxid_prescan(
    cfg: &Config,
    log_files: &[PathBuf],
    jobs: usize,
    is_stdin_pipe: bool,
) -> Result<Option<Config>>;
//   None = 无需预扫描（无事务过滤器，或 stdin pipe 降级）
//   Some(merged_cfg) = 预扫描完成且合并了 trxid

fn make_progress_bar(show_progress: bool) -> Option<ProgressBar>;

#[allow(clippy::too_many_arguments)]
fn run_csv_parallel(
    log_files: &[PathBuf],
    final_cfg: &Config,
    pipeline: &Pipeline,
    jobs: usize,
    show_progress: bool,
    interrupted: &Arc<AtomicBool>,
    do_normalize: bool,
    placeholder_override: Option<bool>,
    field_mask: FieldMask,
    ordered_indices: &[usize],
    verbose: bool,
) -> Result<(Vec<(PathBuf, usize)>, usize, ErrorStats)>;
//   返回 (processed_files, skipped_files, stats)

#[allow(clippy::too_many_arguments)]
fn run_sqlite_parallel(... 签名与 run_csv_parallel 完全相同 ...)
  -> Result<(Vec<(PathBuf, usize)>, usize, ErrorStats)>;

#[allow(clippy::too_many_arguments)]
fn run_sequential(
    log_files: &[PathBuf],
    final_cfg: &Config,
    pipeline: &Pipeline,
    do_normalize: bool,
    placeholder_override: Option<bool>,
    field_mask: FieldMask,
    ordered_indices: &[usize],
    verbose: bool,
    quiet: bool,
    show_progress: bool,
    pb: Option<&ProgressBar>,
    interrupted: &Arc<AtomicBool>,
) -> Result<(Vec<(PathBuf, usize)>, ErrorStats)>;
//   注: D-05 选首选方案（finalize/log_stats 移至调用方 OR 内联简化 fatal 检查）
//       使函数体 ≤40 行；具体方案见 Task 2 action

fn print_run_summary(
    quiet: bool,
    verbose: bool,
    use_parallel: bool,
    elapsed: f64,
    processed_files: &[(PathBuf, usize)],
    total_records: usize,
    skipped_files: usize,
    run_stats: &ErrorStats,
);
//   elapsed 由调用方计算 (total_start.elapsed().as_secs_f64()) 后传入
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 提取 4 个独立私有辅助函数</name>
  <files>src/cli/run/mod.rs</files>
  <read_first>
    - src/cli/run/mod.rs（完整文件，理解 handle_run 第 26–260 行当前结构）
    - .planning/phases/58-cli-run/58-CONTEXT.md（D-01~D-07 锁定决策）
    - .planning/phases/58-cli-run/58-PATTERNS.md（每个函数的精确源代码块与目标签名）
    - .planning/phases/58-cli-run/58-RESEARCH.md（行数分析、Pitfall 1/4/5）
  </read_first>
  <action>
    在 `src/cli/run/mod.rs` 内提取以下 4 个私有函数（顺序无强依赖，可按出现顺序写在 handle_run 之后；不新建子模块）：

    1. **resolve_input_files(cfg: &Config) -> Result<(Vec<PathBuf>, bool)>**
       - 源代码块：当前 mod.rs 第 34、37–60 行（不含 `let mut run_stats = ErrorStats::default();` 那一行，该行保留在 handle_run 内）
       - 封装：`SqllogParser::new(cfg.sqllog.inputs.clone()).log_files()?` 调用 + Windows/非 Windows cfg 条件编译的 `is_stdin_pipe` 计算 + stdin pipe fallback / NoFilesFound 错误返回
       - 返回：`(log_files: Vec<PathBuf>, is_stdin_pipe: bool)`
       - 保留 cfg 条件编译块（`#[cfg(target_os = "windows")]`、`#[cfg(not(target_os = "windows"))]`、Windows 下 stdin terminal 警告 `warn!`）
       - 保留 `info!("No log files found, reading from stdin (pipe mode)")` 与 Windows `warn!("Stdin pipe mode is not supported on Windows. No log files found.")` 原文案
       - 错误类型保持 `crate::error::Error::Parser(crate::error::ParserError::NoFilesFound { inputs: cfg.sqllog.inputs.clone() })`

    2. **merge_trxid_prescan(cfg: &Config, log_files: &[PathBuf], jobs: usize, is_stdin_pipe: bool) -> Result<Option<Config>>**
       - 源代码块：当前 mod.rs 第 63–92 行（包括 `owned_cfg`/`final_cfg` 三元式逻辑）
       - 重写为返回 `Option<Config>` 模式（按 D-03/D-04）：
         - 若 `cfg.filter.as_ref().is_some_and(crate::pipeline::FiltersFeature::has_transaction_filters)` 为 false → `Ok(None)`
         - 若是且 `is_stdin_pipe` 为 true → 打印同样的 `warn!` + `eprintln!`（原 lines 70–79 文案一字不差保留）后 `Ok(None)`
         - 否则调用 `scan_for_trxids_by_transaction_filters(log_files, cfg, jobs)?` 得到 `extra_trxids`，`let mut tmp = cfg.clone(); if let Some(f) = &mut tmp.filter { f.merge_found_trxids(extra_trxids); }`，返回 `Ok(Some(tmp))`
       - 移除 `owned_cfg` 局部变量（D-04 模式不再需要）

    3. **make_progress_bar(show_progress: bool) -> Option<ProgressBar>**
       - 源代码块：当前 mod.rs 第 112–123 行
       - 完整封装 `ProgressBar::new_spinner()` + `set_style(ProgressStyle::with_template("{msg}").unwrap_or_else(|_| ProgressStyle::default_spinner()).tick_chars("⠋⠙⠹⠸⠼⠴⠦⠧⠇⠏ "))` + `enable_steady_tick(Duration::from_millis(80))`
       - `show_progress` 为 false 时返回 `None`

    4. **print_run_summary(quiet: bool, verbose: bool, use_parallel: bool, elapsed: f64, processed_files: &[(PathBuf, usize)], total_records: usize, skipped_files: usize, run_stats: &ErrorStats)**
       - 源代码块：当前 mod.rs 第 230–252 行（`if !quiet { ... }` 整块）
       - `elapsed` 由调用方计算后传入（不再在函数内部调用 `total_start.elapsed()`）
       - 保留 `mode_label` / `skip_label` / verbose `for ... eprintln!("Processed: {} — {} records", ...)` / 最终 `eprintln!("\n✓ SQL Log Export Task Completed...")` 与 errors 行的原文案
       - 提示：函数体可能因 if 嵌套接近 ~22 行，无需额外处理

    **本任务不修改 handle_run 本体**（仅在文件末尾新增 4 个函数定义；handle_run 仍调用原内联逻辑）。Task 2 会改造 handle_run 调用这些新函数并提取剩余 3 个 orchestrator。

    **代码风格**：每个新函数前加文档注释（`/// ...`）说明单一职责（D-06 命名原则要求）。所有现有 `use` 已包含必要类型（`PathBuf`、`ProgressBar`、`ProgressStyle`、`info!`、`warn!`、`IsTerminal`、`SqllogParser`），无需新增 import。`PathBuf` 在文件内通过 `std::path::PathBuf` 全路径或新增 `use std::path::PathBuf;` 引用（选简洁的 `use std::path::PathBuf;` 加在 line 6 之后或合并到 line 6/7 之间）。

    完成后运行 `cargo check` 确认编译通过（此时 handle_run 仍使用原内联逻辑，4 个新函数尚未被调用，可能触发 dead_code 警告——临时容忍，Task 2 会消除）。
  </action>
  <verify>
    <automated>cargo check 2>&1 | grep -v 'dead_code' | grep -i 'error\[' ; test $? -ne 0</automated>
    <!-- 期望：cargo check 编译通过（允许 dead_code 警告）；无任何 error[ 行 -->
  </verify>
  <acceptance_criteria>
    - `src/cli/run/mod.rs` 包含 4 个新增私有函数定义：`fn resolve_input_files(`、`fn merge_trxid_prescan(`、`fn make_progress_bar(`、`fn print_run_summary(`（可用 `grep -E '^fn (resolve_input_files|merge_trxid_prescan|make_progress_bar|print_run_summary)\(' src/cli/run/mod.rs` 验证，输出 4 行）
    - 4 个函数签名与 `<interfaces>` Target signatures 段完全一致（参数顺序、类型、返回类型）
    - `merge_trxid_prescan` 函数体不出现 `owned_cfg` 标识符（按 D-04 改造为 `Option<Config>` 返回）
    - `merge_trxid_prescan` 函数体保留原 `warn!("Transaction-level filters are configured but stdin pipe mode cannot pre-scan...")` 与 `eprintln!("[WARN] Transaction-level filters with stdin: pre-scan disabled...")` 文案完全不变
    - `cargo check` 编译通过（可有 dead_code 警告，但不可有 error）
    - 函数命名不含 `helper`、`util`、`misc` 字样（grep 验证：`grep -E 'fn .*(helper|util|misc)' src/cli/run/mod.rs` 输出为空）
  </acceptance_criteria>
  <done>
    src/cli/run/mod.rs 文件末尾（或 handle_run 之后）新增 4 个私有函数定义，编译通过，签名与 interfaces 段一致，文案与原逻辑一字不差。handle_run 本体此时未改造，可能有 dead_code 警告（Task 2 消除）。
  </done>
</task>

<task type="auto">
  <name>Task 2: 改造 handle_run 调用新函数 + 提取 3 个 orchestrator (run_csv_parallel, run_sqlite_parallel, run_sequential)</name>
  <files>src/cli/run/mod.rs</files>
  <read_first>
    - src/cli/run/mod.rs（Task 1 完成后的状态，已含 4 个辅助函数）
    - .planning/phases/58-cli-run/58-PATTERNS.md（handle_run 骨架范式 lines 436–469 + run_csv_parallel / run_sqlite_parallel / run_sequential 提取模板）
    - .planning/phases/58-cli-run/58-RESEARCH.md（D-05 行数控制方案、Pitfall 1/2/3、Open Question 1/2）
    - .planning/phases/58-cli-run/58-CONTEXT.md（D-01~D-07，特别是 D-02 handle_run ≤40 行的目标）
  </read_first>
  <action>
    在 Task 1 基础上继续修改 `src/cli/run/mod.rs`：

    **步骤 2A: 提取 3 个 orchestrator 私有函数**（写在 Task 1 新增函数之后）：

    1. **run_csv_parallel(...) -> Result<(Vec<(PathBuf, usize)>, usize, ErrorStats)>**
       - 源代码块：当前 mod.rs 第 132–156 行（if use_csv_parallel 的 then-arm 内部）
       - 包含 `if verbose { eprintln!("Processing {} files in parallel ({} jobs)", log_files.len(), jobs); }`、`info!("Parsing and exporting SQL logs (parallel, {jobs} jobs)...");`、`process_csv_parallel(...)` 调用
       - 返回三元组 `(csv_processed_files, parallel_skipped, csv_parallel_stats)`（无需在内部 merge stats / 累加 total_records / 设置 skipped_files —— 这些由调用方处理）
       - 加 `#[allow(clippy::too_many_arguments)]`

    2. **run_sqlite_parallel(...) -> Result<(Vec<(PathBuf, usize)>, usize, ErrorStats)>**
       - 源代码块：当前 mod.rs 第 158–182 行（else if use_sqlite_parallel 的 then-arm 内部）
       - 结构与 `run_csv_parallel` 对称，但 `info!` 文案为 `"Parsing and exporting SQL logs (SQLite parallel, {jobs} jobs)..."`，内部调用 `process_sqlite_parallel`
       - 加 `#[allow(clippy::too_many_arguments)]`

    3. **run_sequential(...) -> Result<(Vec<(PathBuf, usize)>, ErrorStats)>**
       - 源代码块：当前 mod.rs 第 184–228 行（else 分支内部）
       - 函数内部维护 `exporter_manager`、`params_buffer`、`ns_scratch`、`per_file_counts`、`run_stats: ErrorStats`（局部，最后返回）
       - 循环内的 `process_log_file` 调用 14 参数不变（D-07：子模块函数签名不动）
       - **D-05 行数控制方案（首选方案 1 = finalize/log_stats 移至调用方）**：
         - 在 `run_sequential` 内部不调用 `exporter_manager.finalize()` 和 `exporter_manager.log_stats()`
         - 函数返回前丢弃 `exporter_manager`（在函数体 scope 结束）—— 但这会导致 finalize 不被调用，行为不等价
         - **修正方案**：将 `exporter_manager` 作为函数返回值的一部分，或在 `run_sequential` 内部仍保留 `finalize() + log_stats()` 调用（保持行为等价），通过其他方式减行
         - **推荐最终方案（D-05 备选 = 内联简化 fatal）**：保留 `exporter_manager.finalize()?;` 和 `if !quiet { exporter_manager.log_stats(); }` 在函数内部（共 4 行），但将 fatal 错误处理（原 4 行 if-block）简化为表达式形式：
           ```
           if file_stats.has_fatal() {
               return Err(Error::Export(crate::error::ExportError::WriteFailed {
                   path: log_file.into(),
                   reason: file_stats.fatal_error.unwrap_or_default(),
               }));
           }
           ```
           保持现状不动（4 行），并将 `if verbose { eprintln!("Processing: ..."); }` 压成单行 `if verbose { eprintln!("Processing: {}", log_file.display()); }`（已是 3 行，可压成 1 行 `verbose.then(|| eprintln!("Processing: {}", log_file.display()));`）
         - **目标硬指标**：`run_sequential` 函数体（从 `{` 后第一行到 `}` 前最后一行）≤40 行。可通过 `awk '/^fn run_sequential/,/^}$/' src/cli/run/mod.rs | wc -l` 大致衡量（fn 签名包围行除外）。若仍超 40 行，进一步将 `let mut params_buffer = ...` 与 `let mut ns_scratch = ...` 合并/简化，或将 `for ... { let (processed, file_stats) = process_log_file(...)?; ... }` 内部的 14 参数调用利用结构体或链式收紧（不改 process_log_file 签名）。
       - 函数内部累加 `run_stats: ErrorStats`（局部），返回 `(per_file_counts, run_stats)`
       - 加 `#[allow(clippy::too_many_arguments)]`（12 个参数超 clippy 默认阈值 7）

    **步骤 2B: 改造 handle_run 本体调用新函数**

    将当前 handle_run 第 26–260 行重写为以下结构（行数 ≤40，参考 58-PATTERNS.md lines 438–469 骨架）：
    - 第一段：`let total_start = Instant::now();` + `let mut run_stats = ErrorStats::default();`
    - 第二段：`let (log_files, is_stdin_pipe) = resolve_input_files(cfg)?;`
    - 第三段：`let jobs = std::thread::available_parallelism().map_or(1, std::num::NonZero::get);`
    - 第四段：`let merged = merge_trxid_prescan(cfg, &log_files, jobs, is_stdin_pipe)?;` + `let final_cfg: &Config = merged.as_ref().unwrap_or(cfg);`（D-04 模式，注意 `merged` 在 `final_cfg` 整个使用范围内必须存活；不要在 if-block 内声明）
    - 第五段（字段配置，18 行内联保留 D-02）：`let pipeline = build_pipeline(final_cfg);` + `field_mask` + `ordered_indices` + `do_normalize` + `placeholder_override` —— 与原 lines 93–110 完全相同
    - 第六段：`let show_progress = !quiet && !verbose;` + `let pb = make_progress_bar(show_progress);`
    - 第七段：`let mut total_records = 0usize;` + `let mut skipped_files = 0usize;` + `let use_csv_parallel = ...` + `let use_sqlite_parallel = ...` + `let use_parallel = ...`（4 行与原一致）
    - 第八段（处理分发，3 个 arm 各 ~4 行）：
      ```
      let processed_files: Vec<(PathBuf, usize)> = if use_csv_parallel {
          let (files, skipped, stats) = run_csv_parallel(...)?;
          run_stats.merge(&stats); total_records = files.iter().map(|(_, c)| *c).sum(); skipped_files = skipped; files
      } else if use_sqlite_parallel {
          let (files, skipped, stats) = run_sqlite_parallel(...)?;
          run_stats.merge(&stats); total_records = files.iter().map(|(_, c)| *c).sum(); skipped_files = skipped; files
      } else {
          let (files, seq_stats) = run_sequential(...)?;
          run_stats.merge(&seq_stats); total_records = files.iter().map(|(_, c)| *c).sum(); files
      };
      ```
       —— 注意：CSV/SQLite arm 的 `total_records = files.iter().map(|(_, c)| *c).sum()` 与原代码语义一致（原也是 `.iter().map(|(_, c)| *c).sum()`）；顺序 arm 由 run_sequential 内部累加然后返回，但调用方仍按 `.iter().map(...).sum()` 重新计算以保持与并行路径一致（无副作用，run_sequential 返回的 `per_file_counts` 已含 processed 计数）
    - 第九段：`print_run_summary(quiet, verbose, use_parallel, total_start.elapsed().as_secs_f64(), &processed_files, total_records, skipped_files, &run_stats);`
    - 第十段（收尾）：`if let Some(pb) = &pb { pb.finish_and_clear(); }` + `if interrupted.load(Ordering::Relaxed) { return Err(Error::Interrupted); }` + `Ok(run_stats)`

    **额外注意**：
    - `merge_trxid_prescan` 调用产生的 `merged: Option<Config>` 必须 hold 在 handle_run 整个 scope，不能在 if-block 内丢弃（Pitfall 4）
    - 字段配置段（pipeline、field_mask、ordered_indices、do_normalize、placeholder_override）保持与原 lines 93–110 完全一致，不简化
    - handle_run 函数本体（不含 fn 签名所在多行）目标 ≤40 行
    - 删除文件中所有 dead_code（Task 1 引入的 4 个未被调用的辅助函数现在都在 handle_run 中被调用）

    **完成后跑 `cargo check` 确认无任何 warning 或 error。**
  </action>
  <verify>
    <automated>cargo check --all-targets 2>&1 | grep -E '^(warning|error)\[' ; test $? -ne 0</automated>
    <!-- 期望：无任何 warning[ 或 error[ 行（dead_code 警告已被消除） -->
  </verify>
  <acceptance_criteria>
    - `src/cli/run/mod.rs` 包含全部 7 个私有函数定义：`fn resolve_input_files`、`fn merge_trxid_prescan`、`fn make_progress_bar`、`fn run_csv_parallel`、`fn run_sqlite_parallel`、`fn run_sequential`、`fn print_run_summary`（`grep -cE '^fn (resolve_input_files|merge_trxid_prescan|make_progress_bar|run_csv_parallel|run_sqlite_parallel|run_sequential|print_run_summary)\(' src/cli/run/mod.rs` 输出 `7`）
    - `run_csv_parallel`、`run_sqlite_parallel`、`run_sequential` 三个函数前的代码行包含 `#[allow(clippy::too_many_arguments)]` 标注（`grep -B1 -E '^fn (run_csv_parallel|run_sqlite_parallel|run_sequential)\(' src/cli/run/mod.rs | grep -c 'clippy::too_many_arguments'` 输出 `3`）
    - `handle_run` 调用 7 个新函数（`grep -cE '(resolve_input_files|merge_trxid_prescan|make_progress_bar|run_csv_parallel|run_sqlite_parallel|run_sequential|print_run_summary)\(' src/cli/run/mod.rs` ≥ 14：每个函数至少 1 次定义 + 1 次调用，共 ≥14 次出现）
    - `handle_run` 体内不再出现 `owned_cfg` 标识符（`grep -c 'owned_cfg' src/cli/run/mod.rs` 输出 `0`，D-04 模式）
    - `handle_run` 体内出现 `merged.as_ref().unwrap_or(cfg)` 模式（`grep -c 'merged.as_ref().unwrap_or(cfg)' src/cli/run/mod.rs` 输出 `1`）
    - `cargo check --all-targets` 编译通过且无任何 warning（含 dead_code）
    - `cargo test` 全部通过（含 Phase 57 e2e 测试 test_cli_run_csv_output_header_and_row_count、test_cli_run_sqlite_output_row_count、test_cli_init_creates_config、test_cli_init_existing_file_errors、test_cli_stats_rejects_from_after_to 等）
  </acceptance_criteria>
  <done>
    handle_run 被改造为调用 7 个私有函数的薄编排层；3 个 orchestrator 提取完成。`cargo check --all-targets` 无任何 warning；`cargo test` 全部通过。
  </done>
</task>

<task type="auto">
  <name>Task 3: 函数行数静态验证 + 质量门禁（clippy/fmt/test）</name>
  <files>src/cli/run/mod.rs</files>
  <read_first>
    - src/cli/run/mod.rs（Task 2 完成后的状态）
    - .planning/REQUIREMENTS.md（CLEAN-02 完整需求描述）
    - .planning/ROADMAP.md（Phase 58 成功条件 4 条）
  </read_first>
  <action>
    对 `src/cli/run/mod.rs` 中所有 `fn` 定义进行行数静态验证，并跑完整的质量门禁。

    **步骤 3A：函数行数静态验证（CLAUDE.md "函数不超过 40 行" 约束）**

    使用如下命令测量每个函数体的行数（fn 关键字开头到对应 `}` 闭合）：

    ```
    awk '
      /^(pub )?fn [a-z_]+\(/ {fn_start=NR; fn_name=$0; brace=0; body_start=0}
      fn_start && /\{/ {if(!body_start) body_start=NR; brace++}
      fn_start && /\}/ {brace--; if(brace==0 && body_start){printf "%s: body lines %d (NR %d-%d)\n", fn_name, NR-body_start-1, body_start, NR; fn_start=0}}
    ' src/cli/run/mod.rs
    ```

    上面 awk 对嵌套花括号的处理可能不精确，使用以下更稳健的人工/工具组合：

    1. 用 `grep -n '^\(pub \)\?fn ' src/cli/run/mod.rs` 列出所有 fn 起始行
    2. 用 `grep -n '^}$' src/cli/run/mod.rs` 列出所有顶层 `}` 结束行
    3. 对每个 fn，body 行数 = (顶层 } 行号) − (fn 签名首行) − (签名占用的行数)，期望均 ≤40

    **预期函数清单（行数应满足 ≤40，签名行不计入函数体）**：
    - `handle_run` — 目标 ≤40 行（D-02）
    - `resolve_input_files` — ~27 行（应远低于 40）
    - `merge_trxid_prescan` — ~20 行（D-04 改造后比原 31 行少 owned_cfg 处理）
    - `make_progress_bar` — ~12 行
    - `run_csv_parallel` — ~18 行
    - `run_sqlite_parallel` — ~19 行
    - `run_sequential` — 目标 ≤40 行（D-05 控制）
    - `print_run_summary` — ~22 行

    **若任一函数 >40 行**：回到 Task 2 进一步内联化简（例如合并 `let mut params_buffer = ...` 与 `let mut ns_scratch = ...`、压缩 if-then 单行、移除多余注释）；优先压缩 `handle_run` 与 `run_sequential` 这两个最接近上限的函数。

    **步骤 3B：质量门禁**

    依次运行（任一失败需返回修复）：

    1. `cargo fmt`（先格式化，避免后续 fmt --check 失败）
    2. `cargo clippy --all-targets -- -D warnings`（无任何 warning，含 `too_many_arguments`、`needless_borrow` 等；`run_csv_parallel`/`run_sqlite_parallel`/`run_sequential` 已加 `#[allow(clippy::too_many_arguments)]`，但若 clippy 报其他类型 warning 需逐一修复）
    3. `cargo test`（全部测试通过，含 Phase 57 e2e 测试的 6 个新增测试 + 之前的 68 个测试，目标 ≥74 个测试 passed）
    4. `cargo fmt --check`（确认格式合规）
    5. `cargo build --release`（可选但建议，确认 release 构建无问题）

    **步骤 3C：D-06 命名清洁性验证**

    - `grep -E 'fn .*(helper|util|misc)' src/cli/run/mod.rs` 输出为空（确认无禁用后缀）

    所有验证通过后，本任务完成。无文件改动需求时跳过 fmt 步骤外的修改。
  </action>
  <verify>
    <automated>cargo fmt && cargo clippy --all-targets -- -D warnings && cargo test && cargo fmt --check</automated>
  </verify>
  <acceptance_criteria>
    - `cargo fmt --check` 退出码 0
    - `cargo clippy --all-targets -- -D warnings` 退出码 0，输出末尾包含 `Finished` 且无 `warning:` / `error:` 前缀的行
    - `cargo test` 退出码 0，所有测试 passed（Phase 57 增加的 e2e 测试 `test_cli_run_csv_output_header_and_row_count`、`test_cli_run_sqlite_output_row_count`、`test_cli_init_creates_config`、`test_cli_init_existing_file_errors`、`test_cli_stats_rejects_from_after_to` 至少有 5 个出现在 `cargo test` 输出中并标记 `ok`）
    - `src/cli/run/mod.rs` 每个 `fn` 的函数体不超过 40 行（人工/awk 验证，特别关注 `handle_run` 和 `run_sequential`）
    - `grep -E 'fn .*(helper|util|misc)' src/cli/run/mod.rs` 输出为空（D-06：命名不含 helper/util/misc 后缀）
    - `grep -c '^fn \|^pub fn ' src/cli/run/mod.rs` 输出 `8`（handle_run 1 个 + 7 个私有辅助函数）
  </acceptance_criteria>
  <done>
    全部质量门禁通过（clippy/fmt/test 均绿）；所有 fn 函数体 ≤40 行；CLEAN-02 需求达成。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

本 Phase 是纯代码结构重构，不引入新的信任边界、输入处理或数据流变化。所有提取函数均为同文件内的私有函数，可见性范围未扩展。

| Boundary | Description |
|----------|-------------|
| 无新增 | 仅函数拆分，无新数据流跨越边界 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58-01 | Tampering | 重构引入行为偏差（如 trxid 预扫描降级逻辑漏掉 stdin pipe 警告） | mitigate | Phase 57 e2e 测试套件（test_cli_run_csv_*、test_cli_run_sqlite_*、test_cli_init_*、test_cli_stats_*）作为安全网，重构后 cargo test 必须全绿；warn!/eprintln! 文案在 Task 1/2 中要求一字不差保留并由 acceptance_criteria grep 验证 |
| T-58-02 | Tampering | clippy 抑制（#[allow(clippy::too_many_arguments)]）被滥用到不该有的位置，掩盖未来的接口债 | accept | 仅在 D-07 锁定的 3 个 orchestrator (run_csv_parallel/run_sqlite_parallel/run_sequential) 添加；这是已知必要妥协（process_log_file 14 参数本就超阈值，子模块不动是 D-07 约束） |
| T-58-03 | Repudiation | 重构破坏 stdin pipe 模式降级警告（用户失去事务过滤器静默降级的可观察性） | mitigate | merge_trxid_prescan 内 warn! + eprintln! 文案在 Task 1 acceptance_criteria 中要求一字不差保留；grep 验证文案 |

本 Phase 不涉及 Spoofing/Information Disclosure/Denial of Service/Elevation of Privilege（无新输入解析、无加密、无认证、无权限边界变更）。
</threat_model>

<verification>
**Phase 级验证**：

1. **行为不变性（CLEAN-02 + 成功条件 3）**：
   - `cargo test` 全部通过，Phase 57 新增的 6 个 e2e 测试（run CSV、run SQLite、init 新建、init 已存在、stats from>to、stats 边界值）均 ok
   - 重构前测试总数与重构后保持一致（无测试因签名变更失败）

2. **函数行数限制（CLEAN-02 + 成功条件 1）**：
   - `src/cli/run/mod.rs` 中每个 fn 函数体 ≤40 行（人工 + awk 验证）
   - 重点关注 `handle_run`（D-02）与 `run_sequential`（D-05）

3. **命名清洁性（成功条件 2）**：
   - `grep -E 'fn .*(helper|util|misc)' src/cli/run/mod.rs` 输出为空（D-06）
   - 7 个新函数名称语义清晰：resolve_input_files / merge_trxid_prescan / make_progress_bar / run_csv_parallel / run_sqlite_parallel / run_sequential / print_run_summary

4. **质量门禁（成功条件 4）**：
   - `cargo clippy --all-targets -- -D warnings` 退出码 0，无 warning
   - `cargo fmt --check` 退出码 0
</verification>

<success_criteria>
1. `src/cli/run/mod.rs` 包含 1 个公共函数 `handle_run` 与 7 个私有辅助函数（resolve_input_files、merge_trxid_prescan、make_progress_bar、run_csv_parallel、run_sqlite_parallel、run_sequential、print_run_summary），每个函数体 ≤40 行
2. 提取后 `handle_run` 的实现是上述 7 个函数的薄编排层，使用 D-04 模式（`merge_trxid_prescan` 返回 `Option<Config>`，调用方 `merged.as_ref().unwrap_or(cfg)`）
3. `cargo test` 全部通过（含 Phase 57 引入的 e2e 安全网测试），无行为变化
4. `cargo clippy --all-targets -- -D warnings` + `cargo fmt --check` 通过，无新增警告
5. 重构后的私有函数命名无 `helper`/`util`/`misc` 后缀（D-06）
6. 所有提取来源的关键日志/警告文案（如 stdin pipe 降级 `warn!` + `eprintln!`、stdin pipe info 提示、`✓ SQL Log Export Task Completed` 摘要）一字不差保留
</success_criteria>

<output>
Create `.planning/phases/58-cli-run/58-01-SUMMARY.md` when done.
</output>
