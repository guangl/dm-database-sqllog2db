---
phase: 63-test-coverage
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/error.rs
  - src/cli/run/prescan.rs
autonomous: true
requirements:
  - TEST-02

must_haves:
  truths:
    - "Error::Config / Error::File / Error::Export / Error::Io / Error::Interrupted 各变体的 is_fatal/severity/suggestion 方法均被新增测试覆盖"
    - "ConfigError 全部 5 个变体（NotFound/ParseFailed/InvalidLogLevel/InvalidValue/NoExporters）的 suggestion() 被覆盖"
    - "FileError 全部 3 个变体（AlreadyExists/WriteFailed/CreateDirectoryFailed）的 is_fatal/severity 被覆盖"
    - "ExportError 全部 2 个变体（WriteFailed/DatabaseFailed）的 is_fatal/severity 被覆盖"
    - "ParserError 全部 4 个变体（PathNotFound/InvalidPath/ReadDirFailed/NoFilesFound）的 suggestion 被覆盖"
    - "prescan.rs 新增 #[cfg(test)] mod tests 块，覆盖 build_indicator_filters(min_row_count=0) 与 build_sql_exclude_filters 多元素分支"
  artifacts:
    - path: "src/error.rs"
      provides: "mod tests 末尾追加错误变体方法测试"
      contains: "fn test_config_not_found_is_fatal"
      min_new_tests: 12
    - path: "src/cli/run/prescan.rs"
      provides: "新建 #[cfg(test)] mod tests 块"
      contains: "mod tests"
      min_new_tests: 3
  key_links:
    - from: "src/error.rs (mod tests)"
      to: "src/error.rs:91-167 (Error::is_fatal/severity/suggestion)"
      via: "直接构造 Error::Config/File/Export/Io/Interrupted 变体并调用方法"
      pattern: "is_fatal|severity|suggestion"
    - from: "src/cli/run/prescan.rs (新增 mod tests)"
      to: "src/cli/run/prescan.rs:8-29 (build_indicator_filters)"
      via: "super::build_indicator_filters 直接调用 + min_row_count=0 边界"
      pattern: "min_row_count == 0"
    - from: "src/cli/run/prescan.rs (新增 mod tests)"
      to: "src/cli/run/prescan.rs:40-47 (build_sql_exclude_filters)"
      via: "super::build_sql_exclude_filters + SqlFilters { excludes: Some(vec![...]) }"
      pattern: "build_sql_exclude_filters"
---

<objective>
为错误类型方法（is_fatal/severity/suggestion）与 prescan 模块私有函数补充单元测试，覆盖 RESEARCH.md 识别的两个剩余低覆盖区域。

Purpose: `src/error.rs`（78.29% 行）缺少对 ConfigError 全 5 变体、FileError 全 3 变体、ExportError 全 2 变体、ParserError 部分变体、Error::Interrupted 的方法分支覆盖；`src/cli/run/prescan.rs`（70.79% 行）当前无 mod tests，build_indicator_filters 的 min_row_count=0 与 build_sql_exclude_filters 多元素路径未被测试。
Output: `src/error.rs` 现有 mod tests 末尾追加 ≥ 12 个测试函数；`src/cli/run/prescan.rs` 末尾新建 `#[cfg(test)] mod tests` 块，包含 ≥ 3 个测试函数。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/63-test-coverage/63-CONTEXT.md
@.planning/phases/63-test-coverage/63-RESEARCH.md
@.planning/phases/63-test-coverage/63-PATTERNS.md

<interfaces>
<!-- 关键签名（取自现有代码），执行器无需翻找源码 -->

From src/error.rs:
- `pub enum Error { Config(ConfigError), File(FileError), Parser(ParserError), Export(ExportError), Io(io::Error), Interrupted }`
- `pub enum ErrorSeverity { Warning, Error, Critical }`
- `impl Error { pub fn is_fatal(&self) -> bool; pub fn severity(&self) -> ErrorSeverity; pub fn suggestion(&self) -> &str }`

is_fatal 真值表（src/error.rs:91-103）：
- Config → true
- Io → true
- Interrupted → true
- File::AlreadyExists → true; File::CreateDirectoryFailed → true; File::WriteFailed → false
- Parser::ReadDirFailed → true; 其他 Parser 变体 → false
- Export::DatabaseFailed → true; Export::WriteFailed → false

severity 映射（src/error.rs:105-121）：
- Config → Critical; Io → Critical; Interrupted → Critical
- File::WriteFailed → Error; File::AlreadyExists/CreateDirectoryFailed → Critical
- Parser::* → Warning
- Export::WriteFailed → Error; Export::DatabaseFailed → Critical

suggestion 返回字符串（src/error.rs:123-167）每个变体均映射到一个固定字符串；测试只需断言子串：
- ConfigError::NotFound → "init"
- ConfigError::ParseFailed → "TOML"
- ConfigError::InvalidLogLevel → "log levels"
- ConfigError::InvalidValue → "field value"
- ConfigError::NoExporters → "exporter"
- FileError::AlreadyExists → "--force"
- FileError::WriteFailed → "disk space"
- FileError::CreateDirectoryFailed → "parent directory"
- ParserError::PathNotFound → "log file exists"
- ParserError::InvalidPath → "path format"
- ParserError::ReadDirFailed → "directory permissions"
- ParserError::NoFilesFound → "glob" （已被现有测试覆盖，不重复）
- ExportError::WriteFailed → "disk space"
- ExportError::DatabaseFailed → "SQLite"
- Error::Io → "filesystem"
- Error::Interrupted → "interrupted"

ConfigError 变体构造：
- `ConfigError::NotFound(PathBuf::from("/path"))`
- `ConfigError::ParseFailed { path: PathBuf::from("/p"), reason: "r".into() }`
- `ConfigError::InvalidLogLevel { level: "x".into(), valid_levels: vec!["info".into()] }`
- `ConfigError::InvalidValue { field: "f".into(), value: "v".into(), reason: "r".into() }`
- `ConfigError::NoExporters`

FileError 变体构造：
- `FileError::AlreadyExists { path: PathBuf::from("/p") }`
- `FileError::WriteFailed { path: PathBuf::from("/p"), reason: "r".into() }`
- `FileError::CreateDirectoryFailed { path: PathBuf::from("/p"), reason: "r".into() }`

ExportError 变体构造：
- `ExportError::WriteFailed { path: PathBuf::from("/p"), reason: "r".into() }`
- `ExportError::DatabaseFailed { reason: "r".into() }`

ParserError 变体构造：
- `ParserError::PathNotFound { path: PathBuf::from("/p") }`
- `ParserError::InvalidPath { path: PathBuf::from("/p"), reason: "r".into(), line_number: Some(42) }`
- `ParserError::ReadDirFailed { path: PathBuf::from("/p"), reason: "r".into() }`

io::Error 构造：`std::io::Error::other("test")`

From src/cli/run/prescan.rs:
- `fn build_indicator_filters(indicators: &IndicatorFilters) -> Vec<Filter>` — 模块私有
- `fn build_sql_include_filters(sf: &SqlFilters) -> Vec<Filter>` — 模块私有
- `fn build_sql_exclude_filters(sf: &SqlFilters) -> Vec<Filter>` — 模块私有
- min_row_count=0 走 `FilterBuilder::new().build()` 分支（无 rowcount 约束）；min_row_count>0 走 `rowcount_gt(min_r - 1)`

From src/pipeline/filters/types.rs:
- `pub struct IndicatorFilters { exec_ids: Option<HashSet<i64>>, min_runtime_ms: Option<u32>, min_row_count: Option<u32> }`（已有 Default）
- `pub struct SqlFilters { includes: Option<Vec<String>>, excludes: Option<Vec<String>> }`（已有 Default）

From src/error.rs 现有 mod tests（lines 237-285）已覆盖：
- `test_no_files_found_display_contains_inputs`
- `test_no_files_found_suggestion_non_empty`
- `test_no_files_found_not_fatal`

→ 新增测试避免与上述重名。
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: src/error.rs mod tests 末尾追加错误变体方法测试</name>
  <files>src/error.rs</files>
  <read_first>
    - src/error.rs（全文，确认 Error/ConfigError/FileError/ExportError/ParserError 变体定义、is_fatal/severity/suggestion 映射、现有 mod tests 命名风格；新增测试追加到 mod tests 块内末尾、不修改现有 3 个测试）
    - .planning/phases/63-test-coverage/63-PATTERNS.md §4
    - .planning/phases/63-test-coverage/63-RESEARCH.md §"区域 D" 错误变体测试方向
  </read_first>
  <behavior>
    Test 1: `test_config_not_found_is_fatal_critical_suggestion` — `Error::Config(ConfigError::NotFound(PathBuf::from("/x")))`，断言 `is_fatal() == true`、`severity() == Critical`、`suggestion().contains("init")`。
    Test 2: `test_config_parse_failed_suggestion_mentions_toml` — `ConfigError::ParseFailed { path, reason }`，断言 suggestion contains "TOML"。
    Test 3: `test_config_invalid_log_level_suggestion` — `ConfigError::InvalidLogLevel { level, valid_levels }`，断言 suggestion contains "log levels"。
    Test 4: `test_config_invalid_value_suggestion` — `ConfigError::InvalidValue { field, value, reason }`，断言 suggestion contains "field value"。
    Test 5: `test_config_no_exporters_suggestion` — `ConfigError::NoExporters`，断言 suggestion contains "exporter"。
    Test 6: `test_file_already_exists_is_fatal` — `Error::File(FileError::AlreadyExists { path })`，断言 `is_fatal() == true`、`severity() == Critical`、`suggestion().contains("--force")`。
    Test 7: `test_file_write_failed_not_fatal_error_severity` — `Error::File(FileError::WriteFailed { path, reason })`，断言 `is_fatal() == false`、`severity() == Error`、`suggestion().contains("disk space")`。
    Test 8: `test_file_create_directory_failed_is_fatal` — `FileError::CreateDirectoryFailed { path, reason }`，断言 `is_fatal() == true`、`severity() == Critical`、`suggestion().contains("parent directory")`。
    Test 9: `test_export_write_failed_not_fatal_error_severity` — `Error::Export(ExportError::WriteFailed { path, reason })`，断言 `is_fatal() == false`、`severity() == Error`、`suggestion().contains("disk space")`。
    Test 10: `test_export_database_failed_is_fatal_critical` — `Error::Export(ExportError::DatabaseFailed { reason })`，断言 `is_fatal() == true`、`severity() == Critical`、`suggestion().contains("SQLite")`。
    Test 11: `test_io_error_is_fatal_critical` — `Error::Io(std::io::Error::other("test"))`，断言 `is_fatal() == true`、`severity() == Critical`、`suggestion().contains("filesystem")`。
    Test 12: `test_interrupted_is_fatal_critical` — `Error::Interrupted`，断言 `is_fatal() == true`、`severity() == Critical`、`suggestion().contains("interrupted")`。
    Test 13: `test_parser_path_not_found_suggestion` — `Error::Parser(ParserError::PathNotFound { path })`，断言 `is_fatal() == false`、`severity() == Warning`、`suggestion().contains("log file exists")`。
    Test 14: `test_parser_invalid_path_suggestion` — `ParserError::InvalidPath { path, reason, line_number: Some(42) }`，断言 `is_fatal() == false`、`suggestion().contains("path format")`。
    Test 15: `test_parser_read_dir_failed_is_fatal` — `ParserError::ReadDirFailed { path, reason }`，断言 `is_fatal() == true`（Parser 中唯一 fatal 变体）、`suggestion().contains("directory permissions")`。
    Test 16（可选）: `test_error_severity_display_strings` — 断言 `format!("{}", ErrorSeverity::Warning) == "WARNING"`、`"{ErrorSeverity::Error}" == "ERROR"`、`"{ErrorSeverity::Critical}" == "CRITICAL"`（覆盖 error.rs:16-24 Display 实现的三条 match）。
  </behavior>
  <action>
    在 `src/error.rs` 文件末尾的 `mod tests` 块内（紧接现有 3 个测试之后）追加 ≥ 12 个测试函数。具体实现要求：
    1. 必要时在 mod tests 块顶部追加 `use std::path::PathBuf;`（若未导入）。
    2. 所有断言使用 `assert_eq!` 或 `assert!`，断言消息含变体名便于失败定位（参照现有 `test_no_files_found_*` 风格）。
    3. suggestion 子串断言使用 `.contains(...)`，子串选择见上方 `<interfaces>` 段的映射表（确保不与多个变体的 suggestion 字符串重合 — 子串需唯一对应该变体）。
    4. ConfigError 测试中 ParseFailed/InvalidLogLevel/InvalidValue 三个变体需要构造完整字段（PathBuf、String、Vec<String>），参照 `<interfaces>` 段已列出的构造方法。
    5. 不修改 `Error`/`ConfigError`/`FileError`/`ExportError`/`ParserError` 任何变体定义；不修改 `is_fatal`/`severity`/`suggestion` 任何 match 分支。
    6. 不修改现有 3 个测试（test_no_files_found_*）的任何内容。
    新增测试数量 ≥ 12（覆盖 ConfigError 5 个变体 + FileError 3 个变体 + ExportError 2 个变体 + Io + Interrupted + ParserError 3 个变体）。
  </action>
  <verify>
    <automated>cargo test --lib error::tests 2>&amp;1 | grep -E "test result.*ok.*passed" | grep -v "0 passed"</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --lib error::tests` 退出码 0；passed ≥ 15（现有 3 + 新增 ≥ 12）
    - `grep -c "fn test_config_\|fn test_file_\|fn test_export_\|fn test_parser_\|fn test_io_error\|fn test_interrupted" src/error.rs` ≥ 12
    - `cargo clippy --all-targets -- -D warnings` 通过
    - `cargo fmt --check` 通过
    - `git diff src/error.rs` 仅在 mod tests 块内（lines 285 之前的生产代码无变更）；用 `git diff src/error.rs | grep -E "^\+.*fn (is_fatal|severity|suggestion|Display)"` 应为空（即未新增/修改生产方法）
    - 至少 5 个 suggestion 测试断言 contains 子串与 `<interfaces>` 映射表一致
  </acceptance_criteria>
  <done>新增 ≥ 12 个测试全部通过；error.rs 生产代码无改动；三道质量门禁全绿。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: src/cli/run/prescan.rs 末尾新建 mod tests 块，覆盖私有函数边界分支</name>
  <files>src/cli/run/prescan.rs</files>
  <read_first>
    - src/cli/run/prescan.rs（全文，确认 build_indicator_filters/build_sql_include_filters/build_sql_exclude_filters 当前签名与 min_row_count==0 分支位置，新增 mod tests 块在文件末尾）
    - src/pipeline/filters/types.rs（确认 IndicatorFilters/SqlFilters 字段与 Default 实现）
    - .planning/phases/63-test-coverage/63-PATTERNS.md §5
    - .planning/phases/63-test-coverage/63-RESEARCH.md §"区域 F"
  </read_first>
  <behavior>
    Test 1: `test_build_indicator_filters_min_row_count_zero` — 构造 `IndicatorFilters { min_row_count: Some(0), ..Default::default() }`，调用 `build_indicator_filters(&i)`，断言返回 `Vec<Filter>` 长度 == 1（min_row_count=0 触发 `FilterBuilder::new().build()` 分支，prescan.rs:17）。
    Test 2: `test_build_indicator_filters_min_row_count_positive` — 构造 `IndicatorFilters { min_row_count: Some(5), ..Default::default() }`，断言返回长度 == 1（min_row_count>0 触发 `rowcount_gt(min_r - 1)` 分支，prescan.rs:19）。两个测试共同覆盖 prescan.rs:16-21 的 if/else 两条分支。
    Test 3: `test_build_indicator_filters_empty_returns_empty` — 构造 `IndicatorFilters::default()`（无任何字段），断言返回 `Vec<Filter>` 长度 == 0（覆盖 prescan.rs:8-29 中所有 Option 均为 None 的全 false 分支）。
    Test 4: `test_build_sql_exclude_filters_multiple_returns_correct_count` — 构造 `SqlFilters { excludes: Some(vec!["SELECT 1".into(), "DROP".into(), "DELETE FROM x".into()]), includes: None }`，断言 `build_sql_exclude_filters(&sf).len() == 3`（覆盖 prescan.rs:40-47 的非空 excludes 分支）。
    Test 5: `test_build_sql_exclude_filters_none_returns_empty` — 构造 `SqlFilters::default()`（excludes=None），断言 `build_sql_exclude_filters(&sf).len() == 0`（覆盖 prescan.rs:42 unwrap_or(&[]) 的 None 分支）。
    Test 6（可选追加）: `test_build_sql_include_filters_multiple` — 类似 Test 4 但针对 includes，断言长度匹配（覆盖 prescan.rs:31-38）。
    Test 7（可选追加）: `test_build_indicator_filters_exec_ids_each_id_one_filter` — 构造 `IndicatorFilters { exec_ids: Some(HashSet::from([1i64, 2, 3])), ..Default::default() }`，断言返回长度 == 3（覆盖 prescan.rs:23-27 for &id in ids 循环）。
  </behavior>
  <action>
    在 `src/cli/run/prescan.rs` 文件末尾新建 `#[cfg(test)] mod tests { ... }` 块。具体实现要求：
    1. mod tests 顶部：`use super::*;` 与 `use crate::pipeline::filters::types::{IndicatorFilters, SqlFilters};`（IndicatorFilters/SqlFilters 已在 prescan.rs:3 import，但 mod tests 子模块需要重新引入）。
    2. 实现上方 `<behavior>` 列出的 5–7 个测试函数；命名前缀统一 `test_build_`。
    3. 不修改 prescan.rs 的任何 use 语句、`scan_log_file_for_matches`、`scan_for_trxids_by_transaction_filters` 等生产函数；测试仅访问 `super::build_indicator_filters`/`super::build_sql_include_filters`/`super::build_sql_exclude_filters`（这三个函数都在 prescan.rs 顶部，模块私有但 mod tests 通过 super:: 访问）。
    4. 断言使用 `assert_eq!(filters.len(), N, "min_row_count=0 应构建一个全匹配 Filter")` 等含上下文消息的形式（参照 PATTERNS.md §5 范例）。
    5. 测试不依赖文件系统、不创建临时文件 —— 只测试纯函数路径，故无需 tempfile。
    6. 若 `cargo clippy` 提示 `dead_code` 等关于私有函数被测试访问的警告，使用合适的 `#[cfg(test)]` 注解修复测试代码，**不可** 通过修改生产函数可见性。
    新增测试数量 ≥ 3（最少覆盖 min_row_count=0 / min_row_count>0 / build_sql_exclude_filters 多元素 三条关键分支）。
  </action>
  <verify>
    <automated>cargo test --lib cli::run::prescan::tests 2>&amp;1 | grep -E "test result.*ok.*passed" | grep -v "0 passed"</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --lib cli::run::prescan::tests` 退出码 0；passed ≥ 3
    - `grep -c "fn test_build_" src/cli/run/prescan.rs` ≥ 3
    - `grep -c "mod tests" src/cli/run/prescan.rs` ≥ 1（新建 mod tests 块）
    - `cargo clippy --all-targets -- -D warnings` 通过
    - `cargo fmt --check` 通过
    - `git diff src/cli/run/prescan.rs` 显示新增内容仅为文件末尾 mod tests 块（lines 1-137 现有内容保持不变）；用 `git diff src/cli/run/prescan.rs | grep -E "^\+fn (build_|scan_)" | grep -v "fn test_"` 应为空（未新增/修改生产函数）
    - 至少 2 个测试覆盖 build_indicator_filters 的 if/else 双分支（min_row_count=0 与 min_row_count>0）
  </acceptance_criteria>
  <done>新增 mod tests 至少 3 个测试函数全部通过；prescan.rs 生产代码无改动；三道质量门禁全绿。</done>
</task>

</tasks>

<verification>
1. `cargo test --lib error::tests cli::run::prescan::tests` — 所有新增测试通过
2. `cargo clippy --all-targets -- -D warnings` 全绿
3. `cargo fmt --check` 全绿
4. `git diff src/error.rs src/cli/run/prescan.rs` 显示仅在 mod tests 块内新增内容；生产代码、use 语句、变体定义、impl 块均无变更
</verification>

<success_criteria>
- error.rs 现有 mod tests 块末尾追加 ≥ 12 个测试覆盖 ConfigError/FileError/ExportError/ParserError/Error::Io/Error::Interrupted 各变体的 is_fatal/severity/suggestion
- prescan.rs 末尾新建 mod tests 块包含 ≥ 3 个测试覆盖 build_indicator_filters(min_row_count=0/>0/empty) 与 build_sql_exclude_filters(多元素/空)
- 三道质量门禁全绿
- 所有生产代码（error.rs 与 prescan.rs 的非 mod tests 部分）无 git diff 变更
</success_criteria>

<output>
Create `.planning/phases/63-test-coverage/63-03-SUMMARY.md` when done. SUMMARY 必须记录：
- error.rs 与 prescan.rs 各自新增的测试函数命名清单
- 每个测试针对的具体 error.rs / prescan.rs 行号或变体
- 若有测试合并、拆分或跳过 optional 测试的情况，记录原因
</output>
