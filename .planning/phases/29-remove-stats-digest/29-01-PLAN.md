---
phase: 29-remove-stats-digest
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/cli/stats.rs (DELETE)
  - src/cli/mod.rs
  - src/cli/opts.rs
  - src/main.rs
  - src/lang.rs
autonomous: true
requirements: [RM-03]
user_setup: []
must_haves:
  truths:
    - "`sqllog2db --help` 不显示 `stats` 子命令"
    - "`src/cli/stats.rs` 文件已被删除"
    - "cargo build 编译通过，无 serde_json 相关编译错误（digest.rs 仍使用 serde_json，保留依赖）"
  artifacts:
    - path: "src/cli/stats.rs"
      provides: "stats 命令实现文件"
      min_lines: 0
      not_exists: true
    - path: "src/cli/mod.rs"
      provides: "stats 模块声明"
      not_contains: "pub mod stats"
    - path: "src/cli/opts.rs"
      provides: "Commands::Stats variant"
      not_contains: "Stats"
    - path: "src/main.rs"
      provides: "Stats match arm + needs_simple_logging"
      not_contains: "Commands::Stats"
    - path: "src/lang.rs"
      provides: "中文翻译中的 stats 相关函数"
      not_contains: "zh_stats"
  key_links:
    - from: "src/cli/opts.rs"
      to: "src/main.rs"
      via: "Commands::Stats 被移出 enum，match arm dead code → 移除"
      pattern: "dead"
    - from: "src/lang.rs"
      to: "src/cli/opts.rs"
      via: "zh_stats 在 `apply_zh()` 中被 `.mut_subcommand(\"stats\", zh_stats)` 引用"
      pattern: "zh_stats"
---

<objective>
移除 stats 子命令（RM-03）

Purpose: 删除低频功能 stats 统计命令，减少代码量。serde_json 依赖暂保留（digest.rs 仍使用，由 Plan 02 统一移除）。
Output: stats 子命令从 CLI 中消失，相关代码文件删除，引用清理。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/29-remove-stats-digest/29-RESEARCH.md

**需要删除的引用清单（全部来自 research）：**

1. `src/cli/stats.rs` — 整个文件删除（约 1043 行，含 ~230 行测试）
2. `src/cli/mod.rs` 第 7 行：`pub mod stats;`
3. `src/cli/opts.rs` 第 121-158 行：`Commands::Stats { .. }` variant（含 7 个 arg 字段）
4. `src/main.rs`：
   - 第 131 行：`matches!` 中的 `cli::opts::Commands::Stats { .. } |`
   - 第 248-283 行：`Commands::Stats` match arm（约 36 行）
5. `src/lang.rs`：
   - 第 94 行：`.mut_subcommand("stats", zh_stats)`
   - 第 149-160 行：`fn zh_stats(...)` 函数定义
</context>

<tasks>

<task type="auto">
  <name>Task 1: 删除 stats.rs 并清理所有引用</name>
  <files>
    src/cli/stats.rs, src/cli/mod.rs, src/cli/opts.rs, src/main.rs, src/lang.rs
  </files>
  <action>
    **操作步骤（按顺序执行，每一步完成后 cargo check 验证编译）：**

    1. **删除 src/cli/stats.rs** — 整个文件删除。

    2. **更新 src/cli/mod.rs** — 删除第 7 行 `pub mod stats;`。

    3. **更新 src/cli/opts.rs** — 删除 `Commands::Stats` variant（第 121-158 行，从 `Stats {` 到 `}` 整个 block，包含 `#[arg(...)]` 和大括号）。**注意：** 删除后检查 enum 的逗号对齐——前一项 `ShowConfig` 末尾有逗号，删除 `Stats` 后直接连接 `Digest` 即可。

    4. **更新 src/main.rs**：
       - `needs_simple_logging`（第 127-134 行）：从 `matches!` 中移除 `| cli::opts::Commands::Stats { .. }`。更新后应为：
         ```rust
         let needs_simple_logging = !matches!(
             &cli.command,
             Some(cli::opts::Commands::Run { .. } | cli::opts::Commands::Digest { .. })
         );
         ```
         （注意：Digest 暂保留，Plan 02 再移除）
       - 移除 `Commands::Stats` match arm（第 248-283 行）。从 `Some(cli::opts::Commands::Stats {` 到匹配的 `}` 结束。**注意：** 后续 `Commands::Digest` 的 match arm 紧接着 Stats，删除后确保 Digest arm 前没有多余的空行或语法错误。

    5. **更新 src/lang.rs**：
       - 删除第 94 行 `.mut_subcommand("stats", zh_stats)`。
       - 删除第 149-160 行 `fn zh_stats(...)` 函数定义（从 `fn zh_stats(s: Command) -> Command {` 到匹配的 `}` 结尾）。

    6. **验证编译：**
       ```bash
       cargo build 2>&1
       ```
       **预期结果：** 编译成功。可能的编译错误来源：
       - `serde_json` 相关：如果出现 `unused crate: serde_json` 警告，忽略（digest.rs 仍需使用，Plan 02 再移除）。如果出现 `cannot find crate serde_json` 错误，说明有其他文件使用，需恢复 Cargo.toml 中的 serde_json 声明。
       - 移除后 `pub mod stats;` 残留 → 确保已删除第 7 行。
       - `needs_simple_logging` 中残留 `Stats` 引用 → 仔细检查 `matches!` 宏。

    7. **运行回归测试：**
       ```bash
       cargo test --lib 2>&1
       ```
       **预期：** 所有测试通过（stats.rs 中的 ~230 行测试被连带删除，不影响其他测试）。

    8. **运行 clippy：**
       ```bash
       cargo clippy --all-targets -- -D warnings 2>&1
       ```
       **注意：** 如果 clippy 报告 `serde_json` 为未使用的依赖（因为 stats.rs 已删除，只剩 digest.rs 使用），可以接受告警（Plan 02 最终移除 serde_json 后自然会消除）。但其他 clippy 警告必须修复。
  </action>
  <verify>
    <automated>cargo build 2>&1 && cargo clippy --all-targets -- -D warnings 2>&1 && cargo test --lib 2>&1</automated>
  </verify>
  <done>
    - `src/cli/stats.rs` 文件不再存在
    - `src/cli/mod.rs` 中无 `pub mod stats` 声明
    - `src/cli/opts.rs` 中无 `Stats` variant
    - `src/main.rs` 中无 Stats match arm
    - `src/lang.rs` 中无 `zh_stats`
    - `cargo build` 编译成功
    - `cargo clippy --all-targets -- -D warnings` 通过（serde_json unused 警告可接受）
    - `cargo test --lib` 全部通过
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI→API | 用户通过子命令触发操作，本次仅删除不再需要的命令 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-01 | Tampering | 编译验证 | mitigate | 删除后执行 cargo build + cargo clippy + cargo test 三重验证，确保无遗漏引用 |
| T-29-02 | DoS | 模板管道 | mitigate | normalize_template 暂不受影响（Plan 02 迁移），processor.rs 的调用路径保持不变 |
</threat_model>

<verification>
1. `cargo build` 编译成功
2. `cargo clippy --all-targets -- -D warnings` 仅允许 serde_json 未使用依赖警告
3. `cargo test --lib` 全部测试通过
4. `grep -r "pub mod stats" src/cli/mod.rs` 无输出
5. `grep -r "Commands::Stats" src/main.rs` 无输出
6. `grep -r "zh_stats" src/lang.rs` 无输出
</verification>

<success_criteria>
- `sqllog2db --help` 不再列出 `stats` 子命令
- 所有 stat 相关源文件和引用已清理
- serde_json 依赖保留（供 digest.rs 使用）
- 模板管道和其他核心功能未受影响
</success_criteria>
