---
phase: 69-watch
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - src/cli/mod.rs
  - src/cli/opts.rs
  - src/cli/watch.rs
  - src/error.rs
  - src/cli/run/mod.rs
autonomous: false
requirements:
  - WATCH-01
user_setup: []
tags:
  - rust-cli
  - notify
  - watch
  - scaffold

must_haves:
  truths:
    - "Cargo.toml 中存在 notify = \"6\" 依赖条目"
    - "ErrorStats 结构体新增 records_exported: usize 字段，Default 自动为 0"
    - "ErrorStats::merge 累计 records_exported"
    - "handle_run 返回前将 total_records 写入 run_stats.records_exported"
    - "Commands::Watch { config: String } 变体存在于 opts.rs 中"
    - "src/cli/mod.rs 声明 pub mod watch;"
    - "src/cli/watch.rs 文件存在，声明 pub fn handle_watch(cfg, quiet, verbose, interrupted) -> Result<()> 函数签名 (此 plan 仅为骨架)"
  artifacts:
    - path: "Cargo.toml"
      provides: "notify dependency"
      contains: "notify = \"6\""
    - path: "src/error.rs"
      provides: "ErrorStats.records_exported field + merge accumulation"
      contains: "records_exported"
    - path: "src/cli/run/mod.rs"
      provides: "records_exported 赋值"
      contains: "run_stats.records_exported = total_records"
    - path: "src/cli/opts.rs"
      provides: "Commands::Watch variant with -c/--config arg"
      contains: "Watch {"
    - path: "src/cli/mod.rs"
      provides: "pub mod watch declaration"
      contains: "pub mod watch"
    - path: "src/cli/watch.rs"
      provides: "handle_watch 函数签名骨架（实现见 Plan 02）"
      contains: "pub fn handle_watch"
  key_links:
    - from: "src/error.rs ErrorStats"
      to: "src/cli/run/mod.rs handle_run"
      via: "records_exported 字段赋值"
      pattern: "run_stats\\.records_exported = total_records"
    - from: "src/cli/opts.rs Commands::Watch"
      to: "src/cli/mod.rs pub mod watch"
      via: "Watch variant 与 watch 模块声明同时存在以便 main.rs 后续 dispatch"
      pattern: "pub mod watch"
---

<objective>
建立 Phase 69 的基础设施：新增 notify = "6" 依赖、扩展 ErrorStats 累计字段、注册 Watch 子命令骨架及 handle_watch 函数签名。Plan 02 将在此骨架上实现 watch loop 逻辑。

Purpose: 为 Plan 02 的核心实现提供编译可通过的最小契约面（types/variants/signatures），消除 02/03 的"找契约"探索。
Output: Cargo.toml + ErrorStats 字段 + Commands::Watch variant + cli/mod.rs 声明 + src/cli/watch.rs 函数签名骨架。本 plan 完成后 `cargo build` 通过。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/69-watch/69-CONTEXT.md
@.planning/phases/69-watch/69-PATTERNS.md
@.planning/phases/69-watch/69-RESEARCH.md

<interfaces>
<!-- 当前 ErrorStats 结构体（src/error.rs:78）— Plan 任务需在此基础上新增 records_exported -->

```rust
// src/error.rs:77-86
#[derive(Debug, Default, Clone)]
pub struct ErrorStats {
    pub total_errors: usize,
    pub parse_errors: usize,
    pub export_errors: usize,
    pub fatal_error: Option<String>,
    pub by_type: HashMap<ErrorKind, u64>,
    pub filtered_out: u64,
    pub parse_error_records: Vec<ParseErrorRecord>,
}

// src/error.rs:118-140  merge() 当前实现
pub fn merge(&mut self, other: &ErrorStats) {
    self.total_errors += other.total_errors;
    self.parse_errors += other.parse_errors;
    self.export_errors += other.export_errors;
    if self.fatal_error.is_none() && other.fatal_error.is_some() {
        self.fatal_error.clone_from(&other.fatal_error);
    }
    for (kind, count) in &other.by_type {
        *self.by_type.entry(*kind).or_insert(0) += count;
    }
    self.filtered_out += other.filtered_out;
    const MAX_RECORDS: usize = 10_000;
    let remaining_cap = MAX_RECORDS.saturating_sub(self.parse_error_records.len());
    if remaining_cap > 0 {
        self.parse_error_records.extend(
            other.parse_error_records.iter().take(remaining_cap).cloned(),
        );
    }
}

// src/cli/run/mod.rs handle_run 关键尾部（lines 132-150）
let total_records: usize = processed_files.iter().map(|(_, c)| *c).sum();
if let Some(pb) = &pb {
    pb.finish_and_clear();
}
print_run_summary(quiet, verbose, use_parallel, total_start.elapsed().as_secs_f64(),
                  &processed_files, total_records, skipped_files, &run_stats);
write_error_log(final_cfg, &run_stats);
if interrupted.load(Ordering::Relaxed) {
    return Err(Error::Interrupted);
}
Ok(run_stats)

// src/cli/mod.rs 当前所有声明（5 行）
pub mod init;
pub mod opts;
pub mod run;
pub mod stats;
pub mod validate;

// src/cli/opts.rs 现有 Commands::Validate variant 风格参考（lines 104-122）
/// Validate a configuration file
#[command(
    long_about = "Validate a TOML configuration file for correctness. Checks that all required sections and fields are present and valid.",
    after_help = "\
EXAMPLES:
    Validate a configuration file:
        sqllog2db validate -c config.toml"
)]
Validate {
    /// TOML configuration file path to validate
    #[arg(
        short = 'c',
        long = "config",
        default_value = "config.toml",
        env = "SQLLOG2DB_CONFIG",
        help = "TOML configuration file path to validate."
    )]
    config: String,
},
```
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 0: 验证 notify crate 合法性（slopcheck unavailable，按 RESEARCH 表标记 ASSUMED）</name>
  <files>Cargo.toml</files>
  <what-built>
    本 plan 引入新外部依赖 notify = "6"。RESEARCH.md §"Package Legitimacy Audit" 标记为 [ASSUMED]（slopcheck 工具不可用）。在执行 `cargo add notify@6` 之前必须人工核实包真实性。
  </what-built>
  <action>
    暂停自动化执行，等待用户在浏览器中按 how-to-verify 步骤完成 notify crate 真实性核验。用户回复 "approved" 后由执行器继续 Task 1；回复 "abort" 则停止整个 plan。本 task 不修改任何文件，仅是 gate。
  </action>
  <how-to-verify>
    1. 打开 https://crates.io/crates/notify
    2. 确认 owner 为 notify-rs 组织（或个人 maintainer 列表与 GitHub repo notify-rs/notify 一致）
    3. 确认最新 6.x 版本（应为 6.1.1 或更高 patch）发布日期合理，下载量为百万级
    4. 浏览 source 仓库 https://github.com/notify-rs/notify 确认仓库存在且活跃
    5. 若以上任意一项不通过，回复 "abort" 并说明原因
  </how-to-verify>
  <verify>
    <automated>echo "human gate — see how-to-verify"; exit 0</automated>
  </verify>
  <acceptance_criteria>
    - 用户已访问 crates.io/notify 并目测确认 owner、版本、下载量
    - 用户回复 "approved" 才进入 Task 1
  </acceptance_criteria>
  <done>
    用户已完成 notify 真实性核验并 approved。
  </done>
  <resume-signal>回复 "approved" 后继续；回复 "abort" 中止 plan</resume-signal>
</task>

<task type="auto">
  <name>Task 1: 新增 notify = "6" 依赖并扩展 ErrorStats.records_exported</name>
  <files>Cargo.toml, src/error.rs, src/cli/run/mod.rs</files>
  <read_first>
    - Cargo.toml（lines 33-50，[dependencies] 段以确认插入位置）
    - src/error.rs（lines 75-141，ErrorStats 结构体定义与 merge 方法全文）
    - src/cli/run/mod.rs（lines 125-151，handle_run 函数尾部，total_records 计算到 Ok(run_stats) 之间）
    - .planning/phases/69-watch/69-PATTERNS.md（"src/error.rs (model)" 段落，新字段插入位置已锁定）
    - .planning/phases/69-watch/69-CONTEXT.md（D-09 决策）
  </read_first>
  <action>
    实施三处独立但语义相关的修改（per D-09）：

    (1) Cargo.toml — 在 [dependencies] 段中，于 `ctrlc = "3"` 之后、`indicatif = "0.18"` 之前的位置追加 `notify = "6"`。保持字母序与现有格式（无 features 表）。

    (2) src/error.rs — 在 `pub struct ErrorStats { ... }` 字段列表末尾（`parse_error_records: Vec<ParseErrorRecord>,` 之后）追加字段 `pub records_exported: usize,`，附行注释 `// 累计成功导出/处理的记录数（D-09，watch 模式累计统计用）`。结构体已 `#[derive(Default)]`，新字段自动初始化为 0。

    (3) src/error.rs `impl ErrorStats { pub fn merge(...) }` — 在 `self.filtered_out += other.filtered_out;` 之后、`const MAX_RECORDS: usize = 10_000;` 之前插入一行 `self.records_exported += other.records_exported;`。

    (4) src/cli/run/mod.rs handle_run — 在 line 132 `let total_records: usize = processed_files.iter().map(|(_, c)| *c).sum();` 与 line 133 `if let Some(pb) = &pb {` 之间插入一行 `run_stats.records_exported = total_records;`，使 handle_run 返回的 ErrorStats 持有真实记录数（供 watch 累计）。

    禁止修改其他字段或 merge 逻辑。禁止在本任务内 touch src/cli/opts.rs 或 src/cli/mod.rs。
  </action>
  <verify>
    <automated>cargo build --release 2>&1 | grep -E "error\[|warning:" ; cargo test --lib error:: 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^\s*#' Cargo.toml | grep -c 'notify = "6"'` 输出 1
    - `grep -n 'pub records_exported: usize' src/error.rs` 命中行号位于 ErrorStats 结构体内部（行号 < `impl ErrorStats` 起始行）
    - `grep -c 'self.records_exported += other.records_exported' src/error.rs` 输出 1
    - `grep -n 'run_stats.records_exported = total_records' src/cli/run/mod.rs` 命中行号位于 132 与 150 之间（handle_run 内）
    - `cargo build --release` 退出码 0，输出中无 `error[` 与 `warning:` 行
    - `cargo test --lib error::` 全部通过（已有 ErrorStats 单元测试不破坏）
    - `cargo clippy --all-targets -- -D warnings` 退出码 0
  </acceptance_criteria>
  <done>
    Cargo.lock 包含 notify 6.x；ErrorStats 拥有 records_exported 字段并通过 merge 累计；handle_run 返回的 ErrorStats.records_exported 等于本次处理的总记录数；现有 lib 测试无回归。
  </done>
</task>

<task type="auto">
  <name>Task 2: 注册 Commands::Watch variant 与 src/cli/watch.rs 函数签名骨架</name>
  <files>src/cli/opts.rs, src/cli/mod.rs, src/cli/watch.rs</files>
  <read_first>
    - src/cli/opts.rs（lines 100-145，Commands::Validate 与 Commands::Stats 完整结构作为模式参考）
    - src/cli/mod.rs（全文 5 行，确定 pub mod 顺序）
    - .planning/phases/69-watch/69-CONTEXT.md（D-13/D-14/D-15 决策）
    - .planning/phases/69-watch/69-PATTERNS.md（"src/cli/opts.rs" 段 + "src/cli/mod.rs" 段）
    - .planning/phases/69-watch/69-UI-SPEC.md（"Copywriting Contract" — long_about/after_help 文案约束）
  </read_first>
  <action>
    实施三处修改（per D-13/D-14/D-15）：

    (1) src/cli/opts.rs — 在 `Commands::Stats { ... }` variant 之后追加新的 `Commands::Watch` variant。结构必须严格复用 `Commands::Validate` 风格：
      - doc comment `/// Watch directory for new .log files and process them automatically`
      - `#[command(long_about = "...", after_help = "...")]` 属性
      - long_about 文本：`"Watch configured input directories for new .log files. Automatically triggers processing when new files appear. Press Ctrl+C to stop."`
      - after_help 文本：以 `EXAMPLES:` 起首，单个示例 `sqllog2db watch -c config.toml`，格式与 Validate variant 完全对称
      - 唯一字段：`config: String`，使用 `#[arg(short = 'c', long = "config", default_value = "config.toml", env = "SQLLOG2DB_CONFIG", help = "TOML configuration file path.")]`
      - 不接受 `--input` override（D-14 明确）；不接受 `--top/--from/--to`

    (2) src/cli/mod.rs — 在末尾追加一行 `pub mod watch;`。保持文件其他 4 行 (`init/opts/run/stats/validate`) 顺序不变。

    (3) 新建文件 src/cli/watch.rs 仅包含函数签名骨架（实际 watch loop 实现在 Plan 02）：
      - 必要 imports：`use crate::config::Config;`、`use crate::error::Result;`、`use std::sync::Arc;`、`use std::sync::atomic::AtomicBool;`
      - `pub fn handle_watch(cfg: &Config, quiet: bool, verbose: bool, interrupted: &Arc<AtomicBool>) -> Result<()>`
      - 函数体仅一行：`let _ = (cfg, quiet, verbose, interrupted); Ok(())`（占位，避免 unused 警告）
      - 文件顶部加 `//! Watch 子命令的实现入口。Phase 69 框架，Plan 02 填充 watch loop 逻辑。`

    禁止本任务内实现 watch loop、notify 调用或 ProgressBar 构造。禁止修改 src/main.rs（Plan 02 负责 dispatch）。
  </action>
  <verify>
    <automated>cargo build --release 2>&1 | grep -E "error\[|warning:" ; cargo run --release -- watch --help 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'Watch {' src/cli/opts.rs` 命中且位于 `Stats {` 之后
    - `grep -c 'pub mod watch;' src/cli/mod.rs` 输出 1
    - `test -f src/cli/watch.rs` 退出码 0
    - `grep -c 'pub fn handle_watch' src/cli/watch.rs` 输出 1
    - `tr -d '\n' < src/cli/watch.rs | grep -c 'pub fn handle_watch(cfg: &Config, quiet: bool, verbose: bool, interrupted: &Arc<AtomicBool>) -> Result<()>'` 输出 1
    - `cargo build --release` 退出码 0，输出无 `error[` 与 `warning:`
    - `cargo run --release -- watch --help` 输出包含 `TOML configuration file path` 且包含 `sqllog2db watch -c config.toml`
    - `cargo clippy --all-targets -- -D warnings` 退出码 0
  </acceptance_criteria>
  <done>
    `sqllog2db watch --help` 可显示帮助信息；编译通过零警告；watch.rs 仅含签名骨架，Plan 02 可直接填充函数体。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| crates.io → 项目 | 引入 notify = "6" 外部依赖，需人工验证包真实性 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-69-01-SC | Tampering | cargo add notify@6 | mitigate | Task 0 (`checkpoint:human-verify`, gate="blocking-human") 强制人工验证 crates.io owner / 仓库 / 下载量；slopcheck 不可用故按 RESEARCH [ASSUMED] 处理 |
| T-69-01-D | DoS | ErrorStats.records_exported 累计字段（usize） | accept | 单次进程内累计，u64/usize 上限远超实际日志规模；溢出风险可忽略 |
| T-69-01-I | Information Disclosure | watch --help 输出文案 | accept | 文案为静态英文 long_about/after_help，不泄露用户配置或路径 |
</threat_model>

<verification>
- `cargo build --release` 零错误零警告
- `cargo clippy --all-targets -- -D warnings` 退出码 0
- `cargo test --lib error::` 通过（ErrorStats 现有单元测试不破坏）
- `cargo run --release -- watch --help` 显示 watch 子命令帮助
- `grep -c 'records_exported' src/error.rs` 输出 ≥ 3（字段定义 + Default 注释 + merge 行）
- `cargo fmt --check` 通过
</verification>

<success_criteria>
- WATCH-01 子命令骨架可调用：`sqllog2db watch --help` 输出非空、退出码 0
- Cargo.toml 新增 notify = "6"，cargo build 通过
- ErrorStats 拥有 records_exported 字段，merge 累计，handle_run 赋值
- src/cli/mod.rs 声明 pub mod watch，src/cli/watch.rs 含 handle_watch 函数签名骨架
- 编译零警告、clippy 零错误、现有测试零回归
</success_criteria>

<output>
Create `.planning/phases/69-watch/69-01-SUMMARY.md` when done with structured summary covering: notify version added, ErrorStats fields modified, watch.rs signature locked, ready-for-Plan-02 contract.
</output>
