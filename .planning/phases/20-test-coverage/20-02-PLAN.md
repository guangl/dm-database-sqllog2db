---
phase: 20-test-coverage
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/integration.rs
autonomous: true
requirements:
  - TEST-02
  - TEST-03
tags:
  - testing
  - integration
  - boundary

must_haves:
  truths:
    - "tests/integration.rs 新增三条端到端测试: test_e2e_filter_pipeline / test_e2e_template_normalization / test_e2e_field_projection 全部通过"
    - "tests/integration.rs 新增四条边界测试: test_boundary_empty_log_file / test_boundary_all_filtered / test_boundary_malformed_line / test_boundary_long_sql 全部通过"
    - "cargo test --test integration 在新增测试加入后保持全绿"
    - "cargo clippy --all-targets -- -D warnings 零警告"
  artifacts:
    - path: "tests/integration.rs"
      provides: "端到端 + 边界集成测试"
      contains: "test_e2e_filter_pipeline"
  key_links:
    - from: "tests/integration.rs"
      to: "src/cli/run/mod.rs::handle_run"
      via: "完整 pipeline 调用"
      pattern: "handle_run"
    - from: "tests/integration.rs::test_e2e_filter_pipeline"
      to: "src/pipeline/filters/types.rs::IncludeFilters"
      via: "FiltersFeature.include 配置构造"
      pattern: "IncludeFilters"
    - from: "tests/integration.rs::test_e2e_template_normalization"
      to: "src/pipeline/fingerprint.rs::normalize_template"
      via: "TemplateConfig.enable=true 启用 + CSV normalized_sql 列非空"
      pattern: "TemplateConfig"
    - from: "tests/integration.rs::test_e2e_field_projection"
      to: "src/config OutputConfig"
      via: "fields 字段指定子集顺序"
      pattern: "OutputConfig"
---

<objective>
为 sqllog2db 补充端到端集成测试 (TEST-02) 与边界条件测试 (TEST-03)。

Purpose: 关闭 v1.4 Requirements 中 TEST-02/TEST-03 的覆盖缺口,验证完整 pipeline (输入 log → 过滤/归一化/投影 → CSV 输出) 在三条主功能路径和四个边界场景下的正确性。
Output: tests/integration.rs 文件末尾追加 7 条 `#[test]` 函数 (3 端到端 + 4 边界),全部通过 `cargo test`。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-test-coverage/20-CONTEXT.md
@.planning/phases/20-test-coverage/20-RESEARCH.md
@.planning/phases/20-test-coverage/20-PATTERNS.md
@tests/integration.rs
@src/cli/run/mod.rs
@src/cli/run/processor.rs

<interfaces>
<!-- 测试需要构造的关键 API,来自代码库 [VERIFIED]。执行者直接复用,无需探索。 -->

From `src/cli/run/mod.rs` (handle_run 10 参数签名):
```rust
pub fn handle_run(
    cfg: &Config,
    limit: Option<usize>,
    dry_run: bool,
    quiet: bool,
    interrupted: &Arc<AtomicBool>,
    progress_interval: u64,
    resume: bool,
    state_file_override: Option<&str>,
    jobs: usize,
    compiled_filters: Option<(CompiledMetaFilters, CompiledSqlFilters)>,
) -> Result<()>
```

From `tests/integration.rs` 已存在 helper:
```rust
fn write_test_log(path: &std::path::Path, count: usize)
// 写入达梦格式 N 条记录: "2025-01-15 10:30:28.001 (EP[0] sess:0x{i:04x} user:TESTUSER trxid:{i} stmt:0x1 appname:App ip:10.0.0.1) [SEL] SELECT * FROM t WHERE id={i}. EXECTIME: {exec}(ms) ROWCOUNT: {rows}(rows) EXEC_ID: {i}."

fn make_run_config(log_dir: &std::path::Path, csv_file: &std::path::Path) -> Config
// 返回最小 Config: sqllog.path = log_dir, exporter.csv.file = csv_file, overwrite=true
```

From `src/pipeline/filters/types.rs`:
```rust
pub struct IncludeFilters {
    pub users: Option<Vec<String>>,
    pub ips: Option<Vec<String>>,
    pub sessions: Option<Vec<String>>,
    pub threads: Option<Vec<String>>,
    pub statements: Option<Vec<String>>,
    pub apps: Option<Vec<String>>,
    pub tags: Option<Vec<String>>,
}
pub struct ExcludeFilters { /* 同 IncludeFilters */ }
pub struct FiltersFeature {
    pub enable: bool,
    pub include: IncludeFilters,
    pub exclude: ExcludeFilters,
    /* indicators, sql, record_sql */
}
```

From `src/pipeline/mod.rs` (顶层 pub use):
```rust
pub use config::{TemplateConfig, OutputConfig, FiltersFeature, NormalizeConfig};
// FIELD_NAMES: ["ts","ep","sess_id","thrd_id","username","trx_id","statement","appname",
//               "client_ip","tag","sql","exec_time_ms","row_count","exec_id","normalized_sql"]
```

From `src/cli/run/processor.rs` (错误行处理,L195 附近):
```rust
// 解析错误分支不 panic,执行 errors_in_file += 1 + log::warn!,继续处理后续行
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 追加三条端到端测试 (TEST-02, D-06)</name>
  <files>tests/integration.rs</files>
  <read_first>
    - tests/integration.rs (现有 1293 行——必读: 第 1-17 行 import 块、第 20-33 行 write_test_log、第 35-51 行 make_run_config、第 146-173 行 test_handle_run_real_csv_export 标准模式、第 850-881 行 test_handle_run_with_filters_builds_pipeline 过滤器构造模式、第 907-909 行 CSV 行数断言模式)
    - src/pipeline/mod.rs (FIELD_NAMES 常量 + TemplateConfig/OutputConfig 顶层 re-export)
    - src/pipeline/filters/types.rs (IncludeFilters/ExcludeFilters/FiltersFeature 字段)
    - src/config/mod.rs (Config 顶层结构,确认 cfg.filter / cfg.template / cfg.output 三个 Option 字段路径)
    - .planning/phases/20-test-coverage/20-RESEARCH.md §Pattern 2 (端到端测试写法范本)
    - .planning/phases/20-test-coverage/20-PATTERNS.md §tests/integration.rs — 端到端测试
  </read_first>
  <behavior>
    - test_e2e_filter_pipeline: 调用 `write_test_log(log/test.log, 10)` 写入 10 条 user=TESTUSER 记录,设置 `cfg.filter = Some(FiltersFeature { enable: true, include: IncludeFilters { users: Some(vec!["TESTUSER".into()]), ..Default::default() }, .. })`,运行 `handle_run`,断言 CSV 数据行数 = 10 (header + 10 data = 11 lines);追加一条 user=OTHER 的 log 文件后,断言仍只有 10 条数据行
    - test_e2e_template_normalization: 写入 5 条 log,设置 `cfg.template = Some(TemplateConfig { enable: true, output_csv_path: String::new(), output_sqlite_table: String::new() })`,运行 `handle_run`,断言 CSV header 包含 "normalized_sql",且每条数据行 normalized_sql 列非空 (split 后第 14 列长度 > 0)
    - test_e2e_field_projection: 写入 3 条 log,设置 `cfg.output = Some(OutputConfig { fields: Some(vec!["ts".into(), "username".into(), "sql".into()]) })`,运行 `handle_run`,断言 CSV 第一行 (header) 等于 "ts,username,sql" (注意 CSV 字段间逗号分隔且无额外空格),且数据行 split(',').count() == 3
  </behavior>
  <action>
    在 `tests/integration.rs` 文件末尾追加一个 section divider 注释 `// ── E2E pipeline tests (TEST-02) ─────────────────────────────────────────────` 后,新增三个 `#[test]` 函数: `test_e2e_filter_pipeline`、`test_e2e_template_normalization`、`test_e2e_field_projection`,严格遵循文件首部 imports 与现有 Arrange-Act-Assert 模式 (TempDir + write_test_log + make_run_config + handle_run + read_to_string + 断言)。

    若 `OutputConfig` / `TemplateConfig` 不在文件顶部 `use` 块中,在第 14 行 `use dm_database_sqllog2db::pipeline::{FiltersFeature, NormalizeConfig};` 中追加为 `use dm_database_sqllog2db::pipeline::{FiltersFeature, NormalizeConfig, OutputConfig, TemplateConfig};`。

    handle_run 调用必须使用完整 10 参数形式: `handle_run(&cfg, None, false, true, &interrupted, 80, false, None, 1, None).unwrap();` (D-05 验证真实 CSV 输出,不用 dry_run)。

    CSV 行数断言务必正确处理 header (CSV 第 1 行为 header,N 条 log 记录 → header + N data 行 = N+1 总行数,使用 `content.lines().count().saturating_sub(1)` 或 `assert_eq!(content.lines().count(), N + 1)`)。

    test_e2e_template_normalization 中,验证 normalized_sql 列非空的方式: 取第二行 (header 后第一行数据),用 `line.split(',').nth(14)` 获取索引 14 的字段 (即 FIELD_NAMES[14] = "normalized_sql"),断言该字段去除引号后长度 > 0。注意 CSV 字段含逗号或引号时会被 `write_csv_escaped` 包裹引号,普通 SQL 文本可能不被引号包裹——这里只断言"非空"即可。

    test_e2e_field_projection 的 CSV header 断言要求精确匹配 "ts,username,sql" (无尾随回车符,用 `content.lines().next().unwrap() == "ts,username,sql"`)。

    禁止: 使用 `#[should_panic]`; 引入 mock crate; 直接访问 processor.rs 内部的 errors_in_file 局部变量;使用未导入的类型。
  </action>
  <verify>
    <automated>cargo test --test integration test_e2e_ 2>&1 | tail -20 | grep -E 'test result: ok\. (3|[4-9]+) passed'</automated>
  </verify>
  <acceptance_criteria>
    - `tests/integration.rs` 含 `fn test_e2e_filter_pipeline` 函数 (grep `^fn test_e2e_filter_pipeline` 返回 1)
    - `tests/integration.rs` 含 `fn test_e2e_template_normalization` 函数
    - `tests/integration.rs` 含 `fn test_e2e_field_projection` 函数
    - `tests/integration.rs` 顶部 use 块含 `OutputConfig` 与 `TemplateConfig`
    - `cargo test --test integration test_e2e_filter_pipeline` 退出码 0
    - `cargo test --test integration test_e2e_template_normalization` 退出码 0
    - `cargo test --test integration test_e2e_field_projection` 退出码 0
    - `cargo clippy --all-targets -- -D warnings` 退出码 0
  </acceptance_criteria>
  <done>三条 E2E 测试函数追加完毕且全部 cargo test 通过</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 追加四条边界条件测试 (TEST-03, D-07)</name>
  <files>tests/integration.rs</files>
  <read_first>
    - tests/integration.rs (Task 1 完成后的最新版本,继续在末尾追加;再次确认第 56-81 行 test_handle_run_dry_run_empty_dir 模式)
    - src/cli/run/processor.rs (确认错误行处理: Err 分支 errors_in_file += 1 + log::warn!,不 panic、不中止)
    - src/cli/run/mod.rs (确认 handle_run 在 log_dir 内只有 0 字节文件时不会 panic)
    - .planning/phases/20-test-coverage/20-RESEARCH.md §Pitfall 3, §Pitfall 4 (错误日志不可外部访问、超长 SQL 必须保持达梦格式)
    - .planning/phases/20-test-coverage/20-PATTERNS.md §tests/integration.rs — 边界条件测试
  </read_first>
  <behavior>
    - test_boundary_empty_log_file: log_dir 中写入 0 字节 `empty.log`,运行 `handle_run`,断言 1) 返回 Ok 不 panic, 2) CSV 文件存在, 3) `content.lines().count() == 1` (只有 header,无数据行)
    - test_boundary_all_filtered: 写入 5 条 user=TESTUSER 记录,设置 include.users=["NONEXISTENT"],运行 `handle_run`,断言 1) 不 panic, 2) CSV 只有 header (lines().count() == 1)
    - test_boundary_malformed_line: 手动写一个 log 文件,包含 2 条正常达梦格式行 + 1 条无效行 (例如 "INVALID LINE NO TIMESTAMP HERE\n") + 2 条正常行;运行 `handle_run`,断言 1) 不 panic, 2) CSV 数据行数 = 4 (正常行被导出,无效行被跳过);用 `content.lines().count() == 5` (header + 4 data) 等价验证 (按 Pitfall 3,不直接访问内部 errors_in_file)
    - test_boundary_long_sql: 构造一条达梦格式 log 行,其中 SQL 文本部分用 `"X".repeat(1_048_576)` (1MB 字符) 替换 (注意保持完整格式: "2025-01-15 10:30:28.001 (EP[0] sess:0x0001 user:TESTUSER trxid:1 stmt:0x1 appname:App ip:10.0.0.1) [SEL] SELECT FROM t WHERE c='{huge}'. EXECTIME: 1(ms) ROWCOUNT: 1(rows) EXEC_ID: 1.");运行 `handle_run`,断言 1) 不 panic, 2) CSV 数据行数 == 1 (1 条记录正常导出)
  </behavior>
  <action>
    在 Task 1 追加的 E2E 测试段之后,新增 section divider 注释 `// ── Boundary tests (TEST-03) ─────────────────────────────────────────────────` 后,追加四个 `#[test]` 函数: `test_boundary_empty_log_file`、`test_boundary_all_filtered`、`test_boundary_malformed_line`、`test_boundary_long_sql`。

    所有测试遵循同一 Arrange-Act-Assert 结构与 handle_run 10 参数调用规范。

    test_boundary_empty_log_file 用 `std::fs::write(log_dir.join("empty.log"), b"").unwrap();` 创建 0 字节文件。

    test_boundary_all_filtered 沿用 Task 1 的 FiltersFeature/IncludeFilters 构造方式,users 设为不存在的字符串。

    test_boundary_malformed_line 用 `std::fs::write(...)` 直接拼接字符串,使用与 write_test_log 完全一致的达梦行格式作为正常行模板,在中间插入一行 `"INVALID LINE NO TIMESTAMP HERE\n"`。

    test_boundary_long_sql 用 `format!` 拼接达梦完整行,其中 SQL 文本部分通过 `"X".repeat(1_048_576)` 构造 1MB 字符串;关键: 必须保持 ". EXECTIME: ... ROWCOUNT: ... EXEC_ID: ..." 后缀完整,SQL 文本必须放在 `[SEL] ` 之后、`. EXECTIME:` 之前 (参考 Pitfall 4 的格式说明)。整个 log 文件只有这一条记录。

    所有断言用 `assert_eq!` 或 `assert!`,错误路径用 `result.unwrap()` 直接展开 (期望 Ok)。禁止 `#[should_panic]`。

    禁止: 验证 errors_in_file 内部变量;读取 sqllog2db.log error 日志文件 (路径不可控,见 Pitfall 3);把 SQL 字段直接超长但破坏行格式 (会被识别为解析错误而非超长字段,见 Pitfall 4)。
  </action>
  <verify>
    <automated>cargo test --test integration test_boundary_ 2>&1 | tail -20 | grep -E 'test result: ok\. (4|[5-9]+) passed'</automated>
  </verify>
  <acceptance_criteria>
    - `tests/integration.rs` 含 `fn test_boundary_empty_log_file` 函数
    - `tests/integration.rs` 含 `fn test_boundary_all_filtered` 函数
    - `tests/integration.rs` 含 `fn test_boundary_malformed_line` 函数
    - `tests/integration.rs` 含 `fn test_boundary_long_sql` 函数
    - `cargo test --test integration test_boundary_empty_log_file` 退出码 0
    - `cargo test --test integration test_boundary_all_filtered` 退出码 0
    - `cargo test --test integration test_boundary_malformed_line` 退出码 0
    - `cargo test --test integration test_boundary_long_sql` 退出码 0
    - `cargo test --test integration` 全套 (含原有 + Task 1 + Task 2 共 ≥ 62 个) 退出码 0
    - `cargo clippy --all-targets -- -D warnings` 退出码 0
  </acceptance_criteria>
  <done>四条边界测试函数追加完毕,全部 cargo test 通过,clippy 零警告</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 测试输入 → handle_run | 测试通过 tempfile::TempDir + 写入 log 文件控制输入,边界测试主动构造异常输入 (空文件/全过滤/格式错误/1MB SQL) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-04 | Denial of Service | handle_run 处理 1MB SQL 字段 | mitigate | test_boundary_long_sql 验证 1MB 字段不导致 panic 或 OOM (V5 Input Validation 边界);依据 RESEARCH.md 已验证 normalize_template 流式处理 + Vec::with_capacity 不额外扩容 |
| T-20-05 | Tampering | 格式错误的 log 行 | mitigate | test_boundary_malformed_line 验证解析错误被跳过 + 后续正常行继续导出 (processor.rs 既有错误处理) |
| T-20-06 | Information Disclosure | 测试临时文件 (TempDir) 泄露 | accept | tempfile crate RAII 自动清理,测试退出即删除 |
</threat_model>

<verification>
- `cargo test --test integration` 全套通过 (≥ 62 个测试: 原 55 + 3 E2E + 4 边界)
- `cargo clippy --all-targets -- -D warnings` 零警告
- `cargo fmt --check` 通过
- 7 个新增测试函数名可被 grep `^fn test_(e2e|boundary)_` 全部找到
</verification>

<success_criteria>
TEST-02 + TEST-03 完成: `tests/integration.rs` 新增 3 条端到端测试 + 4 条边界测试,全部通过 cargo test,clippy 零警告,fmt 通过。所有测试遵循现有 TempDir + write_test_log + make_run_config + handle_run 模式,无 mock,无 `#[should_panic]`。
</success_criteria>

<output>
Create `.planning/phases/20-test-coverage/20-02-SUMMARY.md` when done
</output>
