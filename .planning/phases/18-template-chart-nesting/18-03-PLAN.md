---
phase: 18-template-chart-nesting
plan: 03
type: execute
wave: 3
depends_on:
  - 18-02
files_modified:
  - src/cli/init.rs
  - tests/integration.rs
autonomous: true
requirements:
  - CONFIG-03
  - CONFIG-04

must_haves:
  truths:
    - "cargo run -- init -o /tmp/test.toml --force 生成的配置文件含 [template] / [charts] / [replace_parameters] / [filter] / [filter.include] / [filter.exclude] / [filter.indicators] / [filter.sql] 顶层段"
    - "生成的配置不再含 [pipeline.template_analysis] / [pipeline.charts] / [pipeline.filters] / [pipeline.normalize] / [pipeline.fields] 任何旧路径"
    - "cargo run -- validate -c /tmp/test.toml 退出码 0 — init 默认配置通过 validate()"
    - "中英两套 CONFIG_TEMPLATE 都已迁移；handle_init 函数体不变，仅模板字符串改造"
    - "tests/integration.rs 中 7 处 [pipeline.filters.*] 断言更新为新路径，3 处 cfg.pipeline.filters 字段访问改为 cfg.filter"
    - "新增端到端集成测试：init 后用 validate 命令验证生成的新格式配置可被解析"
    - "新增集成测试：含旧 [pipeline] 段的配置被 validate 命令拒绝，stderr 含 5 条迁移映射"
    - "cargo test 全套（含 integration tests）退出码 0；cargo clippy --all-targets -- -D warnings 零警告"
  artifacts:
    - path: "src/cli/init.rs"
      provides: "CONFIG_TEMPLATE_ZH 与 CONFIG_TEMPLATE_EN 全新格式（无 [pipeline.*] 路径）"
      contains: "[template]"
    - path: "tests/integration.rs"
      provides: "init 模板验证测试 + 旧路径拒绝测试 + filter 路径同步"
  key_links:
    - from: "src/cli/init.rs CONFIG_TEMPLATE_*"
      to: "src/config/mod.rs validate()"
      via: "init 命令生成 → validate 命令读取"
      pattern: "\\[template\\]"
    - from: "tests/integration.rs"
      to: "src/cli/init.rs + src/config/mod.rs"
      via: "端到端 CLI 调用断言"
      pattern: "test_init_generates_new_format"
---

<objective>
完成 Phase 18 的最后一步：

1. 更新 `cargo run -- init` 生成的默认配置模板（中英双语），输出新顶层格式，且 `validate` 命令可直接通过
2. 同步更新 `tests/integration.rs` 中所有引用旧 `[pipeline.*]` 路径的断言与 `cfg.pipeline.filters` 字段访问
3. 新增两条端到端集成测试：
   - `test_init_generated_config_passes_validate`：init → validate 链路成功
   - `test_validate_rejects_legacy_pipeline_format`：含 `[pipeline.template_analysis]` 段的 toml 触发明确的迁移错误

这是用户可见的最终接触点 — Plan 01/02 完成后即使代码全部通过测试，新用户仍会收到旧格式模板。此 Plan 收尾，使整个 Phase 18 的破坏性升级对外一致。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/18-template-chart-nesting/18-CONTEXT.md
@.planning/phases/18-template-chart-nesting/18-PATTERNS.md
@.planning/phases/18-template-chart-nesting/18-01-PLAN.md
@.planning/phases/18-template-chart-nesting/18-01-SUMMARY.md
@.planning/phases/18-template-chart-nesting/18-02-PLAN.md
@.planning/phases/18-template-chart-nesting/18-02-SUMMARY.md
@src/cli/init.rs

<interfaces>
Plan 01 后旧路径检测错误消息（reason 字符串前缀，integration 测试需断言此内容）：
```
配置格式已升级，请迁移以下字段：
  [pipeline.template_analysis] → [template]
  [pipeline.charts] → [charts]
  [pipeline.normalize] → [replace_parameters]
  [pipeline.filters.*] → [filter.*]
  [pipeline.fields] → [output.fields]
```

Plan 01/02 后 Config 顶层字段（用户配置应填充的目标）：
- [replace_parameters] → enable: bool（default true）, placeholders: Vec<String>
- [template] → enable: bool（default false）, output_csv_path: String（default ""），output_sqlite_table: String（default ""）
- [filter] → enable: bool
- [filter.include] → users / ips / sessions / threads / statements / apps / tags / start_ts / end_ts / trxids
- [filter.exclude] → users / ips / sessions / threads / statements / apps / tags
- [filter.indicators] → exec_ids / min_runtime_ms / min_row_count
- [filter.sql] → includes / excludes（事务级字面量）
- [filter.record_sql] → includes / excludes（记录级正则）
- [charts] → output_dir, top_n, frequency_bar, latency_hist, trend_line, user_pie
- [output] → fields: Option<Vec<String>>

handle_init 函数体（src/cli/init.rs lines 1-61）无需修改，只更新 CONFIG_TEMPLATE_ZH / CONFIG_TEMPLATE_EN 两个常量字符串内容。

tests/integration.rs lines 873/936/971/1165-1178 是当前需要迁移的位置（grep 结果）。
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 重写 src/cli/init.rs 中英两套 CONFIG_TEMPLATE，并新增 init→validate 端到端集成测试</name>
  <files>src/cli/init.rs, tests/integration.rs</files>
  <read_first>
    - src/cli/init.rs 全文件（含 handle_init 函数与 CONFIG_TEMPLATE_ZH/EN 常量）
    - tests/integration.rs（重点：lines 1140-1190 与 init 相关的现有断言、lines 860-1000 三处 cfg.pipeline.filters 引用）
    - .planning/phases/18-template-chart-nesting/18-CONTEXT.md `<specifics>` 段（新格式样例）
    - .planning/phases/18-template-chart-nesting/18-PATTERNS.md lines 309-368（init 模板改造段）
    - src/config/mod.rs（Plan 01 后的 Config struct 与 validate() — 校验集成测试断言能 match 错误消息）
    - src/cli/init.rs 已修改（如已存在）— 用于增量编辑
  </read_first>
  <behavior>
    - 测试 1（新增 tests/integration.rs）：调用 `handle_init(temp_path, true, Lang::Zh)`，断言生成文件包含字符串 `[template]`, `[charts]`, `[replace_parameters]`, `[filter]`, `[filter.include]`, `[filter.exclude]`, `[filter.indicators]`, `[filter.sql]`，且不包含 `[pipeline.template_analysis]` / `[pipeline.charts]` / `[pipeline.filters]` / `[pipeline.normalize]`
    - 测试 2（新增）：对 Lang::En 同样断言（仅检查路径名，注释不检查）
    - 测试 3（新增）：init 生成文件后用 `Config::from_file` 加载并调用 `validate()`，断言 `is_ok()`（即默认配置通过 validate 校验）
    - 测试 4（新增）：构造旧格式 toml 字符串（包含 `[pipeline.template_analysis]\nenabled = true`），写入临时文件，`Config::from_file` 后 `validate()` 返回 Err；err.to_string() 同时包含 `[pipeline.template_analysis] → [template]` 与 `[pipeline.filters.*] → [filter.*]`（验证 5 条迁移映射来自 Plan 01 的统一错误消息）
    - 测试 5（更新现有）：lines 1165-1178 的 init 模板内容断言从 `[pipeline.filters.include]` 等改为 `[filter.include]` / `[filter.exclude]` / `[filter.indicators]` / `[filter.sql]`
    - 测试 6（更新现有）：lines 873/936/971 三处 `cfg.pipeline.filters = Some(FiltersFeature { ... })` 改为 `cfg.filter = Some(FiltersFeature { ... })`
  </behavior>
  <action>
    第一步 — 完整重写 src/cli/init.rs 中 CONFIG_TEMPLATE_ZH 常量（lines 65-162）：
    - 保留顶部 `[sqllog]` 与 `[logging]` 段（路径与字段不变）
    - 删除整个 `[pipeline.normalize]` / `[pipeline.template_analysis]` / `[pipeline.filters]` / `[pipeline.filters.include]` / `[pipeline.filters.exclude]` / `[pipeline.filters.indicators]` / `[pipeline.filters.sql]` 段
    - 插入新顶层段（按 CONTEXT.md `<specifics>` 顺序），每段中文注释清晰：
        * `[replace_parameters]` — 含 `enable = true` + 注释说明（v1.3 中是 [pipeline.normalize]，v1.4 顶层化）
        * `[template]` — 含 `enable = false` + 注释中给出 `# output_csv_path = "outputs/templates.csv"` 与 `# output_sqlite_table = "sql_templates"` 两行示例（默认注释掉，与 D-04 默认 = 不生成对齐）
        * `[filter]` — 含 `enable = false` 与简短说明（v1.4 路径从 [pipeline.filters] 上升）
        * `[filter.include]` — 注释 users / ips / sessions / threads / statements / apps / tags / start_ts / end_ts / trxids 全部默认注释掉
        * `[filter.exclude]` — 同上，注释 users / ips / sessions / threads / statements / apps / tags
        * `[filter.indicators]` — 注释 exec_ids / min_runtime_ms / min_row_count
        * `[filter.sql]` — 注释 includes / excludes，注释中说明"事务级字面量匹配，不支持正则"
        * `[charts]` — 注释 output_dir / top_n / frequency_bar / latency_hist / trend_line / user_pie（默认全部注释，新用户用 `cargo run -- init` 不应触发 charts 生成，避免 validate_charts 跨字段依赖报错）
        * 注：原模板末尾的 `[resume]` 与 `[exporter.csv]` / `[exporter.sqlite]` 保持不变
    - 注释顶部增加一行说明：`# 配置说明：v1.4 已将原 [pipeline.*] 子表统一提升到顶层（[template] / [charts] / [filter] / [replace_parameters]）`
    - 严格不出现字符串 `pipeline.template_analysis`, `pipeline.charts`, `pipeline.filters`, `pipeline.normalize`, `pipeline.fields`（grep 反向断言会校验）

    第二步 — 同步重写 CONFIG_TEMPLATE_EN 常量（lines 164-248）：
    - 完全镜像中文版结构，注释翻译成英文
    - 段头与字段名与中文版完全一致（仅注释语言不同）

    第三步 — 验证 init 模板与 validate 兼容：
    - `[charts]` 段全部字段保持注释状态 → ChartsConfig 为 None → validate_charts 跳过（不要求 template.enable=true）
    - `[template]` 段 enable=false 且 output_csv_path/output_sqlite_table 为空 → validate 通过
    - `[filter]` 段 enable=false → validate_filter 跳过编译
    - `[replace_parameters]` 段 enable=true 但无 placeholders → NormalizeConfig 默认值，无需 placeholder_override
    - `[exporter.csv]` 段保留 file/overwrite/append → 默认 exporter 存在

    第四步 — 更新 tests/integration.rs 中已有 init 模板测试（lines 1140-1190）：
    - 找到 `content.contains("[pipeline.filters.include]")` 等四条断言，替换为：
        * `assert!(content.contains("[filter.include]"), "init template must contain [filter.include]")`
        * `assert!(content.contains("[filter.exclude]"), "init template must contain [filter.exclude]")`
        * `assert!(content.contains("[filter.indicators]"), "init template must contain [filter.indicators]")`
        * `assert!(content.contains("[filter.sql]"), "init template must contain [filter.sql]")`
    - 新增四条断言：
        * `assert!(content.contains("[template]"), "init template must contain [template]")`
        * `assert!(content.contains("[charts]"), "init template must contain [charts]")`
        * `assert!(content.contains("[replace_parameters]"), "init template must contain [replace_parameters]")`
        * `assert!(!content.contains("[pipeline."), "init template must NOT contain legacy [pipeline.*]")`

    第五步 — 更新 tests/integration.rs 中三处 cfg.pipeline.filters 字段访问（lines 873/936/971）：
    - 直接替换 `cfg.pipeline.filters = Some(FiltersFeature { ... })` 为 `cfg.filter = Some(FiltersFeature { ... })`（共 3 处）
    - 同步任何 `cfg.pipeline.template_analysis` / `cfg.pipeline.charts` / `cfg.pipeline.normalize` 引用（如有遗留）改为顶层字段

    第六步 — 新增端到端集成测试（追加到 tests/integration.rs 末尾或合适位置）：
    - `test_init_generated_zh_template_passes_validate`：
        ```text
        步骤：
        1. 创建临时 dir
        2. let path = dir.path().join("config.toml")
        3. handle_init(path.to_str().unwrap(), true, Lang::Zh) → ok
        4. Config::from_file(&path)? → cfg
        5. cfg.validate() → Ok(())
        6. （额外）grep 反向断言：内容不含 "pipeline." 子串
        ```
    - `test_init_generated_en_template_passes_validate`：同上但 Lang::En
    - `test_validate_rejects_legacy_pipeline_template_analysis`：
        ```text
        步骤：
        1. 临时 dir + config.toml
        2. 写入字符串：
           [sqllog]\npath = "sqllogs"\n[pipeline.template_analysis]\nenabled = true\n[exporter.csv]\nfile = "out.csv"\n
        3. Config::from_file(&path)? → cfg（解析应成功）
        4. cfg.validate() → Err
        5. 断言 err.to_string() 包含子串：
           - "[pipeline.template_analysis] → [template]"
           - "[pipeline.charts] → [charts]"
           - "[pipeline.filters.*] → [filter.*]"
        ```
    - `test_validate_rejects_legacy_pipeline_filters_section`：
        ```text
        类似上一条，但用 [pipeline.filters]\nenable = true 触发；断言同样的 5 条映射出现在错误消息中
        ```

    第七步 — 测试模块引入：
    - tests/integration.rs 顶部如已有 `use sqllog2db::cli::init::handle_init` / `use sqllog2db::lang::Lang` 则复用；否则补充 use 语句
    - 注意：handle_init 在 src/cli/init.rs 中是 `pub fn`，但 tests/integration.rs 走的是 crate 外部测试，需通过 `use sqllog2db::cli::init::handle_init` 访问 — 若 init 模块不是 `pub mod`，先在 src/cli/mod.rs 内确保 `pub mod init`（实施时检查 src/cli/mod.rs 当前可见性）

    第八步 — 最终验证流程：
    - 运行 `cargo run -- init -o /tmp/sqllog2db_init_test.toml --force`
    - 运行 `cargo run -- validate -c /tmp/sqllog2db_init_test.toml`
    - 两条命令均退出码 0
    - cat /tmp/sqllog2db_init_test.toml | grep -c "pipeline\." 输出 0
  </action>
  <verify>
    <automated>cargo test --test integration -- --test-threads=1 2>&amp;1 | tail -40 &amp;&amp; cargo run --quiet -- init -o /tmp/sqllog2db_phase18_test.toml --force &amp;&amp; cargo run --quiet -- validate -c /tmp/sqllog2db_phase18_test.toml &amp;&amp; (grep -c 'pipeline\.' /tmp/sqllog2db_phase18_test.toml | grep -qE '^0$' &amp;&amp; echo "OK: no legacy paths" || echo "FAIL: legacy paths found")</automated>
  </verify>
  <acceptance_criteria>
    - CLI 端到端：`cargo run -- init -o /tmp/sqllog2db_phase18_test.toml --force` 退出码 0
    - CLI 端到端：`cargo run -- validate -c /tmp/sqllog2db_phase18_test.toml` 退出码 0
    - 源断言（init.rs 无旧路径）：`grep -nE '\\[pipeline\\.' src/cli/init.rs` 不命中
    - 源断言（init.rs 含新路径）：`grep -cE '\\[template\\]|\\[charts\\]|\\[replace_parameters\\]|\\[filter\\]|\\[filter\\.include\\]' src/cli/init.rs` 对 ZH+EN 两个模板都命中（>= 10 行总匹配）
    - 行为断言（init 输出文件）：`grep -cE 'pipeline\\.' /tmp/sqllog2db_phase18_test.toml` 输出 0
    - 测试命令：`cargo test --test integration test_init_generated_zh_template_passes_validate test_init_generated_en_template_passes_validate test_validate_rejects_legacy_pipeline_template_analysis test_validate_rejects_legacy_pipeline_filters_section` 退出码 0
    - 测试命令：`cargo test --test integration` 退出码 0（全套集成测试通过）
    - 源断言（integration tests 字段访问）：`grep -nE 'cfg\\.pipeline\\.' tests/integration.rs` 不命中
    - 全局 clippy：`cargo clippy --all-targets -- -D warnings` 退出码 0
  </acceptance_criteria>
  <done>init 命令生成全新格式配置，validate 命令直接接受；tests/integration.rs 四条新端到端测试通过 + 旧 [pipeline.*] 路径断言全部清理；cargo test 与 cargo clippy --all-targets 全套零警告通过。</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user → `init` 命令生成的配置文件 | 模板默认值直接成为生产配置，必须能通过 validate 校验 |
| user → 旧版配置文件（v1.3） | 用户升级到 v1.4 后旧配置应被显式拒绝，避免功能默默关闭 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-09 | Information Disclosure / Repudiation | init 生成的默认配置含 `[charts]` 段且 `[template].enable = false`，触发 validate_charts 跨字段依赖错误 | mitigate | 第三步明确：init 模板中 `[charts]` 段全部字段保留为注释状态（output_dir 等以 `# output_dir = ...` 形式），使 ChartsConfig 为 None，跳过校验 |
| T-18-10 | Tampering | 集成测试断言 string match 弱，可能放过细微 reason 文本回归 | mitigate | 测试 4/5 同时断言 5 条迁移映射中的 3 条具体字符串（`[pipeline.template_analysis] → [template]`, `[pipeline.charts] → [charts]`, `[pipeline.filters.*] → [filter.*]`），构成多点交叉验证 |
| T-18-11 | DoS | init 命令在 CI / 测试环境创建临时文件不清理 | accept | 集成测试使用 `tempfile::TempDir`，drop 时自动清理；CLI 由用户负责其 -o 路径 |
| T-18-SC | Tampering | 包管理（cargo） | n/a | 本 Plan 不新增 crate 依赖 |

</threat_model>

<verification>
Plan 03 完成后（也是整个 Phase 18 的最终验证）：

1. `cargo build --release` 退出码 0
2. `cargo clippy --all-targets -- -D warnings` 退出码 0
3. `cargo test` 全套（含 --lib + --test integration + --doc）退出码 0
4. `cargo fmt --check` 退出码 0
5. CLI 端到端：`cargo run -- init -o /tmp/x.toml --force && cargo run -- validate -c /tmp/x.toml` 两步退出码均 0
6. CLI 反向：旧格式 toml（手写含 `[pipeline.template_analysis]`）通过 `cargo run -- validate -c old.toml` 时退出码非 0 且 stderr 含 5 条迁移映射

ROADMAP.md / STATE.md 在 SUMMARY 阶段会被自动更新。
</verification>

<success_criteria>
- CONFIG_TEMPLATE_ZH 与 CONFIG_TEMPLATE_EN 完全去除 `[pipeline.*]` 字符串，新增 `[template] / [charts] / [replace_parameters] / [filter] / [filter.include] / [filter.exclude] / [filter.indicators] / [filter.sql]` 八个段
- `cargo run -- init -o /tmp/x.toml --force && cargo run -- validate -c /tmp/x.toml` 端到端退出码 0
- tests/integration.rs 至少新增 4 条 init/validate 端到端测试
- tests/integration.rs 中所有 `cfg.pipeline.*` 字段访问与 `[pipeline.*]` 字符串引用全部清除
- cargo test 全套通过，cargo clippy --all-targets -- -D warnings 零警告，cargo fmt --check 通过
- Phase 18 全部完成：CONFIG-03 与 CONFIG-04 需求满足；REQUIREMENTS.md / ROADMAP.md 待 SUMMARY 阶段同步
</success_criteria>

<output>
Create `.planning/phases/18-template-chart-nesting/18-03-SUMMARY.md` when done
</output>
