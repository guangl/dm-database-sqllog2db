---
phase: 19-code-refactor
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/config/mod.rs
  - src/config/validate.rs
  - src/config/apply_one.rs
autonomous: true
requirements:
  - REFACTOR-01
  - REFACTOR-04
tags:
  - rust
  - refactor
  - module-split
  - visibility
  - config

must_haves:
  truths:
    - "src/config/mod.rs 行数从 1418 行精简到 ≤ 300 行（仅保留 Config struct + from_file + 子模块声明 + re-export）"
    - "validate / validate_and_compile / 私有 validate_* 方法已迁移到 src/config/validate.rs"
    - "apply_overrides / apply_one 已迁移到 src/config/apply_one.rs"
    - "src/config/mod.rs 中原 89 个 #[test] 函数已分类迁移到对应子文件的 #[cfg(test)] mod tests 块，且全部通过"
    - "外部调用路径 crate::config::Config / Config::from_file / Config::validate / Config::validate_and_compile / Config::apply_overrides 保持不变"
    - "Config 及其方法的可见性已按 D-10 收紧（pub → pub(crate)），仅 tests/integration.rs 必需的项保持 pub"
    - "cargo test 全部通过，cargo clippy --all-targets -- -D warnings 零警告"
  artifacts:
    - path: "src/config/mod.rs"
      provides: "精简后的模块入口：Config struct + from_file + 子模块声明 + re-export"
      contains: "pub struct Config"
      max_lines: 300
    - path: "src/config/validate.rs"
      provides: "Config::validate / validate_and_compile / 所有 validate_* 私有方法 + 对应测试"
      contains: "fn validate"
      max_lines: 300
    - path: "src/config/apply_one.rs"
      provides: "Config::apply_overrides / apply_one + 对应测试"
      contains: "fn apply_one"
      max_lines: 300
  key_links:
    - from: "src/config/mod.rs"
      to: "src/config/validate.rs"
      via: "mod validate"
      pattern: "mod validate"
    - from: "src/config/mod.rs"
      to: "src/config/apply_one.rs"
      via: "mod apply_one"
      pattern: "mod apply_one"
    - from: "src/cli/run.rs"
      to: "src/config/validate.rs"
      via: "cfg.validate_and_compile()"
      pattern: "validate_and_compile"
---

<objective>
将 1418 行的 `src/config/mod.rs` 就地拆分为 `src/config/mod.rs` (精简) + `src/config/validate.rs` + `src/config/apply_one.rs`（按 D-03），同时按 D-10 收紧可见性。

Purpose: 该文件中约 1000 行是测试代码（89 个 `#[test]`），真正业务逻辑约 400 行（Config struct + validate/apply_one 方法）。RESEARCH.md 推荐拆分顺序第 2 位（独立于 filters，可与 Plan 01 并行）。

Output:
- `src/config/mod.rs`（精简到 ≤ 300 行）
- `src/config/validate.rs`（新建，~200-300 行含测试）
- `src/config/apply_one.rs`（新建，~200-300 行含测试）
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/19-code-refactor/19-CONTEXT.md
@.planning/phases/19-code-refactor/19-RESEARCH.md
@.planning/phases/19-code-refactor/19-PATTERNS.md
@src/config/mod.rs
@src/lib.rs

<interfaces>
<!-- 外部消费者依赖路径（必须保持稳定） -->

```
crate::config::Config                              // src/cli/run.rs, src/cli/init.rs, src/cli/show_config.rs, src/cli/validate.rs, tests/integration.rs
crate::config::Config::from_file(path) -> Result<Self>
crate::config::Config::validate(&self) -> Result<()>
crate::config::Config::validate_and_compile(&self) -> Result<Option<(CompiledMetaFilters, CompiledSqlFilters)>>
crate::config::Config::apply_overrides(&mut self, &[String]) -> Result<()>
```

子模块（已存在，本 Plan 不动）：

```
src/config/exporter.rs   // pub struct CsvExporterConfig, ExporterConfig, SqliteExporterConfig
src/config/logging.rs    // pub struct LoggingConfig, LOG_LEVELS
src/config/resume.rs     // pub struct ResumeConfig
src/config/sqllog.rs     // pub struct SqllogConfig
```

src/config/mod.rs 现有 re-export（保持不变）：

```
pub use exporter::{CsvExporterConfig, ExporterConfig, SqliteExporterConfig};
pub use logging::{LOG_LEVELS, LoggingConfig};
pub use resume::ResumeConfig;
pub use sqllog::SqllogConfig;
```

tests/integration.rs 引用（必须保持可达）：

```rust
use dm_database_sqllog2db::config::{ ... };  // 多项导入，包括 Config 与各 *Config struct
let cfg = dm_database_sqllog2db::config::Config::from_file(&path).unwrap();
let cfg: dm_database_sqllog2db::config::Config = toml::from_str(&content).unwrap();
```
</interfaces>

<visibility_rules>
- `pub struct Config` → 保持 `pub`（tests/integration.rs 直接构造与反序列化，pub(crate) 不够，因为 integration test 是独立 binary 编译单元，引用 lib crate）

  说明：tests/integration.rs 是 `tests/` 目录下的 integration test，从 crate 外部链接 lib，因此 `Config` 必须保持 `pub`。Config 的字段同样需要 `pub`（integration test 通过 `toml::from_str` + 字段访问，或通过类型断言访问字段类型）。

- `Config::from_file` → 保持 `pub`（integration test 调用）
- `Config::validate` → 保持 `pub`（CLI handle_validate 调用，且可能被 integration test 间接覆盖）
- `Config::validate_and_compile` → `pub(crate)`（仅 src/cli/run.rs 内部使用，grep 验证）
- `Config::apply_overrides` → `pub(crate)`（仅 src/cli/run.rs / opts.rs 内部使用）
- 私有 `fn validate_filter / validate_output_fields / validate_template / validate_charts` → 保持 private（不带 pub）；若 validate.rs 需要让 Config impl 拆分到独立文件，使用 `impl Config { fn validate_* }` 块即可，不需额外 pub

收紧前后逐项以 `cargo build` + grep 验证：
```
grep -nE "^\s*pub\s+(fn|struct|enum)\b" src/config/mod.rs
# 预期仅命中 `pub struct Config` 与 `pub fn from_file` / `pub fn validate`
```
</visibility_rules>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 抽取 apply_overrides / apply_one 到 src/config/apply_one.rs</name>
  <files>
    src/config/mod.rs,
    src/config/apply_one.rs (new)
  </files>
  <read_first>
    src/config/mod.rs,
    .planning/phases/19-code-refactor/19-PATTERNS.md,
    src/cli/opts.rs
  </read_first>
  <action>
    1) 新建 `src/config/apply_one.rs`，从 src/config/mod.rs 第 129-316 行迁移 `apply_overrides` 与 `apply_one` 方法。具体步骤：

    2) 在 apply_one.rs 文件顶部：
       - `use super::Config;`
       - `use crate::error::{ConfigError, Error, Result};`
       - 若 apply_one 引用了任何 helper 函数（如 `Self::db_err` 类）或外部 const，逐项 `use super::*` 引入。

    3) 用 `impl Config { ... }` 块包裹两个方法：
       - `pub(crate) fn apply_overrides(&mut self, overrides: &[String]) -> Result<()>`（原方法体不变，可见性 `pub` → `pub(crate)`）
       - `fn apply_one(&mut self, key: &str, value: &str) -> Result<()>`（原方法体不变，保持 private 或改 `pub(super)` 仅当 validate.rs 需要调用）

    4) 在 src/config/mod.rs 中：
       - 删除原第 129-316 行的 apply_overrides / apply_one 方法（保留 `impl Config` 块中的其他方法）
       - 在文件顶部 mod 声明区添加 `mod apply_one;`（私有子模块，方法通过 `impl Config` 块自动归属于 Config）

    5) 迁移测试：在 src/config/mod.rs 第 404 行起的 `#[cfg(test)] mod tests` 中识别测 apply_overrides / apply_one 的测试函数（如 `test_apply_one_*`, `test_apply_overrides_*`），剪贴到 apply_one.rs 末尾的新 `#[cfg(test)] mod tests { use super::*; use crate::config::Config; ... }` 块。**测试函数体逐字不变**。

    6) apply_one.rs 行数控制：业务代码约 190 行 + 测试约 50-80 行 = ~270 行（≤ 300，符合 D-02）。

    7) `cargo build && cargo test --lib config::apply_one`（若测试模块路径无法这样指定，用 `cargo test apply_one` 模糊匹配）。
  </action>
  <verify>
    <automated>cargo build 2>&amp;1 | grep -E "error|warning" | head -10; cargo test --lib 2>&amp;1 | tail -10; wc -l src/config/apply_one.rs</automated>
  </verify>
  <acceptance_criteria>
    - src/config/apply_one.rs 存在，包含 `impl Config { pub(crate) fn apply_overrides` 和 `fn apply_one`
    - src/config/mod.rs 第 129-316 行附近的 apply_overrides/apply_one 方法已被删除
    - src/config/mod.rs 顶部包含 `mod apply_one;` 声明
    - `cargo build` 退出码 0
    - `cargo test --lib` 退出码 0，apply_one 相关测试在新位置仍通过（test count 不减少）
    - `wc -l src/config/apply_one.rs` ≤ 300 行
  </acceptance_criteria>
  <done>apply_one 已独立成文件，编译与测试通过。</done>
</task>

<task type="auto">
  <name>Task 2: 抽取 validate / validate_and_compile / validate_* 到 src/config/validate.rs</name>
  <files>
    src/config/mod.rs,
    src/config/validate.rs (new)
  </files>
  <read_first>
    src/config/mod.rs,
    .planning/phases/19-code-refactor/19-PATTERNS.md,
    src/cli/run.rs,
    src/cli/validate.rs
  </read_first>
  <action>
    1) 新建 `src/config/validate.rs`，从 src/config/mod.rs 第 63-401 行（除 apply_one 已迁出的 129-316 行外）迁移 validate 系列方法。具体步骤：

    2) validate.rs 文件顶部：
       - `use super::Config;`
       - `use crate::error::{ConfigError, Error, Result};`
       - `use crate::pipeline::filters::{CompiledMetaFilters, CompiledSqlFilters};`
       - 其他必要 use（按编译错误补齐）

    3) 用 `impl Config { ... }` 块包裹以下方法（逐字搬移，仅调整可见性）：
       - `pub fn validate(&self) -> Result<()>` → 保持 `pub`（tests/integration.rs 与 src/cli/validate.rs 都调用）
       - `pub fn validate_and_compile(&self) -> Result<Option<(CompiledMetaFilters, CompiledSqlFilters)>>` → `pub(crate)`（仅 src/cli/run.rs 调用，grep 确认）
       - 所有私有 `fn validate_filter / validate_output_fields / validate_template / validate_charts` 等保持 private

    4) 在 src/config/mod.rs 中：
       - 删除原 validate 系列方法
       - 在 mod 声明区添加 `mod validate;`

    5) 迁移测试：src/config/mod.rs 第 404 行起的 `#[cfg(test)] mod tests` 中，识别测 validate 的测试函数（约占 89 个测试的 80%+，命名模式：`test_validate_*`, `test_config_validate_*`, `test_validate_filter_*`, `test_validate_template_*` 等）。剪贴到 validate.rs 末尾的 `#[cfg(test)] mod tests { use super::*; use crate::config::Config; use crate::pipeline::*; ... }` 块。

    6) 余下少量不归属于 validate/apply_one 的测试（如测 `from_file`、Config struct serde 反序列化、整体集成行为）保留在 src/config/mod.rs 的 `#[cfg(test)] mod tests` 中。

    7) validate.rs 行数控制：业务约 200 行 + 测试约 60-100 行 = ~260-300 行。若超过 300，将私有 validate_filter/validate_template/validate_charts 中行数最大者再拆为 `src/config/validate/filter.rs` 等（D-02 硬约束）。**优先方案是 validate.rs 单文件容纳全部，仅在 wc -l 超过 300 时才进一步拆分**。

    8) `cargo build && cargo test --lib` 完整回归。
  </action>
  <verify>
    <automated>cargo build 2>&amp;1 | grep -E "error|warning" | head -10; cargo test --lib 2>&amp;1 | tail -10; wc -l src/config/*.rs</automated>
  </verify>
  <acceptance_criteria>
    - src/config/validate.rs 存在，包含 `impl Config { pub fn validate(` 和 `pub(crate) fn validate_and_compile(`
    - src/config/mod.rs 顶部包含 `mod validate;` 声明
    - src/config/mod.rs 不再包含原 validate / validate_and_compile / validate_filter / validate_output_fields / validate_template / validate_charts 方法定义（grep 验证）
    - `cargo build` 退出码 0
    - `cargo test --lib` 退出码 0，validate 相关测试在新位置仍通过
    - `wc -l src/config/validate.rs` ≤ 300 行（若超过，需进一步拆分）
  </acceptance_criteria>
  <done>validate 完整迁移到独立文件，编译与测试通过。</done>
</task>

<task type="auto">
  <name>Task 3: 精简 src/config/mod.rs，回归整体测试，收紧可见性</name>
  <files>
    src/config/mod.rs
  </files>
  <read_first>
    src/config/mod.rs,
    src/config/validate.rs,
    src/config/apply_one.rs,
    src/cli/run.rs,
    src/cli/init.rs,
    src/cli/show_config.rs,
    src/cli/validate.rs,
    src/cli/opts.rs,
    tests/integration.rs
  </read_first>
  <action>
    1) 检查 src/config/mod.rs 当前内容，确保仅保留以下内容（其余应在 Task 1/2 中已迁出）：
       - 文件顶部 `use` 语句
       - 子模块声明：`pub mod exporter; pub mod logging; pub mod resume; pub mod sqllog; mod validate; mod apply_one;`
       - 子模块 re-export：`pub use exporter::{...}; pub use logging::{...}; pub use resume::*; pub use sqllog::*;`
       - `pub struct Config { ... }` 定义（保留所有 `#[serde(default)]` 等属性）
       - `impl Config { pub fn from_file<P: AsRef<Path>>(path: P) -> Result<Self> { ... } }` 块
       - 余下未归属 validate/apply_one 的少量测试（约 5-10 个）

    2) 按 D-10 收紧 mod.rs 内可见性：
       - `pub struct Config` → 保持 `pub`（integration test 必需，binary crate 也通过 lib target）
       - Config 字段：保留 `pub`（integration test 通过 `toml::from_str` 反序列化时不直接访问字段，但 `serde(default)` 在 pub(crate) 字段上工作正常 → 优先尝试 `pub(crate)`；若 integration test 有字面字段访问则恢复 pub）

       具体做法：先全部改 `pub(crate)`，跑 `cargo build`，遇到 integration test 报字段不可见时再逐字段恢复 `pub`。

       grep 命令辅助识别字面访问：
       ```
       grep -nE "cfg\.\w+|config\.\w+" tests/integration.rs | head -40
       ```

       若 integration test 仅通过 `Config { ..Default::default() }` 或 `toml::from_str::<Config>()` 间接使用 → 字段可以全部 `pub(crate)`。

    3) `from_file` 方法可见性：
       - 若 tests/integration.rs 调用 `Config::from_file` → 保持 `pub`
       - grep 确认：`grep -n "from_file" tests/integration.rs` → 有调用 → 保持 `pub`

    4) 行数验证：`wc -l src/config/mod.rs` 应 ≤ 300（D-02 硬约束）。若仍超过：
       - 检查测试是否还有遗留未迁出的部分 → 继续迁移
       - 检查 use 语句是否有死代码 → 删除
       - 检查 Config struct 定义是否过大（字段过多）→ 接受，因为 struct 定义无法拆分；但应不会超过 300 行

    5) 完整回归：
       - `cargo build`
       - `cargo test --lib`（lib unit tests + doctest）
       - `cargo test --tests`（integration tests）
       - `cargo clippy --all-targets -- -D warnings`

    6) 若 clippy 报 `dead_code` 警告（pub 项被收紧后无消费者）：
       - 检查项是否真的未使用 → 是则**删除**该 pub 项
       - 否则评估是否应为 `pub(crate)` / `pub(super)` 而非加 `#[allow(dead_code)]`
  </action>
  <verify>
    <automated>cargo clippy --all-targets -- -D warnings 2>&amp;1 | tail -5; cargo test 2>&amp;1 | tail -5; wc -l src/config/mod.rs</automated>
  </verify>
  <acceptance_criteria>
    - `wc -l src/config/mod.rs` 输出 ≤ 300
    - `cargo build` 退出码 0
    - `cargo test` 退出码 0（lib + integration test 全通过）
    - `cargo clippy --all-targets -- -D warnings` 退出码 0
    - `grep -cE "^\s*pub\s+fn\s+validate" src/config/mod.rs` 应为 0（validate 全部迁出，除非 mod.rs 仍重新导出）
    - `grep -cE "^\s*fn\s+apply_one" src/config/mod.rs` 应为 0
    - tests/integration.rs 中 `dm_database_sqllog2db::config::Config::from_file` 调用编译通过
  </acceptance_criteria>
  <done>config/mod.rs 精简达标，所有测试通过，clippy 零警告，可见性收紧完成。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TOML 配置文件 → Config struct | 用户提供的 config.toml 经 `serde::Deserialize` 反序列化为 Config；本 Plan 不改变反序列化逻辑或 validate 内容 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-02-01 | Tampering | validate 方法行为变化 | mitigate | 拆分时**禁止修改方法体**，仅搬移文件位置与调整可见性；现有 89 个测试是回归安全网，Task 3 全量 `cargo test` 验证 |
| T-19-02-02 | Information Disclosure | Config 字段可见性 | mitigate | 字段优先尝试 `pub(crate)`；若 integration test 需字面访问再恢复 `pub`，避免无意义暴露 |
| T-19-02-SC | Tampering | npm/pip/cargo installs | N/A | 本 Plan 不引入新依赖，跳过包合法性门禁 |
</threat_model>

<verification>
- `cargo build` 通过
- `cargo test` 通过，原 89 个 #[test] 在新位置全部仍执行（test count 不减少）
- `cargo clippy --all-targets -- -D warnings` 零警告
- `wc -l src/config/mod.rs` ≤ 300
- `wc -l src/config/validate.rs` ≤ 300
- `wc -l src/config/apply_one.rs` ≤ 300
- tests/integration.rs 编译通过
</verification>

<success_criteria>
- src/config/mod.rs 行数从 1418 精简到 ≤ 300
- src/config/validate.rs 和 src/config/apply_one.rs 创建完成
- 89 个测试分类迁移到对应子文件，全部通过
- Config + from_file + validate 公开 API 路径保持稳定
- REFACTOR-01（config/mod.rs 拆分）和 REFACTOR-04（config 可见性收紧）部分完成
</success_criteria>

<output>
Create `.planning/phases/19-code-refactor/19-02-SUMMARY.md` when done
</output>
