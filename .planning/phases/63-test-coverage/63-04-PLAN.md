---
phase: 63-test-coverage
plan: 04
type: execute
wave: 2
depends_on:
  - 63-01-PLAN
  - 63-02-PLAN
  - 63-03-PLAN
files_modified:
  - .planning/phases/63-test-coverage/63-COVERAGE-REPORT.md
autonomous: true
requirements:
  - TEST-01
  - TEST-02

must_haves:
  truths:
    - "重新运行 cargo llvm-cov 后，Wave 1 的 4 个目标模块行覆盖率达到 80% 或文档化无法提升原因"
    - "Phase 63 覆盖率报告（63-COVERAGE-REPORT.md）记录 baseline → after 对比数字"
    - "至少 3 个覆盖不足区域（baseline < 60% 行覆盖或函数覆盖率明显低于平均）在报告中列出并对应 Wave 1 的测试补充"
    - "难以测试路径按 CONTEXT.md D-04 在 63-COVERAGE-REPORT.md 中文档化说明原因（无法测试）"
  artifacts:
    - path: ".planning/phases/63-test-coverage/63-COVERAGE-REPORT.md"
      provides: "覆盖率对比报告 + 难以测试路径文档"
      contains: "Baseline vs After"
      min_lines: 60
  key_links:
    - from: "63-COVERAGE-REPORT.md"
      to: "Wave 1 (Plans 01/02/03) 新增测试"
      via: "为每个识别区域记录 baseline 数字、after 数字、Δ 提升、对应测试函数名"
      pattern: "Baseline.*After.*Δ"
---

<objective>
Wave 2 收尾任务：重新运行覆盖率分析，对比 baseline → after 数字，生成 Phase 63 最终覆盖率报告并文档化无法测试路径。

Purpose: TEST-01 与 TEST-02 的成功标准要求识别 ≥3 个覆盖不足区域并使其行覆盖率达到 80% 或文档化无法测试原因。本计划在 Wave 1 完成全部测试补充后执行：(1) 重新运行 `cargo llvm-cov` 得到 after 数字；(2) 与 RESEARCH.md 中 baseline 对比；(3) 输出最终报告。
Output: `.planning/phases/63-test-coverage/63-COVERAGE-REPORT.md`，包含 baseline → after 对比表、识别区域提升对应、难以测试路径清单。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/63-test-coverage/63-CONTEXT.md
@.planning/phases/63-test-coverage/63-RESEARCH.md
@.planning/phases/63-test-coverage/63-PATTERNS.md
@.planning/phases/63-test-coverage/63-01-SUMMARY.md
@.planning/phases/63-test-coverage/63-02-SUMMARY.md
@.planning/phases/63-test-coverage/63-03-SUMMARY.md

<interfaces>
<!-- baseline 数字（来自 RESEARCH.md 实际运行，2026-06-03） -->

| 文件 | Baseline 行 | Baseline 函数 | Wave 1 责任计划 |
|------|-------------|---------------|------------------|
| `pipeline/filters/serde_helpers.rs` | 0.00% | 0.00% | Plan 01（types.rs mod tests 间接覆盖）|
| `pipeline/filters/types.rs` | 82.21% | 31.58% | Plan 01（types.rs mod tests 直接覆盖）|
| `exporter/csv/writer.rs` | 66.29% | 75.00% | Plan 02（csv/tests.rs has_metrics + 投影）|
| `exporter/sqlite/mod.rs` | 78.26% | 53.33% | Plan 02（sqlite/tests.rs 未初始化 + 投影）|
| `cli/init.rs` | 68.33% | 50.00% | — 本次不补，因 RESEARCH.md 优先级为 P2 |
| `cli/run/prescan.rs` | 70.79% | 64.29% | Plan 03（新建 mod tests）|
| `cli/run/collector.rs` | 71.74% | 66.67% | — 本次不补，P2 |
| `cli/run/processor.rs` | 72.09% | 75.00% | — 本次不补（Fatal/BreakQuota 难以测试，D-04）|
| `error.rs` | 78.29% | 92.31% | Plan 03（mod tests 末尾追加变体方法测试）|

Integral baseline TOTAL：行 90.63% / 函数 86.05%（RESEARCH.md 已记录；Plan 01 任务 1 已重新确认）。

<!-- 难以测试路径（来自 PATTERNS.md §难以测试路径，按 CONTEXT.md D-04 文档化） -->

| 路径 | 文件 | 原因 |
|------|------|------|
| `writer.write_all` 失败（磁盘满）| csv/writer.rs:203-208 | 需要 OS 状态（磁盘满），D-04 标记 |
| `initialize_pragmas` 执行失败 | sqlite/mod.rs:30-42 | 需要损坏的 SQLite 环境 |
| 非 UTF-8 路径 warn 分支 | cli/run/prescan.rs:126-130 | macOS/Linux 上模拟非 UTF-8 PathBuf 需要 unsafe |
| `ExportAction::BreakFatal` 路径 | cli/run/processor.rs | Fatal export 错误依赖 OS |
| `tick_progress` 中断检测 | cli/run/processor.rs | 多线程 AtomicBool，可测试但复杂度高，本阶段标记为 OUT OF SCOPE |
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 重新运行 cargo llvm-cov 并采集 after 数字</name>
  <files>无源码修改；生成 target/llvm-cov/html/index.html（覆盖 baseline）+ target/llvm-cov/after-summary.txt</files>
  <read_first>
    - .planning/phases/63-test-coverage/63-01-SUMMARY.md（baseline 数字与新增测试清单）
    - .planning/phases/63-test-coverage/63-02-SUMMARY.md
    - .planning/phases/63-test-coverage/63-03-SUMMARY.md
    - .planning/phases/63-test-coverage/63-RESEARCH.md §"实际覆盖率报告（已运行）" 与 §"完整文件覆盖率表"
  </read_first>
  <action>
    1. 执行 `cargo llvm-cov clean --workspace`（清除 Plan 01 任务 1 留下的 baseline 数据，避免与 Wave 1 新增测试合并产生混淆）。
    2. 执行 `cargo llvm-cov --html`（D-01）生成新报告至 `target/llvm-cov/html/index.html`。
    3. 执行 `cargo llvm-cov --summary-only 2>&1 | tee target/llvm-cov/after-summary.txt` 保存文本摘要。
    4. 在任务完成笔记中记录：(a) TOTAL 行/函数覆盖率（after 数字）；(b) baseline 中行覆盖率 < 80% 或函数覆盖率 < 60% 的 4 个目标文件（`pipeline/filters/serde_helpers.rs`、`pipeline/filters/types.rs`、`exporter/csv/writer.rs`、`exporter/sqlite/mod.rs`、`error.rs`、`cli/run/prescan.rs`）的 after 数字。
    5. 不修改任何源代码；不删除 Wave 1 生成的测试。
    若 `cargo llvm-cov` 命令报错或结果为空，先确认 `cargo test` 整体通过（执行 `cargo test 2>&1 | tail -20` 查看），再排查 llvm-cov 配置。
  </action>
  <verify>
    <automated>test -f target/llvm-cov/after-summary.txt &amp;&amp; grep -E "TOTAL" target/llvm-cov/after-summary.txt</automated>
  </verify>
  <acceptance_criteria>
    - 文件 `target/llvm-cov/after-summary.txt` 存在并包含 `TOTAL` 行
    - 文件 `target/llvm-cov/html/index.html` 存在（cargo llvm-cov --html 成功生成）
    - 任务完成笔记记录 after TOTAL 行/函数覆盖率数字
    - 任务完成笔记列出 6 个目标文件（serde_helpers/filters/types/csv/writer/sqlite/mod/error/prescan）的 after 行覆盖率与函数覆盖率
    - `cargo test 2>&1 | grep -E "test result:.*passed"` 不含 FAILED（确认 Wave 1 测试未引入回归）
  </acceptance_criteria>
  <done>after 报告生成完成；6 个目标文件 after 数字全部采集；cargo test 全绿。</done>
</task>

<task type="auto">
  <name>Task 2: 生成 Phase 63 覆盖率对比报告 63-COVERAGE-REPORT.md</name>
  <files>.planning/phases/63-test-coverage/63-COVERAGE-REPORT.md</files>
  <read_first>
    - Task 1 完成笔记中记录的 after 数字
    - .planning/phases/63-test-coverage/63-RESEARCH.md §"完整文件覆盖率表"（baseline 数字）
    - .planning/phases/63-test-coverage/63-PATTERNS.md §"难以测试的路径"
    - .planning/phases/63-test-coverage/63-CONTEXT.md D-04（难以测试路径处理原则）
    - .planning/phases/63-test-coverage/63-01-SUMMARY.md / 63-02-SUMMARY.md / 63-03-SUMMARY.md（新增测试清单）
  </read_first>
  <action>
    用 Write 工具新建 `.planning/phases/63-test-coverage/63-COVERAGE-REPORT.md`，内容必须包含以下 5 个章节（按列出顺序）：

    1. **章节 "## 报告元数据"**：phase 编号、生成日期、cargo-llvm-cov 版本（0.8.5）、引用 baseline 与 after summary 文件路径（target/llvm-cov/baseline-summary.txt 与 after-summary.txt）。

    2. **章节 "## TOTAL 覆盖率对比"**：表格列 `指标 | Baseline | After | Δ`，至少包含两行（行覆盖率、函数覆盖率），数字来自 Task 1 笔记。

    3. **章节 "## 识别的覆盖不足区域（Baseline）"**：表格列 `文件 | Baseline 行 | Baseline 函数 | 优先级（来自 RESEARCH.md）`，按 RESEARCH.md 已识别的 6 个区域填充：serde_helpers.rs、filters/types.rs、csv/writer.rs、sqlite/mod.rs、error.rs、prescan.rs。在表后注明至少 3 个区域（成功标准 2 要求）满足"行覆盖率低于 60% 的函数或模块"定义，且这些区域均在 Wave 1 中得到测试补充。

    4. **章节 "## 提升对比（按文件）"**：表格列 `文件 | Baseline 行 | After 行 | Δ 行 | Baseline 函数 | After 函数 | Δ 函数 | 责任计划 | 新增测试数`，覆盖 Plan 01/02/03 已补充的全部目标文件（至少 5 行：serde_helpers.rs、filters/types.rs、csv/writer.rs、sqlite/mod.rs、error.rs、prescan.rs）。"责任计划" 列填写 `Plan 01` / `Plan 02` / `Plan 03`。"新增测试数" 从对应 SUMMARY 文件读取。

    5. **章节 "## 难以测试路径（D-04 文档化）"**：表格列 `路径 | 文件 | 原因 | 处理决定`，至少包含 4 行（writer.write_all 失败、initialize_pragmas 失败、非 UTF-8 路径 warn、ExportAction::BreakFatal）。"处理决定" 统一填写 "本阶段标记为难以测试，按 CONTEXT.md D-04 文档化，不强行添加脆弱测试"。

    6. **章节 "## 成功标准对照"**：列出 Phase 63 ROADMAP 中 4 条成功标准，并在每条后注明本阶段交付如何满足：
       - 标准 1（生成覆盖率报告）：引用 `target/llvm-cov/html/index.html` + `target/llvm-cov/after-summary.txt`
       - 标准 2（识别 ≥3 个覆盖不足区域）：引用本报告 §3 表格中 ≥3 个 < 60% 行/函数覆盖率区域
       - 标准 3（识别区域行覆盖率达到 80% 或文档化）：引用本报告 §4（after 数字）与 §5（难以测试文档化）
       - 标准 4（cargo test 全绿 + clippy 全绿）：引用 Wave 1 三个 SUMMARY 与 Task 1 的 cargo test 验证

    报告使用 Markdown 表格；不使用截图；所有数字必须来自 Task 1 实测，不允许使用 baseline 或 RESEARCH.md 中数字作为 after 值。若 after 数字未达 80% 阈值，在该行 Δ 列追加备注说明是否归类为难以测试（D-04）。
  </action>
  <verify>
    <automated>test -f .planning/phases/63-test-coverage/63-COVERAGE-REPORT.md &amp;&amp; grep -c "^## " .planning/phases/63-test-coverage/63-COVERAGE-REPORT.md | awk '$1 >= 6 {exit 0} {exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - 文件 `.planning/phases/63-test-coverage/63-COVERAGE-REPORT.md` 存在
    - 文件中 `^## ` 二级标题数量 ≥ 6（对应章节 1–6）
    - 文件包含字符串 "Baseline" 与 "After" 各出现 ≥ 3 次（表格列名）
    - 文件 §3 表格至少 6 行数据行（不含表头）
    - 文件 §4 表格至少 5 行数据行，每行含 "Plan 01" / "Plan 02" / "Plan 03" 之一作为责任计划
    - 文件 §5 表格至少 4 行，每行 "处理决定" 列均含 "D-04"
    - 文件 §6 4 条成功标准均出现并附满足说明
    - 文件总行数 ≥ 60（must_haves 要求）
    - `grep -c "TOTAL" .planning/phases/63-test-coverage/63-COVERAGE-REPORT.md` ≥ 2（§2 表格中两次出现 TOTAL：行/函数对比）
  </acceptance_criteria>
  <done>63-COVERAGE-REPORT.md 写入完毕，6 个章节齐全；所有数字来自 Task 1 实测。</done>
</task>

<task type="auto">
  <name>Task 3: Phase 63 最终质量门禁验证</name>
  <files>无文件修改</files>
  <read_first>
    - Wave 1 各 SUMMARY 中记录的新增测试函数数量
    - 63-COVERAGE-REPORT.md（Task 2 输出）
  </read_first>
  <action>
    顺序执行以下三道质量门禁命令：
    1. `cargo test 2>&1 | tail -30` —— 验证整体测试通过；记录 passed 总数与 failed 数（failed 必须为 0）
    2. `cargo clippy --all-targets -- -D warnings 2>&1 | tail -10` —— 验证无 clippy 警告（退出码必须为 0）
    3. `cargo fmt --check 2>&1 | tail -5` —— 验证格式无偏差（退出码必须为 0）
    每道门禁结果均记录在任务完成笔记中（passed 数量、failed 数量、警告数量、格式偏差行数）。
    若任一门禁失败，停止 Phase 63 并返回失败原因，**不**通过 D-04 简化或跳过失败项。
  </action>
  <verify>
    <automated>cargo test 2>&amp;1 | grep -E "test result:.*passed.*0 failed" &amp;&amp; cargo clippy --all-targets -- -D warnings 2>&amp;1 | grep -v "warning:" | tail -3 &amp;&amp; cargo fmt --check</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test` 退出码 0，输出包含 "0 failed"，且总 passed 数量大于 Wave 1 baseline（Wave 1 新增 ≥ 22 个测试：Plan 01 ≥ 10 + Plan 02 ≥ 9 + Plan 03 ≥ 15）
    - `cargo clippy --all-targets -- -D warnings` 退出码 0
    - `cargo fmt --check` 退出码 0
    - 任务完成笔记记录三道门禁的实际数字（test passed 总数、clippy 警告数 0、fmt 偏差行数 0）
  </acceptance_criteria>
  <done>三道质量门禁全绿；Phase 63 准备就绪可进入 verify-work 与 ship 流程。</done>
</task>

</tasks>

<verification>
1. `cargo llvm-cov --summary-only | grep TOTAL` — after 数字可读取
2. `cargo test` — 全绿，passed 数比 Phase 63 启动前增加 ≥ 22
3. `cargo clippy --all-targets -- -D warnings` — 全绿
4. `cargo fmt --check` — 全绿
5. `.planning/phases/63-test-coverage/63-COVERAGE-REPORT.md` 存在且包含 6 个二级章节
</verification>

<success_criteria>
- `target/llvm-cov/after-summary.txt` 与 `target/llvm-cov/html/index.html` 存在（满足 ROADMAP 成功标准 1：覆盖率报告生成完毕）
- `63-COVERAGE-REPORT.md` §3 列出 ≥ 3 个 baseline 行覆盖率低于 60% 或函数覆盖率明显低于 60% 的区域（满足成功标准 2）
- §4 提升对比表显示 Wave 1 责任的目标文件 after 行覆盖率达到 80%，或在 §5 文档化为难以测试（满足成功标准 3）
- 三道质量门禁全绿（满足成功标准 4 + CONTEXT.md D-03 单元测试优先策略验证）
</success_criteria>

<output>
Create `.planning/phases/63-test-coverage/63-04-SUMMARY.md` when done. SUMMARY 必须记录：
- After 总体覆盖率数字（TOTAL 行 / 函数）
- baseline → after 提升幅度（百分比点）
- 三道质量门禁结果（passed 数 / 警告数 / 格式偏差数）
- 难以测试路径条目数（来自报告 §5）
</output>
