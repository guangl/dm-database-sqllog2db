---
phase: 45-ci
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/cli/run/sqlite_parallel.rs
  - src/cli/run/mod.rs
  - src/exporter/sqlite/mod.rs
  - src/exporter/mod.rs
  - src/cli/run/tests.rs
autonomous: true
requirements:
  - PERF-03

must_haves:
  truths:
    - "用户运行 `sqllog2db run` 配置 SQLite exporter 且日志目录有 >1 个文件时，自动走多文件并行解析路径"
    - "并行 SQLite 路径输出的 .db 文件内容与顺序模式逐条等价（排序后 record 完全相同）"
    - "并行 SQLite 路径在 ExporterManager::initialize() 之后启用 WAL 模式（PRAGMA journal_mode=WAL, synchronous=NORMAL）"
    - "现有 benchmark 路径（bench_sqlite.rs）仍使用 JOURNAL_MODE=OFF, SYNCHRONOUS=OFF 不受影响"
    - "并行解析任一线程失败时主线程返回 first_err，不写入部分数据"
  artifacts:
    - path: "src/cli/run/sqlite_parallel.rs"
      provides: "process_sqlite_parallel 函数 + collect_log_file 辅助函数"
      contains: "pub(super) fn process_sqlite_parallel"
    - path: "src/cli/run/mod.rs"
      provides: "use_sqlite_parallel 路由分支"
      contains: "use_sqlite_parallel"
    - path: "src/exporter/sqlite/mod.rs"
      provides: "SqliteExporter::set_wal_mode 方法"
      contains: "pub(crate) fn set_wal_mode"
    - path: "src/exporter/mod.rs"
      provides: "ExporterManager::set_sqlite_wal_mode 包装"
      contains: "pub(crate) fn set_sqlite_wal_mode"
    - path: "src/cli/run/tests.rs"
      provides: "test_sqlite_parallel_matches_sequential 测试"
      contains: "fn test_sqlite_parallel_matches_sequential"
  key_links:
    - from: "src/cli/run/mod.rs"
      to: "src/cli/run/sqlite_parallel.rs"
      via: "mod parallel + use parallel::process_sqlite_parallel"
      pattern: "use parallel::\\{.*process_sqlite_parallel"
    - from: "src/cli/run/sqlite_parallel.rs"
      to: "src/exporter/mod.rs"
      via: "exporter_manager.set_sqlite_wal_mode() 调用"
      pattern: "set_sqlite_wal_mode"
    - from: "src/exporter/mod.rs"
      to: "src/exporter/sqlite/mod.rs"
      via: "ExporterKind::Sqlite(e) => e.set_wal_mode()"
      pattern: "set_wal_mode"
---

<objective>
为 SQLite exporter 实现多文件跨文件并行解析路径：rayon 并行各文件解析（每线程产出 `Vec<(Sqllog, Option<String>)>`），主线程按文件原始顺序写入同一个 SqliteExporter（WAL 模式），与现有 process_csv_parallel 在 src/cli/run/parallel.rs 中的 merge-then-write 设计对称（per D-01, D-02）。通过追加 cargo test 验证并行输出与顺序模式 record 集合一致（D-03）。

Purpose: 实现 PERF-03 — 在多文件场景下利用多核 CPU 提升 SQLite 导出的解析吞吐量，同时不破坏 benchmark 历史可比性（保持 initialize_pragmas 的 OFF+OFF 配置不变）。

Output: 4 个文件修改 + 1 个新文件 + 1 个新测试，全部 cargo test/clippy/fmt 通过。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45-ci/45-CONTEXT.md
@.planning/phases/45-ci/45-RESEARCH.md
@.planning/phases/45-ci/45-PATTERNS.md

@src/cli/run/parallel.rs
@src/cli/run/mod.rs
@src/cli/run/processor.rs
@src/exporter/sqlite/mod.rs
@src/exporter/mod.rs
@src/cli/run/tests.rs

<interfaces>
<!-- 关键 API 契约，执行者无需再次探索代码库 -->

From src/cli/run/parallel.rs (现有 process_csv_parallel 签名，新函数对称):
```rust
pub(super) fn process_csv_parallel(
    log_files: &[PathBuf],
    cfg: &crate::config::Config,
    pipeline: &Pipeline,
    jobs: usize,
    show_progress: bool,
    interrupted: &Arc<AtomicBool>,
    do_normalize: bool,
    placeholder_override: Option<bool>,
    field_mask: FieldMask,
    ordered_indices: &[usize],
    sql_record_filter: Option<&CompiledSqlFilters>,
) -> Result<(Vec<(PathBuf, usize)>, usize)>
```

From src/cli/run/mod.rs 第 125 行 (现有路由判断):
```rust
let use_parallel = jobs > 1 && log_files.len() > 1 && final_cfg.exporter.csv.is_some();
```

From src/exporter/sqlite/mod.rs:
```rust
pub(crate) struct SqliteExporter { conn: Option<Connection>, ... }
fn conn_ref(&self) -> Result<&Connection>          // 第 99 行，已存在
fn db_err(reason: impl Into<String>) -> Error      // 第 93 行，已存在
fn initialize_pragmas(conn: &Connection)           // 第 30 行，必须保持不变
```

From src/exporter/mod.rs:
```rust
pub(crate) enum ExporterKind { Csv(CsvExporter), Sqlite(SqliteExporter) }
pub(crate) struct ExporterManager { exporter: ExporterKind }
impl ExporterManager {
    pub(crate) fn from_config(config: &Config) -> Result<Self>
    pub(crate) fn initialize(&mut self) -> Result<()>
    pub(crate) fn export_one_preparsed(&mut self, sqllog: &Sqllog, include_pm: bool, normalized: Option<&str>) -> Result<()>
    pub(crate) fn finalize(&mut self) -> Result<()>
}
```

From src/pipeline/normalizer.rs (compute_normalized 实际签名，含 record 参数):
```rust
pub fn compute_normalized<'a>(
    record: &dm_database_parser_sqllog::Sqllog,
    pm_sql: &str,
    buffer: &mut ParamBuffer,
    placeholder_override: Option<bool>,
    scratch: &'a mut Vec<u8>,
) -> Option<&'a str>
```

From src/pipeline/mod.rs:
```rust
pub trait LogProcessor: Send + Sync + std::fmt::Debug { ... }
// Pipeline 内部为 Vec<Box<dyn LogProcessor>>，因为 trait bound 包含 Send+Sync，故 Pipeline: Send+Sync
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 给 SqliteExporter 与 ExporterManager 添加 WAL 模式切换 API</name>
  <files>src/exporter/sqlite/mod.rs, src/exporter/mod.rs</files>
  <read_first>
    - src/exporter/sqlite/mod.rs (完整文件，特别是 initialize_pragmas 第 30-42 行，conn_ref 第 99-103 行，db_err 第 93-97 行)
    - src/exporter/mod.rs (完整文件，特别是 ExporterKind 第 47-101 行，ExporterManager 第 130-246 行)
    - .planning/phases/45-ci/45-PATTERNS.md §`src/exporter/sqlite/mod.rs` 章节
  </read_first>
  <action>
    在 src/exporter/sqlite/mod.rs 的 `impl SqliteExporter` 块内（紧接 conn_ref 之后，第 103 行之后）追加方法（**禁止修改 initialize_pragmas 函数，禁止改动 OFF+OFF 配置**，原因：benchmark 路径依赖此配置作为历史 baseline，修改会让 bench_sqlite.rs 历史对比失效——参见 RESEARCH.md Pitfall 1）:

    ```rust
    /// 启用 WAL 模式（仅供并行路径在 initialize 之后调用）。
    /// 不修改 initialize_pragmas，避免影响 benchmark 路径的 OFF+OFF 配置。
    pub(crate) fn set_wal_mode(&self) -> Result<()> {
        let conn = self.conn_ref()?;
        conn.execute_batch(
            "PRAGMA journal_mode = WAL;
             PRAGMA synchronous = NORMAL;",
        )
        .map_err(|e| Self::db_err(format!("set WAL mode failed: {e}")))?;
        Ok(())
    }
    ```

    在 src/exporter/mod.rs 的 `impl ExporterManager` 块内（紧接 initialize 第 195-200 行之后）追加包装方法，让并行路径无需直接访问 ExporterKind:

    ```rust
    /// 仅对 SQLite exporter 生效，启用 WAL 模式。
    /// 非 SQLite exporter 时静默 no-op（per D-02：并行场景下启用 WAL）。
    pub(crate) fn set_sqlite_wal_mode(&self) -> Result<()> {
        match &self.exporter {
            ExporterKind::Sqlite(e) => e.set_wal_mode(),
            ExporterKind::Csv(_) => Ok(()),
        }
    }
    ```

    严格遵循 CLAUDE.md：函数小于 40 行（实际均 <10 行），变量名描述性（`conn`/`e`，与上下文一致）。
  </action>
  <verify>
    <automated>cargo build --release 2>&amp;1 | grep -E "warning|error" | grep -v "warning: unused" || echo "OK"; cargo test --lib exporter::sqlite -- --nocapture 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - 源码断言：`grep -c 'pub(crate) fn set_wal_mode' src/exporter/sqlite/mod.rs` 输出 `1`
    - 源码断言：`grep -c 'pub(crate) fn set_sqlite_wal_mode' src/exporter/mod.rs` 输出 `1`
    - 源码断言：`grep -c 'PRAGMA journal_mode = WAL' src/exporter/sqlite/mod.rs` 输出 `1`
    - 源码断言（不变）：`grep -c 'PRAGMA journal_mode = OFF' src/exporter/sqlite/mod.rs` 仍输出 `1`（initialize_pragmas 未被修改）
    - 命令：`cargo build --release` 成功退出码 0，stderr 无 `warning:` 行
    - 命令：`cargo clippy --all-targets -- -D warnings` 退出码 0
  </acceptance_criteria>
  <done>SqliteExporter 暴露 set_wal_mode 方法，ExporterManager 暴露 set_sqlite_wal_mode 方法；initialize_pragmas 保持 OFF+OFF 不变；cargo build + clippy 全绿。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 新建 src/cli/run/sqlite_parallel.rs 并在 mod.rs 注册路由</name>
  <files>src/cli/run/sqlite_parallel.rs, src/cli/run/mod.rs</files>
  <read_first>
    - src/cli/run/parallel.rs (完整文件，特别是 process_csv_parallel 函数第 69-220 行)
    - src/cli/run/processor.rs (完整文件，理解 params_buffer/ns_scratch/sql_record_filter/pipeline 在单文件循环内的作用顺序)
    - src/cli/run/mod.rs (完整文件，特别是第 14-20 行 mod/use 声明、第 123-184 行路由分支、第 185-202 行 mode_label)
    - src/pipeline/normalizer.rs 第 339-380 行 (compute_normalized 签名)
    - .planning/phases/45-ci/45-PATTERNS.md §`src/cli/run/sqlite_parallel.rs` 和 §`src/cli/run/mod.rs` 章节
    - .planning/phases/45-ci/45-RESEARCH.md §"Code Examples" 章节
  </read_first>
  <behavior>
    - Test 1（隐式覆盖于 Task 3）: 当 jobs > 1 + log_files.len() > 1 + sqlite exporter 配置时，handle_run 走 process_sqlite_parallel 分支
    - Test 2（编译期断言）: process_sqlite_parallel 签名与 process_csv_parallel 对称（除返回类型）
    - Test 3（运行时）: 任一线程返回 Err 时，主线程不调用 finalize，不写入部分数据
  </behavior>
  <action>
    **A. 新建文件 src/cli/run/sqlite_parallel.rs**（per D-01/D-02，与 PATTERNS.md §`src/cli/run/sqlite_parallel.rs` 完全一致）。

    模块导入（参照 parallel.rs 第 1-9 行，去掉 CsvExporter 和 Path，新增 dm_database_parser_sqllog::Sqllog 和 LogParserBuilder）：

    ```rust
    use crate::error::{Error, Result};
    use crate::exporter::ExporterManager;
    use crate::pipeline::normalizer::ParamBuffer;
    use crate::pipeline::{CompiledSqlFilters, FieldMask, Pipeline};
    use dm_database_parser_sqllog::{LogParserBuilder, Sqllog};
    use log::info;
    use rayon::prelude::*;
    use std::path::PathBuf;
    use std::sync::Arc;
    use std::sync::atomic::{AtomicBool, Ordering};
    ```

    定义辅助函数 `collect_log_file`（线程内只解析+normalize+filter，不写出，**必须使用与 processor.rs 第 65-145 行完全一致的逻辑顺序**——参见 RESEARCH.md "Code Examples"）：

    - 函数签名：`fn collect_log_file(file: &PathBuf, pipeline: &Pipeline, do_normalize: bool, placeholder_override: Option<bool>, sql_record_filter: Option<&CompiledSqlFilters>, interrupted: &Arc<AtomicBool>) -> Result<Vec<(Sqllog, Option<String>)>>`
    - 内部建 `LogParserBuilder::new(file.to_str().unwrap_or_default()).build()`，失败映射为 `Error::Parser(ParserError::InvalidPath { path, reason, line_number: None })`，与 processor.rs 第 53-59 行一致
    - 在线程内维护 `ParamBuffer::default()` 和 `Vec<u8>::with_capacity(4096)` 各自的 ns_scratch
    - 复刻 processor.rs 第 65-145 行的循环逻辑（pipeline.is_empty 快速路径 → pipeline.run_with_meta → sql_record_filter.matches → compute_normalized → push 到 Vec），严格遵循下面的 PARAMS 双分支规则
    - **PARAMS 记录的 normalized buffer 更新规则（严格 mirror processor.rs 第 75-143 行）**:
      - 计算 `passes = pipeline.is_empty() || pipeline.run_with_meta(&record)`
      - 计算 `needs_processing = passes || (do_normalize && record.tag.is_none())`
      - 若 `!needs_processing` → 跳过该 record
      - 若 `passes`（进入 if 分支）: 检查 sql_record_filter（DML 记录走 filter，PARAMS 记录始终通过：`f.is_none_or(|f| record.tag.is_none() || f.matches(&record.sql))`）；通过后用快速路径条件 `do_normalize && (!params_buf.is_empty() || record.tag.is_none())` 决定是否调用 `compute_normalized`；最终 `rows.push((record, normalized.map(|s| s.to_owned())))`
      - 若 `!passes`（else 分支，即被过滤掉但 `do_normalize && record.tag.is_none()` 的 PARAMS 记录）: **仍必须调用** `compute_normalized` 更新 `params_buf`，不 push 到 rows（mirror processor.rs 第 134-143 行）。**忽略此分支会导致参数化 SQL 场景下 normalize 静默失效**（后续 DML 记录无法替换参数占位符）。
    - 中断检查：每条 record 进入循环时 `if interrupted.load(Ordering::Relaxed) { break; }`
    - 关键点 1（compute_normalized 签名 — 五参数）：实际签名为 `compute_normalized(record: &Sqllog, pm_sql: &str, buffer: &mut ParamBuffer, placeholder_override: Option<bool>, scratch: &mut Vec<u8>) -> Option<&str>`。调用必须严格按此顺序（参照 processor.rs 第 89-95 行）：`compute_normalized(&record, &record.sql, params_buf, placeholder_override, ns_scratch)`。**严禁使用四参数形式**（`(&record.sql, params_buf, ns_scratch, placeholder_override)` 是错误的旧签名，会导致编译失败）。
    - 关键点 2（跨线程返回）：`compute_normalized` 返回 `Option<&str>`，**必须复制为 `Option<String>` 才能跨线程传递**（&str 借用 ns_scratch，在函数返回后失效）。使用 `.map(|s| s.to_owned())`。
    - 函数长度必须 ≤40 行（CLAUDE.md 约束）；如超出需提取辅助函数（例如把循环体内 record-pass 分支提取为 `process_record` 闭包/函数）

    定义主函数 `pub(super) fn process_sqlite_parallel`（签名与 process_csv_parallel 完全对称，返回 `Result<(usize, usize)>` = (total_records, skipped)），参数顺序与 process_csv_parallel 一致（field_mask 和 ordered_indices 即便本函数不直接使用，仍保留参数以保持调用对称）：

    ```rust
    pub(super) fn process_sqlite_parallel(
        log_files: &[PathBuf],
        cfg: &crate::config::Config,
        pipeline: &Pipeline,
        jobs: usize,
        show_progress: bool,
        interrupted: &Arc<AtomicBool>,
        do_normalize: bool,
        placeholder_override: Option<bool>,
        field_mask: FieldMask,
        ordered_indices: &[usize],
        sql_record_filter: Option<&CompiledSqlFilters>,
    ) -> Result<(usize, usize)>
    ```

    （`show_progress`、`field_mask`、`ordered_indices` 当前在 SQLite 路径未使用——添加 `let _ = (show_progress, field_mask, ordered_indices);` 或在签名上以 `_` 前缀避免 clippy `unused_variables` 警告。保留参数以维持与 process_csv_parallel 调用方对称，方便日后扩展。）

    函数体（必须用辅助函数拆分以保持 ≤40 行/函数）：

    1. 第一辅助函数 `parallel_collect`（≤40 行）：构建 rayon ThreadPool，在 pool.install 中 par_iter 调用 `collect_log_file`，返回 `Vec<Result<Option<(PathBuf, Vec<(Sqllog, Option<String>)>)>>>`，每个任务在 interrupt 时 return Ok(None)
    2. 第二辅助函数 `merge_and_write`（≤40 行）：构建 `ExporterManager::from_config(cfg)?`、`initialize()?`、**紧接调用 `set_sqlite_wal_mode()?`**（per D-02 启用 WAL）、按 results 顺序展开写入 `export_one_preparsed(&record, true, normalized.as_deref())`，累计 total_records；遇到首个 Err 立即返回（不调用 finalize，避免部分数据持久化）；正常结束时调用 `finalize()?`
    3. 主函数 `process_sqlite_parallel`：调 parallel_collect → 处理 first_err 早返回 → 调 merge_and_write → 返回 (total, skipped)
    4. `info!("Parsing and exporting SQL logs (SQLite parallel, {jobs} jobs)...");` 由 mod.rs 调用前打印（与 CSV 分支一致）

    线程安全确认：`Pipeline`（由 `LogProcessor: Send + Sync` 推导）、`CompiledSqlFilters`（regex::Regex 为 Send+Sync）、`Arc<AtomicBool>` 均可跨线程共享。

    **B. 修改 src/cli/run/mod.rs**：

    1. 第 14-15 行 mod 声明区域：在 `mod parallel;` 之后追加 `mod sqlite_parallel;`
    2. 第 19 行 use 语句：保持 `use parallel::process_csv_parallel;`，新增一行 `use sqlite_parallel::process_sqlite_parallel;`
    3. 第 125 行替换（per RESEARCH.md Pattern 3，注意保留 `!is_stdin_pipe` 守卫）:
       ```rust
       let use_csv_parallel = jobs > 1 && log_files.len() > 1 && !is_stdin_pipe && final_cfg.exporter.csv.is_some();
       let use_sqlite_parallel = jobs > 1 && log_files.len() > 1 && !is_stdin_pipe && final_cfg.exporter.sqlite.is_some();
       let use_parallel = use_csv_parallel || use_sqlite_parallel;
       ```
       **注意**：现有第 125 行没有 `!is_stdin_pipe`，本次新增此守卫是为了避免 stdin 管道场景下错误进入并行（PATTERNS.md §`src/cli/run/mod.rs` 已采用此扩展）。**为保持向后兼容**：CSV 现有行为是允许 stdin（因为 stdin 时 log_files.len() == 1，jobs > 1 && len > 1 自然为 false），新增 !is_stdin_pipe 不改变实际行为，仅作显式安全网。
    4. 第 127 行 `if use_parallel {` 改为 `if use_csv_parallel {`
    5. 在 CSV 并行 `else` 之前插入 SQLite 并行分支（顺序：CSV 并行 → SQLite 并行 → 顺序）：
       ```rust
       } else if use_sqlite_parallel {
           info!("Parsing and exporting SQL logs (SQLite parallel, {jobs} jobs)...");
           let (total, parallel_skipped) = process_sqlite_parallel(
               &log_files, final_cfg, &pipeline, jobs, show_progress,
               interrupted, do_normalize, placeholder_override,
               field_mask, &ordered_indices, sql_record_filter,
           )?;
           total_records = total;
           skipped_files = parallel_skipped;
       ```
    6. 第 187 行 `let mode_label = if use_parallel { " [parallel]" } else { "" };` 保持不变（use_parallel 已是 use_csv_parallel || use_sqlite_parallel 的别名）

    严格遵循 CLAUDE.md：函数全部 ≤40 行（通过 parallel_collect/merge_and_write 拆分实现）；变量名 `params_buf` / `ns_scratch` / `total_records` / `skipped` 等描述性；选择更简单的 collect-merge-write 方案而非更复杂的 channel/ATTACH（per D-01）。
  </action>
  <verify>
    <automated>cargo build --release 2>&amp;1 | grep -E "^(warning|error)" || echo OK; cargo clippy --all-targets -- -D warnings 2>&amp;1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - 文件存在：`test -f src/cli/run/sqlite_parallel.rs &amp;&amp; echo OK` 输出 `OK`
    - 源码断言：`grep -c 'pub(super) fn process_sqlite_parallel' src/cli/run/sqlite_parallel.rs` 输出 `1`
    - 源码断言：`grep -c 'fn collect_log_file' src/cli/run/sqlite_parallel.rs` 输出 `1`
    - 源码断言：`grep -c 'set_sqlite_wal_mode' src/cli/run/sqlite_parallel.rs` 输出 `1`（Task 1 添加的 WAL 启用必须被调用）
    - 源码断言（五参数调用）：`grep -E 'compute_normalized\s*\(\s*&record\s*,\s*&record\.sql\s*,' src/cli/run/sqlite_parallel.rs | grep -v '^[[:space:]]*//' | wc -l | awk '{print ($1>=1)?"OK":"FAIL"}'` 输出 `OK`（compute_normalized 至少出现一次，且首两参数为 `&record, &record.sql`，符合五参数签名）
    - 源码断言（PARAMS 双分支）：`grep -c 'do_normalize && record\.tag\.is_none' src/cli/run/sqlite_parallel.rs` 输出 ≥ `1`（needs_processing 计算存在）
    - 源码断言（PARAMS 过滤分支保留 normalize）：`awk '/fn collect_log_file/,/^}/' src/cli/run/sqlite_parallel.rs | grep -cE 'else\s*\{' | awk '{print ($1>=1)?"OK":"FAIL"}'` 输出 `OK`（collect_log_file 内部至少有一个 else 分支，对应被过滤的 PARAMS 仍调用 compute_normalized）
    - 源码断言：`grep -vE '^\s*//' src/cli/run/mod.rs | grep -c 'mod sqlite_parallel'` 输出 `1`
    - 源码断言：`grep -vE '^\s*//' src/cli/run/mod.rs | grep -c 'use sqlite_parallel::process_sqlite_parallel'` 输出 `1`
    - 源码断言：`grep -c 'use_sqlite_parallel' src/cli/run/mod.rs` 输出 ≥ `3`（声明 + use_parallel 合成 + if 分支）
    - 源码断言：`grep -c 'use_csv_parallel' src/cli/run/mod.rs` 输出 ≥ `3`
    - 函数长度约束（CLAUDE.md）：`awk '/^\s*fn |^\s*pub(\(.*\))? fn /{name=$0; lines=0; next} /^\}/{if (lines>40) print FILENAME":"name" => "lines" lines"; next} {lines++}' src/cli/run/sqlite_parallel.rs` 无输出（所有函数 ≤40 行）
    - 命令：`cargo build --release` 退出码 0，无 `warning:` 行
    - 命令：`cargo clippy --all-targets -- -D warnings` 退出码 0
    - 命令：`cargo test --lib cli::run -- --nocapture` 退出码 0（现有 4 个 cli::run 测试全部通过）
  </acceptance_criteria>
  <done>sqlite_parallel.rs 文件存在，process_sqlite_parallel 与 collect_log_file 函数实现且均 ≤40 行；mod.rs 注册新模块并扩展为 use_csv_parallel/use_sqlite_parallel 双路由；现有测试全部通过；clippy + build 全绿。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 追加 test_sqlite_parallel_matches_sequential 正确性测试</name>
  <files>src/cli/run/tests.rs</files>
  <read_first>
    - src/cli/run/tests.rs (完整文件，特别是 test_parallel_merge_consistent 第 113-158 行，作为多文件 TOML 构造模板)
    - .planning/phases/45-ci/45-PATTERNS.md §"测试配置模板"
    - .planning/phases/45-ci/45-RESEARCH.md §"Pitfall 3: 并行路径顺序不确定性与测试"
  </read_first>
  <behavior>
    - Test 1: 3 个临时 .log 文件，**每个文件必须同时包含 INS（DML）记录与 PARAMS 记录**（验证并行路径下参数替换路径与顺序模式一致 — 此为本任务关键覆盖目标，单纯 INS 无法验证 PARAMS 分支），顺序模式跑 sqllog2db → seq.db；同样 3 个文件，并行模式（自动触发，因 jobs > 1 + len > 1 + sqlite.is_some()）→ par.db
    - Test 2: 用 rusqlite 打开两个 db，`SELECT exec_time_ms, row_count, exec_id, sql, normalized_sql FROM sqllog ORDER BY exec_id`（exec_id 是唯一定序键；新增 normalized_sql 字段以验证参数替换结果），收集为 `Vec<(i64, i64, i64, String, Option<String>)>`
    - Test 3: 排序后断言两个 Vec 相等（D-03：顺序可不同，内容必须相同；用 ORDER BY 消除并行写入顺序差异 → 详见 RESEARCH.md Pitfall 3）。**特别断言：含 PARAMS 上下文的 DML 记录在并行模式下的 normalized_sql 与顺序模式逐字节相同**（验证 collect_log_file 中 PARAMS 过滤分支正确更新 params_buf）
    - Test 4: 断言 par.db 文件存在且非空
  </behavior>
  <action>
    在 src/cli/run/tests.rs 末尾追加 `test_sqlite_parallel_matches_sequential` 函数。**禁止使用单线程模拟**——必须真正调用 handle_run 让其根据 jobs/log_files.len/exporter 自行路由。

    测试结构（参照 test_parallel_merge_consistent 第 113-158 行 TOML 构造模板）：

    1. 创建 `tempfile::TempDir`
    2. 写 3 个不同的 .log 文件：`a.log`、`b.log`、`c.log`，每个文件含 **混合 PARAMS + 参数化 DML 记录**（exec_id 全局唯一以便 ORDER BY 定序），使用项目中已验证的真实日志行格式（参考 tests.rs 已有样本与 dm-database-parser-sqllog 测试数据中的 PARAMS 行格式）：

       ```rust
       // a.log：1 条无参 INS、1 条 PARAMS（绑定参数）、1 条参数化 INS 引用上面 PARAMS
       // PARAMS 行 tag 为 None（record.tag.is_none()），DML 行 tag = Some("INS"/"UPD"/...)
       // 关键：PARAMS 行必须与紧随其后的参数化 DML 共享 sess/stmt，使 compute_normalized 能完成替换
       let log_a = "       2025-01-15 10:30:28.001 (EP[0] sess:0x0001 user:U trxid:1 stmt:0x1 appname:A ip:10.0.0.1) [INS] INSERT INTO t VALUES (1). EXECTIME: 1(ms) ROWCOUNT: 1(rows) EXEC_ID: 100.\n       2025-01-15 10:30:28.010 (EP[0] sess:0x0001 user:U trxid:1 stmt:0x2 appname:A ip:10.0.0.1) PARAMS(VARCHAR<42>) <参数 1 真实绑定值, 见 dm-database-parser-sqllog 测试样本>.\n       2025-01-15 10:30:28.011 (EP[0] sess:0x0001 user:U trxid:1 stmt:0x2 appname:A ip:10.0.0.1) [INS] INSERT INTO t VALUES (?). EXECTIME: 2(ms) ROWCOUNT: 1(rows) EXEC_ID: 101.\n";
       // b.log 同样格式但 EXEC_ID: 200, 201, 202（INS + PARAMS + 参数化 INS）
       // c.log 同样格式但 EXEC_ID: 300, 301, 302
       ```

       **如无法即时构造合法 PARAMS 字符串**：读取 `dm-database-parser-sqllog` crate 已有测试 fixtures（位于该 crate 的 `tests/` 或 `examples/`）复制一组已知合法的 PARAMS + 参数化 DML 对，确保 parser 能成功解析并触发 `compute_normalized` 的替换分支。

    3. 顺序模式：先把 3 个文件写入 `seq_dir` 子目录（**必须使用 ≥2 个文件**才能触发并行——使用 jobs=1 的方式不可控；改为只放 1 个文件触发顺序路径）。**简化方案**：使用单文件强制顺序路径（与现有 test_parallel_merge_consistent 第 137 行注释一致——"Force sequential by using a single-file config"）。

       因此修改方案：
       - **seq_dir**：放 1 个文件，合并所有 6 条记录（顺序：a 的 2 条 → b 的 2 条 → c 的 2 条），SQLite 输出到 `seq.db`
       - **par_dir**：放 3 个独立文件 a/b/c（与上面 6 条记录完全等价），SQLite 输出到 `par.db`
       - 触发 jobs > 1：使用 `std::thread::available_parallelism()`（与 mod.rs 第 52 行一致），CI 通常 ≥2

    4. 构造 SQLite TOML（参照 tests.rs CSV TOML 模板，替换 [exporter.csv] 为 [exporter.sqlite]）：

       ```rust
       let toml = format!(
           "[sqllog]\npath = \"{logdir}\"\n[error]\nfile = \"{errlog}\"\n[logging]\nfile = \"{applog}\"\nlevel = \"warn\"\nretention_days = 1\n[exporter.sqlite]\ndatabase_url = \"{db}\"\ntable_name = \"sqllog\"\noverwrite = true\nappend = false\nbatch_size = 1000\n",
           ...
       );
       ```

    5. 调用 `handle_run(&cfg, true, &Arc::new(AtomicBool::new(false)), None).unwrap();` 各一次
    6. 用 `rusqlite::Connection::open(db_path)` 打开两个 db，`SELECT exec_time_ms, row_count, exec_id, sql FROM sqllog ORDER BY exec_id`
    7. 用 `stmt.query_map([], |row| Ok((row.get::<_, i64>(0)?, row.get::<_, i64>(1)?, row.get::<_, i64>(2)?, row.get::<_, String>(3)?))).unwrap().collect::<Result<Vec<_>, _>>().unwrap()` 收集
    8. 断言两个 Vec 长度均为 6 且 `assert_eq!(seq_rows, par_rows, "并行 SQLite 输出与顺序模式记录集合应一致")`
    9. 断言 `std::fs::metadata(par_db_path).unwrap().len() > 0`

    测试函数命名严格为 `test_sqlite_parallel_matches_sequential`（per Wave 0 Gaps 列表 in RESEARCH.md §Validation Architecture）。**不要 #[ignore]**——该测试必须在 cargo test 默认运行集中执行。

    严格遵循 CLAUDE.md：测试函数可超过 40 行但应通过辅助闭包（如 `make_toml`、`read_db_rows`）拆分；变量名 `seq_db_path`/`par_db_path`/`seq_rows`/`par_rows` 描述性。
  </action>
  <verify>
    <automated>cargo test --lib cli::run::tests::test_sqlite_parallel_matches_sequential -- --nocapture 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - 源码断言：`grep -c 'fn test_sqlite_parallel_matches_sequential' src/cli/run/tests.rs` 输出 `1`
    - 源码断言：`grep -B1 'fn test_sqlite_parallel_matches_sequential' src/cli/run/tests.rs | grep -c '#\[test\]'` 输出 `1`（确认是测试函数）
    - 源码断言：`grep -A100 'fn test_sqlite_parallel_matches_sequential' src/cli/run/tests.rs | grep -c '#\[ignore\]'` 输出 `0`（确认未被忽略）
    - 源码断言（测试数据含 PARAMS）：`awk '/fn test_sqlite_parallel_matches_sequential/,/^}/' src/cli/run/tests.rs | grep -c 'PARAMS'` 输出 ≥ `1`（确保测试 fixture 字符串包含 PARAMS 行，否则无法验证并行路径下 params_buf 更新与顺序模式一致）
    - 源码断言（断言 normalized_sql 字段）：`awk '/fn test_sqlite_parallel_matches_sequential/,/^}/' src/cli/run/tests.rs | grep -c 'normalized_sql'` 输出 ≥ `1`（确保 SELECT 取出且断言了 normalized_sql 列）
    - 命令：`cargo test --lib cli::run::tests::test_sqlite_parallel_matches_sequential` 退出码 0，输出包含 `test cli::run::tests::test_sqlite_parallel_matches_sequential ... ok`
    - 命令：`cargo test` 退出码 0，无任何失败的测试
    - 命令：`cargo clippy --all-targets -- -D warnings` 退出码 0
    - 命令：`cargo fmt --check` 退出码 0
  </acceptance_criteria>
  <done>test_sqlite_parallel_matches_sequential 测试存在且 cargo test 通过；现有所有测试无回归；clippy/fmt/build 全部绿灯。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 文件系统 → 解析进程 | 读取本地 .log 文件（已在前序 phase 验证路径，本 phase 不引入新的输入源） |
| 并行线程 → 主线程数据传递 | `Vec<(Sqllog, Option<String>)>` 通过 par_iter().collect() 传递，无共享可变状态 |
| 主线程 → SQLite Connection | 单 Connection，单线程使用，无并发写入 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-01 | Tampering | 并行线程间共享数据 | mitigate | 线程内 collect Vec，主线程接收；无 Arc&lt;Mutex&gt; 共享状态；Pipeline/CompiledSqlFilters 均为 Send+Sync 不可变借用 |
| T-45-02 | Denial of Service | 大文件并行解析内存峰值 | accept | RESEARCH.md Pitfall 2 已说明：所有解析结果同时在内存中。本 phase 测试用 6 条小记录，生产文档说明此取舍；future work 可改 channel 流式（per Deferred） |
| T-45-03 | Information Disclosure | WAL 文件（output.db-wal）泄露中间数据 | accept | WAL 文件与 .db 同目录，权限继承（用户控制目录权限）；finalize 后 COMMIT 自动 checkpoint，无敏感数据残留 |
| T-45-04 | Tampering | 部分并行任务失败后写入残留数据 | mitigate | first_err 检查在 merge_and_write 之前；early return 不调用 finalize（事务未 COMMIT，SQLite 自动回滚 BEGIN TRANSACTION） |
| T-45-05 | Repudiation | 并行模式日志归因到具体文件 | mitigate | `info!("Parsing ... SQLite parallel, {jobs} jobs")` 标识模式；每文件解析仍走 LogParserBuilder，错误信息含文件路径 |
</threat_model>

<verification>
## Phase 整体检查

```bash
cargo build --release && \
cargo test && \
cargo clippy --all-targets -- -D warnings && \
cargo fmt --check
```

全部退出码 0 视为 Plan 1 完成。

## 并行路径手动验证（可选，跨 phase 抽样）

构造 3 个真实 .log 文件，分别用顺序（单文件）和并行（多文件）SQLite 模式跑一次，对比 sqlite3 dump 输出（ORDER BY exec_id）。本 phase 已通过 test_sqlite_parallel_matches_sequential 自动化覆盖此路径。
</verification>

<success_criteria>
1. **PERF-03 达成**：多文件场景（log_files.len() > 1 + jobs > 1 + SQLite exporter）自动走 process_sqlite_parallel；输出与顺序模式记录集合等价（test_sqlite_parallel_matches_sequential 通过）
2. **WAL 模式正确**：set_sqlite_wal_mode 在 initialize 后调用，但 initialize_pragmas 保持 OFF+OFF（benchmark 不受影响）
3. **质量门禁**：cargo build --release / test / clippy --all-targets -D warnings / fmt --check 全部退出码 0
4. **函数长度**：sqlite_parallel.rs 所有函数 ≤40 行（CLAUDE.md 约束）
5. **零新依赖**：Cargo.toml 不新增任何 crate（rayon/rusqlite 已存在）
</success_criteria>

<output>
Create `.planning/phases/45-ci/45-01-SUMMARY.md` when done, 内容遵循 summary.md 模板：
- 标记 PERF-03 已完成
- 列出新增文件（sqlite_parallel.rs）和修改文件（mod.rs, exporter/mod.rs, exporter/sqlite/mod.rs, tests.rs）
- 记录关键决策点：collect-merge-write 模式而非 channel；WAL 模式局部启用不修改 initialize_pragmas
- 记录测试命名与覆盖：test_sqlite_parallel_matches_sequential
</output>
