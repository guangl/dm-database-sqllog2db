---
phase: 59-cli-run-exporter-pipeline
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - src/cli/run/processor.rs
  - src/cli/run/sqlite_parallel.rs
autonomous: true
gap_closure: true
requirements:
  - STRUCT-01

must_haves:
  truths:
    - "normalize_and_export 函数体（processor.rs）≤40 行（关闭 VERIFICATION gap 1）"
    - "parallel_collect 函数体（sqlite_parallel.rs）≤40 行（关闭 VERIFICATION gap 2）"
    - "cargo test 638 项全部通过，0 失败（行为完全不变）"
    - "cargo clippy --all-targets -- -D warnings 零警告"
    - "cargo fmt --check 通过"
  artifacts:
    - path: "src/cli/run/processor.rs"
      provides: "normalize_and_export ≤40 行 + 新私有辅助函数 update_params_buffer_only"
      contains: "fn update_params_buffer_only("
    - path: "src/cli/run/sqlite_parallel.rs"
      provides: "parallel_collect ≤40 行 + 新私有辅助函数 run_parallel_parse"
      contains: "fn run_parallel_parse("
  key_links:
    - from: "src/cli/run/processor.rs::normalize_and_export"
      to: "update_params_buffer_only"
      via: "!passes 分支内直接调用，无返回值"
      pattern: "update_params_buffer_only\\("
    - from: "src/cli/run/sqlite_parallel.rs::parallel_collect"
      to: "run_parallel_parse"
      via: "替换 pool 创建 + pool.install 整块逻辑为单次调用 + ? 传播"
      pattern: "run_parallel_parse\\("
---

<objective>
关闭 Phase 59 VERIFICATION.md 报告的两个 SC-1 缺口（gap closure）：

1. **Gap 1**: `src/cli/run/processor.rs::normalize_and_export` 函数体 47 行（超 7 行）
   → 提取 `!passes` 早返回分支为私有辅助函数 `update_params_buffer_only`，使函数体降至 ≤40 行。

2. **Gap 2**: `src/cli/run/sqlite_parallel.rs::parallel_collect` 函数体 50 行（超 10 行）
   → 提取 `pool.install(...)` 并行块为私有辅助函数 `run_parallel_parse`，使函数体降至 ≤40 行。

Purpose: 满足 STRUCT-01 / ROADMAP SC-1 —— `src/cli/run/` 下所有函数体不超过 40 行（`handle_run`、`concat_csv_parts` 豁免已文档化）。
Output: 两个源文件各新增一个私有辅助函数；公开签名、行为、测试结果完全不变。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/59-cli-run-exporter-pipeline/59-CONTEXT.md
@.planning/phases/59-cli-run-exporter-pipeline/59-VERIFICATION.md
@src/cli/run/processor.rs
@src/cli/run/sqlite_parallel.rs
@src/cli/run/collector.rs
@src/pipeline/mod.rs

<interfaces>
<!-- 仅使用已有接口，不修改公开签名 -->

src/pipeline/mod.rs 第 5 行（pub re-export）:
```rust
pub(crate) use normalizer::compute_normalized;
```
compute_normalized 签名（在 src/pipeline/normalizer.rs 中定义）：以位置参数顺序传入
`record: &Sqllog, sql: &str, params_buffer: &mut ParamBuffer, placeholder_override: Option<bool>, ns_scratch: &mut Vec<u8>`，
返回 `Option<&str>`（在本辅助函数中调用结果将被丢弃）。

ParamBuffer：
- `use crate::pipeline::normalizer::ParamBuffer;`（processor.rs 已 import）

src/cli/run/collector.rs::collect_log_file 第 15-21 行：
```rust
pub(super) fn collect_log_file(
    file: &Path,
    pipeline: &Pipeline,
    do_normalize: bool,
    placeholder_override: Option<bool>,
    interrupted: &Arc<AtomicBool>,
) -> Result<(Vec<(Sqllog, Option<String>)>, usize)>
```

rayon::ThreadPool::install:
```rust
fn install<OP, R>(&self, op: OP) -> R where OP: FnOnce() -> R + Send
```

ExportAction（已存在于 processor.rs 第 13-20 行）：枚举 Continue / BreakQuota / BreakFatal。
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 提取 update_params_buffer_only，使 normalize_and_export 函数体 ≤40 行</name>
  <read_first>
    - src/cli/run/processor.rs（全文，重点关注 normalize_and_export 函数体第 41-87 行；尤其确认 !passes 分支 第 41-51 行（11 行）的 compute_normalized 调用参数顺序）
    - src/pipeline/mod.rs（第 5 行 compute_normalized re-export；其底层签名定义在 src/pipeline/normalizer.rs，需确认参数顺序：record, &record.sql, params_buffer, placeholder_override, ns_scratch）
    - .planning/phases/59-cli-run-exporter-pipeline/59-VERIFICATION.md（Gap 1 详情与拆分建议）
  </read_first>
  <files>src/cli/run/processor.rs</files>
  <behavior>
    - 新增私有 `fn update_params_buffer_only(record, params_buffer, placeholder_override, ns_scratch)`：仅在 `!passes` 路径被调用，封装 compute_normalized 调用，返回值被丢弃（不导出）。
    - normalize_and_export 行为完全不变：`!passes` 时调用 `update_params_buffer_only` 后立即 `return ExportAction::Continue;`；`passes==true` 路径（ns 计算、remaining 配额、export_one_preparsed + 三分支错误处理）逻辑保持原样、字节级一致。
    - 不修改 normalize_and_export 的签名（`pub(super) fn`、参数列表、`#[allow(clippy::too_many_arguments)]` 属性、返回类型 ExportAction 全部保留）。
  </behavior>
  <action>
    在 src/cli/run/processor.rs 中，在 `pub(super) fn normalize_and_export(` 之前（紧邻其上方，doc comment 之前）插入新私有辅助函数定义：

    `fn update_params_buffer_only(record, params_buffer, placeholder_override, ns_scratch)`
    - 参数类型：
      - `record: &Sqllog`
      - `params_buffer: &mut ParamBuffer`（ParamBuffer 已在文件顶部 use 引入）
      - `placeholder_override: Option<bool>`
      - `ns_scratch: &mut Vec<u8>`
    - 返回类型：`()`（无返回值）
    - 函数体：调用 `crate::pipeline::compute_normalized(record, &record.sql, params_buffer, placeholder_override, ns_scratch);`（结果丢弃，无需 `let _ =` 包装，因为 compute_normalized 返回 Option<&str>，可以裸调用——若 clippy 报 `must_use` 警告，则改为 `let _ = ...`）

    然后定位 normalize_and_export 函数体内当前的 `!passes` 分支（源文件第 41-51 行，共 11 行代码），将以下 11 行：

      ```
      if !passes {
          // 被过滤的 PARAMS 记录：仅更新 params_buffer，不导出。
          crate::pipeline::compute_normalized(
              record,
              &record.sql,
              params_buffer,
              placeholder_override,
              ns_scratch,
          );
          return ExportAction::Continue;
      }
      ```

    精确替换为以下 4 行（保留注释作为一行；如保留注释会导致行数仍超，可省略注释，但替换后总行数必须 ≤4 行代码）：

      ```
      if !passes {
          update_params_buffer_only(record, params_buffer, placeholder_override, ns_scratch);
          return ExportAction::Continue;
      }
      ```

    注意：
    - `do_normalize` guard：调用方 `process_log_file` 在第 184 行已通过 `needs_processing = passes || (do_normalize && record.tag.is_none())` 守卫，到达 normalize_and_export 且 `!passes` 时一定满足 `do_normalize && record.tag.is_none()`，因此辅助函数无需重复判断（与现有第 41-51 行的语义保持一致）。
    - 不修改 ns 计算、remaining 配额检查、export_one_preparsed 调用及其 Ok/fatal/non-fatal 三分支错误处理，所有相关行字节级保留。
    - 不修改文件顶部 use 语句（compute_normalized 与 ParamBuffer 已可用）。
    - 净影响：normalize_and_export 函数体行数从 47 行降至 ≤40 行（减少 7-8 行）；文件总行数变化约 +5（新增 8 行辅助函数 − 减少 7 行内联代码 + 空行）。
  </action>
  <verify>
    <automated>cargo build --message-format=short 2>&1 | grep -E "^(error|warning)" || echo BUILD_OK; cargo test --quiet 2>&1 | tail -5; cargo clippy --all-targets -- -D warnings 2>&1 | grep -E "^(error|warning)" || echo CLIPPY_OK; cargo fmt --check 2>&1 && echo FMT_OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^fn update_params_buffer_only(" src/cli/run/processor.rs` 输出 `1`
    - `grep -c "^pub(super) fn normalize_and_export(" src/cli/run/processor.rs` 输出 `1`（签名未改）
    - `grep -c "update_params_buffer_only(record, params_buffer, placeholder_override, ns_scratch)" src/cli/run/processor.rs` 输出 `1`（在 normalize_and_export 内部调用一次）
    - normalize_and_export 函数体行数 ≤40：使用 `awk '/^pub\(super\) fn normalize_and_export\(/{f=1} f{print} f && /^}$/{exit}' src/cli/run/processor.rs | sed -n '/-> ExportAction {/,/^}$/p' | tail -n +2 | head -n -1 | wc -l` 输出 ≤40
    - `cargo build` 退出码 0，零 warning
    - `cargo test --quiet` 退出码 0；总测试数 638，0 failed
    - `cargo clippy --all-targets -- -D warnings` 退出码 0，零警告
    - `cargo fmt --check` 退出码 0，无差异
  </acceptance_criteria>
  <done>
    - src/cli/run/processor.rs 包含新增私有辅助函数 update_params_buffer_only
    - normalize_and_export 函数体 ≤40 行
    - VERIFICATION gap 1 关闭
    - 所有质量门禁通过
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: 提取 run_parallel_parse，使 parallel_collect 函数体 ≤40 行</name>
  <read_first>
    - src/cli/run/sqlite_parallel.rs（全文，重点关注 parallel_collect 函数体第 21-70 行：pool 创建第 21-24 行、pool.install par_iter 块第 26-44 行（19 行）、results 汇总循环第 46-69 行）
    - src/cli/run/collector.rs（collect_log_file 签名第 15-21 行，确认 `pub(super) fn collect_log_file(file: &Path, pipeline: &Pipeline, do_normalize: bool, placeholder_override: Option<bool>, interrupted: &Arc<AtomicBool>) -> Result<(Vec<(Sqllog, Option<String>)>, usize)>`）
    - src/cli/run/parallel.rs（确认 super::collector::collect_log_file 在并行路径的调用方式，避免提取后语义偏离）
    - .planning/phases/59-cli-run-exporter-pipeline/59-VERIFICATION.md（Gap 2 详情与拆分建议）
  </read_first>
  <files>src/cli/run/sqlite_parallel.rs</files>
  <behavior>
    - 新增私有 `fn run_parallel_parse(log_files, pipeline, jobs, do_normalize, placeholder_override, interrupted)`：负责创建 rayon::ThreadPool、在 pool.install 中调用 par_iter().map() 完成所有文件的并行解析（每个文件调用 super::collector::collect_log_file），收集并返回原始 `Vec<Result<Option<(PathBuf, Vec<(Sqllog, Option<String>)>, usize)>>>`。
    - rayon ThreadPoolBuilder 失败时返回 Err 而非 panic（保留原有 `.map_err(|e| Error::Io(std::io::Error::other(e)))?` 语义）。
    - parallel_collect 行为完全不变：以 `?` 传播 run_parallel_parse 错误；保留原有 collected/first_err/skipped/total_parse_errors 汇总循环；最终返回 `(collected, skipped, total_parse_errors)`。
    - 不修改 parallel_collect 的签名、返回类型；不修改 process_sqlite_parallel 调用方。
  </behavior>
  <action>
    在 src/cli/run/sqlite_parallel.rs 中，在 `fn parallel_collect(` 定义之前（紧邻其上方）新增私有辅助函数 run_parallel_parse。

    1. 在 parallel_collect 上方插入类型别名（私有，文件作用域）以降低签名复杂度：

      `type ParseResults = Vec<Result<Option<(PathBuf, Vec<(Sqllog, Option<String>)>, usize)>>>;`

      （PathBuf、Sqllog、Result 均已在文件顶部 use 引入）

    2. 紧接其下插入辅助函数定义：

      `fn run_parallel_parse(log_files: &[PathBuf], pipeline: &Pipeline, jobs: usize, do_normalize: bool, placeholder_override: Option<bool>, interrupted: &Arc<AtomicBool>) -> Result<ParseResults>`

      函数体（≤25 行）：
      - 创建 ThreadPool：复用现有第 21-24 行的 `let pool = rayon::ThreadPoolBuilder::new().num_threads(jobs).build().map_err(|e| Error::Io(std::io::Error::other(e)))?;`
      - 返回 `Ok(pool.install(|| { log_files.par_iter().map(|file| { ... }).collect() }))`
      - 内层闭包内容字节级复用原 par_iter().map() 闭包（第 30-42 行）：interrupted 检查、调用 super::collector::collect_log_file、构造 `Ok(Some((file.clone(), rows, parse_errors)))`

    3. 修改 parallel_collect 函数体，将当前第 21-44 行（pool 创建 + pool.install 块，共 24 行）整体替换为单行：

      `let results = run_parallel_parse(log_files, pipeline, jobs, do_normalize, placeholder_override, interrupted)?;`

    4. 保留 parallel_collect 中：
       - 第 46-49 行：collected/first_err/skipped/total_parse_errors 变量初始化
       - 第 51-65 行：for result in results { ... } 汇总循环
       - 第 66-69 行：first_err 检查 + Ok 返回
       字节级一致。

    5. 不修改：
       - parallel_collect 签名（fn 名、参数列表、返回类型）
       - process_sqlite_parallel（其调用 `let (collected, skipped, total_parse_errors) = parallel_collect(...)?;` 接口完全不变）
       - 文件顶部 use 语句

    6. 若 clippy 报 type_complexity 警告：保留类型别名 ParseResults（已抽出复杂返回类型）；若仍报警，可在 run_parallel_parse 上加 `#[allow(clippy::type_complexity)]`。
       若 clippy 报 dead_code（type alias 不导出）：用 `#[allow(dead_code)]` 或将别名内联到签名中。优先保留别名以增强可读性。

    净影响：parallel_collect 函数体从 50 行降至 ≤30 行（减少约 20 行）；新增 ~20 行辅助函数；文件总行数基本持平。
  </action>
  <verify>
    <automated>cargo build --message-format=short 2>&1 | grep -E "^(error|warning)" || echo BUILD_OK; cargo test --quiet 2>&1 | tail -5; cargo clippy --all-targets -- -D warnings 2>&1 | grep -E "^(error|warning)" || echo CLIPPY_OK; cargo fmt --check 2>&1 && echo FMT_OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^fn run_parallel_parse(" src/cli/run/sqlite_parallel.rs` 输出 `1`
    - `grep -c "^fn parallel_collect(" src/cli/run/sqlite_parallel.rs` 输出 `1`（签名未改）
    - `grep -c "let results = run_parallel_parse(" src/cli/run/sqlite_parallel.rs` 输出 `1`（在 parallel_collect 内调用一次）
    - `grep -c "pool.install" src/cli/run/sqlite_parallel.rs` 输出 `1`（仅出现在 run_parallel_parse 内，不再出现在 parallel_collect 内）
    - parallel_collect 函数体行数 ≤40：使用 `awk '/^fn parallel_collect\(/{f=1} f{print} f && /^}$/{exit}' src/cli/run/sqlite_parallel.rs | sed -n '/-> Result<(Vec.*usize, usize)> {/,/^}$/p' | tail -n +2 | head -n -1 | wc -l` 输出 ≤40（备选：手动按 `{` 到对应 `}` 之间行数核算）
    - `cargo build` 退出码 0，零 warning
    - `cargo test --quiet` 退出码 0；总测试数 638，0 failed（特别包含 `test_sqlite_parallel_matches_sequential` 通过）
    - `cargo clippy --all-targets -- -D warnings` 退出码 0，零警告
    - `cargo fmt --check` 退出码 0，无差异
  </acceptance_criteria>
  <done>
    - src/cli/run/sqlite_parallel.rs 包含新增私有辅助函数 run_parallel_parse + ParseResults 类型别名
    - parallel_collect 函数体 ≤40 行
    - process_sqlite_parallel 接口与行为不变
    - VERIFICATION gap 2 关闭
    - 所有质量门禁通过
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: 最终验证 — STRUCT-01 / ROADMAP SC-1 全量满足</name>
  <read_first>
    - src/cli/run/processor.rs（Task 1 修改后状态）
    - src/cli/run/sqlite_parallel.rs（Task 2 修改后状态）
    - src/cli/run/parallel.rs（确认 concat_csv_parts 43 行豁免文档仍存在于 PLAN-04 Pitfall 4）
    - src/cli/run/collector.rs（确认 collect_log_file 仍为 40 行未受影响）
    - src/cli/run/mod.rs（确认 run_file_loop 36 行 + run_sequential 18 行未受影响）
    - .planning/phases/59-cli-run-exporter-pipeline/59-VERIFICATION.md（核对 gap 列表）
  </read_first>
  <files>src/cli/run/processor.rs, src/cli/run/sqlite_parallel.rs</files>
  <behavior>
    纯验证任务：不修改任何源代码；仅运行命令、统计函数体行数并核对 VERIFICATION.md 中两个 gap 已关闭。
  </behavior>
  <action>
    按顺序执行下列验证步骤；任一失败必须报告具体错误（输出 + 行数），不允许跳过：

    1. 编译：`cargo build` —— 期望零 error、零 warning
    2. 测试：`cargo test --quiet` —— 期望 638 passed，0 failed，0 ignored（与 VERIFICATION.md 基线一致）
    3. clippy：`cargo clippy --all-targets -- -D warnings` —— 期望零 error、零 warning
    4. 格式：`cargo fmt --check` —— 期望无差异
    5. 函数体行数审计（processor.rs::normalize_and_export）：
       从 `pub(super) fn normalize_and_export(` 起到对应 `^}$` 行；
       函数体行数 = 闭合 `{` 后下一行 至 闭合 `}` 前一行 的行数；
       期望 ≤40。
    6. 函数体行数审计（sqlite_parallel.rs::parallel_collect）：
       同方法统计；期望 ≤40。
    7. 顺手检查 src/cli/run/ 其余函数体行数无回退（concat_csv_parts 43 行豁免、其余 ≤40）：
       `for f in src/cli/run/*.rs; do echo "=== $f ==="; awk '/^(pub(\(.*\))?\s+)?fn /{name=$0; line=NR} /^}$/{if(name){print NR-line, name; name=""}}' "$f"; done`
       人工核对：除 handle_run (101 行，已文档化豁免) 与 concat_csv_parts (43 行，已文档化豁免) 外，所有函数 ≤40 行。

    若全部通过，在生成的 59-06-SUMMARY.md 中记录：
    - normalize_and_export 最终函数体行数
    - parallel_collect 最终函数体行数
    - cargo test 通过数（应为 638）
    - 已关闭 gap 编号（gap 1、gap 2）
  </action>
  <verify>
    <automated>cargo build 2>&1 | grep -E "^(error|warning)" || echo BUILD_OK; cargo test --quiet 2>&1 | tail -5; cargo clippy --all-targets -- -D warnings 2>&1 | grep -E "^(error|warning)" || echo CLIPPY_OK; cargo fmt --check && echo FMT_OK</automated>
  </verify>
  <acceptance_criteria>
    - cargo build: 零 error，零 warning
    - cargo test --quiet: 638 passed, 0 failed（与 VERIFICATION.md Truth 3 基线一致）
    - cargo clippy --all-targets -- -D warnings: 零警告
    - cargo fmt --check: 无差异
    - processor.rs::normalize_and_export 函数体 ≤40 行（VERIFICATION gap 1 关闭）
    - sqlite_parallel.rs::parallel_collect 函数体 ≤40 行（VERIFICATION gap 2 关闭）
    - src/cli/run/ 下其他函数行数无回退（除 handle_run 101 行 + concat_csv_parts 43 行已文档化豁免外，全部 ≤40 行）
    - STRUCT-01 / ROADMAP SC-1 完全满足：「src/cli/run/ 下所有函数体不超过 40 行」（带豁免）
  </acceptance_criteria>
  <done>
    - Phase 59 两个 VERIFICATION gap 全部关闭
    - STRUCT-01 完全满足
    - ROADMAP SC-1 完全满足
    - 全量质量门禁通过
  </done>
</task>

</tasks>

<threat_model>
本计划为纯代码结构整理（提取私有辅助函数 + 类型别名），无新输入边界、无外部依赖变更、不修改公开 API、不变更可观察行为。

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 无新增 | 本次修改完全位于现有 cli/run 内部，不引入新边界 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-59-06-01 | Tampering | update_params_buffer_only 中 compute_normalized 参数顺序错配（位置参数易错） | mitigate | read_first 强制阅读 pipeline/mod.rs 第 5 行 re-export 与 normalizer.rs 中签名；cargo build 编译期类型检查会拦截顺序错误；现有 638 测试集中的 PARAMS-处理相关用例覆盖 !passes 路径 |
| T-59-06-02 | Tampering | run_parallel_parse 提取后 Arc<AtomicBool> 借用语义改变（误用 Arc::clone 导致语义偏离） | mitigate | 严格传递 `&Arc<AtomicBool>` 引用而非 clone，保持原 Arc 共享语义；执行 test_sqlite_parallel_matches_sequential 验证并行结果与顺序一致 |
| T-59-06-03 | Information Disclosure | 函数提取后丢失对 file_str / parse_errors 的错误日志路径 | accept | 错误处理路径完全位于 parallel_collect 内的汇总循环（未修改），run_parallel_parse 仅负责解析；行为零变化 |
</threat_model>

<verification>
- cargo build 通过且无 warning
- cargo test 全部 638 项通过（含 test_sqlite_parallel_matches_sequential、test_parallel_merge_consistent）
- cargo clippy --all-targets -- -D warnings 零警告
- cargo fmt --check 通过
- processor.rs::normalize_and_export 函数体 ≤40 行
- sqlite_parallel.rs::parallel_collect 函数体 ≤40 行
- update_params_buffer_only / run_parallel_parse 两个新私有辅助函数存在且被调用
</verification>

<success_criteria>
- VERIFICATION gap 1 关闭：normalize_and_export 函数体 ≤40 行
- VERIFICATION gap 2 关闭：parallel_collect 函数体 ≤40 行
- STRUCT-01 完全满足：src/cli/run/ 下所有函数体不超过 40 行（含已文档化豁免）
- ROADMAP SC-1 完全满足
- 行为完全不变：638 项测试全部通过，无回归
</success_criteria>

<output>
Create `.planning/phases/59-cli-run-exporter-pipeline/59-06-SUMMARY.md` when done
</output>
