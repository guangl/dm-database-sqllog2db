---
phase: 21-readme
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - docs/images/frequency_bar.png
  - docs/images/latency_histogram.png
autonomous: true
requirements:
  - DOC-01
  - DOC-02
  - DOC-03
  - DOC-04
  - DOC-07
  - DOC-08
  - DOC-09
user_setup: []

must_haves:
  truths:
    - "User can read v1.3 template analysis (normalize_template, TemplateAggregator, dual-stat output) in README"
    - "User can read v1.4 nested config model ([filter.include]/[filter.exclude], [template], [charts] sub-tables) in README"
    - "User can copy-paste 3 QuickStart commands (init/validate/run) and execute them"
    - "README displays a Mermaid architecture diagram (GitHub-rendered)"
    - "README has 4-6 badges at top (CI, crates.io, license, release)"
    - "Config code block in README matches actual sqllog2db init output"
    - "README has no bare links to CONTRIBUTING.md, SECURITY.md, or docs/architecture.md"
    - "README displays 1-2 embedded PNG screenshots of representative SVG charts generated from real logs (frequency bar + latency histogram)"
    - "Links to docs/quickstart.md and docs/config-reference.md are annotated with '(Coming in Phase 23)'"
    - "Remaining chart types (trend line, user pie) are linked to the Pages Gallery"
    - "README length is between 200-250 lines, pure English"
  artifacts:
    - path: "README.md"
      provides: "Complete project README (pure English, 200-250 lines)"
      min_lines: 200
      max_lines: 250
    - path: "docs/images/frequency_bar.png"
      provides: "PNG screenshot of top-N frequency bar chart generated from sample logs"
      min_lines: 0
    - path: "docs/images/latency_histogram.png"
      provides: "PNG screenshot of latency histogram generated from sample logs"
      min_lines: 0
  key_links:
    - from: "README.md"
      to: "CLI"
      via: "code blocks with shell commands"
      pattern: "```bash"
    - from: "README.md"
      to: "docs/quickstart.md"
      via: "hyperlink with status marker"
      pattern: "Coming in Phase 23"
    - from: "README.md"
      to: "docs/config-reference.md"
      via: "hyperlink with status marker"
      pattern: "Coming in Phase 23"
    - from: "README.md"
      to: "docs/images/frequency_bar.png"
      via: "markdown image embed"
      pattern: "!\\[.*\\]\\(docs/images/frequency_bar\\.png\\)"
    - from: "README.md"
      to: "docs/images/latency_histogram.png"
      via: "markdown image embed"
      pattern: "!\\[.*\\]\\(docs/images/latency_histogram\\.png\\)"
---

<objective>
重写 README.md 为 200-250 行纯英文最小骨架，覆盖 v1.3/v1.4 全部功能特性。遵循 D-01 至 D-10、D-15 的所有用户决策。

Purpose: 当前 README 395 行中英文混合，内容膨胀且缺少 v1.3 模板分析/SVG 图表和 v1.4 嵌套配置的文档。需精简为纯英文参考源，与 Phase 22 落地页互补（Pages 侧重可视化展示，README 是文字参考源）。
Output: 重写后的 README.md（200-250 行），docs/images/frequency_bar.png，docs/images/latency_histogram.png
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>

@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/STACK.md

<interfaces>
<!-- README links to external references -- no code interfaces needed -->
<!-- Key project metadata for README badges and version info -->

From Cargo.toml:
```
name = "dm-database-sqllog2db"
version = "1.4.0"
rust-version = "1.85"
license = "Apache-2.0"
repository = "https://github.com/guangl/sqllog2db"
homepage = "https://github.com/guangl/sqllog2db"
```

Current `sqllog2db init` config output (v1.4 nested format):
Top-level sections: [sqllog], [logging], [replace_parameters], [template], [filter], [filter.include], [filter.exclude], [filter.indicators], [filter.sql], [charts], [resume], [exporter.csv], [exporter.sqlite]

Benchmark data (from PROJECT.md):
- CSV synthetic (criterion): ~5.2M records/sec
- Real file 1.1GB (~3M records, NVMe SSD): ~1.55M records/sec

Chart generation config (per src/charts/mod.rs):
- Charts are generated via `sqllog2db run` when `[template].enable = true` and `[charts]` section is configured
- Output: `top_n_frequency.svg`, `latency_histogram_{key}.svg` (one per template), `frequency_trend.svg`, `user_schema_pie.svg`
- Use ImageMagick (`magick convert`) for SVG-to-PNG conversion on macOS (available at /opt/homebrew/bin/convert)
</interfaces>

</context>

<tasks>

<task type="auto">
  <name>Task 1: Write README header through feature overview</name>
  <files>README.md</files>
  <read_first>
    - README.md (the current file, for reference of existing badges and content to compress)
    - .planning/PROJECT.md (for architecture, core value, and performance data)
    - .planning/codebase/STACK.md (for tech stack description)
    - Cargo.toml (for crate name, version, license)
  </read_first>
  <action>
    重写 README.md 的第一部分（约 100-120 行）：

    1. **项目简介**（标题 + 1-2 段纯英文）：
       - 标题：`# sqllog2db`（去掉 emoji 火箭）
       - 一句话描述："Parse Dameng (达梦) database SQL logs and export to CSV or SQLite."
       - 核心特点 bullet points：streaming (constant memory), ~5.2M records/sec CSV throughput, zero-dependency binary
       - 适用场景：log archiving, audit, analytics preprocessing

    2. **Badges**（6 个，顺序保留但移除 downloads badge，或保持 6 个）：
       - crates.io、CI、License、Release、Rust 1.85+、Downloads（全部 flat-square 风格）
       - 使用 shields.io 动态 badge 语法。crates.io badge: `https://img.shields.io/crates/v/dm-database-sqllog2db`
       - License badge: `https://img.shields.io/badge/license-Apache--2.0-blue`

    3. **Feature overview**（按领域分组，每领域 2-4 bullet，纯英文）：
       - **Parsing & Export**: streaming parser, CSV (buffered + itoa zero-alloc) and SQLite (batch + PRAGMA) exporters
       - **Filtering & Field Control**: record-level include/exclude filters (AND + OR-veto), transaction-level indicator/sql filters, field projection via `ordered_indices`
       - **Template Analysis & Charts**: SQL fingerprint normalization (`normalize_template`), `TemplateAggregator` streaming statistics, dual output (CSV table + SQLite table), 4 chart types (frequency bar, latency histogram, trend line, user pie)
       - **Configuration & Performance**: TOML config with nested sub-tables (`[filter.include]`, `[template]`, `[charts]`), zero-overhead fast path when pipeline is empty, single-threaded streaming

    4. **Installation**: `cargo install dm-database-sqllog2db`（推荐），附注：Rust 1.85+ required，二进制约 5MB
  </action>
  <verify>
    <automated>wc -l README.md && grep -c "crates.io\|license\|CI\|Release\|Rust" README.md</automated>
  </verify>
  <acceptance_criteria>
    - README.md exists and has at least 100 non-empty lines of pure English text
    - README starts with "# sqllog2db" (no emoji in title per D-02 pure English)
    - Feature overview groups features into 4 categories: Parsing & Export, Filtering & Field Control, Template Analysis & Charts, Configuration & Performance (per D-03)
    - Feature overview mentions both v1.3 features (normalize_template, TemplateAggregator, 4 chart types) and v1.4 features (nested [filter.include]/[filter.exclude]/[template]/[charts] sub-tables)
    - Installation includes `cargo install dm-database-sqllog2db` command
    - At least 4 badges present at top of README
    - No Chinese text present in the README
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Generate chart screenshots, then write QuickStart, config, architecture diagram, link index, and embed images</name>
  <files>README.md, docs/images/frequency_bar.png, docs/images/latency_histogram.png</files>
  <read_first>
    - README.md (the state after Task 1—the header section just written)
    - .planning/PROJECT.md (for benchmark data and architecture data flow)
    - .planning/ROADMAP.md (for understanding what Phase 22/23 deliver)
    - src/charts/mod.rs (for understanding chart generation: output filenames, configuration)
  </read_first>
  <action>
    **Step A: Generate representative SVG charts from sample log data (per D-07)**

    1. 创建临时配置文件 `/tmp/chart-gen-config.toml`：
       ```toml
       [sqllog]
       path = "sqllogs"

       [template]
       enable = true

       [charts]
       output_dir = "charts/"
       top_n = 10
       frequency_bar = true
       latency_hist = true
       trend_line = false
       user_pie = false

       [exporter.csv]
       file = "/dev/null"
       overwrite = true
       ```

    2. 运行 chart 生成：
       ```bash
       cargo run -- run -c /tmp/chart-gen-config.toml
       ```
       这会解析 sqllogs/ 下的 3 个示例 DM 日志文件（~4.5M 总行数），执行模板聚合，生成：
       - `charts/top_n_frequency.svg` （频率柱状图）
       - `charts/latency_histogram_*.svg` （延迟直方图，每模板一张）

    3. 创建 `docs/images/` 目录，将 SVG 转换为 PNG：
       ```bash
       mkdir -p docs/images
       # 频率柱状图：最具代表性的图表
       /opt/homebrew/bin/convert charts/top_n_frequency.svg docs/images/frequency_bar.png

       # 延迟直方图：选第一个模板的直方图
       FIRST_HIST=$(ls charts/latency_histogram_*.svg 2>/dev/null | head -1)
       if [ -n "$FIRST_HIST" ]; then
         /opt/homebrew/bin/convert "$FIRST_HIST" docs/images/latency_histogram.png
       fi
       ```

    4. 清理临时文件：
       ```bash
       rm -rf /tmp/chart-gen-config.toml charts/
       ```

    **Step B: 在 README.md 已有内容后追加以下部分（确保总行数在 200-250 行之间）**

    1. **QuickStart**（3 个核心命令，代码块格式，纯英文说明。per D-06）：
       ```
       # Generate default config
       sqllog2db init -o config.toml

       # Validate config
       sqllog2db validate -c config.toml

       # Run export
       sqllog2db run -c config.toml
       ```
       然后用一句话提及 stats 和 digest："For per-file statistics, slow-query ranking, and SQL fingerprint aggregation, see `sqllog2db stats --help` and `sqllog2db digest --help`, or the QuickStart guide (Coming in Phase 23)."

    2. **TOML config example**（per D-04, 5-10 行核心片段）：
       ```toml
       [sqllog]
       path = "sqllogs"

       [exporter.csv]
       file = "outputs/sqllog.csv"
       ```
       附注："Full reference: docs/config-reference.md (Coming in Phase 23)"
       注意：此配置片段必须与 `sqllog2db init` 实际输出的格式一致（当前 v1.4 格式：`[filter]` 为顶层，非嵌套在 `[features]` 下）

    3. **Architecture diagram**（Mermaid.js，标题 "Data Flow"）：
       ```
       ```mermaid
       graph LR
           A[SQL Log Files] --> B[SqllogParser]
           B --> C{Pipeline}
           C -->|empty| D[ExporterManager]
           C -->|filters| E[FilterProcessor]
           E --> D
           D --> F[CSV / SQLite]
       ```
       ```
       per D-05

    4. **Performance section**：
       Create a table with:
       | Mode | Throughput | Notes |
       | CSV (synthetic benchmark) | ~5.2M rec/s | criterion, M-series |
       | SQLite (synthetic) | ~1.1M rec/s | batch + PRAGMA |
       | Real file (1.1 GB, NVMe) | ~1.55M rec/s | ~3M records |

    5. **Link index**（per D-09, D-10, D-15）：
       列出关键链接，对非存在文档标注状态：
       - [GitHub Repository](https://github.com/guangl/sqllog2db)
       - [crates.io](https://crates.io/crates/dm-database-sqllog2db)
       - [Releases](https://github.com/guangl/sqllog2db/releases)
       - [CHANGELOG](./CHANGELOG.md)
       - [QuickStart Guide](./docs/quickstart.md) *(Coming in Phase 23)*
       - [Config Reference](./docs/config-reference.md) *(Coming in Phase 23)*
       - [Contributing Guide](./CONTRIBUTING.md) *(Coming v1.6)*
       - [Security Policy](./SECURITY.md) *(Coming v1.6)*
       - [Architecture Documentation](./docs/architecture.md) *(Coming v1.6)*

    6. **Embedded Chart Screenshots**（per D-07）：
       Insert a "### SVG Charts" subsection with the two generated PNG screenshots:

       ```
       ### SVG Charts

       Two sample charts generated from real DaMeng SQL logs:

       ![Frequency Bar Chart](docs/images/frequency_bar.png)
       *Top-N SQL frequency bar chart — most frequent query templates by occurrence count*

       ![Latency Histogram](docs/images/latency_histogram.png)
       *Latency histogram — execution time distribution for a selected query template*
       ```

       Then add a paragraph explaining all 4 chart types are available and link remaining types to Pages Gallery:
       "All four chart types (frequency bar, latency histogram, trend line, user pie) are available. For additional chart samples from different datasets, see the [Gallery](https://guangl.github.io/sqllog2db/) on the project landing page (Phase 22)."

       Image file paths must be relative from repository root: `docs/images/frequency_bar.png` and `docs/images/latency_histogram.png`. GitHub renders relative PNG paths in README correctly.

    7. **License footer**：Apache-2.0，链接到 LICENSE 文件

    完成追加后，用 `wc -l README.md` 确认行数在 200-250 之间。如果超过 250 行，精简 QuickStart 说明文字和性能备注。
  </action>
  <verify>
    <automated>
      wc -l README.md | awk '$1 >= 200 && $1 <= 250 {print "OK: " $1 " lines"} $1 < 200 {print "LINES-TOO-FEW: " $1} $1 > 250 {print "LINES-TOO-MANY: " $1}' &&
      grep -c "Coming in Phase 23\|Coming v1.6" README.md &&
      grep -c "\`\`\`mermaid" README.md &&
      grep -c "5\.2M\|1\.55M\|1\.1M" README.md &&
      test -f docs/images/frequency_bar.png && echo "PNG1-OK" || echo "PNG1-MISSING" &&
      test -f docs/images/latency_histogram.png && echo "PNG2-OK" || echo "PNG2-MISSING" &&
      grep -c "docs/images/frequency_bar.png" README.md &&
      grep -c "docs/images/latency_histogram.png" README.md
    </automated>
  </verify>
  <acceptance_criteria>
    - README.md total lines between 200 and 250 (inclusive)
    - Contains Mermaid architecture diagram (detectable by ````mermaid```` marker)
    - Contains 3 QuickStart commands: `sqllog2db init`, `sqllog2db validate`, `sqllog2db run`
    - Contains TOML config snippet with `[sqllog]` and `[exporter.csv]` sections matching v1.4 nested format
    - Contains performance table with CSV synthetic ~5.2M rec/s and real file ~1.55M rec/s
    - Contains link index with "(Coming in Phase 23)" and "(Coming v1.6)" status markers
    - Contains "Apache-2.0" license reference
    - No bare links to CONTRIBUTING.md, SECURITY.md, docs/architecture.md without status markers per D-09
    - **docs/images/frequency_bar.png exists and is a valid PNG file (per D-07)**
    - **docs/images/latency_histogram.png exists and is a valid PNG file (per D-07)**
    - **README displays exactly 2 embedded PNG images with relative paths `docs/images/frequency_bar.png` and `docs/images/latency_histogram.png` (per D-07)**
    - **README links remaining chart types (trend line, user pie) to the Pages Gallery URL**
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Verify README consistency, image validity, and fix discrepancies</name>
  <files>README.md</files>
  <read_first>
    - README.md (the complete file after Tasks 1 and 2)
  </read_first>
  <action>
    Perform automated consistency checks and fix any issues found:

    1. Run `cargo run -- init -o /tmp/verify-config.toml --force 2>/dev/null` and read the output. Compare the config section in README.md against the actual init output. If the README config snippet doesn't match the actual init format (e.g. `[filter]` should be a top-level section, not under `[features]`), fix it.

    2. Verify README line count is 200-250. If outside range, trim or expand:
       - If >250: remove verbose descriptions, merge redundant bullet points
       - If <200: add brief subsection about SVG charts or expand performance table notes

    3. Verify no Chinese characters remain: `grep -cPn '[\x{4e00}-\x{9fff}]' README.md` should return 0 matches. If Chinese found, replace each instance with English equivalent:
       - "达梦" → "Dameng"
       - "性能" → "Performance/throughput"
       - Check all headings are in English

    4. Verify all links in the link index section:
       - For each `./docs/` or `./CONTRIBUTING.md` or `./SECURITY.md` link, verify it has "(Coming v1.6)" or "(Coming in Phase 23)" annotation
       - `./LICENSE` and `./CHANGELOG.md` links should NOT have status markers (they exist)
       - Badge URLs are all valid shields.io format

    5. Confirm feature overview mentions all required topics:
       - v1.3: template normalization (normalize_template or fingerprint), TemplateAggregator, dual-stat output
       - v1.4: nested config sub-tables ([filter.include], [filter.exclude], [template], [charts])
       - SVG chart types: frequency bar, latency histogram, trend line, user pie

    6. Verify PNG images are valid (not corrupted):
       ```bash
       file docs/images/frequency_bar.png | grep -q "PNG image data" || echo "WARNING: frequency_bar.png is NOT a valid PNG"
       file docs/images/latency_histogram.png | grep -q "PNG image data" || echo "WARNING: latency_histogram.png is NOT a valid PNG"
       ```

    7. Run `cargo clippy --all-targets -- -D warnings` and `cargo test` to ensure no code regressions (the README should not affect compilation, but verify).

    Fix any discrepancies found with targeted edits.
  </action>
  <verify>
    <automated>
      wc -l README.md &&
      grep -cP '[\x{4e00}-\x{9fff}]' README.md | awk '$1 > 0 {print "Chinese chars found: " $1; exit 1} {print "OK: no Chinese chars"}' &&
      cargo run -- init -o /tmp/verify-trial.toml --force 2>/dev/null && diff &lt;(head -5 /tmp/verify-trial.toml) &lt;(grep -A 10 '\`\`\`toml' README.md | head -10) 2>/dev/null; echo "Config check: done" &&
      file docs/images/frequency_bar.png | grep -q "PNG image data" && echo "PNG1-VALID" || echo "PNG1-INVALID" &&
      file docs/images/latency_histogram.png | grep -q "PNG image data" && echo "PNG2-VALID" || echo "PNG2-INVALID" &&
      cargo clippy --all-targets -- -D warnings &&
      cargo test --quiet 2>&1 | tail -3
    </automated>
  </verify>
  <acceptance_criteria>
    - No Chinese characters remain in README.md (grep for Chinese Unicode range returns 0)
    - README.md line count is exactly 200-250 and verified
    - Config TOML snippet matches the actual `sqllog2db init` output format (top-level [filter] section, not [features.filter])
    - Every non-existent file link has "(Coming v1.6)" or "(Coming in Phase 23)" annotation
    - Feature overview covers all required v1.3 and v1.4 topics
    - Both PNG files are valid PNG image data (per D-07)
    - `cargo clippy --all-targets -- -D warnings` passes with no new warnings
    - `cargo test` passes without regressions
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan modifies only documentation (README.md) and generates chart PNGs from sample log data. No code changes, no network endpoints, no data processing boundaries are introduced.

| Boundary | Description |
|----------|-------------|
| README → External URLs | Badge URLs and link index reference external services (shields.io, crates.io, GitHub) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-01 | Tampering | shields.io badge URLs | accept | Badge images loaded from third-party CDN; no sensitive data involved |
| T-21-02 | Information Disclosure | README code examples | accept | Code snippets show public CLI usage patterns; no secrets or credentials embedded |
| T-21-03 | Information Disclosure | docs/images/ charts | accept | Chart images generated from sample log data; contains no credentials or sensitive data |
| T-21-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this documentation-only phase |
</threat_model>

<verification>
- README.md rewritten as pure English, 200-250 lines
- All 6 badges present and valid
- Feature overview covers v1.3 template analysis and v1.4 nested config
- Mermaid architecture diagram renders on GitHub
- Config snippet matches `sqllog2db init` output
- QuickStart has 3 core commands (init/validate/run)
- Performance table has current benchmark data
- Link index has status markers for deferred docs
- **docs/images/frequency_bar.png and docs/images/latency_histogram.png exist and are valid PNG files (per D-07)**
- **README embeds both PNG screenshots with descriptive captions and links remaining chart types to Pages Gallery**
- No broken links or bare references to non-existent files
- `cargo test` passes, `cargo clippy` passes with no new warnings
</verification>

<success_criteria>
1. README is 200-250 lines of pure English
2. README contains 4-6 valid shields.io badges
3. Feature overview groups features by domain (parsing/export, filtering/field control, template analysis/charts, config/performance)
4. README includes v1.3 template analysis description (normalize_template, TemplateAggregator, dual output)
5. README includes v1.4 nested config description ([filter.include]/[filter.exclude], [template], [charts] as top-level TOML sub-tables)
6. Config code block matches actual `sqllog2db init` output format
7. QuickStart section has 3 copy-paste commands (init/validate/run)
8. **docs/images/frequency_bar.png exists — PNG screenshot of frequency bar chart generated from sample logs (per D-07)**
9. **docs/images/latency_histogram.png exists — PNG screenshot of latency histogram generated from sample logs (per D-07)**
10. **README embeds both PNGs with descriptive captions in a "### SVG Charts" subsection (per D-07)**
11. **README links remaining chart types (trend line, user pie) to Pages Gallery URL (per D-07)**
12. Mermaid.js architecture diagram present
13. Links to docs/quickstart.md and docs/config-reference.md marked "(Coming in Phase 23)"
14. Links to CONTRIBUTING.md, SECURITY.md, docs/architecture.md marked "(Coming v1.6)"
15. `cargo test` passes, `cargo clippy --all-targets -- -D warnings` passes
</success_criteria>

<output>
Create `.planning/phases/21-readme/21-01-SUMMARY.md` when done
</output>
