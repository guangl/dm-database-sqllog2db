---
phase: 42-criterion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - benches/bench_parser.rs
  - Cargo.toml
  - benches/BENCHMARKS.md
autonomous: true
requirements:
  - BENCH-01
must_haves:
  truths:
    - "cargo bench --bench bench_parser --no-run 编译通过，无任何 warning"
    - "cargo bench --bench bench_parser 实际运行成功，输出 parser_throughput group 三个规模（1000/10000/50000）的 records/sec 指标"
    - "cargo bench（不指定 --bench）运行四个 bench 文件均成功，BENCH-01 四大场景齐全"
    - "cargo clippy --all-targets -- -D warnings 通过，bench_parser.rs 无警告"
    - "benches/BENCHMARKS.md 包含 Phase 42 baseline 段落，列出三个规模的 median time 与 throughput"
  artifacts:
    - path: "benches/bench_parser.rs"
      provides: "parser 原始解析速度 benchmark，覆盖 BENCH-01 第四场景"
      contains: "parser_throughput"
    - path: "Cargo.toml"
      provides: "bench_parser 注册条目"
      contains: "name = \"bench_parser\""
    - path: "benches/BENCHMARKS.md"
      provides: "Phase 42 parser baseline 数据归档"
      contains: "Phase 42"
  key_links:
    - from: "benches/bench_parser.rs"
      to: "dm_database_parser_sqllog::LogParserBuilder"
      via: "LogParserBuilder::new(path).build()"
      pattern: "LogParserBuilder::new"
    - from: "Cargo.toml"
      to: "benches/bench_parser.rs"
      via: "[[bench]] name 与文件名对应"
      pattern: "name = \"bench_parser\""
---

<objective>
新增 `benches/bench_parser.rs` 文件，补全 BENCH-01 的第四个场景（parser 原始解析速度），使 criterion benchmark 套件完整覆盖 CSV 导出、SQLite 导出、filter 启用/禁用、parser 原始解析四大场景。同步在 `Cargo.toml` 中注册 `[[bench]]` 条目，在 `benches/BENCHMARKS.md` 追加 Phase 42 baseline 数据。

Purpose: 解锁后续 Phase 44（热路径优化）、Phase 45（CI 基准集成）所依赖的"可量化对比 parser 性能"基础设施。当前三个 bench 文件（bench_csv/bench_sqlite/bench_filters）测的都是含 CSV/SQLite 导出或 filter 处理的全链路，无法单独量化 parser 库本身的解析速度，Phase 44 优化 parser 调用模式后无对比基线。
Output:
- 新文件 `benches/bench_parser.rs`
- `Cargo.toml` 新增一个 `[[bench]]` 条目
- `benches/BENCHMARKS.md` 追加 "Phase 42 — Parser baseline" 段落
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/42-criterion/42-CONTEXT.md
@.planning/phases/42-criterion/42-RESEARCH.md
@benches/bench_csv.rs
@benches/bench_filters.rs
@benches/BENCHMARKS.md
@Cargo.toml

<interfaces>
<!-- 关键 API 与约定，executor 无需再去探索代码库 -->

From `dm-database-parser-sqllog` 2.0.0（依赖已锁定，src/exporter 多处已使用）：
- `LogParserBuilder::new(path: &str) -> LogParserBuilder` — 构造器入口
- `LogParserBuilder::build(self) -> Result<LogParser, _>` — 终结方法，返回解析器
- `LogParser::iter(&self) -> impl Iterator<Item = Result<Sqllog, _>>` — 迭代器接口

From `criterion` 0.7（已在 dev-dependencies）：
- `Criterion::benchmark_group(name: &str) -> BenchmarkGroup` — group 命名将作为 `benches/baselines/{name}/` 目录名（D-07）
- `BenchmarkGroup::throughput(Throughput::Elements(N))` — 每 group 必须设置（D-06）
- `BenchmarkGroup::bench_with_input(BenchmarkId::from_parameter(n), &input, |b, input| b.iter(|| ...))` — 多规模标准模式
- `criterion_group!(benches, fn1)` / `criterion_main!(benches)` — 文件末尾注册宏

From `benches/bench_csv.rs`（已存在并编译通过，模仿对象）：
- 函数 `synthetic_log(record_count: usize) -> String` — 生成约 170 bytes/record 达梦格式日志
- 目录约定 `target/bench_xxx/sqllogs/` — 由 `fs::create_dir_all` 创建，bench 文件直接写
- 末尾宏 `criterion_group!(benches, bench_xxx_export, bench_xxx_real_file, bench_xxx_format_only)` + `criterion_main!(benches)`

From `Cargo.toml` 现有 `[[bench]]` 条目（行 89-99）：
```
[[bench]]
name = "bench_csv"
harness = false

[[bench]]
name = "bench_sqlite"
harness = false

[[bench]]
name = "bench_filters"
harness = false
```
新增条目格式必须严格对称。
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 新增 benches/bench_parser.rs 并实现 parser_throughput benchmark</name>
  <files>benches/bench_parser.rs</files>
  <read_first>
    - benches/bench_csv.rs（必读：完整阅读 synthetic_log 函数、bench_csv_export 函数体、criterion_group! / criterion_main! 宏使用、文件顶部的 use 声明顺序）
    - .planning/phases/42-criterion/42-RESEARCH.md §"Code Examples" 中 "bench_parser.rs 完整骨架" 段落（提供推荐实现）
    - .planning/phases/42-criterion/42-CONTEXT.md §"Implementation Decisions" D-01 到 D-07
    - CLAUDE.md 中"Keep functions under 40 lines"约束
  </read_first>
  <behavior>
    - bench_parser.rs 编译通过：`cargo bench --bench bench_parser --no-run` 无错误且无 warning
    - 文件包含独立的 synthetic_log 函数（与 bench_csv.rs 中实现完全一致），不内联到 bench 函数体内
    - 文件包含一个 bench 函数 `bench_parser_throughput(c: &mut Criterion)`，函数体 ≤ 40 行
    - bench 函数注册 group 名为 "parser_throughput"（纯小写下划线，对齐 benches/baselines 目录命名约定）
    - 三个规模 n ∈ {1_000, 10_000, 50_000}，每个 n 调用 `group.throughput(Throughput::Elements(n as u64))` 和 `group.bench_with_input(BenchmarkId::from_parameter(n), &log_path, ...)`
    - benchmark 闭包：`b.iter(|| { let parser = LogParserBuilder::new(path.to_str().unwrap()).build().unwrap(); parser.iter().filter_map(|r| r.ok()).count() })` —— 每次 iter 重建 parser，测全链路（mmap + 解析）
    - 文件末尾 `criterion_group!(benches, bench_parser_throughput);` + `criterion_main!(benches);`
    - 中间结果文件写入 `target/bench_parser/sqllogs/bench.log`（与 bench_csv 风格一致，固定 target/ 路径而非 tempfile）
    - 写文件操作在 bench 函数体的 for 循环内但 b.iter 闭包外（D-RESEARCH Anti-Pattern：不在 iter 内 fs::write）
  </behavior>
  <action>
    创建新文件 benches/bench_parser.rs。实现严格参照 42-RESEARCH.md §"Code Examples" 中的 "bench_parser.rs 完整骨架" 段落（行 277-327）。落实 D-01（新增独立 bench 文件）、D-04（合成数据不依赖外部文件）、D-05（synthetic_log 格式 170 bytes/record）、D-06（Throughput::Elements）。group 命名使用 "parser_throughput"（per Pitfall 2：纯小写下划线，避免 baselines 目录路径异常）。规模数组按 RESEARCH Discretion 推荐选择 [1_000, 10_000, 50_000]，与 bench_csv/bench_sqlite 的 csv_export/sqlite_export group 规模对齐，便于跨场景对比。使用 `filter_map(|r| r.ok())` 而非裸 `count()`（per Anti-Pattern：避免把 ParseError 计入）。synthetic_log 拆为独立函数（per Pitfall 3：bench 主函数 ≤40 行）。导入顺序：`use criterion::{...}; use dm_database_parser_sqllog::LogParserBuilder; use std::fs; use std::path::PathBuf;`（对齐 bench_csv.rs 风格）。不要使用 black_box（criterion 自动处理返回值，per Pitfall 4）。
  </action>
  <verify>
    <automated>cargo bench --bench bench_parser --no-run 2>&amp;1 | tee /tmp/bench_parser_compile.log; grep -q "warning:" /tmp/bench_parser_compile.log &amp;&amp; exit 1; grep -qE "error\[E[0-9]+\]" /tmp/bench_parser_compile.log &amp;&amp; exit 1; exit 0</automated>
  </verify>
  <acceptance_criteria>
    - 文件 benches/bench_parser.rs 存在且包含 `fn synthetic_log(`、`fn bench_parser_throughput(`、`criterion_group!`、`criterion_main!` 四个标志
    - `grep -c '"parser_throughput"' benches/bench_parser.rs` == 1
    - `grep -c 'LogParserBuilder::new' benches/bench_parser.rs` == 1
    - `grep -c 'Throughput::Elements' benches/bench_parser.rs` == 1
    - `awk '/^fn bench_parser_throughput/,/^}$/' benches/bench_parser.rs | wc -l` ≤ 42（函数体 ≤40 行 + 函数头/尾 2 行）
    - `cargo bench --bench bench_parser --no-run` 退出码 0，输出无 `warning:` 也无 `error[`
  </acceptance_criteria>
  <done>
    bench_parser.rs 已创建并通过编译，函数长度符合 CLAUDE.md 40 行约束，使用 RESEARCH 指定的 group 名和规模。
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: 在 Cargo.toml 注册 bench_parser 条目并验证四套 bench 联动可运行</name>
  <files>Cargo.toml</files>
  <read_first>
    - Cargo.toml 第 89-99 行（现有 [[bench]] 块的格式）
    - .planning/phases/42-criterion/42-CONTEXT.md D-02
  </read_first>
  <behavior>
    - Cargo.toml 包含第四个 `[[bench]]` 条目：`name = "bench_parser"` + `harness = false`
    - 新条目格式与既有三个条目完全对称（每个条目两行配置，块之间空行分隔）
    - `cargo bench --no-run` 顺序编译四个 bench 文件全部成功，无 warning
    - 不修改 [dependencies] / [dev-dependencies]（per RESEARCH："无需新增任何依赖"）
  </behavior>
  <action>
    在 Cargo.toml 中找到 `[[bench]] name = "bench_filters"` 条目末尾（约第 99 行），追加空行 + 新的 `[[bench]]` 块：`name = "bench_parser"` + `harness = false`。落实 D-02（同步注册）。不要重排现有三个条目；新条目必须放在 bench_filters 之后保持时间顺序。
  </action>
  <verify>
    <automated>grep -A1 'name = "bench_parser"' Cargo.toml | grep -q 'harness = false' &amp;&amp; cargo bench --no-run 2>&amp;1 | tee /tmp/bench_all_compile.log; grep -v '^#' /tmp/bench_all_compile.log | grep -qE '^warning:' &amp;&amp; exit 1; grep -qE 'error\[E[0-9]+\]' /tmp/bench_all_compile.log &amp;&amp; exit 1; exit 0</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^\[\[bench\]\]$' Cargo.toml` == 4
    - `grep -c 'name = "bench_parser"' Cargo.toml` == 1
    - `cargo bench --no-run` 退出码 0
    - cargo bench --bench bench_parser -- --quick（可选烟囱测试，限 30s）能产出 parser_throughput/1000、parser_throughput/10000、parser_throughput/50000 三个 BenchmarkId 输出
    - `cargo clippy --all-targets -- -D warnings` 退出码 0（含 benches/）
  </acceptance_criteria>
  <done>
    Cargo.toml 注册完毕，四个 bench 文件均能通过 cargo bench --no-run 编译，clippy 净化。
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: 实测采集 parser_throughput baseline 并在 BENCHMARKS.md 追加 Phase 42 段落</name>
  <files>benches/BENCHMARKS.md</files>
  <read_first>
    - benches/BENCHMARKS.md 全文（找到现有"Phase 10"段落末尾作为追加位置）
    - .planning/phases/42-criterion/42-CONTEXT.md D-07（baseline 管理 CRITERION_HOME=benches/baselines 约定）
  </read_first>
  <behavior>
    - 实际运行 `CRITERION_HOME=benches/baselines cargo bench --bench bench_parser -- --save-baseline v1.0` 采集 parser baseline 并落地 JSON 到 benches/baselines/parser_throughput/
    - benches/BENCHMARKS.md 文件末尾追加 "## Phase 42 — Parser baseline（v1.11）" 段落
    - 段落包含至少：日期、test environment、三个规模的实测 median time + throughput 表格、criterion 原文输出（用 details/summary 折叠）
    - 表格列：Records / Median time / Throughput
    - 文档表格数值来自本任务实际运行结果（不要使用 RESEARCH 中的占位估算）
    - 文档末尾包含 "本 baseline 用于 Phase 44 热路径优化对比基线" 一句说明用途
  </behavior>
  <action>
    Step 1：运行 `CRITERION_HOME=benches/baselines cargo bench --bench bench_parser -- --save-baseline v1.0`，保存原始输出到临时文件。Step 2：解析三个 BenchmarkId（1000/10000/50000）的 `time: [low median high]` 和 `thrpt: [low median high]`，提取 median 数值。Step 3：使用 Edit 工具在 benches/BENCHMARKS.md 文件末尾追加新段落，格式严格参照已存在的 "## Phase 4 — CSV 性能优化（v1.1）" / "## Phase 5 — SQLite 性能优化" 段落的结构（标题、Date、Goal、Test environment、表格、Criterion 输出原文 details/summary、结论列表）。落实 D-07（baseline 管理）。题头使用 "## Phase 42 — Parser baseline（v1.11）"。如果 cargo bench 实际运行超过 8 分钟（criterion 默认配置 + 三规模 + warmup），允许使用 `--profile-time 5` 缩短采集时间但需在文档中明确标注 "采样时间缩短为 5s/scenario"。
  </action>
  <verify>
    <automated>grep -c "^## Phase 42 — Parser baseline" benches/BENCHMARKS.md | grep -q '^1$' &amp;&amp; grep -c "parser_throughput/1000\|parser_throughput/10000\|parser_throughput/50000" benches/BENCHMARKS.md | awk '$1 >= 3 {exit 0} {exit 1}' &amp;&amp; ls benches/baselines/parser_throughput/ 2>/dev/null | grep -q . &amp;&amp; exit 0 || exit 1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^## Phase 42 — Parser baseline" benches/BENCHMARKS.md` == 1
    - `grep -E "parser_throughput/(1000|10000|50000)" benches/BENCHMARKS.md | wc -l` ≥ 3
    - 目录 benches/baselines/parser_throughput/ 存在且非空（v1.0 子目录包含 estimates.json 等 criterion 标准产物）
    - 新追加段落包含 "Median time"、"Throughput" 字面的表格列名
    - cargo fmt --check 通过（仅文档变更不应触发 Rust fmt，但作为整体质量门禁验证）
    - cargo clippy --all-targets -- -D warnings 退出码 0
  </acceptance_criteria>
  <done>
    Phase 42 baseline 数据已采集落地（benches/baselines/parser_throughput/v1.0/），BENCHMARKS.md 包含完整 Phase 42 段落，为 Phase 44 性能对比建立可信基线。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| benchmark → 本地 target/ 目录 | 仅写入 target/bench_parser/ 临时数据，不读取外部输入 |
| benchmark → benches/baselines/ | criterion 写入 JSON 统计数据，源数据来自合成日志 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-01 | Tampering | target/bench_parser/sqllogs/bench.log | accept | 文件由 bench 函数每次运行覆盖写入，攻击者篡改无意义（下一次 bench 立即重写） |
| T-42-02 | Denial of Service | bench 运行时长 | accept | 三规模合计 ≤ 200K records，实测预期 < 2 分钟，远低于 CI 超时阈值 |
| T-42-03 | Information Disclosure | benches/baselines/ JSON | accept | baseline 数据仅含合成日志的解析时间，无敏感信息 |
| T-42-SC | Tampering | 包依赖（criterion/dm-database-parser-sqllog/tempfile） | mitigate | RESEARCH §"Package Legitimacy Audit" 明确：本 Phase 不新增任何外部包依赖，所有库均已在 Cargo.lock 中锁定使用 |

本 Phase 为纯 benchmark 基础设施，不涉及用户输入、网络、认证、加密等敏感域（per RESEARCH §"Security Domain"）。
</threat_model>

<verification>
- `cargo bench --no-run` 退出码 0，四个 bench 文件全部编译通过，无 warning
- `cargo bench --bench bench_parser` 实际运行成功，输出 parser_throughput group 三个 BenchmarkId 数据
- `cargo clippy --all-targets -- -D warnings` 退出码 0
- `cargo fmt --check` 退出码 0
- `cargo test` 退出码 0（确认 bench 改动未影响主测试）
- benches/baselines/parser_throughput/v1.0/ 目录存在并包含 criterion 标准 JSON 产物
- benches/BENCHMARKS.md 包含 "## Phase 42 — Parser baseline" 段落和三规模数据表
</verification>

<success_criteria>
1. BENCH-01 四大场景齐全：CSV（bench_csv.rs，已有）+ SQLite（bench_sqlite.rs，已有）+ filter 启用/禁用（bench_filters.rs，已有）+ parser 原始解析（bench_parser.rs，本 Plan 新增）
2. `cargo bench` 独立可运行，不依赖任何外部数据文件或环境变量（合成数据由代码内 synthetic_log 生成）
3. parser_throughput group 包含 Throughput::Elements 设置，输出含 records/sec 指标
4. baseline 数据已存档到 benches/baselines/parser_throughput/v1.0/，可被未来 `--baseline v1.0` 对比命令复用
5. 全链路质量门禁通过：cargo build / cargo test / cargo clippy / cargo fmt 全部绿灯，无任何 warning
6. BENCHMARKS.md 包含 Phase 42 段落，提供 Phase 44 性能对比的明文基线参考
</success_criteria>

<output>
完成后创建 `.planning/phases/42-criterion/42-01-SUMMARY.md` 记录：
- 三个规模实测 median time 与 throughput（与 BENCHMARKS.md 表格一致）
- bench_parser.rs 实际代码行数 + bench_parser_throughput 函数体实际行数（验证 ≤40 行约束）
- cargo clippy / fmt / test / bench --no-run 四道门禁结果
- 任何与 RESEARCH 推荐方案的偏离（如有），及偏离原因
</output>
