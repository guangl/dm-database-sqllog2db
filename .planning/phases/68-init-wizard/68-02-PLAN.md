---
phase: 68-init-wizard
plan: 02
type: execute
wave: 2
depends_on:
  - 68-01
files_modified:
  - src/cli/opts.rs
  - src/cli/init.rs
  - src/main.rs
  - tests/integration.rs
autonomous: true
requirements:
  - INIT-01
  - INIT-02
  - INIT-03
must_haves:
  truths:
    - "Commands::Init 新增 interactive: bool 字段，clap 自动识别 --interactive 与 -i 短 flag"
    - "main.rs 分发分支根据 interactive 标志选择 handle_init_interactive 或 handle_init"
    - "handle_init_interactive 调用 run_wizard 收集答案，调用 apply_wizard_answers_to_template 生成内容，调用 write_config_file 写入"
    - "sqllog2db init --interactive 在 stdin 全 Enter 时退出 0 并生成与 sqllog2db init 完全相同的 config.toml（默认 CSV 路径）"
    - "sqllog2db init -i 在向导后通过 stdin 输入自定义 inputs 路径，生成的 config.toml 包含该自定义值且可被 validate 通过"
    - "sqllog2db validate -c <向导生成的文件> 退出 0（SC4）"
  artifacts:
    - path: "src/cli/opts.rs"
      provides: "Commands::Init.interactive bool flag"
      contains: "interactive: bool"
    - path: "src/cli/init.rs"
      provides: "handle_init_interactive public function"
      contains: "pub fn handle_init_interactive"
    - path: "src/main.rs"
      provides: "Init match arm 中的 interactive 分发分支"
      contains: "handle_init_interactive"
    - path: "tests/integration.rs"
      provides: "e2e CLI 测试 (interactive flag + stdin 注入 + validate 通过)"
      contains: "test_cli_init_interactive"
  key_links:
    - from: "Commands::Init"
      to: "main.rs match arm"
      via: "interactive 字段解构"
      pattern: "Init \\{ output, force, interactive \\}"
    - from: "main.rs 分发"
      to: "handle_init_interactive / handle_init"
      via: "if *interactive { ... } else { ... }"
      pattern: "if \\*interactive"
    - from: "handle_init_interactive"
      to: "run_wizard / apply_wizard_answers_to_template / write_config_file"
      via: "顺序调用"
      pattern: "run_wizard.*apply_wizard.*write_config_file"
---

<objective>
将 Plan 01 完成的向导核心通过 CLI 层接入：在 `src/cli/opts.rs` 的 `Commands::Init` variant 新增 `interactive: bool` flag（per D-01）；在 `src/main.rs` 的 Init match arm 增加 if-分支根据 interactive 调用 `handle_init_interactive` 或现有 `handle_init`（per D-03）；在 `src/cli/init.rs` 新增 `pub fn handle_init_interactive(output: &str, force: bool) -> Result<()>` —— 该函数串联 stdin/stdout、`run_wizard`、`apply_wizard_answers_to_template`、`write_config_file`，并在写入完成后输出与 `handle_init` 完全一致的 "Next steps" 三行 info!（per D-07/D-10 + UI-SPEC Completion Output Contract）。

新增 e2e CLI 测试（per D-12 + 测试映射表）：(a) `init --interactive` 全默认输入 → 生成文件 + 退出 0；(b) 自定义 stdin 输入 → 生成文件含自定义值；(c) 向导生成的文件能通过 `validate` 子命令（SC4）。

Purpose: 完成 INIT-01（flag 可达）+ INIT-02（端到端能跑）+ INIT-03（生成文件与非交互式一致）三个 requirement 的 CLI 边界覆盖。
Output: 4 个文件的精确修改 + 3+ 个新 e2e 测试。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/68-init-wizard/68-CONTEXT.md
@.planning/phases/68-init-wizard/68-RESEARCH.md
@.planning/phases/68-init-wizard/68-PATTERNS.md
@.planning/phases/68-init-wizard/68-UI-SPEC.md
@.planning/phases/68-init-wizard/68-01-PLAN.md
@src/cli/opts.rs
@src/main.rs
@src/cli/init.rs
@tests/integration.rs

<interfaces>
<!-- Plan 01 已提供 / Plan 02 待消费 -->

From src/cli/init.rs (Plan 01 已添加):
- pub fn run_wizard(reader: &mut impl BufRead, writer: &mut impl Write) -> Result<WizardAnswers>
- pub enum ExporterChoice { Csv, Sqlite }
- pub struct WizardAnswers { pub inputs, pub exporter, pub csv_file, pub sqlite_db, pub sqlite_table }
- fn apply_wizard_answers_to_template(answers: &WizardAnswers) -> String  (私有，同文件内可直接调用)
- fn write_config_file(path: &Path, content: &str, force: bool) -> Result<()>  (私有，同文件内可直接调用)
- pub fn handle_init(output_path: &str, force: bool) -> Result<()>  (Plan 01 已重构)

From src/cli/opts.rs (现状):
- Commands::Init { output: String, force: bool }  ← Plan 02 在此新增 interactive: bool

From src/main.rs (现状):
- match arm: Some(cli::opts::Commands::Init { output, force }) => { cli::init::handle_init(output, *force)?; Ok(None) }
- ← Plan 02 改为解构 { output, force, interactive } 并在 body 中 if 分支

From tests/integration.rs:
- 现有 handle_init 测试 (lines 158-275) 与 test_cli_init_creates_file_exit_0 (line 2183) 提供 e2e 参考模式
- assert_cmd::Command::cargo_bin("sqllog2db") 用法可参考第 2183 行
- 注入 stdin: Command::write_stdin("...") （assert_cmd API）
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: opts.rs 新增 interactive flag + main.rs 分发分支 + handle_init_interactive 实现</name>
  <files>src/cli/opts.rs, src/main.rs, src/cli/init.rs</files>
  <read_first>
    - src/cli/opts.rs  (Commands::Init 完整定义位于第 88-100 行；force flag 在第 97-99 行作为复制模板)
    - src/main.rs  (Init match arm 在第 141-143 行)
    - src/cli/init.rs  (Plan 01 添加的 run_wizard / WizardAnswers / write_config_file / apply_wizard_answers_to_template)
    - .planning/phases/68-init-wizard/68-PATTERNS.md  ("src/cli/opts.rs" / "src/main.rs" / "handle_init_interactive" 章节给出精确代码片段)
    - .planning/phases/68-init-wizard/68-CONTEXT.md  (D-01/D-02/D-03/D-07)
    - .planning/phases/68-init-wizard/68-UI-SPEC.md  ("Completion Output Contract" 完成输出文本)
  </read_first>
  <behavior>
    - `sqllog2db init --help` 输出包含 `-i, --interactive` 行
    - `sqllog2db init -i` 与 `sqllog2db init --interactive` 等价（clap 自动）
    - 不加 -i 时行为与现有 init 完全一致（test_handle_init_* 与 test_cli_init_creates_file_exit_0 全部继续通过）
    - 加 -i 时主程序进入向导分支，等待 stdin 输入；stdin 关闭/EOF 时使用全部默认值并写出文件
    - 向导写入完成后 stderr/stdout 含 "Next steps:" 三行（log::info! 输出，与 handle_init 一致格式）
    - --output 与 --force 在 interactive 模式下仍然生效（控制 config.toml 写入路径与覆盖行为）
  </behavior>
  <action>
    **修改 src/cli/opts.rs**：在 `Commands::Init` variant 第 97-99 行 `force` flag 之后新增（per PATTERNS "src/cli/opts.rs" 章节）：

    ```
    /// Start interactive configuration wizard
    #[arg(short = 'i', long = "interactive")]
    interactive: bool,
    ```

    flag 位置紧跟 force，不改变 output/force 顺序。

    **修改 src/main.rs**：
    - 第 141-143 行 Init match arm 解构改为 `Some(cli::opts::Commands::Init { output, force, interactive })`。
    - body 改为 if 分支（per D-03 + PATTERNS "src/main.rs" 章节）：interactive 为 true 时调 `cli::init::handle_init_interactive(output, *force)?;`，否则调 `cli::init::handle_init(output, *force)?;`，最终 `Ok(None)` 保持不变。
    - 不修改 `needs_simple_logging` 块（第 132-138 行已经对 Init 生效，interactive 仍走 simple logging）。
    - 同步更新 #[cfg(test)] mod tests 中如有任何 Commands::Init 字面量构造代码（若无则跳过）。

    **修改 src/cli/init.rs**：新增公开函数 `pub fn handle_init_interactive(output: &str, force: bool) -> Result<()>`（per D-07 + PATTERNS "handle_init_interactive 公开函数" 章节）：

    1. `let stdin = std::io::stdin();` `let stdout = std::io::stdout();` `let mut reader = stdin.lock();` `let mut writer = stdout.lock();`
    2. `let answers = run_wizard(&mut reader, &mut writer)?;`
    3. `let content = apply_wizard_answers_to_template(&answers);`
    4. `let path = Path::new(output);`
    5. `write_config_file(path, &content, force)?;`
    6. 输出 "Next steps:" 三行 info!（与 handle_init 完成后输出相同，per UI-SPEC Completion Output Contract）。可将三行 info! 抽取为私有函数 `fn log_next_steps(output: &str)` 供 handle_init 和 handle_init_interactive 复用，避免重复。若拆分，handle_init Plan 01 中保留的三行 info! 也改为调用 log_next_steps，并新增/更新对应单元测试（不必新增专门测试，现有 test_handle_init_creates_config_file 已覆盖文件创建路径）。

    handle_init_interactive 函数体必须 ≤ 40 行（按上述结构约 10-15 行即可）。
  </action>
  <verify>
    <automated>cargo build --release && cargo test --test integration test_handle_init && cargo test --test integration test_cli_init_creates_file_exit_0 && cargo clippy --all-targets -- -D warnings && cargo fmt --check</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F "interactive: bool" src/cli/opts.rs` 返回 ≥ 1 行
    - `grep -F "long = \"interactive\"" src/cli/opts.rs` 返回 ≥ 1 行
    - `grep -F "short = 'i'" src/cli/opts.rs` 在 Init variant 区域出现（与 Run variant 的 -i 同时存在，clap 自动按子命令隔离）
    - `grep -F "handle_init_interactive" src/main.rs` 返回 ≥ 1 行
    - `grep -E "Init \\{ output, force, interactive" src/main.rs` 匹配 1 次（match arm 已更新解构）
    - `grep -F "pub fn handle_init_interactive" src/cli/init.rs` 返回 1 行
    - handle_init_interactive 函数体 ≤ 40 行
    - `cargo build --release` 退出 0
    - `cargo run --release -- init --help` stdout 包含 `--interactive`（手工或脚本 grep 验证）
    - 全部 4 个 test_handle_init_* 测试继续通过（per Plan 01 设定）
    - test_cli_init_creates_file_exit_0 继续通过
    - `cargo clippy --all-targets -- -D warnings` 退出 0
    - `cargo fmt --check` 退出 0
  </acceptance_criteria>
  <done>opts.rs / main.rs / init.rs 三处修改完成，handle_init_interactive 串联 Plan 01 三组件，三道质量门禁绿灯，现有 init 测试零回归。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: e2e CLI 测试（INIT-01/02/03）+ 向导生成文件 validate 通过</name>
  <files>tests/integration.rs</files>
  <read_first>
    - tests/integration.rs  (现有 test_cli_init_creates_file_exit_0 第 2183 行作为 assert_cmd 模板；第 156-275 行 handle_init 单元测试参考)
    - src/cli/init.rs  (Plan 01 + Task 1 完成后状态)
    - .planning/phases/68-init-wizard/68-CONTEXT.md  (D-12 测试覆盖列表 + D-11 stdin 注入策略)
    - .planning/phases/68-init-wizard/68-UI-SPEC.md  ("Wizard Steps Contract" 精确提示串以构造 stdin 内容)
    - .planning/phases/68-init-wizard/68-RESEARCH.md  ("Phase Requirements → Test Map" 表)
  </read_first>
  <behavior>
    - test_cli_init_interactive_all_defaults：`sqllog2db init -i -o <tmp>/cfg.toml`，stdin 注入 `"\n\n\n"`，退出 0，<tmp>/cfg.toml 存在且包含 `inputs = ["sqllogs"]` 与 `file = "outputs/sqllog.csv"`
    - test_cli_init_interactive_custom_inputs：stdin 注入 `"my/dir\n\n\n"`，<tmp>/cfg.toml 含 `inputs = ["my/dir"]`
    - test_cli_init_interactive_sqlite：stdin 注入 `"\nsqlite\n\n\n"`，<tmp>/cfg.toml 含未注释的 `[exporter.sqlite]` 与 `database_url = "export/sqllog2db.db"` 与 `table_name = "sqllog_records"`，且 `[exporter.csv]` 行变为 `# [exporter.csv]`
    - test_cli_init_interactive_generates_validatable_config：先 `init -i -o <tmp>/cfg.toml` 生成（全默认），再 `sqllog2db validate -c <tmp>/cfg.toml` 退出 0（SC4）
    - test_cli_init_interactive_format_matches_non_interactive：`init -i -o <tmp>/a.toml` 全默认 + `init -o <tmp>/b.toml`（非交互），两文件内容字节级相等（INIT-03）
    - test_cli_init_interactive_existing_without_force_fails：先在 <tmp>/cfg.toml 写入任意内容，再 `init -i -o <tmp>/cfg.toml`（stdin 注入 `"\n\n\n"`），退出非零且 stderr 含 "already exists"（验证 D-02 的 --force 仍然生效）
  </behavior>
  <action>
    在 tests/integration.rs 末尾追加 6 个测试（per "behavior" 列表）。使用 assert_cmd 模式（参考第 2183 行 test_cli_init_creates_file_exit_0）：

    ```
    use assert_cmd::Command;
    let dir = tempfile::TempDir::new().unwrap();
    let out_file = dir.path().join("cfg.toml");
    Command::cargo_bin("sqllog2db").unwrap()
        .args(["init", "-i", "-o"]).arg(&out_file)
        .write_stdin("\n\n\n")
        .assert().success();
    ```

    每个测试独立 TempDir，避免相互干扰。stdin 内容必须严格对应向导步骤数：
    - CSV 路径 = 3 个换行（inputs / format / csv_path）
    - SQLite 路径 = 4 个换行（inputs / format=sqlite / sqlite_db / sqlite_table）

    test_cli_init_interactive_format_matches_non_interactive 的字节相等断言用 `std::fs::read_to_string` 双方对比 + `assert_eq!(a, b, "interactive and non-interactive must produce identical default config")`。

    test_cli_init_interactive_generates_validatable_config 第二步命令：
    ```
    Command::cargo_bin("sqllog2db").unwrap()
        .args(["validate", "-c"]).arg(&out_file)
        .assert().success();
    ```

    test_cli_init_interactive_existing_without_force_fails 使用 `predicates::str::contains("already exists")` 断言 stderr。

    不引入新依赖（assert_cmd / predicates / tempfile 已是项目 dev-deps，per tests/integration.rs 现有 imports）。
  </action>
  <verify>
    <automated>cargo test --test integration test_cli_init_interactive -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "fn test_cli_init_interactive" tests/integration.rs` ≥ 6
    - `cargo test --test integration test_cli_init_interactive` 退出 0（6 个测试全通过）
    - test_cli_init_interactive_format_matches_non_interactive 通过：交互式（全默认）与非交互式生成的 config.toml 字节级相同（INIT-03 端到端确认）
    - test_cli_init_interactive_generates_validatable_config 通过：向导生成文件能被 validate 子命令接受（SC4）
    - test_cli_init_interactive_existing_without_force_fails 通过：interactive 模式仍然遵守 --force 语义（D-02）
    - `cargo test` 全量退出 0（无任何回归，包括 Plan 01 添加的 lib 测试）
    - `cargo clippy --all-targets -- -D warnings` 退出 0
    - `cargo fmt --check` 退出 0
  </acceptance_criteria>
  <done>6 个 e2e 测试通过，端到端覆盖 INIT-01（flag 可达）、INIT-02（stdin 驱动不崩溃）、INIT-03（字节级与非交互一致）+ SC4（validate 通过），三道质量门禁全绿。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| clap 参数解析 → main.rs match | --interactive flag 字符串跨入应用层 |
| main.rs dispatch → handle_init_interactive | 控制流分支由用户传入的 flag 决定 |
| stdin → handle_init_interactive 内的 run_wizard | 复用 Plan 01 已建立的边界（详见 68-01-PLAN.md threat_model） |
| handle_init_interactive → write_config_file → fs::write | 写入路径由 `-o` 提供（用户可控） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-68-05 | Tampering | clap interactive flag 解析 | mitigate | clap 强类型 bool（#[arg]），用户输入仅能是布尔状态（指定 / 未指定），无字符串注入面 |
| T-68-06 | Elevation of Privilege | --output 路径覆盖任意磁盘文件 | accept | 与现有 handle_init 等价：本工具是 CLI，用户主动指定写入路径属预期行为；--force 检查由 write_config_file 强制；test_cli_init_interactive_existing_without_force_fails 守护此行为不在 interactive 模式下被绕过 |
| T-68-07 | Information Disclosure | 向导提示输出到 stdout 在 CI 日志可见 | accept | 提示文本无凭证/秘密；CI 日志暴露与现有 init 行为一致 |
| T-68-08 | Denial of Service | stdin 不关闭（持续等待）| accept | 终端用户可 Ctrl+C 中断；CI 场景必须显式 write_stdin，无意外阻塞场景。EOF 情况下 read_line 返回 0 字节 → trim 后判空 → 使用默认值，正常退出 |
| T-68-SC | Tampering | npm/pip/cargo installs | mitigate | 本 Plan 不引入任何新 crate（per D-06），无安装步骤 |
</threat_model>

<verification>
- `cargo build --release` 成功
- `cargo test` 全量通过（lib + integration + 新 e2e）
- `cargo clippy --all-targets -- -D warnings` 零警告
- `cargo fmt --check` 零差异
- 手工验证：`echo -e "\n\n\n" | cargo run --release -- init -i -o /tmp/wizard.toml` 退出 0
- 手工验证：`cargo run --release -- validate -c /tmp/wizard.toml` 退出 0
- 手工验证：`diff /tmp/wizard.toml <(cargo run --release -- init -o /tmp/non.toml && cat /tmp/non.toml)` 无差异
</verification>

<success_criteria>
- INIT-01 ✓ — `sqllog2db init --interactive` 启动向导（test_cli_init_interactive_all_defaults 验证）
- INIT-02 ✓ — 全程 Enter 不崩溃 + 自定义输入被采纳（test_cli_init_interactive_all_defaults + custom_inputs + sqlite 共同验证）
- INIT-03 ✓ — 字节级与非交互式 init 一致（test_cli_init_interactive_format_matches_non_interactive 验证）
- SC4 ✓ — 向导生成文件能 validate 通过（test_cli_init_interactive_generates_validatable_config 验证）
- D-02 ✓ — --force 在 interactive 模式下仍然生效（test_cli_init_interactive_existing_without_force_fails 验证）
- 零回归 — 所有现有 test_handle_init_* 与 test_cli_init_creates_file_exit_0 继续通过
- 三道质量门禁全绿
</success_criteria>

<output>
Create `.planning/phases/68-init-wizard/68-02-SUMMARY.md` when done. Must list:
- opts.rs / main.rs / init.rs 的精确修改行号与改动摘要
- handle_init_interactive 函数体行数
- 6 个 e2e 测试名称
- 任何在 e2e 测试中发现的字节级一致性验证细节（特别是 SQLite 模式的精确替换结果）
- 手工验证 `init --interactive` + `validate` 双步流程的结果
</output>
