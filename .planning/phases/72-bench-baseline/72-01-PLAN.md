---
phase: 72-bench-baseline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - benches/BENCHMARKS.md
autonomous: true
requirements:
  - BENCH-01
must_haves:
  truths:
    - "开发者运行 hyperfine 命令可以测量到当前 release binary 的冷启动延迟"
    - "BENCHMARKS.md 末尾出现 Phase 72 段落，含 --version 与 validate 两条 hyperfine 命令的实测数值"
    - "Phase 72 段落保留与 Phase 9 (v1.9) ~3ms 的对比维度，让历史回归一目了然"
    - "hyperfine 未安装时段落给出占位说明与安装方式，文档不阻断 CI"
  artifacts:
    - path: "benches/BENCHMARKS.md"
      provides: "Phase 72 — 基准体系完善（v1.20）段落（hyperfine 半部分）"
      contains: "## Phase 72 — 基准体系完善（v1.20）"
    - path: "target/release/sqllog2db"
      provides: "release binary，hyperfine 的测量对象"
  key_links:
    - from: "Phase 72 段落表格"
      to: "Phase 9 段落 ~2.9ms / ~2.8ms 历史值"
      via: "对比表格中 Phase 9 (v1.9) mean 列"
      pattern: "Phase 9 \\(v1.9\\)"
    - from: "hyperfine 折叠原始输出块"
      to: "实测 stdout"
      via: "<details><summary>hyperfine 原始输出</summary>"
      pattern: "<details>"
---

<objective>
为 v1.20 里程碑采集 CLI 冷启动延迟基线（BENCH-01），将 hyperfine 实测数值追加到 `benches/BENCHMARKS.md` 末尾的新段落"Phase 72 — 基准体系完善（v1.20）"，与 Phase 9（v1.9）历史 ~3ms 形成对比。

Purpose: 锁定 v1.20 当前冷启动性能，为后续 Phase 73–76 的性能优化提供回归比对的参照点。
Output: `benches/BENCHMARKS.md` 末尾追加"Phase 72 — 基准体系完善（v1.20）"段落的 hyperfine 部分，criterion 部分由 72-02 继续追加。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/72-bench-baseline/72-CONTEXT.md
@.planning/phases/72-bench-baseline/72-RESEARCH.md
@.planning/phases/72-bench-baseline/72-PATTERNS.md
@benches/BENCHMARKS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 构建 release binary 并采集 hyperfine 冷启动数值</name>
  <files>
    target/release/sqllog2db (rebuild only, no source change)
  </files>
  <read_first>
    - .planning/phases/72-bench-baseline/72-CONTEXT.md (D-01 锁定 hyperfine 命令)
    - .planning/phases/72-bench-baseline/72-RESEARCH.md §Common Pitfalls Pitfall 1 (release binary 过期)
    - benches/BENCHMARKS.md lines 315–388 (Phase 9 段落作为格式参照)
    - Cargo.toml (确认当前 version 号，用于"binary 是最新"自检)
  </read_first>
  <action>
    Step A — 确保 release binary 最新：执行 `cargo build --release`。完成后运行 `./target/release/sqllog2db --version`，确认输出版本号与 Cargo.toml 中 `[package].version` 一致（避免 Pitfall 1）。

    Step B — 检查 hyperfine 可用性：执行 `command -v hyperfine`。
    - 若存在：进入 Step C 采集真实数值。
    - 若不存在：跳到 Step D，按 D-03 走占位路径，并把 `command -v hyperfine` 的失败事实写入草稿。

    Step C（hyperfine 存在）— 严格按 D-01 锁定的命令执行，将两条命令各自的完整 stdout 原文保存为草稿（暂存到本地变量或临时文件，不要修改 BENCHMARKS.md）：
      命令 1: `hyperfine --warmup 3 './target/release/sqllog2db --version'`
      命令 2: `hyperfine --warmup 3 './target/release/sqllog2db validate -c config.toml'`
    从 stdout 中提取 `Time (mean ± σ)` 行的 mean 毫秒值（保留一位小数）作为 Phase 72 (v1.20) mean，供 Task 2 表格使用。Phase 9 (v1.9) 对比值固定使用：`--version` ~2.9 ms、`validate` ~2.8 ms（来自 RESEARCH.md §Specific Ideas 与 BENCHMARKS.md Phase 9 段落）。

    Step D（hyperfine 缺失，per D-03）— 在草稿中记录：
      - 测量被跳过的事实与原因（环境无 hyperfine）
      - 安装命令：`brew install hyperfine`（macOS）/ `cargo install hyperfine`（跨平台备选）
      - 表格中 Phase 72 (v1.20) mean 列填写"未测量（环境缺 hyperfine）"

    本 Task 不修改任何文件，只采集数据供 Task 2 写入。
  </action>
  <verify>
    <automated>cargo build --release && ./target/release/sqllog2db --version | grep -E "sqllog2db [0-9]+\.[0-9]+\.[0-9]+"</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build --release` 退出码 0，无 warning。
    - `./target/release/sqllog2db --version` 输出版本号与 Cargo.toml `[package].version` 字段完全相等。
    - 若 hyperfine 存在：已记下两条命令的 mean 毫秒值（数字）+ 完整 stdout 文本，可在 Task 2 直接粘贴。
    - 若 hyperfine 缺失：已记下"未测量"判定 + 安装命令 `brew install hyperfine`，可在 Task 2 写入占位段。
  </acceptance_criteria>
  <done>
    草稿数据齐备（hyperfine 测量值或占位说明 + 安装指引），release binary 已就位，等待 Task 2 写入文档。
  </done>
</task>

<task type="auto">
  <name>Task 2: 在 BENCHMARKS.md 末尾追加 Phase 72 段落（hyperfine 半部分）</name>
  <files>
    benches/BENCHMARKS.md
  </files>
  <read_first>
    - benches/BENCHMARKS.md (整文件，确认 Phase 56 在 line 723 之后并构成当前文件末尾，确认无其他 Phase 72 段落已存在)
    - benches/BENCHMARKS.md lines 315–388 (Phase 9 段落格式参照：标题、Date、Goal、Test environment、对比表、<details> 折叠、结论 checklist)
    - .planning/phases/72-bench-baseline/72-PATTERNS.md §"Phase 9 冷启动段落结构" 与 §"<details> 折叠原始输出模式"
    - .planning/phases/72-bench-baseline/72-CONTEXT.md (D-02/D-03/D-08/D-09 段落与对比要求)
  </read_first>
  <action>
    在 `benches/BENCHMARKS.md` 文件末尾（Phase 56 段落之后）追加一个新二级段落，标题严格写为：`## Phase 72 — 基准体系完善（v1.20）`（per D-02）。绝不修改 line 723 之前的任何已有段落（per D-08）。

    段落骨架（遵循 PATTERNS.md §"Phase 9 冷启动段落结构"模板，字段与小节标题用以下精确字符串）：

    1. 顶部元信息三行：
       - `**Date:** 2026-06-08`
       - `**Goal:** 建立 v1.20 里程碑 CLI 冷启动基线（BENCH-01）+ criterion throughput baseline 存档（BENCH-02，由 Plan 72-02 完成）`
       - `**Test environment:** Apple Silicon (Darwin 25.5.0), release build (\`opt-level=3\`, LTO=fat, strip=symbols, panic=abort)`

    2. 三级小节 `### CLI 冷启动基线（hyperfine, BENCH-01）`，包含：
       - 一段引言句子，注明"测量命令与 Phase 9 保持一致（per D-01）"
       - 紧跟一个 bash 代码块，原文复制 Task 1 Step C 的两条命令（即 `hyperfine --warmup 3 './target/release/sqllog2db --version'` 与 `hyperfine --warmup 3 './target/release/sqllog2db validate -c config.toml'`）
       - 对比表，列头固定为：`| 命令 | Phase 9 (v1.9) mean | Phase 72 (v1.20) mean | 差值 |`，两行内容：
         * `\`--version\`` 行：Phase 9 列填 `~2.9 ms`；Phase 72 列填 Task 1 Step C 的实测 mean（hyperfine 缺失走 D-03 时填 `未测量（环境缺 hyperfine）`）；差值列填 `[新值] - 2.9 ms` 的实测结果（缺失时填 `—`）
         * `\`validate -c config.toml\`` 行：Phase 9 列填 `~2.8 ms`，其余规则同上
       - 两个 `<details>` 折叠块：
         * `<details><summary>hyperfine 原始输出（--version）</summary>` 内嵌三反引号代码块，粘贴 Task 1 Step C 的 `--version` 完整 stdout（hyperfine 缺失走 D-03 时写："测量被跳过，本机/CI 环境未安装 hyperfine。安装方式：\`brew install hyperfine\`（macOS）或 \`cargo install hyperfine\`（跨平台备选）。"），收尾 `</details>`
         * `<details><summary>hyperfine 原始输出（validate）</summary>` 结构同上，粘贴 validate 命令的 stdout 或占位文本，收尾 `</details>`

    3. 三级小节 `### Criterion v1.20 Baseline 存档（BENCH-02）`，只写一句占位："详见 Plan 72-02 — 由 `CRITERION_HOME=benches/baselines cargo bench -- --save-baseline v1.20` 写入，全部 baseline JSON 已纳入 repo。"（criterion 数值与对比命令由 72-02 在同一段落继续填充）

    4. 三级小节 `### 结论`，使用 markdown checklist 格式（参照 Phase 9 lines 381–387 的 checklist 模式）：
       - `- [x] BENCH-01 hyperfine 冷启动数值已记录（--version + validate 两命令），与 Phase 9 ~3ms 形成对比表`
       - `- [ ] BENCH-02 criterion v1.20 baseline 存档（由 Plan 72-02 完成）`

    内容写入后追加一行 `---` 与文件其他段落分隔（与 Phase 9 lines 388-390 一致）。

    禁止：
    - 修改任何 line 723 之前的内容（D-08）
    - 修改 line 25–38 "How to compare against this baseline" 段落（该段由 Plan 72-02 负责追加 v1.20 命令，per D-06）
    - 使用 emoji
    - 引入 v1.20 之外的 baseline 名称
  </action>
  <verify>
    <automated>grep -c "^## Phase 72 — 基准体系完善（v1.20）$" benches/BENCHMARKS.md | grep -q "^1$" && grep -q "^### CLI 冷启动基线（hyperfine, BENCH-01）$" benches/BENCHMARKS.md && grep -q "Phase 9 (v1.9) mean | Phase 72 (v1.20) mean" benches/BENCHMARKS.md && grep -c "<details>" benches/BENCHMARKS.md | awk '$1>=2{exit 0} {exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^## Phase 72 — 基准体系完善（v1.20）$" benches/BENCHMARKS.md` 输出 `1`（段落标题精确匹配且唯一）。
    - `grep -n "^## Phase 72 — 基准体系完善（v1.20）$" benches/BENCHMARKS.md` 报告的行号 > 723（保证位于 Phase 56 之后，符合 D-08 追加要求）。
    - 段落内含字符串 `### CLI 冷启动基线（hyperfine, BENCH-01）`、`### Criterion v1.20 Baseline 存档（BENCH-02）`、`### 结论`。
    - 段落内的对比表表头精确包含 `Phase 9 (v1.9) mean` 与 `Phase 72 (v1.20) mean` 两列。
    - 段落新增 `<details>` 折叠块至少 2 个（hyperfine --version + validate），每个 `<details>` 都有对应 `</details>` 闭合。
    - 段落 `### 结论` 含两条 checklist：BENCH-01 标记 `[x]`，BENCH-02 标记 `[ ]`。
    - `git diff --stat benches/BENCHMARKS.md` 显示纯追加（lines added > 0、lines removed == 0）。
    - `cargo clippy --all-targets -- -D warnings` 与 `cargo test` 全部通过（本任务无代码变更，作为兜底门禁）。
  </acceptance_criteria>
  <done>
    BENCHMARKS.md 末尾新增 Phase 72 段落含 hyperfine 实测/占位数据、对比表、原始输出折叠块、结论 checklist；clippy + test 通过；历史段落（含 Phase 9）零修改。
  </done>
</task>

</tasks>

<verification>
- BENCHMARKS.md 末尾出现单一 Phase 72 段落，标题精确匹配 D-02
- hyperfine 部分含 `--version` 与 `validate` 两条命令的数值或 D-03 占位说明
- 对比表保留 Phase 9 (v1.9) 列，体现回归比较维度
- 仅追加，line 723 及之前的所有段落保持原状
- cargo clippy/test 全绿
</verification>

<success_criteria>
- BENCH-01 验收：hyperfine 测量结果（或 D-03 占位说明）已在 BENCHMARKS.md 末尾段落落地，可用 grep 验证段落与表头存在
- 历史段落（Phase 4/5/6/9/10/42/44/56）字节级保持不变
- release binary 已重建，版本号与 Cargo.toml 一致
- 为 Plan 72-02 留好 Criterion 半部分的占位锚点
</success_criteria>

<output>
Create `.planning/phases/72-bench-baseline/72-01-SUMMARY.md` when done
</output>
