---
phase: "66.1-jobs"
verified: "2026-06-04T00:00:00Z"
status: passed
score: 6/6 must-haves verified
overrides_applied: 0
gaps: []
deferred: []
---

# Phase 66.1: 修复并行集成测试覆盖缺陷 Verification Report

**Phase Goal:** 修复并行集成测试覆盖缺陷 — 通过 jobs_override 参数注入和异构测试数据，确保并行 CSV 路径在所有 CI 环境（含单核）下都被实际执行并被强测试断言覆盖。
**Verified:** 2026-06-04
**Status:** PASSED
**Requirements:** PARALLEL-06, PARALLEL-07
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | `handle_run` 接受第 5 个参数 `jobs_override: Option<usize>`；传 `None` 时与改前行为等价 | VERIFIED | `src/cli/run/mod.rs` line 34: `jobs_override: Option<usize>`；line 39-40: `jobs_override.unwrap_or_else(|| std::thread::available_parallelism()...)` |
| 2 | `tests/integration.rs` 中新增 `write_heterogeneous_log` helper，可基于 `trxid_offset/username` 生成跨文件可区分的数据 | VERIFIED | `tests/integration.rs` line 31: `fn write_heterogeneous_log(path, count, trxid_offset, username)` — 函数体 20 行，trxid 基于 offset 偏移，user 用入参 username |
| 3 | 新集成测试 `test_parallel_csv_jobs_override_forces_parallel` 显式传 `Some(2)`，在单核 CI 上也能进入并行路径 | VERIFIED | `tests/integration.rs` line 2466: 函数定义存在；line 2496: `handle_run(..., Some(2)).unwrap()`；测试在 `cargo test` 中实际通过 |
| 4 | 新集成测试 `test_parallel_csv_heterogeneous_matches_sequential` 使用异构数据对比顺序基线，并行漏文件 bug 必然被检出 | VERIFIED | `tests/integration.rs` line 2519: 函数定义存在；顺序基线逐文件用 `None`，并行用 `Some(2)`；排序后 `assert_eq!(seq_lines, par_lines)` 逐字节比对；`cargo test` 通过 |
| 5 | 所有现有 `handle_run` 调用点（35+）均同步传入第 5 个参数，`cargo build` 编译通过 | VERIFIED | `src/main.rs:167` 传 `None`；`src/cli/run/tests.rs` 7 处全部传 `None`（含多行形式 line 132,153,225,233）；`tests/integration.rs` 23 处（含 2 条新测试用 `Some(2)`，其余 21 处用 `None`）；benchmarks 6 处全传 `None`；另有 `tests/jemalloc_peak.rs` 1 处（共 36 处，超出 PLAN 预估的 35 处）；`cargo test` 全工作区编译通过 |
| 6 | `cargo test` 全部通过（780 测试），`cargo clippy --all-targets -- -D warnings` 零警告，`cargo fmt --check` 通过 | VERIFIED | `cargo test` 输出：335+366+3+75+1=780 passed，0 failed，含 `test_parallel_csv_jobs_override_forces_parallel ... ok` 和 `test_parallel_csv_heterogeneous_matches_sequential ... ok`；clippy 零警告；fmt 通过 |

**Score:** 6/6 truths verified

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src/cli/run/mod.rs` | `handle_run` 新签名 + `jobs_override.unwrap_or_else(...)` | VERIFIED | line 29-35 新签名；line 39-40 `jobs_override.unwrap_or_else` 跨行实现 |
| `tests/integration.rs` | 异构数据 helper + 2 条强制并行测试 | VERIFIED | line 31 helper；line 2466 PARALLEL-06 测试；line 2519 PARALLEL-07 测试 |
| `src/main.rs` | 生产调用点传 `None` 保持原行为 | VERIFIED | line 167: `handle_run(&cfg, cli.quiet, cli.verbose, &interrupted, None)` |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `tests/integration.rs::test_parallel_csv_jobs_override_forces_parallel` | `src/cli/run/mod.rs::handle_run` | `Some(2)` 注入绕过 `available_parallelism` | VERIFIED | line 2496: `handle_run(&par_cfg, true, false, &interrupted, Some(2))` |
| `src/cli/run/mod.rs::handle_run` | jobs 并行触发条件 | `jobs_override.unwrap_or_else(...)` → `jobs` 变量 → `jobs > 1` 判断 | VERIFIED | line 39-40 计算 jobs；line 64: `let use_csv_parallel = jobs > 1 && ...` |

---

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|--------------------|--------|
| `test_parallel_csv_jobs_override_forces_parallel` | `data_lines: Vec<&str>` | `write_heterogeneous_log` 写入真实格式日志 + `handle_run` 解析 | Yes — 35 条记录断言 | FLOWING |
| `test_parallel_csv_heterogeneous_matches_sequential` | `seq_lines / par_lines` | 相同文件分别走顺序/并行路径 | Yes — 排序后逐字节比对 | FLOWING |

---

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| 两条新并行测试通过 | `cargo test 2>&1 | grep test_parallel_csv` | `test_parallel_csv_jobs_override_forces_parallel ... ok`、`test_parallel_csv_heterogeneous_matches_sequential ... ok` | PASS |
| 全工作区 780 测试 0 失败 | `cargo test 2>&1 | grep "test result:"` | 所有 target: `0 failed` | PASS |
| clippy 零警告 | `cargo clippy --all-targets -- -D warnings` | `Finished` 无错误输出 | PASS |
| fmt 格式合规 | `cargo fmt --check` | 退出码 0 | PASS |

---

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| PARALLEL-06 | 66.1-01-PLAN.md | `jobs_override=Some(2)` 强制并行路径，单核 CI 也触发并行 | SATISFIED | `test_parallel_csv_jobs_override_forces_parallel` 实现并通过；`handle_run` 签名落地 |
| PARALLEL-07 | 66.1-01-PLAN.md | 异构数据 helper + 顺序基线比对，聚合 bug 立即可见 | SATISFIED | `write_heterogeneous_log` + `test_parallel_csv_heterogeneous_matches_sequential` 实现并通过 |

---

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| (none) | — | — | — | — |

clippy 零警告，无 TBD/FIXME/XXX 标记，无空实现。

---

### Human Verification Required

(none — all must-haves are programmatically verifiable and verified)

---

## Gaps Summary

No gaps. All 6 must-have truths verified. PARALLEL-06 and PARALLEL-07 fully implemented, wired, and passing.

**Notable observations (informational, not blockers):**

- SUMMARY.md 报告 36 处调用点（含 `tests/jemalloc_peak.rs:121`），超出 PLAN 预估的 35 处。这是正向偏差，不是缺陷。
- `jobs_override.unwrap_or_else` 模式跨两行实现，单行 grep 会漏匹配，但逻辑完全正确（已通过多行读取确认）。
- `write_heterogeneous_log` 中 trxid=0 时 ROWCOUNT=0，可能触发 `has_metrics` 的 `rowcount != 0` 守卫（见 REVIEW.md WR-03）。PARALLEL-06 的 file_a 使用 offset=0，包含 trxid=0 的记录。两个路径（顺序/并行）都经过相同守卫，因此 PARALLEL-07 的排序比对仍然有效。该 edge case 已由 REVIEW.md 记录，不影响测试覆盖正确性。

---

_Verified: 2026-06-04_
_Verifier: Claude (gsd-verifier)_
