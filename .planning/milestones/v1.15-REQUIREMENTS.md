# Requirements Archive: v1.15 工程质量全面提升

**Archived:** 2026-06-02
**Status:** ✅ ALL REQUIREMENTS SATISFIED (with tech debt acknowledged)

---

## Milestone v1.15 — 工程质量全面提升

### CI/CD 基础设施

- [x] **CICD-01**: 用户推送 PR/branch 时，GitHub Actions CI 自动运行 test/clippy/fmt 全绿（三平台：ubuntu/windows/macos）
  - _Outcome: 静态代码验证通过（ci.yaml 三平台矩阵正确）；运行时验证待人工推送 PR 确认，属外部环境限制_
- [x] **CICD-02**: 用户推送 tag 时，CD workflow 成功构建 4 个平台的二进制并创建 GitHub Release
  - _Outcome: 静态验证通过（release.yaml artifact 暂存 + 独立 create-release job）；运行时验证待推送 tag 确认_
- [x] **CICD-03**: CD workflow 在 4 个 job 并行运行时正确创建 release notes（无竞争条件，发布内容完整）
  - _Outcome: VERIFIED — needs:[release] 串行化，单次写入 release body_
- [x] **CICD-04**: 项目包含 Cross.toml，aarch64-linux 跨编译构建无需手动干预
  - _Outcome: VERIFIED — Cross.toml 新建，ghcr.io/cross-rs/aarch64-unknown-linux-gnu:edge 镜像配置_

### e2e 测试

- [x] **TEST-01**: run 子命令 CLI 全链路测试——给定真实输入文件，验证 CSV 输出内容与退出码；给定真实输入文件，验证 SQLite 输出与退出码
  - _Outcome: SATISFIED — test_cli_run_csv_output_header_and_row_count + test_cli_run_sqlite_output_row_count_
- [x] **TEST-02**: init 子命令 assert_cmd 测试——验证生成 config.toml 的 CLI 行为、文件存在与退出码
  - _Outcome: SATISFIED — test_cli_init_creates_file_exit_0 + test_cli_init_existing_file_without_force_exits_nonzero_
- [x] **TEST-03**: stats 子命令 --from/--to 边界条件 e2e 测试（空范围、边界值、无效格式拒绝）
  - _Outcome: SATISFIED — validate_stats_time_range from≤to 跨字段校验 + test_cli_stats_rejects_from_after_to_

### 代码清理

- [x] **CLEAN-01**: stats 模块删除遗留 warn! 占位符，stats/output.rs 所有函数不超过 40 行
  - _Outcome: SATISFIED — warn! 占位符已删除，scanner 公共模块提取，静态断言通过_
- [x] **CLEAN-02**: cli/run 模块中超 40 行的函数提取为私有函数（仅拆分确实超长的，不做预防性拆分）
  - _Outcome: PARTIAL (override accepted) — 7 个私有函数提取完成，handle_run 逻辑语句数 ~37，物理行数约 100（cargo fmt 展开所致），override 已由 guangl 接受_

### Benchmark 稳定化

- [x] **BENCH-01**: 确认 scripts/collect_bench_results.sh 存在（或补充创建），bench.yml 以信息性（non-blocking，continue-on-error）方式运行
  - _Outcome: SATISFIED — scripts/collect_bench_results.sh 存在，bench.yml continue-on-error:true，BENCHMARKS.md CI Artifact 指南已补充_

---

## Future Requirements（延后）

| Requirement | Milestone | Notes |
|-------------|-----------|-------|
| benchmark CI 门控（regression 自动拒绝） | 未定 | 需要稳定的基准基线，v1.15 先信息性收集 |
| cargo audit 定时扫描 | 未定 | 安全扫描，低优先级 |
| 多平台 e2e matrix（Windows + Linux） | 未定 | v1.15 先本地通过，后续 CI 加平台 |
| Cross.toml edge tag 固定为 SHA digest | 未定 | 可复现性，低优先级 |

---

## Out of Scope

| Feature | Reason |
|---------|--------|
| x86_64-unknown-linux-musl | rusqlite bundled 在 musl 下有已知 segfault |
| cargo-dist / release-plz | 对单维护者项目过度工程化 |
| 第三方覆盖率服务（Codecov 等） | 项目已有 cargo-llvm-cov，不需要外部服务 |
| trycmd 迁移 | assert_cmd 模式已成熟，迁移无收益 |
| criterion 作为 merge 门控 | CI runner 噪声 ±15%，不适合门控 |

---

## Traceability

| REQ-ID | Phase | Status | Notes |
|--------|-------|--------|-------|
| CICD-01 | 55 | PARTIALLY VERIFIED | 静态代码正确，运行时待 GitHub Actions 确认 |
| CICD-02 | 55 | PARTIALLY VERIFIED | 静态代码正确，运行时待 GitHub Actions 确认 |
| CICD-03 | 55 | VERIFIED | needs:[release] 串行化消除竞争条件 |
| CICD-04 | 55 | VERIFIED | Cross.toml 创建并配置 |
| TEST-01 | 57 | SATISFIED | run CSV+SQLite 全链路 e2e 测试 |
| TEST-02 | 57 | SATISFIED | init 成功/冲突路径 e2e 测试 |
| TEST-03 | 57 | SATISFIED | stats from>to 拒绝 e2e 测试 |
| CLEAN-01 | 56 | SATISFIED | warn! 占位符删除，output.rs ≤40 行 |
| CLEAN-02 | 58 | PARTIAL (override) | 7 个私有函数提取，handle_run override 已接受 |
| BENCH-01 | 56 | SATISFIED | collect_bench_results.sh + bench.yml non-blocking |
