---
phase: 71-mod-rs-mod-rs-pub-use
plan: 07
type: execute
wave: 2
depends_on: []
files_modified:
  - src/exporter/csv/mod.rs
  - src/exporter/csv/exporter.rs
  - src/exporter/csv/impls.rs
autonomous: true
requirements: []
must_haves:
  truths:
    - "src/exporter/csv/mod.rs 仅含 mod 声明与 pub use 重导出"
    - "CsvExporter struct 含全部字段不变，可通过 crate::exporter::csv::CsvExporter 访问"
    - "WriteMode 内部 enum 保持私有"
    - "Exporter trait 实现行为完全不变"
    - "cargo test 通过（含 csv/tests.rs 全部测试）"
    - "cargo clippy --all-targets -- -D warnings 通过"
  artifacts:
    - path: "src/exporter/csv/mod.rs"
      provides: "module declarations + pub use only"
    - path: "src/exporter/csv/exporter.rs"
      provides: "CsvExporter struct + WriteMode enum + 构造方法 (new, from_config) + build_header + Debug impl + Drop impl + writer_ref helper"
      contains: "pub struct CsvExporter"
    - path: "src/exporter/csv/impls.rs"
      provides: "impl Exporter for CsvExporter 块"
      contains: "impl Exporter for CsvExporter"
  key_links:
    - from: "src/exporter/csv/mod.rs"
      to: "exporter / impls"
      via: "mod 声明 + pub use"
      pattern: "pub use exporter::CsvExporter"
    - from: "src/exporter/mod.rs"
      to: "crate::exporter::csv::CsvExporter"
      via: "pub(crate) use csv::CsvExporter"
      pattern: "pub(crate) use csv::CsvExporter"
---

<objective>
重构 src/exporter/csv/mod.rs（243 行）：将 CsvExporter struct 与构造逻辑、Drop、Debug 拆到 exporter.rs，把 `impl Exporter for CsvExporter` trait 实现块拆到 impls.rs，mod.rs 仅保留 mod 声明与 pub use 重导出。writer.rs 与 tests.rs 不动。

Purpose: csv 子模块入口清晰化，按"数据 vs 行为"切分（struct + 构造在一个文件，trait impl 在另一个文件）。
Output: 3 个文件（mod.rs 极简 + exporter.rs + impls.rs），cargo test/clippy 全绿。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md

<interfaces>
<!-- 必须保持的导出 -->

公开：
- `pub struct CsvExporter` 含字段：
  - path: PathBuf
  - write_mode: WriteMode（私有 enum）
  - writer: Option<BufWriter<File>>
  - stats: ExportStats
  - itoa_buf: itoa::Buffer
  - line_buf: Vec<u8>
  - pub(crate) normalize: bool
  - pub(crate) field_mask: FieldMask
  - pub(crate) ordered_indices: Vec<usize>
  - pub(crate) include_performance_metrics: bool
- `pub fn CsvExporter::new(path: impl AsRef<Path>) -> Self`
- `pub fn CsvExporter::from_config(config: &CsvExporterConfig) -> Self`

私有：
- `enum WriteMode { Truncate, Append }`
- `fn build_header(&self) -> Vec<u8>`
- `fn writer_ref<'a>(w, path) -> Result<&'a mut BufWriter<File>>`
- `impl Debug for CsvExporter`
- `impl Drop for CsvExporter`
- `impl Exporter for CsvExporter`（含 initialize / export / export_one_normalized / export_one_preparsed / finalize / stats_snapshot）

子模块（不动）：
- `pub(crate) mod writer;`
- `#[cfg(test)] mod tests;`
- `use self::writer::{write_record, write_record_preparsed};` — 移到 impls.rs

外部引用路径：
- `crate::exporter::CsvExporter`（通过 exporter/mod.rs 的 pub(crate) use）
- `crate::exporter::csv::CsvExporter`（通过 csv/mod.rs 的 pub use）
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 拆分 csv/mod.rs 到 exporter.rs + impls.rs</name>
  <files>src/exporter/csv/mod.rs, src/exporter/csv/exporter.rs, src/exporter/csv/impls.rs</files>
  <read_first>
    - src/exporter/csv/mod.rs（全文 243 行）
    - src/exporter/csv/writer.rs（确认 write_record / write_record_preparsed 签名）
    - src/exporter/csv/tests.rs（确认 use 路径，预期 use super::CsvExporter; 等）
    - src/exporter/mod.rs（确认 `pub(crate) use csv::CsvExporter;`）
  </read_first>
  <action>
    Step 1: 创建 `src/exporter/csv/exporter.rs`：
      - 移入 mod.rs 第 14-91 行 + 第 222-240 行：
        - `enum WriteMode { Truncate, Append }`（保持 private，加 `pub(super)` 让 impls.rs 可访问；或维持 super 私有，因为同一父模块的兄弟文件通过 super:: 可见）
        - `pub struct CsvExporter { ... }`（所有字段保持原可见性）
        - `impl Debug for CsvExporter`
        - `impl CsvExporter { new, from_config, build_header }`（build_header 改为 `pub(super) fn` 让 impls.rs 可调用；或保留 fn 私有，因为 impls.rs 是 crate 同模块的兄弟文件需通过 super:: 访问，则用 pub(super)）
        - `impl Drop for CsvExporter`
        - `pub(super) fn writer_ref<'a>(w, path) -> Result<&'a mut BufWriter<File>>` （让 impls.rs 调用）
      - 顶部 use：
        ```rust
        use super::super::ensure_parent_dir;
        use super::super::ExportStats;
        use crate::config;
        use crate::error::{Error, ExportError, Result};
        use std::fs::{File, OpenOptions};
        use std::io::{BufWriter, Write};
        use std::path::{Path, PathBuf};
        ```

    Step 2: 创建 `src/exporter/csv/impls.rs`：
      - 移入 mod.rs 第 93-220 行 `impl Exporter for CsvExporter`（包含 initialize/export/export_one_normalized/export_one_preparsed/finalize/stats_snapshot）
      - 顶部 use：
        ```rust
        use super::exporter::{writer_ref, CsvExporter, WriteMode};
        use super::writer::{write_record, write_record_preparsed};
        use super::super::{Exporter, ExportStats};
        use crate::error::{Error, ExportError, Result};
        use dm_database_parser_sqllog::Sqllog;
        use std::fs::OpenOptions;
        use std::io::{BufWriter, Write};
        ```
        说明：实现中 `WriteMode::Append` 比较需要 WriteMode 在 impls.rs 中可见，所以 exporter.rs 中要把 WriteMode 改为 `pub(super) enum WriteMode`。

    Step 3: 改写 `src/exporter/csv/mod.rs` 为：
      ```rust
      //! CSV exporter：struct 与构造在 exporter.rs，trait impl 在 impls.rs，序列化在 writer.rs。

      pub(crate) mod writer;

      mod exporter;
      mod impls;

      #[cfg(test)]
      mod tests;

      pub use exporter::CsvExporter;
      ```
      仅注释、mod 声明、pub use。

    Step 4: 运行质量门禁：
      - `cargo build`
      - `cargo clippy --all-targets -- -D warnings`
      - `cargo test`（特别确认 tests.rs 中所有 CsvExporter 单元测试通过）
      - `cargo fmt`

    可见性细节：
      - WriteMode：mod.rs 私有 → 改为 exporter.rs 中 `pub(super) enum WriteMode`（让 impls.rs 内部可见）
      - CsvExporter::build_header：原是私有 fn → 改为 `pub(super) fn`（impls.rs 中调用 self.build_header() 实际通过 self 方法分发，不必改）
      - 等等：build_header 是 impl CsvExporter 块内的方法，且 initialize() 中调用 `self.build_header()` — 那 build_header 留 fn 也可以（self 方法调用不受字段可见性约束）；impls.rs 通过 self.build_header() 调用同样合法。所以 build_header 保持私有 fn 即可。
      - writer_ref：是文件级 fn，不在 impl 块内 → 必须 `pub(super) fn` 才能让 impls.rs 用 super::exporter::writer_ref 调用
      - WriteMode 同理：impls.rs 内的 `self.write_mode == WriteMode::Append` 直接构造枚举变体 → WriteMode 必须 `pub(super)`
  </action>
  <verify>
    <automated>cargo clippy --all-targets -- -D warnings && cargo test</automated>
    <grep-check>grep -v '^\s*//' src/exporter/csv/mod.rs | grep -vE '^\s*$' | grep -E '\bfn \w+|\bstruct \w+|\bimpl |\benum ' && echo "FAIL: mod.rs 仍含实现" || echo "OK: mod.rs 仅声明"</grep-check>
  </verify>
  <acceptance_criteria>
    - cargo clippy --all-targets -- -D warnings 退出码 0
    - cargo test 退出码 0
    - src/exporter/csv/mod.rs 过滤注释/空行后只含 `mod`/`pub(crate) mod`/`pub use`/属性，无 `fn`/`struct`/`impl`/`enum`/`trait`
    - CsvExporter 在 crate::exporter::csv::CsvExporter 和 crate::exporter::CsvExporter 两条路径下都可达
    - csv/tests.rs 中所有测试无需修改 use 路径即可继续运行
    - writer.rs 文件内容完全不变
  </acceptance_criteria>
  <done>
    mod.rs 简化到 ≤ 15 行（含注释），exporter.rs（数据） + impls.rs（行为）按职责承接全部实现，质量门禁全绿。
  </done>
</task>

</tasks>

<verification>
- cargo clippy --all-targets -- -D warnings 通过
- cargo test 通过（含 csv 集成测试）
- src/exporter/csv/mod.rs grep -E '\bfn |\bstruct |\bimpl |\benum |\btrait ' 无匹配
- src/exporter/csv/writer.rs 与 src/exporter/csv/tests.rs 内容不变
</verification>

<success_criteria>
- mod.rs 仅含 mod 声明与 pub use（≤ 15 行）
- exporter.rs 承接 struct/enum/构造/Drop/Debug
- impls.rs 承接 Exporter trait 实现
- 全套质量门禁通过
</success_criteria>

<output>
Create `.planning/phases/71-mod-rs-mod-rs-pub-use/71-07-SUMMARY.md` when done
</output>
