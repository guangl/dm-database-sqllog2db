---
phase: 44-hotpath
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - tests/jemalloc_peak.rs
  - benches/baselines/csv_export/1000/phase44-before
  - benches/baselines/csv_export/10000/phase44-before
  - benches/baselines/csv_export/50000/phase44-before
autonomous: true
requirements:
  - PERF-01
  - PERF-02

must_haves:
  truths:
    - "Cargo.toml 的 [dev-dependencies] 段包含 tikv-jemallocator 0.6.1 和 tikv-jemalloc-ctl 0.6.1"
    - "release binary 不链接 jemalloc（仅 dev/bench 使用）"
    - "tests/jemalloc_peak.rs 存在并定义 measure_alloc_delta 辅助函数"
    - "cargo test test_jemalloc_peak_baseline -- --nocapture 输出 v1.10 基线峰值堆数值"
    - "benches/baselines/csv_export/{1000,10000,50000}/phase44-before/ 目录存在并包含 criterion estimates.json"
    - "cargo clippy --all-targets -- -D warnings 通过"
  artifacts:
    - path: "Cargo.toml"
      provides: "jemalloc dev-deps 声明"
      contains: "tikv-jemallocator"
    - path: "tests/jemalloc_peak.rs"
      provides: "jemalloc peak heap baseline measurement"
      min_lines: 40
    - path: "benches/baselines/csv_export/10000/phase44-before/estimates.json"
      provides: "Wave 0 优化前 criterion 基线"
  key_links:
    - from: "tests/jemalloc_peak.rs"
      to: "tikv_jemalloc_ctl::{epoch, stats}"
      via: "use 语句"
      pattern: "use tikv_jemalloc_ctl"
    - from: "tests/jemalloc_peak.rs"
      to: "#[global_allocator] tikv_jemallocator::Jemalloc"
      via: "cfg(test) static"
      pattern: "global_allocator"
---

<objective>
建立 Phase 44 的优化前基线测量基础设施：将 jemalloc 作为 dev-dep 集成（D-01/D-03），编写独立 jemalloc 峰值堆测试输出 v1.10 基线数值（D-02），并保存当前 csv_export benchmark 的 phase44-before criterion baseline。

Purpose: PERF-01 需要 criterion `--baseline phase44-before` 对比输出"Performance has improved"；PERF-02 需要量化的堆分配基线数值供 Wave 1 优化后对比。Wave 0 必须先采集，否则后续两个 requirements 无可量化依据。
Output: Cargo.toml 新增 dev-deps，新增 tests/jemalloc_peak.rs，新增 benches/baselines/csv_export/*/phase44-before/ 目录。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-hotpath/44-CONTEXT.md
@.planning/phases/44-hotpath/44-RESEARCH.md
@.planning/phases/44-hotpath/44-PATTERNS.md

@Cargo.toml
@benches/bench_csv.rs

<interfaces>
<!-- jemalloc 测量 API 形态（来自 RESEARCH.md Code Examples，已 ASSUMED 标注） -->
<!-- 执行时若 API 不匹配，以 tikv-jemalloc-ctl 0.6.1 实际编译错误为准调整 -->

来自 tikv-jemalloc-ctl 0.6.1（dev-dep）：
- epoch::mib() -> Result<Mib, Error>，Mib::advance() 刷新统计
- stats::allocated::mib() -> Result<Mib, Error>，Mib::read() -> Result<usize, Error>
- 必须每次 read 前调用 epoch.advance()，否则读到陈旧值（Pitfall 5）

来自 tikv-jemallocator 0.6.1：
- pub struct Jemalloc;
- 用法：#[global_allocator] static ALLOC: Jemalloc = Jemalloc;

Cargo.toml 现状（第 85-87 行）：
```toml
[dev-dependencies]
tempfile = "3.27.0"
criterion = { version = "0.7", features = ["html_reports"] }
```

benches/bench_csv.rs 的 bench group 名称：csv_export（参数 1000/10000/50000）
现有 baseline 目录：benches/baselines/csv_export/{1000,10000,50000}/phase33/
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 在 Cargo.toml 追加 jemalloc dev-dependencies</name>
  <read_first>
    - Cargo.toml（整文件，确认 [dev-dependencies] 段当前位置和内容）
    - .planning/phases/44-hotpath/44-PATTERNS.md（第 102-136 行：Cargo.toml 修改模式）
    - .planning/phases/44-hotpath/44-RESEARCH.md（第 82-99 行：Standard Stack dev-deps 与版本号）
  </read_first>
  <files>Cargo.toml</files>
  <action>
  在 Cargo.toml 第 85-87 行的 [dev-dependencies] 段尾部追加两行（按 D-01/D-03，仅 dev-dep，绝对不修改 [dependencies] 段也不修改 src/main.rs）：

    tikv-jemallocator = "0.6.1"
    tikv-jemalloc-ctl = "0.6.1"

  追加后 [dev-dependencies] 段应包含 4 个条目：tempfile、criterion、tikv-jemallocator、tikv-jemalloc-ctl。

  完成后运行 `cargo build --release` 确认 release binary 不链接 jemalloc（Pitfall 1 防护：dev-deps 不应进入 release 编译图）。再运行 `cargo build --tests` 触发 jemalloc 编译，验证版本可解析。

  注意：版本号必须精确为 "0.6.1"（已通过 cargo search 在 crates.io 验证为 VERIFIED 包，per Package Legitimacy Audit）。不要改用 "0.6" 或 "^0.6.1"。
  </action>
  <verify>
    <automated>grep -F 'tikv-jemallocator = "0.6.1"' Cargo.toml && grep -F 'tikv-jemalloc-ctl = "0.6.1"' Cargo.toml && cargo build --release 2>&1 | grep -i jemalloc; test $? -ne 0 || echo "FAIL: release binary unexpectedly references jemalloc"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^tikv-jemallocator = "0.6.1"$' Cargo.toml` 返回 1
    - `grep -c '^tikv-jemalloc-ctl = "0.6.1"$' Cargo.toml` 返回 1
    - `[dev-dependencies]` 段共 4 个条目（grep -A4 '^\[dev-dependencies\]' Cargo.toml 行数 = 4 个 ` = ` 条目）
    - `cargo build --release` 成功，输出不包含 "compiling tikv-jemallocator"（仅 dev 模式编译）
    - `cargo build --tests` 成功，下载并编译 tikv-jemallocator 与 tikv-jemalloc-ctl
    - `[dependencies]` 段未被修改（grep '^tikv' 在 [dependencies] 段范围内无匹配）
  </acceptance_criteria>
  <done>Cargo.toml 包含两个 jemalloc dev-deps，cargo build --release 成功且不链接 jemalloc，cargo build --tests 成功编译两个新 crate。</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: 创建 tests/jemalloc_peak.rs 并输出 v1.10 基线堆分配数值</name>
  <read_first>
    - .planning/phases/44-hotpath/44-RESEARCH.md（第 214-236 行：jemalloc 测量策略；第 330-350 行：Code Examples）
    - .planning/phases/44-hotpath/44-PATTERNS.md（第 172-191 行：measure_alloc_delta 函数模式；第 339 行：标注此文件无项目内 analog）
    - benches/bench_csv.rs（第 1-60 行：bench_csv_export 函数结构，复用 synthetic_log 与 make_config 模式作参考）
    - src/cli/run/mod.rs（确认 handle_run 签名供测试调用，复用 bench_csv.rs 的调用形式）
  </read_first>
  <files>tests/jemalloc_peak.rs</files>
  <action>
  创建新文件 tests/jemalloc_peak.rs，实现 PERF-02 的基线测量测试（D-02：jemalloc 统计接口在测试中集成）。文件结构如下：

  1. 文件顶部使用 `#[global_allocator]` 启用 jemalloc，但必须用 `cfg(test)` 隔离（Pitfall 1：避免影响 release binary）：

     #[cfg(test)]
     #[global_allocator]
     static ALLOC: tikv_jemallocator::Jemalloc = tikv_jemallocator::Jemalloc;

     注意：integration test 文件本身就是 cfg(test) 编译，但显式标注以提高可读性。

  2. 定义辅助函数 `measure_alloc_delta(f: impl FnOnce()) -> usize`（保持 ≤ 40 行，per CLAUDE.md）：
     - 使用 `tikv_jemalloc_ctl::{epoch, stats}` 模块
     - 在调用 f() 前后各调用一次 `epoch.advance()`（Pitfall 5：防止读到陈旧值）
     - 通过 `stats::allocated::mib().read()` 读取累计 allocated 字节数
     - 返回 `after.saturating_sub(before)`

  3. 定义 helper 函数 `synthetic_log(records: usize) -> String`：从 benches/bench_csv.rs 复用相同的合成日志生成逻辑（保证测量的工作负载与 criterion benchmark 一致）。包含 PARAMS 记录与 DML 记录交替的真实结构。如果直接复用 bench 内函数过于复杂，则直接在测试文件内复刻其逻辑（与 bench_csv.rs 中的 synthetic_log 字节级一致）。

  4. 定义 `#[test] fn test_jemalloc_peak_baseline()`：
     - 创建临时目录（用 tempfile::TempDir）
     - 写入 10_000 条合成日志到临时 .log 文件
     - 构造 Config（参考 bench_csv.rs make_config）
     - 调用 measure_alloc_delta 包裹 `handle_run(&cfg, true, &Arc::new(AtomicBool::new(false)), None)`
     - 使用 `println!("[phase44-before] jemalloc allocated delta (10000 records) = {} bytes", delta)` 输出基线值（注意：使用 println! 而非 log，确保 `-- --nocapture` 可见）
     - assert delta > 0（sanity check：不可能零分配）

  5. 函数命名前缀统一使用 `test_jemalloc_peak_*`，方便单独运行：
     - `cargo test test_jemalloc_peak_baseline -- --nocapture`

  6. 在测试函数顶部添加 `// PERF-02 baseline measurement — Wave 0` 注释，明确目的。

  约束：
  - 函数体每个 ≤ 40 行（CLAUDE.md 规约）
  - 不使用 unsafe（D-06）
  - 描述性变量名（user preference）
  - 文件顶部加 doc comment 说明用途与 cfg 隔离原因
  - 执行后将测试输出的 delta 数值记录到 .planning/phases/44-hotpath/44-01-SUMMARY.md（由 SUMMARY 生成时记录），供 Wave 2 验收对比
  </action>
  <verify>
    <automated>cargo test --test jemalloc_peak test_jemalloc_peak_baseline -- --nocapture 2>&1 | tee /tmp/jemalloc-baseline.log | grep -E 'phase44-before.*delta.*bytes' && cargo clippy --tests --all-targets -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    - `tests/jemalloc_peak.rs` 存在，行数 ≥ 40
    - 文件包含字符串 `#[global_allocator]`，且该行被 `#[cfg(test)]` 修饰
    - 文件包含 `static ALLOC: tikv_jemallocator::Jemalloc`
    - 文件包含 `use tikv_jemalloc_ctl::` 导入
    - 文件包含 `epoch.advance()` 调用 ≥ 2 次（前后各一次）
    - 文件包含函数 `measure_alloc_delta`，该函数体行数 ≤ 40
    - 文件包含 `#[test] fn test_jemalloc_peak_baseline`
    - `cargo test --test jemalloc_peak test_jemalloc_peak_baseline -- --nocapture` 退出码 0
    - 输出包含正则 `\[phase44-before\] jemalloc allocated delta \(10000 records\) = [1-9][0-9]+ bytes`（delta 为正整数，至少两位数）
    - `cargo clippy --tests --all-targets -- -D warnings` 通过
    - 不包含 `unsafe` 关键字（grep -c unsafe tests/jemalloc_peak.rs 返回 0；外部 crate 内部 unsafe 不计）
  </acceptance_criteria>
  <done>tests/jemalloc_peak.rs 编译通过，test_jemalloc_peak_baseline 输出 phase44-before 堆分配 delta 数值（>0 字节），clippy 无警告。</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: 保存 phase44-before criterion baseline 到 benches/baselines/</name>
  <read_first>
    - benches/BENCHMARKS.md（搜索 CRITERION_HOME 用法与 --save-baseline 语法）
    - benches/bench_csv.rs（确认 group 名称为 csv_export，参数 1000/10000/50000）
    - benches/baselines/csv_export/10000/phase33/（确认 phase33 baseline 目录结构作为模板）
    - .planning/phases/44-hotpath/44-RESEARCH.md（第 196-208 行：v1.10 基线数据；第 305-312 行：Pitfall 2 criterion baseline 路径）
  </read_first>
  <files>
    benches/baselines/csv_export/1000/phase44-before/,
    benches/baselines/csv_export/10000/phase44-before/,
    benches/baselines/csv_export/50000/phase44-before/
  </files>
  <action>
  执行 criterion 基线保存命令（D-08 验收前置：phase44-before 必须存在才能在 Wave 2 用 --baseline phase44-before 对比）。

  命令（必须使用 CRITERION_HOME 环境变量，per Pitfall 2，否则 criterion 默认写到 target/criterion，不会进入项目内 baselines/）：

    CRITERION_HOME=benches/baselines cargo bench --bench bench_csv -- --save-baseline phase44-before

  执行后验证：
  1. benches/baselines/csv_export/{1000,10000,50000}/phase44-before/ 三个目录均存在
  2. 每个目录内包含 estimates.json、sample.json、tukey.json 等 criterion 标准文件（与现有 phase33 目录结构一致）
  3. 读取 benches/baselines/csv_export/10000/phase44-before/estimates.json 的 median.point_estimate，记录该数值（单位 ns）供 Wave 2 对比

  注意：
  - 此 task 不修改任何源代码，仅运行 cargo bench 并产生 baselines/ 目录下的数据文件
  - 运行时间较长（criterion 默认每个 benchmark 100 samples × warm-up），约 3-5 分钟，属于正常预期
  - 不要尝试缩短 sample 数量或 warm-up 时间——基线必须使用 criterion 默认配置以与未来对比可比
  - 此 task 前提：Cargo.toml 已包含 jemalloc dev-dep（Task 1），但 jemalloc 不被 bench_csv.rs 使用，所以不影响 criterion 测量精度
  - 若机器负载高导致 criterion 检测到噪声警告，记录在 SUMMARY 中但不阻断 task（基线本身是采集事实，不是对比）

  完成后将记录到 SUMMARY 的数据：
  - csv_export/1000 phase44-before median (ns)
  - csv_export/10000 phase44-before median (ns)
  - csv_export/50000 phase44-before median (ns)
  </action>
  <verify>
    <automated>test -f benches/baselines/csv_export/1000/phase44-before/estimates.json && test -f benches/baselines/csv_export/10000/phase44-before/estimates.json && test -f benches/baselines/csv_export/50000/phase44-before/estimates.json && python3 -c "import json; d=json.load(open('benches/baselines/csv_export/10000/phase44-before/estimates.json')); print(f\"phase44-before 10000 median = {d['median']['point_estimate']:.0f} ns\"); assert d['median']['point_estimate'] > 0"</automated>
  </verify>
  <acceptance_criteria>
    - 三个目录 benches/baselines/csv_export/{1000,10000,50000}/phase44-before/ 均存在
    - 每个目录包含 estimates.json 文件
    - estimates.json 可被 Python json 模块解析，包含 median.point_estimate 字段且数值为正
    - 10000 样本的 median.point_estimate 在 (1_000_000, 10_000_000) ns 范围内（与 phase33 基线 2,104,371 ns 同数量级；±5x 容差应对环境波动）
    - benches/baselines/ 下不存在意外的 target/criterion 残留（CRITERION_HOME 设置正确）
    - 运行期间未修改任何 src/ 或 benches/ 代码文件（git status 仅显示新增 baselines 文件）
  </acceptance_criteria>
  <done>benches/baselines/csv_export/{1000,10000,50000}/phase44-before/ 目录存在，每个目录包含完整 criterion estimates.json，Wave 2 可使用 --baseline phase44-before 对比。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-time / dev-deps | Cargo.toml 引入两个新 crate；构建工具链消费它们 |
| test-time allocator | jemalloc 在 cfg(test) 下接管全局分配器，仅影响测试进程内存行为 |
| filesystem (baselines/) | criterion 写入项目内 benches/baselines/ 子目录 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-01 | Tampering | Cargo.toml dev-deps | mitigate | 仅使用 crates.io 已 VERIFIED 包（tikv-jemallocator/tikv-jemalloc-ctl 0.6.1，per RESEARCH.md Package Legitimacy Audit），版本号精确锁定 |
| T-44-02 | Tampering | release binary 被误链接 jemalloc | mitigate | Task 1 acceptance criteria 强制 `cargo build --release` 输出不包含 "compiling tikv-jemallocator"；dev-deps 隔离 |
| T-44-03 | Denial of Service | jemalloc cfg 错配导致生产 allocator 被替换（Pitfall 1） | mitigate | tests/jemalloc_peak.rs 使用 `#[cfg(test)]` 双重隔离（integration test + cfg attr） |
| T-44-SC | Tampering | npm/pip/cargo installs | mitigate | 本 phase 仅 cargo dev-dep 新增；两包均为 VERIFIED（TiKV 项目维护，主流 Rust jemalloc 封装）；无 ASSUMED/SUS/SLOP 包 |

注：本 phase 为纯性能优化阶段，无 I/O 边界变化、无新外部输入、无用户 API 变更，整体威胁面与既有阶段无差异。
</threat_model>

<verification>
- `cargo build --release` 通过，且不链接 jemalloc
- `cargo build --tests` 通过，两个 jemalloc crate 成功编译
- `cargo test --test jemalloc_peak test_jemalloc_peak_baseline -- --nocapture` 输出 phase44-before 堆分配 delta 数值
- `CRITERION_HOME=benches/baselines cargo bench --bench bench_csv -- --save-baseline phase44-before` 成功
- `benches/baselines/csv_export/{1000,10000,50000}/phase44-before/estimates.json` 均存在
- `cargo clippy --all-targets -- -D warnings` 通过
- `cargo fmt --check` 通过
</verification>

<success_criteria>
1. Cargo.toml 包含 tikv-jemallocator 0.6.1 与 tikv-jemalloc-ctl 0.6.1 两个 dev-deps
2. release binary 不链接 jemalloc（dev-only 隔离）
3. tests/jemalloc_peak.rs 输出 v1.10 (优化前) 堆分配基线数值（写入 SUMMARY 供 Wave 2 对比）
4. benches/baselines/csv_export/{1000,10000,50000}/phase44-before/ 三个 criterion baseline 存在
5. 全部质量门禁通过（build/test/clippy/fmt）
</success_criteria>

<output>
Create `.planning/phases/44-hotpath/44-01-SUMMARY.md` when done, recording:
- Cargo.toml diff 摘要
- jemalloc 基线 delta 数值（bytes）
- 三个 criterion phase44-before median 数值（ns 与 records/sec）
- 任何 ASSUMED 项的实际验证结果（如 tikv-jemalloc-ctl API 形态是否需要调整）
</output>
