---
phase: 69-watch
plan: 03
type: execute
wave: 3
depends_on:
  - 69-02
files_modified:
  - tests/integration.rs
autonomous: true
requirements:
  - WATCH-02
  - WATCH-05
  - WATCH-06
user_setup: []
tags:
  - rust-cli
  - integration-tests
  - watch

must_haves:
  truths:
    - "集成测试 test_watch_triggers_on_new_log_file 通过：tempdir 启动 handle_watch，spawned 线程延迟写入 .log 文件，验证 CSV 输出文件存在且行数 > header"
    - "集成测试 test_watch_exits_when_interrupted 通过：interrupted 启动前置 true，handle_watch 立即 Ok(())"
    - "集成测试 test_watch_help_lists_subcommand 通过：assert_cmd 调用 watch --help 验证子命令文案与示例"
    - "集成测试 test_watch_ignores_non_log_files 通过：写入 .txt 文件不触发 handle_run，CSV 输出不产生"
    - "cargo test 全套（lib + integration + benches）通过，无回归"
    - "cargo clippy --all-targets -- -D warnings 通过"
    - "cargo fmt --check 通过"
  artifacts:
    - path: "tests/integration.rs"
      provides: "4 个 watch e2e 测试"
      contains: "test_watch_triggers_on_new_log_file"
  key_links:
    - from: "tests/integration.rs make_run_config helper"
      to: "watch e2e tests"
      via: "复用现有 CSV config 构造方式"
      pattern: "make_run_config"
    - from: "spawned 写入线程"
      to: "handle_watch loop"
      via: "tempdir 共享 + 延迟 200ms 后写文件 + 1.5s 后设置 interrupted"
      pattern: "thread::spawn"
---

<objective>
覆盖 WATCH-02/05/06 行为的端到端集成测试，作为 Phase 69 验收门禁。本 plan 不修改产品代码（除非测试发现 bug，则回退至 Plan 02 修复）。

Purpose: 自动化验证 watch 子命令的核心行为合约，保障 Phase 70 重构有安全网。
Output: tests/integration.rs 新增 4 个 #[test] 函数；cargo test/clippy/fmt 全套绿灯。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/69-watch/69-CONTEXT.md
@.planning/phases/69-watch/69-RESEARCH.md
@.planning/phases/69-watch/69-02-SUMMARY.md

<interfaces>
<!-- handle_watch 签名（Plan 02 完成后锁定） -->
pub fn handle_watch(
    cfg: &Config,
    quiet: bool,
    verbose: bool,
    interrupted: &Arc<AtomicBool>,
) -> Result<()>;

<!-- tests/integration.rs 现有 helper（line 71-88） -->
fn make_run_config(log_dir: &std::path::Path, csv_file: &std::path::Path) -> Config {
    Config {
        sqllog: SqllogConfig {
            inputs: vec![log_dir.to_str().unwrap().to_string()],
            path_deprecated: None,
        },
        exporter: ExporterConfig {
            csv: Some(CsvExporterConfig {
                file: csv_file.to_str().unwrap().to_string(),
                overwrite: true,
                append: false,
                ..CsvExporterConfig::default()
            }),
            ..Default::default()
        },
        ..Default::default()
    }
}

<!-- 现有 imports（tests/integration.rs 顶部） -->
use std::sync::Arc;
use std::sync::atomic::{AtomicBool, Ordering};
use sqllog2db::cli::run::handle_run;
use sqllog2db::cli::watch::handle_watch;  // Plan 02 后新增导入

<!-- 已有的 assert_cmd 模式（tests/integration.rs:1315+） -->
use assert_cmd::Command;
let mut cmd = Command::cargo_bin("sqllog2db").unwrap();
cmd.arg("watch").arg("--help").assert().success();
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: tests/integration.rs 新增 4 个 watch e2e 测试覆盖 WATCH-02/05/06</name>
  <files>tests/integration.rs</files>
  <read_first>
    - tests/integration.rs（lines 1-110，imports + make_run_config helper）
    - tests/integration.rs（lines 1383-1500，assert_cmd help 测试参考模式）
    - src/cli/watch.rs（Plan 02 完成后的实现，确认 handle_watch 公开 API）
    - .planning/phases/69-watch/69-RESEARCH.md（"Code Examples > 集成测试骨架" + "Common Pitfalls > Pitfall 5 时序"）
    - .planning/phases/69-watch/69-CONTEXT.md（D-19/D-20 测试策略）
    - Cargo.toml（确认 lib name；测试通过 `use sqllog2db::cli::watch::handle_watch;` 引入）
  </read_first>
  <action>
    在 tests/integration.rs 末尾追加一个新的子模块 `mod watch_tests { ... }`，包含 4 个 #[test] 函数，全部使用 `tempfile::TempDir` 隔离副作用。如缺少 `use` 语句，在该 mod 顶部加入：
    - `use std::sync::Arc;`
    - `use std::sync::atomic::{AtomicBool, Ordering};`
    - `use sqllog2db::cli::watch::handle_watch;`
    - `use super::make_run_config;`（若 mod 内可见）或在调用处使用 `crate::make_run_config`，按 tests/integration.rs 结构调整

    辅助：在 mod 内提供 `fn write_minimal_log(path: &std::path::Path)`，向 path 写入一行最小可解析的 DM 日志文本（参考 tests/integration.rs 现有 write_test_log/write_minimal_log helper，若 helper 已在 outer scope 则直接复用 `super::` 路径）。若现有 helper 不存在最小行，则 inline 写 5 行符合 DM 日志格式的内容。

    四个测试：

    (a) `#[test] fn test_watch_help_lists_subcommand()` — 覆盖 WATCH-01 可发现性：
      - 使用 `assert_cmd::Command::cargo_bin("sqllog2db").unwrap()`
      - 执行 `watch --help`
      - 断言 stdout 包含 "TOML configuration file path"
      - 断言 stdout 包含 "sqllog2db watch -c config.toml"
      - 断言 success() 退出码

    (b) `#[test] fn test_watch_exits_when_interrupted()` — 覆盖 WATCH-06 Ctrl+C 优雅退出：
      - 创建 `tempfile::TempDir`，子目录 `logs/`（mkdir），csv 输出路径
      - `let cfg = make_run_config(&log_dir, &csv_file);`
      - `let interrupted = Arc::new(AtomicBool::new(true));`（预先置 true）
      - 调用 `let result = handle_watch(&cfg, /*quiet=*/true, /*verbose=*/false, &interrupted);`
      - 断言 `result.is_ok()`（quiet=true 抑制摘要 stderr，避免污染测试输出）

    (c) `#[test] fn test_watch_triggers_on_new_log_file()` — 覆盖 WATCH-02/05 触发与累计：
      - tempdir + log_dir + csv_file（同 b）
      - `let cfg = make_run_config(&log_dir, &csv_file);`
      - `let interrupted = Arc::new(AtomicBool::new(false));`
      - `let interrupted_clone = Arc::clone(&interrupted);`
      - `let log_dir_clone = log_dir.clone();`
      - `std::thread::spawn(move || { std::thread::sleep(Duration::from_millis(300)); write_minimal_log(&log_dir_clone.join("new_file.log")); std::thread::sleep(Duration::from_millis(1500)); interrupted_clone.store(true, Ordering::Relaxed); });`
      - 主线程：`handle_watch(&cfg, true, false, &interrupted).unwrap();`
      - 断言 `csv_file` 存在；读取内容，断言行数 > 1（header + 至少 1 条数据行）
      - 整测试设置 `Duration` 字符使用 `std::time::Duration`
      - 此测试可能在慢速 CI 环境下 flaky，按 RESEARCH Pitfall 5 建议：300ms 写入延迟 + 1.5s 间隔 + 写入后 sleep 缓冲

    (d) `#[test] fn test_watch_ignores_non_log_files()` — 覆盖 WATCH-02 扩展名过滤：
      - tempdir + log_dir + csv_file
      - 同 c 的 spawn 模式，但写入 `garbage.txt` 而非 `.log` 文件
      - 主线程 handle_watch 后断言 `!csv_file.exists()` 或 `csv_file 不存在/为空（行数 0）`
      - interrupted 在 1.0s 后置 true（不需要等待长时间）

    平台与 CI 注意事项：
    - macOS kqueue backend 可能产生 `Create(Any)` 而非 `Create(File)`，Plan 02 已用 `Create(_)` 全捕获
    - 慢速 CI 可放宽至 sleep(500ms) 写入 + sleep(2.5s) interrupted；本 plan 默认值（300/1500）覆盖正常机器
    - 所有测试设置 `#[ignore]` 标记若实测 flaky；优先不加，CI 失败再回头

    禁止改动 src/ 任何文件（若测试暴露 bug，回退至 Plan 02 修复并重跑此 plan）。
  </action>
  <verify>
    <automated>cargo test --test integration watch_tests:: -- --test-threads=1 --nocapture 2>&1 | tail -20 ; cargo test 2>&1 | tail -10 ; cargo clippy --all-targets -- -D warnings 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'fn test_watch_help_lists_subcommand' tests/integration.rs` 输出 1
    - `grep -c 'fn test_watch_exits_when_interrupted' tests/integration.rs` 输出 1
    - `grep -c 'fn test_watch_triggers_on_new_log_file' tests/integration.rs` 输出 1
    - `grep -c 'fn test_watch_ignores_non_log_files' tests/integration.rs` 输出 1
    - `cargo test --test integration test_watch_help_lists_subcommand` 退出码 0
    - `cargo test --test integration test_watch_exits_when_interrupted` 退出码 0
    - `cargo test --test integration test_watch_triggers_on_new_log_file -- --test-threads=1` 退出码 0
    - `cargo test --test integration test_watch_ignores_non_log_files -- --test-threads=1` 退出码 0
    - `cargo test` 全套（包含现有 740+ 测试）退出码 0，无任何 FAILED 行
    - `cargo clippy --all-targets -- -D warnings` 退出码 0
    - `cargo fmt --check` 退出码 0
  </acceptance_criteria>
  <done>
    4 个 watch e2e 测试通过，覆盖 WATCH-01/02/05/06 关键行为；全套 cargo test 无回归；clippy/fmt 通过；Phase 69 行为契约自动化验证就绪。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 测试线程 → 测试目录 | 测试创建临时目录与文件，由 tempfile 在 drop 时清理 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-69-03-T | Tampering | 测试 tempdir 隔离 | mitigate | 使用 tempfile::TempDir，自动 RAII 清理；不写入项目外固定路径 |
| T-69-03-D | DoS | flaky 测试因 CI 时序导致频繁失败 | mitigate | 写入延迟 300ms + interrupted 1.5s + 不并行（--test-threads=1）；缺乏 channel 信号同步是已知限制（RESEARCH A4） |
| T-69-03-I | Information Disclosure | 测试输出泄露 | accept | quiet=true 抑制 stderr 摘要；assert_cmd 输出已截断 |
</threat_model>

<verification>
- `cargo test --test integration watch_tests::` 4/4 通过
- `cargo test` 全套通过（包含 lib + integration + bench tests）
- `cargo clippy --all-targets -- -D warnings` 退出码 0
- `cargo fmt --check` 退出码 0
- 手动检查 `cargo test` 输出统计行 `test result: ok. N passed; 0 failed`
</verification>

<success_criteria>
- WATCH-01 行为可自动化验证（help 子命令测试）
- WATCH-02 行为可自动化验证（新文件触发 + 非 log 忽略）
- WATCH-05 累计行数验证（CSV 输出行数 > header）
- WATCH-06 优雅退出验证（interrupted 后 Ok(())）
- Phase 69 全套质量门禁通过；ROADMAP Phase 69 success criteria SC1-SC4 行为对齐
</success_criteria>

<output>
Create `.planning/phases/69-watch/69-03-SUMMARY.md` when done, covering: tests added with their requirement coverage table, full cargo test pass count, clippy/fmt status, links to follow-up work (Phase 70 ready).
</output>
