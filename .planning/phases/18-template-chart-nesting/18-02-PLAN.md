---
phase: 18-template-chart-nesting
plan: 02
type: execute
wave: 2
depends_on:
  - 18-01
files_modified:
  - src/cli/run.rs
  - src/main.rs
  - src/cli/validate.rs
  - src/cli/show_config.rs
  - src/cli/stats.rs
  - src/exporter/mod.rs
  - src/exporter/csv.rs
  - src/exporter/sqlite.rs
  - src/pipeline/filters.rs
autonomous: true
requirements:
  - CONFIG-03
  - CONFIG-04

must_haves:
  truths:
    - "cargo build --release 在本 Plan 完成后全局成功（无未解析的 cfg.pipeline.* 引用）"
    - "cargo clippy --all-targets -- -D warnings 零警告"
    - "cargo test --lib 退出码 0（含 cli::, exporter::, pipeline::filters:: 等所有模块）"
    - "热路径 do_template 从 cfg.template.as_ref().is_some_and(|t| t.enable) 读取"
    - "热路径 do_normalize 从 cfg.replace_parameters 读取"
    - "charts 生成代码从 cfg.charts.as_ref() 读取（而非 cfg.pipeline.charts.as_ref()）"
    - "字段投影热路径从 cfg.output 读取 field_mask / ordered_field_indices"
    - "FilterProcessor / scan_log_file_for_matches / recompile_meta_if_needed 等 6 个 filter 相关函数从 cfg.filter 读取（而非 cfg.pipeline.filters）"
    - "write_template_stats 接受 csv_output_path 与 sqlite_table_name 两个显式参数（替代原 final_path: Option<&Path>），空串/None 时 exporter 跳过写入"
    - "src/pipeline/filters.rs 内 compile_patterns 字段路径前缀从 pipeline.filters.* 改为 filter.*"
  artifacts:
    - path: "src/cli/run.rs"
      provides: "热路径 + 顺序/并行处理调用方迁移到新顶层配置 + write_template_stats 显式路径"
    - path: "src/exporter/mod.rs"
      provides: "Exporter trait write_template_stats 签名升级为 (csv_path: Option<&str>, sqlite_table: Option<&str>)"
    - path: "src/main.rs"
      provides: "apply_date_range 与 main 流程使用 cfg.filter（不再 cfg.pipeline.filters）"
  key_links:
    - from: "src/cli/run.rs handle_run / process_csv_parallel"
      to: "cfg.template / cfg.charts / cfg.replace_parameters / cfg.filter / cfg.output"
      via: "直接字段访问"
      pattern: "cfg\\.(template|charts|replace_parameters|filter|output)"
    - from: "src/cli/run.rs handle_run"
      to: "src/exporter/mod.rs write_template_stats"
      via: "传入 cfg.template.output_csv_path 与 cfg.template.output_sqlite_table"
      pattern: "write_template_stats\\("
---

<objective>
将 Phase 18 顶层化后的 Config 结构应用到所有非 init 的调用方：CLI 处理器（run / validate / show_config / stats）、main 入口、Exporter trait 与实现（csv / sqlite）。

同步完成两件事：
1. 路径重命名：所有 `cfg.pipeline.{filters,normalize,template_analysis,charts,fields}` 引用切换到 `cfg.{filter,replace_parameters,template,charts,output}`
2. D-04 行为变更：`write_template_stats` 由 trait 默认推导 companion 文件改为接受显式 `Option<&str>` 路径与表名，由调用方根据 `cfg.template.output_csv_path` / `output_sqlite_table` 传入（空串 = 不生成）

最终目标：全局 `cargo build` 编译成功，`cargo clippy --all-targets -- -D warnings` 零警告，`cargo test --lib` 全通过。
此 Plan 完成后，唯一遗留是 `src/cli/init.rs` 模板还输出旧格式（Plan 03 处理）+ `tests/integration.rs` 端到端断言（Plan 03 处理）。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/18-template-chart-nesting/18-CONTEXT.md
@.planning/phases/18-template-chart-nesting/18-PATTERNS.md
@.planning/phases/18-template-chart-nesting/18-01-PLAN.md
@.planning/phases/18-template-chart-nesting/18-01-SUMMARY.md

<interfaces>
Plan 01 完成后可用的新顶层结构（来自 src/config/mod.rs）：

```rust
pub struct Config {
    pub sqllog: SqllogConfig,
    pub logging: LoggingConfig,
    pub replace_parameters: Option<NormalizeConfig>,
    pub template: Option<TemplateConfig>,
    pub filter: Option<FiltersFeature>,
    pub charts: Option<ChartsConfig>,
    pub output: Option<OutputConfig>,
    pub exporter: ExporterConfig,
    pub resume: ResumeConfig,
    // private _pipeline_deprecated
}

// from src/pipeline/mod.rs
pub struct TemplateConfig {
    pub enable: bool,
    pub output_csv_path: String,         // "" = 不生成
    pub output_sqlite_table: String,     // "" = 不生成
}
pub struct OutputConfig {
    pub fields: Option<Vec<String>>,
}
impl OutputConfig {
    pub fn field_mask(&self) -> FieldMask;
    pub fn ordered_field_indices(&self) -> Vec<usize>;
}
```

当前 Exporter trait write_template_stats 签名（即将被替换）— src/exporter/mod.rs:49:

```rust
fn write_template_stats(
    &mut self,
    stats: &[crate::pipeline::TemplateStats],
    final_path: Option<&std::path::Path>,
) -> Result<()>;
```

热路径关键位置：
- src/cli/run.rs:29 build_pipeline 引用 cfg.pipeline.filters
- src/cli/run.rs:340 scan_log_file_for_matches 引用 cfg.pipeline.filters
- src/cli/run.rs:673 recompile_meta_if_needed 引用 final_cfg.pipeline.filters
- src/cli/run.rs:728-742 pre-scan 路径引用 cfg.pipeline.filters
- src/cli/run.rs:748-768 do_normalize / do_template / placeholder_override
- src/cli/run.rs:769-776 sql_record_filter 计算
- src/cli/run.rs:813-816 / 920-923 charts 生成（两处相同）
- src/cli/run.rs:822-828 / 932-935 write_template_stats 调用

filters.rs 内字段路径常量（共 16 处 compile_patterns "pipeline.filters.*" 字符串）— src/pipeline/filters.rs:208, 224, 395-441, 580-587。
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 升级 Exporter trait + CsvExporter / SqliteExporter write_template_stats 签名</name>
  <files>src/exporter/mod.rs, src/exporter/csv.rs, src/exporter/sqlite.rs</files>
  <read_first>
    - src/exporter/mod.rs（Exporter trait, ExporterKind, ExporterManager, DryRunExporter）
    - src/exporter/csv.rs（lines 25 build_companion_path、lines 565-580 write_template_stats 实现、lines 1057+ 测试）
    - src/exporter/sqlite.rs（lines 409-456 write_template_stats 实现 + lines 956+ 测试）
    - src/pipeline/mod.rs（Plan 01 后的 TemplateStats 类型）
    - .planning/phases/18-template-chart-nesting/18-CONTEXT.md（D-04 行为变更说明）
  </read_first>
  <behavior>
    - 测试 1（CSV，新增）：调用 `exporter.write_template_stats(stats, Some("/tmp/path/out.csv"), None)`，预期写入 `/tmp/path/out.csv`，无 companion 推导
    - 测试 2（CSV，新增）：调用 `exporter.write_template_stats(stats, None, None)`，预期不创建任何文件，返回 Ok(())
    - 测试 3（CSV，替换原 test_csv_write_template_stats）：传入显式路径，验证文件内容首行是 header `template_key,count,avg_us,...,last_seen`
    - 测试 4（SQLite，新增）：调用 `exporter.write_template_stats(stats, None, Some("my_templates"))`，预期建表 `my_templates`，COUNT(*) 等于 stats.len()
    - 测试 5（SQLite，新增）：调用 `exporter.write_template_stats(stats, None, None)`，预期不建任何表，返回 Ok(())
    - 测试 6（DryRun，新增）：调用 `exporter.write_template_stats(stats, Some("..."), Some("..."))`，记录 info 日志，不创建文件，返回 Ok(())
  </behavior>
  <action>
    第一步 — 修改 src/exporter/mod.rs Exporter trait（lines 49-56）：
    - 将方法签名改为：
        ```
        fn write_template_stats(
            &mut self,
            stats: &[crate::pipeline::TemplateStats],
            csv_output_path: Option<&str>,
            sqlite_table_name: Option<&str>,
        ) -> Result<()>
        ```
    - 默认实现保持 `Ok(())` no-op，绑定参数下划线消除未使用警告

    第二步 — 修改 src/exporter/mod.rs ExporterKind::write_template_stats（lines 119-129）与 ExporterManager::write_template_stats（lines 313-319）：
    - 两个 wrapper 方法的签名同步升级为 `(stats, csv_output_path, sqlite_table_name)`
    - 内部 match 分支按原结构透传到具体 exporter

    第三步 — 修改 src/exporter/mod.rs DryRunExporter::write_template_stats（lines 199-209）：
    - 签名升级为新格式
    - 内部 info! 日志改为同时打印两个参数（csv_output_path / sqlite_table_name），形如 `Dry-run: would write {N} template stats (csv={:?}, sqlite_table={:?})`

    第四步 — 修改 src/exporter/csv.rs CsvExporter::write_template_stats（lines 569-579）：
    - 签名升级为 `(stats, csv_output_path: Option<&str>, _sqlite_table_name: Option<&str>)`
    - 逻辑：当 `csv_output_path` 为 None 或空串（trim 后判断）时直接返回 Ok(())，不写文件、不打印 info
    - 当 `csv_output_path` 为 Some(path) 且非空时：用 path 构造 Path，**不再调用 build_companion_path**（显式路径已是最终输出位置）；继续调用 `write_companion_rows(path, stats)?`
    - 函数名 build_companion_path 与 write_companion_rows 保留导出（D-04 路径推导从调用方剥离后，build_companion_path 仍可能在测试中使用；保留为 `pub(crate)` 不动）
    - 删除 info! 日志中 `Template companion CSV written` 字样，改为 `Template stats CSV written: {path}`

    第五步 — 修改 src/exporter/sqlite.rs SqliteExporter::write_template_stats（lines 409-456）：
    - 签名升级为 `(stats, _csv_output_path: Option<&str>, sqlite_table_name: Option<&str>)`
    - 逻辑：当 `sqlite_table_name` 为 None 或空串时直接返回 Ok(())，不创建任何表
    - 当 `sqlite_table_name` 为 Some(name) 且非空时：在 DROP / CREATE / INSERT 语句中将硬编码的 `sql_templates` 替换为 `name`
    - 注意 SQL identifier 注入风险：直接拼接 `name` 到 SQL 字符串。由于 Config::validate() 链路上 sqlite.table_name 已用 `is_valid_ascii_identifier` 校验，本字段需复用同样校验逻辑。但 SQLite exporter 测试可能传入任意 name；为隔离风险，本任务**仅做空串短路**，identifier 合法性校验由 Plan 03 在 config validate 中补强（添加 `validate_template_sqlite_table_name`）。在 sqlite.rs 内部用 `rusqlite::named_params` 或 `format!` 拼接均可，**但 table_name 不能用 ? 占位符（SQLite 不允许）**，必须 format!；为防止注入，先做轻量 assert：`if name.is_empty() || name.chars().any(|c| !c.is_ascii_alphanumeric() && c != '_')` 返回 InvalidValue 错误
    - info! 日志中 `sql_templates: {} rows` 改为 `{}: {} rows` 输出实际 name

    第六步 — 更新 src/exporter/csv.rs 与 src/exporter/sqlite.rs 的单元测试：
    - csv.rs lines 1057+ test_csv_write_template_stats：
        * 改为传入 `Some("/tmp/.../out_templates.csv")` 显式路径，断言该路径文件存在
        * 新增 test_csv_write_template_stats_none_skips：传入 None，断言无文件创建
        * 新增 test_csv_write_template_stats_empty_path_skips：传入 `Some("")`，断言无文件创建
        * 删除 lines 1173 附近 final_path Some 测试中对 build_companion_path 推导的依赖
    - sqlite.rs lines 956+ test_sqlite_write_template_stats：
        * 改为传入 `Some("sql_templates")`，断言 `SELECT COUNT(*) FROM sql_templates` 等于 stats.len()
        * 新增 test_sqlite_write_template_stats_none_skips：传入 `(None, None)`，断言 sqlite_master 中无 sql_templates 表
        * 新增 test_sqlite_write_template_stats_custom_table：传入 `Some("custom_tpl")`，断言该表存在且行数正确
        * 新增 test_sqlite_write_template_stats_invalid_name_rejected：传入 `Some("bad name;DROP")`，断言返回 Err（identifier 校验）
        * 现有 test_sqlite_overwrite_clears_existing_data / 等所有调用 .write_template_stats(&stats, None) 的位置都改为 `.write_template_stats(&stats, None, Some("sql_templates"))`

    第七步 — 接口扩散：本任务仅修改 exporter 模块内部签名。所有外部调用方（cli/run.rs 中两处 `exporter_manager.write_template_stats(stats, None)`）会编译失败，由 Task 2 修复。
  </action>
  <verify>
    <automated>cargo test --lib exporter:: 2>&amp;1 | tail -50</automated>
  </verify>
  <acceptance_criteria>
    - 源断言：`grep -nE 'fn write_template_stats' src/exporter/mod.rs src/exporter/csv.rs src/exporter/sqlite.rs` 显示所有签名都包含 `csv_output_path` 与 `sqlite_table_name` 两个 Option 参数（且不再包含 `final_path: Option<&std::path::Path>`）
    - 源断言：`grep -n 'build_companion_path' src/exporter/csv.rs` 仍命中（保留），但 `write_template_stats` 函数体内不再调用它
    - 源断言：`grep -n '"sql_templates"' src/exporter/sqlite.rs` 仅出现在测试代码中（生产代码使用动态 sqlite_table_name 参数）
    - 测试命令：`cargo test --lib -- exporter::` 退出码 0
    - 测试命令：`cargo test --lib exporter::csv::tests::test_csv_write_template_stats_none_skips exporter::csv::tests::test_csv_write_template_stats_empty_path_skips exporter::sqlite::tests::test_sqlite_write_template_stats_none_skips exporter::sqlite::tests::test_sqlite_write_template_stats_custom_table exporter::sqlite::tests::test_sqlite_write_template_stats_invalid_name_rejected` 退出码 0
    - 行为断言：在临时目录运行 sqlite 写入 `Some("custom_tpl")` 后，`sqlite3 {db} "SELECT name FROM sqlite_master WHERE type='table'"` 输出包含 `custom_tpl`
  </acceptance_criteria>
  <done>Exporter trait 与三个实现（Csv / Sqlite / DryRun）签名升级；CSV 空路径跳过；SQLite 空表名跳过 + 非法 identifier 拒绝；新旧单元测试全部通过；exporter 模块本身 cargo test --lib exporter:: 成功。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 迁移所有调用方 — run.rs / main.rs / validate.rs / show_config.rs / stats.rs / filters.rs 字段路径</name>
  <files>src/cli/run.rs, src/main.rs, src/cli/validate.rs, src/cli/show_config.rs, src/cli/stats.rs, src/pipeline/filters.rs</files>
  <read_first>
    - src/cli/run.rs（全部 1255 行 — 至少完整 handle_run + process_csv_parallel + scan_log_file_for_matches + build_pipeline + recompile_meta_if_needed + 测试模块）
    - src/main.rs lines 1-100（apply_date_range, run() 顶部 cfg 加载流程）
    - src/cli/validate.rs（全部 77 行）
    - src/cli/show_config.rs（全部 323 行）
    - src/cli/stats.rs lines 240-290（filter_cfg 引用段）
    - src/pipeline/filters.rs lines 200-230（warn! 字符串）+ lines 390-445（compile_patterns 字段路径）+ lines 575-590（CompiledSqlFilters compile 路径）+ lines 1304-1502（测试 toml 字符串）
    - src/pipeline/mod.rs（Plan 01 后 OutputConfig::field_mask / ordered_field_indices 签名）
    - src/exporter/mod.rs（Task 1 后 write_template_stats 签名）
    - .planning/phases/18-template-chart-nesting/18-PATTERNS.md（cli/run.rs 改造模式段 lines 224-305）
  </read_first>
  <behavior>
    - 编译：`cargo build --release` 全局成功
    - Clippy：`cargo clippy --all-targets -- -D warnings` 零警告
    - 测试 1（src/cli/run.rs 已有）：test_aggregator_disabled_none_path / test_template_stats_enabled_end_to_end_sequential / test_no_template_stats_when_disabled / test_parallel_merge_consistent / test_include_performance_metrics_false_csv_excludes_pm_columns —— 五条均通过；toml 字符串中 `[pipeline.template_analysis]\nenabled = true` 改为 `[template]\nenable = true\noutput_csv_path = "{path_to_companion_csv}"` 以保留原伴随文件断言意图
    - 测试 2（filters.rs 已有 18 条测试中含 toml 字符串的 7 条）：[pipeline.filters] → [filter]、cfg.pipeline.filters → cfg.filter 路径全部更新后通过
    - 测试 3（main.rs lines 432-460）：apply_date_range 单元测试更新为读取 `cfg.filter`
  </behavior>
  <action>
    第一步 — src/cli/run.rs 全文件改造：
    - lines 9-11 use 语句：保留对 pipeline 内部子模块（filters, normalizer）的引用；删除 `CompiledMetaFilters, CompiledSqlFilters, FieldMask, LogProcessor, Pipeline, TemplateAggregator` 中已迁移至 OutputConfig 的间接依赖（实际这些 type 都在 pipeline 顶层，保持 use 不变）
    - lines 29-35 build_pipeline 函数体：`cfg.pipeline.filters.as_ref()` → `cfg.filter.as_ref()`
    - lines 340-343 scan_log_file_for_matches：`cfg.pipeline.filters` → `cfg.filter`
    - lines 673-684 recompile_meta_if_needed：`final_cfg.pipeline.filters` → `final_cfg.filter`
    - lines 727-742 pre-scan 路径：
        * `cfg.pipeline.filters.as_ref().is_some_and(crate::pipeline::FiltersFeature::has_transaction_filters)` → `cfg.filter.as_ref().is_some_and(crate::pipeline::FiltersFeature::has_transaction_filters)`
        * `tmp.pipeline.filters` → `tmp.filter`
    - lines 750-768 热路径变量：
        * `final_cfg.pipeline.field_mask()` → `final_cfg.output.as_ref().map_or(FieldMask::ALL, OutputConfig::field_mask)`（保留 None=全字段语义）
        * `final_cfg.pipeline.ordered_field_indices()` → `final_cfg.output.as_ref().map_or_else(|| (0..crate::pipeline::FIELD_NAMES.len()).collect(), OutputConfig::ordered_field_indices)`
        * `final_cfg.pipeline.normalize.as_ref().is_none_or(|r| r.enable)` → `final_cfg.replace_parameters.as_ref().is_none_or(|r| r.enable)`
        * `final_cfg.pipeline.template_analysis.as_ref().is_some_and(|t| t.enabled)` → `final_cfg.template.as_ref().is_some_and(|t| t.enable)`
        * `final_cfg.pipeline.normalize.as_ref().and_then(...)` → `final_cfg.replace_parameters.as_ref().and_then(...)`
    - lines 769-776 sql_record_filter：`final_cfg.pipeline.filters.as_ref()` → `final_cfg.filter.as_ref()`
    - lines 813-816 / 920-923 charts 生成两处：`final_cfg.pipeline.charts.as_ref()` → `final_cfg.charts.as_ref()`
    - lines 819-828 并行路径 write_template_stats 段：
        * 移除现有 lines 822-827 的 `if let Some(csv_cfg) = final_cfg.exporter.csv.as_ref() { ... write_companion_rows(&companion, stats)? }` 的内联逻辑
        * 替换为：调用 `exporter_manager.write_template_stats(stats, csv_out_path, sqlite_table)?`，其中：
            - 受 do_template 控制：上下文中 parallel_agg 已是 `Option<TemplateAggregator>`，将 `template_stats` 推导逻辑保留
            - csv_out_path: 当 `final_cfg.template.as_ref().is_some_and(|t| !t.output_csv_path.trim().is_empty())` 时为 `Some(&t.output_csv_path)`，否则 None
            - sqlite_table: 同理对 `output_sqlite_table`
        * 注意并行路径当前的 exporter_manager 在并行任务内是各自的；最终写出 template stats 时需构建一次主 ExporterManager 用于 stats 写入。**简化方案**：在 parallel agg 完成后，调用顶层 `ExporterManager::from_config(final_cfg)?` 创建临时实例（不 initialize 主数据流），仅用于 write_template_stats 调用；或者复用现有 `exporter::csv::write_companion_rows` 直接写 CSV（仅当 csv_out_path 非空时）。**实施时优先**：调用方仍以 `crate::exporter::csv::write_companion_rows(Path::new(csv_path), stats)?` 直接处理 csv 输出，并在有 sqlite 配置 + sqlite_table 时 fall through 到 SqliteExporter（构造新实例并初始化、写表、finalize）。具体策略由实现选择，二者皆可。
    - lines 932-935 顺序路径 write_template_stats：
        * `exporter_manager.write_template_stats(stats, None)?` → `exporter_manager.write_template_stats(stats, csv_out_path, sqlite_table)?`
        * csv_out_path / sqlite_table 计算同上
    - lines 985-1255 测试模块：
        * `[pipeline.template_analysis]\nenabled = true` → 全部替换为 `[template]\nenable = true\noutput_csv_path = "{推导路径}"`（用 dir.path().join("out_templates.csv") 等显式路径填入；test_template_stats_enabled_end_to_end_sequential 的 companion_path 断言改为读取 output_csv_path 给定路径）
        * test_no_template_stats_when_disabled 保持不变（不设置 [template]，等价于 None）
        * `[pipeline.template_analysis]\nenabled = true` 在 test_parallel_merge_consistent 中也需更新

    第二步 — src/main.rs 改造：
    - lines 82-90 apply_date_range：`cfg.pipeline.filters.get_or_insert_with(...)` → `cfg.filter.get_or_insert_with(...)`；其余逻辑不变
    - lines 432-460 单元测试（apply_date_range tests）：`cfg.pipeline.filters` → `cfg.filter`（4 处）

    第三步 — src/cli/validate.rs 改造：
    - lines 10-16：`cfg.pipeline.normalize` → `cfg.replace_parameters`；info 字符串中 `pipeline.normalize` → `replace_parameters`
    - lines 17-60：`cfg.pipeline.filters` → `cfg.filter`；info 字符串中 `features.filters` → `filter`（注意：现有代码中竟出现 `features.filters`，是历史遗留，统一修正为 `filter`）

    第四步 — src/cli/show_config.rs 改造（lines 87-212）：
    - `cfg.pipeline.normalize` → `cfg.replace_parameters`，标签 `[pipeline.normalize]` → `[replace_parameters]`
    - `cfg.pipeline.filters` → `cfg.filter`，标签 `[pipeline.filters]` → `[filter]`
    - `cfg.pipeline.template_analysis` → `cfg.template`，标签 `[pipeline.template_analysis]` → `[template]`，kv `enabled` → `enable`；新增打印 `output_csv_path` 和 `output_sqlite_table`（仅当非空时）
    - `cfg.pipeline.charts` → `cfg.charts`，标签 `[pipeline.charts]` → `[charts]`
    - lines 230-322 测试：
        * 删除测试中所有 `PipelineConfig { ... }` 显式构造，改为单字段赋值（cfg.template = Some(...), cfg.filter = Some(...), cfg.replace_parameters = Some(...)）
        * `use crate::pipeline::{FiltersFeature, NormalizeConfig, PipelineConfig}` → `use crate::pipeline::{FiltersFeature, NormalizeConfig, TemplateConfig}`

    第五步 — src/cli/stats.rs 改造：
    - lines 258：`cfg.pipeline.filters.as_ref()` → `cfg.filter.as_ref()`

    第六步 — src/pipeline/filters.rs 路径字符串改造：
    - lines 208 / 224 warn! 字符串：`[pipeline.filters]` → `[filter]`，`[pipeline.filters.include]` → `[filter.include]`，`[pipeline.filters.exclude]` → `[filter.exclude]`
    - lines 394-441 compile_patterns 调用：所有 `"pipeline.filters.include.users"`, `"pipeline.filters.include.ips"`, …, `"pipeline.filters.exclude.tags"` 共 14 处的 field 字符串前缀 `pipeline.filters.` → `filter.`
    - lines 580-587 CompiledSqlFilters：`"pipeline.filters.record_sql.includes"` / `"pipeline.filters.record_sql.excludes"` → `"filter.record_sql.includes"` / `"filter.record_sql.excludes"`
    - lines 1304-1502 测试模块所有 toml 字符串：
        * `[pipeline.filters]` → `[filter]`（出现于 test_filters_toml_deserialization_with_trxids_and_exec_ids / test_new_nested_format_include / test_new_nested_format_exclude / test_backward_compat_flat_format / test_sql_filters_alias_backward_compat / test_sql_filters_new_field_names / test_mixed_format_new_format_wins_and_warns 共 7 条）
        * 同时 `[pipeline.filters.include]` / `[pipeline.filters.exclude]` / `[pipeline.filters.indicators]` / `[pipeline.filters.sql]` → 对应去掉 pipeline. 前缀
        * `cfg.pipeline.filters.unwrap()` → `cfg.filter.unwrap()`（共 7 处）
  </action>
  <verify>
    <automated>cargo build --release 2>&amp;1 | tail -20 &amp;&amp; cargo clippy --all-targets -- -D warnings 2>&amp;1 | tail -30 &amp;&amp; cargo test --lib 2>&amp;1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - 编译断言：`cargo build --release` 退出码 0
    - Clippy 断言：`cargo clippy --all-targets -- -D warnings` 退出码 0
    - 单测断言：`cargo test --lib` 退出码 0
    - 源断言（无残留 pipeline. 字段访问）：`grep -nE 'cfg\.pipeline\.(filters|normalize|template_analysis|charts|fields)|final_cfg\.pipeline\.(filters|normalize|template_analysis|charts|fields)' src/cli/ src/main.rs src/exporter/` 不命中
    - 源断言（filters.rs 字段路径）：`grep -n '"pipeline\\.filters\\.' src/pipeline/filters.rs` 不命中
    - 源断言（filters.rs toml 测试）：`grep -n '\\[pipeline\\.filters' src/pipeline/filters.rs` 不命中
    - 源断言（cli/run.rs charts 引用）：`grep -nE 'final_cfg\.charts\.as_ref\(\)' src/cli/run.rs` 命中两处（顺序路径 + 并行路径）
    - 源断言（cli/run.rs write_template_stats 显式路径）：`grep -nE 'write_template_stats\(' src/cli/run.rs` 命中至少两处，且每处传入三个参数（stats + csv_out_path + sqlite_table 或等价表达式）
    - 行为断言（端到端）：执行 `cargo test --lib cli::run::tests::test_template_stats_enabled_end_to_end_sequential` 退出码 0；其中 output_csv_path 指定路径文件存在且首行匹配 `template_key,count,avg_us,min_us,max_us,p50_us,p95_us,p99_us,first_seen,last_seen`
  </acceptance_criteria>
  <done>cli/run.rs 全部 cfg.pipeline.* 引用改为顶层；main.rs / validate.rs / show_config.rs / stats.rs 同步；filters.rs 内部错误消息与测试 toml 全部去除 pipeline. 前缀；cargo build / clippy / test 三联全通过。</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user → CLI args (`--set key=value`) | 用户通过 --set 注入配置覆盖；apply_one 必须拒绝旧 pipeline.* 路径（Plan 01 已处理） |
| user → `template.output_sqlite_table` | 用户提供 SQLite 表名直接拼接到 DDL；必须做 identifier 校验 |
| 旧 exporter trait 实现 → 新签名 | trait 签名升级是破坏性改动；trait 默认实现保留向后兼容（no-op） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-05 | Tampering / Injection | SqliteExporter::write_template_stats 拼接 sqlite_table_name 到 DDL | mitigate | Task 1 第五步：在拼接前用 `name.chars().all(\|c\| c.is_ascii_alphanumeric() \|\| c == '_')` 校验，非法返回 `ConfigError::InvalidValue`；防止 SQL 注入 |
| T-18-06 | Information Disclosure | template.output_csv_path 用户指定任意路径 | accept | 运行时进程权限决定；写入失败由 IO 错误向上回流，无静默路径越权 |
| T-18-07 | DoS | output_csv_path 在并行模式下被多个 rayon 任务并发写入 | mitigate | 并行路径已有 parts_dir / merged_agg 隔离；write_template_stats 只在主线程 merged_agg 完成后调用一次 |
| T-18-08 | Repudiation | cfg.filter 改名后旧代码静默读取错位字段 | mitigate | Plan 01 已在 validate() 加入旧 [pipeline] 检测；本 Plan 通过 cargo build + clippy 强制所有调用方迁移，无静默错位 |
| T-18-SC | Tampering | 包管理（cargo） | n/a | 本 Plan 不新增 crate 依赖 |

</threat_model>

<verification>
全局验证（Plan 02 完成后必须满足）：

1. `cargo build --release` 退出码 0
2. `cargo clippy --all-targets -- -D warnings` 退出码 0（零警告）
3. `cargo test --lib` 退出码 0
4. `grep -rnE 'cfg\.pipeline\.|final_cfg\.pipeline\.|tmp\.pipeline\.' src/ tests/` 仅在 src/cli/init.rs 模板字符串与 tests/integration.rs 内残留（这是 Plan 03 处理范围）
5. 端到端：临时 toml `[template]\nenable = true\noutput_csv_path = "/tmp/x.csv"\noutput_sqlite_table = ""` + `[charts]\noutput_dir = "/tmp/c/"` 跑 handle_run 应在 /tmp/x.csv 写出 templates 文件，且不创建任何 sqlite 表
</verification>

<success_criteria>
- 所有 cfg.pipeline.* / final_cfg.pipeline.* / tmp.pipeline.* 引用已从 src/cli/, src/main.rs, src/exporter/ 中移除
- Exporter trait 与 ExporterManager.write_template_stats 签名为 `(stats, csv_output_path: Option<&str>, sqlite_table_name: Option<&str>)`
- CsvExporter / SqliteExporter / DryRunExporter 三个实现都正确处理 None / 空串短路
- SqliteExporter 在拼接 sqlite_table_name 前做 ascii_alphanumeric_or_underscore 校验
- src/pipeline/filters.rs 内 compile_patterns 字段路径与 toml 测试字符串前缀从 pipeline.filters 全改为 filter
- cli/run.rs handle_run 两条路径（顺序 + 并行）都从 cfg.template.output_csv_path / output_sqlite_table 派生 write_template_stats 入参
- cargo build / cargo clippy --all-targets -- -D warnings / cargo test --lib 三联全通过
</success_criteria>

<output>
Create `.planning/phases/18-template-chart-nesting/18-02-SUMMARY.md` when done
</output>
