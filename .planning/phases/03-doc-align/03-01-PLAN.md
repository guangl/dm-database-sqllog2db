---
phase: 03-doc-align
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/cli/opts.rs
autonomous: true
requirements: [DOC-05]
tags: [docs, cli, after_help, help-examples]

must_haves:
  truths:
    - "用户运行 `sqllog2db watch --help` 至少看到 2 个 EXAMPLES 示例（包含 quiet 模式示例）"
    - "用户运行 `sqllog2db validate --help` 至少看到 2 个 EXAMPLES 示例（包含 verbose 模式示例）"
    - "`cargo clippy --all-targets -- -D warnings` 通过，无警告"
    - "`cargo fmt --check` 通过，无 diff"
    - "现有 `Stats` variant 的 3 个示例保持不变（D-10）"
  artifacts:
    - path: "src/cli/opts.rs"
      provides: "Watch variant after_help（2 个示例）+ Validate variant after_help（2 个示例）"
      contains: "Watch in quiet mode (suitable for cron/background)"
    - path: "src/cli/opts.rs"
      provides: "Validate variant after_help 第 2 个示例"
      contains: "Validate and show detailed field information"
  key_links:
    - from: "src/cli/opts.rs Watch variant"
      to: "clap after_help 渲染"
      via: "#[command(after_help = ...)] 字符串"
      pattern: "Watch in quiet mode"
    - from: "src/cli/opts.rs Validate variant"
      to: "clap after_help 渲染"
      via: "#[command(after_help = ...)] 字符串"
      pattern: "Validate and show detailed field information"
---

<objective>
为 `watch` 和 `validate` 子命令的 `--help` 输出各追加 1 个使用示例，使两者各满足 DOC-05 要求的 ≥2 示例。`stats` 子命令已有 3 个示例（D-10），本计划不修改。

Purpose: 让用户无需阅读外部文档即可通过 `--help` 学到 quiet/verbose 等常用模式，缩短 watch/validate 子命令的上手路径。
Output: `src/cli/opts.rs` 中 `Watch` 与 `Validate` variant 的 `after_help` 字符串均扩展为 2 个示例，三道质量门禁（`cargo test` / `cargo clippy -D warnings` / `cargo fmt --check`）全部绿灯。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-doc-align/03-CONTEXT.md
@.planning/phases/03-doc-align/03-RESEARCH.md
@.planning/phases/03-doc-align/03-PATTERNS.md
@.planning/phases/03-doc-align/03-VALIDATION.md
@src/cli/opts.rs

<interfaces>
<!-- src/cli/opts.rs 中两个需要修改的 variant，executor 直接对照下方现有字符串改写。 -->

Watch variant（现状，src/cli/opts.rs 行 169-187）：
```rust
/// Watch directory for new .log files and process them automatically
#[command(
    long_about = "Watch configured input directories for new .log files. Automatically triggers processing when new files appear. Press Ctrl+C to stop.",
    after_help = "\
EXAMPLES:
    Watch and process new log files automatically:
        sqllog2db watch -c config.toml"
)]
Watch {
    /// TOML configuration file path
    #[arg(
        short = 'c',
        long = "config",
        default_value = "config.toml",
        env = "SQLLOG2DB_CONFIG",
        help = "TOML configuration file path."
    )]
    config: String,
},
```

Validate variant（现状，src/cli/opts.rs 行 104-122）：
```rust
/// Validate a configuration file
#[command(
    long_about = "Validate a TOML configuration file for correctness. Checks that all required sections and fields are present and valid.",
    after_help = "\
EXAMPLES:
    Validate a configuration file:
        sqllog2db validate -c config.toml"
)]
Validate {
    /// TOML configuration file path to validate
    #[arg(
        short = 'c',
        long = "config",
        default_value = "config.toml",
        env = "SQLLOG2DB_CONFIG",
        help = "TOML configuration file path to validate."
    )]
    config: String,
},
```

Stats variant（参考样板，src/cli/opts.rs 行 126-135，**不修改**）—— 提供"多示例 + 空行分隔 + 末尾 `"` 紧跟最后一行命令"的标准模式：
```rust
after_help = "\
EXAMPLES:
    Run statistics with default top-20:
        sqllog2db stats -c config.toml

    Limit output to top 5 per table:
        sqllog2db stats -c config.toml --top 5

    Filter records by time range:
        sqllog2db stats -c config.toml --from \"2024-01-01\" --to \"2024-01-31\""
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 为 Watch variant 追加 quiet 模式示例（D-08）</name>
  <files>src/cli/opts.rs</files>
  <read_first>
    - src/cli/opts.rs（整个文件，重点确认 Watch variant 行 169-187 的 after_help 现状与 Stats variant 行 126-135 的多示例格式）
    - .planning/phases/03-doc-align/03-CONTEXT.md（决策 D-08、D-11）
    - .planning/phases/03-doc-align/03-RESEARCH.md（§ Code Examples 中的目标格式）
    - .planning/phases/03-doc-align/03-PATTERNS.md（§ `after_help` 多示例字符串格式）
  </read_first>
  <action>
    定位 `src/cli/opts.rs` 中 `Watch` variant 的 `#[command(... after_help = "\EXAMPLES: ...")]` 字符串（当前以 `        sqllog2db watch -c config.toml"` 结尾，紧跟 `)]`）。

    把该字符串扩展为包含 2 个示例：保留现有 "Watch and process new log files automatically" 示例不变；在其后追加 1 个空行 + 1 段新示例。新示例描述行用 `Watch in quiet mode (suitable for cron/background):`，命令行用 `sqllog2db watch -c config.toml --quiet`。

    新增段落必须严格匹配 D-08 文本与 Stats variant（行 126-135）的既有缩进规则：描述行 4 空格缩进且以冒号 `:` 结尾、命令行 8 空格缩进、示例之间用 1 个空行分隔；最后一行命令之后立即写 `"`，不留多余换行或空格。

    不要修改 Watch variant 的 `long_about`、`config` 字段、struct 字段顺序，也不要触碰其他 variant（Run/Init/Validate/Stats 和 Cli 顶层 after_help 均不在本任务范围）。

    完成后运行 `cargo fmt`（修改 opts.rs），但不要让 fmt 改变字符串字面内容。
  </action>
  <verify>
    <automated>cargo build --bin sqllog2db 2>&1 | tee /tmp/03-01-task1-build.log && cargo run --quiet -- watch --help 2>&1 | tee /tmp/03-01-task1-help.log && grep -q "Watch in quiet mode (suitable for cron/background):" /tmp/03-01-task1-help.log && grep -q "sqllog2db watch -c config.toml --quiet" /tmp/03-01-task1-help.log && [ "$(grep -E '^EXAMPLES:|^    [A-Z]' /tmp/03-01-task1-help.log | grep -cE '^    [A-Z].*:$')" -ge 2 ]</automated>
  </verify>
  <acceptance_criteria>
    - `src/cli/opts.rs` 中 `Watch` variant 的 `after_help` 字符串包含字面子串 `Watch in quiet mode (suitable for cron/background):`
    - `src/cli/opts.rs` 中 `Watch` variant 的 `after_help` 字符串包含字面子串 `sqllog2db watch -c config.toml --quiet`
    - `src/cli/opts.rs` 中 `Watch` variant 的 `after_help` 字符串仍包含字面子串 `Watch and process new log files automatically:` 与 `sqllog2db watch -c config.toml`（原示例保留）
    - 字符串末尾紧跟 `"`（最后一行命令后不留空行）—— 通过 `cargo build` 编译成功验证
    - `cargo run --quiet -- watch --help` stdout 中 `EXAMPLES:` 下至少出现 2 段（每段为"描述行 + 命令行"），可用 `grep -cE '^    [A-Z].*:$' >= 2` 量化
    - 未修改 `Run`/`Init`/`Validate`/`Stats` variant 的 `after_help`（`git diff src/cli/opts.rs` 仅显示 Watch variant 区段变化）
  </acceptance_criteria>
  <done>
    Watch variant after_help 含 2 个示例（原示例 + quiet 模式示例），格式与 Stats variant 一致；`cargo build` 编译成功，`watch --help` 输出包含两段示例。
  </done>
</task>

<task type="auto">
  <name>Task 2: 为 Validate variant 追加 verbose 模式示例（D-09）+ 三道质量门禁</name>
  <files>src/cli/opts.rs</files>
  <read_first>
    - src/cli/opts.rs（整个文件，重点确认 Validate variant 行 104-122 的 after_help 现状；同时核对 Task 1 提交后 Watch variant 的最终形态，避免回退）
    - .planning/phases/03-doc-align/03-CONTEXT.md（决策 D-09、D-11）
    - .planning/phases/03-doc-align/03-RESEARCH.md（§ Code Examples 中 Validate 目标格式）
    - .planning/phases/03-doc-align/03-PATTERNS.md（§ `after_help` 多示例字符串格式）
    - CLAUDE.md（质量门禁命令：cargo test、cargo clippy --all-targets -- -D warnings、cargo fmt）
  </read_first>
  <action>
    定位 `src/cli/opts.rs` 中 `Validate` variant 的 `#[command(... after_help = "\EXAMPLES: ...")]` 字符串（当前以 `        sqllog2db validate -c config.toml"` 结尾，紧跟 `)]`）。

    把该字符串扩展为包含 2 个示例：保留现有 "Validate a configuration file" 示例不变；在其后追加 1 个空行 + 1 段新示例。新示例描述行用 `Validate and show detailed field information:`，命令行用 `sqllog2db validate -c config.toml --verbose`。

    缩进规则与 Task 1 相同：描述行 4 空格 + 末尾冒号、命令行 8 空格、示例之间空行分隔、末尾紧跟 `"`。

    不要修改 Validate variant 的 `long_about`、`config` 字段，不要触碰其他 variant。`-v/--verbose` 当前作为 `Cli` 顶层 global 标志已存在（行 27-33），无需为 Validate 子命令新增本地参数。

    然后运行三道质量门禁并保留输出供验证：
    1. `cargo fmt`（自动格式化）
    2. `cargo clippy --all-targets -- -D warnings`
    3. `cargo test`

    任一失败必须修复后重跑，禁止跳过。完成后提交一次 `docs(03-01): add quiet/verbose --help examples for watch/validate`（conventional commit 格式，里程碑要求每个任务后跑 clippy+test+fmt 见 user memory `feedback_post_op_checks.md`）。
  </action>
  <verify>
    <automated>cargo fmt --check && cargo clippy --all-targets -- -D warnings 2>&1 | tee /tmp/03-01-task2-clippy.log && cargo test 2>&1 | tee /tmp/03-01-task2-test.log && cargo run --quiet -- validate --help 2>&1 | tee /tmp/03-01-task2-help.log && grep -q "Validate and show detailed field information:" /tmp/03-01-task2-help.log && grep -q "sqllog2db validate -c config.toml --verbose" /tmp/03-01-task2-help.log && [ "$(grep -cE '^    [A-Z].*:$' /tmp/03-01-task2-help.log)" -ge 2 ] && grep -E "test result: ok\." /tmp/03-01-task2-test.log</automated>
  </verify>
  <acceptance_criteria>
    - `src/cli/opts.rs` 中 `Validate` variant 的 `after_help` 字符串包含字面子串 `Validate and show detailed field information:`
    - `src/cli/opts.rs` 中 `Validate` variant 的 `after_help` 字符串包含字面子串 `sqllog2db validate -c config.toml --verbose`
    - `src/cli/opts.rs` 中 `Validate` variant 仍包含原 `Validate a configuration file:` 与 `sqllog2db validate -c config.toml` 子串
    - `cargo fmt --check` 退出码 0、无 diff
    - `cargo clippy --all-targets -- -D warnings` 退出码 0、无 warning 行
    - `cargo test` 退出码 0、所有测试 `test result: ok.`
    - `cargo run --quiet -- validate --help` stdout 包含 2 段 `EXAMPLES`（grep `^    [A-Z].*:$` 至少 2 行）
    - `cargo run --quiet -- stats --help` stdout 中 `EXAMPLES:` 下示例描述行仍为 3 段（D-10：stats 未被改动，可用 `grep -cE '^    [A-Z].*:$' >= 3` 验证）
    - `git log -1 --oneline` 提交信息以 `docs(03-01):` 开头并描述 watch/validate --help 示例补充
  </acceptance_criteria>
  <done>
    Validate variant after_help 含 2 个示例（原示例 + verbose 模式示例），格式与 Watch 一致；`cargo fmt --check`、`cargo clippy -D warnings`、`cargo test` 三道门禁全绿；提交以 `docs(03-01):` 开头。DOC-05 需求落地。
  </done>
</task>

</tasks>

<verification>
全局检查（任一不满足则计划失败）：
1. `cargo run --quiet -- watch --help` 输出中 `EXAMPLES:` 段落含 2 段示例，第二段为 `Watch in quiet mode (suitable for cron/background)` + `sqllog2db watch -c config.toml --quiet`
2. `cargo run --quiet -- validate --help` 输出中 `EXAMPLES:` 段落含 2 段示例，第二段为 `Validate and show detailed field information` + `sqllog2db validate -c config.toml --verbose`
3. `cargo run --quiet -- stats --help` 输出中 `EXAMPLES:` 仍为 3 段（D-10 不动）
4. `cargo clippy --all-targets -- -D warnings` 通过；`cargo fmt --check` 通过；`cargo test` 全绿
5. `git diff main -- src/cli/opts.rs` 仅显示 Watch 与 Validate variant 的 after_help 字符串扩展，无其他副作用
</verification>

<success_criteria>
DOC-05 完成：watch / validate 子命令 --help 各至少 2 个示例；stats 保持 3 个示例。三道质量门禁全绿，无回归。提交以 conventional commit 格式入库。
</success_criteria>

<output>
执行结束后创建 `.planning/phases/03-doc-align/03-01-SUMMARY.md`，记录：
- tasks_completed、files_modified、commits（含 SHA + 主题）
- self-check: PASSED/FAILED 列表（对照 must_haves.truths 逐条）
- 三道质量门禁结果（cargo test / clippy / fmt 输出关键行）
- requirements-completed: [DOC-05]
- 与原 PLAN 的差异（如有）
</output>
