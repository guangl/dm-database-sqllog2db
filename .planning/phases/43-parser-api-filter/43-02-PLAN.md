---
phase: 43-parser-api-filter
plan: 02
type: execute
wave: 2
depends_on: ["43-01"]
files_modified:
  - src/pipeline/filters/compiled.rs
  - src/cli/run/prescan.rs
autonomous: true
requirements: [REFACTOR-01]
must_haves:
  truths:
    - "D-03: FilterBuilder 未引入（动态配置驱动的过滤逻辑无法通过字面量谓词 FilterBuilder 简化，不强制替换）"
    - "D-04: pre-scan 与 main-pass 逻辑未拆子模块，仅在原文件内以注释 section 分隔，重构未移动函数到新文件"
    - "D-05: src/pipeline/filters/compiled.rs 中 Pre-scan 辅助方法 (has_filters / has_any_filters) 与 Main-pass 方法 (should_keep / exclude_veto / include_and) 通过 `// ===` 注释 section 清晰区隔"
    - "D-05: src/cli/run/prescan.rs 中 scan_log_file_for_matches 与 scan_for_trxids_by_transaction_filters 与 recompile_meta_if_needed 通过 `// ===` 注释 section 清晰区隔"
    - "D-07: filter 模块单元测试数量 ≥ 50（重构前基线），cargo test 全部通过，无功能回归"
    - "cargo clippy --all-targets -- -D warnings + cargo fmt --check 通过"
  artifacts:
    - path: "src/pipeline/filters/compiled.rs"
      provides: "compiled meta filter 实现，含 Pre-scan 与 Main-pass section 注释"
      contains: "// ===== Pre-scan"
    - path: "src/cli/run/prescan.rs"
      provides: "prescan 编排逻辑，含内部 section 注释"
      contains: "// ===== "
  key_links:
    - from: "src/pipeline/filters/compiled.rs::has_filters"
      to: "section: Pre-scan 辅助"
      via: "section 注释组织"
      pattern: "// ===== Pre-scan"
    - from: "src/pipeline/filters/compiled.rs::should_keep"
      to: "section: Main-pass（热路径）"
      via: "section 注释组织"
      pattern: "// ===== Main-pass"
---

<objective>
对 filter 模块做轻量级重构，使 Pre-scan 与 Main-pass 的逻辑边界通过注释 section 清晰呈现，便于读者在不增加新模块的前提下理解职责划分。不拆子模块，不移动函数，不改变任何公开 API 或行为。

Purpose: REFACTOR-01 — 让 filter 模块的"Pre-scan 用于事务级 trxid 收集"与"Main-pass 用于记录级保留判断"两种用途在代码组织上一目了然（D-04 / D-05 / D-06）。
Output: compiled.rs 与 prescan.rs 的 impl/函数之间插入 section 注释，单元测试基线保持。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/43-parser-api-filter/43-CONTEXT.md
@.planning/phases/43-parser-api-filter/43-RESEARCH.md
@.planning/phases/43-parser-api-filter/43-VALIDATION.md
@.planning/phases/43-parser-api-filter/43-01-PLAN.md

# 必读源文件（理解结构）
@src/pipeline/filters/compiled.rs
@src/cli/run/prescan.rs

<interfaces>
<!-- 当前 compiled.rs::impl CompiledMetaFilters 方法分布 -->
方法清单（按现有代码顺序）:
  - try_from_include_exclude(...)  ← 构造（启动期，非热路径）
  - has_filters(&self) -> bool     ← Pre-scan/Main-pass 都可能查询
  - has_any_filters(&self) -> bool ← 由 FilterProcessor::new 预计算，属 Main-pass 准备阶段
  - should_keep(&self, meta)       ← Main-pass 热路径入口
  - exclude_veto(&self, meta)      ← Main-pass 私有辅助
  - include_and(&self, meta)       ← Main-pass 私有辅助

<!-- 当前 prescan.rs 函数分布 -->
函数清单:
  - scan_log_file_for_matches(file_path, cfg)          ← Pre-scan 单文件并行扫描
  - scan_for_trxids_by_transaction_filters(...)         ← Pre-scan 跨文件并行编排
  - recompile_meta_if_needed(final_cfg, original)      ← Pre-scan 完成后重新编译 meta（衔接 Main-pass）

<!-- D-06 规定的 section 注释风格 -->
风格示例（来自 43-RESEARCH.md Code Examples）:
  // ===== Pre-scan 辅助 =====
  // ===== Main-pass（热路径）=====
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 在 src/pipeline/filters/compiled.rs 添加 Pre-scan / Main-pass section 注释</name>
  <files>src/pipeline/filters/compiled.rs</files>
  <read_first>
    - src/pipeline/filters/compiled.rs（完整文件，理解 impl 块内部方法顺序）
    - .planning/phases/43-parser-api-filter/43-CONTEXT.md（D-06 规定注释 section 风格）
    - .planning/phases/43-parser-api-filter/43-RESEARCH.md（Pitfall 4 警示注释插入位置，不要跨越 #[cfg(test)] mod 边界）
  </read_first>
  <action>
    在 `impl CompiledMetaFilters` 块内部按顺序插入 4 个 section 注释，使方法按职责分组（不移动方法，仅添加注释行）：
      1. 在 `pub(crate) fn try_from_include_exclude` 之前插入：`// ===== 构造（启动期，非热路径）=====`
      2. 在 `pub(crate) fn has_filters` 之前插入：`// ===== Pre-scan 辅助（在事务级预扫描时查询是否需要做某类过滤）=====`
      3. 在 `pub(crate) fn should_keep` 之前插入：`// ===== Main-pass（热路径：每条记录调用一次，AND + OR-veto）=====`
         同时把 `pub(crate) fn has_any_filters` 紧邻 `should_keep` 之上的位置：has_any_filters 由 FilterProcessor::new 预计算，属 Main-pass 准备，归 Main-pass section。在 `has_any_filters` 上方独立加 `// has_any_filters 由 FilterProcessor::new 预计算，归入 Main-pass 准备阶段`。
      4. 不改变方法顺序、不删除既有注释。
    对 `impl CompiledSqlFilters` 块也做类似处理：在 `try_from_sql_filters` 上方加 `// ===== 构造（启动期）=====`，在 `matches` 上方加 `// ===== Main-pass（热路径：SQL 记录级过滤）=====`。注：CompiledSqlFilters 没有 Pre-scan 辅助方法，无需 Pre-scan section。
    严格不要把 section 注释插入到 `#[cfg(test)] #[path = "compiled_tests.rs"] mod compiled_tests;` 之后；所有 section 注释必须在该 mod 行之前。运行 `cargo fmt` 保持格式。
  </action>
  <verify>
    <automated>grep -v '^#' src/pipeline/filters/compiled.rs | grep -c '===== Pre-scan'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "===== 构造" src/pipeline/filters/compiled.rs` 输出 ≥ 2（CompiledMetaFilters + CompiledSqlFilters 各一处）
    - `grep -c "===== Pre-scan" src/pipeline/filters/compiled.rs` 输出 ≥ 1（CompiledMetaFilters 处）
    - `grep -c "===== Main-pass" src/pipeline/filters/compiled.rs` 输出 ≥ 2（CompiledMetaFilters + CompiledSqlFilters 各一处）
    - `grep -n "===== " src/pipeline/filters/compiled.rs` 所有命中行号都在文件末尾 `mod compiled_tests;` 之前（人工/grep 双重确认）
    - `cargo build` 成功
    - `cargo test --lib -- pipeline::filters` 通过
    - `cargo fmt --check` 通过
  </acceptance_criteria>
  <done>
    compiled.rs 的 impl 块内通过 section 注释清晰展示 构造 / Pre-scan 辅助 / Main-pass 三类职责；方法顺序与逻辑未改动；测试基线保持。
  </done>
</task>

<task type="auto">
  <name>Task 2: 在 src/cli/run/prescan.rs 添加内部 section 注释 + 全套质量门禁</name>
  <files>src/cli/run/prescan.rs</files>
  <read_first>
    - src/cli/run/prescan.rs（完整文件，理解 3 个函数的位置和职责）
    - .planning/phases/43-parser-api-filter/43-CONTEXT.md（D-05：prescan.rs 内部结构调整，确保 scan_for_trxids_by_transaction_filters 等函数与 filter 编译逻辑不交叉）
    - .planning/phases/43-parser-api-filter/43-VALIDATION.md（验证清单）
  </read_first>
  <action>
    在 prescan.rs 文件中插入 3 个顶层 section 注释（不要插入到 use 语句中间，必须在 use 块结束之后、第一个 fn 之前）：
      1. 紧接 use 块之后、在 `pub(super) fn scan_log_file_for_matches` 上方插入：`// ===== Pre-scan: 单文件扫描（rayon 并行 + 文件内去重）=====`
      2. 在 `pub(super) fn scan_for_trxids_by_transaction_filters` 上方插入：`// ===== Pre-scan: 跨文件编排（两级 rayon 嵌套并行）=====`
      3. 在 `pub(super) fn recompile_meta_if_needed` 上方插入：`// ===== Pre-scan -> Main-pass 衔接: 合并 trxids 后重新编译 CompiledMetaFilters =====`
    不修改任何函数体、不调换函数顺序、不更改可见性。保留 Task 1（plan 43-01）已有的修正后注释 `LogIterator 未实现 rayon::IntoParallelIterator trait` 不变。
    完成后运行最终质量门禁：cargo build --release / cargo test / cargo clippy --all-targets -- -D warnings / cargo fmt --check，全部必须通过。
  </action>
  <verify>
    <automated>cargo test --lib -- filter && cargo clippy --all-targets -- -D warnings && cargo fmt --check</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "===== Pre-scan: 单文件扫描" src/cli/run/prescan.rs` 输出 1
    - `grep -c "===== Pre-scan: 跨文件编排" src/cli/run/prescan.rs` 输出 1
    - `grep -c "===== Pre-scan -> Main-pass 衔接" src/cli/run/prescan.rs` 输出 1
    - `git diff src/cli/run/prescan.rs` 仅显示新增注释行（无函数体修改、无函数移动）
    - `cargo test --lib -- filter` 通过，filter 模块测试数 ≥ 50（基线）
    - `cargo test` 全套通过，无回归
    - `cargo clippy --all-targets -- -D warnings` 通过
    - `cargo fmt --check` 通过
    - `cargo build --release` 无 warning
  </acceptance_criteria>
  <done>
    prescan.rs 顶层结构通过 section 注释明确呈现"单文件扫描 / 跨文件编排 / 衔接 Main-pass"三段职责；全套质量门禁全绿；filter 测试 ≥ 50 基线保持。
  </done>
</task>

</tasks>

<verification>
全 Plan 完成后：
- `git diff --stat src/pipeline/filters/compiled.rs src/cli/run/prescan.rs` 显示注释行增加（约 6-8 行新增），无函数体改动
- `cargo test --lib -- filter` 通过，至少 50 个 filter 测试（与基线相同）
- `cargo test` 全套通过
- `cargo clippy --all-targets -- -D warnings` + `cargo fmt --check` 通过
- 人工 review `git diff` 确认未拆出新子模块、未移动函数
</verification>

<success_criteria>
- REFACTOR-01 满足：filter 模块 Pre-scan 与 Main-pass 边界通过 section 注释清晰；测试覆盖未下降；代码组织复杂度未增加（仅添加注释，未拆模块）
- D-04 / D-05 / D-06 满足：注释 section 风格统一，仅在原文件内部分隔
- D-07 满足：测试场景数 ≥ 重构前
- D-08 满足：clippy + fmt 无新增警告
</success_criteria>

<output>
Create `.planning/phases/43-parser-api-filter/43-02-SUMMARY.md` when done
</output>
