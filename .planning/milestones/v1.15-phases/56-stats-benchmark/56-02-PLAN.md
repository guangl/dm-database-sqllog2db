---
phase: 56-stats-benchmark
plan: 02
type: execute
wave: 2
depends_on:
  - 56-01
files_modified:
  - src/cli/run/processor.rs
  - benches/BENCHMARKS.md
autonomous: true
requirements:
  - CLEAN-01
  - BENCH-01

must_haves:
  truths:
    - "src/cli/run/processor.rs 的 parser 创建 + 解析迭代循环改为调用 crate::scanner，但 process_log_file 函数签名保持不变"
    - "run 命令现有行为不变：CSV/SQLite 导出、ProgressBar、ErrorStats 合并、fatal export 中断等语义完全保留"
    - "benches/BENCHMARKS.md 末尾新增章节，说明 GitHub Actions artifact 下载方式、JSON 结构、手动对比方法"
    - "scripts/collect_bench_results.sh 存在且可执行（BENCH-01 文件检查）"
    - ".github/workflows/bench.yml 包含 continue-on-error: true（BENCH-01 文件检查）"
  artifacts:
    - path: "src/cli/run/processor.rs"
      provides: "run 命令文件扫描调用 scanner（D-03 仅提取 parser 创建+迭代循环）"
      contains: "crate::scanner"
    - path: "benches/BENCHMARKS.md"
      provides: "CI artifact 使用说明章节（D-04）"
      contains: "CI Artifact"
  key_links:
    - from: "src/cli/run/processor.rs"
      to: "src/scanner.rs"
      via: "crate::scanner::scan_files"
      pattern: "crate::scanner"
    - from: "benches/BENCHMARKS.md"
      to: ".github/workflows/bench.yml"
      via: "artifact 名称 bench-results-{sha}"
      pattern: "bench-results-"
---

<objective>
完成 Phase 56 余下工作：(a) 让 src/cli/run/processor.rs 的解析循环复用 Plan 01 新建的 scanner 公共模块（CONTEXT D-03，
范围严格限定为 parser 创建 + 迭代循环，不动 process_log_file 签名）；(b) 在 benches/BENCHMARKS.md 末尾追加 CI artifact 使用说明章节（CONTEXT D-04）；
(c) 通过 stat/grep 验证 BENCH-01 验证类成功标准（scripts 存在 + bench.yml 含 continue-on-error）。

Purpose: 完成 D-03 + D-04，并通过自动化静态检查闭环 BENCH-01。
Output: src/cli/run/processor.rs（D-03 重构）、benches/BENCHMARKS.md（D-04 文档章节追加）。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/56-stats-benchmark/56-CONTEXT.md
@.planning/phases/56-stats-benchmark/56-RESEARCH.md
@.planning/phases/56-stats-benchmark/56-PATTERNS.md
@.planning/phases/56-stats-benchmark/56-VALIDATION.md
@.planning/phases/56-stats-benchmark/56-01-SUMMARY.md
@src/cli/run/processor.rs
@src/scanner.rs
@benches/BENCHMARKS.md
@.github/workflows/bench.yml
@scripts/collect_bench_results.sh

<interfaces>
<!-- 关键现有类型与函数签名 -->

From src/scanner.rs（Plan 01 新建）:
- pub(crate) fn scan_files<F>(log_files: &[PathBuf], on_record: &mut F, stats: &mut ErrorStats) -> Result<()>
  where F: FnMut(&dm_database_parser_sqllog::Sqllog)
- 内部对每个文件调用 LogParserBuilder::new(path_str).build()，parse error 走 stats.add_parse_error() + log::warn!

From src/cli/run/processor.rs（待重构部分，第 52-153 行）:
- pub(super) fn process_log_file(
    file_path: &str,
    file_index: usize,
    total_files: usize,
    exporter_manager: &mut ExporterManager,
    pipeline: &Pipeline,
    show_progress: bool,
    remaining: Option<usize>,
    interrupted: &Arc<AtomicBool>,
    do_normalize: bool,
    placeholder_override: Option<bool>,
    params_buffer: &mut ParamBuffer,
    ns_scratch: &mut Vec<u8>,
    reset_pb: bool,
    pb: Option<&ProgressBar>,
  ) -> Result<(usize, ErrorStats)>
- 第 52-58 行：当前直接 LogParserBuilder::new(file_path).build() + map_err 构造 ParserError::InvalidPath
- 第 64-149 行：'outer: for result in parser.iter() { match result { Ok(record) => ..., Err(e) => { errors_in_file += 1; file_stats.add_parse_error(); log::warn!("{file_path} | {e:?}"); } } }

From .github/workflows/bench.yml（已存在）:
- continue-on-error: true（第 25 行）
- artifact name: bench-results-${{ github.sha }}（第 44 行，使用完整 SHA）

From scripts/collect_bench_results.sh（已存在）:
- 输出文件名：bench-results-${SHORT_SHA}.json（脚本第 11 行，SHA 前 8 位）
- JSON 结构：{ timestamp, commit_sha, benchmarks: { "<group>/<bench_id>": { mean_ns, stddev_ns }, ... } }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: src/cli/run/processor.rs 内部解析循环改用 scanner，保持函数签名与外部行为不变</name>
  <files>src/cli/run/processor.rs</files>
  <read_first>
    - src/cli/run/processor.rs（完整文件，理解 process_log_file 全部 14 个参数与 'outer 循环中 break、配额、ProgressBar、export error 处理路径）
    - src/scanner.rs（Plan 01 新建，确认 scan_files 签名）
    - src/stats/mod.rs（Plan 01 重构后版本，作为同类调用模式参考）
    - src/error.rs（ErrorStats 字段与方法）
    - .planning/phases/56-stats-benchmark/56-PATTERNS.md（§"src/cli/run/processor.rs"，含重构范围限定与现有模式）
    - .planning/phases/56-stats-benchmark/56-RESEARCH.md（§Common Pitfalls Pitfall 1：若改动超过 processor.rs 第 52-68 行范围应停下重新评估）
    - .planning/phases/56-stats-benchmark/56-CONTEXT.md（§Implementation Decisions D-03："run 命令的文件扫描部分同步重构为调用公共模块（保持行为不变，仅提取）"）
  </read_first>
  <behavior>
    - Test 1: 现有所有 cli::run 单元测试与 tests/integration.rs 端到端测试在重构后继续通过（无任何行为回归）
    - Test 2: process_log_file 函数签名 14 个参数与返回类型 Result<(usize, ErrorStats)> 保持不变（grep 断言）
    - Test 3: src/cli/run/processor.rs 不再直接 `LogParserBuilder::new(...)`；改为通过 scanner 调用（grep 断言）
    - Test 4: parse error 仍累加到返回的 ErrorStats.parse_errors（cargo test 现有用例自动覆盖）
  </behavior>
  <action>
    在 src/cli/run/processor.rs 范围严格限定为：第 52-58 行 parser 创建 + 第 64-149 行解析循环中的"`Ok(record)` 处理之外"的"parser 创建 + 错误分支"部分。保持 process_log_file 函数签名 14 个参数 + 返回 Result<(usize, ErrorStats)> 完全不变。

    具体重构策略——采用"内部回调改造"模式：

    1. 删除第 52-58 行直接的 `LogParserBuilder::new(file_path).build().map_err(|e| ...)?;` 语句。

    2. 将第 64-149 行 `'outer: for result in parser.iter() { match result { Ok(record) => { /* 大段处理 */ } Err(e) => { errors_in_file += 1; file_stats.add_parse_error(); log::warn!("{file_path} | {e:?}"); } } }` 重构为：
       - 引入闭包局部变量将原 Ok(record) 分支整段逻辑包装为 `let mut on_record = |record: &dm_database_parser_sqllog::Sqllog| -> bool { /* 返回 false 时表示需要 break 'outer */ ... };`
       - 调用 `crate::scanner::scan_files(&[std::path::PathBuf::from(file_path)], &mut |record| { on_record(record); }, &mut file_stats)?;`
       - 由于 scanner::scan_files 内部已使用 stats.add_parse_error() + log::warn!，原 `Err(e) => { errors_in_file += 1; file_stats.add_parse_error(); log::warn!(...); }` 分支删除。
       - errors_in_file 局部计数从 file_stats.parse_errors 末态读取（保留第 151-153 行 "X parse errors" 汇总日志）。

       由于 'outer break 与 export 配额、interrupted 检查、fatal export 路径仍需在闭包内触发，可改用 `AtomicBool` 或 `Cell<bool>` 让闭包写入一个 "should_break" 标志，scanner 返回后检查标志。

       **替代方案（如执行者评估 AtomicBool/Cell 改造过重）：** 仅提取 parser 创建（第 52-58 行）的 InvalidPath 错误构造逻辑到 scanner 内部，主循环 `'outer: for result in parser.iter()` 保留在 processor.rs。此情形需让 scanner 提供 `pub(crate) fn build_parser(file_path: &std::path::Path) -> Result<LogParser>` 辅助函数（小范围提取），主迭代循环仍由 processor.rs 持有以便 'outer break。

       **决策标准：** 若 AtomicBool/Cell 实现不超过 30 行新代码且不引入 unsafe，采用回调闭包改造；否则采用辅助函数小范围提取方案，并在 SUMMARY.md 明确说明 D-03 仅完成 parser 创建提取，迭代循环保留在 processor 内（行为完全不变）。

    3. 不论采用哪个方案：
       - 不修改 process_log_file 签名（第 16-31 行参数列表）
       - 不修改返回值 Ok((records_in_file, file_stats))
       - 不修改 ExporterManager、ProgressBar、ParamBuffer、do_normalize、placeholder_override 任何外部参数语义
       - 不删除第 151-153 行 `if errors_in_file > 0 { log::warn!("{file_path}: {errors_in_file} parse errors"); }`（如采用回调改造，errors_in_file 从 file_stats.parse_errors 读取）

    禁止：
    - 不要改变 process_log_file 的任何参数类型或顺序（Pitfall 1：触碰 ProgressBar/parallel/normalize 复杂参数）。
    - 不要让 scanner 学会任何 ExporterManager / ProgressBar / Pipeline 知识（保持 scanner 通用性）。
    - 不要将 errors_in_file 计数语义改变（必须仍能进入第 151-153 行汇总日志）。
    - 不要新增 unsafe。
    - 如评估两个方案都需要 >50 行改动或破坏现有测试，停止本任务并在 SUMMARY 中说明保持 processor.rs 不变（D-03 在 stats 侧已通过 Plan 01 完成，run 侧延后）。
  </action>
  <verify>
    <automated>cd /Users/guang/Projects/sqllog2db &amp;&amp; cargo test &amp;&amp; cargo clippy --all-targets -- -D warnings &amp;&amp; cargo fmt --check &amp;&amp; bash -c 'sig=$(grep -A 15 "pub(super) fn process_log_file" src/cli/run/processor.rs | head -16); echo "$sig" | grep -q "file_path: &str" &amp;&amp; echo "$sig" | grep -q "exporter_manager: &mut ExporterManager" &amp;&amp; echo "$sig" | grep -q "pb: Option<&ProgressBar>" || (echo FAIL: process_log_file signature changed; exit 1)'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "pub(super) fn process_log_file" src/cli/run/processor.rs` == 1（函数仍存在）
    - process_log_file 签名包含 `file_path: &str`、`exporter_manager: &mut ExporterManager`、`pb: Option<&ProgressBar>` 三个标志参数（grep 断言；这三项是签名敏感参数，任一变化都意味着触碰外部接口）
    - process_log_file 返回类型仍为 `Result<(usize, ErrorStats)>`：`grep -c "Result<(usize, ErrorStats)>" src/cli/run/processor.rs` ≥ 1
    - `grep -c "crate::scanner" src/cli/run/processor.rs` ≥ 1（已接入 scanner）OR（若执行者选择"保持 processor.rs 不变"兜底方案）SUMMARY.md 中包含明确说明"D-03 兜底：run 侧未改动，stats 侧已在 Plan 01 完成 scanner 共用"
    - `cargo test` 全套退出码 0（重点：cli::run::tests + tests/integration.rs 端到端）
    - `cargo clippy --all-targets -- -D warnings` 退出码 0
    - `cargo fmt --check` 退出码 0
    - `grep -c "errors_in_file" src/cli/run/processor.rs` ≥ 1（若采用回调改造，仍可从 file_stats.parse_errors 读取）OR 第 151-153 行原汇总日志保留
  </acceptance_criteria>
  <done>processor.rs 或被重构为调用 scanner（保留所有现有 run 行为），或在评估后兜底保留原样并在 SUMMARY 明确说明。无论哪种结果，cargo test/clippy/fmt 全部绿灯，process_log_file 签名不变。</done>
</task>

<task type="auto">
  <name>Task 2: benches/BENCHMARKS.md 追加 CI Artifact 章节 + 验证 BENCH-01 静态条件</name>
  <files>benches/BENCHMARKS.md</files>
  <read_first>
    - benches/BENCHMARKS.md（现有文档，注意末尾最后一节是 "Phase 44"，新章节应追加在此之后；理解整体 Markdown 风格与"How to reproduce"、"How to compare against this baseline"等章节标题层级）
    - .github/workflows/bench.yml（完整文件，确认 artifact name 模式 `bench-results-${{ github.sha }}` 第 44 行 + retention-days: 60 第 46 行 + continue-on-error: true 第 25 行）
    - scripts/collect_bench_results.sh（完整文件，确认本地短 SHA 输出文件名 `bench-results-${SHORT_SHA}.json` 第 11 行 + JSON 结构 `{ timestamp, commit_sha, benchmarks: { "<group>/<bench_id>": { mean_ns, stddev_ns } } }`）
    - .planning/phases/56-stats-benchmark/56-CONTEXT.md（§D-04 与 §Specific Ideas："新节应说明 artifact 名称格式（bench-results-{sha8}.json）、从 GitHub Actions UI 或 gh CLI 下载方式、JSON 结构说明、手动对比历史数据的方法"）
    - .planning/phases/56-stats-benchmark/56-PATTERNS.md（§"benches/BENCHMARKS.md"，含追加位置指引与"新节应包含内容"清单）
  </read_first>
  <behavior>
    - Test 1: 文件末尾新增章节标题 `## CI Artifact 使用说明` 或 `## CI Benchmark Artifact 使用说明`（grep 断言）
    - Test 2: 新章节包含三个关键内容块：(a) artifact 名称与下载方式、(b) JSON 文件结构说明、(c) 手动对比历史数据方法（grep 关键字断言）
    - Test 3: scripts/collect_bench_results.sh 存在且可执行（stat 检查，BENCH-01）
    - Test 4: .github/workflows/bench.yml 包含 continue-on-error: true（grep 检查，BENCH-01）
  </behavior>
  <action>
    在 benches/BENCHMARKS.md 文件末尾（现有最后一节 "Phase 44" 之后）追加新章节，标题为 `## CI Benchmark Artifact 使用说明`，包含以下四个子小节：

    1. 子小节 `### Artifact 命名规则`：说明 CI 上传的 artifact 名为 `bench-results-{github.sha}`（GitHub Actions 使用完整 40 位 SHA），artifact 内部文件名为 `bench-results-{short_sha}.json`（脚本 scripts/collect_bench_results.sh 使用 8 位 SHA），retention-days 为 60。注明 artifact 来源 workflow 为 `.github/workflows/bench.yml`，触发条件为 PR to main + push to main。

    2. 子小节 `### 下载方式`：提供两种下载方式：
       - GitHub UI：进入 Actions 页面 → 选中 Benchmark workflow → 选择具体 run → 在 Artifacts 区域下载
       - gh CLI 命令示例（fenced ```bash 代码块）：`gh run list --workflow=bench.yml --limit 10`、`gh run download <run-id> -n bench-results-<sha>`

    3. 子小节 `### JSON 结构`：使用 ```json fenced 代码块展示输出结构：
       ```
       {
         "timestamp": "2026-MM-DDTHH:MM:SSZ",
         "commit_sha": "<full 40-char SHA>",
         "benchmarks": {
           "<group>/<bench_id>": {
             "mean_ns": <number>,
             "stddev_ns": <number>
           }
         }
       }
       ```
       说明 group/bench_id 对应 criterion 目录结构（如 `csv_export/10000`、`parser_throughput/1000`），mean_ns 为 criterion mean.point_estimate（纳秒），stddev_ns 为 criterion std_dev.point_estimate（纳秒）。

    4. 子小节 `### 手动历史对比方法`：说明：
       - 下载两个不同 commit 的 artifact JSON 文件
       - 使用 `jq` 提取目标 benchmark：`jq '.benchmarks["csv_export/10000"].mean_ns' bench-results-<sha>.json`
       - 计算相对变化：`echo "scale=2; (new - old) / old * 100" | bc` 或 Python/Excel 比对
       - 当前 bench.yml 设置 `continue-on-error: true`，benchmark 失败不阻塞 PR merge，是 informational data collection（与 Phase 55/56 设计一致：不作为 merge 门控，未来若引入 regression 门控需在另一个 workflow 中实现）

    新章节末尾追加一行小字注解：`> 实施于 Phase 56（v1.15）— D-04`。

    禁止：
    - 不要修改 benches/BENCHMARKS.md 现有任何章节内容（仅在末尾追加）
    - 不要修改 .github/workflows/bench.yml 或 scripts/collect_bench_results.sh（本 task 为纯文档）
    - 不要在新章节中编造 benchmark 数据或保证 regression 门控（CONTEXT Deferred 明确：regression 门控延后）
  </action>
  <verify>
    <automated>cd /Users/guang/Projects/sqllog2db &amp;&amp; bash -c 'grep -qE "^## CI .*Artifact" benches/BENCHMARKS.md || (echo FAIL: CI Artifact heading missing; exit 1)' &amp;&amp; bash -c 'grep -qE "bench-results-" benches/BENCHMARKS.md || (echo FAIL: artifact filename pattern missing; exit 1)' &amp;&amp; bash -c 'grep -qE "mean_ns" benches/BENCHMARKS.md || (echo FAIL: JSON structure missing; exit 1)' &amp;&amp; bash -c 'grep -qE "gh run" benches/BENCHMARKS.md || (echo FAIL: gh CLI usage missing; exit 1)' &amp;&amp; bash -c 'test -x scripts/collect_bench_results.sh || (echo FAIL: collect_bench_results.sh not executable; exit 1)' &amp;&amp; bash -c 'grep -q "continue-on-error: true" .github/workflows/bench.yml || (echo FAIL: bench.yml missing continue-on-error; exit 1)' &amp;&amp; echo "BENCH-01 verified"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "^## CI .*Artifact" benches/BENCHMARKS.md` ≥ 1（新章节标题存在）
    - `grep -cE "bench-results-" benches/BENCHMARKS.md` ≥ 2（artifact 名称模式至少出现 2 次，对应"命名规则"与"下载方式"两节）
    - `grep -c "mean_ns" benches/BENCHMARKS.md` ≥ 1（JSON 结构说明存在）
    - `grep -c "stddev_ns" benches/BENCHMARKS.md` ≥ 1
    - `grep -c "gh run" benches/BENCHMARKS.md` ≥ 1（gh CLI 下载示例存在）
    - `grep -c "Phase 56" benches/BENCHMARKS.md` ≥ 1（追加位置可追溯）
    - `test -x scripts/collect_bench_results.sh` 退出码 0（BENCH-01 验证：脚本存在且可执行）
    - `grep -c "continue-on-error: true" .github/workflows/bench.yml` ≥ 1（BENCH-01 验证：informational 模式）
  </acceptance_criteria>
  <done>benches/BENCHMARKS.md 末尾新增 CI Artifact 使用说明章节，包含命名规则、下载方式、JSON 结构、手动对比方法四个子小节；BENCH-01 验证类条件（脚本可执行 + bench.yml continue-on-error）经 stat/grep 静态断言通过。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| processor.rs 闭包/回调 → scanner | scanner 通过 FnMut 回调将 &Sqllog 传给 processor；闭包持有 ExporterManager 与 ProgressBar 的 mut 引用，作用域局限于 process_log_file 一次调用 |
| 用户/CI → benches/BENCHMARKS.md | 文档章节描述如何从 GitHub Actions 下载 artifact；artifact 来自 trusted GitHub Actions workflow（已由 Phase 55 加固） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-56-05 | Tampering | processor.rs 闭包通过 Cell/AtomicBool 实现 'outer break | mitigate | 选用 Cell<bool> 单线程读写（process_log_file 同步执行，无跨线程访问）；若评估为复杂改用辅助函数兜底方案保持原循环 |
| T-56-06 | Information disclosure | BENCHMARKS.md 新章节示例 SHA | accept | 文档仅展示 `<sha>` 占位符与命令格式，不泄露内部信息 |
| T-56-07 | Repudiation | benchmark 历史数据缺乏完整性签名 | accept | 当前 bench artifact 仅作 informational（CONTEXT Deferred: regression 门控延后），无须 PGP/SHA256 签名 |
| T-56-08 | Denial of service | benchmark 文档误导用户引入回归门控 | mitigate | 新章节明确写明"当前 continue-on-error: true，不作为 merge 门控"——用户预期对齐设计 |
| T-56-SC | Tampering | npm/pip/cargo installs | accept | 本 Phase 不安装任何新包（RESEARCH §Package Legitimacy Audit 明确"本 Phase 不安装任何新包，跳过此节"） |
</threat_model>

<verification>
执行完两个 task 后：

1. `cargo build --release` 成功，无警告
2. `cargo test` 全套通过（包含 tests/integration.rs 端到端，因 processor 改动需端到端覆盖）
3. `cargo clippy --all-targets -- -D warnings` 零告警
4. `cargo fmt --check` 零差异
5. BENCH-01 验证类条件：
   - `test -x scripts/collect_bench_results.sh` 退出码 0
   - `grep -c "continue-on-error: true" .github/workflows/bench.yml` ≥ 1
6. benches/BENCHMARKS.md 新章节内容完整（grep 四项关键字 + 标题）
7. process_log_file 函数签名与返回类型完全不变（grep 断言）
</verification>

<success_criteria>
- [x] src/cli/run/processor.rs 内部解析循环复用 scanner 模块（或兜底保留原样并在 SUMMARY 明确说明 D-03 在 stats 侧已完成）
- [x] process_log_file 函数签名 14 个参数 + 返回 Result<(usize, ErrorStats)> 完全不变
- [x] benches/BENCHMARKS.md 末尾新增 CI Artifact 使用说明章节，包含命名规则、下载方式、JSON 结构、手动对比方法
- [x] BENCH-01 验证类条件经 stat+grep 静态断言通过（脚本可执行 + bench.yml continue-on-error）
- [x] cargo test 全套通过、clippy 零告警、fmt 零差异
- [x] run 命令行为不变：tests/integration.rs 端到端测试全部通过
</success_criteria>

<output>
Create `.planning/phases/56-stats-benchmark/56-02-SUMMARY.md` when done
</output>
