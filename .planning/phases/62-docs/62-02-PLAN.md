---
phase: 62-docs
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements:
  - DOC-01
must_haves:
  truths:
    - "README.md 在 `## 快速入门` 节下 stats 现有示例代码块之后追加一段 stats --from/--to 用法示例（覆盖 `--from YYYY-MM-DD --to YYYY-MM-DD` 与 `--from \"YYYY-MM-DD HH:MM:SS\" --to \"YYYY-MM-DD HH:MM:SS\" --top N` 两种格式）"
    - "README.md 在 `## 链接` 节之前新增独立 `## 版本亮点` 二级节，包含 `### v1.15.0 — CI/CD 修复（2026-06-02）` 子节，说明 actions/checkout 版本修复、Cross.toml SHA 固定、release workflow 重构三项内容"
    - "README.md 现有功能特性章节（『简洁的 CLI』描述行）中提及 stats 子命令的功能列表无需改动"
    - "README.md 不破坏现有结构（其他 11 个二级标题 `## 功能特性`、`## 架构`、`## 安装`、`## 快速入门`、`## 配置`、`## 性能`、`## 错误处理`、`## 链接`、`## 许可证` 顺序、子节、徽章区全部保持原样）"
    - "README.md 末尾尾巴上的孤立 `E)。` 残留字符（第 179 行）被清理掉"
  artifacts:
    - path: "README.md"
      provides: "更新后的项目主页文档"
      contains: "sqllog2db stats -c config.toml --from"
    - path: "README.md"
      provides: "v1.15 版本亮点章节"
      contains: "## 版本亮点"
  key_links:
    - from: "README.md `## 版本亮点` 节"
      to: "Cross.toml + .github/workflows/ci.yaml"
      via: "v1.15.0 子节描述指向 Phase 55/61 落地的两份文件"
      pattern: "Cross\\.toml"
---

<objective>
更新 `README.md`：(1) 在 `## 快速入门` 节的 stats 示例代码块后追加 `--from`/`--to` 用法示例；(2) 在 `## 链接` 节之前新增 `## 版本亮点` 二级节，描述 v1.15.0 CI/CD 修复内容（actions/checkout 版本修复、Cross.toml SHA 固定、release workflow 重构）；(3) 清理文件末尾遗留的孤立字符 `E)。`。

Purpose: 实现 DOC-01（Phase 62 Success Criteria #1）——"README.md 包含 `stats` 子命令的用法示例（含 `--from`/`--to` 参数）、v1.15 CI/CD 修复说明，功能列表与当前代码一致"。

Output: 修改后的 `README.md`，在保留现有 11 个二级节顺序与所有徽章/链接的前提下，新增两处内容：快速入门末段的 stats 时间范围示例 + 链接节之前的 `## 版本亮点` 节。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/62-docs/62-CONTEXT.md
@.planning/phases/62-docs/62-RESEARCH.md
@.planning/phases/62-docs/62-PATTERNS.md

<interfaces>
<!-- README.md 当前结构（grep 提取的二级/三级标题，行号供参考） -->
<!--
16: ## 功能特性
18:   ### 解析与导出
26:   ### 过滤与字段控制
34:   ### 配置与性能
43: ## 架构
70:   ### 关键模块
81: ## 安装
83:   ### 从 crates.io 安装（推荐）
91:   ### 本地构建
98:   ### 验证安装的二进制文件
105: ## 快速入门              ← stats --from/--to 示例插入到本节末尾（115–122 行的 stats 代码块之后）
124: ## 配置
146: ## 性能
148:   ### 基准测试结果
158: ## 错误处理
164: ## 链接                  ← `## 版本亮点` 节插入到此节之前
176: ## 许可证
-->

<!-- 现有「快速入门」节中 stats 示例（README.md 第 115–120 行） -->
统计分析慢 SQL 和高频 SQL（输出到 `slow_sql.csv` / `frequent_sql.csv`）：

```bash
sqllog2db stats -c config.toml
sqllog2db stats -c config.toml --top 10
```

<!-- 末尾遗留字符（README.md 第 178–179 行）—— 需要清理 -->
基于 Apache License, Version 2.0 许可。详见 [LICENSE](./LICENSE)。
E)。  ← 残留字符，需删除
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 在 README.md `## 快速入门` 节末尾追加 stats --from/--to 时间范围示例</name>

  <read_first>
    - README.md（待修改文件本身——必读，确认 `## 快速入门` 节当前内容、stats 代码块位置约 115–120 行、本节末尾 122 行的 `详细用法参见[快速入门指南]` 句子）
    - .planning/phases/62-docs/62-CONTEXT.md（D-03 锁定示例内容 `sqllog2db stats -c config.toml --from 2024-01-01 --to 2024-01-31`、Claude's Discretion 锁定插入位置）
    - .planning/phases/62-docs/62-PATTERNS.md（已给出 README 追加段落的目标 markdown 文本）
    - src/cli/opts.rs 或 src/cli/stats/（如执行器需要确认 stats --from/--to 真实命令行接受形式；CONTEXT 已给出权威示例文本）
  </read_first>

  <behavior>
    - 行为 1：在 README.md 现有 stats 代码块（第 117–120 行的三行 fenced block）后插入一段过渡说明文字（中文）+ 一个新的 bash fenced code block
    - 行为 2：新代码块包含两条命令示例：(a) `sqllog2db stats -c config.toml --from 2024-01-01 --to 2024-01-31`；(b) `sqllog2db stats -c config.toml --from "2024-01-01 00:00:00" --to "2024-01-31 23:59:59" --top 20`
    - 行为 3：过渡说明文字提及 `v1.14+` 起支持的能力，例如「指定时间范围只统计该窗口内的记录（v1.14+）」
    - 行为 4：保留原 stats 代码块（不删除已有的 `sqllog2db stats -c config.toml` 与 `sqllog2db stats -c config.toml --top 10` 两行）；新示例追加为独立段落与独立 code block
    - 行为 5：本节末尾原句 `详细用法参见[快速入门指南](./docs/quickstart.md)。` 保留不动，且仍在本节末尾
    - 行为 6：不修改 `## 快速入门` 之前与之后的任何二级节
  </behavior>

  <action>
    打开 `README.md`，定位 `## 快速入门` 节（约第 105 行）。在该节现有 stats 示例 fenced code block（包含 `sqllog2db stats -c config.toml` 与 `sqllog2db stats -c config.toml --top 10` 两行，结束 fence 在第 120 行）之后、`详细用法参见[快速入门指南]` 句子之前，插入以下结构的新段落：

    1. 一个空行
    2. 一行中文说明文字：「指定时间范围统计（v1.14+，仅聚合 `ts` 字段落入 `--from` 与 `--to` 区间的记录）：」
    3. 一个空行
    4. 一个 bash fenced code block（开 fence ```bash），包含两行命令：
       - `sqllog2db stats -c config.toml --from 2024-01-01 --to 2024-01-31`
       - `sqllog2db stats -c config.toml --from "2024-01-01 00:00:00" --to "2024-01-31 23:59:59" --top 20`
    5. 关闭 fence ```
    6. 一个空行

    然后保留紧随其后的 `详细用法参见[快速入门指南](./docs/quickstart.md)。` 句子。

    执行 D-03（"在『功能特性』章节中新增 `stats` 子命令的用法示例（`sqllog2db stats -c config.toml --from 2024-01-01 --to 2024-01-31`）"——按 Claude's Discretion 实际插入到 `## 快速入门` 节末尾 stats 段落延续处，文档结构上更自然，且与现有 stats 示例位置一致）。

    不要修改本节之外任何内容；不要触碰徽章区（第 1–9 行）、`## 功能特性`、`## 配置`、`## 链接` 等其他节。
  </action>

  <verify>
    <automated>cd /Users/guang/Projects/sqllog2db && grep -c -- "--from 2024-01-01 --to 2024-01-31" README.md</automated>
    <automated>cd /Users/guang/Projects/sqllog2db && grep -c -- "--from \"2024-01-01 00:00:00\" --to \"2024-01-31 23:59:59\" --top 20" README.md</automated>
    <automated>cd /Users/guang/Projects/sqllog2db && grep -c "v1.14+" README.md</automated>
    <!-- 期望三行 grep -c 输出均为 ≥1。 -->
    <automated>cd /Users/guang/Projects/sqllog2db && grep -c "详细用法参见\[快速入门指南\](./docs/quickstart.md)。" README.md</automated>
    <!-- 末句必须仍存在且仅出现一次。 -->
  </verify>

  <acceptance_criteria>
    - `grep -c "sqllog2db stats -c config.toml --from 2024-01-01 --to 2024-01-31" README.md` 输出 = 1
    - `grep -c "sqllog2db stats -c config.toml --from \"2024-01-01 00:00:00\" --to \"2024-01-31 23:59:59\" --top 20" README.md` 输出 = 1
    - `grep -c "v1.14+" README.md` 输出 ≥ 1（说明已标注版本起点）
    - 原有 `sqllog2db stats -c config.toml` 与 `sqllog2db stats -c config.toml --top 10` 两行仍存在（`grep -c "sqllog2db stats -c config.toml$" README.md` ≥ 1 且 `grep -c "sqllog2db stats -c config.toml --top 10" README.md` = 1）
    - 原句 `详细用法参见[快速入门指南](./docs/quickstart.md)。` 仍存在且只出现一次
    - `## 快速入门` 节标题仍存在（`grep -c "^## 快速入门$" README.md` = 1）
    - 文件中所有 fenced code block 数量为偶数（即所有 ``` 都成对，未破坏 markdown 结构）
  </acceptance_criteria>

  <done>
    `## 快速入门` 节新增 stats --from/--to 示例段落，含两条命令覆盖纯日期与带时分秒两种格式；原 stats 示例和末句指南链接均保留；其他节未被修改。
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: 在 README.md `## 链接` 节之前新增 `## 版本亮点` 节描述 v1.15 CI/CD 修复</name>

  <read_first>
    - README.md（Task 1 完成后的最新版本——必读，确认 `## 错误处理` 节末尾位置约 162 行、`## 链接` 节起始约 164 行）
    - .planning/phases/62-docs/62-CONTEXT.md（D-03 锁定"补充 v1.15 CI/CD 修复内容（aarch64 cross-build、SHA 固定上下文）"）
    - .planning/phases/62-docs/62-PATTERNS.md（已给出 `## 版本亮点` 节的目标 markdown 文本，第 42–52 行参考）
    - Cross.toml（必读，确认 Phase 61 落地的 SHA 固定具体描述，用于撰写要点措辞）
    - .github/workflows/ci.yaml（如可访问，确认 Phase 55 修复的 actions 版本，作为亮点要点的依据）
    - .planning/ROADMAP.md（Phase 55 与 Phase 61 节，确认 v1.15 CI/CD 修复的 4 个工作要点）
  </read_first>

  <behavior>
    - 行为 1：在 `## 错误处理` 节末尾（约第 162 行 `退出码：0（成功）、2（配置错误）、3（文件/解析错误）、4（导出错误）、130（用户中断）。` 之后）与 `## 链接` 标题（约第 164 行）之间，新增一个空行 + `## 版本亮点` 二级标题
    - 行为 2：`## 版本亮点` 节包含一个三级子节 `### v1.15.0 — CI/CD 修复（2026-06-02）`
    - 行为 3：三级子节下含 4 条无序列表项，分别说明：(a) actions/checkout 与 upload-artifact 版本修复；(b) Cross.toml 镜像 SHA256 摘要固定；(c) release workflow 独立 create-release job 消除竞争；(d) handle_run 与 cli/run 模块重构（链接到 Phase 58/59 工作）
    - 行为 4：标题层级（`##` 二级、`###` 三级）与 README 其他章节（如 `## 安装` + `### 从 crates.io 安装（推荐）`）一致
    - 行为 5：保留 `## 链接` 节及其后所有内容（链接列表、`## 许可证` 节）顺序与内容不变
  </behavior>

  <action>
    打开 `README.md`，定位 `## 错误处理` 节末尾（句子 `退出码：0（成功）、2（配置错误）、3（文件/解析错误）、4（导出错误）、130（用户中断）。` 后）。

    在该句子之后、`## 链接` 二级标题之前，插入以下 markdown 结构：

    - 一个空行
    - 二级标题：`## 版本亮点`
    - 一个空行
    - 三级标题：`### v1.15.0 — CI/CD 修复（2026-06-02）`
    - 一个空行
    - 四条无序列表项（每条以 `- ` 开头，独立成行），覆盖以下内容（措辞依据 Cross.toml 与 ROADMAP Phase 55/58/59/61）：
      1. 修复 `.github/workflows/` 下 `ci.yaml`、`bench.yml`、`lychee.yml`、`pages.yml` 中误升级的 `actions/checkout@v6` 与 `actions/upload-artifact@v7`，全部回退到 `@v4`（Phase 55）
      2. 新增 `Cross.toml`，将 aarch64-linux 跨编译镜像 `ghcr.io/cross-rs/aarch64-unknown-linux-gnu` 锁定到 SHA256 摘要，保证可复现构建（Phase 55 + Phase 61）
      3. 重构 `release.yaml`：拆出独立 `create-release` job 在 4 个 matrix 构建 job 之前运行，消除并行写入 release body 的竞争条件（Phase 55）
      4. `cli/run` 模块重构：`handle_run` 拆分为 7 个私有辅助函数，`run_sequential` 压缩到 ≤40 行，并新建 `scanner` 公共扫描模块（Phase 58 + Phase 56）
    - 一个空行

    然后保留紧随其后的 `## 链接` 节及其内容不动。

    Cross.toml 中的镜像源信息必须真实——若 Cross.toml 注释中提到 `cross-rs/aarch64-unknown-linux-gnu`，则使用该完整路径；若仅写 `aarch64-unknown-linux-gnu@sha256:...`，则在列表项中如实描述「锁定到 SHA256 摘要」即可，不要伪造具体 hash。
  </action>

  <verify>
    <automated>cd /Users/guang/Projects/sqllog2db && grep -c "^## 版本亮点$" README.md</automated>
    <automated>cd /Users/guang/Projects/sqllog2db && grep -c "^### v1.15.0" README.md</automated>
    <automated>cd /Users/guang/Projects/sqllog2db && grep -c "Cross\.toml" README.md</automated>
    <automated>cd /Users/guang/Projects/sqllog2db && grep -c "aarch64" README.md</automated>
    <automated>cd /Users/guang/Projects/sqllog2db && grep -c "actions/checkout" README.md</automated>
    <!-- 期望所有 grep -c 输出均 ≥ 1。 -->
    <automated>cd /Users/guang/Projects/sqllog2db && awk '/^## 版本亮点$/{found=1} /^## 链接$/{if(found) print "ORDER_OK"; exit}' README.md</automated>
    <!-- 验证「版本亮点」节在「链接」节之前出现，期望输出 "ORDER_OK"。 -->
  </verify>

  <acceptance_criteria>
    - `grep -c "^## 版本亮点$" README.md` 输出 = 1
    - `grep -c "^### v1.15.0" README.md` 输出 = 1（v1.15.0 子节标题存在）
    - `grep -i "SHA\|aarch64\|cross" README.md | wc -l` 输出 ≥ 3（DOC-01 验证：含 Cross/aarch64/SHA 关键词）
    - `grep "actions/checkout" README.md` 至少出现 1 次（actions 版本修复说明）
    - awk 验证「版本亮点」节出现在「链接」节之前，输出 `ORDER_OK`
    - `## 链接` 节标题、`## 许可证` 节标题仍存在且顺序不变
    - 文件中所有 fenced code block 仍成对（验证未破坏 markdown 结构）
  </acceptance_criteria>

  <done>
    `README.md` 新增 `## 版本亮点` 节，包含 v1.15.0 CI/CD 修复 4 项要点，覆盖 actions 版本修复、Cross.toml SHA 固定、release workflow 重构、cli/run 重构；节顺序正确（错误处理 → 版本亮点 → 链接 → 许可证）；DOC-01 Success Criteria #1 关于 v1.15 CI/CD 修复说明部分达成。
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: 清理 README.md 末尾遗留字符并最终验证 DOC-01</name>

  <read_first>
    - README.md（Task 1 + Task 2 完成后的最新版本——必读，确认末尾第 178–179 行的孤立字符 `E)。`，该字符是历史编辑残留，不属于任何句子）
    - .planning/ROADMAP.md（Phase 62 Success Criteria #1，作为最终验证依据）
  </read_first>

  <behavior>
    - 行为 1：删除 README.md 末尾紧跟 `基于 Apache License, Version 2.0 许可。详见 [LICENSE](./LICENSE)。` 之后的孤立 `E)。` 字符串（独占一行的残留字符）
    - 行为 2：删除后文件以「基于 Apache License, Version 2.0 许可。详见 [LICENSE](./LICENSE)。」加单个换行结尾，符合 markdown 文件末尾规范
    - 行为 3：DOC-01 三项 Success Criteria 全部可验证（stats --from/--to 示例存在、v1.15 CI/CD 修复说明存在、功能列表与代码一致——后者无需改动）
  </behavior>

  <action>
    打开 `README.md`，定位末尾两行：
    ```
    基于 Apache License, Version 2.0 许可。详见 [LICENSE](./LICENSE)。
    E)。
    ```

    删除第二行 `E)。`（含其换行符），使文件以 `基于 Apache License, Version 2.0 许可。详见 [LICENSE](./LICENSE)。` 行结尾（保留该行末尾的单个 `\n`）。

    然后执行 DOC-01 完整验证：
    1. README 含 stats --from/--to 示例（Task 1 已交付）
    2. README 含 v1.15 CI/CD 修复说明（Task 2 已交付）
    3. README 功能列表与代码一致（当前『简洁的 CLI：init / validate / run / stats』描述已覆盖 stats，无需改动）

    若验证发现遗漏，回到 Task 1 或 Task 2 调整；不要在本任务中重新写入这些段落。

    本任务不修改 README 其他内容，仅做末尾清理 + 最终断言。
  </action>

  <verify>
    <automated>cd /Users/guang/Projects/sqllog2db && tail -3 README.md</automated>
    <automated>cd /Users/guang/Projects/sqllog2db && grep -c "^E)。$" README.md</automated>
    <!-- 期望 grep -c 输出 0（残留字符已清理）。 -->
    <automated>cd /Users/guang/Projects/sqllog2db && grep -c -- "--from" README.md</automated>
    <automated>cd /Users/guang/Projects/sqllog2db && grep -c "版本亮点" README.md</automated>
    <automated>cd /Users/guang/Projects/sqllog2db && grep -c "init / validate / run / stats" README.md</automated>
    <!-- 功能列表中 stats 已被列出（旧文已有，不需新增）。 -->
  </verify>

  <acceptance_criteria>
    - `grep -c "^E)。$" README.md` 输出 = 0（残留行已删除）
    - `tail -1 README.md` 输出为 `基于 Apache License, Version 2.0 许可。详见 [LICENSE](./LICENSE)。`
    - `grep -c -- "--from" README.md` 输出 ≥ 2（来自 Task 1 的两条示例）
    - `grep -c "^## 版本亮点$" README.md` 输出 = 1（Task 2 章节存在）
    - `grep -c "init / validate / run / stats" README.md` 输出 ≥ 1（功能列表含 stats，DOC-01 第三条达成——原文已有）
    - 二级节顺序正确：`## 功能特性` → `## 架构` → `## 安装` → `## 快速入门` → `## 配置` → `## 性能` → `## 错误处理` → `## 版本亮点` → `## 链接` → `## 许可证`（可用 `grep "^## " README.md` 列出后目视确认）
    - 文件末尾无孤立字符或多余空行
  </acceptance_criteria>

  <done>
    README.md 末尾遗留字符 `E)。` 已清理；DOC-01 三项 Success Criteria 全部达成（stats --from/--to 示例 + v1.15 CI/CD 修复说明 + 功能列表完整）；README 结构正确，二级节顺序为「功能特性→架构→安装→快速入门→配置→性能→错误处理→版本亮点→链接→许可证」。
  </done>
</task>

</tasks>

<verification>
- `grep -c "sqllog2db stats -c config.toml --from 2024-01-01 --to 2024-01-31" README.md` = 1
- `grep -c "^## 版本亮点$" README.md` = 1
- `grep -c "^### v1.15.0" README.md` = 1
- `grep -c "^E)。$" README.md` = 0
- `grep "^## " README.md` 输出 10 个二级节标题，顺序为「功能特性→架构→安装→快速入门→配置→性能→错误处理→版本亮点→链接→许可证」
- `awk '/^## 版本亮点$/{found=1} /^## 链接$/{if(found) print "ORDER_OK"; exit}' README.md` 输出 `ORDER_OK`
</verification>

<success_criteria>
- ROADMAP Phase 62 Success Criteria #1 达成：README.md 包含 stats 子命令的用法示例（含 --from/--to 参数）、v1.15 CI/CD 修复说明，功能列表与当前代码一致
- DOC-01 requirement 完成
- README.md 末尾遗留字符已清理
</success_criteria>

<output>
Create `.planning/phases/62-docs/62-02-SUMMARY.md` when done
</output>
