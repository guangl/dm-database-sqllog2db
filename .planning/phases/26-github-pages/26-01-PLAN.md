---
phase: 26-github-pages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - site/src/SUMMARY.md
  - site/src/index.md
  - site/book.toml
  - site/src/quickstart.md
  - site/src/config-reference.md
  - site/src/architecture.md
  - site/src/contributing.md
  - site/src/security.md
autonomous: true
requirements:
  - PAGES-01

must_haves:
  truths:
    - 用户在 GitHub Pages 上看到的是多页导航结构（侧边栏含四个章节），不是单页落地页
    - 用户可以点击导航栏中"快速入门"链接，阅读快速入门文档全文
    - 用户可以点击导航栏中"配置参考"链接，阅读配置参考文档全文
    - 用户可以点击导航栏中"架构说明"链接，阅读架构文档全文
    - 用户可以点击导航栏中"贡献指南"链接，阅读贡献指南全文
    - 用户可以点击"贡献指南"子项"安全策略"链接，阅读安全策略全文
    - 文档站首页提供项目概述、安装命令和章节导航
    - 文档站支持搜索功能（mdBook 默认启用）
    - GitHub Actions 自动构建并部署多页文档站
  artifacts:
    - path: "site/src/SUMMARY.md"
      provides: "多页导航目录 — 四章结构"
      contains: "快速入门|配置参考|架构说明|贡献指南"
    - path: "site/src/quickstart.md"
      provides: "快速入门章节页面"
      contains: "{{#include"
    - path: "site/src/config-reference.md"
      provides: "配置参考章节页面"
      contains: "{{#include"
    - path: "site/src/architecture.md"
      provides: "架构说明章节页面"
      contains: "{{#include"
    - path: "site/src/contributing.md"
      provides: "贡献指南章节页面"
      contains: "{{#include"
    - path: "site/src/security.md"
      provides: "安全策略子页面"
      contains: "{{#include"
    - path: "site/src/index.md"
      provides: "首页概览 — 项目简介、安装命令、章节入口"
      min_lines: 20
    - path: "site/book.toml"
      provides: "mdBook 构建配置（description 中文化）"
      contains: 'language = "zh"'
  key_links:
    - from: "site/src/SUMMARY.md"
      to: "site/src/quickstart.md"
      via: "导航链接 quickstart.md"
      pattern: "quickstart"
    - from: "site/src/SUMMARY.md"
      to: "site/src/config-reference.md"
      via: "导航链接 config-reference.md"
      pattern: "config-reference"
    - from: "site/src/SUMMARY.md"
      to: "site/src/architecture.md"
      via: "导航链接 architecture.md"
      pattern: "architecture"
    - from: "site/src/SUMMARY.md"
      to: "site/src/contributing.md"
      via: "导航链接 contributing.md"
      pattern: "contributing"
    - from: "site/src/SUMMARY.md"
      to: "site/src/security.md"
      via: "导航子链接 security.md"
      pattern: "security"
    - from: "site/src/quickstart.md"
      to: "docs/quickstart.md"
      via: "{{#include}} 嵌入"
      pattern: "{{#include.*quickstart"
    - from: "site/src/config-reference.md"
      to: "docs/config-reference.md"
      via: "{{#include}} 嵌入"
      pattern: "{{#include.*config-reference"
    - from: "site/src/architecture.md"
      to: "docs/architecture.md"
      via: "{{#include}} 嵌入"
      pattern: "{{#include.*architecture"
    - from: "site/src/contributing.md"
      to: "CONTRIBUTING.md"
      via: "{{#include}} 嵌入"
      pattern: "{{#include.*CONTRIBUTING"
    - from: "site/src/security.md"
      to: "SECURITY.md"
      via: "{{#include}} 嵌入"
      pattern: "{{#include.*SECURITY"
    - from: ".github/workflows/pages.yml"
      to: "site/book/"
      via: "mdBook build 输出"
      pattern: "mdbook build site"
---

<objective>
将 GitHub Pages 从单页落地页升级为 mdBook 多页中文文档站。创建四大章节页面（快速入门、配置参考、架构说明、贡献指南/安全策略），通过 `{{#include}}` 嵌入 Phase 24+25 的中文化源文件内容，确保内容不重复维护。首页改为概览页，提供项目简介和章节入口。

Purpose: 用户访问 GitHub Pages 时看到的是带导航栏和搜索功能的多页文档站，能够按章节浏览全中文项目文档，而非单页落地页。
Output: 重构的 SUMMARY.md（四章导航）、首页 index.md（概览）、5 个章节页面文件（嵌入源文档）、中文化 book.toml description。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@site/book.toml
@site/src/SUMMARY.md
@site/src/index.md
@site/theme/custom.css
@.github/workflows/pages.yml

<interfaces>
<!-- 当前站点结构（执行前状态）：
     site/src/ 目录包含：
       - SUMMARY.md (单页: "[sqllog2db](index.md)")
       - index.md (Phase 24 已翻译的中文单页落地页，无 SVG，大约 200+ 行)
       - asciicast/demo.cast (终端录像文件)

     site/book.toml 关键配置：
       - language = "zh" (Phase 24 已改)
       - [output.html.fold] enable = true, level = 1
       - create-missing = false
       - additional-css = ["theme/custom.css"]

     Phase 24+25 执行完毕后的源文件（{pwd} 根目录）：
       - README.md (中文，H1: "# sqllog2db")
       - docs/quickstart.md (中文，H1: "# QuickStart Guide")
       - docs/config-reference.md (中文，H1: "# Configuration Reference")
       - docs/architecture.md (中文，H1: "# 架构说明" 或类似)
       - CONTRIBUTING.md (中文，H1: "# 贡献指南" 或类似)
       - SECURITY.md (中文，H1: "# 安全策略" 或类似)
-->

<!-- pages.yml 部署流水线：
     - mdbook build site → site/book/ 输出
     - peaceiris/actions-gh-pages 将 site/book/ 推送到 gh-pages 分支
     - 触发条件: push to main, paths: ["site/**"]
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 重构 SUMMARY.md 为四章导航并创建章节页面</name>
  <files>
    site/src/SUMMARY.md,
    site/src/quickstart.md,
    site/src/config-reference.md,
    site/src/architecture.md,
    site/src/contributing.md,
    site/src/security.md
  </files>
  <read_first>
    site/src/SUMMARY.md,
    @.planning/phases/26-github-pages/26-CONTEXT.md (D-01 四大章节结构)
  </read_first>
  <action>
    **1. 重写 site/src/SUMMARY.md:**

    将当前单行 "[sqllog2db](index.md)" 替换为以下四章导航结构（D-01，全中文）:

    ```markdown
    # Summary

    [简介](index.md)
    [快速入门](quickstart.md)
    [配置参考](config-reference.md)
    [架构说明](architecture.md)
    [贡献指南](contributing.md)
      - [安全策略](security.md)
    ```

    说明：
    - 第一章"快速入门"对应 docs/quickstart.md
    - 第二章"配置参考"对应 docs/config-reference.md
    - 第三章"架构说明"对应 docs/architecture.md
    - 第四章"贡献指南"包括两个子页面：contributing.md（主页面）和 security.md（子页面）
    - 子页面"安全策略"在 SUMMARY.md 中用两个空格缩进表示层级关系，mdBook 自动以此为依据在侧边栏渲染可折叠结构

    **2. 创建 site/src/quickstart.md:**

    内容仅包含以下 mdBook include 指令，不添加额外标题或文字（由源文件 H1 提供页面标题）:

    ```
    {{#include ../../docs/quickstart.md}}
    ```

    **3. 创建 site/src/config-reference.md:**

    ```
    {{#include ../../docs/config-reference.md}}
    ```

    **4. 创建 site/src/architecture.md:**

    ```
    {{#include ../../docs/architecture.md}}
    ```

    **5. 创建 site/src/contributing.md:**

    ```
    {{#include ../../CONTRIBUTING.md}}
    ```

    **6. 创建 site/src/security.md:**

    ```
    {{#include ../../SECURITY.md}}
    ```

    路径说明：CWD 是 site/ 目录下的 mdbook build，所以 site/src/ 下的文件需要 `../../` 回到项目根目录来引用 `docs/quickstart.md`、`CONTRIBUTING.md` 等文件。mdBook 的 `{{#include}}` 路径相对于当前文件。
  </action>
  <verify>
    <automated>
      # 验证 SUMMARY.md 包含全部四个章节
      grep -q '快速入门' site/src/SUMMARY.md || echo "FAIL: missing 快速入门 in SUMMARY"
      grep -q '配置参考' site/src/SUMMARY.md || echo "FAIL: missing 配置参考 in SUMMARY"
      grep -q '架构说明' site/src/SUMMARY.md || echo "FAIL: missing 架构说明 in SUMMARY"
      grep -q '贡献指南' site/src/SUMMARY.md || echo "FAIL: missing 贡献指南 in SUMMARY"
      grep -q '安全策略' site/src/SUMMARY.md || echo "FAIL: missing 安全策略 in SUMMARY"

      # 验证所有章节页面文件存在
      for f in quickstart config-reference architecture contributing security; do
        test -f "site/src/$f.md" || echo "FAIL: missing site/src/$f.md"
      done

      # 验证所有章节页面包含 {{#include}} 指令
      grep -q '{{#include' site/src/quickstart.md || echo "FAIL: quickstart.md missing include"
      grep -q '{{#include' site/src/config-reference.md || echo "FAIL: config-reference.md missing include"
      grep -q '{{#include' site/src/architecture.md || echo "FAIL: architecture.md missing include"
      grep -q '{{#include' site/src/contributing.md || echo "FAIL: contributing.md missing include"
      grep -q '{{#include' site/src/security.md || echo "FAIL: security.md missing include"
    </automated>
  </verify>
  <acceptance_criteria>
    - site/src/SUMMARY.md 包含"快速入门""配置参考""架构说明""贡献指南""安全策略"五个导航条目（四章 + 一个子页面）
    - 子页面"安全策略"在"贡献指南"下方缩进
    - site/src/ 下存在 quickstart.md、config-reference.md、architecture.md、contributing.md、security.md 五个文件
    - 每个章节文件仅包含 `{{#include}}` 指令，无额外内容
    - site/src/index.md 保持不变（由 Task 2 修改，Task 3 统一验证）
    - site/src/SUMMARY.md 保留第一行 "# Summary"
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: 重写首页为概览页 + 更新 book.toml description 为中文化</name>
  <files>
    site/src/index.md,
    site/book.toml
  </files>
  <read_first>
    site/src/index.md,
    site/book.toml
  </read_first>
  <action>
    **1. 重写 site/src/index.md 为简洁概览页：**

    将当前 Phase 24 翻译后的大型单页文档替换为简洁的项目概览页。页面结构如下（按顺序，使用中文）：

    - **标题行 + 徽章**：保留当前所有徽章行不变（Crates.io、CI、License、Release、Rust 1.85+）
    - **项目简介段**：一句中文概要，如 "**达梦数据库 SQL 日志高性能解析工具** — 流式处理百万级记录，常量内存占用，支持 CSV/SQLite 导出，内置 SQL 模板分析与图表。"
    - **分隔线 `---`**
    - **快速安装**：保留三个核心命令（`cargo install`、`sqllog2db init`、`sqllog2db run`），使用代码块
    - **分隔线 `---`**
    - **文档章节导航**：以无序列表列出四个章节，每项包含中文名称、文件名链接和一句话描述：
      - [快速入门](quickstart.md) — 从安装到导出，逐步引导完成常见使用场景
      - [配置参考](config-reference.md) — 所有配置项的完整说明
      - [架构说明](architecture.md) — 数据流、模块划分和关键设计
      - [贡献指南](contributing.md) — 环境搭建、编码规约和 PR 提交流程
    - **分隔线 `---`**
    - **链接**：保留 GitHub 仓库链接、crates.io、Changelog、License 四个链接
    - **尾注**：保留 `*Built with mdBook. Deployed via GitHub Actions.*` 但去除 "Charts rendered by plotters"（SVG charts 已移除）
    - **不做 include**: 特别注意不要在这个文件中使用 `{{#include}}` 引入 README.md，因为 README 内容应分布在各自的章节页面中，首页仅作概览

    约束：
    - 不包含 SVG Gallery 相关内容（Phase 24 已移除）
    - 不包含 Demo / terminal recording 段落（属于 README 内容，不在首页）
    - 保持 asciicast 目录不动（后续使用不依赖此文件）
    - 代码块、链接、徽章行的原始 Markdown 格式保持正确

    **2. 更新 site/book.toml description 为中文化：**

    将 `[book]` 段的 `description` 英文改为中文：

    ```toml
    description = "达梦数据库 SQL 日志高性能解析工具 — 流式处理，常量内存，支持 CSV/SQLite 导出"
    ```

    约束（D-02）:
    - 不修改 `language` 字段（Phase 24 已改为 "zh"）
    - 保留 `title`、`authors` 字段不变
    - 保留 `[output.html]` 段所有配置不变
    - 保留 `[build]` 段 `create-missing = false` 不变
  </action>
  <verify>
    <automated>
      # index.md 验证
      grep -q 'cargo install' site/src/index.md || echo "FAIL: index missing install command"
      grep -q '快速入门' site/src/index.md || echo "FAIL: index missing chapter nav"
      grep -q 'Built with' site/src/index.md || echo "FAIL: index missing footer"
      # 排除常见错误
      ! grep -q '{{#include' site/src/index.md || echo "WARN: index should not use include for README"

      # book.toml 验证
      grep -q 'language = "zh"' site/book.toml || echo "FAIL: book.toml language not zh"
      grep -q '达梦数据库' site/book.toml || echo "FAIL: book.toml description not Chinese"
      # 确保 title 和作者未被意外修改
      grep -q 'title = "sqllog2db"' site/book.toml || echo "FAIL: book.toml title changed"
      grep -q 'authors = \["guangl"\]' site/book.toml || echo "FAIL: book.toml authors changed"
    </automated>
  </verify>
  <acceptance_criteria>
    - site/src/index.md 内容为简洁概览页，包含徽章行、项目简介、安装命令、四章导航链接、GitHub 链接
    - site/src/index.md 不包含 `{{#include}}` 指令
    - site/src/index.md 不包含 SVG Gallery 相关段落
    - site/book.toml 的 `description` 字段值为中文描述
    - site/book.toml 的 `language` 保持为 `"zh"`（未修改）
    - site/book.toml 的 `title`、`authors` 字段未改变
    - site/book.toml 的 `[output.html]`、`[build]` 配置未改变
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: 构建并验证多页文档站输出</name>
  <files>
    site/book/ (输出目录)
  </files>
  <read_first>
    .github/workflows/pages.yml
  </read_first>
  <action>
    运行 mdBook 构建并验证多页输出结构正确。

    **1. 执行构建：**

    运行 `mdbook build site` 并确认退出码为 0。

    **2. 验证输出结构：**

    确认 site/book/ 目录下包含以下 HTML 文件（即每个章节都被正确渲染为独立页面）：
    - site/book/index.html（首页概览）
    - site/book/quickstart/index.html（快速入门）
    - site/book/config-reference/index.html（配置参考）
    - site/book/architecture/index.html（架构说明）
    - site/book/contributing/index.html（贡献指南）
    - site/book/contributing/security/index.html（安全策略，作为贡献指南的子页面放置在同一目录下的 security/ 子目录）

    注意：mdBook 的 HTML 输出路径与源文件路径一致。由于 SUMMARY.md 中 security.md 没有指定子目录前缀，默认输出为 site/book/security/index.html。验证存在的实际路径即可。

    **3. 验证 pages.yml 兼容性：**

    确认 .github/workflows/pages.yml 中的构建命令 (`mdbook build site`) 和发布目录 (`site/book`) 与当前站点结构兼容，无需修改。

    分析：pages.yml 使用 `mdbook build site`（输出到 site/book/），再使用 `peaceiris/actions-gh-pages` 将 site/book/ 部署到 gh-pages 分支。由于输出结构从单页变为多页，但 mdBook 的构建命令和输出路径不变，因此 pages.yml 无需任何修改。
  </action>
  <verify>
    <automated>
      mdbook build site || echo "FAIL: mdbook build failed"

      # 检查所有章节 HTML 文件存在
      for page in index quickstart config-reference architecture contributing; do
        test -f "site/book/$page/index.html" && echo "OK: $page/index.html exists" || echo "FAIL: missing site/book/$page/index.html"
      done

      # 检查安全策略页面
      test -f "site/book/security/index.html" && echo "OK: security/index.html exists" || echo "FAIL: missing site/book/security/index.html"

      # 验证首页包含章节导航链接（验证多页链接正常工作）
      grep -q 'quickstart' site/book/index.html || echo "WARN: index page missing quickstart link"
    </automated>
  </verify>
  <acceptance_criteria>
    - `mdbook build site` 退出码为 0，无构建错误
    - site/book/ 下存在 index.html、quickstart/index.html、config-reference/index.html、architecture/index.html、contributing/index.html、security/index.html 共 6 个页面
    - pages.yml 无需修改（构建命令和发布路径兼容多页输出）
    - `cargo clippy --all-targets -- -D warnings` 不受影响（无 Rust 代码变更）
  </acceptance_criteria>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 文档内容 → 渲染输出 | 源文档通过 mdBook 渲染为静态 HTML。所有内容来自项目已审核的 markdown 源文件（Phase 24+25 中文化产出），无用户输入或动态内容引入 |
| 部署流水线 | pages.yml 使用 GITHUB_TOKEN (自动限定为当前仓库)，构建后仅将 site/book/ 推送到 gh-pages 分支 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-01 | Spoofing | book.toml content | accept | book.toml 为版本控制中的项目配置文件，不受外部用户控制 |
| T-26-02 | Tampering | site/src/ 目录文件 | mitigate | 所有文档页面通过 {{#include}} 引用项目根目录的源文件（README.md、docs/*.md、CONTRIBUTING.md、SECURITY.md），这些文件已在 Phase 24+25 中审核。站点不引入外部内容 |
| T-26-03 | Information Disclosure | site/book/ 部署产物 | accept | 静态 HTML 站点仅包含公开文档内容，不含密钥、密钥轮换信息、数据库连接串等保密数据 |
| T-26-04 | Denial of Service | mdbook build | accept | 文档构建为本地进程，无网络暴露面。pages.yml 仅在 main 分支 site/** 变更时触发 |
| T-26-SC | Tampering | no packages | accept | 本计划不涉及任何包安装操作。mdBook 已通过 actions-mdbook action 安装，由 GitHub Actions 托管环境管理 |
</threat_model>

<verification>

全自动验证步骤：

```bash
# 1. SUMMARY.md 结构验证
grep -c '快速入门' site/src/SUMMARY.md | xargs test 1 -eq
grep -c '配置参考' site/src/SUMMARY.md | xargs test 1 -eq
grep -c '架构说明' site/src/SUMMARY.md | xargs test 1 -eq
grep -c '贡献指南' site/src/SUMMARY.md | xargs test 1 -eq
grep -c '安全策略' site/src/SUMMARY.md | xargs test 1 -eq

# 2. 章节文件存在且含 include
for f in quickstart config-reference architecture contributing security; do
  test -f "site/src/$f.md" && grep -q '{{#include' "site/src/$f.md"
done

# 3. index.md 为简洁概览页
grep -q 'cargo install' site/src/index.md
grep -q '快速入门' site/src/index.md
! grep -q '{{#include' site/src/index.md

# 4. book.toml 中文化
grep -q 'language = "zh"' site/book.toml
grep -q '达梦数据库' site/book.toml

# 5. 多页构建验证
mdbook build site
test -f site/book/index.html
test -f site/book/quickstart/index.html
test -f site/book/config-reference/index.html
test -f site/book/architecture/index.html
test -f site/book/contributing/index.html
test -f site/book/security/index.html
```

</verification>

<success_criteria>

- 用户在 GitHub Pages 上看到的是多页文档站（侧边栏导航含四个中文章节 + 搜索框），而非单页内容
- 点击侧边栏各章节可导航到对应文档页面，内容正确渲染
- 首页提供项目概览、安装命令和章节入口链接
- 章节页面内容通过 `{{#include}}` 从项目根目录源文件引入，无内容重复维护
- site/book.toml 的 description 为中文、language 为 "zh"
- GitHub Actions（pages.yml）自动构建部署，无需额外配置变更
- custom.css 主题样式保留，多页布局兼容现有样式

</success_criteria>

<output>
创建 `.planning/phases/26-github-pages/26-01-SUMMARY.md` 描述执行结果和关键决策。
</output>
