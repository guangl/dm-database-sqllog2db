---
phase: 67-prog-diag
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/error.rs
  - src/config/mod.rs
  - src/cli/run/processor.rs
autonomous: true
requirements: [DIAG-01, DIAG-02]
must_haves:
  truths:
    - "ErrorStats 新增 by_type / filtered_out / parse_error_records 三字段，merge() 同步累加"
    - "ErrorKind 枚举三值（EncodingError/FieldMissing/ParseFailed），classify_error_kind 启发式分类"
    - "Config.error: Option<ErrorLogConfig> 字段；[error] TOML 段从此处接入（之前被静默忽略）"
    - "process_log_file 解析失败路径收集 ParseErrorRecord（上限 10000）+ by_type 同步递增"
    - "truncate_to_120_chars 对多字节 UTF-8 安全截断（不在字符中间切）"
  artifacts:
    - path: "src/error.rs"
      provides: "ErrorKind, ParseErrorRecord, classify_error_kind, truncate_to_120_chars, ErrorStats 新字段 + merge 扩展"
      contains: "pub enum ErrorKind"
    - path: "src/config/mod.rs"
      provides: "ErrorLogConfig struct + Config.error: Option<ErrorLogConfig>"
      contains: "pub struct ErrorLogConfig"
    - path: "src/cli/run/processor.rs"
      provides: "process_log_file Err 路径调用 classify_error_kind + 收集 ParseErrorRecord"
      contains: "classify_error_kind"
  key_links:
    - from: "src/cli/run/processor.rs Err(e) 路径"
      to: "ErrorStats::add_parse_error_with_kind / parse_error_records.push"
      via: "ParseError::InvalidFormat { raw, line_number } 模式匹配"
      pattern: "ParseError::InvalidFormat"
    - from: "src/config/mod.rs Config"
      to: "ErrorLogConfig"
      via: "#[serde(default)] pub error: Option<ErrorLogConfig>"
      pattern: "pub error: Option<ErrorLogConfig>"
---

<objective>
DIAG-01/DIAG-02：扩展 ErrorStats 与 Config，在 process_log_file 的解析失败路径收集行号 + 原文截断 + 分类。

Purpose: 为 Plan 03（hint、摘要、写出 error log）准备数据底盘。
Output: ErrorKind 枚举 + ParseErrorRecord + 分类/截断函数 + Config.error 字段 + processor 收集逻辑 + 单元测试 GREEN。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/67-prog-diag/67-CONTEXT.md
@.planning/phases/67-prog-diag/67-RESEARCH.md
@.planning/phases/67-prog-diag/67-PATTERNS.md
@src/error.rs
@src/config/mod.rs
@src/cli/run/processor.rs

<interfaces>
当前 ErrorStats（src/error.rs:27-33）：4 字段 total_errors/parse_errors/export_errors/fatal_error，#[derive(Default, Clone)]
目标新增：
  pub by_type: HashMap<ErrorKind, u64>
  pub filtered_out: u64
  pub parse_error_records: Vec<ParseErrorRecord>

ParseErrorRecord：
  pub file_path: String
  pub line_number: u64
  pub raw_truncated: String
  pub kind: ErrorKind

ErrorKind：
  pub enum ErrorKind { EncodingError, FieldMissing, ParseFailed }
  derive Debug, Clone, Copy, PartialEq, Eq, Hash

classify_error_kind(raw: &str) -> ErrorKind 规则（D-12）：
  raw.contains('\\u{FFFD}') -> EncodingError
  raw.starts_with("(EP[") -> FieldMissing
  else -> ParseFailed

dm_database_parser_sqllog::ParseError::InvalidFormat { raw: String, line_number: u64 } 已可解构。
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — ErrorKind/分类/截断/merge 单元测试（RED）</name>
  <files>src/error.rs</files>
  <read_first>
    - src/error.rs:1-68（ErrorStats 现有 struct/方法/merge 模式）
    - src/error.rs:567-665（现有测试块风格）
    - .planning/phases/67-prog-diag/67-VALIDATION.md（Wave 0 条目）
  </read_first>
  <behavior>
    - test_classify_error_kind: classify_error_kind("含\u{FFFD}字符") == EncodingError；classify_error_kind("(EP[ 不完整") == FieldMissing；classify_error_kind("普通乱码") == ParseFailed。
    - test_truncate_to_120_chars: ASCII 长度 200 → 输出长度 120；输入 "中" 重复 150 次（每字符 3 字节）→ 输出 char_count == 120 且 byte 边界合法（utf8 解析成功）；输入空串 → 空串；输入 100 字符 → 原样返回。
    - test_error_stats_by_type_merge: a.by_type 含 (EncodingError=2)，b.by_type 含 (EncodingError=3, FieldMissing=1)，a.merge(&b) 后 by_type[EncodingError]==5、by_type[FieldMissing]==1。
    - test_error_stats_merge_propagates_filtered_and_records: a.filtered_out=2, b.filtered_out=5 → merge 后 7；parse_error_records 累加（a 1 条 + b 2 条 → 3 条）。
  </behavior>
  <action>
    在 src/error.rs 末尾 mod tests 块内追加 4 个 #[test] 函数：test_classify_error_kind / test_truncate_to_120_chars / test_error_stats_by_type_merge / test_error_stats_merge_propagates_filtered_and_records。测试以新签名调用 classify_error_kind、truncate_to_120_chars、ErrorStats { ... by_type, filtered_out, parse_error_records }。由于这些类型尚未存在，本任务后 cargo build 编译失败 = RED。所有 \\u{FFFD} 字符串用 "\\u{FFFD}" 字面量。per D-11/D-12, RESEARCH.md Pattern 4。
  </action>
  <verify>
    <automated>cargo build --lib 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - grep -c 'fn test_classify_error_kind' src/error.rs 输出 1
    - grep -c 'fn test_truncate_to_120_chars' src/error.rs 输出 1
    - grep -c 'fn test_error_stats_by_type_merge' src/error.rs 输出 1
    - grep -c 'fn test_error_stats_merge_propagates_filtered_and_records' src/error.rs 输出 1
    - cargo build --lib 报 "cannot find type/function" 错误（确认 RED）
  </acceptance_criteria>
  <done>4 个测试函数写入；cargo build 报告未定义 ErrorKind/classify_error_kind/truncate_to_120_chars/字段不存在。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 扩展 ErrorStats + ErrorKind/ParseErrorRecord + 辅助函数（GREEN）</name>
  <files>src/error.rs, src/config/mod.rs</files>
  <read_first>
    - src/error.rs:1-68（现有 ErrorStats / impl 块 / merge）
    - src/config/mod.rs:1-52（Config 字段列表与 from_file）
    - .planning/phases/67-prog-diag/67-RESEARCH.md Pattern 3、Pattern 4、Pattern 5
    - .planning/phases/67-prog-diag/67-PATTERNS.md "Shared Patterns: HashMap entry API"
  </read_first>
  <action>
    步骤 A — src/error.rs:
      1) 文件顶部 use 追加：use std::collections::HashMap;
      2) 新增 pub enum ErrorKind { EncodingError, FieldMissing, ParseFailed }，derive (Debug, Clone, Copy, PartialEq, Eq, Hash)。
      3) impl ErrorKind 实现方法 kind_display(&self) -> &'static str：EncodingError -> "encoding_error"；FieldMissing -> "field_missing"；ParseFailed -> "parse_failed"。
      4) 新增 pub struct ParseErrorRecord { pub file_path: String, pub line_number: u64, pub raw_truncated: String, pub kind: ErrorKind }，derive (Debug, Clone)。
      5) 新增 pub fn classify_error_kind(raw: &str) -> ErrorKind：按 D-12 顺序判断（\\u{FFFD} → EncodingError；starts_with("(EP[") → FieldMissing；else → ParseFailed）。
      6) 新增 pub fn truncate_to_120_chars(raw: &str) -> String：按 RESEARCH Pattern 4 实现（raw.char_indices().nth(120).map_or(raw.len(), |(i,_)| i)）。
      7) ErrorStats 新增三字段（保留 #[derive(Debug, Default, Clone)]）：
            pub by_type: HashMap<ErrorKind, u64>,
            pub filtered_out: u64,
            pub parse_error_records: Vec<ParseErrorRecord>,
      8) impl ErrorStats 新增方法：
            pub fn add_parse_error_with_kind(&mut self, kind: ErrorKind) {
                self.add_parse_error();
                *self.by_type.entry(kind).or_insert(0) += 1;
            }
      9) merge() 扩展：
            for (kind, count) in &other.by_type {
                *self.by_type.entry(*kind).or_insert(0) += count;
            }
            self.filtered_out += other.filtered_out;
            self.parse_error_records.extend(other.parse_error_records.iter().cloned());
    步骤 B — src/config/mod.rs:
      10) 新增 pub struct ErrorLogConfig { pub file: String }，derive (Debug, Deserialize, Clone)。pub use 不导出（仅内部使用）。
      11) Config 新增字段（在 stats 字段后）：#[serde(default)] pub error: Option<ErrorLogConfig>,
    步骤 C：cargo fmt + cargo clippy --all-targets -- -D warnings + cargo test --lib error::tests。
    注意：保留 ErrorStats #[derive(Default)]（HashMap/Vec 默认即空，无需手 impl）。per D-07/D-08/D-11/D-13。
  </action>
  <verify>
    <automated>cargo clippy --all-targets -- -D warnings 2>&1 | tail -10 && cargo test --lib error::tests 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - grep -c 'pub enum ErrorKind' src/error.rs 输出 1
    - grep -c 'pub struct ParseErrorRecord' src/error.rs 输出 1
    - grep -c 'pub fn classify_error_kind' src/error.rs 输出 1
    - grep -c 'pub fn truncate_to_120_chars' src/error.rs 输出 1
    - grep -c 'pub fn add_parse_error_with_kind' src/error.rs 输出 1
    - grep -c 'pub by_type: HashMap<ErrorKind, u64>' src/error.rs 输出 1
    - grep -c 'pub filtered_out: u64' src/error.rs 输出 1
    - grep -c 'pub parse_error_records: Vec<ParseErrorRecord>' src/error.rs 输出 1
    - grep -c 'pub struct ErrorLogConfig' src/config/mod.rs 输出 1
    - grep -E 'pub error: Option<ErrorLogConfig>' src/config/mod.rs 命中 1 行
    - cargo clippy --all-targets -- -D warnings 通过
    - cargo test --lib error::tests::test_classify_error_kind / test_truncate_to_120_chars / test_error_stats_by_type_merge / test_error_stats_merge_propagates_filtered_and_records 4 项全 GREEN
    - 现有 error::tests 全部不回归（test_error_stats_merge_accumulates_counts 等）
  </acceptance_criteria>
  <done>4 个 Wave 0 测试 GREEN；clippy 通过；Config TOML 含 [error] file = "..." 解析后可见值。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: process_log_file 收集 ParseErrorRecord（DIAG-01 数据来源）</name>
  <files>src/cli/run/processor.rs</files>
  <read_first>
    - src/cli/run/processor.rs:175-226（process_log_file，特别是 Err(e) 分支 line 216-220）
    - src/error.rs（Task 2 已写入 ErrorKind/ParseErrorRecord/classify_error_kind/truncate_to_120_chars）
    - .planning/phases/67-prog-diag/67-RESEARCH.md Pattern 5
  </read_first>
  <behavior>
    - 当 parser.iter() 返回 Err(ParseError::InvalidFormat { raw, line_number }) 时：file_stats.parse_error_records 新增一条 ParseErrorRecord（file_path = file_path.to_string()，line_number、raw_truncated = truncate_to_120_chars(raw)、kind = classify_error_kind(raw)），且 file_stats.by_type[kind] 同步 +1。
    - 上限 10000：file_stats.parse_error_records.len() >= 10000 时不再 push（计数仍递增 add_parse_error_with_kind）。
    - 其他 ParseError 变体（非 InvalidFormat）：kind = ParseFailed，line_number = 0，raw_truncated = String::new()（仍按上限规则收集）。
  </behavior>
  <action>
    1) 文件顶部 use 追加：use crate::error::{classify_error_kind, truncate_to_120_chars, ErrorKind, ParseErrorRecord}；use dm_database_parser_sqllog::ParseError;
    2) 修改 process_log_file 的 Err(e) 分支（约 line 216-220）：
       - 用 match &e 解构：
           ParseError::InvalidFormat { raw, line_number } => (*line_number, raw.as_str())
           _ => (0u64, "")
         得到 (line_number, raw_ref)。
       - 计算 kind = classify_error_kind(raw_ref)。
       - 将旧 file_stats.add_parse_error() 替换为 file_stats.add_parse_error_with_kind(kind)。
       - if file_stats.parse_error_records.len() < 10_000 {
             file_stats.parse_error_records.push(ParseErrorRecord {
                 file_path: file_path.to_string(),
                 line_number,
                 raw_truncated: truncate_to_120_chars(raw_ref),
                 kind,
             });
         }
       - 保留 errors_in_file += 1 与 log::warn!("{file_path} | {e:?}")。
    3) 若 dm-database-parser-sqllog 的 ParseError 实际无 InvalidFormat 变体名（请用 cargo doc -p dm-database-parser-sqllog --open 或 src 确认；CONTEXT 引用 2.0.2 src/error.rs），则改用实际变体名并保持解构语义；若该 crate 用 thiserror 提供 raw/line_number 访问器（如 .raw() / .line_number()），改用方法访问。本任务先按 match &e { ParseError::InvalidFormat { raw, line_number } => ... _ => ... } 实现；若编译失败按真实变体名修正（变更范围仅本步骤）。
    4) cargo fmt + cargo clippy --all-targets -- -D warnings + cargo test。
    5) 不修改 scanner.rs（CONTEXT.md Pitfall 4：prescan 路径不收集 ParseErrorRecord）。
  </action>
  <verify>
    <automated>cargo clippy --all-targets -- -D warnings 2>&1 | tail -10 && cargo test --lib 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - grep -c 'add_parse_error_with_kind' src/cli/run/processor.rs 输出 >= 1
    - grep -c 'parse_error_records.push' src/cli/run/processor.rs 输出 1
    - grep -c 'classify_error_kind' src/cli/run/processor.rs 输出 >= 1
    - grep -c 'truncate_to_120_chars' src/cli/run/processor.rs 输出 >= 1
    - grep -nE 'len\\(\\) < 10_000|len\\(\\) < 10000' src/cli/run/processor.rs 命中 1 行（上限守卫）
    - grep -c 'add_parse_error\\(\\)' src/cli/run/processor.rs 输出 0（旧调用全部替换）
    - cargo clippy --all-targets -- -D warnings 通过
    - cargo test --lib 全绿（不回归 Plan 01 测试）
  </acceptance_criteria>
  <done>process_log_file Err 路径产出 ParseErrorRecord + by_type 计数；上限 10000 生效；clippy 与 test 全部通过。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| 日志文件 → ErrorStats.parse_error_records 内存 | 不可信日志原文写入进程内存 |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-67-03 | DoS | parse_error_records 无界增长 | mitigate | 硬上限 10_000 记录；超过后只计数不收集（CONTEXT Claude's Discretion） |
| T-67-04 | Information Disclosure | raw_truncated 含日志原文 | mitigate | 截断至前 120 字符；后续写出仅到本地 error log 文件（用户掌控） |
| T-67-05 | Tampering | classify_error_kind 输入未校验 | accept | 纯字符串启发式无副作用；无 SQL/路径解析 |
</threat_model>

<verification>
- cargo fmt --check
- cargo clippy --all-targets -- -D warnings
- cargo test --lib（error::tests + cli::run::tests）
</verification>

<success_criteria>
- ErrorStats 三新字段可由代码直接访问；并行路径通过 merge() 同步聚合（无需改 parallel.rs，merge 扩展已覆盖）
- ROADMAP SC3（行号 + 原文 120 字符）与 SC4（by_type 分类）数据底盘就绪
</success_criteria>

<output>
完成后创建 .planning/phases/67-prog-diag/67-02-SUMMARY.md
</output>
