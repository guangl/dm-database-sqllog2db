---
phase: 31-remove-resume
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/resume.rs
  - src/config/resume.rs
  - src/config/mod.rs
  - src/lib.rs
  - src/cli/run/mod.rs
  - src/cli/run/parallel.rs
  - src/cli/opts.rs
  - src/lang.rs
  - src/main.rs
autonomous: true
requirements:
  - RM-06
user_setup: []
must_haves:
  truths:
    - "handle_run 不再接受 resume/state_file 参数"
    - "Config 结构体不再包含 resume 字段"
    - "lib.rs 和 main.rs 不再声明 mod resume"
    - "opts.rs 中 Run 命令不再有 --resume 和 --state-file 选项"
    - "lng.rs 中 zh_run 不再有 resume/state_file 帮助文本"
    - "cargo build 编译通过"
  artifacts:
    - path: "src/resume.rs"
      provides: "ResumeState 源文件"
      min_lines: 0
    - path: "src/config/resume.rs"
      provides: "ResumeConfig 源文件"
      min_lines: 0
    - path: "src/config/mod.rs"
      provides: "Config 结构体"
      contains: "pub struct Config"
    - path: "src/cli/run/mod.rs"
      provides: "handle_run 编排函数"
      exports:
        - "pub fn handle_run"
    - path: "src/cli/opts.rs"
      provides: "CLI 参数定义"
      exports:
        - "Commands::Run"
  key_links:
    - from: "src/config/mod.rs"
      to: "不再存在 pub resume: ResumeConfig 字段"
    - from: "src/lib.rs"
      to: "不再存在 pub(crate) mod resume"
    - from: "src/cli/run/mod.rs"
      to: "handle_run 签名减少 2 个参数"
---

<objective>
删除 resume.rs、config/resume.rs 源文件，清理所有模块声明、配置字段、CLI 参数和运行循环中的 resume_state 逻辑。

**Purpose:** 移除断点续传（resume/checkpoint）功能的源代码，使 `cargo build` 编译通过。

**Output:** 编译通过的干净的 codebase，不再引用 `crate::resume`、`ResumeState`、`ResumeConfig`、`--resume`、`--state-file`。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@file:/Users/guang/Projects/sqllog2db/.planning/ROADMAP.md
@file:/Users/guang/Projects/sqllog2db/.planning/STATE.md
@file:/Users/guang/Projects/sqllog2db/.planning/phases/31-remove-resume/31-RESEARCH.md
@file:/Users/guang/Projects/sqllog2db/.planning/phases/31-remove-resume/31-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 删除 resume 源文件并更新模块声明</name>
  <files>
    src/resume.rs,
    src/config/resume.rs,
    src/config/mod.rs,
    src/lib.rs
  </files>
  <action>
    1. 删除 `src/resume.rs`（214 行，包含 ResumeState 结构体、所有方法、辅助函数和 9 个单元测试）

    2. 删除 `src/config/resume.rs`（21 行，ResumeConfig 结构体）

    3. 编辑 `src/config/mod.rs`：
       - 删除第 4 行：`pub mod resume;`
       - 删除第 10 行：`pub use resume::ResumeConfig;`
       - 删除第 34 行：`pub resume: ResumeConfig,`（整行，包含末尾逗号）
       - 注意：第 33-35 行的 `#[serde(default)]` 属性和 `pub resume: ResumeConfig,` 字段需整体移除

    4. 编辑 `src/lib.rs`：
       - 删除第 11 行：`pub(crate) mod resume;`
  </action>
  <verify>
    <automated>cargo build 2>&1 | head -20</automated>
  </verify>
  <done>
    resume.rs 已删除；config/resume.rs 已删除；config/mod.rs 中不再提及 resume；lib.rs 中不再声明 resume 模块。
  </done>
</task>

<task type="auto">
  <name>Task 2: 移除运行循环中的 resume_state 逻辑</name>
  <files>
    src/cli/run/mod.rs,
    src/cli/run/parallel.rs
  </files>
  <action>
    编辑 `src/cli/run/mod.rs`：

    a) handle_run 函数签名（第 63-74 行）：
       - 删除第 70 行：`resume: bool,`
       - 删除第 71 行：`state_file_override: Option<&str>,`
       - 保留 `jobs: usize,` 和 `compiled_filters: ...` 参数不变

    b) 删除 resume_state 初始化块（第 85-97 行）：
       - 从 `let state_path =` 到 `None };` 之间的所有代码
       - 包含：state_path 变量定义、resume_state 变量定义、info! 日志调用
       - 注意保留第 98 行注释 `// 仅当有事务级过滤器时才克隆配置...`

    c) 在并行路径的 process_csv_parallel 调用中（第 151-168 行）：
       - 删除第 160 行：`resume_state.as_ref(),`
       - 保持其余参数不变

    d) 删除并行路径后的 resume_state.save 块（第 182-189 行）：
       - 从 `if !interrupted.load(Ordering::Relaxed) {` 到 `}`（第 182-189 行，共 8 行）
       - 包含：check 条件、if-let(state)、for 循环、mark_processed 调用、save 调用

    e) 删除顺序路径中的 resume_state 跳过检查（第 216-228 行）：
       - 从 `if let Some(state) = &resume_state {` 到 `continue; }`（第 216-228 行，共 13 行）
       - 包含：is_processed check、skipped_files++、println、continue

    f) 删除顺序路径中的 resume_state.mark_processed + save（第 246-251 行）：
       - 在 `if !dry_run {` 块内（第 246-251 行）
       - 仅删除第 247-250 行：`if let Some(state) = &mut resume_state { ... state.mark_processed(...)... state.save(...)... }`
       - 第 246 行 `if !dry_run {` 和第 251 行 `}` 保留（如果 mark_processed 块是 dry_run 块内唯一的内容，则需要调整逻辑）

    编辑 `src/cli/run/parallel.rs`：

    a) process_csv_parallel 函数签名（第 72-87 行）：
       - 删除第 79 行：`resume_state: Option<&crate::resume::ResumeState>,`

    b) 删除 resume_state 跳过逻辑（第 136-149 行）：
       - 从 `if let Some(state) = resume_state {` 到 `}`（整个 if-let 块，含 is_processed check、println、return Ok(None)）
       - 注意：删除后，`if interrupted.load(Ordering::Relaxed)` 检查（当前第 151 行）仍保留

    c) 更新第 25 行注释：
       - 原文 "无任何 part（如 resume 模式下全部文件已跳过）时不触碰输出文件"
       - 改为：`// 无任何 part 时不触碰输出文件，避免 overwrite=true 把已有数据清空。`
  </action>
  <verify>
    <automated>cargo build 2>&1 | head -20</automated>
  </verify>
  <done>
    handle_run 签名已移除 resume/state_file 参数，函数体内 resume_state 所有逻辑已删除；
    process_csv_parallel 签名已移除 resume_state 参数及其跳过逻辑。
  </done>
</task>

<task type="auto">
  <name>Task 3: 清理 CLI 层中的 resume 参数</name>
  <files>
    src/cli/opts.rs,
    src/lang.rs,
    src/main.rs
  </files>
  <action>
    编辑 `src/cli/opts.rs`：

    a) 在 Commands::Run 变体中删除 --resume 参数（第 71-76 行）：
       - 删除 `/// Skip files already processed...` 注释行（第 71 行）
       - 删除 `#[arg(long = "resume")]`（第 72 行）
       - 删除 `resume: bool,`（第 73 行）
       - 删除 `/// Override the state file path...` 注释行（第 74 行）
       - 删除 `#[arg(long = "state-file" ... requires = "resume")]`（第 75 行）
       - 删除 `state_file: Option<String>,`（第 76 行）

    编辑 `src/lang.rs`：

    a) 在 zh_run 函数中删除 resume/state_file 帮助文本（第 127-130 行）：
       - 删除 `.mut_arg("resume", |a| a.help("跳过上次已完整处理的文件（断点续传）"))`
       - 删除 `.mut_arg("state_file", |a| { ... })`（共 3 行，从 `.mut_arg` 到 `})`）

    编辑 `src/main.rs`：

    a) 删除第 16 行：`mod resume;`

    b) 在 Run 命令模式匹配中（第 165-177 行）：
       - 删除 `resume,`（第 174 行）
       - 删除 `state_file,`（第 175 行）

    c) 在 handle_run 调用中（第 215-227 行）：
       - 删除 `*resume,`（第 222 行）
       - 删除 `state_file.as_deref(),`（第 223 行）
  </action>
  <verify>
    <automated>cargo build 2>&1 | head -20</automated>
  </verify>
  <done>
    `--resume` 和 `--state-file` 从 Run 命令中移除；zh_run 不再设置其中文帮助文本；
    main.rs 不再声明 mod resume、Run 匹配臂和 handle_run 调用不再传递 resume 参数。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

本阶段为纯删除操作，无可信边界变化。无新输入路径，无新攻击面。

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-01 | Tampering | npm/pip/cargo installs | mitigate | slopcheck + blocking human checkpoint for [ASSUMED]/[SUS] — 本阶段无新依赖，无需检查 |
</threat_model>

<verification>
完成所有 3 个任务后：

```bash
cargo build
```

编译成功后，还需验证：
- 不再有 `crate::resume`、`ResumeState`、`ResumeConfig` 引用
- `cargo clippy --all-targets -- -D warnings` 通过（允许 `#[allow(dead_code)]` 相关警告，应在 Phase 32 统一清理）
</verification>

<success_criteria>
- [ ] `cargo build` 编译通过
- [ ] `src/resume.rs` 和 `src/config/resume.rs` 已删除
- [ ] `config/mod.rs` 中不再有 `pub mod resume;`、`pub use ResumeConfig;`、`pub resume: ResumeConfig`
- [ ] `lib.rs` 中不再有 `pub(crate) mod resume;`
- [ ] `run/mod.rs` 中 handle_run 签名减少 2 个参数，函数体内 resume_state 逻辑完全移除
- [ ] `parallel.rs` 中 process_csv_parallel 签名减少 1 个参数，跳过逻辑完全移除
- [ ] `opts.rs` 中 Run 命令不再有 `--resume` 和 `--state-file`
- [ ] `lang.rs` 中 zh_run 不再有 resume/state_file 中文帮助
- [ ] `main.rs` 中不再有 `mod resume;`、Run 匹配臂和 handle_run 调用不再传递 resume 参数
</success_criteria>

<output>
创建 `.planning/phases/31-remove-resume/31-01-SUMMARY.md`
</output>
