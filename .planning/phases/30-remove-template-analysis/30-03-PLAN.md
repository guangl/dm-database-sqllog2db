---
phase: 30-remove-template-analysis
plan: 03
type: execute
wave: 3
depends_on: [30-01, 30-02]
files_modified:
  - src/cli/run/tests.rs
  - src/exporter/csv/tests.rs
  - src/exporter/sqlite/tests.rs
  - src/exporter/tests.rs
  - tests/integration.rs
autonomous: true
requirements: [RM-05]
user_setup: []

must_haves:
  truths:
    - "cli/run/tests.rs 不再包含模板相关端到端测试"
    - "exporter/csv/tests.rs 不再包含 write_template_stats 测试"
    - "exporter/sqlite/tests.rs 不再包含 write_template_stats 测试和辅助函数"
    - "exporter/tests.rs 不再包含 write_template_stats 测试块"
    - "tests/integration.rs 不再包含 [template] 断言和 e2e 模板测试"
    - "cargo build --release 编译成功"
    - "cargo test 全部通过"
    - "cargo clippy --all-targets -- -D warnings 无警告"
    - "pipeline.is_empty() 快路径不受影响（pipeline/mod.rs 中的 Pipeline 结构体和 is_empty() 方法无修改）"
  artifacts:
    - path: src/cli/run/tests.rs (MUST NOT contain "test_no_template_stats" or "test_template_stats_enabled")
    - path: src/exporter/csv/tests.rs (MUST NOT contain "write_template_stats" or "companion")
    - path: src/exporter/sqlite/tests.rs (MUST NOT contain "write_template_stats" or "make_template_stats_sqlite")
    - path: src/exporter/tests.rs (MUST NOT contain "write_template_stats" or "make_template_stats")
    - path: tests/integration.rs (MUST NOT contain "test_e2e_template_normalization" or "[template]")
  key_links:
    - from: tests/integration.rs -> src/pipeline/aggregator.rs via "TemplateConfig" import — import removed
    - from: src/exporter/tests.rs -> crate::pipeline::TemplateStats via "make_template_stats" — entire block removed
---

<objective>
清理模板相关测试并全链路验证（RM-05：测试清理 + 编译验证）

**Purpose**: 从 5 个测试文件中删除所有 write_template_stats / TemplateAggregator 相关的测试。运行完整的 cargo build + test + clippy + fmt 验证以上两个计划无残留问题。

**Output**: 所有测试通过，构建通过，cargo clippy 无警告，cargo fmt 格式正确。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/30-remove-template-analysis/30-RESEARCH.md
@.planning/phases/30-remove-template-analysis/30-01-PLAN.md
@.planning/phases/30-remove-template-analysis/30-02-PLAN.md

<source_refs>
当前代码库中需要修改的测试文件（基于 research doc 的分析）：

### src/cli/run/tests.rs
- lines 57-95: `test_aggregator_disabled_none_path` — **保留**（验证无 template 时 handle_run 正常工作，仍然有效）
- lines 97-161: `test_parallel_merge_consistent` — **修改**：删除 toml 中的 `[template]\nenable = true`
- lines 163-205: `test_no_template_stats_when_disabled` — **删除**
- lines 207-268: `test_template_stats_enabled_end_to_end_sequential` — **删除**

### src/exporter/csv/tests.rs
- lines 468-534: `test_csv_write_template_stats` — 删除
- lines 562-601: `test_parallel_csv_companion_file` — 删除
- lines 603-634: `test_csv_write_template_stats_none_skips` — 删除
- lines 637-665: `test_csv_write_template_stats_empty_path_skips` — 删除

### src/exporter/sqlite/tests.rs
- lines 448-462: `make_template_stats_sqlite` 辅助函数 — 删除
- lines 464-494: `test_sqlite_write_template_stats` — 删除
- 后续 ~300 行：所有 `write_template_stats` 相关测试 — 全部删除

### src/exporter/tests.rs
- lines 238-351: 整个 `write_template_stats` 测试块 — 删除
  - `make_template_stats` 辅助函数
  - `test_default_write_template_stats_noop`
  - `test_dry_run_write_template_stats_noop`
  - `test_exporter_manager_write_template_stats_dry_run`
  - `test_exporter_kind_dispatch_write_template_stats`

### tests/integration.rs
- line 15: 从 `use dm_database_sqllog2db::pipeline::{FiltersFeature, NormalizeConfig, OutputConfig, TemplateConfig,}` 中删除 `TemplateConfig,`
- lines 1191-1193: 删除 `assert!(content.contains("[template]"), "init template must contain [template]");`
- lines 1269-1272: 修改 `test_validate_rejects_legacy_pipeline_template_analysis` 中删除 `[pipeline.template_analysis] → [template]` 断言行
- lines 1418-1468: 删除整个 `test_e2e_template_normalization` 测试函数
</source_refs>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 清理 cli/run/tests.rs 和 exporter/tests.rs 中模板相关测试</name>
  <files>
    src/cli/run/tests.rs
    src/exporter/tests.rs
  </files>
  <read_first>
    src/cli/run/tests.rs (lines 57-268 — aggregator_disabled、parallel_merge、no_template_stats、template_stats_enabled 共 4 个测试)
    src/exporter/tests.rs (lines 238-351 — make_template_stats + 4 个 write_template_stats 测试)
  </read_first>
  <action>
    每步删除后运行 `cargo test` 验证。

    **Step 1: 修改 src/cli/run/tests.rs**

    1a) 保留 `test_aggregator_disabled_none_path`（lines 57-95）— 该测试验证无 template 配置时 handle_run 正常工作，仍然有效，保留不变。

    1b) 修改 `test_parallel_merge_consistent`（lines 97-161）— 从 toml 配置字符串中删除 `[template]\nenable = true\n`。当前 line 109 的 make_cfg 闭包中有：
    ```
    [template]\nenable = true\n\
    ```
    删除该行。测试本身验证顺序与并行 CSV 输出行数一致，不依赖 template 功能。删除后 toml 不再包含 template 段。

    1c) 删除 `test_no_template_stats_when_disabled`（lines 163-205）— 完整删除此测试函数，包括函数定义和所有断言。

    1d) 删除 `test_template_stats_enabled_end_to_end_sequential`（lines 207-268）— 完整删除此测试函数。

    **Step 2: 清理 src/exporter/tests.rs**

    删除 lines 238-351 整个 `write_template_stats` 测试块，包含：
    - `/// 辅助：构造一个最小 TemplateStats 实例` 注释
    - `fn make_template_stats(key: &str)` 辅助函数（lines 241-254）
    - `/// Test 1: 自定义 mock exporter` 到 `/// Test 4: ExporterKind 三个 variant` 共 4 个测试 + 1 个辅助函数
    - `test_default_write_template_stats_noop`（lines 257-279）
    - `test_dry_run_write_template_stats_noop`（lines 282-300）
    - `test_exporter_manager_write_template_stats_dry_run`（lines 303-309）
    - `test_exporter_kind_dispatch_write_template_stats`（lines 312-351）
  </action>
  <verify>
    <automated>
      grep -c 'test_no_template_stats' src/cli/run/tests.rs; echo "expect 0"
      grep -c 'test_template_stats_enabled' src/cli/run/tests.rs; echo "expect 0"
      grep -c 'make_template_stats' src/exporter/tests.rs; echo "expect 0"
      grep -c 'write_template_stats' src/exporter/tests.rs; echo "expect 0"
      cargo test --lib 2>&1 | tail -10
    </automated>
  </verify>
  <done>cli/run/tests.rs 和 exporter/tests.rs 中模板相关测试已删除/修改，cargo test --lib 通过。</done>
</task>

<task type="auto">
  <name>Task 2: 清理 CSV 和 SQLite 导出器测试中的 write_template_stats 测试</name>
  <files>
    src/exporter/csv/tests.rs
    src/exporter/sqlite/tests.rs
  </files>
  <read_first>
    src/exporter/csv/tests.rs (lines 468-665 — test_csv_write_template_stats、test_parallel_csv_companion_file、test_csv_write_template_stats_none_skips、test_csv_write_template_stats_empty_path_skips)
    src/exporter/sqlite/tests.rs (lines 448-760+ — make_template_stats_sqlite + test_sqlite_write_template_stats + 所有相关测试)
  </read_first>
  <action>
    每步删除后运行 `cargo test --lib` 验证。

    **Step 1: 清理 src/exporter/csv/tests.rs**

    删除以下 4 个测试函数：
    - `test_csv_write_template_stats`（lines 468-560）— 完整函数
    - `test_parallel_csv_companion_file`（lines 562-601）— 完整函数
    - `test_csv_write_template_stats_none_skips`（lines 603-634）— 完整函数
    - `test_csv_write_template_stats_empty_path_skips`（lines 637-665）— 完整函数

    注意确认函数末尾位置，每个测试用 `#[test]` 或 `#[test_case]` 属性标记，以上 4 个测试全部以 `#[test]` 开头。

    **Step 2: 清理 src/exporter/sqlite/tests.rs**

    删除所有 write_template_stats 相关测试和辅助函数。从 line 448 开始（`/// 辅助：构造 TemplateStats 测试数据`）到文件末尾。包含：
    - `make_template_stats_sqlite` 辅助函数（lines 448-462）
    - `test_sqlite_write_template_stats`（lines 464-544）
    - 所有后续 write_template_stats 测试函数。根据文件内容，这些测试包括（具体行号会因版本不同而偏移）：
      - 测试 1: 基本写入验证 — COUNT=2
      - 测试 2: None 路径跳过
      - 测试 3: 空表名跳过
      - 测试 4: 自定义表名写入
      - 测试 5: 无效表名拒绝
      - 及所有剩余测试直到文件末尾

    使用 `cargo test --lib exporter::sqlite::tests` 确认所有测试过后且有合理的测试数量（排除 template 测试后应有 CSV 文件写入测试等保留测试）。

    注意：不要删除 sqlite/tests.rs 中的非 template 测试（如 CSV/SQLite 写入测试、性能指标测试等）。
  </action>
  <verify>
    <automated>
      grep -c 'write_template_stats' src/exporter/csv/tests.rs; echo "expect 0"
      grep -c 'make_template_stats_sqlite' src/exporter/sqlite/tests.rs; echo "expect 0"
      grep -c 'write_template_stats' src/exporter/sqlite/tests.rs; echo "expect 0"
      cargo test --lib 2>&1 | tail -10
    </automated>
  </verify>
  <done>CSV 和 SQLite 导出器的测试文件中所有 write_template_stats 测试已删除，cargo test --lib 通过。</done>
</task>

<task type="auto">
  <name>Task 3: 清理集成测试并全链路验证</name>
  <files>tests/integration.rs</files>
  <read_first>
    tests/integration.rs (line 15 — TemplateConfig import in `use dm_database_sqllog2db::pipeline::{...}`)
    tests/integration.rs (lines 1191-1193 — `assert!(content.contains("[template]"))`)
    tests/integration.rs (lines 1269-1272 — `[pipeline.template_analysis] → [template]` 断言)
    tests/integration.rs (lines 1418-1468 — `test_e2e_template_normalization` 函数)
  </read_first>
  <action>
    按顺序执行，最后运行全链路验证。

    **Step 1: 清理导入**
    从 `use dm_database_sqllog2db::pipeline::{FiltersFeature, NormalizeConfig, OutputConfig, TemplateConfig,}` 中删除 `TemplateConfig,`。

    **Step 2: 删除 [template] 断言**
    从 `test_init_generated_content_zh` 中删除以下断言（约 lines 1191-1193）：
    ```
    assert!(
        content.contains("[template]"),
        "init template must contain [template]"
    );
    ```

    **Step 3: 修改旧路径检测测试**
    在 `test_validate_rejects_legacy_pipeline_template_analysis` 中：
    - 删除 lines 1269-1272（`[pipeline.template_analysis] → [template]` 迁移提示断言行）：
    ```
    assert!(
        err_msg.contains("[pipeline.template_analysis] → [template]"),
        "error must contain migration hint for template_analysis; got: {err_msg}"
    );
    ```

    注意：`PIPELINE_MIGRATION_HINT` 常量中保留 `[pipeline.template_analysis] → [template]` 文本——它仍然是旧配置格式迁移提示的一部分，不会导致编译错误。Phase 32 会整体清理该常量。

    **Step 4: 删除 e2e 模板测试**
    删除 `test_e2e_template_normalization` 函数（从 line 1418 `#[test]` 到 line 1468 函数闭合 `}`）。

    **Step 5: 运行 `cargo update` 清理 Cargo.lock**
    运行 `cargo update` 以从 Cargo.lock 中移除 hdrhistogram 条目。

    **Step 6: 全链路验证**
    依次运行以下命令，确保全部通过：
    ```
    cargo build --release
    cargo test
    cargo clippy --all-targets -- -D warnings
    cargo fmt
    ```
  </action>
  <verify>
    <automated>
      grep -c 'TemplateConfig' tests/integration.rs; echo "expect 0"
      grep -c 'test_e2e_template_normalization' tests/integration.rs; echo "expect 0"
      grep -c 'must contain \[template\]' tests/integration.rs; echo "expect 0"
      grep -c 'template_analysis' tests/integration.rs; echo "expect 0"
      cargo build --release 2>&1 | tail -3
      cargo test 2>&1 | tail -10
    </automated>
  </verify>
  <done>集成测试已清理，全链路验证通过（build --release + test + clippy + fmt）。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

本计划为测试清理 + 编译验证，不引入新代码或新攻击面。

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-05 | Tampering | Test removal | mitigate | `cargo test` 验证所有保留测试通过，确认无遗漏嵌套引用 |
| T-30-06 | Tampering | Dead code warning | mitigate | `cargo clippy --all-targets -- -D warnings` 确保无残留未使用导入/函数 |
| T-30-07 | Information Disclosure | Cargo.lock stale dep | mitigate | `cargo update` 清理 hdrhistogram 锁定条目 |
| T-30-SC | Tampering | Build integrity | mitigate | `cargo build --release` + `cargo test` + `cargo clippy` + `cargo fmt` 四步完整验证 |
</threat_model>

<verification>
1. `cargo build --release` 编译成功
2. `cargo test` 全部测试通过
3. `cargo clippy --all-targets -- -D warnings` 无警告
4. `cargo fmt` 格式检查通过
5. `grep -r 'TemplateAggregator\|TemplateStats\|TemplateReporter\|write_template_stats' src/ tests/` 返回 0（无残留）
</verification>

<success_criteria>
- 所有 5 个测试文件中的模板相关测试已清理
- `cargo build --release` 编译成功
- `cargo test` 全部通过
- `cargo clippy --all-targets -- -D warnings` 无警告
- `cargo fmt` 格式通过
- 热循环 `pipeline.is_empty()` 快路径未受影响
</success_criteria>
</task>
