---
phase: 48-logging
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/cli/opts.rs
  - src/main.rs
  - src/cli/run/mod.rs
  - src/cli/run/tests.rs
  - tests/integration.rs
autonomous: true
requirements: [LOG-01, LOG-02]

must_haves:
  truths:
    - "`-v`/`--verbose` 是布尔标志（出现一次即 true），不再接受 `-vv`"
    - "`-v` 和 `-q` 同时指定时 clap 报告冲突并退出非零"
    - "`sqllog2db run -c config.toml --verbose` 在 stderr 为每个处理的文件输出一行 `Processing: <path>`"
    - "`sqllog2db run -c config.toml --verbose` 期间不绘制 ProgressBar（无 spinner 字符 `⠋`/`⠙` 等）"
    - "`sqllog2db run -c config.toml --quiet` 期间既不绘制 ProgressBar，也不在 stderr 打印 `Completed with N error(s)` 摘要"
    - "默认模式（既无 -v 也无 -q）保留现有 ProgressBar 与运行结束摘要行为"
  artifacts:
    - path: "src/cli/opts.rs"
      provides: "Cli.verbose 字段类型为 bool"
      contains: "pub(crate) verbose: bool"
    - path: "src/main.rs"
      provides: "apply_verbosity_to_config 签名使用 bool；handle_run 调用传递 verbose"
      contains: "handle_run(&cfg, cli.quiet, cli.verbose"
    - path: "src/cli/run/mod.rs"
      provides: "handle_run 接收 verbose 参数；ProgressBar 仅在 !quiet && !verbose 时实例化；verbose 时输出 Processing 行"
      contains: "verbose: bool"
  key_links:
    - from: "src/cli/opts.rs::Cli::verbose"
      to: "src/main.rs::run() -> handle_run"
      via: "cli.verbose 直传"
      pattern: "cli\\.verbose"
    - from: "src/main.rs::run()"
      to: "src/cli/run/mod.rs::handle_run"
      via: "新增 verbose: bool 参数（位于 quiet 之后）"
      pattern: "handle_run\\([^)]*cli\\.quiet,\\s*cli\\.verbose"
    - from: "src/cli/run/mod.rs::handle_run"
      to: "ProgressBar 实例化条件"
      via: "show_progress = !quiet && !verbose"
      pattern: "let show_progress = !quiet && !verbose"
---

<objective>
将 `-v` 从计数日志级别开关重新定位为布尔型「显示每文件处理详情」标志（LOG-01），同时让 `--quiet` 完全跳过 ProgressBar 与运行摘要（LOG-02）。所有相关函数签名（`apply_verbosity_to_config`、`handle_run`）、单元测试和集成测试同步更新。

Purpose: 将 `-v` 的语义从「调整日志级别」转换为「展示运行时进度详情」，使脚本调用 `--quiet` 时获得纯净输出，调试场景使用 `--verbose` 时获得逐文件可见性。
Output: opts/main/run 三处源文件的 API 重塑 + 全部 35+ 处 handle_run 调用点同步更新 + 现有 verbosity 测试改写为新行为断言。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/48-logging/48-CONTEXT.md
@.planning/phases/48-logging/48-PATTERNS.md
@src/cli/opts.rs
@src/main.rs
@src/cli/run/mod.rs
@src/cli/run/tests.rs

<interfaces>
<!-- 关键签名与调用点，执行时无需再次浏览代码 -->

From src/cli/opts.rs（当前状态，需修改）:
```rust
#[arg(short = 'v', action = clap::ArgAction::Count, global = true, ...)]
pub(crate) verbose: u8,

#[arg(short = 'q', global = true, conflicts_with = "verbose")]
pub(crate) quiet: bool,
```

From src/main.rs（当前状态，需修改）:
```rust
fn init_simple_logging(verbose: u8, quiet: bool) { ... }     // line 27
fn apply_verbosity_to_config(cfg: &mut Config, verbose: u8, quiet: bool) { ... }  // line 51
// line 107
init_simple_logging(cli.verbose, cli.quiet);
// line 119 (Run arm), line 143 (Validate arm)
apply_verbosity_to_config(&mut cfg, cli.verbose, cli.quiet);
// line 136
let stats = cli::run::handle_run(&cfg, cli.quiet, &interrupted, compiled_filters)?;
```

From src/cli/run/mod.rs（当前状态，需修改）:
```rust
pub fn handle_run(
    cfg: &Config,
    quiet: bool,
    interrupted: &Arc<AtomicBool>,
    compiled_filters: Option<(CompiledMetaFilters, CompiledSqlFilters)>,
) -> Result<ErrorStats>;
// line 114: let show_progress = !quiet;
// lines 175-204: 顺序 for 循环，每个 log_file 进入 process_log_file
```

From src/cli/run/tests.rs and tests/integration.rs:
- 共 35 处 `handle_run(&cfg, true, &interrupted, ...)` / `handle_run(&cfg, true, &Arc::new(AtomicBool::new(false)), ...)` / `handle_run(&cfg, false, &interrupted, ...)` 调用
- 所有调用须在 `quiet` 之后插入 `false` 作为 `verbose` 实参（除非测试目的需要 verbose=true，否则一律 false）
- src/main.rs 内单元测试 test_apply_verbosity_verbose (line 237) 与 test_apply_verbosity_trace (line 258) 当前断言 `cfg.logging.level == "debug"`——需改为断言 verbose=true 不改变 logging.level
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 改造 opts.rs/main.rs，重塑 -v 语义并清理 debug 映射</name>
  <files>src/cli/opts.rs, src/main.rs</files>
  <read_first>
    - src/cli/opts.rs（当前 verbose: u8 + quiet: bool 定义）
    - src/main.rs（当前 init_simple_logging / apply_verbosity_to_config / run / 单元测试）
    - .planning/phases/48-logging/48-CONTEXT.md（D-01、D-02、D-06）
    - .planning/phases/48-logging/48-PATTERNS.md（quiet bool 参照模式与 verbose 改写目标块）
  </read_first>
  <action>
  第一步：修改 `src/cli/opts.rs`（per D-01）。将第 24-32 行的 `verbose` 字段定义替换为布尔标志，照搬现有 `quiet` 字段风格：
  - doc comment 改为 `/// Show per-file processing details`
  - 属性宏改为 `#[arg(short = 'v', long = "verbose", global = true, conflicts_with = "quiet", help = "Show per-file processing details on stderr.")]`
  - 字段类型从 `pub(crate) verbose: u8,` 改为 `pub(crate) verbose: bool,`
  保留 `quiet` 字段不变（其 `conflicts_with = "verbose"` 与 verbose 端的 `conflicts_with = "quiet"` 双向互斥）。

  第二步：修改 `src/main.rs` 函数签名（per D-02）。
  1. `init_simple_logging(verbose: u8, quiet: bool)`（line 27）：参数类型 `u8` → `bool`，函数体重写为：
     ```
     let level = if quiet { "error" } else { "info" };
     ```
     （移除 trace/debug 分支——本函数仅用于非-Run 命令的 init/validate，verbose 不再控制日志级别）。filter 推导分支同步简化，仅保留 `"error"` 与 `_` 两条匹配臂。

  2. `apply_verbosity_to_config(cfg: &mut Config, verbose: u8, quiet: bool)`（line 51）：参数类型 `u8` → `bool`，函数体重写为单条 quiet 判断：
     ```
     if quiet {
         cfg.logging.level = "error".to_string();
     }
     ```
     （移除 `verbose >= 1 → "debug"` 分支；verbose 参数虽不再控制 logging level，仍保留在签名中以保持调用点的对称性与未来扩展性——若 planner 改主意删除参数，那是另一次重构）。注意：保留 `verbose: bool` 参数即使函数体不使用，是为避免污染调用点；为避免 clippy `unused_variables` 警告，将参数改名为 `_verbose: bool`。

  3. 调用点更新：
     - 第 107 行 `init_simple_logging(cli.verbose, cli.quiet);` — 因签名已改为 bool，且 `cli.verbose` 已是 bool，无需修改调用本身
     - 第 119、143 行 `apply_verbosity_to_config(&mut cfg, cli.verbose, cli.quiet);` — 同上无需修改
     - 第 136 行的 handle_run 调用增加 verbose 参数：
       ```
       let stats = cli::run::handle_run(&cfg, cli.quiet, cli.verbose, &interrupted, compiled_filters)?;
       ```

  4. quiet 抑制 Run 命令的错误摘要（per D-06）。修改 main() 函数第 78-84 行的 `Ok(Some(stats))` 分支：将 `eprintln!("Completed with ...")` 行用 `if !cli.quiet { ... }` 包裹。但 `cli` 在 `main()` 中不可见——需重构 main 的流程：让 `run()` 函数返回 `Result<Option<(ErrorStats, bool /*quiet*/)>>` 携带 quiet 信号；或更简洁地，在 run() 内部 Run 分支检测 quiet 并将其编码到 ErrorStats 的新字段。

     具体选择：扩展 `run()` 返回类型为 `Result<Option<(ErrorStats, bool)>>`，其中 bool 即 `cli.quiet`。Run 分支返回 `Ok(Some((stats, cli.quiet)))`，其他分支返回 `Ok(None)`。main() 中模式匹配相应展开，在 `if stats.has_errors()` 内层加 `if !quiet { eprintln!(...); }`。

  5. 同步更新 main.rs 单元测试（lines 237-263）：
     - `test_apply_verbosity_verbose`：调用改为 `apply_verbosity_to_config(&mut cfg, true, false);`，断言改为 `assert_eq!(cfg.logging.level, Config::default().logging.level);` 验证 verbose=true 不改变默认级别
     - `test_apply_verbosity_quiet`：调用改为 `apply_verbosity_to_config(&mut cfg, false, true);`，断言保持 `cfg.logging.level == "error"`
     - `test_apply_verbosity_neither`：调用改为 `apply_verbosity_to_config(&mut cfg, false, false);`，断言保持 logging.level 不变
     - `test_apply_verbosity_trace`：当前断言 `verbose=2 → debug`。bool 模式下没有 trace 概念，删除此测试（不再有意义）

  目标：编译时 `cargo build --release` 在改完 opts/main 后仍可能因 handle_run 签名暂未扩展而失败——这是预期的，Task 2 会完成。允许此中间态留给 Task 2，但本任务结束前必须确认 opts.rs 与 main.rs 自身改动语法正确（`cargo check --bin sqllog2db` 报错应仅与 handle_run 签名相关，不应有 opts/main 内部语法错误）。
  </action>
  <verify>
    <automated>cargo check --bin sqllog2db 2>&amp;1 | grep -E "verbose: bool|verbose: u8" -c</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "pub(crate) verbose: bool" src/cli/opts.rs` 返回精确匹配（恰好 1 行）
    - `grep -n "long = \"verbose\"" src/cli/opts.rs` 至少 1 处匹配
    - `grep -n "conflicts_with = \"quiet\"" src/cli/opts.rs` 至少 1 处匹配
    - `grep -n "cfg.logging.level = \"debug\"" src/main.rs` 返回 0 行（debug 映射已删除）
    - `grep -n "handle_run(&cfg, cli.quiet, cli.verbose" src/main.rs` 返回恰好 1 处匹配
    - `grep -n "test_apply_verbosity_trace" src/main.rs` 返回 0 行（该测试已删除）
    - 在 main.rs 第 78-84 行附近的 `Ok(Some(...))` 分支中，`grep -n "if !quiet" src/main.rs | grep -i "eprintln"` 上下文显示 quiet 被用作摘要抑制门
  </acceptance_criteria>
  <done>
    opts.rs 中 verbose 已改为 bool 并带 conflicts_with；main.rs 中 init_simple_logging 与 apply_verbosity_to_config 已改为 bool 参数，debug 映射已彻底删除；handle_run 调用已添加 cli.verbose 实参；quiet 摘要抑制逻辑已通过 run() 返回值传递到 main() 中并由 if !quiet 包裹 eprintln。cargo check 失败的唯一原因是 handle_run 自身签名（Task 2 修复）。
  </done>
</task>

<task type="auto">
  <name>Task 2: 扩展 handle_run 签名，串联 verbose/quiet 在运行路径的展示行为</name>
  <files>src/cli/run/mod.rs, src/cli/run/tests.rs, tests/integration.rs</files>
  <read_first>
    - src/cli/run/mod.rs（当前 handle_run 签名 + ProgressBar 实例化块 + 顺序 for 循环）
    - src/cli/run/tests.rs（1 处 handle_run 调用，line 26）
    - tests/integration.rs（34 处 handle_run 调用——使用 `grep -n "handle_run" tests/integration.rs` 一次性获取行号清单）
    - .planning/phases/48-logging/48-CONTEXT.md（D-03、D-04、D-05）
    - .planning/phases/48-logging/48-PATTERNS.md（ProgressBar 条件创建模式与 eprintln 文件输出模式）
  </read_first>
  <action>
  第一步：`src/cli/run/mod.rs` 签名扩展（per CONTEXT D-03 集成段）。在第 28-33 行的 `handle_run` 签名中，于 `quiet: bool,` 之后插入 `verbose: bool,`：
  ```
  pub fn handle_run(
      cfg: &Config,
      quiet: bool,
      verbose: bool,
      interrupted: &Arc<AtomicBool>,
      compiled_filters: Option<(CompiledMetaFilters, CompiledSqlFilters)>,
  ) -> Result<ErrorStats> {
  ```

  第二步：ProgressBar 条件实例化（per D-04、D-05）。第 114 行将 `let show_progress = !quiet;` 改为 `let show_progress = !quiet && !verbose;`。`show_progress` 后续在并行函数调用、`pb` 实例化分支中均沿用此变量，无需进一步修改并行函数。

  第三步：verbose 模式下的每文件 stderr 输出（per D-03）。在第 175-204 行的顺序 for 循环中，找到 `for (idx, log_file) in log_files.iter().enumerate() {` 之后、`if interrupted.load(Ordering::Relaxed)` 之后、`process_log_file(...)` 调用之前的位置插入：
  ```
  if verbose {
      eprintln!("Processing: {}", log_file.display());
  }
  ```
  使用 `log_file.display()`，这与 `prescan.rs` 中已有的路径格式化模式一致。

  并行路径（`use_csv_parallel` / `use_sqlite_parallel` 分支，第 135-168 行）的每文件输出：本计划暂不在并行路径添加 Processing 行（D-03 未明确要求并行路径同等支持，且并行模式下输出顺序不确定，可能造成误导）。在并行分支入口添加单行：
  ```
  if verbose {
      eprintln!("Processing {} files in parallel ({} jobs)", log_files.len(), jobs);
  }
  ```
  这一行紧跟 `info!("Parsing and exporting SQL logs (parallel, ...)")` 之前或之后插入，分别在 `if use_csv_parallel` 与 `else if use_sqlite_parallel` 两个分支各加一处（共 2 处）。

  第四步：更新所有 handle_run 调用点。共 35 处需要在 `quiet` 实参之后插入 `false` 作为 `verbose`：
  - `src/cli/run/tests.rs` line 26：`handle_run(&cfg, true, &Arc::new(AtomicBool::new(false)), None).unwrap();` → `handle_run(&cfg, true, false, &Arc::new(AtomicBool::new(false)), None).unwrap();`
  - 其他在 tests.rs 中 lines 67、98、143、158、224、231 类似形式的调用
  - `tests/integration.rs` 共 34 处：使用 `grep -n "handle_run" tests/integration.rs` 获取完整清单，对每处在 `quiet` 之后（`true` 或 `false`）插入 `false`。例如：
    - `handle_run(&cfg, true, &interrupted, None).unwrap();` → `handle_run(&cfg, true, false, &interrupted, None).unwrap();`
    - `handle_run(&cfg, false, &interrupted, None).unwrap();` → `handle_run(&cfg, false, false, &interrupted, None).unwrap();`
    - `handle_run(&cfg, true, &Arc::new(AtomicBool::new(false)), None).unwrap();` → `handle_run(&cfg, true, false, &Arc::new(AtomicBool::new(false)), None).unwrap();`
  必须逐一处理，不可遗漏。完成后 `grep -n "handle_run(&cfg, true, &" tests/integration.rs` 应返回 0 行（旧三参数顺序已不复存在）。

  第五步：编译并跑全部测试，确保无回归。`cargo build --release` 必须成功，`cargo test` 全部通过。
  </action>
  <verify>
    <automated>cargo build --release 2>&amp;1 | grep -E "error\[" -c; cargo test --no-fail-fast 2>&amp;1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "verbose:\s*bool" src/cli/run/mod.rs` &gt;= 1（签名包含 verbose: bool）
    - `grep -n "let show_progress = !quiet && !verbose;" src/cli/run/mod.rs` 返回恰好 1 处匹配
    - `grep -n "eprintln!(\"Processing: \"" src/cli/run/mod.rs` 在顺序路径出现 1 处（或检测 `eprintln!("Processing: {}"`）
    - `grep -cE "handle_run\([^)]*,\s*(true|false)\s*,\s*(true|false)\s*," tests/integration.rs` &gt;= 34（所有调用都已带新 verbose 参数）
    - `grep -nE "handle_run\(&cfg, true, &" tests/integration.rs` 返回 0 行（旧三参数顺序已被替换）
    - `grep -nE "handle_run\(&cfg, true, &" src/cli/run/tests.rs` 返回 0 行
    - `cargo build --release` 退出码 0
    - `cargo test --lib` 退出码 0
    - `cargo test --test integration` 退出码 0
  </acceptance_criteria>
  <done>
    handle_run 签名已扩展 verbose 参数；show_progress 条件改为 `!quiet && !verbose`；顺序路径在每个文件前 verbose-gated 输出 Processing 行；并行路径在入口 verbose-gated 输出 "Processing N files in parallel"；35+ 处调用点全部更新；`cargo build --release` 与 `cargo test` 全绿。
  </done>
</task>

<task type="auto">
  <name>Task 3: 端到端 CLI 行为验证（verbose/quiet 互斥、输出抑制、Processing 行存在）</name>
  <files>tests/integration.rs</files>
  <read_first>
    - tests/integration.rs（既有端到端 CLI 测试模式——查找 `Command::new` 或 `assert_cmd` 用法）
    - src/cli/opts.rs（确认 verbose/quiet 互斥已就位）
    - .planning/phases/48-logging/48-CONTEXT.md（成功标准第 4 项：互斥提示错误）
  </read_first>
  <action>
  在 `tests/integration.rs` 末尾追加以下 3 个测试。如已有 `Command::new(env!("CARGO_BIN_EXE_sqllog2db"))` 或 `assert_cmd` 模式则沿用；否则使用 `std::process::Command` + `env!("CARGO_BIN_EXE_sqllog2db")` 启动二进制并断言 stderr/stdout/exit code。

  Test 1: `test_cli_verbose_quiet_mutual_exclusion`
  目标：验证 `sqllog2db -v -q run -c <某配置>` 因 clap conflicts_with 返回非零并在 stderr 中包含冲突提示。
  实现：
  ```
  let output = std::process::Command::new(env!("CARGO_BIN_EXE_sqllog2db"))
      .args(["-v", "-q", "run", "-c", "nonexistent.toml"])
      .output()
      .expect("failed to spawn");
  assert!(!output.status.success(), "expected non-zero exit for -v -q conflict");
  let stderr = String::from_utf8_lossy(&output.stderr);
  assert!(
      stderr.contains("cannot be used with") || stderr.contains("conflict"),
      "expected clap conflict message, got: {stderr}"
  );
  ```

  Test 2: `test_cli_verbose_prints_processing_line_per_file`
  目标：验证 `--verbose run` 在 stderr 中包含 `Processing: <path>` 行。
  实现：
  - 创建 temp dir，写入 2 个 .log 文件（用 `write_test_log`）
  - 构造一个最小 TOML 配置文件，sqllog.path 指向 temp dir，exporter.csv.file 指向 temp 内的 out.csv
  - 调用：
    ```
    let output = std::process::Command::new(env!("CARGO_BIN_EXE_sqllog2db"))
        .args(["--verbose", "run", "-c", config_path.to_str().unwrap()])
        .output()
        .expect("failed to spawn");
    let stderr = String::from_utf8_lossy(&output.stderr);
    assert!(output.status.success(), "verbose run should succeed, stderr: {stderr}");
    let processing_count = stderr.matches("Processing: ").count();
    assert!(
        processing_count &gt;= 2,
        "expected &gt;=2 Processing lines (one per file), got {processing_count}: {stderr}"
    );
    ```

  Test 3: `test_cli_quiet_suppresses_summary`
  目标：验证 `--quiet run` 在 stderr 中既无 ProgressBar spinner 字符也无完成摘要。
  实现：
  - 同上构造 temp dir + .log + config
  - 调用：
    ```
    let output = std::process::Command::new(env!("CARGO_BIN_EXE_sqllog2db"))
        .args(["--quiet", "run", "-c", config_path.to_str().unwrap()])
        .output()
        .expect("failed to spawn");
    assert!(output.status.success(), "quiet run should succeed");
    let stderr = String::from_utf8_lossy(&output.stderr);
    assert!(
        !stderr.contains("✓ SQL Log Export Task Completed"),
        "quiet should suppress completion summary, got: {stderr}"
    );
    assert!(
        !stderr.contains("Completed with"),
        "quiet should suppress error count line, got: {stderr}"
    );
    // 注意：stderr 可能仍有 env_logger 的 info 行（取决于 logging 配置），但不应有进度/摘要装饰
    ```

  注意：每个 CLI 测试都要在配置中明确写出 logging.file、error.file、exporter.csv.file 路径，否则会回退到默认路径而污染工作目录。沿用 tests/integration.rs 顶部 `make_run_config` 或在 toml 字符串中显式列出。

  完成后跑 `cargo test --test integration` 必须全绿。
  </action>
  <verify>
    <automated>cargo test --test integration test_cli_verbose -- --nocapture 2>&amp;1 | tail -10; cargo test --test integration test_cli_quiet -- --nocapture 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "fn test_cli_verbose_quiet_mutual_exclusion" tests/integration.rs` 返回 1 行
    - `grep -n "fn test_cli_verbose_prints_processing_line_per_file" tests/integration.rs` 返回 1 行
    - `grep -n "fn test_cli_quiet_suppresses_summary" tests/integration.rs` 返回 1 行
    - `cargo test --test integration test_cli_verbose_quiet_mutual_exclusion` 退出码 0
    - `cargo test --test integration test_cli_verbose_prints_processing_line_per_file` 退出码 0
    - `cargo test --test integration test_cli_quiet_suppresses_summary` 退出码 0
    - 三个测试均不依赖固定路径，使用 `tempfile::TempDir` 隔离
  </acceptance_criteria>
  <done>
    3 个端到端 CLI 测试已添加并全部通过：互斥提示、verbose 文件级输出、quiet 摘要抑制三条核心成功标准（ROADMAP SC1/SC2/SC4）有可执行的回归保护。
  </done>
</task>

</tasks>

<verification>
执行后端到端检查：
1. `cargo clippy --all-targets -- -D warnings` 通过，无新增 warning
2. `cargo test` 全部通过（含本计划新增 3 个端到端 CLI 测试）
3. `cargo fmt --check` 通过
4. 手动跑 `cargo run --release -- run -c <某测试配置> --verbose` 观察 stderr 出现 `Processing: <path>` 行，无 spinner
5. 手动跑 `cargo run --release -- run -c <某测试配置> --quiet` 观察 stderr 仅含 env_logger 必要输出，无完成摘要
6. 手动跑 `cargo run --release -- -v -q run -c x.toml` 观察非零退出 + clap 冲突提示
</verification>

<success_criteria>
- ROADMAP Phase 48 成功标准 1/2/4 全部满足（SC1: verbose 输出文件名；SC2: quiet 抑制进度与摘要；SC4: 互斥提示）
- LOG-01、LOG-02 两个 requirement 行为已实现并有回归测试覆盖
- 既有 35+ handle_run 调用点全部已迁移到新签名
- 编译零警告，所有现有测试无回归
- LOG-03 的摘要差异化（默认 vs verbose）由 Plan 02 处理
</success_criteria>

<output>
Create `.planning/phases/48-logging/48-01-SUMMARY.md` when done
</output>
