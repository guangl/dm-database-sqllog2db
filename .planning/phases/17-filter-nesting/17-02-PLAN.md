---
phase: 17-filter-nesting
plan: 02
type: execute
wave: 2
depends_on:
  - 17-01
files_modified:
  - src/config.rs
  - src/cli/run.rs
  - src/cli/init.rs
  - tests/integration.rs
autonomous: true
requirements:
  - CONFIG-01
  - CONFIG-02
  - CONFIG-05
tags:
  - callers
  - cli
  - init-template
  - integration-tests

must_haves:
  truths:
    - "config.rs 的 validate() 和 validate_and_compile() 调用 CompiledMetaFilters::try_from_include_exclude(&filters.include, &filters.exclude)，编译路径无 try_from_meta 引用"
    - "cli/run.rs 的 FilterProcessor::new 从 filter.include.start_ts / filter.include.end_ts 读取时间范围"
    - "cli/run.rs 的 recompile_meta_if_needed 调用 try_from_include_exclude"
    - "cli/init.rs 的 CONFIG_TEMPLATE_ZH 与 CONFIG_TEMPLATE_EN 模板使用新嵌套格式（[features.filter.include] / [features.filter.exclude] / [features.filter.indicators] / [features.filter.sql] 子表）"
    - "tests/integration.rs 中所有手动构造 FiltersFeature { ... meta: MetaFilters { ... } ... } 的字面量改为 { include: IncludeFilters { ... }, exclude: ExcludeFilters { ... }, ... } 形态"
    - "`cargo run -- init -o test_init.toml --force` 生成的配置含 [features.filter.include] / [features.filter.exclude] 子表头"
    - "`cargo run -- validate -c <根目录 config.toml>` 对旧格式 config 成功通过（exit code 0）"
    - "`cargo run -- validate -c <新嵌套格式 config>` 对新格式 config 成功通过（exit code 0）"
    - "cargo test 全部通过，cargo clippy --all-targets -- -D warnings 零警告"
  artifacts:
    - path: "src/config.rs"
      provides: "validate() / validate_and_compile() 使用新 try_from_include_exclude API"
      contains: "try_from_include_exclude"
    - path: "src/cli/run.rs"
      provides: "FilterProcessor::new 和 recompile_meta_if_needed 使用新字段路径"
      contains: "filter.include"
    - path: "src/cli/init.rs"
      provides: "CONFIG_TEMPLATE_ZH 和 CONFIG_TEMPLATE_EN 含新嵌套格式 filter 区块"
      contains: "[features.filter.include]"
    - path: "tests/integration.rs"
      provides: "FiltersFeature 字面构造使用 include/exclude 字段"
      contains: "IncludeFilters"
  key_links:
    - from: "src/config.rs::Config::validate"
      to: "src/features/filters.rs::CompiledMetaFilters::try_from_include_exclude"
      via: "filters.include + filters.exclude"
      pattern: "try_from_include_exclude\\(&filters\\.include, &filters\\.exclude\\)"
    - from: "src/cli/run.rs::FilterProcessor::new"
      to: "src/features/filters.rs::FiltersFeature"
      via: "filter.include.start_ts / filter.include.end_ts"
      pattern: "filter\\.include\\.(start_ts|end_ts)"
    - from: "src/cli/init.rs::CONFIG_TEMPLATE_ZH"
      to: "[features.filter.include] / [features.filter.exclude] 子表示例"
      via: "template string"
      pattern: "\\[features\\.filter\\.include\\]"
---

<objective>
将 Plan 01 已重构的 `FiltersFeature` 新字段路径与 API 签名传播到所有调用方：`src/config.rs`、`src/cli/run.rs`、`src/cli/init.rs`、`tests/integration.rs`。同时把 `init` 生成的默认配置模板换成新嵌套格式（per D-06），让新用户直接得到最新用法；旧 config（项目根目录 `config.toml`）通过 Plan 01 实现的向后兼容 Deserialize 继续可用。

Purpose: 让 Phase 17 重构形成完整闭环——构建通过、所有测试通过、init 命令产出新格式、旧格式 validate 通过。
Output: 4 个文件更新；新增 1 个集成测试验证新嵌套格式 + 旧扁平格式均能 validate 成功；CLI 命令行为符合 D-06。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-filter-nesting/17-CONTEXT.md
@.planning/phases/17-filter-nesting/17-RESEARCH.md
@.planning/phases/17-filter-nesting/17-PATTERNS.md
@.planning/phases/17-filter-nesting/17-01-PLAN.md
@src/features/filters.rs
@src/config.rs
@src/cli/run.rs
@src/cli/init.rs
@tests/integration.rs
@config.toml

<interfaces>
Plan 01 完成后 src/features/filters.rs 暴露的新 API（必须使用，不可自创）：

```
pub struct FiltersFeature {
    pub enable: bool,
    pub include: IncludeFilters,
    pub exclude: ExcludeFilters,
    pub indicators: IndicatorFilters,
    pub sql: SqlFilters,
    pub record_sql: SqlFilters,
}

pub struct IncludeFilters {
    pub users, ips, sessions, threads, statements, apps, tags: Option<Vec<String>>,
    pub start_ts, end_ts: Option<String>,
    pub trxids: Option<TrxidSet>,
}

pub struct ExcludeFilters {
    pub users, ips, sessions, threads, statements, apps, tags: Option<Vec<String>>,
}

pub struct SqlFilters {
    pub includes: Option<Vec<String>>,
    pub excludes: Option<Vec<String>>,
}

impl CompiledMetaFilters {
    pub fn try_from_include_exclude(
        include: &IncludeFilters,
        exclude: &ExcludeFilters,
    ) -> crate::error::Result<Self>;
}
```

`pub use` 检查（src/features/mod.rs）：本计划必须确认 `IncludeFilters` 和 `ExcludeFilters` 已导出，或调用方使用完整路径 `crate::features::filters::IncludeFilters`。
</interfaces>

<field_path_map>
重构前 → 重构后（caller 视角）：

| 重构前 | 重构后 |
|---|---|
| `filters.meta` (作为参数) | `&filters.include, &filters.exclude` |
| `filter.meta.start_ts` | `filter.include.start_ts` |
| `filter.meta.end_ts` | `filter.include.end_ts` |
| `CompiledMetaFilters::try_from_meta(&filters.meta)` | `CompiledMetaFilters::try_from_include_exclude(&filters.include, &filters.exclude)` |
| `MetaFilters { usernames: ..., exclude_usernames: ..., client_ips: ..., ... }` | `IncludeFilters { users: ..., ips: ..., ... }` + `ExcludeFilters { users: ..., ips: ..., ... }` |
| `SqlFilters { include_patterns: ..., exclude_patterns: ... }` | `SqlFilters { includes: ..., excludes: ... }` |

注意：现存集成测试中错误消息断言 `err_msg.contains("features.filters.usernames")` 需更新为 `err_msg.contains("features.filters.include.users")`（路径字符串随 D-01 字段改名同步更新，与 Plan 01 中 `try_from_include_exclude` 内的 `compile_patterns` field 路径字符串一致）。
</field_path_map>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 更新 callers（config.rs / cli/run.rs / tests/integration.rs）使新签名编译通过</name>
  <files>src/config.rs, src/cli/run.rs, tests/integration.rs</files>
  <read_first>
    - src/features/filters.rs (Plan 01 完成后状态，FiltersFeature/IncludeFilters/ExcludeFilters/SqlFilters/CompiledMetaFilters::try_from_include_exclude 定义)
    - src/config.rs (第 54-65 行 validate / 第 118-187 行 validate_and_compile / 第 880-920 行测试块 test_validate_invalid_regex_in_filters / 第 1140-1160 行 test_validate_and_compile_invalid_regex_returns_err)
    - src/cli/run.rs (第 41-65 行 FilterProcessor / 第 660-680 行 recompile_meta_if_needed)
    - tests/integration.rs (第 780-960 行 test_handle_validate_with_filters_all_fields / test_handle_validate_filters_disabled / test_handle_run_with_filters_builds_pipeline / test_handle_run_with_transaction_filters_prescans / test_handle_run_with_min_runtime_filter)
    - .planning/phases/17-filter-nesting/17-PATTERNS.md (key field path map 全表 + src/config.rs / src/cli/run.rs 章节)
  </read_first>
  <behavior>
    - `cargo build` 整体通过（lib + tests + bins）。
    - `cargo test` 全部测试通过。
    - `cargo run -- validate -c config.toml`（项目根目录的旧格式 config）退出码 0，无报错。
    - 新增集成测试 `test_validate_new_nested_format_passes` 与 `test_validate_old_flat_format_passes`：在 tempfile 写入新嵌套格式 toml / 旧扁平格式 toml，调用 `Config::from_file` 后 `validate()` 返回 Ok。
  </behavior>
  <action>
    **(A) src/config.rs**
    - 第 60 行：`CompiledMetaFilters::try_from_meta(&filters.meta)?;` → `CompiledMetaFilters::try_from_include_exclude(&filters.include, &filters.exclude)?;`
    - 第 132 行：同样改 `try_from_meta(&filters.meta)?` → `try_from_include_exclude(&filters.include, &filters.exclude)?`。
    - 第 903 行测试断言 `err_msg.contains("features.filters.usernames")` → `err_msg.contains("features.filters.include.users")`。
    - 第 1157 行同样更新断言字符串。
    - 在 `mod tests` 中新增两个测试函数：
      `fn test_validate_new_nested_format_passes()` — 构造含 `[features.filter.include] users = ["admin"]` + `[features.filter.exclude] users = ["guest"]` + `[exporter.csv] file = "out.csv"` + `[sqllog] path = "sqllogs"` 的 toml 字符串，`toml::from_str::<Config>(...)` 解析后调用 `validate()`，断言 `is_ok()`。
      `fn test_validate_old_flat_format_passes()` — 构造含 `[features.filter] usernames = ["admin"]` + `exclude_usernames = ["guest"]` + 旧字段的 toml，validate() 返回 Ok。

    **(B) src/cli/run.rs**
    - 第 58-59 行：`start_ts: filter.meta.start_ts.clone()` → `start_ts: filter.include.start_ts.clone()`；`end_ts: filter.meta.end_ts.clone()` → `end_ts: filter.include.end_ts.clone()`。
    - 第 678 行：`CompiledMetaFilters::try_from_meta(&filters.meta)?` → `CompiledMetaFilters::try_from_include_exclude(&filters.include, &filters.exclude)?`。
    - 文件其它部分不动（热路径 `process_with_meta` 只访问拷贝后的 self.start_ts/self.end_ts，无字段路径变化；`scan_log_file_for_matches` 引用 `filters.indicators` / `filters.sql` 子表名不变）。

    **(C) tests/integration.rs**
    - 引用部分：保留 `use dm_database_sqllog2db::features::{FeaturesConfig, FiltersFeature, ReplaceParametersConfig};`；新增 `use dm_database_sqllog2db::features::filters::{IncludeFilters, ExcludeFilters};` 到合适位置（约第 13 行附近）。
    - 第 783-810 行 `test_handle_validate_with_filters_all_fields`：
      `meta: MetaFilters { start_ts, end_ts, usernames, client_ips, trxids, ..Default::default() }` 替换为：
      `include: IncludeFilters { start_ts: Some("2025-01-01".to_string()), end_ts: Some("2025-12-31".to_string()), users: Some(vec!["admin".to_string()]), ips: Some(vec!["10.0.0.1".to_string()]), trxids: Some([CompactString::new("tx1")].into_iter().collect()), ..Default::default() },`
      `exclude: ExcludeFilters::default(),`
      `SqlFilters { include_patterns: Some(vec!["SELECT".to_string()]), exclude_patterns: Some(vec!["DROP".to_string()]) }` → `SqlFilters { includes: Some(vec!["SELECT".to_string()]), excludes: Some(vec!["DROP".to_string()]) }`。
    - 第 820-832 行 `test_handle_validate_filters_disabled`：`meta: MetaFilters::default()` → `include: IncludeFilters::default(), exclude: ExcludeFilters::default()`。
    - 第 871-878 行 `test_handle_run_with_filters_builds_pipeline`：`meta: MetaFilters { usernames: Some(vec!["TESTUSER".to_string()]), ..Default::default() }` → `include: IncludeFilters { users: Some(vec!["TESTUSER".to_string()]), ..Default::default() }, exclude: ExcludeFilters::default()`。
    - 第 933-942 行 `test_handle_run_with_transaction_filters_prescans`：`meta: MetaFilters::default()` → `include: IncludeFilters::default(), exclude: ExcludeFilters::default()`。
    - 第 967-976 行 `test_handle_run_with_min_runtime_filter`：同样模式。
    - 删除任何不再使用的 `MetaFilters` import（若有 `use ...::MetaFilters;`）。
    - 若 `compact_str::CompactString` 在测试内首次使用，按现有 import 模式增补 `use compact_str::CompactString;` 到 test mod 或文件顶部（参照现有代码中已存在的 `compact_str::CompactString::new(s)` 调用）。

    **(D) 提交**
    使用 `git commit -m "feat(17-02): update filter callers to new nested API"`（Co-Authored-By 行保留）。
  </action>
  <verify>
    <automated>cargo build --tests 2>&1 | tail -5 && cargo test 2>&1 | tail -15 && cargo clippy --all-targets -- -D warnings 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build --tests` 退出码 0。
    - `cargo test` 退出码 0，所有测试 PASS（含 Plan 01 的 5 个新测试 + 本任务新增的 2 个测试 + 现存所有测试）。
    - `cargo clippy --all-targets -- -D warnings` 零警告。
    - `grep -n "try_from_meta" src/config.rs src/cli/run.rs` 0 命中。
    - `grep -n "filters\.meta\b\|filter\.meta\b" src/config.rs src/cli/run.rs tests/integration.rs` 0 命中。
    - `grep -n "MetaFilters" tests/integration.rs` 0 命中（已全部替换为 IncludeFilters / ExcludeFilters）。
    - `grep -c "try_from_include_exclude" src/config.rs` ≥ 2。
    - `grep -c "try_from_include_exclude" src/cli/run.rs` ≥ 1。
    - `grep -c "filter\.include\.start_ts\|filter\.include\.end_ts" src/cli/run.rs` ≥ 2。
    - `grep -c "include_patterns:\|exclude_patterns:" tests/integration.rs` 0 命中（SqlFilters 字段已改名）。
    - `grep -c "fn test_validate_new_nested_format_passes" src/config.rs` ≥ 1。
    - `grep -c "fn test_validate_old_flat_format_passes" src/config.rs` ≥ 1。
    - `cargo run -- validate -c config.toml` 退出码 0（旧根目录 config 可解析）。
    - git log 最新一条 commit 信息匹配 `feat(17-02): update filter callers to new nested API`。
  </acceptance_criteria>
  <done>4 个 caller 文件适配完成；2 个新增 validate 测试存在并 PASS；全套 cargo test/clippy 通过；旧根目录 config.toml validate 成功；commit 已创建。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: init 命令模板切换为新嵌套格式 + 端到端 init/validate 验收</name>
  <files>src/cli/init.rs, tests/integration.rs</files>
  <read_first>
    - src/cli/init.rs (第 1-284 行全部：CONFIG_TEMPLATE_ZH 第 1-173 行，CONFIG_TEMPLATE_EN 第 175-283 行)
    - .planning/phases/17-filter-nesting/17-CONTEXT.md (`<specifics>` 章节：新格式 toml 示例 + Claude's Discretion 中 init 字段命名)
    - .planning/phases/17-filter-nesting/17-PATTERNS.md (src/cli/init.rs 章节：目标格式 toml 注释示例)
    - tests/integration.rs (任何已存在的 init / handle_init 测试函数；通过 `grep -n "handle_init\|init_template" tests/integration.rs` 定位)
  </read_first>
  <behavior>
    - `cargo run -- init -o /tmp/test_init.toml --force` 生成的文件包含 `[features.filter.include]` 与 `[features.filter.exclude]` 子表头。
    - `cargo run -- init -o /tmp/test_init.toml --force` 生成的文件**不**包含旧字段名 `usernames`、`exclude_usernames`、`client_ips`、`exclude_client_ips`、`sess_ids`、`thrd_ids`、`appnames`、`include_patterns`、`exclude_patterns` 作为活跃配置（仅可在注释里出现 0 次；最简单的做法是模板里这些旧名都不出现）。
    - `cargo run -- validate -c /tmp/test_init.toml` 退出码 0。
    - 新增集成测试 `test_init_generates_new_nested_format`：在 tempfile 目录运行 init，断言生成文件含 `[features.filter.include]` 子表头，且 `Config::from_file` 解析无报错。
  </behavior>
  <action>
    **(A) src/cli/init.rs — CONFIG_TEMPLATE_ZH（中文模板，第 90-152 行 [features.filters] 区块）**
    将整个 `[features.filters]` 至 `# --- SQL 过滤器 ...` 至 `[features.filters.sql]` 含 include_patterns / exclude_patterns 注释行的区块替换为以下结构（保留 `enable = false` 行；按 PATTERNS.md src/cli/init.rs 章节给出的目标模板）：

    ```
    [features.filter]
    # 是否启用过滤器
    enable = false

    # --- 包含过滤器（Record-level，AND 语义：所有已配置字段都必须匹配才保留）---
    [features.filter.include]
    # 过滤指定的用户名（支持正则匹配）
    # users = ["SYSDBA"]
    # 过滤指定的客户端 IP（支持正则匹配）
    # ips = ["127.0.0.1", "192\\.168"]
    # 过滤指定的会话 ID（支持正则匹配）
    # sessions = ["0x7f41435437a8"]
    # 过滤指定的线程 ID（支持正则匹配）
    # threads = ["2188515"]
    # 过滤指定的语句类型（支持正则匹配）
    # statements = ["INS", "UPD", "DEL"]
    # 过滤指定的应用名称（支持正则匹配）
    # apps = ["DMSQL"]
    # 过滤指定的 tag（支持正则匹配）
    # tags = ["\\[SEL\\]"]
    # 过滤时间范围（格式：2023-01-01 00:00:00）
    # start_ts = "2023-01-01 00:00:00"
    # end_ts   = "2023-01-01 23:59:59"
    # 过滤指定的事务 ID
    # trxids = ["257809109", "257809110"]

    # --- 排除过滤器（Record-level，OR-veto：任一命中则丢弃）---
    [features.filter.exclude]
    # users = ["guest", "^anon"]
    # ips = ["^10\\.0", "^172\\.16"]
    # sessions = ["^0x0000"]
    # threads = ["^0$"]
    # statements = ["SEL", "SET"]
    # apps = ["monitor", "health"]
    # tags = ["\\[SET\\]", "\\[OTH\\]"]

    # --- 指标过滤器（Transaction-level：满足条件则保留包含该语句的整个事务，需要预扫描）---
    [features.filter.indicators]
    # 过滤指定的执行 ID（保留整个事务）
    # exec_ids = [257809109, 257809110]
    # 过滤最小执行时长（毫秒）
    # min_runtime_ms = 1000
    # 过滤最小影响行数
    # min_row_count = 100

    # --- SQL 过滤器（Transaction-level：满足模式则保留整个事务，需要预扫描）---
    [features.filter.sql]
    # 包含模式列表（SQL 包含任一模式则匹配）
    # includes = ["FROM USER_TABLES", "DELETE FROM"]
    # 排除模式列表（SQL 包含任一模式则剔除）
    # excludes = ["SELECT 1", "DUAL"]
    ```

    替换时整段 raw string 字面值连同 `# --- 元数据过滤器 ...` 注释一并删除；保留模板前后未涉及的部分（`[features.template_analysis]` 及 `[features.filters.indicators]` 之后的内容，注意原 `[features.filters.indicators]` 是旧子表名，新模板的 `[features.filter.indicators]` 同样位于过滤器区块内；以新模板内容为准）。

    **(B) CONFIG_TEMPLATE_EN（英文模板，第 200-261 行）**
    同样规则替换为英文版本：将原 `[features.filters]` / `# --- Meta filters ...` 起至 `# exclude_patterns = ["SELECT 1", "DUAL"]` 末尾整段替换为：

    ```
    [features.filter]
    # Enable the filter pipeline
    enable = false

    # --- Include filters (record-level, AND semantics: every configured field must match) ---
    [features.filter.include]
    # users = ["SYSDBA"]
    # ips = ["127.0.0.1", "192\\.168"]
    # sessions = ["0x7f41435437a8"]
    # threads = ["2188515"]
    # statements = ["INS", "UPD", "DEL"]
    # apps = ["DMSQL"]
    # tags = ["\\[SEL\\]"]
    # start_ts = "2023-01-01 00:00:00"
    # end_ts   = "2023-01-01 23:59:59"
    # trxids = ["257809109", "257809110"]

    # --- Exclude filters (record-level, OR-veto: any match drops the record) ---
    [features.filter.exclude]
    # users = ["guest", "^anon"]
    # ips = ["^10\\.0", "^172\\.16"]
    # sessions = ["^0x0000"]
    # threads = ["^0$"]
    # statements = ["SEL", "SET"]
    # apps = ["monitor", "health"]
    # tags = ["\\[SET\\]", "\\[OTH\\]"]

    # --- Indicator filters (transaction-level: match retains the whole transaction; requires pre-scan) ---
    [features.filter.indicators]
    # exec_ids = [257809109, 257809110]
    # min_runtime_ms = 1000
    # min_row_count = 100

    # --- SQL filters (transaction-level: match retains the whole transaction; requires pre-scan) ---
    [features.filter.sql]
    # includes = ["FROM USER_TABLES", "DELETE FROM"]
    # excludes = ["SELECT 1", "DUAL"]
    ```

    **重要——TOML 表名约定**：
    Plan 01 的 `FiltersFeature` 字段名是 `enable / include / exclude / indicators / sql / record_sql`，但 `FeaturesConfig.filters: Option<FiltersFeature>` 字段名仍叫 `filters`（**复数**）。因此 TOML 顶层路径是 `[features.filters]`（带 s），而非 `[features.filter]`。**修正以上模板中所有 `[features.filter.X]` 为 `[features.filters.X]`**（保持与现有 FeaturesConfig 字段名兼容；CONTEXT.md 的 `[features.filter]` 写法是文档简写，实际 toml 用 features.filters）。
    检查方式：`grep "filters: Option<FiltersFeature>" src/features/mod.rs` 应命中。如果该字段也已被 Phase 17 改名为单数 `filter`，则使用 `[features.filter.*]`；否则使用 `[features.filters.*]`。**默认（无证据证明改名）使用 `[features.filters.*]`**，并据此撰写模板。

    **(C) tests/integration.rs — 新增 init 端到端测试**
    在文件末尾追加 `test_init_generates_new_nested_format`：
    - 用 `tempfile::TempDir` 创建临时目录。
    - 构造路径 `dir.path().join("init.toml")`。
    - 调用项目对应的 init 入口（通过 `grep -n "pub fn handle_init\|fn write_template\|CONFIG_TEMPLATE" src/cli/init.rs` 找到公开函数；若公开函数为 `handle_init(path: &Path, force: bool, lang: ...)` 形态则直接调用；若仅暴露常量则 `std::fs::write(path, CONFIG_TEMPLATE_ZH).unwrap()`）。
    - 读取生成文件内容 `std::fs::read_to_string(path).unwrap()`，断言：
      - `content.contains("[features.filters.include]")` 为 true（或视 B 节约定为 `[features.filter.include]`）。
      - `content.contains("[features.filters.exclude]")` 为 true。
      - `content.contains("users = ")` 至少作为注释行存在（粗略字符串包含即可）。
      - 不含旧字段名作为活跃 key：`!content.contains("\nusernames = ")`、`!content.contains("\nexclude_usernames = ")`、`!content.contains("\ninclude_patterns = ")`、`!content.contains("\nexclude_patterns = ")`（注：注释行 `# usernames = ...` 不算活跃，本断言用换行前缀匹配；本模板按 B 节已无任何旧字段名注释，断言会通过）。
    - `let cfg: Config = toml::from_str(&content).unwrap();` 解析成功（断言不 panic）。
    - `cfg.validate()` 返回 Ok（虽然模板里 enable=false，但 validate 仍 OK）。

    **(D) 提交**
    `git commit -m "feat(17-02): regenerate init template with nested filter format"`（Co-Authored-By 行保留）。
  </action>
  <verify>
    <automated>rm -f /tmp/sqllog2db_init_test.toml && cargo run --quiet -- init -o /tmp/sqllog2db_init_test.toml --force 2>&1 | tail -3 && grep -c "features.filters.include\|features.filter.include" /tmp/sqllog2db_init_test.toml && grep -c "features.filters.exclude\|features.filter.exclude" /tmp/sqllog2db_init_test.toml && cargo run --quiet -- validate -c /tmp/sqllog2db_init_test.toml 2>&1 | tail -3 && cargo run --quiet -- validate -c config.toml 2>&1 | tail -3 && cargo test 2>&1 | tail -10 && cargo clippy --all-targets -- -D warnings 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `cargo run -- init -o /tmp/sqllog2db_init_test.toml --force` 退出码 0。
    - 生成文件 `/tmp/sqllog2db_init_test.toml` 内 `grep -c "\[features\.filters\.include\]"` ≥ 1（或路径采用 `[features.filter.include]` 时 ≥ 1）。
    - 生成文件内 `grep -c "\[features\.filters\.exclude\]"` ≥ 1（或对应单数路径）。
    - 生成文件内 `grep -c "\[features\.filters\.indicators\]"` ≥ 1。
    - 生成文件内 `grep -c "\[features\.filters\.sql\]"` ≥ 1。
    - 生成文件内 `grep -E "^[^#]*\busernames\s*=" /tmp/sqllog2db_init_test.toml` 0 命中（无活跃旧字段）。
    - 生成文件内 `grep -E "^[^#]*\binclude_patterns\s*=" /tmp/sqllog2db_init_test.toml` 0 命中。
    - `cargo run -- validate -c /tmp/sqllog2db_init_test.toml` 退出码 0。
    - `cargo run -- validate -c config.toml`（旧根目录 config）退出码 0，证明向后兼容。
    - `cargo test` 全部 PASS，含新增 `test_init_generates_new_nested_format`。
    - `cargo clippy --all-targets -- -D warnings` 零警告。
    - `grep -n "include_patterns\|exclude_patterns" src/cli/init.rs` 0 命中（模板内只用新字段名）。
    - `grep -nE "\busernames\b|\bexclude_usernames\b|\bclient_ips\b|\bexclude_client_ips\b|\bsess_ids\b|\bthrd_ids\b|\bappnames\b" src/cli/init.rs` 0 命中。
    - git log 最新一条 commit 信息匹配 `feat(17-02): regenerate init template with nested filter format`。
  </acceptance_criteria>
  <done>init 模板换新；端到端 init→validate 在新模板上通过；validate 对旧根目录 config 通过；全套 test/clippy 绿；commit 已创建。</done>
</task>

</tasks>

<verification>
- 所有 caller 文件不再引用 `filters.meta` / `MetaFilters` / `try_from_meta`
- init 命令产出文件使用新嵌套格式子表头
- `cargo run -- validate -c <新格式配置>` 退出码 0
- `cargo run -- validate -c <旧格式根目录 config.toml>` 退出码 0（向后兼容验证）
- `cargo test` 全绿，`cargo clippy --all-targets -- -D warnings` 零警告
- Phase 17 4 条 ROADMAP Success Criteria 全部满足
</verification>

<success_criteria>
- 整体 `cargo test` 通过，`cargo clippy --all-targets -- -D warnings` 零警告
- `cargo run -- init -o file.toml --force` 生成新嵌套格式
- `cargo run -- validate -c <file.toml>` 对新格式和旧格式 config 均通过
- 项目根目录现有 `config.toml`（旧格式）继续 validate 成功
- pipeline.is_empty() 热路径快速退出逻辑未被破坏（全部测试 + clippy 通过即证明）
</success_criteria>

<output>
Create `.planning/phases/17-filter-nesting/17-02-SUMMARY.md` when done
</output>
