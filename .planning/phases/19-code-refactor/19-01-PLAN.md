---
phase: 19-code-refactor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pipeline/filters.rs
  - src/pipeline/filters/mod.rs
  - src/pipeline/filters/types.rs
  - src/pipeline/filters/compiled.rs
  - src/pipeline/filters/serde_helpers.rs
  - src/pipeline/mod.rs
autonomous: true
requirements:
  - REFACTOR-01
  - REFACTOR-04
tags:
  - rust
  - refactor
  - module-split
  - visibility

must_haves:
  truths:
    - "src/pipeline/filters.rs 不再存在；其内容已迁移到 src/pipeline/filters/ 目录下的子模块"
    - "外部调用路径 crate::pipeline::filters::{FiltersFeature, IncludeFilters, ExcludeFilters, RecordMeta, SqlFilters, IndicatorFilters, CompiledMetaFilters, CompiledSqlFilters} 保持不变"
    - "src/pipeline/filters/ 目录下每个 .rs 子文件行数 ≤ 300 行"
    - "tests/integration.rs 中对 pipeline::filters 类型的引用编译通过（IncludeFilters, ExcludeFilters, IndicatorFilters, SqlFilters）"
    - "cargo test 全部 55+ 测试通过"
    - "cargo clippy --all-targets -- -D warnings 零警告"
  artifacts:
    - path: "src/pipeline/filters/mod.rs"
      provides: "filters 模块入口与 re-export，保持原有 crate::pipeline::filters::* 路径"
      contains: "pub use"
    - path: "src/pipeline/filters/types.rs"
      provides: "RecordMeta / IncludeFilters / ExcludeFilters / FiltersFeature / RawFiltersFeature / IndicatorFilters / SqlFilters 结构定义"
      contains: "pub struct"
    - path: "src/pipeline/filters/compiled.rs"
      provides: "CompiledMetaFilters / CompiledSqlFilters 编译后过滤器实现"
      contains: "pub struct CompiledMetaFilters"
    - path: "src/pipeline/filters/serde_helpers.rs"
      provides: "私有 serde 辅助函数 vec_to_hashset / vec_to_i64_hashset / compile_patterns / match_any_regex 及类型别名 TrxidSet"
      contains: "fn vec_to_hashset"
  key_links:
    - from: "src/pipeline/mod.rs"
      to: "src/pipeline/filters/mod.rs"
      via: "pub mod filters"
      pattern: "pub mod filters"
    - from: "src/pipeline/filters/mod.rs"
      to: "types::*, compiled::*"
      via: "pub use"
      pattern: "pub use (types|compiled)::"
    - from: "src/pipeline/filters/compiled.rs"
      to: "super::types, super::serde_helpers"
      via: "use super::*"
      pattern: "use super::(types|serde_helpers)"
---

<objective>
将 1481 行的 `src/pipeline/filters.rs` 就地拆分为 `src/pipeline/filters/` 目录子模块（按 D-03 模式），同时按 D-10 全面收紧 `pub` 可见性。

Purpose: REFACTOR-01 要求超过 300 行的源文件按职责拆分（D-01/D-02），REFACTOR-04 要求收紧内部类型可见性。filters.rs 独立性最强（无下游 src/ 引用，仅被 pipeline/mod.rs 内部使用），因此放在 Wave 1 最优先拆分。

Output:
- `src/pipeline/filters/mod.rs`（re-export + 入口）
- `src/pipeline/filters/types.rs`（serde 数据结构）
- `src/pipeline/filters/compiled.rs`（编译后过滤器）
- `src/pipeline/filters/serde_helpers.rs`（私有辅助函数）
- 删除 `src/pipeline/filters.rs`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-code-refactor/19-CONTEXT.md
@.planning/phases/19-code-refactor/19-RESEARCH.md
@.planning/phases/19-code-refactor/19-PATTERNS.md
@src/pipeline/filters.rs
@src/pipeline/mod.rs
@src/config/mod.rs

<interfaces>
<!-- 本 Plan 必须保持稳定的外部路径与签名（编译契约） -->

外部消费者（src/ 内 + tests/integration.rs）依赖的类型路径（拆分后必须保持可达）：

```
crate::pipeline::filters::FiltersFeature       // src/config/mod.rs, src/cli/run.rs, src/pipeline/mod.rs
crate::pipeline::filters::IncludeFilters       // tests/integration.rs (pub use), src/cli/run.rs
crate::pipeline::filters::ExcludeFilters       // tests/integration.rs (pub use), src/cli/run.rs
crate::pipeline::filters::RecordMeta           // src/pipeline/mod.rs（Pipeline 热路径）
crate::pipeline::filters::SqlFilters           // tests/integration.rs (use), src/cli/run.rs
crate::pipeline::filters::IndicatorFilters     // tests/integration.rs (use)
crate::pipeline::filters::CompiledMetaFilters  // src/cli/run.rs, src/config/mod.rs
crate::pipeline::filters::CompiledSqlFilters   // src/cli/run.rs, src/config/mod.rs
```

关键热路径函数签名（必须保持不变，逐字搬移）：

```
CompiledMetaFilters::try_from_include_exclude(&IncludeFilters, &ExcludeFilters) -> crate::error::Result<Self>
CompiledMetaFilters::has_filters(&self) -> bool
CompiledMetaFilters::has_any_filters(&self) -> bool
CompiledMetaFilters::should_keep(&self, meta: &RecordMeta) -> bool      // #[inline]
CompiledSqlFilters::try_from_sql_filters(sf: &SqlFilters) -> crate::error::Result<Self>
CompiledSqlFilters::has_filters(&self) -> bool
CompiledSqlFilters::matches(&self, sql: &str) -> bool
IncludeFilters::has_filters(&self) -> bool
ExcludeFilters::has_filters(&self) -> bool
SqlFilters::has_filters(&self) -> bool
SqlFilters::has_transaction_filters(&self) -> bool
SqlFilters::merge_found_trxids(&mut self, Vec<CompactString>)
IndicatorFilters::has_filters(&self) -> bool
IndicatorFilters::matches(&self, exec_id: i64, runtime_ms: f32, row_count: i64) -> bool
```

手写 Deserialize impl（必须随结构一起搬到 types.rs）：

```
impl<'de> Deserialize<'de> for FiltersFeature
struct RawFiltersFeature   // crate-private helper, must stay alongside FiltersFeature
impl From<RawFiltersFeature> for FiltersFeature
```

类型别名（建议放在 serde_helpers.rs，types.rs 导入）：

```
type TrxidSet = AHashSet<CompactString>;
```
</interfaces>

<visibility_rules>
按 D-10 / D-11 + tests/integration.rs 实际引用情况执行：

- 所有原 `pub struct` / `pub fn` 一律改 `pub(crate)`（binary crate，无跨 crate 暴露）
- 结构体字段：原 `pub field: ...` → `pub(crate) field: ...`
- src/lib.rs 中的 `pub mod pipeline` 与 `pub mod filters` 路径保持现状不动（本 Plan 不修改 lib.rs；最终全局 lib.rs 收紧由 Plan 04 处理）
- `serde_helpers.rs` 内部函数与 `TrxidSet` 类型别名一律 private（不带 pub），仅在本目录内被 types.rs / compiled.rs 通过 `use super::serde_helpers::*` 引用
</visibility_rules>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 拆分 filters.rs 为 types.rs + serde_helpers.rs，删除原 filters.rs</name>
  <files>
    src/pipeline/filters/mod.rs (new),
    src/pipeline/filters/types.rs (new),
    src/pipeline/filters/serde_helpers.rs (new),
    src/pipeline/filters.rs (delete)
  </files>
  <read_first>
    src/pipeline/filters.rs,
    src/pipeline/mod.rs,
    src/config/mod.rs,
    .planning/phases/19-code-refactor/19-PATTERNS.md,
    .planning/phases/19-code-refactor/19-RESEARCH.md
  </read_first>
  <action>
    1) 删除文件 `src/pipeline/filters.rs`（其内容将被搬到新建子模块）。

    2) 新建 `src/pipeline/filters/mod.rs`：包含三行 `mod` 声明（`mod types; mod compiled; mod serde_helpers;`）和 `pub(crate) use` re-export（FiltersFeature, IncludeFilters, ExcludeFilters, RecordMeta, SqlFilters, IndicatorFilters）。CompiledMetaFilters / CompiledSqlFilters 的 re-export 由 Task 2 一并补齐——本 Task 在 mod.rs 中先用 `mod compiled;` 声明子模块，但 compiled.rs 文件本身在 Task 2 创建。临时方案：本 Task 暂不声明 `mod compiled`，留待 Task 2 添加；这样 Task 1 完成后即可独立编译。

    3) 新建 `src/pipeline/filters/types.rs`：从原 filters.rs 第 1-363 行迁移所有 serde 数据结构。具体迁移项：
       - `use` 区（保留 `ahash::HashSet as AHashSet`, `compact_str::CompactString`, `regex::Regex`, `serde::{Deserialize, Deserializer}`）
       - 类型别名 `type TrxidSet = AHashSet<CompactString>;`（如果在 serde_helpers.rs 也需要，改为在 types.rs 定义后通过 `pub(super) type TrxidSet = ...` 暴露给同目录子模块）
       - `pub struct RecordMeta<'a>` → `pub(crate) struct RecordMeta<'a>`，所有字段加 `pub(crate)`
       - `pub struct IncludeFilters` → `pub(crate)`，所有字段加 `pub(crate)`，保留 `#[derive(Debug, Deserialize, Clone, Default)]` 和所有 `#[serde(default, deserialize_with = "...")]` 属性
       - `impl IncludeFilters { pub fn has_filters }` → `pub(crate) fn has_filters`
       - `pub struct ExcludeFilters` 同上处理
       - `impl ExcludeFilters { pub fn has_filters }` 同上
       - `pub struct FiltersFeature` 同上处理 + 手写的 `impl<'de> Deserialize<'de> for FiltersFeature`
       - `struct RawFiltersFeature`（保持 crate-private，不带 pub）+ `impl From<RawFiltersFeature> for FiltersFeature`
       - `pub struct IndicatorFilters` → `pub(crate)`，所有字段加 `pub(crate)`
       - `impl IndicatorFilters { pub fn has_filters, pub fn matches }` → `pub(crate)`
       - `pub struct SqlFilters` → `pub(crate)`，所有字段加 `pub(crate)`
       - `impl SqlFilters { pub fn has_filters, pub fn has_transaction_filters, pub fn merge_found_trxids }` → `pub(crate)`
       - 凡 types.rs 内部引用 vec_to_hashset / vec_to_i64_hashset 等 serde 辅助函数，改为通过 `use super::serde_helpers::{vec_to_hashset, vec_to_i64_hashset};` 引入

    4) 新建 `src/pipeline/filters/serde_helpers.rs`：从原 filters.rs 第 23-39 行 + 第 333-363 行迁移私有辅助函数。具体迁移项：
       - `use` 区（`ahash::HashSet as AHashSet`, `compact_str::CompactString`, `regex::Regex`, `serde::{Deserialize, Deserializer}`）
       - 函数 `fn vec_to_hashset<'de, D>(deserializer: D) -> Result<Option<TrxidSet>, D::Error>`（保持原签名，私有；如使用 TrxidSet 别名，加 `use super::types::TrxidSet;` 或在本文件重新定义）
       - 函数 `fn vec_to_i64_hashset<'de, D>(deserializer: D) -> Result<Option<AHashSet<i64>>, D::Error>`（私有）
       - 函数 `fn compile_patterns(field: &str, patterns: Option<&[String]>) -> crate::error::Result<Option<Vec<Regex>>>`（私有，但需对同目录子模块可见 → 加 `pub(super)`）
       - 函数 `fn match_any_regex(patterns: Option<&[Regex]>, val: &str) -> bool`（同上，`pub(super)`）
       - vec_to_hashset / vec_to_i64_hashset 用于 serde `deserialize_with`，需保持 `pub(super)`

    5) 在 mod.rs 中通过 `pub(crate) use types::{FiltersFeature, IncludeFilters, ExcludeFilters, RecordMeta, SqlFilters, IndicatorFilters};` re-export。

    6) 暂保留原 filters.rs 中第 670 行起的 `#[cfg(test)] mod tests`——本 Task 暂时不迁移测试（compiled 测试在 Task 2 一并迁），但要确保原文件已删除，否则会冲突。临时方案：将测试代码先剪贴到 types.rs 末尾的 `#[cfg(test)] mod tests` 块中（如果仅测 IncludeFilters/ExcludeFilters/IndicatorFilters/SqlFilters 行为），或放在 mod.rs 末尾（如果跨结构测试）。具体放置位置依测试内容决定。

    每行不超过 300 行限制（D-02）：types.rs 预计 ~280 行，serde_helpers.rs 预计 ~80 行，mod.rs 预计 ~30 行——均符合。

    完成本 Task 后立即 `cargo build`，确保 mod.rs / types.rs / serde_helpers.rs 三个文件能编译；编译失败说明 use 路径缺失，按错误信息补 `use super::*` 或 `pub(super) use`。**禁止改变任何函数体或字段名**——本 Task 是纯文件搬移 + pub → pub(crate) 重写，不修改业务逻辑。
  </action>
  <verify>
    <automated>cargo build 2>&amp;1 | grep -E "(error|warning)" | head -20; test -f src/pipeline/filters/mod.rs &amp;&amp; test -f src/pipeline/filters/types.rs &amp;&amp; test -f src/pipeline/filters/serde_helpers.rs &amp;&amp; ! test -f src/pipeline/filters.rs &amp;&amp; echo "FILES_OK"</automated>
  </verify>
  <acceptance_criteria>
    - src/pipeline/filters.rs 不存在（`test ! -f src/pipeline/filters.rs` 为真）
    - src/pipeline/filters/mod.rs 存在且包含 `mod types;` 和 `mod serde_helpers;`
    - src/pipeline/filters/mod.rs 包含 `pub(crate) use types::{FiltersFeature` 字样（grep 命中）
    - src/pipeline/filters/types.rs 包含 `pub(crate) struct IncludeFilters`、`pub(crate) struct ExcludeFilters`、`pub(crate) struct FiltersFeature`、`pub(crate) struct RecordMeta`、`pub(crate) struct SqlFilters`、`pub(crate) struct IndicatorFilters`
    - src/pipeline/filters/serde_helpers.rs 包含 `fn vec_to_hashset` 和 `fn compile_patterns`，且这些函数**不带** `pub`（仅 `pub(super)` 或无 pub）
    - `cargo build` 退出码 0，无 `error[E0432]: unresolved import` 或 `cannot find type` 错误
    - 每个新建文件行数 ≤ 300（`wc -l src/pipeline/filters/*.rs` 所有数字 ≤ 300）
  </acceptance_criteria>
  <done>filters.rs 已被 mod.rs/types.rs/serde_helpers.rs 替代，cargo build 通过。compiled 部分待 Task 2 完成。</done>
</task>

<task type="auto">
  <name>Task 2: 拆分 compiled.rs 并迁移 filters 测试，收紧字段可见性</name>
  <files>
    src/pipeline/filters/mod.rs,
    src/pipeline/filters/compiled.rs (new),
    src/pipeline/filters/types.rs
  </files>
  <read_first>
    src/pipeline/filters/mod.rs,
    src/pipeline/filters/types.rs,
    src/pipeline/filters/serde_helpers.rs,
    .planning/phases/19-code-refactor/19-PATTERNS.md
  </read_first>
  <action>
    1) 新建 `src/pipeline/filters/compiled.rs`：从原 filters.rs 第 368-668 行迁移编译后过滤器实现（已在 Task 1 时随原 filters.rs 删除前抢救出来，或从 git 历史恢复对应代码块）。具体迁移项：
       - `use super::types::{ExcludeFilters, IncludeFilters, RecordMeta, SqlFilters};`
       - `use super::serde_helpers::compile_patterns;`（pub(super) 已在 Task 1 设置）
       - `use ahash::HashSet as AHashSet; use compact_str::CompactString; use regex::Regex;`
       - `pub struct CompiledMetaFilters` → `pub(crate) struct CompiledMetaFilters`，**所有 15 个字段**（usernames, client_ips, sessions, threads, statements, apps, tags, trxids, exclude_usernames, exclude_client_ips, exclude_sessions, exclude_threads, exclude_statements, exclude_apps, exclude_tags）由 `pub` 改 `pub(crate)`
       - `impl CompiledMetaFilters`:
         - `pub fn try_from_include_exclude(...) -> crate::error::Result<Self>` → `pub(crate) fn try_from_include_exclude`
         - `pub fn has_filters(&self) -> bool` → `pub(crate) fn has_filters`
         - `pub fn has_any_filters(&self) -> bool` → `pub(crate) fn has_any_filters`
         - `#[inline] pub fn should_keep(&self, meta: &RecordMeta) -> bool` → `#[inline] pub(crate) fn should_keep`
       - `pub struct CompiledSqlFilters` → `pub(crate)`，所有字段加 `pub(crate)`
       - `impl CompiledSqlFilters`:
         - `pub fn try_from_sql_filters(sf: &SqlFilters) -> crate::error::Result<Self>` → `pub(crate) fn try_from_sql_filters`
         - `pub fn has_filters(&self) -> bool` → `pub(crate) fn has_filters`
         - `pub fn matches(&self, sql: &str) -> bool` → `pub(crate) fn matches`

    2) 修改 `src/pipeline/filters/mod.rs`：添加 `mod compiled;` 声明，并在 re-export 区追加 `pub(crate) use compiled::{CompiledMetaFilters, CompiledSqlFilters};`。

    3) 迁移原 filters.rs 中第 670 行起的测试模块（Task 1 临时挂在 types.rs 末尾或 mod.rs 末尾的部分）：分类归位：
       - 测 IncludeFilters/ExcludeFilters/FiltersFeature 序列化的测试 → types.rs 末尾的 `#[cfg(test)] mod tests { use super::*; ... }`
       - 测 CompiledMetaFilters/CompiledSqlFilters 行为的测试 → compiled.rs 末尾的 `#[cfg(test)] mod tests { use super::*; use super::super::types::*; ... }`
       - 测 vec_to_hashset 等序列化辅助函数的测试 → serde_helpers.rs 末尾的 `#[cfg(test)] mod tests { use super::*; ... }`（若需要的话；多数辅助函数测试可通过结构体测试间接覆盖）
       原文件共 64 个 `#[test]`，迁移时**保持每个测试函数体逐字不变**，只调整 `use super::*` 引用路径。

    4) compiled.rs 预计 ~280 行（≤ 300，符合 D-02）。

    5) 完成后立即 `cargo build && cargo test --lib pipeline::filters`，确保所有原 filters.rs 测试在新位置仍通过。
  </action>
  <verify>
    <automated>cargo test --lib 2>&amp;1 | tail -20; wc -l src/pipeline/filters/*.rs | awk '{print $1, $2}'</automated>
  </verify>
  <acceptance_criteria>
    - src/pipeline/filters/compiled.rs 存在且包含 `pub(crate) struct CompiledMetaFilters`
    - src/pipeline/filters/compiled.rs 包含 `pub(crate) fn should_keep` 且带 `#[inline]` 属性
    - src/pipeline/filters/mod.rs 包含 `pub(crate) use compiled::{CompiledMetaFilters, CompiledSqlFilters};`
    - `grep -rE "^\s*pub\s+(struct|fn|use)\b" src/pipeline/filters/` 输出为空（所有 pub 已收紧为 pub(crate) 或 pub(super)）
    - `wc -l src/pipeline/filters/*.rs` 每个文件 ≤ 300 行
    - `cargo test --lib` 通过，原 filters.rs 的 64 个测试全部出现在新位置并通过（test passed 数 ≥ 64 在 filters 模块）
    - `cargo build` 退出码 0
  </acceptance_criteria>
  <done>filters.rs 完整拆分为 4 个文件，所有测试通过，可见性收紧完成。</done>
</task>

<task type="auto">
  <name>Task 3: 修复消费者侧编译错误并完整回归</name>
  <files>
    src/pipeline/mod.rs,
    src/config/mod.rs,
    src/cli/run.rs
  </files>
  <read_first>
    src/pipeline/filters/mod.rs,
    src/pipeline/mod.rs,
    src/config/mod.rs,
    src/cli/run.rs,
    tests/integration.rs
  </read_first>
  <action>
    本 Task 修复因 pub → pub(crate) 收紧引发的下游编译错误。**禁止** 通过把可见性恢复为 `pub` 来"修复" tests/integration.rs 报错——integration tests 与 src/ 在同一 crate 内（lib target），`pub(crate)` 完全足够，若它们仍报错说明 lib.rs 上层 re-export 路径不通，需检查 lib.rs `pub use exporter::{...}` 是否影响（本 Plan 不动 lib.rs，但要核查）。

    步骤：

    1) 运行 `cargo build --lib 2>&1` 收集所有 `unresolved import` 或 `cannot find type` 错误。

    2) 对每条错误：
       - 如果是 src/ 内部模块（pipeline/mod.rs, config/mod.rs, cli/run.rs 等）引用 `crate::pipeline::filters::SomeType` 报错 → 检查 mod.rs 是否有对应 `pub(crate) use`。若缺失则在 mod.rs 补 re-export。
       - 如果某 import 之前依赖 `pub` 现在因 `pub(crate)` 仍可用（同 crate）→ 应自动通过，无需改动。
       - 如果是 `use crate::pipeline::filters::{CompiledMetaFilters, ...}` 路径报错 → 确认 Task 2 已在 mod.rs 添加 `pub(crate) use compiled::{CompiledMetaFilters, CompiledSqlFilters};`。

    3) 运行 `cargo test 2>&1` 收集 integration tests 编译错误。若 `tests/integration.rs` 报 `IncludeFilters is private` 等错误：
       - 原因：integration test 通过 `dm_database_sqllog2db::pipeline::filters::IncludeFilters` 路径访问，需要 lib.rs `pub mod pipeline` + `pipeline/mod.rs` `pub mod filters` + `pipeline/filters/mod.rs` `pub use types::IncludeFilters`。
       - **修复策略**：将 `pipeline/filters/mod.rs` 中 integration test 依赖的 4 个类型（IncludeFilters, ExcludeFilters, IndicatorFilters, SqlFilters）的 re-export **保留为 `pub use`**（不是 `pub(crate) use`），并在注释中标注 `// pub: required by tests/integration.rs`。其他类型（FiltersFeature, RecordMeta, CompiledMetaFilters, CompiledSqlFilters）保持 `pub(crate) use`。
       - 同样，types.rs 中 IncludeFilters/ExcludeFilters/IndicatorFilters/SqlFilters 的 struct 定义必须保持 `pub`（不是 `pub(crate)`），否则 re-export 无效。其字段可保持 `pub(crate)`——integration test 只构造 / 反序列化它们，不直接访问 private 字段。

    4) 检查 tests/integration.rs 的实际使用模式以确认 (3) 中字段可见性收紧不会破坏测试。运行 `grep -E "IncludeFilters\s*\{|ExcludeFilters\s*\{|IndicatorFilters\s*\{|SqlFilters\s*\{" tests/integration.rs` 查看是否有字面字段访问。若有，相应字段从 `pub(crate)` 恢复为 `pub`。

    5) 反复迭代 `cargo build && cargo test`，直到全部通过。

    6) 最终运行 `cargo clippy --all-targets -- -D warnings` 确保零警告。常见可能新增的 warning：
       - `dead_code`：原 `pub` 项被收紧后若 src/ 内无消费者会触发 → 删除该项或加 `#[allow(dead_code)]`（仅当确属未来扩展点；优先删除）
       - `unused_imports`：拆分中遗留的 `use` 语句 → 删除

    **禁止** 通过 `#[allow(dead_code)]` 批量绕过 dead_code 警告——逐项分析，若真未使用则删除该 pub 项。
  </action>
  <verify>
    <automated>cargo clippy --all-targets -- -D warnings 2>&amp;1 | tail -10; cargo test 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build` 退出码 0
    - `cargo test` 退出码 0，所有现有 55+ 测试通过（含 tests/integration.rs）
    - `cargo clippy --all-targets -- -D warnings` 退出码 0
    - tests/integration.rs 中 `use dm_database_sqllog2db::pipeline::filters::{IndicatorFilters, SqlFilters};` 和 `use dm_database_sqllog2db::pipeline::filters::{ExcludeFilters, IncludeFilters};` 仍能编译（这是源码层硬约束，integration test 至少有一处构造 `IndicatorFilters { ... }`）
    - `grep -rE "^\s*pub\s+(fn|struct|enum)\b" src/pipeline/filters/` 仅命中 types.rs 中 IncludeFilters / ExcludeFilters / IndicatorFilters / SqlFilters 4 个 struct（其他 struct 与所有 fn 均为 pub(crate)）
  </acceptance_criteria>
  <done>所有消费者编译通过，clippy 零警告，integration test 全绿。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 无外部输入跨界 | 本 Plan 为纯源码结构重组，不引入新的数据流或外部输入处理点 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01-01 | Tampering | filters/mod.rs re-export 配置 | mitigate | re-export 路径必须保持 `crate::pipeline::filters::*` 稳定，避免上层模块 `use` 路径错配导致行为漂移；Task 3 通过 `cargo build` + integration test 双重验证 |
| T-19-01-02 | Information Disclosure | pub → pub(crate) 收紧 | accept | 收紧本身降低暴露面，无新风险；binary crate 无跨 crate 数据外泄路径 |
| T-19-01-SC | Tampering | npm/pip/cargo installs | N/A | 本 Plan 不引入任何新依赖，跳过包合法性门禁 |
</threat_model>

<verification>
- `cargo build` 通过（每个 Task 后均运行）
- `cargo test` 全部通过，filters 模块测试数与原 filters.rs 中 64 个 `#[test]` 数量一致
- `cargo clippy --all-targets -- -D warnings` 零警告
- `wc -l src/pipeline/filters/*.rs` 每个文件 ≤ 300 行
- tests/integration.rs 中所有 `dm_database_sqllog2db::pipeline::filters::*` 引用编译通过
- `grep -c "^pub " src/pipeline/filters/serde_helpers.rs | grep -v "^#"` 应为 0（serde_helpers 中无 pub 项，仅 pub(super)）
</verification>

<success_criteria>
- src/pipeline/filters.rs 已删除
- src/pipeline/filters/ 目录下 4 个新文件（mod.rs, types.rs, compiled.rs, serde_helpers.rs）创建完成
- 每个新文件 ≤ 300 行
- 所有可见性按 D-10 收紧（pub → pub(crate)），仅保留 integration test 必需的 4 个 struct 为 pub
- 现有测试全绿，clippy 零警告
- REFACTOR-01（filters.rs 拆分）和 REFACTOR-04（filters 模块可见性收紧）部分完成
</success_criteria>

<output>
Create `.planning/phases/19-code-refactor/19-01-SUMMARY.md` when done
</output>
