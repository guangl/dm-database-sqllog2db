---
phase: 20-test-coverage
plan: 03
type: tdd
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - src/pipeline/fingerprint.rs
autonomous: true
requirements:
  - TEST-04
tags:
  - testing
  - proptest
  - property-test

must_haves:
  truths:
    - "Cargo.toml [dev-dependencies] 段包含 proptest = \"1.6.0\""
    - "src/pipeline/fingerprint.rs 的 #[cfg(test)] mod tests 含 use proptest::prelude::*;"
    - "src/pipeline/fingerprint.rs 含 prop_normalize_template_is_idempotent 属性测试 (幂等性)"
    - "src/pipeline/fingerprint.rs 含 prop_normalize_template_literal_protection 属性测试 (字面量保护不变性)"
    - "cargo test prop_normalize_template_ 全部通过"
    - "cargo clippy --all-targets -- -D warnings 零警告 (proptest! 宏不应触发 unused attribute)"
  artifacts:
    - path: "Cargo.toml"
      provides: "proptest 1.6.0 dev-dependency"
      contains: "proptest = \"1.6.0\""
    - path: "src/pipeline/fingerprint.rs"
      provides: "normalize_template 幂等性 + 字面量保护属性测试"
      contains: "prop_normalize_template_is_idempotent"
  key_links:
    - from: "src/pipeline/fingerprint.rs::mod tests"
      to: "src/pipeline/fingerprint.rs::normalize_template"
      via: "proptest! { #[test] fn prop_... } 直接调用 super::normalize_template"
      pattern: "normalize_template"
    - from: "Cargo.toml [dev-dependencies]"
      to: "src/pipeline/fingerprint.rs::mod tests"
      via: "use proptest::prelude::*"
      pattern: "use proptest::prelude"
---

<objective>
为 `normalize_template` 添加 proptest 属性测试 (TEST-04),验证两条核心不变量:
1. 幂等性: `normalize_template(normalize_template(s)) == normalize_template(s)`
2. 字面量保护不变性: 单引号字面量内的 `--` 注释符号不会被消除

Purpose: 通过随机生成 + 自动缩减 (shrinking) 暴露 `normalize_template` 在任意 ASCII 字符串输入下的潜在 bug,提供比固定单元测试更强的覆盖。
Output: 1) Cargo.toml 新增 proptest 1.6.0 dev-dependency; 2) src/pipeline/fingerprint.rs 的 `#[cfg(test)] mod tests` 末尾追加两条 proptest 函数。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-test-coverage/20-CONTEXT.md
@.planning/phases/20-test-coverage/20-RESEARCH.md
@.planning/phases/20-test-coverage/20-PATTERNS.md
@src/pipeline/fingerprint.rs
@Cargo.toml

<interfaces>
<!-- proptest 测试目标函数,来自代码库 [VERIFIED] -->

From `src/pipeline/fingerprint.rs` (公共函数,L40 附近):
```rust
pub fn normalize_template(sql: &str) -> String
// 四项变换: 注释去除 (-- 行注释、/* */ 块注释) + IN 列表折叠 + 关键字大写 + 空白折叠
// 字符串字面量内的注释符号受保护
```

From `src/pipeline/fingerprint.rs` 现有 mod tests (L325-435):
```rust
#[cfg(test)]
mod tests {
    use super::*;
    // 9 个 fingerprint 测试
    // 8 个 normalize_template 测试,包括:
    //   test_normalize_string_literal_hides_comment_marker:
    //     normalize_template("WHERE col = '-- not a comment'")
    //     contains "'-- not a comment'" — 字面量保护已通过单元测试验证
}
```

From `Cargo.toml` `[dev-dependencies]` 段 (L106-108):
```toml
[dev-dependencies]
tempfile = "3.27.0"
criterion = { version = "0.7", features = ["html_reports"] }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cargo.toml 新增 proptest dev-dependency + 验证编译</name>
  <files>Cargo.toml</files>
  <read_first>
    - Cargo.toml (现有 [dev-dependencies] 段第 106-108 行,确认 tempfile/criterion 写法,新依赖直接追加)
    - .planning/phases/20-test-coverage/20-RESEARCH.md §Standard Stack (proptest 1.6.0 已在 crates.io 验证) + §Package Legitimacy Audit (proptest 标记为 [ASSUMED],需 cargo build 验证)
  </read_first>
  <action>
    在 `Cargo.toml` 现有 `[dev-dependencies]` 段 (criterion 行之后) 追加一行 `proptest = "1.6.0"`。版本严格锁为 1.6.0 (RESEARCH.md §Standard Stack 已通过 crates.io 验证)。

    不要使用 `cargo add proptest@1.6.0 --dev` 命令——直接手动编辑 Cargo.toml,与现有 tempfile/criterion 风格保持一致 (顶层简单字符串版本号,无 features 表)。

    完成后必须验证: `cargo build --tests` 成功 (确认 proptest 1.6.0 与现有依赖兼容、可下载、可编译);若 `cargo build --tests` 失败,立即回滚版本号或排查冲突。

    禁止: 引入 features 表;同时新增其他依赖;升级现有依赖版本;修改 `[dependencies]` (生产依赖)。
  </action>
  <verify>
    <automated>grep -E '^proptest = "1\.6\.0"$' Cargo.toml && cargo build --tests 2>&1 | tail -5 | grep -E '(Finished|Compiling proptest)'</automated>
  </verify>
  <acceptance_criteria>
    - `Cargo.toml` 含一行精确匹配 `proptest = "1.6.0"`
    - `proptest = "1.6.0"` 这行位于 `[dev-dependencies]` 段内 (即在 `[dev-dependencies]` 之后且在下一段标题 `[[bench]]` 之前)
    - `cargo build --tests` 退出码 0
    - `cargo tree --depth 1 --edges normal` 不显示 proptest (因为是 dev-dep)
    - `Cargo.lock` 含 proptest 1.6.x (cargo build 后自动写入)
  </acceptance_criteria>
  <done>proptest 1.6.0 已加入 dev-dependencies,cargo build --tests 通过</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: src/pipeline/fingerprint.rs 追加两条 proptest 属性测试 (TEST-04, D-09/D-10/D-11/D-12)</name>
  <files>src/pipeline/fingerprint.rs</files>
  <read_first>
    - src/pipeline/fingerprint.rs (整个文件,特别是第 320-435 行的 `#[cfg(test)] mod tests` 现有结构;新增 proptest 函数追加到第 434 行 `} // mod tests` 之前)
    - .planning/phases/20-test-coverage/20-CONTEXT.md (D-09 策略 `any::<String>()`、D-10 仅测 normalize_template、D-11 写入 fingerprint.rs mod tests、D-12 两条不变量)
    - .planning/phases/20-test-coverage/20-RESEARCH.md §Pattern 1 (proptest! 宏写法), §Pattern 4 (字面量保护不变性策略示例), §Pitfall 1 (proptest! 宏内不可外加 #[test])
    - .planning/phases/20-test-coverage/20-PATTERNS.md §src/pipeline/fingerprint.rs — proptest 属性测试
  </read_first>
  <behavior>
    - prop_normalize_template_is_idempotent: 输入 `s in any::<String>()`,验证 `normalize_template(normalize_template(s)) == normalize_template(s)` (即两次归一化结果与一次相同) — 覆盖 D-12 不变量 1
    - prop_normalize_template_literal_protection: 通过有限字符集策略 (例如 `inner in "[A-Za-z0-9 ]{0,50}"`) 构造 `WHERE col = '{inner}-- not a comment'` 形式的 SQL,验证 `normalize_template` 结果中字面量内 `-- not a comment` 序列保留 — 覆盖 D-12 不变量 2
    - 两条测试由 proptest 框架自动运行 (默认 256 cases),失败时框架自动 shrinking 到最小触发 case
  </behavior>
  <action>
    在 `src/pipeline/fingerprint.rs` 的 `#[cfg(test)] mod tests` 内部 (即第 326-435 行之间的 mod 块内,具体位置: 在 `} // mod tests 块尾 }` 之前) 追加一个 section divider 注释 `// --- proptest 属性测试 (TEST-04) ---` 后,新增以下内容:

    1. 在 `use super::*;` 下方 (第 327 行之后) 追加 `use proptest::prelude::*;` 引入 proptest 宏 (注意: 若已有 use 块,则 proptest::prelude 必须独立成行;若与 use super::* 同位置,按 Rust 习惯单独分行排列)。

    2. 追加 `proptest! { ... }` 宏块,内含两个 `#[test]` 函数:

    第一个: `fn prop_normalize_template_is_idempotent(s in any::<String>())` —— body 内调用 `let once = normalize_template(&s); let twice = normalize_template(&once); prop_assert_eq!(once, twice);`

    第二个: `fn prop_normalize_template_literal_protection(inner in "[A-Za-z0-9 ]{0,50}")` —— body 内 `let sql = format!("WHERE col = '{inner}-- not a comment'"); let result = normalize_template(&sql); prop_assert!(result.contains("-- not a comment"), "literal comment marker should survive in: {}", result);`

    严格规则:
    - 整体只用一个 `proptest! { ... }` 宏块包裹两个函数 (不要写两个独立的 `proptest!`)
    - **绝对不能在 `proptest!` 宏块外部再加 `#[test]` 注解** (Pitfall 1: 宏自动展开为 `#[test]`,外加会触发 clippy `unused_attributes` 警告)
    - 函数体内只能用 `prop_assert!` / `prop_assert_eq!` (不能用普通 `assert!`,因 proptest shrinking 需要错误返回值而非 panic)
    - 策略表达式使用 D-09 指定的 `any::<String>()` (幂等性)和 `"[A-Za-z0-9 ]{0,50}"` 正则字符串策略 (字面量保护);不引入 prop_compose 或自定义 Strategy
    - 测试函数名严格使用 `prop_normalize_template_is_idempotent` 和 `prop_normalize_template_literal_protection` (符合 PATTERNS.md 命名约定 `prop_{function}_{invariant}`)

    完成后验证: `cargo test --lib prop_normalize_template_` 至少运行 2 个属性测试且全部通过;`cargo clippy --all-targets -- -D warnings` 零警告 (重点关注 `unused_attributes` 不出现)。

    禁止: 在 proptest! 宏外加 #[test];使用 panic! 或 assert!;引入 prop_compose;覆盖现有 17 个单元测试 (`test_*` 函数);修改 `normalize_template` 函数本身。
  </action>
  <verify>
    <automated>grep -q 'use proptest::prelude::\*' src/pipeline/fingerprint.rs && grep -q 'prop_normalize_template_is_idempotent' src/pipeline/fingerprint.rs && grep -q 'prop_normalize_template_literal_protection' src/pipeline/fingerprint.rs && cargo test --lib prop_normalize_template_ 2>&1 | tail -10 | grep -E 'test result: ok\. (2|[3-9]+) passed' && cargo clippy --all-targets -- -D warnings 2>&1 | tail -5 | grep -qE '(Finished|Checking)' && ! cargo clippy --all-targets -- -D warnings 2>&1 | grep -q 'unused_attributes'</automated>
  </verify>
  <acceptance_criteria>
    - `src/pipeline/fingerprint.rs` 第 326 行 `mod tests` 块内含 `use proptest::prelude::*;`
    - `src/pipeline/fingerprint.rs` 含 `fn prop_normalize_template_is_idempotent` (在 proptest! 宏内)
    - `src/pipeline/fingerprint.rs` 含 `fn prop_normalize_template_literal_protection` (在 proptest! 宏内)
    - `proptest!` 宏块只出现 1 次 (两条测试同在一块内): `grep -c 'proptest! {' src/pipeline/fingerprint.rs` == 1
    - **不存在** `#[test]` 注解直接修饰任何 `prop_normalize_template_*` 函数: `grep -B1 'fn prop_normalize_template' src/pipeline/fingerprint.rs | grep -v '#\[test\]'`
    - `cargo test --lib prop_normalize_template_is_idempotent` 退出码 0
    - `cargo test --lib prop_normalize_template_literal_protection` 退出码 0
    - `cargo clippy --all-targets -- -D warnings` 退出码 0
    - `cargo clippy` 输出不含 `unused_attributes` 字样
    - 原有 17 个 fingerprint/normalize_template 单元测试仍全部通过 (`cargo test --lib fingerprint` 退出码 0)
  </acceptance_criteria>
  <done>两条 proptest 属性测试加入 fingerprint.rs mod tests,cargo test 通过,clippy 零警告,原有单元测试零回归</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| proptest 随机输入 → normalize_template | proptest `any::<String>()` 生成任意有效 UTF-8 字符串,作为函数输入边界 |
| crates.io → Cargo.lock | proptest 1.6.0 从 crates.io 拉取,需在 Cargo.lock 锁定版本 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-07 | Denial of Service | proptest 默认 256 cases × `any::<String>()` 任意长度字符串 → normalize_template 大量内存 | accept | normalize_template 实现 `Vec::with_capacity(sql.len())` 流式扫描,proptest 默认字符串长度受策略约束 (256 cases 实测 < 100ms) |
| T-20-08 | Tampering | proptest 生成包含非 UTF-8 字节的字符串导致 normalize_template panic | mitigate | proptest `any::<String>()` 只生成有效 UTF-8;scan_sql_bytes 末尾 `String::from_utf8(...).expect(...)` 在合法 UTF-8 输入下不会触发 |
| T-20-SC | Tampering | proptest 1.6.0 npm/pip/cargo 供应链 | mitigate | proptest 为 crates.io ~8 年老牌库 (~125M+ 总下载量, [VERIFIED: crates.io]),Cargo.lock 锁定版本;Task 1 通过 `cargo build --tests` 验证可编译 |
</threat_model>

<verification>
- `Cargo.toml` 含 `proptest = "1.6.0"` 一行,位于 `[dev-dependencies]` 段
- `Cargo.lock` 含 proptest 1.6.x
- `src/pipeline/fingerprint.rs` 含 `use proptest::prelude::*;` 和两条 prop_normalize_template_* 函数
- `cargo build --tests` 退出码 0
- `cargo test --lib prop_normalize_template_` 至少 2 个测试通过
- `cargo test --lib fingerprint` 原有测试零回归 (≥ 17 个)
- `cargo clippy --all-targets -- -D warnings` 零警告
- `cargo fmt --check` 通过
</verification>

<success_criteria>
TEST-04 完成: proptest 1.6.0 加入 dev-dependencies,`normalize_template` 拥有两条属性测试 (幂等性 + 字面量保护不变性),proptest 默认 256 cases 全部通过,clippy 零警告,原有 17 个单元测试零回归。
</success_criteria>

<output>
Create `.planning/phases/20-test-coverage/20-03-SUMMARY.md` when done
</output>
