---
phase: 53-cli
plan: 03
type: execute
wave: 3
depends_on:
  - 53-01
  - 53-02
files_modified:
  - src/config/validate.rs
  - src/stats/mod.rs
  - src/cli/init.rs
  - tests/integration.rs
autonomous: true
requirements:
  - STATS-11
must_haves:
  truths:
    - "sqllog2db validate -c <含 from=20240101 的 toml> 退出非零且 stderr 含 stats.from 与 YYYY-MM-DD 子串"
    - "sqllog2db validate -c <含 from=2024-01-01 to=2024-01-31> 退出 0 且 stdout 含 Configuration valid."
    - "sqllog2db stats -c <空 stats 节> 正常运行（与 v1.13 行为一致）"
    - "sqllog2db stats --from not-a-date 退出非零且 stderr 含错误提示与格式说明"
    - "sqllog2db init 生成的 config.toml 含被注释的 [stats] 节并展示 from / to / top 三字段示例与格式说明"
  artifacts:
    - path: "src/config/validate.rs"
      provides: "Config::validate 调用 stats.from / stats.to 的 validate_time_str"
      contains: "validate_time_str"
    - path: "src/stats/mod.rs"
      provides: "run_stats 开头对 cfg.stats.from / cfg.stats.to 防御性验证"
      contains: "validate_time_str"
    - path: "src/cli/init.rs"
      provides: "CONFIG_TEMPLATE_EN 追加 [stats] 注释段（含 from/to/top 示例）"
      contains: "[stats]"
    - path: "tests/integration.rs"
      provides: "端到端覆盖 CLI 与 config 合并、格式错误、init 模板"
      contains: "test_cli_stats_from"
  key_links:
    - from: "src/config/validate.rs::Config::validate"
      to: "src/stats/config.rs::validate_time_str"
      via: "use crate::stats::validate_time_str + 调用"
      pattern: "validate_time_str\\("
    - from: "src/stats/mod.rs::run_stats"
      to: "src/stats/config.rs::validate_time_str"
      via: "在 log_files 查询之前的防御性调用"
      pattern: "validate_time_str\\("
---

<objective>
完成 Phase 53 端到端集成验证：
1. **配置验证（D-08）**：扩展 `Config::validate` 调用 `validate_time_str` 检查 `cfg.stats.from` / `cfg.stats.to`，使 `sqllog2db validate -c config.toml` 能在时间格式不合法时报错。
2. **运行时防御（D-09）**：在 `run_stats` 入口对合并后的 `cfg.stats.from` / `cfg.stats.to` 再次调用 `validate_time_str`，确保 CLI 传入的非法格式被同样捕获。
3. **init 模板**：在 `CONFIG_TEMPLATE_EN` 追加 `[stats]` 注释段，向用户展示 from / to / top 三字段用法。
4. **端到端测试**：在 `tests/integration.rs` 新增至少 6 个集成测试覆盖 STATS-07/08/09/11 的 CLI 端到端行为（合并、格式错、init 模板包含、validate 命令）。

Purpose: 闭合 Phase 53 全部 5 条 ROADMAP Success Criteria；为 Phase 54（StatsAccumulator 时间过滤）提供经过格式验证、可信赖的 `cfg.stats.from` / `cfg.stats.to`。
Output: Phase 53 全部 Success Criteria 端到端可验证；用户可通过 init / validate / stats 三条路径完整使用时间段过滤配置。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/53-cli/53-CONTEXT.md
@.planning/phases/53-cli/53-01-PLAN.md
@.planning/phases/53-cli/53-02-PLAN.md
@.planning/REQUIREMENTS.md

@src/config/validate.rs
@src/stats/mod.rs
@src/cli/init.rs
@tests/integration.rs
@CLAUDE.md

<interfaces>
Plan 01 提供：
- `pub fn validate_time_str(s: &str) -> Result<(), String>` 位于 src/stats/config.rs
- 通过 src/stats/mod.rs 的 `pub use config::{StatsConfig, validate_time_str};` 暴露：可写 `crate::stats::validate_time_str` 或 `crate::stats::config::validate_time_str`
- Config 根结构新增字段 `pub stats: StatsConfig`（含 from / to / top 三 Option 字段）

Plan 02 提供：
- `pub fn handle_stats(cfg: &Config, top: Option<u32>, from: Option<String>, to: Option<String>) -> Result<()>` 已合并优先级，合并后值写入 merged_cfg.stats 并传递给 run_stats

既有接口（不变）：
- Config::validate 见 src/config/validate.rs 第 4-30 行（impl Config 块）
- run_stats 签名 `pub fn run_stats(cfg: &Config, top_n: u32) -> Result<()>` 见 src/stats/mod.rs 第 14 行
- ConfigError::InvalidValue 字段：field: String / value: String / reason: String（src/error/mod.rs）
- handle_validate 函数 src/cli/validate.rs 仅输出 println!("Configuration valid."); 不需修改
- main.rs 的 Validate 分支已通过 cfg.validate() 触发，并通过 format_validate_error 输出 [FAIL] + hint
- CONFIG_TEMPLATE_EN 位于 src/cli/init.rs 当前包含 [sqllog] / [logging] / [replace_parameters] / [filter] / [filter.include] / [filter.exclude] / [filter.indicators] / [filter.sql] / [output] / [exporter.csv] / [exporter.sqlite] 多个节
- tests/integration.rs 既有辅助函数 make_stats_config_file（第 1297 行）可复用
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Config::validate 与 run_stats 接入 validate_time_str（D-08 + D-09）</name>
  <files>src/config/validate.rs, src/stats/mod.rs</files>
  <read_first>
    - src/config/validate.rs（完整文件，重点 impl Config 的 validate 方法 4-30 行）
    - src/stats/mod.rs（完整文件，重点 run_stats 函数 14-26 行）
    - src/stats/config.rs（Plan 01 创建，确认 validate_time_str 签名）
    - .planning/phases/53-cli/53-CONTEXT.md §Decisions D-08、D-09
    - src/error/mod.rs（确认 ConfigError::InvalidValue 字段：field / value / reason）
  </read_first>
  <behavior>
    - Config::validate 在 cfg.stats.from = Some("not-a-date") 时返回 Err(Error::Config(ConfigError::InvalidValue { field: "stats.from", ... }))，错误信息含 YYYY-MM-DD 子串
    - Config::validate 在 cfg.stats.to = Some("20240101") 时返回类似错误，field 等于 "stats.to"
    - Config::validate 在 cfg.stats.from / to 为 None 时不调用 validate_time_str，返回 Ok
    - Config::validate 在 cfg.stats.from = Some("2024-01-01") 时返回 Ok
    - Config::validate 在 cfg.stats.to = Some("2024-01-31 23:59:59") 时返回 Ok
    - run_stats 在 cfg.stats.from / to 不合法（CLI 注入）时返回同样形态的 Err
    - run_stats 在 cfg.stats 全 None 时不返回该错误（log_files 流程不受影响）
    - 既有 config validate 测试（test_validate_default_config_passes 等约 20 个）零回归
    - 既有 run_stats 测试（test_run_stats_csv_mode_selected_when_only_csv_configured 等三个）零回归
  </behavior>
  <action>
    修改 src/config/validate.rs（Config::validate 方法）：

    第 1 步：在 Config::validate 函数体的 self.validate_output_fields()? 之后、Ok(()) 之前，调用一个新私有方法 self.validate_stats_time_fields()?。

    第 2 步：在同一 impl Config 块内新增私有方法 fn validate_stats_time_fields(&self) -> Result<()>：
    - 对 self.stats.from.as_deref() 和 self.stats.to.as_deref() 各调用一次 crate::stats::validate_time_str
    - 若返回 Err(reason_string)，构造 Error::Config(ConfigError::InvalidValue { field: "stats.from".to_string()（或 "stats.to"）, value: 原字符串.to_string(), reason: reason_string }) 并 return Err
    - 方法体保持在 40 行以内；如超出，抽出私有泛型函数 fn check_one_stats_time(field: &str, value: Option<&str>) -> Result<()>
    - 不修改既有 validate 与 validate_output_fields 函数体（除追加一行 self.validate_stats_time_fields()?）

    第 3 步：在测试模块末尾追加新单元测试（4 个）：
    - test_validate_rejects_invalid_stats_from：构造 cfg.stats.from = Some("not-a-date".into())，断言 cfg.validate().is_err() 且错误 Display 含 stats.from 子串与 YYYY-MM-DD 子串
    - test_validate_rejects_invalid_stats_to：类似，针对 stats.to = Some("20240101".into())
    - test_validate_accepts_valid_stats_time_strings：cfg.stats.from = Some("2024-01-01"), cfg.stats.to = Some("2024-01-31 23:59:59")，断言 cfg.validate().is_ok()
    - test_validate_accepts_none_stats_time：cfg.stats 默认（全 None），断言 cfg.validate().is_ok()

    修改 src/stats/mod.rs（run_stats 入口防御）：

    第 4 步：在 pub fn run_stats(cfg: &Config, top_n: u32) -> Result<()> 函数体的 debug_assert!(top_n >= 1, ...) 之后、let log_files = ... 之前，新增一段防御性验证：
    - 对 cfg.stats.from.as_deref() 与 cfg.stats.to.as_deref() 调用 crate::stats::config::validate_time_str（或经 pub use 后的 validate_time_str）
    - 错误构造同 Task 1.2 形态：Error::Config(ConfigError::InvalidValue { field, value, reason })
    - 可抽出 fn validate_cfg_stats_time(cfg: &Config) -> Result<()> 私有函数并在 run_stats 第一行调用，以遵守 40 行函数上限

    第 5 步：在 src/stats/mod.rs 的 #[cfg(test)] mod tests 末尾新增 2 个单元测试：
    - test_run_stats_rejects_invalid_from：构造 cfg.stats.from = Some("bad".into())，断言 run_stats(&cfg, 5).is_err() 且匹配 Error::Config(ConfigError::InvalidValue { field, .. }) 其中 field == "stats.from"
    - test_run_stats_rejects_invalid_to：类似针对 stats.to

    禁止：修改 validate_time_str 函数（Plan 01 锁定）；修改 ConfigError 枚举；引入 chrono / regex 等新依赖。
  </action>
  <verify>
    <automated>cargo test --lib config::validate:: && cargo test --lib stats:: && cargo clippy --all-targets -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    - grep 'validate_stats_time_fields' src/config/validate.rs 至少命中两行（声明 + 调用）
    - grep -E 'validate_time_str|validate_cfg_stats_time' src/stats/mod.rs 命中至少一行在 run_stats 函数体内
    - 新增的 6 个单元测试（config::validate 4 个 + stats:: 2 个）存在并通过
    - 既有 config::validate 测试与 stats:: 测试零回归（cargo test --lib config:: + cargo test --lib stats:: 全部通过）
    - cargo clippy --all-targets -- -D warnings 通过
    - 错误信息字符串可被外部 grep 命中：stats.from / stats.to / YYYY-MM-DD
  </acceptance_criteria>
  <done>D-08 + D-09 落地：validate 命令与 run_stats 入口均能拒绝非法时间格式；单元测试覆盖。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: init 模板追加 [stats] 注释段</name>
  <files>src/cli/init.rs</files>
  <read_first>
    - src/cli/init.rs（完整文件，重点 CONFIG_TEMPLATE_EN 常量从第 65 行至文件末）
    - tests/integration.rs（既有 init 相关测试，搜索 init 与 CONFIG_TEMPLATE，了解断言模式）
    - .planning/phases/53-cli/53-CONTEXT.md（D-04 中的 CLI 参数命名与文案约定，模板注释应与之一致）
  </read_first>
  <behavior>
    - sqllog2db init -o /tmp/new.toml --force 生成的文件中含子串 [stats]
    - 生成文件中含被注释的 from / to / top 三字段示例（每行以 # 开头）
    - 注释行包含 YYYY-MM-DD 与 YYYY-MM-DD HH:MM:SS 格式说明
    - 注释行包含 CLI overrides config 或同义说明（指明 CLI 优先级）
    - 生成的模板能通过 sqllog2db validate（因字段全为注释，不破坏既有验证）
  </behavior>
  <action>
    在 src/cli/init.rs::CONFIG_TEMPLATE_EN 常量中追加 [stats] 注释段：

    第 1 步：定位插入位置。建议在 [filter.sql] 之后、[output] 之前；如模板未含 [output] 显式区段，则插入到第一个 [exporter.*] 区块之前。读取文件后通过搜索字符串 [exporter] 或最末尾的 [filter.*] 精确定位。

    第 2 步：追加内容（保持注释风格，所有字段示例均以 # 开头，让用户按需启用，不为模板字段设置实际值；与 [filter.include] 区段的注释模式一致）：
    - 段落标题：# --- Stats subcommand time-range filter (optional) ---
    - 节标题：[stats]
    - 三行注释字段示例：
      - # from = "2024-01-01"   # Start of time range. Formats: "YYYY-MM-DD" or "YYYY-MM-DD HH:MM:SS"
      - # to   = "2024-01-31"   # End of time range. Same formats as from.
      - # top  = 20             # Default top-N. CLI --top overrides this value.
    - 一行说明 CLI 优先级：# CLI args --from / --to / --top override the values above. When both CLI and config are absent, stats runs without time filtering (top defaults to 20).

    第 3 步：不修改 handle_init 函数体或其他 use 语句；仅在 CONFIG_TEMPLATE_EN 字符串字面量内追加段落。

    第 4 步：不删除既有任何注释或区段。

    禁止：将 [stats] 字段设置为非注释值（会强制启用时间过滤）；移动既有区段；改变文件其他常量。
  </action>
  <verify>
    <automated>cargo build && ./target/debug/sqllog2db init -o /tmp/sqllog2db_init_test.toml --force && grep -E '^\[stats\]' /tmp/sqllog2db_init_test.toml && grep -E 'YYYY-MM-DD' /tmp/sqllog2db_init_test.toml && ./target/debug/sqllog2db validate -c /tmp/sqllog2db_init_test.toml</automated>
  </verify>
  <acceptance_criteria>
    - grep -E 监测：生成的临时 toml 文件包含 [stats] 行（行首），以及 # from、# to、# top 三个被注释字段行，以及 YYYY-MM-DD 与 YYYY-MM-DD HH:MM:SS 两个子串
    - 生成的 toml 通过 sqllog2db validate（exit 0）
    - cargo build 通过；cargo clippy --all-targets -- -D warnings 通过
    - CONFIG_TEMPLATE_EN 其他已有节（[sqllog] / [logging] / [filter] / [exporter.csv] / [exporter.sqlite] 等）保持原样（grep 验证仍存在）
  </acceptance_criteria>
  <done>init 模板含 [stats] 注释段，用户可见三字段示例与格式说明；模板仍通过 validate。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 端到端集成测试覆盖 Phase 53 全部 Success Criteria</name>
  <files>tests/integration.rs</files>
  <read_first>
    - tests/integration.rs 第 1292-1444 行（既有 S1-S6 stats CLI 测试与 make_stats_config_file 辅助函数）
    - tests/integration.rs 全文检索 init 与 CONFIG_TEMPLATE 子串，了解 init 端到端测试模式（如已有 init 模板内容断言）
    - .planning/ROADMAP.md §"Phase 53" 5 条 Success Criteria（验收依据）
    - .planning/phases/53-cli/53-CONTEXT.md §Decisions D-04~D-09
    - src/cli/stats/mod.rs（Plan 02 后状态：handle_stats 新签名 + log::info! 输出格式）
  </read_first>
  <behavior>
    - 6 个新集成测试全部通过，使用 assert_cmd::Command 与 predicates::str::contains
    - 既有 S1-S6 stats CLI 测试零回归
    - 既有 Phase 52 stats 输出测试零回归
    - cargo clippy --all-targets -- -D warnings 通过；cargo fmt --check 通过
    - cargo test --release 全量通过（含 lib 与 integration）
  </behavior>
  <action>
    在 tests/integration.rs 末尾（或紧随既有 stats 测试段落之后，约第 1445 行 "// ── Phase 52 stats output integration tests ──" 之前）新增以下 6 个集成测试，全部使用 assert_cmd::Command（参考既有 S1-S6 测试模式）。

    第 1 步：复用或扩展辅助函数。如需在 config.toml 中写入 [stats] 节的辅助函数，扩展 make_stats_config_file 为可选参数版本，或新增 make_stats_config_file_with_section 接受 from / to / top 三个 Option 参数（避免修改既有 6 个测试的调用方）。

    第 2 步：新增以下集成测试（每个 #[test] 独立函数）：

    test_cli_stats_help_shows_from_and_to：
    - 断言 sqllog2db stats --help 的 stdout 同时包含 --from、--to、YYYY-MM-DD 三个子串
    - 覆盖 ROADMAP Success Criteria #1

    test_cli_stats_with_cli_from_and_to_succeeds：
    - 准备 make_stats_config_file 生成的 cfg（无 [stats] 节）
    - 调用 sqllog2db stats -c <cfg> --from "2024-01-01" --to "2024-12-31"
    - 断言 exit 0
    - 读取生成的应用日志文件，断言 log_content 含子串 from=Some 与 to=Some（来自 Plan 02 的 log::info! 输出）
    - 覆盖 STATS-07 + ROADMAP Success Criteria #1

    test_cli_stats_validate_accepts_valid_config_stats_section：
    - 生成包含 [stats]\nfrom = "2024-01-01"\nto = "2024-01-31"\ntop = 10 的 config.toml
    - 调用 sqllog2db validate -c <cfg>
    - 断言 exit 0 且 stdout 含 Configuration valid.
    - 覆盖 STATS-08 + ROADMAP Success Criteria #2

    test_cli_stats_validate_rejects_bad_config_from_format：
    - 生成包含 from = "20240101" 的 config.toml
    - 调用 sqllog2db validate -c <cfg>
    - 断言 exit 非 0 且 stderr 含 [FAIL] 与 stats.from 与 YYYY-MM-DD 三个子串
    - 覆盖 STATS-11 + ROADMAP Success Criteria #4

    test_cli_stats_cli_overrides_config_from：
    - 生成 cfg 含 [stats]\nfrom = "2099-12-31"
    - 调用 sqllog2db stats -c <cfg> --from "2024-01-01"
    - 断言 exit 0 + 应用日志含 2024-01-01 子串（CLI 优先 → log::info! 输出合并值）
    - 覆盖 STATS-09 + ROADMAP Success Criteria #3

    test_cli_stats_runtime_rejects_bad_cli_from_format：
    - 准备 make_stats_config_file 生成的 cfg
    - 调用 sqllog2db stats -c <cfg> --from "not-a-date"
    - 断言 exit 非 0 且 stderr 含 stats.from 与 YYYY-MM-DD 子串（D-09 防御性验证）
    - 覆盖 STATS-11 + ROADMAP Success Criteria #4 CLI 路径

    test_init_template_contains_stats_section：
    - 调用 sqllog2db init -o <tempfile> --force
    - 断言 exit 0
    - 读取 tempfile 内容，断言含子串 [stats]、# from = "2024、# to、# top、YYYY-MM-DD HH:MM:SS
    - 覆盖 init 模板新增内容；与 Task 2 的 verify 形成 lib/integration 双层验证

    第 3 步：所有新测试不依赖外部网络、不引入新依赖；使用现有 tempfile / assert_cmd / predicates。

    禁止：删除或修改既有 S1-S6 stats 测试；引入新 dev-dependency；在测试中调用 std::process::exit。
  </action>
  <verify>
    <automated>cargo test --test integration -- stats && cargo test --test integration test_init_template_contains_stats_section && cargo test --release && cargo clippy --all-targets -- -D warnings && cargo fmt --check</automated>
  </verify>
  <acceptance_criteria>
    - 7 个新集成测试存在（grep test_cli_stats_help_shows_from_and_to / test_cli_stats_with_cli_from_and_to_succeeds / test_cli_stats_validate_accepts_valid_config_stats_section / test_cli_stats_validate_rejects_bad_config_from_format / test_cli_stats_cli_overrides_config_from / test_cli_stats_runtime_rejects_bad_cli_from_format / test_init_template_contains_stats_section）
    - cargo test --test integration -- stats 至少匹配 S1-S6 + Plan 03 的 6 个 stats 相关新增测试，全部通过
    - test_init_template_contains_stats_section 通过
    - cargo test --release（全量）通过：lib + integration + bench-related 单元测试
    - cargo clippy --all-targets -- -D warnings 零警告
    - cargo fmt --check 通过
  </acceptance_criteria>
  <done>Phase 53 全部 5 条 ROADMAP Success Criteria 端到端可验证；既有 Phase 51/52 测试零回归；三道质量门禁全通过。</done>
</task>

</tasks>

<verification>
- cargo build --release 通过
- cargo test 全量通过（lib + integration）
- cargo clippy --all-targets -- -D warnings 零警告
- cargo fmt --check 通过
- sqllog2db stats --from "not-a-date" 退出非零，stderr 含 stats.from 与 YYYY-MM-DD 子串
- sqllog2db validate -c <含 from=20240101> 退出非零，stderr 含 [FAIL] 与 stats.from
- sqllog2db validate -c <含 from=2024-01-01 to=2024-01-31> 退出 0
- sqllog2db init 生成的 config.toml 含 [stats] 注释段与三字段示例
- Phase 51/52 既有 8+ 个 stats 集成测试零回归
</verification>

<success_criteria>
- ROADMAP Phase 53 Success Criteria #1: stats --help 含 --from / --to → 测试 1 / 测试 2 覆盖
- ROADMAP Phase 53 Success Criteria #2: validate 通过含 from/to 的 config.toml → 测试 3 覆盖
- ROADMAP Phase 53 Success Criteria #3: CLI 优先于 config，二者均缺省时不过滤 → 测试 5 + Plan 02 既有 S1-S6 覆盖
- ROADMAP Phase 53 Success Criteria #4: 格式不合法明确错误 → 测试 4 + 测试 6 覆盖
- ROADMAP Phase 53 Success Criteria #5: clippy + test 全通过 → cargo clippy + cargo test 验证
- STATS-11 完整落地：Plan 01（validate_time_str）+ Plan 03（接入 validate 命令与 run_stats + init 模板 + 集成测试）
</success_criteria>

<output>
Create `.planning/phases/53-cli/53-03-SUMMARY.md` when done
</output>
