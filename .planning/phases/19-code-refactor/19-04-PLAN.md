---
phase: 19-code-refactor
plan: 04
type: execute
wave: 3
depends_on:
  - 19-01
  - 19-02
  - 19-03
files_modified:
  - src/cli/run.rs
  - src/cli/run/mod.rs
  - src/cli/run/processor.rs
  - src/cli/run/prescan.rs
  - src/cli/run/parallel.rs
  - src/cli/mod.rs
  - src/lib.rs
  - src/charts/mod.rs
  - src/color.rs
  - src/lang.rs
  - src/logging.rs
  - src/error.rs
  - src/parser.rs
  - src/resume.rs
autonomous: true
requirements:
  - REFACTOR-01
  - REFACTOR-04
tags:
  - rust
  - refactor
  - module-split
  - visibility
  - cli

must_haves:
  truths:
    - "src/cli/run.rs 已被替换为 src/cli/run/ 目录子模块（mod.rs + processor.rs + prescan.rs + parallel.rs）"
    - "外部调用 crate::cli::run::handle_run 保持不变（被 src/main.rs 调用）"
    - "src/cli/run/ 目录下每个 .rs 子文件 ≤ 300 行"
    - "src/lib.rs 中所有 `pub mod` / `pub use` 已按 D-10 评估并按需收紧为 `pub(crate)`（仅保留 tests/integration.rs 实际引用所需的项）"
    - "全 codebase 中无意义的 `pub` 已收紧（D-11），cargo clippy --all-targets -- -D warnings 零警告"
    - "cargo test 全部通过（lib + integration），cargo build --release 成功"
  artifacts:
    - path: "src/cli/run/mod.rs"
      provides: "handle_run 公开入口 + build_pipeline + FilterProcessor"
      contains: "fn handle_run"
      max_lines: 300
    - path: "src/cli/run/processor.rs"
      provides: "process_log_file 单文件热循环"
      contains: "fn process_log_file"
      max_lines: 300
    - path: "src/cli/run/prescan.rs"
      provides: "scan_log_file_for_matches + scan_for_trxids_by_transaction_filters + recompile_meta_if_needed"
      contains: "fn scan_log_file_for_matches"
      max_lines: 300
    - path: "src/cli/run/parallel.rs"
      provides: "process_csv_parallel + concat_csv_parts"
      contains: "fn process_csv_parallel"
      max_lines: 300
  key_links:
    - from: "src/main.rs"
      to: "src/cli/run/mod.rs"
      via: "cli::run::handle_run"
      pattern: "handle_run"
    - from: "src/cli/run/mod.rs"
      to: "processor.rs / prescan.rs / parallel.rs"
      via: "use processor::*; use prescan::*; use parallel::*;"
      pattern: "(use processor|use prescan|use parallel)"
    - from: "src/lib.rs"
      to: "modules"
      via: "pub(crate) mod / pub mod 按 integration test 需要"
      pattern: "(pub|pub\\(crate\\)) mod"
---

<objective>
1. 将 1281 行的 `src/cli/run.rs` 就地拆分为 `src/cli/run/` 目录子模块（D-03）
2. 完成 Phase 19 的最终全局可见性收紧（D-10/D-11）：扫描 src/ 全目录所有剩余 `pub` 项，按"binary crate 无跨 crate 暴露需求"原则收紧到 `pub(crate)` / `pub(super)` / 无 pub
3. lib.rs 的 `pub mod` / `pub use` 按 tests/integration.rs 实际引用情况精确收紧

Purpose:
- REFACTOR-01：run.rs 是最后一个超千行文件，依赖 Plan 01/02/03 的 pipeline/config/exporter 拆分稳定，因此排在 Wave 3
- REFACTOR-04：本 Plan 完成全 codebase 可见性扫描，是 Phase 19 验收闭环

Output: 重构后的 cli/run/ 子树，lib.rs 收紧，所有剩余 pub 项审计完成。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/19-code-refactor/19-CONTEXT.md
@.planning/phases/19-code-refactor/19-RESEARCH.md
@.planning/phases/19-code-refactor/19-PATTERNS.md
@src/cli/run.rs
@src/cli/mod.rs
@src/lib.rs
@src/main.rs
@tests/integration.rs

<interfaces>
<!-- 必须保持稳定的外部 API -->

外部消费者依赖路径：

```
crate::cli::run::handle_run              // src/main.rs, tests/integration.rs
```

handle_run 签名（src/cli/run.rs 第 685 行，**严禁修改**）：

```rust
pub fn handle_run(
    cfg: &Config,
    log_files: Vec<PathBuf>,
    jobs: usize,
    limit: Option<usize>,
    interrupted: Arc<AtomicBool>,
    lang: Lang,
) -> Result<()>
```

（实际签名以 src/cli/run.rs 第 685 行为准，逐字搬移至新 mod.rs）

run.rs 内部主要函数（拆分位置参考 PATTERNS.md "Architectural Responsibility Map"）：

```
process_log_file                          (lines 116-332)  → processor.rs  (~217 lines)
scan_log_file_for_matches                 (lines 334-401)  → prescan.rs    (~67 lines)
scan_for_trxids_by_transaction_filters    (lines 401-418)  → prescan.rs    (~17 lines)
concat_csv_parts                          (lines 423-478)  → parallel.rs   (~55 lines)
process_csv_parallel                      (lines 479-668)  → parallel.rs   (~189 lines)
recompile_meta_if_needed                  (lines 669-683)  → prescan.rs    (~14 lines)
handle_run + build_pipeline + FilterProcessor (lines 685-992) → mod.rs     (~307 lines, 接近临界)
#[cfg(test)] mod tests                    (lines 992+)     → 按测对象分到对应子文件
```

注意：handle_run 自身约 307 行 + FilterProcessor + build_pipeline 在 mod.rs 内，可能超 300。若 wc -l > 300，将 FilterProcessor 独立到 `src/cli/run/filter_processor.rs`。
</interfaces>

<visibility_rules>
本 Plan 是 Phase 19 收紧的最后一步，覆盖范围：

1. **cli/run/ 内部**：所有跨子模块共享的函数（process_log_file, scan_*, concat_*, recompile_*）一律 `pub(super)`（cli/run/ 内可见）；不暴露到 cli::run 外。

2. **handle_run**：保持 `pub`（src/main.rs 与 tests/integration.rs 都引用）

3. **src/lib.rs 全局扫描**：

   tests/integration.rs 实际引用路径（已 grep 确认）：
   ```
   dm_database_sqllog2db::cli::digest::{SortBy, handle_digest}
   dm_database_sqllog2db::cli::init::handle_init
   dm_database_sqllog2db::cli::run::handle_run
   dm_database_sqllog2db::cli::show_config::handle_show_config
   dm_database_sqllog2db::cli::stats::handle_stats
   dm_database_sqllog2db::cli::validate::handle_validate
   dm_database_sqllog2db::config::{...}
   dm_database_sqllog2db::config::Config
   dm_database_sqllog2db::lang::Lang
   dm_database_sqllog2db::pipeline::{FiltersFeature, NormalizeConfig}
   dm_database_sqllog2db::pipeline::filters::{IndicatorFilters, SqlFilters, ExcludeFilters, IncludeFilters}
   ```

   lib.rs 现有 `pub mod` 列表（11 项）：
   ```
   pub mod charts;      // ❓ grep 未在 integration test 命中 → 可改 pub(crate) mod
   pub mod cli;         // ✅ integration test 引用 cli::digest/init/run/show_config/stats/validate
   pub mod color;       // ❓ 检查
   pub mod config;      // ✅ integration test 引用
   pub mod error;       // ❓ 检查
   pub mod exporter;    // ❓ Plan 03 已评估
   pub mod lang;        // ✅ integration test 引用 lang::Lang
   pub mod logging;     // ❓ 检查
   pub mod parser;      // ❓ 检查
   pub mod pipeline;    // ✅ integration test 引用 pipeline 子项
   pub mod resume;      // ❓ 检查
   ```

   lib.rs `pub use exporter::{CsvExporter, Exporter, SqliteExporter}`：Plan 03 已处理。

   收紧决策树（每项 `pub mod` 用 grep 在 tests/integration.rs 中逐一验证）：
   ```
   grep -E "dm_database_sqllog2db::<modname>::" tests/integration.rs
   命中 → 保持 pub mod
   未命中 → 改为 pub(crate) mod
   ```

4. **各子模块内剩余的 pub 项**：

   逐文件扫描 grep 模式：
   ```
   grep -rnE "^\s*pub\s+(fn|struct|enum|trait|const|type)\b" src/
   ```

   对每条命中：
   - 同模块内访问 → 改 `pub(super)` 或去 pub
   - 跨模块（crate 内）访问 → 改 `pub(crate)`
   - tests/integration.rs 访问 → 保持 `pub`
   - 无访问者 → **删除该项**（dead code）

   预期收紧大量项（参考 RESEARCH.md "可见性收紧目标"表）：
   - `src/charts/mod.rs` 内 `pub fn render_*` 系列（charts 模块本身未在 integration test 命中 → 整模块 pub(crate)，函数 pub(super) 或 pub(crate)）
   - `src/error.rs` 中 `pub enum Error` 等 → integration test 是否引用？grep 验证
   - `src/parser.rs` 中 `pub struct SqllogParser` → grep 验证
   - `src/pipeline/mod.rs` 中各 `pub use` → 保留 integration test 实际用的子项（FiltersFeature, NormalizeConfig, filters 子模块），其他收紧

5. **dead_code 删除**：收紧后 clippy 若报 `dead_code` 警告，逐项分析：
   - 真未使用 → 删除该项
   - 是未来扩展点 → 评估是否真需要保留；优先删除（YAGNI），需要时未来 Phase 再加回

   **禁止** 通过 `#[allow(dead_code)]` 批量绕过。
</visibility_rules>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 拆分 cli/run.rs 为 processor.rs + prescan.rs + parallel.rs</name>
  <files>
    src/cli/run.rs (delete),
    src/cli/run/mod.rs (new),
    src/cli/run/processor.rs (new),
    src/cli/run/prescan.rs (new),
    src/cli/run/parallel.rs (new),
    src/cli/mod.rs
  </files>
  <read_first>
    src/cli/run.rs,
    src/cli/mod.rs,
    src/main.rs,
    .planning/phases/19-code-refactor/19-PATTERNS.md
  </read_first>
  <action>
    1) 删除 `src/cli/run.rs`。

    2) 新建 `src/cli/run/mod.rs`（从原 run.rs 第 1-115 行 + 685-992 行迁移）：
       - 文件顶部 `use` 区
       - 子模块声明：`mod processor; mod prescan; mod parallel;`
       - 跨子模块共享的 use（如 `use processor::process_log_file; use prescan::{scan_log_file_for_matches, scan_for_trxids_by_transaction_filters, recompile_meta_if_needed}; use parallel::{process_csv_parallel, concat_csv_parts};`）
       - 保留在 mod.rs：
         - `fn build_pipeline(cfg: &Config, compiled_meta: Option<CompiledMetaFilters>) -> Pipeline`（原行 5-115 区域）
         - `struct FilterProcessor { ... }` + `impl FilterProcessor` + `impl LogProcessor for FilterProcessor`
         - `pub fn handle_run(...)`（原签名严格保持）
         - 顶层 mod.rs 内 `#[cfg(test)] mod tests`（迁移测 build_pipeline / FilterProcessor / handle_run 集成的测试）
       - 行数估算：mod.rs ~280 行（接近临界 300）。若超 300，将 FilterProcessor 移到 `src/cli/run/filter_processor.rs`。

    3) 新建 `src/cli/run/processor.rs`：从原 run.rs 第 116-332 行迁移：
       - `use super::*` 或 explicit imports（按编译错误补齐）
       - `pub(super) fn process_log_file(...)`（参数列表严格保持，见 PATTERNS.md "Analog 函数签名模式"）
       - 私有辅助函数（若有）保持私有
       - `#[cfg(test)] mod tests`（迁移测 process_log_file 的测试）
       - 行数估算：~217 行业务 + 50 行测试 = ~267 行 ≤ 300

    4) 新建 `src/cli/run/prescan.rs`：从原 run.rs 第 334-418 行 + 669-683 行迁移：
       - `pub(super) fn scan_log_file_for_matches(file_path: &str, cfg: &Config) -> Vec<CompactString>`
       - `pub(super) fn scan_for_trxids_by_transaction_filters(log_files: &[PathBuf], cfg: &Config, jobs: usize) -> AHashSet<CompactString>`
       - `pub(super) fn recompile_meta_if_needed(...)`（签名按实际现状）
       - `#[cfg(test)] mod tests`
       - 行数估算：~100 行业务 + 30 行测试 = ~130 行

    5) 新建 `src/cli/run/parallel.rs`：从原 run.rs 第 423-668 行迁移：
       - `pub(super) fn concat_csv_parts(parts: &[(PathBuf, usize)], output_path: &Path, overwrite: bool, append_to_existing: bool) -> Result<()>`
       - `pub(super) fn process_csv_parallel(cfg: &Config, log_files: &[PathBuf], ...) -> Result<usize>`（签名按实际现状）
       - `#[cfg(test)] mod tests`
       - 行数估算：~245 行业务 + 50 行测试 = ~295 行 ≤ 300

    6) 测试迁移分类（原 992 行起的 `#[cfg(test)] mod tests`）：
       - 测 build_pipeline / FilterProcessor / handle_run → mod.rs 末尾
       - 测 process_log_file → processor.rs 末尾
       - 测 scan_log_file_for_matches / scan_for_trxids / recompile_meta → prescan.rs 末尾
       - 测 concat_csv_parts / process_csv_parallel → parallel.rs 末尾

    7) handle_run 可见性：保持 `pub`（main.rs + integration test 都引用）。
       process_log_file / scan_* / concat_* / process_csv_parallel / recompile_meta_if_needed：原文件内可能是 `fn` 私有，拆分后跨子模块共享，须改 `pub(super)`。

    8) 注意 Pitfall #1（write_record_preparsed 借用分裂）：本拆分**禁止修改 process_log_file 或 process_csv_parallel 中 itoa::Buffer / line_buf / writer 等参数的所有权与借用模式**——逐字搬移函数体。

    9) `cargo build && cargo test`（含 integration test）。
  </action>
  <verify>
    <automated>cargo build 2>&amp;1 | grep error | head -10; cargo test 2>&amp;1 | tail -10; wc -l src/cli/run/*.rs; ! test -f src/cli/run.rs &amp;&amp; echo "RUN_FILE_REMOVED"</automated>
  </verify>
  <acceptance_criteria>
    - src/cli/run.rs 不存在
    - src/cli/run/mod.rs 存在，包含 `pub fn handle_run`（保留原签名）
    - src/cli/run/processor.rs 存在，包含 `pub(super) fn process_log_file`
    - src/cli/run/prescan.rs 存在，包含 `pub(super) fn scan_log_file_for_matches`
    - src/cli/run/parallel.rs 存在，包含 `pub(super) fn process_csv_parallel` 和 `pub(super) fn concat_csv_parts`
    - `wc -l src/cli/run/*.rs` 每个文件 ≤ 300
    - `cargo build` 退出码 0
    - `cargo test` 退出码 0（含 tests/integration.rs 中 `cli::run::handle_run` 调用）
  </acceptance_criteria>
  <done>cli/run.rs 完整拆分，主编排逻辑跨子模块协作，所有测试通过。</done>
</task>

<task type="auto">
  <name>Task 2: 全局 pub 可见性扫描与收紧（D-10/D-11）</name>
  <files>
    src/lib.rs,
    src/charts/mod.rs,
    src/color.rs,
    src/lang.rs,
    src/logging.rs,
    src/error.rs,
    src/parser.rs,
    src/resume.rs
  </files>
  <read_first>
    src/lib.rs,
    tests/integration.rs,
    src/charts/mod.rs,
    src/color.rs,
    src/lang.rs,
    src/logging.rs,
    src/error.rs,
    src/parser.rs,
    src/resume.rs
  </read_first>
  <action>
    1) **第一步：build integration test 引用清单**

       运行：
       ```
       grep -oE "dm_database_sqllog2db::[a-zA-Z_:]+\b" tests/integration.rs | sort -u
       ```

       生成完整的"integration test 需要保持 pub 的项"清单。本 Task 的收紧动作不能破坏这份清单中的任何项。

    2) **第二步：lib.rs 模块收紧**

       逐项检查 `pub mod`：
       - `pub mod charts` → grep `dm_database_sqllog2db::charts::` → 若未命中 → 改 `pub(crate) mod charts`
       - `pub mod cli` → 命中 → 保持 `pub mod`
       - `pub mod color` → grep → 若未命中 → 改 `pub(crate) mod color`
       - `pub mod config` → 命中 → 保持 `pub mod`
       - `pub mod error` → grep → 多半未命中（integration test 用 `.unwrap()` 不接触 Error 类型） → 改 `pub(crate) mod error`
       - `pub mod exporter` → Plan 03 已评估，按 Plan 03 结果保留
       - `pub mod lang` → 命中 → 保持 `pub mod`
       - `pub mod logging` → grep → 若未命中 → 改 `pub(crate) mod logging`
       - `pub mod parser` → grep → 若未命中 → 改 `pub(crate) mod parser`
       - `pub mod pipeline` → 命中 → 保持 `pub mod`
       - `pub mod resume` → grep → 若未命中 → 改 `pub(crate) mod resume`

       注意：`pub(crate) mod` 只影响从外部 crate 视角的可见性。lib + bin 都是同 crate（sqllog2db 是 lib + bin），main.rs 通过 `crate::xxx` 访问模块仍然有效。

    3) **第三步：各模块顶层 pub 项收紧**

       对 lib.rs 中改为 `pub(crate) mod` 的模块（charts, color, error, logging, parser, resume），其内部所有 `pub` 项可以一并收紧：
       - `pub struct` / `pub fn` / `pub enum` → `pub(crate)` 即可（已经无外部 crate 访问可能）

       对保持 `pub mod` 的模块（cli, config, lang, pipeline）：grep 在 tests/integration.rs 中具体哪些子项被引用，仅这些保持 `pub`。

       具体扫描命令（按模块）：
       ```
       # 一次性列出剩余 pub 项
       grep -rnE "^\s*pub\s+(fn|struct|enum|trait|const|type|use)\b" src/ | grep -v "pub(crate)\|pub(super)"
       ```

       预期数量级：当前约 50-100 个 pub 项，收紧后应降至 20-30 个（仅 integration test 必需 + 极少数 main.rs 必需的 handle_*）。

    4) **第四步：dead code 处理**

       运行 `cargo clippy --all-targets -- -D warnings`，记录所有 `dead_code` warning。

       对每条 warning：
       - 该项确实未使用 → **删除**该项及相关代码
       - 该项是 trait 实现的必要接口 → 加 `#[allow(dead_code)]` 并注释原因
       - 是未来扩展点但当前无用 → 删除（YAGNI 原则）

       **禁止** 批量 `#[allow(dead_code)]`。

    5) **第五步：完整回归**

       - `cargo build`
       - `cargo build --release`（确保 release 构建也通过，因为 LTO 可能暴露 pub use 错误）
       - `cargo test`（lib + integration）
       - `cargo clippy --all-targets -- -D warnings`

    6) **第六步：基线对比**

       记录重构前后 pub 项数量：
       ```
       BEFORE=$(git show HEAD:src/lib.rs | grep -c "^pub")
       AFTER=$(grep -c "^pub" src/lib.rs)
       echo "lib.rs pub items: $BEFORE → $AFTER"
       ```

       预期：AFTER < BEFORE，且 AFTER 仅包含 integration test 必需的项 + handle_run 等。

    7) **若某模块整体 pub 数过多**（如 charts/mod.rs 仍有 10+ 个 pub fn）：评估是否拆分该模块——但若不在 D-01 的 5 个目标文件中，且未超 300 行，**不拆分**（D-01 严格限定范围）。
  </action>
  <verify>
    <automated>cargo build --release 2>&amp;1 | tail -5; cargo test 2>&amp;1 | tail -5; cargo clippy --all-targets -- -D warnings 2>&amp;1 | tail -5; grep -rnE "^\s*pub\s+(fn|struct|enum|trait)\b" src/ | grep -vc "pub(crate)\|pub(super)"</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build` 退出码 0
    - `cargo build --release` 退出码 0
    - `cargo test` 退出码 0（含 integration test）
    - `cargo clippy --all-targets -- -D warnings` 退出码 0
    - src/lib.rs 中 `pub mod charts / color / error / logging / parser / resume` 若 grep 未在 tests/integration.rs 命中 → 已改为 `pub(crate) mod`（grep `pub mod charts` 在 lib.rs 应仅命中 `pub(crate) mod charts`）
    - tests/integration.rs 中所有 `dm_database_sqllog2db::cli::* / config::* / lang::* / pipeline::*` 引用编译通过
    - 全 src/ 中剩余的 `^pub (fn|struct|enum)` 行数（未含 pub(crate)/pub(super) 的）应显著减少（基线 ≥50，收紧后 ≤ 30）
    - 无新增 `#[allow(dead_code)]` 用于批量绕过（grep `#\[allow\(dead_code\)\]` 数量不显著增加；单点保留需有注释说明）
  </acceptance_criteria>
  <done>全局可见性按 D-10/D-11 收紧，dead code 清理，clippy 零警告。</done>
</task>

<task type="auto">
  <name>Task 3: Phase 19 终验：行数审计 + 性能回归 + REFACTOR 需求闭环</name>
  <files>
    .planning/phases/19-code-refactor/19-04-SUMMARY.md (new)
  </files>
  <read_first>
    src/pipeline/filters/,
    src/config/,
    src/exporter/,
    src/cli/run/,
    src/lib.rs,
    .planning/REQUIREMENTS.md,
    .planning/ROADMAP.md
  </read_first>
  <action>
    1) **行数审计（REFACTOR-01 验收）**：

       运行：
       ```
       wc -l src/pipeline/filters/*.rs src/config/*.rs src/exporter/*.rs src/exporter/csv/*.rs src/exporter/sqlite/*.rs src/cli/run/*.rs
       ```

       验证每个新建子文件 ≤ 300 行（exporter/mod.rs 可放宽到 ≤ 600）。

       若仍有文件超 300 → 进一步拆分（不应到这步还有超线项，但作为兜底）。

    2) **共用层验证（REFACTOR-02 验收）**：

       ```
       test -f src/exporter/projection.rs && echo "PROJECTION_EXISTS"
       grep -c "projection::projected_field_names" src/exporter/sqlite/sql_builder.rs
       # 期望 ≥ 1
       grep -c "projection::projected_field_names" src/exporter/csv/writer.rs
       # 期望 == 0（热路径不调用）
       ```

       两处 sqlite 调用（build_insert_sql + build_create_sql）应均使用 projection，确认重复消除。

    3) **DryRunExporter 整合验证（REFACTOR-03 验收）**：

       ```
       grep -c "struct DryRunExporter" src/exporter/
       # 期望 == 0
       grep -c "DryRun { stats" src/exporter/mod.rs
       # 期望 ≥ 1
       ```

    4) **可见性收紧验证（REFACTOR-04 验收）**：

       ```
       BEFORE_PUB=$(git show HEAD~N:src/lib.rs | grep -c "^pub")  # N = Phase 18 末次 commit 距今 commit 数
       AFTER_PUB=$(grep -c "^pub" src/lib.rs)
       echo "lib.rs pub: $BEFORE_PUB → $AFTER_PUB"
       ```

       lib.rs pub 行数显著减少（≥30%）。

       全 src 范围：
       ```
       grep -rnE "^\s*pub\s+(fn|struct|enum)\b" src/ | grep -vE "pub\(crate\)|pub\(super\)" | wc -l
       ```

       记录数值，应远低于重构前。

    5) **性能回归检查**：

       若项目有 criterion bench（查看 `Cargo.toml` 中 `[[bench]]`）：
       ```
       cargo bench --bench <name> -- --quick
       ```
       对比 throughput；偏差 ≤ 5% 视为无回归。

       若无 bench 配置：跳过此项，仅依赖 `cargo build --release` 通过 + 现有测试通过。

    6) **CHANGELOG 与 SUMMARY 更新**：

       创建 `.planning/phases/19-code-refactor/19-04-SUMMARY.md`，列出：
       - 各 Plan 完成项（filters / config / exporter / cli/run 拆分情况）
       - 新建文件清单（13 个新文件，按 PATTERNS.md "File Classification" 表）
       - 删除文件清单（5 个原文件）
       - 可见性变更统计（pub 项数变化）
       - REFACTOR-01/02/03/04 验收证据（grep / wc 输出）
       - 性能基准对比（若有）
       - 遗留事项（如 stats.rs 1039 行未拆分——按 D-01 范围限定）

    7) **更新 .planning/REQUIREMENTS.md** 中 Traceability 表：

       将 REFACTOR-01/02/03/04 状态从 `Pending` 改为 `Complete`（仅在本 Phase 全部 Plan 完成时改）。

    8) **最终命令清单**（手动确认全部通过后才视为 Phase 19 完成）：

       ```
       cargo build
       cargo build --release
       cargo test
       cargo clippy --all-targets -- -D warnings
       cargo fmt --check  # 确保格式化无遗漏
       ```
  </action>
  <verify>
    <automated>cargo build --release 2>&amp;1 | tail -3; cargo test 2>&amp;1 | tail -3; cargo clippy --all-targets -- -D warnings 2>&amp;1 | tail -3; cargo fmt --check 2>&amp;1 | tail -3; wc -l src/pipeline/filters/*.rs src/config/*.rs src/exporter/csv/*.rs src/exporter/sqlite/*.rs src/cli/run/*.rs src/exporter/projection.rs 2>/dev/null | awk '$1 > 300 && $2 !~ /total/ {print "OVER_300:", $0}'</automated>
  </verify>
  <acceptance_criteria>
    - `wc -l` 输出中无任何 src/pipeline/filters/*.rs / src/config/{validate,apply_one,mod}.rs / src/exporter/csv/*.rs / src/exporter/sqlite/*.rs / src/cli/run/*.rs 文件 > 300 行（exporter/mod.rs 单独允许 ≤ 600）
    - `grep -c "projection::projected_field_names" src/exporter/sqlite/sql_builder.rs` ≥ 1
    - `grep -c "projection::projected_field_names" src/exporter/csv/writer.rs` == 0
    - `grep -c "struct DryRunExporter" src/exporter/` 路径下所有 .rs 文件输出 == 0
    - `grep -c "DryRun {" src/exporter/mod.rs` ≥ 1
    - `cargo build --release` 退出码 0
    - `cargo test` 退出码 0
    - `cargo clippy --all-targets -- -D warnings` 退出码 0
    - `cargo fmt --check` 退出码 0
    - .planning/phases/19-code-refactor/19-04-SUMMARY.md 存在，包含 REFACTOR-01/02/03/04 验收章节
    - .planning/REQUIREMENTS.md 中 REFACTOR-01/02/03/04 状态已更新为 Complete
  </acceptance_criteria>
  <done>Phase 19 全部 4 条需求闭环，所有验证通过，REQUIREMENTS.md 状态更新。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cli::run::handle_run → 文件系统 + rayon 并行池 | 主编排函数读取 .log 文件并写 CSV/SQLite；本 Plan 不改变 I/O 语义 |
| lib.rs pub use 收紧 | 收紧本身不影响行为，但 integration test 编译依赖正确路径 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-04-01 | Tampering | process_log_file 热循环借用关系 | mitigate | Task 1 明确禁止修改参数所有权与借用（itoa_buf, line_buf, writer 等）；逐字搬移函数体；现有测试是回归网 |
| T-19-04-02 | Tampering | process_csv_parallel rayon 池配置 | mitigate | 仅搬移文件位置，不改变 ThreadPoolBuilder / install 模式 |
| T-19-04-03 | Information Disclosure | dead_code 删除引入功能丢失 | mitigate | 仅删除 clippy 明确报 `dead_code` 的项；删除前 grep 全 codebase 确认无消费者；若是 trait method 默认实现必要项则 #[allow(dead_code)] 而非删除 |
| T-19-04-04 | Denial of Service | pub 收紧后 integration test 编译失败 | mitigate | Task 2 第 1 步先 grep 完整引用清单，再逐项收紧；遇编译错误立即恢复该项的 pub |
| T-19-04-SC | Tampering | npm/pip/cargo installs | N/A | 本 Plan 不引入新依赖，跳过包合法性门禁 |
</threat_model>

<verification>
- `cargo build` + `cargo build --release` 均通过
- `cargo test` 全部通过（lib 单元 + tests/integration.rs）
- `cargo clippy --all-targets -- -D warnings` 零警告
- `cargo fmt --check` 通过
- 所有 src/cli/run/ 子文件 ≤ 300 行
- `grep -c "struct DryRunExporter" src/exporter/` 输出 0
- `grep -c "projection::projected_field_names" src/exporter/sqlite/sql_builder.rs` ≥ 1
- lib.rs 中 `pub mod` 项数减少（charts/color/error/logging/parser/resume 收紧为 pub(crate) mod）
- 全 src/ 中无 `pub(crate)` / `pub(super)` 标记的 `pub` 项数较 Phase 19 起点显著减少
- 性能基准（若可用）偏差 ≤ 5%
</verification>

<success_criteria>
- src/cli/run.rs 完成拆分（REFACTOR-01 收尾）
- 全 codebase pub 可见性按 D-10/D-11 全面收紧（REFACTOR-04 收尾）
- Phase 19 5 条 ROADMAP Success Criteria 全部满足：
  1. 原 5 个超千行文件全部拆分，子文件行数 ≤ 300
  2. CSV/SQLite 字段投影合并到 projection.rs
  3. Exporter trait 冗余分支清理，DryRunExporter 整合
  4. pub 全面收紧为 pub(crate)/pub(super)，无意义 pub 已删除
  5. cargo clippy 零警告，cargo test 全通过，性能无回归
- REQUIREMENTS.md 中 REFACTOR-01/02/03/04 状态更新为 Complete
</success_criteria>

<output>
Create `.planning/phases/19-code-refactor/19-04-SUMMARY.md` when done
</output>
