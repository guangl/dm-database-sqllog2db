---
phase: 71-mod-rs-mod-rs-pub-use
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/config/mod.rs
  - src/config/error_log.rs
  - src/config/root.rs
  - src/config/tests.rs
autonomous: true
requirements: []
must_haves:
  truths:
    - "src/config/mod.rs 仅含 mod 声明与 pub use 重导出"
    - "Config / ErrorLogConfig / SqllogConfig / LoggingConfig / ExporterConfig / CsvExporterConfig / SqliteExporterConfig / StatsConfig / LOG_LEVELS 在 crate::config 路径下仍可访问"
    - "Config::from_file 公开方法与签名不变"
    - "cargo test 通过（含原 10 个 tests）"
    - "cargo clippy --all-targets -- -D warnings 通过"
  artifacts:
    - path: "src/config/mod.rs"
      provides: "module declarations + pub use only"
    - path: "src/config/error_log.rs"
      provides: "ErrorLogConfig struct + Deserialize"
      contains: "pub struct ErrorLogConfig"
    - path: "src/config/root.rs"
      provides: "Config struct + impl Config { from_file }"
      contains: "pub struct Config"
    - path: "src/config/tests.rs"
      provides: "原 10 个 unit tests"
      contains: "#[test]"
  key_links:
    - from: "src/config/mod.rs"
      to: "root / error_log"
      via: "mod 声明 + pub use"
      pattern: "pub use (root|error_log)"
    - from: "src/cli/run/mod.rs / src/cli/watch/mod.rs / main.rs"
      to: "crate::config::Config"
      via: "原路径不变"
      pattern: "use crate::config::Config"
---

<objective>
重构 src/config/mod.rs（194 行）：将 `ErrorLogConfig` 拆到 error_log.rs，`Config` struct + `impl Config { from_file }` 拆到 root.rs，#[cfg(test)] mod tests 拆到 tests.rs，mod.rs 仅保留 mod 声明、pub mod、pub use 重导出。

Purpose: config 顶层模块清晰化，子模块 sqllog/exporter/logging 保持 pub mod 暴露，Config 主结构独立成文件。
Output: 4 个文件，cargo test/clippy 全绿。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md

<interfaces>
<!-- 必须保持的公开 API（大量被外部使用） -->

公开导出（从外部访问 `crate::config::*`）：
- `Config` struct + `Config::from_file`（pub）
- `ErrorLogConfig` struct（pub）
- `SqllogConfig`（pub use from sqllog 子模块）
- `LoggingConfig`、`LOG_LEVELS`（pub use from logging）
- `ExporterConfig`、`CsvExporterConfig`、`SqliteExporterConfig`（pub use from exporter）
- `StatsConfig`（pub use from `crate::stats::config::StatsConfig`）

子模块（保持不变）：
- `pub mod exporter;`
- `pub mod logging;`
- `pub mod sqllog;`
- `mod validate;`（私有）

Config struct 字段（必须保留含序列化属性）：
```rust
#[derive(Debug, Deserialize, Clone, Default)]
pub struct Config {
    #[serde(default)] pub sqllog: SqllogConfig,
    #[serde(default)] pub logging: LoggingConfig,
    #[serde(default)] pub exporter: ExporterConfig,
    #[serde(default)] pub replace_parameters: Option<NormalizeConfig>,
    #[serde(default)] pub filter: Option<FiltersFeature>,
    #[serde(default)] pub output: Option<OutputConfig>,
    #[serde(default)] pub stats: StatsConfig,
    #[serde(default)] pub error: Option<ErrorLogConfig>,
    #[serde(skip)] pub append_error_log: bool,
}
```

Config::from_file 签名：
```rust
impl Config {
    pub fn from_file<P: AsRef<Path>>(path: P) -> Result<Self>;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 拆分 config/mod.rs 到 root.rs + error_log.rs + tests.rs</name>
  <files>src/config/mod.rs, src/config/error_log.rs, src/config/root.rs, src/config/tests.rs</files>
  <read_first>
    - src/config/mod.rs（全文 194 行）
    - src/config/exporter.rs（确认其 pub 类型与 mod.rs 的 pub use 一致）
    - src/config/sqllog.rs（确认 SqllogConfig pub）
    - src/config/logging.rs（确认 LoggingConfig / LOG_LEVELS pub）
    - src/main.rs（grep 'crate::config::' 验证 Config / from_file 使用）
    - src/stats/config.rs（确认 StatsConfig 在该路径，mod.rs 的 `pub use crate::stats::config::StatsConfig` 仍然合法）
  </read_first>
  <action>
    Step 1: 创建 `src/config/error_log.rs`：
      - 移入 mod.rs 第 17-21 行：
        ```rust
        use serde::Deserialize;

        /// error log 输出配置。
        #[derive(Debug, Deserialize, Clone)]
        pub struct ErrorLogConfig {
            pub file: String,
        }
        ```

    Step 2: 创建 `src/config/root.rs`：
      - 移入 mod.rs 第 23-63 行的：
        - `Config` struct 定义（含全部 serde 属性）
        - `impl Config { from_file }`
      - 顶部 use 完整复制：
        ```rust
        use super::error_log::ErrorLogConfig;
        use super::exporter::ExporterConfig;
        use super::logging::LoggingConfig;
        use super::sqllog::SqllogConfig;
        use crate::error::{ConfigError, Error, Result};
        use crate::pipeline::{FiltersFeature, NormalizeConfig, OutputConfig};
        use crate::stats::config::StatsConfig;
        use serde::Deserialize;
        use std::io;
        use std::path::Path;
        ```

    Step 3: 创建 `src/config/tests.rs`：
      - 移入 mod.rs 第 65-194 行 `#[cfg(test)] mod tests { ... }` 内的全部内容（不嵌套）
      - 顶部 use：
        ```rust
        use super::*;
        use crate::config::{Config, CsvExporterConfig, ExporterConfig, LoggingConfig, SqliteExporterConfig};
        ```
      - 保留全部 10 个 tests（test_exporter_config_has_any_csv / test_exporter_config_default_no_sqlite / test_from_file_not_found / test_from_file_valid_toml / test_from_file_invalid_toml_returns_error / test_default_logging_config_values / test_default_sqlite_exporter_values / test_csv_exporter_default_include_performance_metrics_true / test_csv_toml_default_include_performance_metrics / test_config_has_3_top_level_optional_fields / test_config_default_stats_all_none / test_config_parses_stats_section / test_config_missing_stats_section_defaults_to_none）

    Step 4: 改写 `src/config/mod.rs` 为：
      ```rust
      //! Config 模块：根 Config 结构、子模块（sqllog/exporter/logging）、validate。

      mod error_log;
      pub mod exporter;
      pub mod logging;
      mod root;
      pub mod sqllog;
      mod validate;

      #[cfg(test)]
      mod tests;

      pub use crate::stats::config::StatsConfig;
      pub use error_log::ErrorLogConfig;
      pub use exporter::{CsvExporterConfig, ExporterConfig, SqliteExporterConfig};
      pub use logging::{LOG_LEVELS, LoggingConfig};
      pub use root::Config;
      pub use sqllog::SqllogConfig;
      ```
      仅注释、mod 声明、pub use。

    Step 5: 运行质量门禁：
      - `cargo build`
      - `cargo clippy --all-targets -- -D warnings`
      - `cargo test`
      - `cargo fmt`

    重要：root.rs 中的 Config struct 必须保留全部 9 个字段的 serde 属性。
    验证手段：
      ```bash
      cargo run -- init -o /tmp/_71_test_config.toml --force
      cargo run -- validate -c /tmp/_71_test_config.toml
      ```
      两条命令均应退出码 0。
  </action>
  <verify>
    <automated>cargo clippy --all-targets -- -D warnings && cargo test</automated>
    <grep-check>grep -v '^\s*//' src/config/mod.rs | grep -vE '^\s*$' | grep -E '\bfn \w+|\bstruct \w+|\bimpl ' && echo "FAIL: mod.rs 仍含实现" || echo "OK: mod.rs 仅声明"</grep-check>
  </verify>
  <acceptance_criteria>
    - cargo clippy --all-targets -- -D warnings 退出码 0
    - cargo test 退出码 0
    - src/config/mod.rs 过滤注释/空行后只含 `mod`/`pub mod`/`pub use`/属性，无 `fn`/`struct`/`impl`
    - Config / ErrorLogConfig / SqllogConfig / LoggingConfig / ExporterConfig / CsvExporterConfig / SqliteExporterConfig / StatsConfig / LOG_LEVELS 在 crate::config 路径下可达
    - `cargo run -- init -o <tmpfile> --force` + `cargo run -- validate -c <tmpfile>` 端到端通过
    - 全部 13 个原 tests 全部迁移到 tests.rs
  </acceptance_criteria>
  <done>
    mod.rs 简化到 ≤ 25 行（含注释），error_log.rs + root.rs + tests.rs 承接全部实现与测试，质量门禁全绿。
  </done>
</task>

</tasks>

<verification>
- cargo clippy --all-targets -- -D warnings 通过
- cargo test 通过
- src/config/mod.rs grep -E '\bfn |\bstruct |\bimpl ' 无匹配
- cargo run -- init -o /tmp/_71_test.toml --force && cargo run -- validate -c /tmp/_71_test.toml 均成功
</verification>

<success_criteria>
- mod.rs 仅含 mod 声明与 pub use（≤ 25 行）
- Config struct + from_file 落地 root.rs
- ErrorLogConfig 落地 error_log.rs
- 13 个 tests 落地 tests.rs
- 全套质量门禁通过
</success_criteria>

<output>
Create `.planning/phases/71-mod-rs-mod-rs-pub-use/71-05-SUMMARY.md` when done
</output>
