---
phase: 03-doc-align
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - README.md
autonomous: true
requirements: [DOC-04]
tags: [docs, readme, watch, init-interactive, quiet-verbose]

must_haves:
  truths:
    - "README『功能特性 → 配置与性能』节的 CLI 条目列出 5 个命令（init/validate/run/stats/watch），不再是 4 个"
    - "README 功能特性区域新增一条『持续监听』条目，描述 watch 行为（目录监听、500ms 防抖、增量处理、Ctrl+C 摘要）"
    - "README 快速入门章节包含 watch 用法示例（`sqllog2db watch -c config.toml`）"
    - "README 快速入门章节包含 init --interactive 用法示例（`sqllog2db init --interactive`）"
    - "README 快速入门章节包含 --quiet / --verbose 进度选项说明 + 示例"
    - "新增段落使用与现有功能特性条目（粗体标题 + em dash + 中文描述）和快速入门（独立 bash fenced code block）一致的风格"
    - "现有非 watch 相关段落、解析与导出/过滤与字段控制/架构/性能/安装等章节保持不变"
  artifacts:
    - path: "README.md"
      provides: "DOC-04 三项更新（CLI 五命令 + 持续监听条目 + 快速入门三段）"
      contains: "持续监听"
    - path: "README.md"
      provides: "watch 快速入门示例"
      contains: "sqllog2db watch -c config.toml"
    - path: "README.md"
      provides: "init --interactive 示例"
      contains: "sqllog2db init --interactive"
    - path: "README.md"
      provides: "--quiet / --verbose 全局选项段落"
      contains: "--quiet"
  key_links:
    - from: "README 功能特性章节"
      to: "watch 子命令"
      via: "新增『持续监听』条目"
      pattern: "持续监听"
    - from: "README 快速入门"
      to: "三个新增示例段落"
      via: "独立 bash fenced code block"
      pattern: "sqllog2db watch -c config.toml"
---

<objective>
按 D-05/D-06/D-07 更新 `README.md`，让用户从 README 即可看到 `watch` 子命令、`init --interactive` 向导、`--quiet`/`--verbose` 进度选项，与 v1.18 + v1.19 实际能力对齐。

Purpose: README 是新用户最先看到的入口；当前 README 完全没有 watch / init 向导 / 全局静默与详情选项，造成"功能存在但用户不知道"的认知断层。本计划补全这部分文档，对齐 DOC-04 要求。
Output: `README.md` 三处更新（CLI 条目从 4 命令→5 命令、新增"持续监听"功能特性条目、快速入门追加三段示例），保留原有结构与 Markdown 风格，无任何代码改动。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-doc-align/03-CONTEXT.md
@.planning/phases/03-doc-align/03-RESEARCH.md
@.planning/phases/03-doc-align/03-PATTERNS.md
@README.md

<interfaces>
<!-- README.md 现状关键锚点（executor 直接对照定位） -->

锚点 1（D-05 修改点）— README.md 第 41 行：
```markdown
- **简洁的 CLI**：`init`（生成配置）、`validate`（校验）、`run`（执行导出）、`stats`（统计分析）四个命令。
```
目标：扩展为 5 个命令，加入 `watch`（持续监听）。

锚点 2（D-06 插入点）— README.md "配置与性能" 章节（第 35 行起，第 41 行"简洁的 CLI"条目所在节）末尾：
当前末尾是"简洁的 CLI"条目（第 41 行），下一节是 `## 架构`（第 43 行）。新条目应紧跟"简洁的 CLI"条目之后、`## 架构`标题之前。

锚点 3（D-07 插入点）— README.md "快速入门" 章节（第 105-129 行）。当前结构：
- 第 107-113 行：基础三命令示例（init/validate/run）
- 第 115-120 行：stats 基础示例
- 第 122-127 行：stats 时间范围示例
- 第 129 行：`详细用法参见[快速入门指南](./docs/quickstart.md)。`

新增 3 段必须插入在第 127 行之后、第 129 行"详细用法参见"链接之前。
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: D-05 + D-06 — 更新 CLI 条目为 5 命令并新增"持续监听"功能特性条目</name>
  <files>README.md</files>
  <read_first>
    - README.md（整个文件，确认锚点行号与上下文。文件较长时优先读前 160 行覆盖"功能特性"与"快速入门"两节）
    - .planning/phases/03-doc-align/03-CONTEXT.md（决策 D-05、D-06）
    - .planning/phases/03-doc-align/03-RESEARCH.md（§ Architecture Patterns / README 条目格式）
    - .planning/phases/03-doc-align/03-PATTERNS.md（§ `README.md` — watch/init --interactive/... 章节）
  </read_first>
  <action>
    一、定位 README.md 第 41 行：
    ```
    - **简洁的 CLI**：`init`（生成配置）、`validate`（校验）、`run`（执行导出）、`stats`（统计分析）四个命令。
    ```
    把这一整行**完整替换**为：
    ```
    - **简洁的 CLI**：`init`（生成配置）、`validate`（校验）、`run`（执行导出）、`stats`（统计分析）、`watch`（持续监听）五个命令。
    ```
    要求：保留行首 `- **简洁的 CLI**：` 前缀格式；命令顺序为 init / validate / run / stats / watch（D-05 文本顺序）；句末"五个命令。"中文句号必须使用中文全角句号 `。`，与原行一致；不要改成"5 个命令"。

    二、在该行（"简洁的 CLI"条目）**之后、`## 架构` 标题之前**新增一行作为"持续监听"功能特性条目：
    ```
    - **持续监听**：`watch` 子命令监听配置目录下的新 `.log` 文件，500ms 防抖后自动触发增量处理，Ctrl+C 退出并打印本次运行摘要（处理次数、总行数、运行时长）。
    ```
    要求：使用与"简洁的 CLI"完全一致的 `- **标题**：描述。` 格式；标题用中文"持续监听"；描述中提到的四个关键能力（目录监听 / 500ms 防抖 / 增量处理 / Ctrl+C 摘要）必须全部出现（D-06 要求）；句末用中文句号 `。`。

    三、不要在本任务中修改"快速入门"章节、`## 架构` 章节、解析与导出 / 过滤与字段控制 节、徽章、安装说明。

    四、不修改任何代码文件，本任务纯 Markdown 编辑。
  </action>
  <verify>
    <automated>grep -nF '`init`（生成配置）、`validate`（校验）、`run`（执行导出）、`stats`（统计分析）、`watch`（持续监听）五个命令。' README.md && grep -nF '- **持续监听**：`watch` 子命令监听配置目录下的新 `.log` 文件，500ms 防抖后自动触发增量处理，Ctrl+C 退出并打印本次运行摘要' README.md && ! grep -F 'init`（生成配置）、`validate`（校验）、`run`（执行导出）、`stats`（统计分析）四个命令' README.md && [ "$(grep -cF '- **持续监听**' README.md)" -eq 1 ]</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F '`init`（生成配置）、`validate`（校验）、`run`（执行导出）、`stats`（统计分析）、`watch`（持续监听）五个命令。' README.md` 返回恰好 1 行（D-05 替换完成）
    - `grep -F '四个命令' README.md` 不再匹配旧表述（原 4 命令行已替换）
    - `grep -c '- \*\*持续监听\*\*' README.md` 返回 `1`（D-06 条目恰好新增一处）
    - "持续监听"条目所在行紧跟"简洁的 CLI"条目之后，且位于 `## 架构` 标题之前（可通过 `grep -n` 行号对比验证）
    - "持续监听"条目同时包含字面子串 `500ms 防抖`、`增量处理`、`Ctrl+C`、`摘要` 四个关键词
    - README.md 中 `git diff` 仅触及第 41 行附近 2 行变化；解析与导出 / 过滤与字段控制 / 架构 / 安装 / 性能 章节文本完全未动
    - 无任何源代码或 Cargo.toml 文件被修改（本任务纯 Markdown）
  </acceptance_criteria>
  <done>
    README 第 41 行 CLI 条目升级为 5 个命令；同节末尾新增"持续监听"条目，包含目录监听 / 500ms 防抖 / 增量处理 / Ctrl+C 摘要四要素；其余章节文本未变。
  </done>
</task>

<task type="auto">
  <name>Task 2: D-07 — 快速入门追加 watch / init --interactive / quiet+verbose 三段示例并执行质量门禁</name>
  <files>README.md</files>
  <read_first>
    - README.md（重点确认 Task 1 提交后第 105-129 行"快速入门"区域形态；定位最后一个 stats 示例代码块结尾与"详细用法参见"链接位置）
    - .planning/phases/03-doc-align/03-CONTEXT.md（决策 D-07）
    - .planning/phases/03-doc-align/03-PATTERNS.md（§ `README.md` D-07 快速入门段落格式样例）
    - CLAUDE.md（质量门禁命令）
  </read_first>
  <action>
    一、在 README.md "快速入门" 章节最后一个 stats 时间范围示例代码块（当前第 127 行 ` ``` ` 结束）之后、"详细用法参见[快速入门指南](./docs/quickstart.md)。"（当前第 129 行）之前，按顺序插入以下三段。**每段格式为：1 行说明 + 1 个空行 + 一个独立的 bash fenced code block + 1 个空行**，与现有 stats 示例的版式严格一致：

    段落 1（watch 用法）：
    ```
    持续监听新 `.log` 文件（按 Ctrl+C 停止并打印摘要）：

    ```bash
    sqllog2db watch -c config.toml
    ```
    ```

    段落 2（init --interactive 向导）：
    ```
    交互式向导生成配置文件（每步显示示例值与默认值，回车接受默认值）：

    ```bash
    sqllog2db init --interactive
    ```
    ```

    段落 3（--quiet / --verbose 全局选项）：
    ```
    进度输出控制：`-q`/`--quiet` 抑制非错误输出（适合后台/定时任务），`-v`/`--verbose` 显示每文件详情：

    ```bash
    sqllog2db run -c config.toml --quiet
    sqllog2db run -c config.toml --verbose
    ```
    ```

    要求：
    - 中文标点全部使用全角符号（`：`、`，`、`。`），与现有 README 风格一致
    - 三段顺序固定为 watch → init --interactive → quiet/verbose（与 D-07 列表顺序一致）
    - 三段之间各空 1 行；最后一段代码块结束后空 1 行再接"详细用法参见…"
    - bash 代码块语言标识统一用 `bash`（与上方 stats 示例一致）
    - 不要给段落起 `### 小标题`；直接以中文说明句作为引导，与现有 stats 示例风格对齐
    - 不要在本任务回退 Task 1 对第 41 行附近的修改

    二、不修改任何 Rust 源文件、Cargo.toml、配置文件或 .planning 目录文件。

    三、执行质量门禁（per user memory `feedback_post_op_checks.md`，纯文档修改也跑一遍兜底）：
    1. `cargo fmt --check`
    2. `cargo clippy --all-targets -- -D warnings`
    3. `cargo test`
    全部必须绿灯；任一失败需排查原因（理论上 README 改动不应影响构建/测试）。

    四、提交 `docs(03-02): readme covers watch, init --interactive, quiet/verbose options`（conventional commit 格式）。
  </action>
  <verify>
    <automated>grep -nF 'sqllog2db watch -c config.toml' README.md && grep -nF 'sqllog2db init --interactive' README.md && grep -nF 'sqllog2db run -c config.toml --quiet' README.md && grep -nF 'sqllog2db run -c config.toml --verbose' README.md && grep -nF '持续监听新 `.log` 文件' README.md && grep -nF '交互式向导生成配置文件' README.md && grep -nF '进度输出控制' README.md && cargo fmt --check && cargo clippy --all-targets -- -D warnings 2>&1 | tee /tmp/03-02-task2-clippy.log && cargo test 2>&1 | tee /tmp/03-02-task2-test.log && grep -E "test result: ok\." /tmp/03-02-task2-test.log && git log -1 --pretty=%s | grep -E '^docs\(03-02\):'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F 'sqllog2db watch -c config.toml' README.md` 至少返回 1 行（watch 快速入门示例）
    - `grep -F 'sqllog2db init --interactive' README.md` 返回 1 行（init 向导示例）
    - `grep -F 'sqllog2db run -c config.toml --quiet' README.md` 返回 1 行
    - `grep -F 'sqllog2db run -c config.toml --verbose' README.md` 返回 1 行
    - `grep -F '持续监听新 ` + 反引号 + `.log` + 反引号 + ` 文件' README.md` 返回 1 行（watch 段落说明句）
    - `grep -F '交互式向导生成配置文件' README.md` 返回 1 行（init 段落说明句）
    - `grep -F '进度输出控制' README.md` 返回 1 行（quiet/verbose 段落说明句）
    - 三个新增 bash 代码块均位于 `## 快速入门` 标题之下、"详细用法参见[快速入门指南](./docs/quickstart.md)。" 之前（可通过 `grep -n` 行号顺序确认）
    - `cargo fmt --check` 退出码 0
    - `cargo clippy --all-targets -- -D warnings` 退出码 0，stderr 无 `warning:` 行
    - `cargo test` 退出码 0，输出含 `test result: ok.`
    - 最近一次提交主题以 `docs(03-02):` 开头
    - `git diff main -- README.md` 仅显示功能特性与快速入门两个区段的新增/替换，无对架构/性能/安装/徽章节的改动
    - 全程未修改 src/、Cargo.toml、.planning/ 之外任何文件
  </acceptance_criteria>
  <done>
    README 快速入门含 watch / init --interactive / quiet+verbose 三段示例；三道质量门禁全绿；提交以 `docs(03-02):` 开头。DOC-04 三项子需求全部覆盖。
  </done>
</task>

</tasks>

<verification>
全局检查（任一不满足则计划失败）：
1. `grep -c '五个命令。' README.md` ≥ 1 且 `grep -c '四个命令' README.md` == 0
2. `grep -c '- \*\*持续监听\*\*' README.md` == 1（恰好新增 1 处）
3. README 快速入门同时包含 4 个 watch/init/quiet/verbose 命令字面量
4. `cargo fmt --check && cargo clippy --all-targets -- -D warnings && cargo test` 全绿
5. `git diff main -- README.md` 改动局限于功能特性与快速入门两个区段
</verification>

<success_criteria>
DOC-04 完成：README 功能特性列出 5 命令并描述 watch 行为，快速入门含 watch / init --interactive / quiet+verbose 三段示例。三道质量门禁全绿。提交以 conventional commit 格式入库。
</success_criteria>

<output>
执行结束后创建 `.planning/phases/03-doc-align/03-02-SUMMARY.md`，记录：
- tasks_completed、files_modified（仅 README.md）、commits（SHA + 主题）
- self-check: PASSED/FAILED 列表（对照 must_haves.truths 逐条）
- README diff 摘要（功能特性、快速入门两段改动范围）
- requirements-completed: [DOC-04]
- 与原 PLAN 的差异（如有）
</output>
