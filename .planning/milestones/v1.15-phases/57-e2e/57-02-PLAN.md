---
phase: 57-e2e
plan: 02
type: execute
wave: 2
depends_on:
  - 57-01
files_modified:
  - tests/integration.rs
autonomous: true
requirements:
  - TEST-01
  - TEST-02
user_setup: []

must_haves:
  truths:
    - "run 子命令 CLI 在给定真实 .log 输入与 CSV 配置时退出 0，并在指定路径生成 CSV 文件，文件第一行精确等于 FIELD_NAMES 序列（ts,ep,sess_id,thrd_id,username,trx_id,statement,appname,client_ip,tag,sql,exec_time_ms,row_count,exec_id,normalized_sql），数据行数等于写入日志的记录数"
    - "run 子命令 CLI 在给定真实 .log 输入与 SQLite 配置时退出 0，并在指定路径生成 .db 文件；用 rusqlite::Connection 打开后查询 sqllog_records 表的 COUNT(*) 等于写入日志的记录数"
    - "init 子命令 CLI 在给定不存在的输出路径时退出 0，文件被创建（包含 [sqllog] 段）"
    - "init 子命令 CLI 在输出路径已存在文件且未传 --force 时退出非零，stderr 包含 'already exists' 字面"
  artifacts:
    - path: "tests/integration.rs"
      provides: "新增 2 个 run helper (CSV/SQLite) + 4 个 e2e 测试 (run CSV / run SQLite / init success / init existing-no-force)"
      contains: "fn write_run_config_toml"
  key_links:
    - from: "tests/integration.rs:test_cli_run_csv_output_header_and_row_count"
      to: "src/pipeline/mod.rs:FIELD_NAMES"
      via: "CSV 第一行硬编码字符串与 FIELD_NAMES 逗号拼接顺序逐字相等"
      pattern: "ts,ep,sess_id,thrd_id,username,trx_id,statement,appname,client_ip,tag,sql,exec_time_ms,row_count,exec_id,normalized_sql"
    - from: "tests/integration.rs:test_cli_run_sqlite_output_row_count"
      to: "src/config/mod.rs:SqliteExporterConfig::default().table_name"
      via: "SELECT COUNT(*) FROM sqllog_records"
      pattern: "FROM sqllog_records"
    - from: "tests/integration.rs:test_cli_init_existing_file_without_force_exits_nonzero"
      to: "src/error.rs:FileError::AlreadyExists"
      via: "stderr 包含 FileError::AlreadyExists 的 Display 文案 'File already exists:'"
      pattern: "already exists"
---

<objective>
为 run 子命令和 init 子命令补全 CLI 全链路 assert_cmd 测试，作为 Phase 58 cli/run 函数清理重构的安全网。

Purpose: TEST-01 与 TEST-02 在 ROADMAP 中要求 run（CSV + SQLite）和 init（成功 + 已存在）四条 e2e 路径有 assert_cmd 测试覆盖，但 tests/integration.rs 当前仅包含 stats CLI 与 run 的 handle_run 单元测试，缺少 run/init 直接通过 cargo_bin 二进制启动的端到端断言。本 plan 仅在 tests/integration.rs 末尾追加测试与辅助函数，零生产代码改动，零新增依赖。

Output:
- tests/integration.rs 新增 2 个 helper 函数：`write_run_config_toml` (CSV) 与 `write_run_sqlite_config_toml` (SQLite)，仿照 make_stats_csv_config 风格但 inputs 字段指向目录而非文件
- 新增 4 个 e2e 测试：
  * test_cli_run_csv_output_header_and_row_count (TEST-01)
  * test_cli_run_sqlite_output_row_count (TEST-01)
  * test_cli_init_creates_file_exit_0 (TEST-02)
  * test_cli_init_existing_file_without_force_exits_nonzero (TEST-02)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/57-e2e/57-CONTEXT.md
@.planning/phases/57-e2e/57-RESEARCH.md
@.planning/phases/57-e2e/57-PATTERNS.md
@.planning/phases/57-e2e/57-01-SUMMARY.md

<interfaces>
<!-- 关键签名与字面字符串：执行者直接引用 -->

CSV header 完整字符串（来自 src/pipeline/mod.rs:11–27 FIELD_NAMES，必须逐字精确匹配，per D-05）：
```
ts,ep,sess_id,thrd_id,username,trx_id,statement,appname,client_ip,tag,sql,exec_time_ms,row_count,exec_id,normalized_sql
```

SQLite 默认表名（来自 src/config/mod.rs SqliteExporterConfig::default().table_name）：
```
sqllog_records
```
注意：CONTEXT.md D-07 写的是 `sqllog`，但代码实际默认值是 `sqllog_records`（已由 PATTERNS.md "Critical Notes for Planner" 修正）。SQLite 测试必须使用 sqllog_records。

init 文件已存在错误信息（来自 src/error.rs:197 FileError::AlreadyExists 的 #[error] 文案）：
```
File already exists: {path} (set overwrite=true to replace)
```
stderr 断言子串候选：`"already exists"`（稳定子串，已在 src/cli/init.rs:15 的 error! log 中复现：`"Configuration file already exists: {output_path}"`）

已有可复用 helper（无需改动，直接调用）：
- tests/integration.rs:16 — `write_test_log(path: &Path, count: usize)` 生成 count 条达梦格式日志行（per D-06）
- tests/integration.rs:1464 — `make_stats_csv_config(dir, log_path)` 是 write_run_config_toml 的风格模板（结构一致，inputs 改为目录路径）
- tests/integration.rs:1477 — `make_stats_sqlite_config(dir, log_path)` 是 write_run_sqlite_config_toml 的风格模板

已有 assert_cmd 风格参考：
- tests/integration.rs:1501 — `test_stats_csv_outputs_two_files` 的 Command 链式调用风格
- tests/integration.rs:1838 — `test_init_template_contains_stats_section` 的 init assert_cmd 风格（注意：该测试用 --force，本 plan 的 test_cli_init_creates_file_exit_0 不用 --force，依赖 path 不存在）

从 src/main.rs 退出码常量（仅用于理解，无需在测试中断言具体码值）：
- EXIT_PARTIAL=1, EXIT_FATAL=2；本 plan 仅用 .failure() 判断非零退出（不依赖具体码值，符合 Deferred Ideas 范围）
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 新增 write_run_config_toml 与 write_run_sqlite_config_toml 两个测试 helper</name>
  <files>tests/integration.rs</files>
  <read_first>
    - tests/integration.rs（必读第 1449–1490 行：write_stats_test_log + make_stats_csv_config + make_stats_sqlite_config 三个 helper 的完整签名与实现，本任务两个新 helper 的风格模板；必读第 16–29 行 write_test_log helper 签名——后续任务复用；必读第 1938–1940 行：文件末尾结构，新 helper 应追加到末尾、test 函数之前的合适位置，建议紧贴 make_stats_sqlite_config 之后或文件末尾另起一段，但必须放在所有调用方测试之前）
    - .planning/phases/57-e2e/57-PATTERNS.md（必读 "辅助函数模式：write_run_config_toml" 段落与 "SQLite 表名确认 (Pitfall 1 解决方案)" 段落）
    - src/pipeline/mod.rs（必读第 11–27 行 FIELD_NAMES：理解 inputs 是目录还是文件——SqllogParser 扫描的是目录路径，因此 inputs 写目录字符串）
  </read_first>
  <action>
    在 tests/integration.rs 文件末尾（第 1940 行最后一个 `}` 之后）开辟新的 "Phase 57 e2e helpers" 段落注释（`// ── Phase 57 e2e helpers (TEST-01 / TEST-02) ──`），紧随其下追加两个 helper 函数：

    1. `write_run_config_toml(dir: &std::path::Path, log_dir: &std::path::Path, csv_output: &std::path::Path) -> std::path::PathBuf`
       - 文件名固定 "run_config.toml" 写入到 dir 下
       - TOML 内容用 `format!` 拼接两段：
         * `[sqllog]\ninputs = ["{}"]\n` —— 第一个占位符填 `log_dir.to_string_lossy().replace('\\', "/")`（per Pitfall 4，Windows 路径反斜杠转正斜杠）
         * `[exporter.csv]\nfile = "{}"\noverwrite = true\n` —— 占位符填 `csv_output.to_string_lossy().replace('\\', "/")`
       - 用 `std::fs::write(&cfg_path, content).unwrap();` 写入；返回 `cfg_path`

    2. `write_run_sqlite_config_toml(dir: &std::path::Path, log_dir: &std::path::Path, db_output: &std::path::Path) -> std::path::PathBuf`
       - 文件名固定 "run_sqlite_config.toml" 写入到 dir 下
       - TOML 内容拼接：
         * `[sqllog]\ninputs = ["{}"]\n` —— 同上 log_dir 字符串处理
         * `[exporter.sqlite]\ndatabase_url = "{}"\n` —— 占位符填 db_output 路径
       - 不要指定 `table_name`，让 SqliteExporterConfig::default() 自动赋值为 "sqllog_records"（per PATTERNS.md "处置方式 (b)，保持 config 极简"）
       - 不要包含 [logging] 段（per RESEARCH.md "Anti-Patterns to Avoid"）

    给两个 helper 各加一行 doc 注释：`/// Phase 57 TEST-01: 生成 run 命令的 CSV 配置文件（inputs 字段填日志目录路径）。` 与 `/// Phase 57 TEST-01: 生成 run 命令的 SQLite 配置文件（默认表名 sqllog_records）。`

    严格不引入任何新 use（std::path::Path / PathBuf 已可用）；严格不修改现有 helper；两个新 helper 之间不要互相调用，各自独立。
  </action>
  <verify>
    <automated>cargo build --tests 2>&1 | tail -10 && grep -c 'fn write_run_config_toml\|fn write_run_sqlite_config_toml' tests/integration.rs</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build --tests` 退出码 0（两个 helper 语法正确，文件可编译）
    - `grep -c 'fn write_run_config_toml\|fn write_run_sqlite_config_toml' tests/integration.rs` 输出 2
    - `grep -A 10 'fn write_run_config_toml' tests/integration.rs | grep -c '\[sqllog\]'` 输出 ≥ 1
    - `grep -A 10 'fn write_run_config_toml' tests/integration.rs | grep -c '\[exporter.csv\]'` 输出 ≥ 1
    - `grep -A 10 'fn write_run_sqlite_config_toml' tests/integration.rs | grep -c '\[exporter.sqlite\]'` 输出 ≥ 1
    - `grep -A 10 'fn write_run_sqlite_config_toml' tests/integration.rs | grep -c 'table_name'` 输出 0（不显式指定，依赖默认值）
    - `grep -A 12 'fn write_run_sqlite_config_toml' tests/integration.rs | grep -c '\[logging\]'` 输出 0（不含 logging 段）
    - `cargo clippy --all-targets -- -D warnings` 退出 0
  </acceptance_criteria>
  <done>
    两个 helper 函数加入并编译通过；签名、内容字段、路径转义、SQLite 默认表名策略均符合 PATTERNS.md 锁定模式。
  </done>
</task>

<task type="auto">
  <name>Task 2: 新增 run CSV / run SQLite 两个 e2e 测试 (TEST-01)</name>
  <files>tests/integration.rs</files>
  <read_first>
    - tests/integration.rs（必读第 16–29 行 write_test_log helper：本任务通过它生成测试日志；必读第 1493–1529 行 test_stats_csv_outputs_two_files：assert_cmd 链式调用 + CSV 读取断言模板；必读 Task 1 末尾新增的 write_run_config_toml / write_run_sqlite_config_toml 两个 helper 的签名——本任务直接调用）
    - src/pipeline/mod.rs（必读第 11–27 行 FIELD_NAMES：CSV header 字面字符串必须 100% 匹配 per D-05）
    - src/exporter/sqlite/tests.rs（必读 rusqlite::Connection::open + query_row 用法模式，本任务的 SQLite 测试照搬该模式）
    - .planning/phases/57-e2e/57-PATTERNS.md（必读 "TEST-01 — run CLI CSV 全链路测试" 与 "TEST-01 — run CLI SQLite 全链路测试" 两段，已给出完整测试代码片段）
  </read_first>
  <action>
    在 Task 1 新增的两个 helper 函数之后、文件末尾，追加两个 #[test] 函数。

    **测试 1: `test_cli_run_csv_output_header_and_row_count` (TEST-01 CSV 路径)**
    - doc 注释：`/// TEST-01 (Phase 57): run 子命令 CLI 输出 CSV header 与记录数（D-04/D-05/D-06）。`
    - 函数体头部 use：`use assert_cmd::Command;`（与现有 e2e 测试风格一致）
    - 创建 tempfile：`let dir = tempfile::TempDir::new().unwrap();`（注意：本测试用 TempDir::new()，与 stats helper 一致）
    - 创建日志子目录：`let log_dir = dir.path().join("logs"); std::fs::create_dir_all(&log_dir).unwrap();`
    - 写入测试日志（10 条）：`let record_count = 10usize; write_test_log(&log_dir.join("test.log"), record_count);`（per D-06 复用 write_test_log）
    - 准备 CSV 输出与 config 路径：
      * `let csv_file = dir.path().join("out.csv");`
      * `let cfg_path = write_run_config_toml(dir.path(), &log_dir, &csv_file);`
    - 启动二进制并断言退出 0：
      * `Command::cargo_bin("sqllog2db").unwrap().args(["run", "-c"]).arg(&cfg_path).assert().success();`
    - 读取 CSV 并断言：
      * `let content = std::fs::read_to_string(&csv_file).unwrap();`
      * `let mut lines = content.lines();`
      * 第一行精确匹配（per D-05）：`assert_eq!(lines.next().unwrap(), "ts,ep,sess_id,thrd_id,username,trx_id,statement,appname,client_ip,tag,sql,exec_time_ms,row_count,exec_id,normalized_sql", "CSV header must match FIELD_NAMES order");`
      * 数据行数：`let data_count = lines.filter(|l| !l.is_empty()).count(); assert_eq!(data_count, record_count, "row count must match written records");`

    **测试 2: `test_cli_run_sqlite_output_row_count` (TEST-01 SQLite 路径)**
    - doc 注释：`/// TEST-01 (Phase 57): run 子命令 CLI 输出 SQLite 文件存在与表 sqllog_records 记录数（D-07，表名修正）。`
    - 函数体头部 use：`use assert_cmd::Command;`
    - 准备临时目录、日志子目录与 5 条测试日志：
      * `let dir = tempfile::TempDir::new().unwrap();`
      * `let log_dir = dir.path().join("logs"); std::fs::create_dir_all(&log_dir).unwrap();`
      * `let record_count = 5usize; write_test_log(&log_dir.join("test.log"), record_count);`
    - 准备 db 输出与 config：
      * `let db_file = dir.path().join("out.db");`
      * `let cfg_path = write_run_sqlite_config_toml(dir.path(), &log_dir, &db_file);`
    - 启动并断言退出 0：
      * `Command::cargo_bin("sqllog2db").unwrap().args(["run", "-c"]).arg(&cfg_path).assert().success();`
    - 验证 db 文件存在：`assert!(db_file.exists(), "SQLite output file must exist");`
    - 用 rusqlite 查询 sqllog_records 行数（per PATTERNS.md "rusqlite 查询模式"）：
      * `let conn = rusqlite::Connection::open(&db_file).unwrap();`
      * `let count: i64 = conn.query_row("SELECT COUNT(*) FROM sqllog_records", [], |row| row.get(0)).unwrap();`
      * `assert_eq!(count, record_count as i64, "sqllog_records table row count must match");`
    - 注意：SQL 查询表名必须是 `sqllog_records`，绝对不能写 `sqllog`（per PATTERNS.md 表名确认与 Pitfall 1）

    两个测试都不要：
    - 引入 chrono / time / 自定义日期格式
    - 检查 CSV/DB 的具体字段值（行数 + header 已足够覆盖 TEST-01 "CSV 输出内容（字段名 + 记录数）"）
    - 修改 Task 1 的 helper

    rusqlite 已在 [dependencies] 中（per RESEARCH.md Cargo.toml 验证），测试可直接 `use rusqlite::Connection;` 或在调用点用全限定名 `rusqlite::Connection::open`（推荐后者，避免新 use 行）。
  </action>
  <verify>
    <automated>cargo test --test integration test_cli_run_csv_output_header_and_row_count test_cli_run_sqlite_output_row_count 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --test integration test_cli_run_csv_output_header_and_row_count` 退出 0，输出含 `test test_cli_run_csv_output_header_and_row_count ... ok`
    - `cargo test --test integration test_cli_run_sqlite_output_row_count` 退出 0，输出含 `test test_cli_run_sqlite_output_row_count ... ok`
    - `grep -c 'fn test_cli_run_csv_output_header_and_row_count\|fn test_cli_run_sqlite_output_row_count' tests/integration.rs` 输出 2
    - `grep -A 30 'fn test_cli_run_csv_output_header_and_row_count' tests/integration.rs | grep -c '"ts,ep,sess_id,thrd_id,username,trx_id,statement,appname,client_ip,tag,sql,exec_time_ms,row_count,exec_id,normalized_sql"'` 输出 ≥ 1（CSV header 字面字符串）
    - `grep -A 30 'fn test_cli_run_sqlite_output_row_count' tests/integration.rs | grep -c 'FROM sqllog_records'` 输出 ≥ 1（正确表名）
    - `grep -A 30 'fn test_cli_run_sqlite_output_row_count' tests/integration.rs | grep -c '"FROM sqllog "'` 输出 0（错误表名 sqllog 未出现）
    - `grep -A 30 'fn test_cli_run_csv_output_header_and_row_count\|fn test_cli_run_sqlite_output_row_count' tests/integration.rs | grep -c 'write_test_log'` 输出 ≥ 2（两个测试均复用 helper per D-06）
    - `cargo clippy --all-targets -- -D warnings` 退出 0
  </acceptance_criteria>
  <done>
    两个 TEST-01 e2e 测试通过；CSV header 字面字符串与 SQLite 表名 sqllog_records 在源码中均可 grep 命中；测试通过 cargo_bin 启动真实二进制完成端到端验证。
  </done>
</task>

<task type="auto">
  <name>Task 3: 新增 init 成功 / init 已存在两个 e2e 测试 (TEST-02)</name>
  <files>tests/integration.rs</files>
  <read_first>
    - tests/integration.rs（必读第 1838–1873 行 test_init_template_contains_stats_section：assert_cmd init 风格模板；必读 Task 2 末尾追加的两个 run 测试用以理解新测试在文件中的位置）
    - src/cli/init.rs（必读完整文件 59 行：理解 handle_init 在文件已存在且 force=false 时返回 Err(Error::File(FileError::AlreadyExists))；同时 error! log 输出 "Configuration file already exists: {output_path}"——本任务 stderr 断言可锚定 "already exists" 子串）
    - src/error.rs（必读第 197 行 FileError::AlreadyExists 的 #[error] 文案 "File already exists: {path}" —— "already exists" 子串在两处均出现，断言稳定）
    - .planning/phases/57-e2e/57-PATTERNS.md（必读 "TEST-02 — init CLI 测试" 段落，已给出两个测试的完整代码）
  </read_first>
  <action>
    在 Task 2 新增的两个 run 测试之后、文件末尾，追加两个 init #[test] 函数。

    **测试 1: `test_cli_init_creates_file_exit_0` (D-08 #1)**
    - doc 注释：`/// TEST-02 (Phase 57): init 子命令 CLI 在新路径下成功创建配置文件并退出 0。`
    - 函数体头部 use：`use assert_cmd::Command;`
    - 临时目录与新文件路径：
      * `let dir = tempfile::TempDir::new().unwrap();`
      * `let out_file = dir.path().join("new_config.toml");`
    - 启动 init 子命令（不传 --force，因为目标路径不存在）：
      * `Command::cargo_bin("sqllog2db").unwrap().args(["init", "-o"]).arg(&out_file).assert().success();`
    - 验证文件存在：`assert!(out_file.exists(), "init must create the config file");`
    - 验证文件内容包含 [sqllog]（避免空文件 trick——做一条最小内容断言）：
      * `let content = std::fs::read_to_string(&out_file).unwrap();`
      * `assert!(content.contains("[sqllog]"), "generated config must contain [sqllog] section");`

    **测试 2: `test_cli_init_existing_file_without_force_exits_nonzero` (D-08 #2)**
    - doc 注释：`/// TEST-02 (Phase 57): init 子命令 CLI 在目标文件已存在且未传 --force 时退出非零，stderr 包含 "already exists"。`
    - 函数体头部 use：`use assert_cmd::Command;` 与 `use predicates::str::contains;`
    - 临时目录 + 预先创建已存在的文件：
      * `let dir = tempfile::TempDir::new().unwrap();`
      * `let out_file = dir.path().join("existing.toml");`
      * `std::fs::write(&out_file, "existing content").unwrap();`
    - 启动 init 不带 --force，断言失败：
      * `Command::cargo_bin("sqllog2db").unwrap().args(["init", "-o"]).arg(&out_file).assert().failure().stderr(contains("already exists"));`
    - stderr 断言只用单条 `contains("already exists")`，子串在 FileError::AlreadyExists 的 #[error] Display 文案 ("File already exists: ...") 与 src/cli/init.rs:15 的 error! log ("Configuration file already exists: ...") 中均稳定出现，无需 `.or(...)` 兜底（per CONTEXT.md "specifics" 段落 init 测试的错误信息验证示例）

    两个测试都不要：
    - 修改任何 src/ 代码
    - 引入额外 dev-dependency
    - 在 test_cli_init_creates_file_exit_0 中传 `--force` 标志（与 test_init_template_contains_stats_section 区分开——后者已覆盖 --force 路径，本测试故意覆盖默认行为）
    - 在 test_cli_init_existing_file_without_force_exits_nonzero 中检查具体退出码值（用 .failure() 即可，per CONTEXT.md Deferred Ideas "退出码 1 vs 2 细化"）
  </action>
  <verify>
    <automated>cargo test --test integration test_cli_init_creates_file_exit_0 test_cli_init_existing_file_without_force_exits_nonzero 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --test integration test_cli_init_creates_file_exit_0` 退出 0，输出含 `test test_cli_init_creates_file_exit_0 ... ok`
    - `cargo test --test integration test_cli_init_existing_file_without_force_exits_nonzero` 退出 0，输出含 `test test_cli_init_existing_file_without_force_exits_nonzero ... ok`
    - `grep -c 'fn test_cli_init_creates_file_exit_0\|fn test_cli_init_existing_file_without_force_exits_nonzero' tests/integration.rs` 输出 2
    - `grep -A 15 'fn test_cli_init_creates_file_exit_0' tests/integration.rs | grep -c '.success()'` 输出 ≥ 1
    - `grep -A 15 'fn test_cli_init_creates_file_exit_0' tests/integration.rs | grep -c '"\\-\\-force"'` 输出 0（成功测试不使用 --force）
    - `grep -A 15 'fn test_cli_init_existing_file_without_force_exits_nonzero' tests/integration.rs | grep -c '.failure()'` 输出 ≥ 1
    - `grep -A 15 'fn test_cli_init_existing_file_without_force_exits_nonzero' tests/integration.rs | grep -c 'contains("already exists")'` 输出 ≥ 1
    - `cargo test --test integration` 全套退出 0（无回归；test_init_template_contains_stats_section 仍通过）
    - `cargo clippy --all-targets -- -D warnings` 退出 0
    - `cargo fmt --check` 退出 0
  </acceptance_criteria>
  <done>
    两个 TEST-02 e2e 测试通过；新文件创建路径与文件已存在错误路径分别有独立测试；stderr 断言锚定稳定子串 "already exists"；与 Phase 53 的 test_init_template_contains_stats_section（已覆盖 --force 路径）形成互补；全套 integration 测试无回归。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tests → assert_cmd 启动的真实二进制 | 测试通过 cargo_bin 启动隔离的 sqllog2db 子进程；stdin/stdout/stderr 是断言唯一来源 |
| 测试 → 文件系统（tempfile） | 所有测试产物写入 tempfile::TempDir，Drop 时自动清理，无外部副作用 |
| 测试 → rusqlite Connection | run SQLite 测试打开本地 .db 文件，仅做 SELECT COUNT 查询，无注入面 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-57-04 | Information Disclosure | tempfile 测试目录 | accept | TempDir::new() 在 OS 隔离的临时目录中创建，权限默认 user-only；Drop 时清理；无敏感数据 |
| T-57-05 | Tampering | rusqlite::Connection::open(db_file) | accept | db_file 路径由测试自己生成，未接收外部输入；query_row 使用空参数列表 [] 与硬编码 SQL，无注入面 |
| T-57-06 | Denial of Service | 4 个新 e2e 测试 × 启动子进程 | accept | 每条测试 ~1–2s，10/5 条日志记录的吞吐微不足道；不影响 CI 时间预算（现有 60+ 测试已稳定运行） |
| T-57-07 | Spoofing | assert_cmd cargo_bin 解析 | accept | assert_cmd 使用 CARGO_BIN_EXE_sqllog2db 环境变量定位由 cargo 自身构建的二进制，无第三方注入 |
| T-57-SC | Tampering | npm/pip/cargo installs | mitigate | 本 plan 不引入任何新 crate（assert_cmd / predicates / tempfile / rusqlite 全部已在 Cargo.toml 中），零 supply-chain 变更 |
</threat_model>

<verification>
- `cargo test --test integration` 全套通过（含 4 个新 e2e 测试 + 现有所有测试，无回归）
- `cargo test` 全套通过（含 Plan 01 的 stats::config::tests 新单元测试）
- `cargo clippy --all-targets -- -D warnings` 通过
- `cargo fmt --check` 通过
- 手工抽查：执行 `cargo run -- run -c <临时 cfg>` 与 `cargo run -- init -o /tmp/x.toml` 可复现测试断言行为（非必需，仅 sanity check）
</verification>

<success_criteria>
- TEST-01 两条路径（CSV / SQLite）均有 assert_cmd 测试覆盖：
  * CSV：header 字面字符串精确匹配 FIELD_NAMES 顺序，数据行数等于 write_test_log 写入数
  * SQLite：文件存在 + rusqlite 查询 sqllog_records 表 COUNT(*) 等于写入数
- TEST-02 两条路径（新建成功 / 已存在失败）均有 assert_cmd 测试覆盖：
  * 成功：不传 --force 创建新文件，退出 0，文件含 [sqllog] 段
  * 失败：已存在文件 + 不传 --force，退出非零，stderr 含 "already exists"
- 与现有测试形成完整 e2e 覆盖矩阵：
  * stats CLI：from 格式 (existing) + from==to (existing) + from>to (Plan 01) + valid stats config (existing)
  * run CLI：CSV (本 plan) + SQLite (本 plan)；handle_run 单元测试（existing）
  * init CLI：成功新建 (本 plan) + 已存在拒绝 (本 plan) + 已存在 + --force (existing test_init_template_contains_stats_section)
- 为 Phase 58 cli/run 函数拆分提供安全网（run CLI 端到端行为被 cargo_bin 测试守护，重构后端到端语义不变）
</success_criteria>

<output>
Create `.planning/phases/57-e2e/57-02-SUMMARY.md` when done
</output>
