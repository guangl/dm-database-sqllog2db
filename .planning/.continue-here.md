---
context: default
phase: null
task: 9
total_tasks: 20
status: in_progress
last_updated: 2026-06-11T09:00:37.000Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: BROKEN BUILD — Partial async migration. run_file_loop in sequential.rs calls process_log_file() without .await. parallel.rs, prescan.rs, sqlite_parallel.rs still reference crate::async_rt::parse_file_sync. Do NOT attempt any other work until the build is fixed.
- [ ] CONSTRAINT: AsyncLogParser error handling — AsyncError::Parse means the ENTIRE file failed. Handle as: log warn + return Ok((0, file_stats_with_parse_error)). Do NOT propagate as Err. Tests already expect NO error log when async parse fails (test_watch_error_log_append asserts error log does NOT exist).
- [ ] CONSTRAINT: rayon parallel paths — Cannot `.await` inside rayon closures. Strategy: capture `tokio::runtime::Handle::current()` BEFORE the rayon pool.install(), wrap rayon block in `tokio::task::block_in_place`, call `handle.block_on(AsyncLogParser::new(path).parse())` inside rayon closures.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Stopping mid-migration | Left code with more compile errors than started with | blocking | Complete all async propagation in one session |
| JoinSet for non-Clone Pipeline | Pipeline cannot be cloned (Filter contains boxed Fn) — JoinSet approach requires Send+Clone | advisory | Use block_in_place + handle.block_on() for rayon paths instead |

<current_state>
Mid async migration of dm-database-parser-sqllog 2.0.4 API. Code does not compile. Completed (this session): scanner.rs, stats/runner.rs, processor.rs, sequential.rs (partial — run_sequential is async but run_file_loop is not). Remaining: 12 more items.
</current_state>

<completed_work>

Completed tasks (previous session):
- Task 1: upgrade dm-database-parser-sqllog 2.0.3 → 2.0.4
- Task 2: add tokio macros feature to Cargo.toml
- Task 3: delete src/async_rt.rs bridge layer
- Task 4: remove 'mod async_rt' from lib.rs and main.rs

Completed tasks (this session):
- Task 5: scanner.rs scan_files → async, uses AsyncLogParser, AsyncError::Parse → log warn + Err (file fatal — stats path)
- Task 6: stats/runner.rs run_stats + scan_files_into_accumulator → async
- Task 7: processor.rs process_log_file → async, uses AsyncLogParser, AsyncError::Parse → log warn + Ok((0, stats))
- Task 8 (partial): sequential.rs run_sequential signature → async (run_file_loop NOT yet async)
</completed_work>

<remaining_work>

- Task 9: sequential.rs run_file_loop → async fn, add .await to process_log_file call
- Task 10: sqlite_parallel.rs parse_and_write_sqlite + process_sqlite_parallel → async, use AsyncLogParser
- Task 11: parallel.rs parse_and_write_csv → capture Handle, wrap rayon in block_in_place, use handle.block_on() inside rayon
- Task 12: prescan.rs scan_log_file_for_matches → same block_in_place strategy; scan_for_trxids_by_transaction_filters → async
- Task 13: input.rs merge_trxid_prescan → async (calls scan_for_trxids_by_transaction_filters)
- Task 14: orchestrator.rs route_processing/run_csv_parallel/run_sqlite_parallel/handle_run → async
- Task 15: cli/stats/handler.rs handle_stats → async (calls run_stats)
- Task 16: watch trigger_full.rs trigger_full_file → async; trigger_incremental.rs trigger_incremental + run_incremental_handle_run → async; event.rs handle_event → async; handler.rs run_watch_loop → async + block_in_place for recv_timeout; handle_watch → async
- Task 17: main.rs run() → async fn, main() → #[tokio::main], update all .await call sites
- Task 18: cli/run/tests.rs → change all #[test] that call handle_run to #[tokio::test] async
- Task 19: watch/tests.rs → change tests calling trigger_full_file/trigger_incremental/handle_watch to #[tokio::test] async
- Task 20: cargo clippy + cargo test pass
</remaining_work>

<decisions_made>

- AsyncError::Parse → log warn + return Ok((0, file_stats_with_parse_error)) — NOT Err propagation (sequential paths)
- scanner.rs scan_files: AsyncError::Parse → log warn + return Err (stats path — consistent with original abort-on-file-error behavior)
- rayon paths: capture Handle BEFORE rayon, wrap rayon in block_in_place, call handle.block_on() inside closures
- Pipeline cannot be Clone (Filter has boxed Fn) → avoid JoinSet approach for parallel.rs
- process_sqlite_parallel stays sequential (SQLite writes are serial), just needs async
- Watch recv_timeout: block_in_place wrapping, trigger fns → async
</decisions_made>

<blockers>

- Compile error: run_file_loop in sequential.rs calls process_log_file() without .await
- Compile error: parallel.rs (2 occurrences), prescan.rs (1), sqlite_parallel.rs (1) still reference crate::async_rt::parse_file_sync
</blockers>

## Required Reading (in order)
1. `.planning/HANDOFF.json` — full task list and strategy details
2. `src/cli/run/sequential.rs` — next file to fix
3. `src/cli/run/parallel.rs` — rayon conversion with block_in_place

## Infrastructure State
- Cargo.toml: dm-database-parser-sqllog 2.0.4 with async feature, tokio rt-multi-thread + macros
- Build status: BROKEN — does not compile

<context>
Async migration of dm-database-parser-sqllog 2.0.4 API. Strategy for rayon paths: use tokio::task::block_in_place to wrap the rayon pool.install() call, and capture Handle::current() before entering rayon so it can be used inside rayon closures with handle.block_on(AsyncLogParser). The sequential paths are properly async with .await throughout. Watch module needs block_in_place for recv_timeout and async for the trigger fns.
</context>

<next_action>
Start with: Open src/cli/run/sequential.rs, change fn run_file_loop to async fn, add .await to process_log_file call on line ~81. Then proceed to sqlite_parallel.rs (task 10).
</next_action>
