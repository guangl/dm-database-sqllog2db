---
phase: 67-prog-diag
plan: 03
type: execute
wave: 2
depends_on: ["67-01", "67-02"]
files_modified:
  - src/cli/run/mod.rs
  - src/cli/run/processor.rs
  - src/cli/run/tests.rs
autonomous: true
requirements: [PROG-03, DIAG-03]
must_haves:
  truths:
    - "run_stats.has_errors() 时摘要输出 'errors by type: encoding={n}, field_missing={n}, parse_failed={n}'"
    - "run_stats.filtered_out > 0 时摘要输出 'filtered: {n} records ({pct:.1}% of {total} total)'"
    - "by_type[EncodingError] > 0 触发 stderr 编码 hint；by_type[FieldMissing] > 0 触发字段 hint"
    - "write_error_log 在 cfg.error.is_some() && parse_error_records 非空时按 D-09 格式批量写出（覆盖模式）"
    - "normalize_and_export 的 !passes 路径递增 file_stats.filtered_out（D-15）"
  artifacts:
    - path: "src/cli/run/mod.rs"
      provides: "print_run_summary 扩展 by_type/filtered/hint + 新增 write_error_log + handle_run 末尾调用"
      contains: "write_error_log"
    - path: "src/cli/run/processor.rs"
      provides: "normalize_and_export !passes 路径递增 filtered_out"
      contains: "file_stats.filtered_out += 1"
    - path: "src/cli/run/tests.rs"
      provides: "test_error_log_written + test_hint_output + test_run_summary"
      contains: "test_error_log_written"
  key_links:
    - from: "handle_run 末尾"
      to: "write_error_log(cfg, &run_stats)"
      via: "在 pb.finish_and_clear 之前调用"
      pattern: "write_error_log\\(cfg, &run_stats\\)"
    - from: "print_run_summary"
      to: "stderr"
      via: "eprintln! 新增 4 行：errors by type / filtered / 编码 hint / 字段 hint"
      pattern: "errors by type"
---

<objective>
PROG-03/DIAG-03：补全用户可见输出 — 摘要分组统计 + 过滤率 + hint + error log 文件写出。依赖 Plan 02 的 ErrorStats 三字段。

Purpose: 完成 Phase 67 全部用户可观察的诊断增强。
Output: print_run_summary 扩展 + write_error_log 函数 + normalize_and_export filtered_out 递增 + 三个集成/单元测试 GREEN。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/67-prog-diag/67-CONTEXT.md
@.planning/phases/67-prog-diag/67-RESEARCH.md
@.planning/phases/67-prog-diag/67-PATTERNS.md
@.planning/phases/67-prog-diag/67-01-PLAN.md
@.planning/phases/67-prog-diag/67-02-PLAN.md
@src/cli/run/mod.rs
@src/cli/run/processor.rs
@src/cli/run/tests.rs
@src/error.rs

<interfaces>
依赖来自 Plan 02：
  pub enum ErrorKind { EncodingError, FieldMissing, ParseFailed }
  ErrorStats { by_type: HashMap<ErrorKind, u64>, filtered_out: u64, parse_error_records: Vec<ParseErrorRecord> }
  impl ErrorKind { kind_display(&self) -> &'static str }
  Config { error: Option<ErrorLogConfig> }，ErrorLogConfig { file: String }

print_run_summary 现签名（src/cli/run/mod.rs:386）：
  fn print_run_summary(quiet, verbose, use_parallel, elapsed, processed_files, total_records, skipped_files, run_stats) — 仍可工作。
write_error_log 新增私有函数：
  fn write_error_log(cfg: &Config, stats: &ErrorStats)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — 摘要 + hint + error log 集成测试桩（RED）</name>
  <files>src/cli/run/tests.rs</files>
  <read_first>
    - src/cli/run/tests.rs（现有 test_handle_run_default_config_succeeds 模式：用 tempfile + TOML 字符串 + handle_run 调用）
    - src/cli/run/mod.rs:386-418（print_run_summary 现有输出）
    - .planning/phases/67-prog-diag/67-VALIDATION.md（Wave 0 条目）
  </read_first>
  <behavior>
    - test_error_log_written: 构造一个含 1 条合法 SEL + 2 条非法行（如 "garbage" 与 "(EP[ broken)"）的 .log；TOML 包含 [error] file = "errors.log" 段；handle_run(quiet=true) 后断言 errors.log 文件存在且至少含字符串 "[ERROR] line " 与 "  reason: "。
    - test_hint_output (#[ignore] 或捕获 stderr 困难时改为单元测试 print_run_summary 直接调用): 直接构造 ErrorStats { by_type: {EncodingError: 3}, ... }，调用 print_run_summary(false, false, false, 0.1, &[], 0, 0, &stats) — 由于 eprintln! 难以捕获，本测试改为断言 run_stats.by_type 内容（用 ErrorStats 公共字段直读，已在 Plan 02 设为 pub），即 Wave 0 桩仅作为编译性测试存在；真正的 hint 行为由 manual 验证支撑（VALIDATION 已记录）。
    - test_run_summary: 单元层断言 — 构造 ErrorStats { filtered_out: 5, total_errors: 2, parse_errors: 2, by_type: {EncodingError: 2}, ... }，调用 print_run_summary，本测试不捕获 stderr，仅验证函数不 panic（防回归）。
  </behavior>
  <action>
    在 src/cli/run/tests.rs 末尾追加 3 个 #[test]：
      1) test_error_log_written — 完整 handle_run 流程（参考现有 test_handle_run_default_config_succeeds，TOML 内追加 [error] file = "{errors_log}"，log 文件构造含 1 行 "garbage parse failure" + 1 行合法 SEL；handle_run 调用后用 std::fs::read_to_string 读取 errors.log，断言 contains("[ERROR] line ") 与 contains("reason:")）。
      2) test_hint_output — 直接构造 ErrorStats（公开字段赋值），调用 super::print_run_summary(false, false, false, 0.1, &[], 0, 0, &stats)；only assert no panic（hint 输出走 stderr，集成层手动验证，VALIDATION.md Manual-Only Verifications 已覆盖此差距）。
      3) test_run_summary — 同样构造 ErrorStats（filtered_out=5），调用 print_run_summary 不 panic。
    这些测试在 Task 2 实现前会编译失败（ErrorStats 字段构造 + write_error_log 调用路径未补齐），RED 状态确认。
  </action>
  <verify>
    <automated>cargo test --lib cli::run::tests::test_error_log_written 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - grep -c 'fn test_error_log_written' src/cli/run/tests.rs 输出 1
    - grep -c 'fn test_hint_output' src/cli/run/tests.rs 输出 1
    - grep -c 'fn test_run_summary' src/cli/run/tests.rs 输出 1
    - test_error_log_written 在 Task 2 实现前为 RED（errors.log 不存在或断言失败）
  </acceptance_criteria>
  <done>3 测试函数写入；test_error_log_written RED。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: print_run_summary 扩展 + write_error_log + filtered_out 递增（GREEN）</name>
  <files>src/cli/run/mod.rs, src/cli/run/processor.rs</files>
  <read_first>
    - src/cli/run/mod.rs:29-135（handle_run 主体；目标插入点在 print_run_summary 之后、pb.finish_and_clear 之前）
    - src/cli/run/mod.rs:386-418（print_run_summary 现有结构）
    - src/cli/run/processor.rs:45-100（normalize_and_export，!passes 分支在 line 59-64）
    - src/error.rs（确认 ErrorKind::kind_display、HashMap::get 用法）
    - .planning/phases/67-prog-diag/67-RESEARCH.md "Code Examples" 与 "摘要扩展"
    - .planning/phases/67-prog-diag/67-CONTEXT.md D-09/D-10/D-14/D-15/D-16
  </read_first>
  <action>
    步骤 A — src/cli/run/processor.rs (D-15):
      1) normalize_and_export 函数 !passes 分支（line 59-64）：在 if !passes { ... return ExportAction::Continue; } 块内、return 之前追加 file_stats.filtered_out += 1;
    步骤 B — src/cli/run/mod.rs (D-16 + D-14):
      2) print_run_summary 函数体 if run_stats.has_errors() 块内末尾追加：
            eprintln!(
                "  errors by type: encoding={}, field_missing={}, parse_failed={}",
                run_stats.by_type.get(&crate::error::ErrorKind::EncodingError).copied().unwrap_or(0),
                run_stats.by_type.get(&crate::error::ErrorKind::FieldMissing).copied().unwrap_or(0),
                run_stats.by_type.get(&crate::error::ErrorKind::ParseFailed).copied().unwrap_or(0),
            );
      3) print_run_summary 在 has_errors 块后追加（仍受 !quiet 守卫）：
            if run_stats.filtered_out > 0 {
                let total_read = total_records as u64 + run_stats.filtered_out;
                let pct = if total_read > 0 { run_stats.filtered_out as f64 / total_read as f64 * 100.0 } else { 0.0 };
                eprintln!("  filtered: {} records ({pct:.1}% of {} total)", run_stats.filtered_out, total_read);
            }
            if run_stats.by_type.get(&crate::error::ErrorKind::EncodingError).copied().unwrap_or(0) > 0 {
                eprintln!("  hint: 多行 encoding_error — 建议检查文件编码是否为 GBK/GB18030");
            }
            if run_stats.by_type.get(&crate::error::ErrorKind::FieldMissing).copied().unwrap_or(0) > 0 {
                eprintln!("  hint: 多行 field_missing — 建议确认日志格式与 DM SQL log 格式一致");
            }
    步骤 C — src/cli/run/mod.rs (D-10) 新增 write_error_log:
      4) 新增私有函数（放在 print_run_summary 之后）：
            fn write_error_log(cfg: &Config, stats: &ErrorStats) {
                let Some(error_cfg) = cfg.error.as_ref() else { return };
                if stats.parse_error_records.is_empty() { return; }
                use std::io::Write;
                let file = match std::fs::File::create(&error_cfg.file) {
                    Ok(f) => f,
                    Err(e) => { log::warn!("Failed to create error log {}: {e}", error_cfg.file); return; }
                };
                let mut writer = std::io::BufWriter::new(file);
                let truncated = stats.parse_error_records.len() >= 10_000;
                for rec in &stats.parse_error_records {
                    let _ = writeln!(
                        writer,
                        "[ERROR] line {}: {}  reason: {}",
                        rec.line_number, rec.raw_truncated, rec.kind.kind_display()
                    );
                }
                if truncated {
                    let _ = writeln!(writer, "[truncated at 10000 records]");
                }
                if let Err(e) = writer.flush() {
                    log::warn!("Failed to flush error log: {e}");
                }
            }
      5) handle_run 末尾：在 print_run_summary 调用之后、`if let Some(pb) = &pb { pb.finish_and_clear(); }` 之前，插入：
            write_error_log(cfg, &run_stats);
         （此时 cfg 为 &Config 形参，符合签名）。
    步骤 D：cargo fmt + cargo clippy --all-targets -- -D warnings + cargo test --lib + cargo test --test integration（若有）。
    注意：现有测试 TOML 中已含 [error] file 段（src/cli/run/tests.rs line 18 等），Plan 02 已让该段不再静默忽略；本任务后这些测试将真实写出 errors.log 到 tempdir，无副作用。
  </action>
  <verify>
    <automated>cargo clippy --all-targets -- -D warnings 2>&1 | tail -10 && cargo test --lib 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - grep -c 'file_stats.filtered_out += 1' src/cli/run/processor.rs 输出 1
    - grep -c 'fn write_error_log' src/cli/run/mod.rs 输出 1
    - grep -c 'write_error_log(cfg, &run_stats)' src/cli/run/mod.rs 输出 1
    - grep -c 'errors by type:' src/cli/run/mod.rs 输出 1
    - grep -c 'filtered: {} records' src/cli/run/mod.rs 输出 1
    - grep -c 'encoding_error — 建议检查' src/cli/run/mod.rs 输出 1
    - grep -c 'field_missing — 建议确认' src/cli/run/mod.rs 输出 1
    - grep -c '\\[ERROR\\] line' src/cli/run/mod.rs 输出 1
    - grep -c '\\[truncated at 10000 records\\]' src/cli/run/mod.rs 输出 1
    - cargo clippy --all-targets -- -D warnings 通过
    - cargo test --lib cli::run::tests::test_error_log_written GREEN
    - cargo test --lib cli::run::tests::test_hint_output GREEN
    - cargo test --lib cli::run::tests::test_run_summary GREEN
    - cargo test --lib 全绿（Plan 01 + Plan 02 测试不回归）
  </acceptance_criteria>
  <done>ROADMAP SC4（by_type 分布）+ SC5（hint）+ DIAG-01 文件写出全部可观察；clippy / test 全绿。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| ErrorStats → error log 文件 | 程序内存写入用户指定本地路径 |
| ErrorStats → stderr | 摘要 + hint 直接写 stderr |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-67-06 | Tampering | write_error_log 覆盖任意路径 | accept | cfg.error.file 来源 TOML，由用户自定义；与 logging.file/csv.file 现有模型一致 |
| T-67-07 | DoS | write_error_log 阻塞主线程 | mitigate | BufWriter + 10k 上限；最坏 10k 行 < 1MB 文本；写失败仅 log::warn! 不终止流程 |
| T-67-08 | Information Disclosure | hint 文本含日志格式假设 | accept | hint 为固定中文字符串，无敏感信息 |
</threat_model>

<verification>
- cargo fmt --check
- cargo clippy --all-targets -- -D warnings
- cargo test --lib（全套含 Plan 01 + Plan 02 + Plan 03）
- 手工：cargo run -- run -c config.toml（含 [error] file）— 观察 errors.log 内容含 "[ERROR] line N:" + stderr 含 hint
</verification>

<success_criteria>
- ROADMAP Phase 67 SC1–SC5 全部 TRUE
- 所有 Phase 67 需求 ID（PROG-01/02/03 + DIAG-01/02/03）行为可观察并有 automated 或 Wave 0 测试覆盖
- 现有测试集（cli/run/tests + error/tests + integration）无回归
</success_criteria>

<output>
完成后创建 .planning/phases/67-prog-diag/67-03-SUMMARY.md
</output>
