---
phase: 44-hotpath
plan: 03
type: execute
wave: 3
depends_on:
  - 44-01
  - 44-02
files_modified:
  - benches/baselines/csv_export/1000/phase44-after
  - benches/baselines/csv_export/10000/phase44-after
  - benches/baselines/csv_export/50000/phase44-after
  - benches/BENCHMARKS.md
autonomous: true
requirements:
  - PERF-01
  - PERF-02

must_haves:
  truths:
    - "criterion 输出对比 phase44-before 显示 csv_export/10000 中位数下降（'Performance has improved' 或绝对值改善）"
    - "phase44-after baseline 三个目录均存在并包含 estimates.json"
    - "jemalloc 优化后 delta 数值低于 Wave 0 baseline 数值（量化对比）"
    - "PERF-01 验收数据写入 benches/BENCHMARKS.md Phase 44 段落"
    - "PERF-02 验收数据（jemalloc before/after delta）写入 benches/BENCHMARKS.md Phase 44 段落"
    - "全套 cargo test/clippy/fmt 通过"
  artifacts:
    - path: "benches/baselines/csv_export/10000/phase44-after/estimates.json"
      provides: "PERF-01 优化后基线"
    - path: "benches/BENCHMARKS.md"
      provides: "Phase 44 验收记录段落"
      contains: "Phase 44"
  key_links:
    - from: "Wave 2 优化（Arc + 16MB BufWriter）"
      to: "criterion phase44-after vs phase44-before"
      via: "CRITERION_HOME=benches/baselines cargo bench --bench bench_csv -- --baseline phase44-before"
      pattern: "Performance has improved|change: -"
    - from: "Wave 2 优化（Arc 消除 Vec clone）"
      to: "jemalloc delta 对比"
      via: "tests/jemalloc_peak.rs test_jemalloc_peak_baseline"
      pattern: "phase44-before.*delta.*bytes"
---

<objective>
执行 Phase 44 验收：运行 criterion benchmark 对比 phase44-before baseline 验证 PERF-01（D-08），重新运行 jemalloc 峰值堆测试对比 Wave 0 数值验证 PERF-02（D-09），将两项数据写入 benches/BENCHMARKS.md 的 Phase 44 段落供后续历史追踪。

Purpose: 必须以可量化的 before/after 数据闭环本 phase 的两个 requirements。若任一项未达成（criterion 未显示改善，或 jemalloc delta 未下降），本 task 必须如实记录失败状态并触发 gap closure，不得伪造数据。
Output: phase44-after criterion baseline 三个目录 + benches/BENCHMARKS.md 新增 Phase 44 段落。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-hotpath/44-CONTEXT.md
@.planning/phases/44-hotpath/44-RESEARCH.md
@.planning/phases/44-hotpath/44-PATTERNS.md
@.planning/phases/44-hotpath/44-01-SUMMARY.md
@.planning/phases/44-hotpath/44-02-SUMMARY.md

@benches/BENCHMARKS.md
@benches/bench_csv.rs
@tests/jemalloc_peak.rs

<interfaces>
<!-- 关键命令与基线路径 — Pitfall 2: CRITERION_HOME 必须显式设置 -->

criterion baseline 保存与对比（per RESEARCH.md 第 369-374 行）：
- 优化后保存：CRITERION_HOME=benches/baselines cargo bench --bench bench_csv -- --save-baseline phase44-after
- 优化后对比：CRITERION_HOME=benches/baselines cargo bench --bench bench_csv -- --baseline phase44-before

jemalloc 对比命令：
- cargo test --test jemalloc_peak test_jemalloc_peak_baseline -- --nocapture

数据来源：
- Wave 0 phase44-before median: 从 benches/baselines/csv_export/*/phase44-before/estimates.json 读取
- Wave 0 jemalloc delta: 从 .planning/phases/44-hotpath/44-01-SUMMARY.md 读取
- Wave 3 phase44-after median: 本 task 新采集
- Wave 3 jemalloc delta: 本 task 重跑测试采集

benches/BENCHMARKS.md 现有段落格式（参考 Phase 42，第 509-555 行）：
- ## Phase NN — 标题
- Date / Goal / Test environment 标签
- 数据表格（Median time / Throughput 两列）
- <details> 包裹 criterion 原文输出
- 结论 checklist
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 运行优化后 criterion 与 jemalloc 测量并保存 phase44-after baseline</name>
  <read_first>
    - .planning/phases/44-hotpath/44-01-SUMMARY.md（读取 phase44-before 三个 median 数值和 jemalloc baseline delta）
    - .planning/phases/44-hotpath/44-02-SUMMARY.md（确认 Wave 2 优化已落地）
    - benches/baselines/csv_export/10000/phase44-before/estimates.json（读取 median.point_estimate 作为对比基准）
    - benches/bench_csv.rs（确认 bench_csv_export group 与参数 1000/10000/50000 未在 Wave 2 中变化）
    - tests/jemalloc_peak.rs（Wave 0 已创建，确认 test_jemalloc_peak_baseline 仍存在且签名未改）
    - .planning/phases/44-hotpath/44-RESEARCH.md（第 196-208 行 v1.10 baseline；第 305-312 行 Pitfall 2 CRITERION_HOME）
  </read_first>
  <files>
    benches/baselines/csv_export/1000/phase44-after/,
    benches/baselines/csv_export/10000/phase44-after/,
    benches/baselines/csv_export/50000/phase44-after/
  </files>
  <action>
  执行优化后测量，产生 PERF-01 与 PERF-02 两项验收数据。分两步：

  Step A — criterion benchmark（PERF-01 验收）：

  1. 先运行对比（不保存），观察 criterion 是否报告 "Performance has improved"（D-08）：
     CRITERION_HOME=benches/baselines cargo bench --bench bench_csv -- --baseline phase44-before
     - 完整 stdout 保存到 /tmp/phase44-criterion.log（供 Task 2 写入 BENCHMARKS.md 的 <details> 段落）
     - 关注 csv_export/10000 的 change 段落：
       * 若输出 "Performance has improved" → PERF-01 通过
       * 若输出 "No change in performance" → PERF-01 边界状态，记录但需进一步分析
       * 若输出 "Performance has regressed" → PERF-01 失败，记录失败原因后停止（不要继续 Step B；触发 gap closure）

  2. 再运行 --save-baseline 保存 phase44-after，供后续阶段对比：
     CRITERION_HOME=benches/baselines cargo bench --bench bench_csv -- --save-baseline phase44-after
     - 确认产生 benches/baselines/csv_export/{1000,10000,50000}/phase44-after/estimates.json

  Step B — jemalloc 峰值堆测量（PERF-02 验收）：

  3. 重新运行 jemalloc baseline 测试，捕获优化后 delta：
     cargo test --test jemalloc_peak test_jemalloc_peak_baseline -- --nocapture 2>&1 | tee /tmp/phase44-jemalloc-after.log
     - 输出中将包含 `[phase44-before] jemalloc allocated delta (10000 records) = N bytes`（Wave 0 命名沿用）
     - 注意：此处的"phase44-before"是测试输出 label，**实际是 phase44-after 的数值**（Wave 2 优化已生效，但测试代码 label 未改）
     - 对比：将 /tmp/phase44-jemalloc-after.log 的 delta 与 44-01-SUMMARY.md 中记录的 Wave 0 delta 数值对比
     - 若 delta 下降（after < before）→ PERF-02 通过
     - 若 delta 持平或上升 → PERF-02 失败，记录后停止（触发 gap closure）

     注意：若需要在测试输出中区分 before/after（避免 label 混淆），可考虑在 Wave 2 已完成的前提下，本 task 不修改 tests/jemalloc_peak.rs（保持单一函数签名稳定），仅在 SUMMARY 与 BENCHMARKS.md 中按文档 label 区分两次运行的语义。

  Step C — 数据收集（供 Task 2 写入 BENCHMARKS.md）：

  4. 用 python3 读取 estimates.json 计算各参数下的吞吐量（records/sec = N / (median_ns / 1e9)）：

     for n in 1000 10000 50000:
       before_ns = json.load(open(f"benches/baselines/csv_export/{n}/phase44-before/estimates.json"))['median']['point_estimate']
       after_ns = json.load(open(f"benches/baselines/csv_export/{n}/phase44-after/estimates.json"))['median']['point_estimate']
       before_throughput = n / (before_ns / 1e9)
       after_throughput = n / (after_ns / 1e9)
       delta_pct = (after_throughput - before_throughput) / before_throughput * 100

  5. 将六组数据（before/after × 1000/10000/50000）和 jemalloc before/after delta 写入临时文件 /tmp/phase44-data.json 供 Task 2 消费。

  约束：
  - 必须使用 CRITERION_HOME=benches/baselines（Pitfall 2）
  - 不修改任何 src/ 或 tests/ 代码（验收阶段不调整实现，否则需回退到 Wave 2）
  - 不修改 benches/bench_csv.rs（确保对比可比性）
  - 若机器负载导致 criterion 检测到噪声警告，记录在 SUMMARY 但不阻断（噪声警告是建议性，不影响测量数值）
  - 不得手工编辑 estimates.json 的数值（必须以 criterion 实测为准）

  执行时间预估：criterion bench 约 3-5 分钟 × 2 次 = 6-10 分钟，jemalloc 测试 < 30 秒。
  </action>
  <verify>
    <automated>test -f benches/baselines/csv_export/10000/phase44-after/estimates.json && python3 -c "import json; b=json.load(open('benches/baselines/csv_export/10000/phase44-before/estimates.json'))['median']['point_estimate']; a=json.load(open('benches/baselines/csv_export/10000/phase44-after/estimates.json'))['median']['point_estimate']; print(f'before={b:.0f}ns, after={a:.0f}ns, change={(a-b)/b*100:+.2f}%'); assert a < b * 1.05, f'PERF-01 regression: after {a} > before*1.05 {b*1.05}'" && test -s /tmp/phase44-criterion.log && test -s /tmp/phase44-jemalloc-after.log</automated>
  </verify>
  <acceptance_criteria>
    - 三个目录 benches/baselines/csv_export/{1000,10000,50000}/phase44-after/ 均存在且包含 estimates.json
    - /tmp/phase44-criterion.log 存在且 > 0 字节（包含完整 criterion stdout）
    - /tmp/phase44-jemalloc-after.log 存在且 > 0 字节（包含 jemalloc delta 输出）
    - csv_export/10000 优化后 median 数值 < 优化前 median * 1.05（允许 ±5% 噪声窗口的非回退判断；若实际优化预期为下降则严格 after < before）
    - jemalloc Wave 3 delta 数值 < Wave 0 delta 数值（PERF-02 量化下降；从 44-01-SUMMARY.md 与 /tmp/phase44-jemalloc-after.log 比对）
    - /tmp/phase44-data.json 存在，包含 before/after × 三个规模的 median 数值与 throughput 计算
    - 未修改任何 src/、tests/、benches/*.rs 代码（git status 仅显示 baselines/ 新增文件）
  </acceptance_criteria>
  <done>phase44-after criterion baseline 三个目录已保存，criterion 对比 phase44-before 显示中位数下降或持平（不回退），jemalloc delta 显示量化下降，数据汇总写入 /tmp/phase44-data.json 供 Task 2 使用。</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: 在 benches/BENCHMARKS.md 追加 Phase 44 验收段落并全套质量门禁</name>
  <read_first>
    - benches/BENCHMARKS.md（搜索 "## Phase 42" 段落作为模板，确认其完整格式 — Date/Goal/Test environment/数据表/<details>/结论 checklist）
    - /tmp/phase44-data.json（Task 1 产生的数据）
    - /tmp/phase44-criterion.log（Task 1 产生的 criterion 原文输出）
    - /tmp/phase44-jemalloc-after.log（Task 1 产生的 jemalloc 输出）
    - .planning/phases/44-hotpath/44-01-SUMMARY.md（Wave 0 jemalloc baseline 数值）
    - .planning/phases/44-hotpath/44-PATTERNS.md（第 234-271 行：BENCHMARKS.md 段落结构）
  </read_first>
  <files>benches/BENCHMARKS.md</files>
  <action>
  在 benches/BENCHMARKS.md 文件末尾追加 Phase 44 段落，遵循文件内现有格式（参考 Phase 42 第 509-555 行）。段落结构：

  1. 一级 H2 标题：`## Phase 44 — 热路径与内存优化（v1.11）`

  2. 元数据段（4 行）：
     - **Date:** 2026-05-24（或实际执行日期）
     - **Goal:** 通过 H-3（Arc<Vec<ParamValue>>）与 H-4（16MB BufWriter）优化实现 PERF-01/PERF-02
     - **Test environment:** Apple Silicon (Darwin 25.5.0), release build (LTO fat + strip + panic=abort + opt-level=3)
     - **Optimizations applied:** ParamBuffer Arc 改造（消除 H-3 Vec clone）+ CSV BufWriter 2MB → 16MB

  3. csv_export 对比表格（来自 /tmp/phase44-data.json）：

     ### csv_export 吞吐量对比（PERF-01）

     | Records | phase44-before Median | phase44-after Median | Change | Before Throughput | After Throughput |
     |--------:|----------------------:|---------------------:|-------:|------------------:|-----------------:|
     | 1,000   | {b1000_ns} ns         | {a1000_ns} ns        | {pct}% | {b1000_thr} rec/s | {a1000_thr} rec/s |
     | 10,000  | {b10000_ns} ns        | {a10000_ns} ns       | {pct}% | {b10000_thr} rec/s| {a10000_thr} rec/s|
     | 50,000  | {b50000_ns} ns        | {a50000_ns} ns       | {pct}% | {b50000_thr} rec/s| {a50000_thr} rec/s|

     吞吐量格式化为 "X.XX M rec/s"（除以 1e6，保留 2 位小数）；change 列显示百分比变化（正数为吞吐量提升）。

  4. jemalloc 对比段（PERF-02）：

     ### jemalloc 堆分配对比（PERF-02）

     | 阶段 | allocated delta (10000 records) | 相对变化 |
     |-----:|--------------------------------:|---------:|
     | Wave 0 (优化前) | {w0_delta} bytes | baseline |
     | Wave 3 (优化后) | {w3_delta} bytes | {pct}% |

     测量方法：tests/jemalloc_peak.rs 通过 tikv_jemalloc_ctl::stats::allocated 在 handle_run 前后两次 epoch.advance() + read 计算 delta。
     注意：此值为**累计分配字节**，非 RSS 峰值（per RESEARCH.md A2 假设）。

  5. <details> 包裹 criterion 原文输出（从 /tmp/phase44-criterion.log 完整复制）：

     <details>
     <summary>cargo bench --bench bench_csv --baseline phase44-before（Phase 44 对比输出）</summary>

     ```
     {完整 criterion stdout 内容}
     ```

     </details>

  6. <details> 包裹 jemalloc 原文输出（从 /tmp/phase44-jemalloc-after.log 关键行复制）：

     <details>
     <summary>cargo test test_jemalloc_peak_baseline -- --nocapture（Phase 44 after 测量）</summary>

     ```
     {jemalloc delta 输出行 + test result 行}
     ```

     </details>

  7. 结论 checklist：

     ### 结论

     - [x] **PERF-01**: csv_export 吞吐量提升（基于 phase44-before baseline 对比，criterion 显示 "Performance has improved" 或 median 下降）
     - [x] **PERF-02**: jemalloc 堆分配 delta 下降（从 {w0_delta} bytes 降至 {w3_delta} bytes，降幅 {pct}%）
     - [x] 全套测试通过（cargo test 无回归）
     - [x] 未引入新 unsafe 代码（D-06）
     - [x] CLAUDE.md BufWriter 16MB 描述与代码一致

     若 PERF-01 或 PERF-02 任一项未达成 → checklist 对应行改为 `[ ]` 并在下方加注失败原因（不可掩盖失败）。

  追加完成后，运行全套质量门禁确认：
     - cargo build --release
     - cargo test
     - cargo clippy --all-targets -- -D warnings
     - cargo fmt --check

  约束：
  - 不修改 BENCHMARKS.md 其他段落（仅追加 Phase 44 段，前面 Phase 33/42 等不动）
  - 数据必须精确反映 /tmp/phase44-data.json 与 /tmp/phase44-criterion.log 实测值
  - 不得伪造数据；若 PERF-01 或 PERF-02 失败，必须如实记录失败并停止后续提交（触发 gap closure）
  </action>
  <verify>
    <automated>grep -F '## Phase 44' benches/BENCHMARKS.md && grep -F 'PERF-01' benches/BENCHMARKS.md | tail -3 && grep -F 'PERF-02' benches/BENCHMARKS.md | tail -3 && cargo build --release 2>&1 | tail -3 && cargo test 2>&1 | tail -10 | grep -E 'test result: ok' && cargo clippy --all-targets -- -D warnings && cargo fmt --check</automated>
  </verify>
  <acceptance_criteria>
    - benches/BENCHMARKS.md 包含 `## Phase 44` 标题（grep 匹配 1 次）
    - Phase 44 段落包含 "PERF-01" 与 "PERF-02" 字符串（结论 checklist）
    - Phase 44 段落包含 csv_export 数据表（三行 records: 1,000 / 10,000 / 50,000）
    - Phase 44 段落包含 jemalloc 数据表（Wave 0 / Wave 3 两行）
    - Phase 44 段落包含 <details> 块至少 2 个（criterion + jemalloc 原文输出）
    - 数据表中的数值与 /tmp/phase44-data.json 精确一致（不得手工修改）
    - `cargo build --release` 退出码 0
    - `cargo test` 全部通过
    - `cargo clippy --all-targets -- -D warnings` 退出码 0
    - `cargo fmt --check` 退出码 0
    - 其他 phase 段落（Phase 33、42 等）未被修改（git diff 仅显示追加内容）
  </acceptance_criteria>
  <done>benches/BENCHMARKS.md 追加 Phase 44 段落，包含 PERF-01/PERF-02 完整数据与结论 checklist，全套质量门禁通过。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 验收数据 / 文档 | criterion + jemalloc 实测数值通过 BENCHMARKS.md 进入项目历史记录，供后续 phase 对比 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-08 | Tampering | 数据伪造（BENCHMARKS.md 数值偏离实测） | mitigate | Task 2 acceptance criteria 强制数据来源于 /tmp/phase44-data.json（自动化生成）；criterion estimates.json 是机器写入不可手工编辑 |
| T-44-09 | Repudiation | 验收失败被掩盖 | mitigate | Task 1/2 显式要求 PERF-01/PERF-02 失败时如实记录失败并停止（不得进入 git commit），由 gap closure 流程后续处理 |
| T-44-10 | Information Disclosure | criterion 原文输出包含环境路径泄漏 | accept | benches/ 目录已在项目内公开；路径泄漏无安全敏感性 |

注：验收阶段无代码变更，威胁面仅限于文档诚信问题，通过 acceptance criteria 强制 traceability 缓解。
</threat_model>

<verification>
- `benches/baselines/csv_export/{1000,10000,50000}/phase44-after/estimates.json` 均存在
- `benches/BENCHMARKS.md` 包含完整 Phase 44 段落（标题、数据表 ×2、details ×2、结论 checklist）
- csv_export/10000 phase44-after median ≤ phase44-before median（PERF-01）
- jemalloc Wave 3 delta < Wave 0 delta（PERF-02）
- 全套质量门禁通过（build/test/clippy/fmt）
- 未修改任何 src/、tests/、benches/*.rs 实现代码（git diff 仅显示 baselines/ 与 BENCHMARKS.md）
</verification>

<success_criteria>
1. phase44-after criterion baseline 三个目录均存在并包含完整 estimates.json
2. PERF-01 验收通过：csv_export 优化后 median 不回退（理想：下降并触发 criterion "improved" 提示）
3. PERF-02 验收通过：jemalloc 优化后 delta < Wave 0 baseline delta
4. benches/BENCHMARKS.md 追加 Phase 44 段落，含完整数据表、原文 details、结论 checklist
5. 全套质量门禁通过（build --release、test、clippy、fmt）
6. 全程未修改实现代码（验收阶段纯测量与记录）
</success_criteria>

<output>
Create `.planning/phases/44-hotpath/44-03-SUMMARY.md` when done, recording:
- PERF-01 验收结果（before/after 三组 median 与 throughput，change %）
- PERF-02 验收结果（before/after jemalloc delta，change %）
- criterion 输出关键提示（"Performance has improved" / "No change" / "Regressed"）
- benches/BENCHMARKS.md 新增段落的行号范围
- 全套质量门禁退出码
- 若有任一 requirement 未达成，详细说明失败原因并标记需 gap closure
</output>
