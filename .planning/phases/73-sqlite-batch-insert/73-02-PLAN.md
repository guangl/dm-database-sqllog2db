---
phase: 73-sqlite-batch-insert
plan: 02
type: execute
wave: 2
depends_on:
  - "73-01"
files_modified:
  - benches/bench_sqlite.rs
  - benches/BENCHMARKS.md
autonomous: true
requirements:
  - SQLITE-02

must_haves:
  truths:
    - "benches/bench_sqlite.rs 的 make_config 接受 multi_row_batch_size: usize 第四参数，并注入 TOML 字段"
    - "所有现有 make_config 调用更新为传入 64 作为 multi_row_batch_size，保持原有行为不变"
    - "新增 bench_sqlite_multi_row_insert 函数，benchmark group 名为 sqlite_multi_row"
    - "benchmark 对比 multi_row_batch_size = 1 / 16 / 32 / 64 共四档，覆盖 n=10_000 和 n=50_000 两种规模"
    - "新 benchmark group 注册到 criterion_group! 宏中"
    - "cargo bench 可以成功运行新 benchmark group 并输出 throughput 数据"
    - "BENCHMARKS.md 追加 Phase 73 段落，记录 sqlite_multi_row 基准结果与 multi_row=64 vs multi_row=1 的量化对比"
  artifacts:
    - path: "benches/bench_sqlite.rs"
      provides: "make_config 新增 multi_row_batch_size 参数 + bench_sqlite_multi_row_insert group + criterion_group 注册"
      contains: "multi_row_batch_size"
    - path: "benches/BENCHMARKS.md"
      provides: "Phase 73 benchmark 结果段落（sqlite_multi_row group，multi_row=1 vs 64 吞吐量对比）"
      contains: "Phase 73"
  key_links:
    - from: "benches/bench_sqlite.rs::make_config"
      to: "src/config/exporter.rs::SqliteExporterConfig::multi_row_batch_size"
      via: "TOML 字段 multi_row_batch_size 由 serde 解析为 SqliteExporterConfig"
      pattern: "multi_row_batch_size"
    - from: "benches/bench_sqlite.rs::bench_sqlite_multi_row_insert"
      to: "dm_database_sqllog2db::cli::run::handle_run"
      via: "同现有 bench 函数，直接调用 handle_run 触发 SqliteExporter 走 flush_batch 路径"
      pattern: "handle_run"
---

<objective>
扩展 benches/bench_sqlite.rs，新增 bench_sqlite_multi_row_insert benchmark group，对比 multi_row_batch_size=1（单行基准）vs 16/32/64（批量模式）的吞吐量，量化 SQLITE-01 的性能收益（SQLITE-02）。运行 benchmark 后将结果追加至 benches/BENCHMARKS.md Phase 73 段落。

Purpose: 满足 SQLITE-02 验收条件——criterion 报告中 multi_row=64 vs multi_row=1 的 throughput 差值即为"量化提升"证据。
Output: 扩展后的 bench_sqlite.rs（含 4 档对比 benchmark）+ BENCHMARKS.md Phase 73 结果段落。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/73-sqlite-batch-insert/73-CONTEXT.md
@.planning/phases/73-sqlite-batch-insert/73-RESEARCH.md
@.planning/phases/73-sqlite-batch-insert/73-PATTERNS.md
@benches/bench_sqlite.rs
@benches/BENCHMARKS.md
@src/config/exporter.rs

<interfaces>
<!-- 现有关键接口，executor 直接使用，无需再次探索 -->

From benches/bench_sqlite.rs（Plan 01 执行后）:
- `fn make_config(sqllog_dir: &Path, bench_dir: &Path, batch_size: usize) -> Config`
  （Plan 02 扩展为第四参数 `multi_row_batch_size: usize`）
- `fn bench_sqlite_export(c: &mut Criterion)`：合成 benchmark，3 个 n 值
- `fn bench_sqlite_single_row(c: &mut Criterion)`：batch_size=1 单行对照组
- `criterion_group!(benches, bench_sqlite_export, bench_sqlite_single_row, bench_sqlite_real_file)`

From src/config/exporter.rs（Plan 01 执行后）:
- `SqliteExporterConfig.multi_row_batch_size: usize`（默认 64，范围 [1, 64]）

From benches/BENCHMARKS.md:
- Phase 72 段落为最新内容，Phase 73 追加在其后

From dm_database_sqllog2db::cli::run:
- `handle_run(cfg, parallel: bool, watch: bool, cancel, progress)` — 同现有 bench 调用
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 扩展 make_config 并新增 bench_sqlite_multi_row_insert group</name>
  <files>benches/bench_sqlite.rs</files>
  <read_first>
    benches/bench_sqlite.rs
    src/config/exporter.rs
    .planning/phases/73-sqlite-batch-insert/73-PATTERNS.md
    .planning/phases/73-sqlite-batch-insert/73-CONTEXT.md
  </read_first>
  <action>
    在 benches/bench_sqlite.rs 中进行以下修改：

    1. 扩展 make_config 签名，新增第四参数 `multi_row_batch_size: usize`，在 TOML 模板的
       `[exporter.sqlite]` 节末尾追加一行 `multi_row_batch_size = {multi_row_batch_size}`
       （与 batch_size 行格式一致）。

    2. 更新所有现有 make_config 调用，在原有参数后追加 `64` 作为 multi_row_batch_size，
       保持现有 bench_sqlite_export、bench_sqlite_single_row、bench_sqlite_real_file 行为不变：
       - bench_sqlite_export 中：`make_config(&sqllog_dir, &bench_dir, 10_000, 64)`
       - bench_sqlite_single_row 中：`make_config(&sqllog_dir, &bench_dir, 1, 64)`
         注意：bench_sqlite_single_row 使用 batch_size=1（控制事务间隔），multi_row_batch_size 仍为 64
       - bench_sqlite_real_file 中：`make_config(&real_dir, &bench_dir, 10_000, 64)`

    3. 在 bench_sqlite_single_row 函数之后、bench_sqlite_real_file 之前新增函数
       `fn bench_sqlite_multi_row_insert(c: &mut Criterion)`：
       - bench_dir: `bench_common::bench_target_dir("bench_sqlite_multi_row")`
       - sqllog_dir: `bench_dir.join("sqllogs")`
       - `fs::create_dir_all(&sqllog_dir).unwrap()`
       - group 名: `"sqlite_multi_row"`，`group.sample_size(20)`
       - 外层循环：`for &n in &[10_000usize, 50_000]`
         - `fs::write(sqllog_dir.join("bench.log"), bench_common::synthetic_log(n)).unwrap()`
         - 内层循环：`for &multi_row_size in &[1usize, 16, 32, 64]`
           - `let cfg = make_config(&sqllog_dir, &bench_dir, 10_000, multi_row_size)`
           - `group.throughput(Throughput::Elements(n as u64))`
           - `group.bench_with_input(BenchmarkId::new(format!("n={n}"), format!("multi_row={multi_row_size}")), &cfg, |b, cfg| { b.iter(|| { handle_run(cfg, true, false, &Arc::new(AtomicBool::new(false)), None).unwrap(); }); })`
       - `group.finish()`

    4. 在 criterion_group! 宏中将 `bench_sqlite_multi_row_insert` 插入到
       `bench_sqlite_single_row` 与 `bench_sqlite_real_file` 之间：
       `criterion_group!(benches, bench_sqlite_export, bench_sqlite_single_row, bench_sqlite_multi_row_insert, bench_sqlite_real_file)`

    约束：函数体 ≤ 40 行（bench_sqlite_multi_row_insert 约 20 行，符合要求）；不引入新 use 语句（所有标识符已在文件顶部导入：Criterion、BenchmarkId、Throughput、handle_run、Arc、AtomicBool、fs、bench_common）。
  </action>
  <verify>
    <automated>cargo build --bench bench_sqlite --features sqlite 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - benches/bench_sqlite.rs 的 make_config 签名包含第四参数 `multi_row_batch_size: usize`
    - make_config 生成的 TOML 字符串包含 `multi_row_batch_size = {multi_row_batch_size}` 行
    - 现有三个 bench 函数对 make_config 的调用均更新为四参数形式，最后一个参数为 64
    - benches/bench_sqlite.rs 包含函数 `bench_sqlite_multi_row_insert`
    - criterion_group! 包含 `bench_sqlite_multi_row_insert`
    - `cargo build --bench bench_sqlite --features sqlite` 退出码 0，无编译错误或警告
    - `cargo clippy --all-targets -- -D warnings` 通过（benches 目录包含在内）
  </acceptance_criteria>
  <done>make_config 扩展完成；bench_sqlite_multi_row_insert 函数已注册；编译无错误/警告。</done>
</task>

<task type="auto">
  <name>Task 2: 运行 benchmark 并将结果追加至 BENCHMARKS.md</name>
  <files>benches/BENCHMARKS.md</files>
  <read_first>
    benches/BENCHMARKS.md
    benches/bench_sqlite.rs
    .planning/phases/73-sqlite-batch-insert/73-CONTEXT.md
  </read_first>
  <action>
    1. 以 release profile 运行新 benchmark group，捕获输出：
       `cargo bench --bench bench_sqlite --features sqlite sqlite_multi_row 2>&1`

    2. 从 criterion 输出中提取每个 (n, multi_row_size) 组合的 throughput 数值（records/sec 或 elem/sec）。

    3. 在 benches/BENCHMARKS.md 的末尾（Phase 72 段落之后）追加 Phase 73 段落，格式参照
       Phase 72 段落风格：

       ```
       ## Phase 73 — SQLite batch INSERT（v1.21）

       **Date:** {今日日期 YYYY-MM-DD}
       **Goal:** multi-row batch INSERT 吞吐量量化（SQLITE-02）
       **Test environment:** {platform}，release build

       ### SQLite multi-row INSERT 对比（criterion，sqlite_multi_row group）

       | n | multi_row_batch_size | throughput (elem/s) |
       |---|----------------------|---------------------|
       | 10,000 | 1  | {value} |
       | 10,000 | 16 | {value} |
       | 10,000 | 32 | {value} |
       | 10,000 | 64 | {value} |
       | 50,000 | 1  | {value} |
       | 50,000 | 16 | {value} |
       | 50,000 | 32 | {value} |
       | 50,000 | 64 | {value} |

       **量化收益（SQLITE-02 验收）：**
       - n=10,000：multi_row=64 vs multi_row=1 吞吐量提升 {X}%（{a} → {b} elem/s）
       - n=50,000：multi_row=64 vs multi_row=1 吞吐量提升 {X}%（{a} → {b} elem/s）

       ### 结论

       - [x] SQLITE-02：benchmark 已量化 multi-row INSERT 相较于单行模式的吞吐量提升
       - v1.20 baseline 对比：{是否改善/持平，引用 criterion "Performance has improved" 输出}

       ---
       ```

    约束：数值直接来自 criterion 输出，不得估算；若 criterion 未输出精确 elem/s 而仅输出时间，
    则用 n / mean_time_secs 计算并标注"计算值"；不修改 BENCHMARKS.md 其他段落。
  </action>
  <verify>
    <automated>grep -c "Phase 73" benches/BENCHMARKS.md && grep -cE "multi_row=(64|1).+[0-9]|[0-9]+.+(elem|records)/s" benches/BENCHMARKS.md</automated>
  </verify>
  <acceptance_criteria>
    - `cargo bench --bench bench_sqlite --features sqlite sqlite_multi_row` 退出码 0，输出包含 "sqlite_multi_row" group 结果
    - benches/BENCHMARKS.md 包含字符串 "Phase 73"
    - benches/BENCHMARKS.md Phase 73 段落含量化数值行（至少包含 multi_row=64 与 multi_row=1 的吞吐量数字，如 "| 10,000 | 64 | 1234567 |"）
    - `grep -E "multi_row=(64|1).+[0-9]|[0-9]+.+(elem|records)/s" benches/BENCHMARKS.md` 匹配数 >= 2
    - BENCHMARKS.md Phase 73 段落包含 `[x] SQLITE-02` 验收标记
    - 原有 Phase 72 及之前段落内容未被修改
  </acceptance_criteria>
  <done>benchmark 运行完成；BENCHMARKS.md Phase 73 段落已追加，含量化吞吐量对比和 SQLITE-02 验收标记。</done>
</task>

</tasks>

<verification>
全量执行：
- `cargo build --bench bench_sqlite --features sqlite` 退出码 0（无编译错误/警告）
- `cargo clippy --all-targets -- -D warnings` 通过
- `cargo bench --bench bench_sqlite --features sqlite sqlite_multi_row` 退出码 0，输出 criterion sqlite_multi_row group 吞吐量数据
- `grep -c "Phase 73" benches/BENCHMARKS.md` >= 1
- `grep -c "multi_row_batch_size" benches/bench_sqlite.rs` >= 3（make_config 参数 + TOML 模板 + 循环变量）
- `grep -c "bench_sqlite_multi_row_insert" benches/bench_sqlite.rs` >= 2（函数定义 + criterion_group! 注册）
</verification>

<success_criteria>
- SQLITE-02 benchmark 实现完成（bench_sqlite_multi_row_insert group 含 1/16/32/64 四档对比）
- criterion 可量化输出 multi_row=64 vs multi_row=1 的吞吐量差值
- BENCHMARKS.md 追加 Phase 73 段落，含量化数据与 [x] SQLITE-02 验收标记
- make_config 扩展向后兼容，现有三个 bench 函数行为不变
- cargo build/clippy 通过，无新编译错误/警告
</success_criteria>

<output>
Create `.planning/phases/73-sqlite-batch-insert/73-02-SUMMARY.md` when done.
</output>
