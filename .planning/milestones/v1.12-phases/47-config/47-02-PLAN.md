---
phase: 47-config
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/cli/init.rs
autonomous: true
requirements:
  - CONFIG-01
must_haves:
  truths:
    - "init 生成的 config.toml 中 exporter.csv.append 字段上方有英文注释说明用途与合法值"
    - "init 生成的 config.toml 中 exporter.sqlite.database_url / table_name / overwrite / append 四个字段上方均有英文注释"
    - "新增注释风格与已有注释一致：单行 `# <Description>: <valid values>` 形式，位于字段上方"
    - "现有 handle_init 集成测试不变（assert content.contains 不受新增注释行影响）"
  artifacts:
    - path: "src/cli/init.rs"
      provides: "更新后的 CONFIG_TEMPLATE_EN 常量，覆盖 5 个新增注释字段"
      contains: "Append to existing CSV file"
  key_links:
    - from: "src/cli/init.rs::CONFIG_TEMPLATE_EN"
      to: "用户运行 sqllog2db init -o config.toml 输出"
      via: "fs::write 直接写入文件"
      pattern: "CONFIG_TEMPLATE_EN"
---

<objective>
为 `CONFIG_TEMPLATE_EN` 常量中尚缺注释的 5 个配置字段补充行内注释（位于字段上方独立行，遵循已有英文注释风格）：
- `exporter.csv.append`
- `exporter.sqlite.database_url`
- `exporter.sqlite.table_name`
- `exporter.sqlite.overwrite`
- `exporter.sqlite.append`

Purpose: 让用户 `sqllog2db init` 生成的 config.toml 中每个字段都有"用途 + 合法值示例"说明，对应 CONFIG-01 需求"`init` 命令生成带行内注释的配置模板，每个字段标注用途和合法值示例"。

Output: 更新后的 `src/cli/init.rs::CONFIG_TEMPLATE_EN` 常量；新增 1 个集成测试断言新注释存在。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/47-config/47-CONTEXT.md
@.planning/phases/47-config/47-PATTERNS.md

@src/cli/init.rs
@tests/integration.rs

<interfaces>
<!-- 字段位置与已有注释风格参考 -->

已有注释风格（src/cli/init.rs lines 62-73 取样）:
```toml
[sqllog]
# SQL log path: directory, single file, or glob pattern (e.g. "./logs/2025-*.log")
path = "sqllogs"

[logging]
# Application log file path
file = "logs/sqllog2db.log"
# Log level: trace | debug | info | warn | error
level = "info"
# Log retention in days (1-365)
retention_days = 7
```

需补充的字段当前状态（src/cli/init.rs lines 121-132）:
```toml
[exporter.csv]
file = "outputs/sqllog.csv"
overwrite = true
append = false                        # ← 无注释

# [exporter.sqlite]
# database_url = "export/sqllog2db.db"  # ← 无注释
# table_name = "sqllog_records"          # ← 无注释
# overwrite = true                       # ← 无注释
# append = false                         # ← 无注释
```

注意：sqlite section 整体被注释掉（用户取消注释即启用），新增的字段注释行也需以 `#` 开头并保持井号对齐的视觉一致。
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 在 CONFIG_TEMPLATE_EN 中为 5 个字段补充注释，并新增覆盖测试</name>
  <files>src/cli/init.rs, tests/integration.rs</files>
  <read_first>
    - src/cli/init.rs (完整文件，重点 lines 61-133 的 CONFIG_TEMPLATE_EN 常量与已有注释风格)
    - .planning/phases/47-config/47-PATTERNS.md ("补充内容目标字段"表)
    - .planning/phases/47-config/47-CONTEXT.md (D-04 决策)
    - tests/integration.rs (lines 122-173 现有 handle_init 测试，确保新注释不与既有断言冲突)
  </read_first>
  <behavior>
    - 测试 test_init_template_has_csv_append_comment: 生成模板后 content 含 `Append to existing CSV file` 字符串
    - 测试 test_init_template_has_sqlite_field_comments: 生成模板后 content 同时含 5 段注释关键字（database_url 注释、table_name 注释、sqlite overwrite 注释、sqlite append 注释、ASCII identifiers 提示）
    - 行为：注释位置在对应字段**上方独立行**，不内联在字段同一行（与已有 `# Application log file path` 风格一致）
    - 行为：sqlite section 整体注释状态不变（所有 sqlite 字段行仍以 `# ` 开头），但其上方新增的说明性注释行同样以 `# ` 开头；两者通过缩进/语义区分
    - 行为：所有新增注释英文（保持 EN 模板一致），不混入中文
    - 行为：现有 5 个 handle_init 集成测试全部仍通过
  </behavior>
  <action>
    修改 `src/cli/init.rs::CONFIG_TEMPLATE_EN` 常量字符串：

    **CSV 段（line 122-125 区域）改为：**
    - `file = "outputs/sqllog.csv"` 之上添加 `# CSV output file path` 注释
    - `overwrite = true` 之上添加 `# Drop and recreate the file before writing (true/false)` 注释
    - `append = false` 之上添加 `# Append to existing CSV file instead of overwriting (true/false)` 注释
    - 注意 file 和 overwrite 当前**也无注释**，本任务一并补齐（CONTEXT D-04 表面只点了 append + sqlite.*，但 CONFIG-01 要求"每个字段"，且 PATTERNS.md 已建议保持风格一致——补全 csv.file 和 csv.overwrite 同样必要，且工作量极小）

    **SQLite 段（line 128-132 区域）改为（保持整段被 # 注释的现状，但每个字段行的上方独立新增说明注释行）：**
    - `# [exporter.sqlite]` 这一行保留不变
    - 在 `# database_url = "export/sqllog2db.db"` 之上加 `# SQLite database file path`
    - 在 `# table_name = "sqllog_records"` 之上加 `# Table name to write records into (ASCII identifiers only: [A-Za-z_][A-Za-z0-9_]*)`
    - 在 `# overwrite = true` 之上加 `# Drop and recreate the table before writing (true/false)`
    - 在 `# append = false` 之上加 `# Append rows to existing table instead of overwriting (true/false)`

    注释格式规范（与已有注释 100% 一致）：
    - 单行注释，`#` 后跟一个空格
    - 形式：`# <Description>: <valid values or example>` 或 `# <Description>`
    - 不要使用多行注释、不要使用 `///`、不要在注释中加 emoji
    - 仅英文，不混中文（CONFIG_TEMPLATE_EN 是英文模板）

    **新增集成测试**（追加到 `tests/integration.rs` 现有 handle_init 测试段之后，约 line 173 之后）：

    测试 `test_init_template_has_csv_append_comment`：
    - 用 `tempfile::TempDir` + `handle_init` 生成模板
    - 读 content，断言 `content.contains("Append to existing CSV file instead of overwriting")`
    - 断言 `content.contains("CSV output file path")`

    测试 `test_init_template_has_sqlite_field_comments`：
    - 同样生成模板
    - 断言 content 同时含全部以下子串：
      - `SQLite database file path`
      - `Table name to write records into`
      - `ASCII identifiers only`
      - `Drop and recreate the table`
      - `Append rows to existing table`

    不要：
    - 不要重排 CSV/SQLite 段的字段顺序
    - 不要在注释中改动字段默认值（`overwrite = true` / `append = false` / 各路径示例值保持不变）
    - 不要新增/移除任何字段；本 phase 不改 schema
    - 不要破坏 `# [exporter.sqlite]` 与下方各 `# field = value` 的整体可一键解除注释的形态
  </action>
  <verify>
    <automated>cargo test --test integration handle_init 2>&1 | grep "test result" && cargo test --test integration test_init_template_has 2>&1 | grep "test result" && cargo clippy --all-targets -- -D warnings 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - 源断言: `grep -c 'Append to existing CSV file' src/cli/init.rs` 输出 ≥ 1
    - 源断言: `grep -c 'SQLite database file path' src/cli/init.rs` 输出 ≥ 1
    - 源断言: `grep -c 'Table name to write records into' src/cli/init.rs` 输出 ≥ 1
    - 源断言: `grep -c 'ASCII identifiers only' src/cli/init.rs` 输出 ≥ 1
    - 源断言: `grep -c 'Drop and recreate the table' src/cli/init.rs` 输出 ≥ 1
    - 源断言: `grep -c 'Append rows to existing table' src/cli/init.rs` 输出 ≥ 1
    - 源断言: `grep -c 'CSV output file path' src/cli/init.rs` 输出 ≥ 1
    - 源断言: `grep -c 'fn test_init_template_has_csv_append_comment' tests/integration.rs` 输出 1
    - 源断言: `grep -c 'fn test_init_template_has_sqlite_field_comments' tests/integration.rs` 输出 1
    - 行为断言: `cargo test --test integration test_init_template_has` 两个新测试均通过
    - 行为断言: `cargo test --test integration handle_init` 现有 5 个测试全部通过（无回归）
    - 行为断言: `cargo run -- init -o /tmp/check_phase47.toml --force` 后 `/tmp/check_phase47.toml` 中可 grep 到上述 7 个新注释子串
    - clippy 断言: `cargo clippy --all-targets -- -D warnings` 退出码 0
  </acceptance_criteria>
  <done>CONFIG_TEMPLATE_EN 含 5+2 个补充注释；新增 2 个测试通过；handle_init 现有测试无回归；clippy 零警告</done>
</task>

</tasks>

<verification>
- `cargo test` 全套通过（lib + integration），含本 plan 新增的 2 个测试
- `cargo clippy --all-targets -- -D warnings` 退出码 0
- `cargo fmt --check` 退出码 0
- 手动 smoke：`cargo run -- init -o /tmp/x.toml --force && cat /tmp/x.toml | grep -E '^# (Append|SQLite database|Table name|Drop and recreate|CSV output)'` 应输出 ≥ 5 行匹配
</verification>

<success_criteria>
1. `sqllog2db init -o config.toml` 生成的文件中 `exporter.csv.{file,overwrite,append}` 三个字段各自上方均有英文注释
2. `[exporter.sqlite]` section 中 `database_url / table_name / overwrite / append` 四个字段（即便整体被 `#` 注释掉）各自上方均有英文说明注释
3. 新增注释风格与已有注释（如 `# Log level: trace | debug | info | warn | error`）一致：单行 `#` + 一个空格 + 描述 + 合法值/示例
4. 现有 5 个 handle_init 集成测试无回归
5. clippy + test + fmt 三道质量门禁全部通过
</success_criteria>

<output>
完成后创建 `.planning/phases/47-config/47-02-SUMMARY.md`，包含：
- 修改文件清单（init.rs diff 行数 + tests/integration.rs 新增测试名）
- 7 个新注释行的具体文本列表
- D-04 决策符合性说明（PATTERNS.md "补充内容目标字段"5 项全覆盖 + CSV file/overwrite 也补齐）
</output>
