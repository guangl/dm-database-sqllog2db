---
phase: 32-cleanup-project-structure
plan: 02
type: execute
wave: 2
depends_on: [32-01]
files_modified:
  - src/exporter/csv/companion.rs
  - src/exporter/csv/mod.rs
  - src/exporter/mod.rs
  - src/exporter/tests.rs
  - src/exporter/csv/tests.rs
  - src/cli/opts.rs
  - src/main.rs
  - src/cli/run/mod.rs
  - src/cli/run/parallel.rs
  - src/cli/run/processor.rs
autonomous: true
requirements: [RM-08]
must_haves:
  truths:
    - "src/exporter/csv/companion.rs 文件已删除"
    - "Exporter trait 中没有 write_template_stats 方法"
    - "ExporterKind/ExporterManager 中没有 write_template_stats 方法"
    - "csv/mod.rs 中没有 companion 模块的导入和 write_template_stats 实现"
    - "exporter/tests.rs 和 csv/tests.rs 中没有 write_template_stats 测试"
    - "CLI opts 中不再有 Stats/Digest/Completions/SelfUpdate/Man 命令变体"
    - "Exec 变体中不再有 resume/state_file 字段"
    - "main.rs 中不再有已移除命令的分支处理和错误匹配"
    - "cli/run/mod.rs 中不再有 template/resume/charts 代码"
    - "cli/run/parallel.rs 中不再有 template/resume 参数和逻辑"
    - "cli/run/processor.rs 中不再有 TemplateAggregator 导入和逻辑"
  artifacts:
    - path: "src/exporter/csv/companion.rs"
      provides: "删除整个文件"
      min_lines: 0
    - path: "src/exporter/mod.rs"
      provides: "write_template_stats trait 方法、ExporterKind/ExporterManager 实现已删除"
    - path: "src/cli/opts.rs"
      provides: "5 个 Commands 变体、2 个 Run 字段删除"
    - path: "src/main.rs"
      provides: "stale match arms、error 匹配、启动分支删除"
    - path: "src/cli/run/mod.rs"
      provides: "resume/template/charts 全部代码删除"
---

<objective>
清理 Exporter 层和 CLI 层的残留死代码。

**Purpose:** 删除 Exporter 中已移除功能的 template companion 文件和 trait 方法；清理 CLI opts 中已删除命令的枚举变体、main.rs 匹配分支、以及 run 模块中的 template/resume/charts 逻辑。

**Output:** 1 个文件删除 + 9 个文件编辑，前后 `cargo build` 编译通过。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/32-cleanup-project-structure/32-RESEARCH.md

<interfaces>
<!-- Plan 02 依赖 Plan 01：Config 字段和 pipeline 类型已清理完成 -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 删除 companion.rs + Exporter 层 write_template_stats 及其测试</name>
  <files>src/exporter/csv/companion.rs, src/exporter/csv/mod.rs, src/exporter/mod.rs, src/exporter/tests.rs, src/exporter/csv/tests.rs</files>
  <action>
    **A. src/exporter/csv/companion.rs** (约 3KB, ~100 行):
    - 使用 `rm` 命令删除整个文件

    **B. src/exporter/csv/mod.rs:**
    - 删除 `mod companion;` 导入声明
    - 删除 `pub(crate) use self::companion::write_companion_rows;` re-export
    - 删除 `impl CsvExporter` 中 `fn write_template_stats(&self, ...)` 方法定义（约 7 行，使用 TemplateStats 记录写入 CSV 的 companion 行）

    **C. src/exporter/mod.rs:**
    - 删除 `Exporter` trait 中的 `fn write_template_stats(&self, stats: &TemplateStats) -> Result<()>` 方法签名（约 8 行，含注释）
    - 删除 `ExporterKind` 枚举中的 `fn write_template_stats(&self, ...)` 方法（约 20 行，含 `#[allow(dead_code)]` 注释）
    - 删除 `ExporterManager` 结构体中的 `fn write_template_stats(&self, ...)` 方法（约 10 行）

    **D. src/exporter/tests.rs:**
    - 删除以下 4 个测试函数:
      `test_default_write_template_stats_noop`
      `test_dry_run_write_template_stats_noop`
      `test_exporter_manager_write_template_stats_csv`
      `test_exporter_kind_dispatch_write_template_stats`

    **E. src/exporter/csv/tests.rs:**
    - 删除以下 4 个测试函数:
      `test_csv_write_template_stats`
      `test_csv_write_template_stats_explicit_path`
      `test_csv_write_template_stats_none_skips`
      `test_csv_write_template_stats_empty_path_skips`
  </action>
  <verify>
    <automated>cargo build 2>&1 | head -20</automated>
  </verify>
  <done>
    - companion.rs 文件已从文件系统删除
    - csv/mod.rs 中无 companion 引用
    - exporter/mod.rs 中无 write_template_stats 方法
    - 8 个测试函数已删除
    - cargo build 编译成功
  </done>
</task>

<task type="auto">
  <name>Task 2: 删除 CLI opts 命令变体 + main.rs match arms</name>
  <files>src/cli/opts.rs, src/main.rs</files>
  <action>
    **A. src/cli/opts.rs:**
    - 删除文件头部的 `use clap_complete::{Shell, generate};` 导入
    - 从 `Commands` 枚举中删除以下 5 个变体:
      `Stats { ... }` (约 37 行，含所有 args 定义)
      `Digest { ... }` (约 37 行，含所有 args 定义)
      `Completions { ... }` (约 5 行)
      `SelfUpdate { ... }` (约 6 行)
      `Man` (约 2 行)
    - 从 `Exec` / `Run`（具体枚举变体名以实际代码为准）中删除:
      `resume: bool` 字段
      `state_file: Option<String>` 字段
    - 删除 `impl Cli` 中的 `fn generate_completions(&self)` 方法（约 9 行）

    **B. src/main.rs:**
    注意: main.rs 在 Plan 01 中已删除 `mod charts;` 和 `mod resume;` 声明，此处清理运行时逻辑。
    
    - `exit_code_for()` 函数/匹配: 删除 `error::Error::Update(_) => 1,` 分支（约 1 行）
    - `needs_simple_logging` 匹配: 删除 `Commands::Stats { .. }` 和 `Commands::Digest { .. }` 匹配条件
    - 启动检查: 删除 `cli::update::check_for_updates_at_startup()` 调用及匹配条件中的 `Commands::SelfUpdate { .. } | Commands::Completions { .. }`
    - 命令分发 match: 依次删除以下分支（以实际代码中的变体名为准）:
      - `Commands::Completions { .. } => { ... }`
      - `Commands::SelfUpdate { .. } => { ... }`
      - `Commands::Man => { ... }`
      - `Commands::Stats { ... } => { ... }` (整个分支，约 34 行)
      - `Commands::Digest { ... } => { ... }` (整个分支，约 42 行)
    - Run 命令解构: 删除 `resume` 和 `state_file` 字段提取
    - `handle_run()` 调用: 删除 `*resume` 和 `state_file.as_deref()` 实参
    - 测试模块: 删除 `use crate::error::{..., UpdateError}` 中的 `UpdateError`
  </action>
  <verify>
    <automated>cargo build 2>&1 | head -20</automated>
  </verify>
  <done>
    - Commands 枚举仅包含 `Run`、`Init`、`Validate`、`ShowConfig`、`Preflight` 等核心变体
    - Run 变体中无 resume/state_file 字段
    - main.rs 中无 stats/digest/update/completions/man 命令分支
    - main.rs 无 UpdateError 引用
    - cargo build 编译成功
  </done>
</task>

<task type="auto">
  <name>Task 3: 删除 cli/run/ 模块中的 template/resume/charts 死代码</name>
  <files>src/cli/run/mod.rs, src/cli/run/parallel.rs, src/cli/run/processor.rs</files>
  <action>
    **A. src/cli/run/mod.rs:**
    - 删除 imports: `use crate::pipeline::template_reporter::TemplateReporter;`、`use crate::pipeline::{..., TemplateAggregator};`、`use crate::pipeline::{derive_template_report_paths, template_report_enabled};`
    - 删除完整的 `fn write_template_reports(...)` 函数定义（约 30 行）
    - `handle_run` 函数签名: 删除 `resume` 和 `state_file_override` 参数
    - `handle_run` 函数体:
      - 删除 `state_path` 计算（约 2 行）
      - 删除 resume_state 所有相关代码（读取 checkpoint、跳过语义，约 10 行）
      - 删除 `do_template` 变量声明和使用
      - 并行路径: 删除 charts 生成代码块、template stats 处理代码块、resume state 保存代码块
      - 顺序路径: 删除 `TemplateAggregator::new`、resume skip 逻辑、template_agg 入参传递、resume state 保存、charts 生成、template stats 和 `write_template_reports` 调用

    **B. src/cli/run/parallel.rs:**
    - 删除 imports: `use crate::pipeline::{..., TemplateAggregator};`
    - 函数签名: 删除 `resume_state: Option<&crate::resume::ResumeState>` 参数、`do_template: bool` 参数；修改返回值删除 `Option<TemplateAggregator>`
    - 函数体: 删除 resume state skip 检查、`TemplateAggregator::new`、aggregator merge 逻辑

    **C. src/cli/run/processor.rs:**
    - 删除 imports: `use crate::pipeline::{..., TemplateAggregator};`
    - 函数签名: 删除 `mut aggregator: Option<&mut TemplateAggregator>`、`placeholder_override` 参数
    - 函数体: 删除 `if let Some(ref mut agg) = aggregator { ... }` 整个块（约 30 行，含模板聚合逻辑）
    - 删除 `aggregator.is_some()` 在 `include_pm` 逻辑中的引用
  </action>
  <verify>
    <automated>cargo build 2>&1 | head -20</automated>
  </verify>
  <done>
    - mod.rs 中无 template_reporter/TemplateAggregator/derive_template_report_paths/template_report_enabled 引用
    - mod.rs 中无 write_template_reports 函数
    - mod.rs handle_run 无 resume/charts/template 参数或逻辑
    - parallel.rs 无 TemplateAggregator/resume_state 引用
    - processor.rs 无 TemplateAggregator/placeholder_override 引用
    - cargo build 编译成功
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Phase 32 仅做代码结构清理，不涉及运行时信任边界变更。
</threat_model>

<verification>
```bash
cargo build 2>&1
cargo test --no-run 2>&1
```
预期：编译和测试编译均成功。
</verification>

<success_criteria>
- [ ] `cargo build` 编译成功
- [ ] `cargo test --no-run` 测试编译成功
- [ ] companion.rs 已删除
- [ ] Exporter 层无 write_template_stats 相关代码
- [ ] CLI opts 无已删除命令的枚举变体
- [ ] main.rs 无对应 match 分支和错误处理
- [ ] run 模块无 template/resume/charts 引用
</success_criteria>

<output>
创建 `.planning/phases/32-cleanup-project-structure/32-02-SUMMARY.md` 记录操作内容和编译结果。
</output>
