---
phase: 71-mod-rs-mod-rs-pub-use
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/stats/mod.rs
  - src/stats/runner.rs
  - src/stats/tests.rs
autonomous: true
requirements: []
must_haves:
  truths:
    - "src/stats/mod.rs 仅含 mod 声明与 pub use 重导出"
    - "crate::stats::run_stats 公开签名不变"
    - "crate::stats::StatsConfig / validate_time_str 可访问"
    - "cargo test 通过（含 5 个原集成风格 tests）"
    - "cargo clippy --all-targets -- -D warnings 通过"
  artifacts:
    - path: "src/stats/mod.rs"
      provides: "module declarations + pub use only"
    - path: "src/stats/runner.rs"
      provides: "run_stats / scan_files_into_accumulator / write_stats_output 三个函数实现"
      contains: "pub fn run_stats"
    - path: "src/stats/tests.rs"
      provides: "原 5 个 tests + 2 个 helper（make_csv_config / write_test_log）"
      contains: "#[test]"
  key_links:
    - from: "src/stats/mod.rs"
      to: "runner"
      via: "mod runner; pub use runner::run_stats"
      pattern: "pub use runner::run_stats"
    - from: "src/cli/stats/mod.rs"
      to: "crate::stats::run_stats"
      via: "调用入口"
      pattern: "crate::stats::run_stats"
---

<objective>
重构 src/stats/mod.rs（200 行）：将 `run_stats`/`scan_files_into_accumulator`/`write_stats_output` 三个函数拆分到 runner.rs，#[cfg(test)] mod tests 块拆分到 tests.rs，mod.rs 仅保留 mod 声明、pub mod、pub use 重导出。

Purpose: stats 模块入口清晰化，子模块 aggregate/config/normalize/output 保留 pub mod 暴露，runner 集中协调逻辑。
Output: 3 个文件，cargo test/clippy 全绿。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md

<interfaces>
<!-- 必须保持的导出 -->

公开：
- `crate::stats::run_stats(cfg: &Config, top_n: u32) -> Result<()>`（被 cli/stats/handler.rs 调用）
- `crate::stats::StatsConfig`（从 config 子模块 re-export，被 config/mod.rs 使用：`pub use crate::stats::config::StatsConfig`）
- `crate::stats::validate_time_str`（pub use from config）

子模块（保持 `pub mod`）：
- `pub mod aggregate;` — StatsAccumulator, SlowSqlRow, FrequentSqlRow
- `pub mod config;` — StatsConfig, validate_time_str, validate_stats_time_range
- `pub mod normalize;` — normalize_sql
- `pub mod output;` — write_csv_stats, write_sqlite_stats

私有 helper（仅 runner 内部使用）：
- `fn scan_files_into_accumulator(...)`
- `fn write_stats_output(...)`

外部依赖（runner.rs 需要的 use）：
```rust
use crate::config::Config;
use crate::error::{Error, ErrorStats, ParserError, Result};
use super::aggregate::{StatsAccumulator, SlowSqlRow, FrequentSqlRow};
use super::config;     // for validate_stats_time_range
use super::output;     // for write_csv_stats / write_sqlite_stats
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 拆分 stats/mod.rs 到 runner.rs + tests.rs</name>
  <files>src/stats/mod.rs, src/stats/runner.rs, src/stats/tests.rs</files>
  <read_first>
    - src/stats/mod.rs（全文 200 行）
    - src/stats/config.rs（确认 validate_stats_time_range 签名）
    - src/stats/aggregate.rs（确认 StatsAccumulator::new / update / into_results 签名）
    - src/stats/output.rs（确认 write_csv_stats / write_sqlite_stats 签名）
    - src/cli/stats/mod.rs（确认 crate::stats::run_stats 调用点）
    - src/config/mod.rs（确认 `pub use crate::stats::config::StatsConfig;` 使用路径）
  </read_first>
  <action>
    Step 1: 创建 `src/stats/runner.rs`：
      - 移入 mod.rs 第 14-87 行的：
        - `pub fn run_stats(cfg: &Config, top_n: u32) -> Result<()>` (保持公开)
        - `fn scan_files_into_accumulator(...)` (私有)
        - `fn write_stats_output(...)` (私有)
      - 顶部 use 完整复制：
        ```rust
        use crate::config::Config;
        use crate::error::{Error, ErrorStats, ParserError, Result};
        use super::aggregate::{self, StatsAccumulator};
        use super::config as stats_config;  // 避免与 crate::config 名冲突
        use super::output;
        ```
        实现里 `config::validate_stats_time_range` 调用改为 `stats_config::validate_stats_time_range`，其余 aggregate::SlowSqlRow / aggregate::FrequentSqlRow 通过 `aggregate::` 前缀访问。

    Step 2: 创建 `src/stats/tests.rs`：
      - 移入 mod.rs 第 89-200 行 `#[cfg(test)] mod tests { ... }` 内的全部内容（不嵌套）
      - 顶部 use：
        ```rust
        use super::runner::run_stats;
        use crate::config::{Config, CsvExporterConfig, ExporterConfig, SqllogConfig};
        use crate::error::{ConfigError, Error};
        ```
      - 保留 helper `make_csv_config` 与 `write_test_log`，5 个 #[test]：
        - test_run_stats_csv_mode_selected_when_only_csv_configured
        - test_run_stats_propagates_no_files_found
        - test_run_stats_skips_parse_errors
        - test_run_stats_rejects_invalid_from
        - test_run_stats_rejects_invalid_to

    Step 3: 改写 `src/stats/mod.rs` 为：
      ```rust
      //! SQL 统计分析模块（v1.13）：提供 SQL 标准化、聚合与输出。

      pub mod aggregate;
      pub mod config;
      pub mod normalize;
      pub mod output;

      mod runner;

      #[cfg(test)]
      mod tests;

      pub use runner::run_stats;

      // Public re-exports for lib API consumers; may be unused in the bin target.
      #[allow(unused_imports)]
      pub use config::StatsConfig;
      #[allow(unused_imports)]
      pub use config::validate_time_str;
      ```
      仅注释、mod 声明、pub use。

    Step 4: 运行质量门禁：
      - `cargo build`
      - `cargo clippy --all-targets -- -D warnings`
      - `cargo test`
      - `cargo fmt`

    注意：scan_files_into_accumulator 内调用 `crate::scanner::scan_files`（已存在 src/scanner.rs），保持不变。
  </action>
  <verify>
    <automated>cargo clippy --all-targets -- -D warnings && cargo test</automated>
    <grep-check>grep -v '^\s*//' src/stats/mod.rs | grep -vE '^\s*$' | grep -E '\bfn \w+|\bstruct \w+|\bimpl ' && echo "FAIL: mod.rs 仍含实现" || echo "OK: mod.rs 仅声明"</grep-check>
  </verify>
  <acceptance_criteria>
    - cargo clippy --all-targets -- -D warnings 退出码 0
    - cargo test 退出码 0
    - src/stats/mod.rs 过滤注释/空行后只含 `mod`/`pub mod`/`pub use`/属性宏（#[allow], #[cfg(test)]），无 `fn`/`struct`/`impl`
    - crate::stats::run_stats / StatsConfig / validate_time_str 在原路径可达
    - 5 个 tests 全部迁移到 tests.rs
    - src/config/mod.rs 中 `pub use crate::stats::config::StatsConfig;` 无需修改
  </acceptance_criteria>
  <done>
    mod.rs 简化到 ≤ 20 行（含注释），runner.rs + tests.rs 承接全部实现与测试，质量门禁全绿。
  </done>
</task>

</tasks>

<verification>
- cargo clippy --all-targets -- -D warnings 通过
- cargo test 通过
- src/stats/mod.rs grep -E '\bfn |\bstruct |\bimpl ' 无匹配
</verification>

<success_criteria>
- mod.rs 仅含 mod 声明与 pub use（≤ 20 行）
- runner.rs 接管 run_stats + 两个私有辅助函数
- tests.rs 接管全部 5 个测试 + 2 个 helper
- 全套质量门禁通过
</success_criteria>

<output>
Create `.planning/phases/71-mod-rs-mod-rs-pub-use/71-04-SUMMARY.md` when done
</output>
