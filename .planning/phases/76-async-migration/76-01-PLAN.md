---
phase: 76-async-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
autonomous: true
requirements:
  - ASYNC-01
must_haves:
  truths:
    - "运行 cargo test 显示 503 测试全绿"
    - "运行 cargo clippy --all-targets -- -D warnings 零警告"
    - "运行 cargo build --release 成功并产出约 3.8MB 二进制"
    - "Cargo.toml 同时包含 tokio (features=[\"rt-multi-thread\",\"macros\"]) 与 dm-database-parser-sqllog (features=[\"async\"])"
    - "ROADMAP.md Phase 76 行以 [x] 开头并标注 (completed 2026-06-11)"
    - "REQUIREMENTS.md ASYNC-01 checkbox 为 [x]，Traceability 表格 ASYNC-01 行第三列为 Done"
    - "STATE.md 当前 Phase 字段为 76 且 last_updated 为 2026-06-11"
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Phase 76 完成标记 + Progress 表行更新"
      contains: "[x] **Phase 76: 异步解析路径迁移**"
    - path: ".planning/REQUIREMENTS.md"
      provides: "ASYNC-01 checkbox + Traceability 行更新为 Done"
      contains: "[x] **ASYNC-01**"
    - path: ".planning/STATE.md"
      provides: "Phase 76 完成状态"
      contains: "Phase: 76"
  key_links:
    - from: "Cargo.toml"
      to: "src/main.rs"
      via: "#[tokio::main] 入口"
      pattern: "tokio::main"
    - from: "src/cli/run/parallel.rs"
      to: "AsyncLogParser"
      via: "block_in_place + Handle::current().block_on"
      pattern: "block_in_place|block_on"
    - from: ".planning/ROADMAP.md"
      to: ".planning/REQUIREMENTS.md"
      via: "ASYNC-01 同时标记完成"
      pattern: "ASYNC-01"
---

<objective>
完成 Phase 76（异步解析路径迁移）的验收与文档收尾。

异步迁移的源码改动已在 commit `65c24fd feat: complete async migration — replace async_rt bridge with AsyncLogParser` 中完整落地。本 Plan 不包含源码修改，仅做两件事：

1. **运行 ROADMAP.md 中的 5 条 Success Criteria 命令**，确认实现完整通过质量门禁；
2. **将 ROADMAP.md / REQUIREMENTS.md / STATE.md 中 ASYNC-01 与 Phase 76 的待办标记翻转为完成**，使 phase 状态与代码状态对齐。

Purpose: 让 v1.20 里程碑的最后一个 phase（76）在所有跟踪文档中状态一致，为后续里程碑发布与归档做好准备。

Output:
- 三份 planning 文档完成状态更新（ROADMAP / REQUIREMENTS / STATE）。
- 一份 Phase 76 SUMMARY 记录验收命令的实际输出。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/76-async-migration/76-CONTEXT.md
@.planning/phases/76-async-migration/76-RESEARCH.md
@.planning/phases/76-async-migration/76-PATTERNS.md
@.planning/phases/76-async-migration/76-VALIDATION.md
@CLAUDE.md
@Cargo.toml

<interfaces>
<!-- 验收时需要核查的关键 grep 锚点。无需修改源码，仅核对模式存在。 -->

src/main.rs（已实现，验证锚点）:
  - 行 95–96 包含 `#[tokio::main]` 与 `async fn main()`
  - 行 124 包含 `async fn run() -> Result<Option<(ErrorStats, bool)>>`

src/cli/run/parallel.rs（已实现，验证锚点）:
  - 包含 `tokio::runtime::Handle::current()`
  - 包含 `handle.block_on(AsyncLogParser::new(file).parse())`
  - rayon worker 内调用 `tokio::task::block_in_place(|| ...)`

src/cli/run/prescan.rs（已实现，验证锚点）:
  - 包含 `tokio::task::block_in_place(|| handle.block_on(AsyncLogParser::new(...).parse()))`

src/cli/run/{sequential,collector,processor}.rs、src/cli/run/sqlite_parallel.rs、src/scanner.rs（已实现，验证锚点）:
  - 全部为 `async fn`，调用 `AsyncLogParser::new(path).parse().await`

Cargo.toml（已实现，验证锚点）:
  - `tokio = { version = "1", features = ["rt-multi-thread", "macros"] }`
  - `dm-database-parser-sqllog = { version = "2.0.4", features = ["async"] }`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 01: 运行 SC-1/3/4/5 验收命令并核对依赖与代码锚点</name>
  <files>（仅读取，不修改源码或文档）</files>
  <read_first>
    - Cargo.toml（确认 SC-1 依赖声明存在）
    - src/main.rs（确认 D-01 入口模式）
    - src/cli/run/parallel.rs（确认 D-03 桥接模式）
    - src/cli/run/prescan.rs（确认 D-03 桥接模式）
    - src/cli/run/processor.rs（确认 D-05 async fn 模式）
    - .planning/phases/76-async-migration/76-RESEARCH.md（参照"验收命令模板"段落）
    - .planning/phases/76-async-migration/76-PATTERNS.md（参照"验证目标文件"段落）
  </read_first>
  <action>
    本任务执行 Phase 76 的 4 个非 bench 验收命令并核对 grep 锚点，确认 commit 65c24fd 的实现满足 ROADMAP Phase 76 Success Criteria #1/#3/#4/#5。所有命令必须实际运行并将完整输出（含 summary 行）写入 76-01-SUMMARY.md 的"Verification Log"段落。

    步骤 A（SC-1 依赖核对）— 运行 `grep -n "^tokio = \|^dm-database-parser-sqllog = " Cargo.toml`。要求 tokio 行包含 `features = [` 段且枚举 `"rt-multi-thread"` 与 `"macros"`，dm-database-parser-sqllog 行 features 段包含 `"async"`。版本要求：tokio major version 为 1；dm-database-parser-sqllog 为 2.0.4 或更高的 2.x。任一未满足即视为 Task 失败。

    步骤 B（代码锚点核对，对应 D-01/D-03/D-05）— 依次运行：
      1. `grep -n "#\[tokio::main\]\|async fn main\|async fn run" src/main.rs`，要求 3 个锚点均命中（tokio::main 宏、async fn main、async fn run）。
      2. `grep -n "Handle::current\|block_on\|block_in_place\|AsyncLogParser" src/cli/run/parallel.rs`，要求至少出现 Handle::current 与 block_on 与 AsyncLogParser 三个锚点。
      3. `grep -n "block_in_place\|block_on\|AsyncLogParser" src/cli/run/prescan.rs`，要求三锚点均命中。
      4. `grep -rn "AsyncLogParser" src/cli/run/sequential.rs src/cli/run/collector.rs src/cli/run/processor.rs src/cli/run/sqlite_parallel.rs src/scanner.rs`，要求每个文件至少 1 处命中。

    步骤 C（SC-3 全量测试）— 运行 `cargo test`，要求最终汇总行为 `test result: ok. 0 failed`。预期总 test 数量 ≥ 503（来自 RESEARCH.md 实测：408 lib + 87 integration + 7 watch_incremental + 1 jemalloc）。若任一测试失败，立即停止任务并将失败摘要写入 SUMMARY 的"Blockers"段落。

    步骤 D（SC-4 clippy）— 运行 `cargo clippy --all-targets -- -D warnings`。要求最终输出含 `Finished` 行且无 `warning:` 行（紧跟 `error:` 也属失败）。若有警告，停止任务并将告警摘要写入 SUMMARY 的"Blockers"段落。

    步骤 E（SC-5 release 构建）— 运行 `cargo build --release` 后立即运行 `ls -lh target/release/sqllog2db`。要求 `cargo build --release` 以 `Finished` 行结束、二进制存在、显示的字节尺寸位于 3.0M–4.5M 之间（允许偏离 RESEARCH.md 报告的 3.8MB ±20% 容差，覆盖 platform/工具链差异）。超出范围视为告警但不 fail（在 SUMMARY 记录差异）。

    步骤 F（SC-2 条件 bench，对应 D-07）— 先运行 `test -d sqllogs && echo HAS_LOGS || echo NO_LOGS`：
      - 若返回 `HAS_LOGS`：运行 `cargo bench --bench bench_csv -- csv_export_real`，在 SUMMARY 记录吞吐量数值（throughput 行）并标记 SC-2=PASS 或 FAIL（PASS = throughput 不低于 ~1.55M records/sec 的 90%，即 ≥1.40M records/sec）。
      - 若返回 `NO_LOGS`：在 SUMMARY 中明确写入 `SC-2: SKIPPED — sqllogs/ directory absent (per RESEARCH.md PENDING note)`，并附 RESEARCH.md 第 17 行的解释链接（"待运行 — 需要真实 sqllogs/ 目录才能执行"）。

    步骤 G（产出 SUMMARY）— 在 phase 目录下创建 `.planning/phases/76-async-migration/76-01-SUMMARY.md`，结构包含：frontmatter（plan: 01, status: done, tests_status: pass）；"Verification Log"段（步骤 A–F 的每一条原始命令、退出码、关键输出/匹配行）；"SC Status Matrix"段（5 条 SC 的 PASS/SKIPPED/FAIL 表）；"Blockers"段（步骤 C/D/E 中遇到的任何 fail）；"Notes"段（步骤 F SC-2 跳过原因、二进制体积差异等）。SUMMARY 必须由 Write 工具生成，不得使用 heredoc。

    禁止：本任务不得修改 src/ 下任何文件，不得修改 Cargo.toml，不得使用 `cargo test --release` 或 `--no-default-features` 等会改变验收语义的参数。
  </action>
  <verify>
    <automated>cargo test &amp;&amp; cargo clippy --all-targets -- -D warnings &amp;&amp; cargo build --release &amp;&amp; test -f target/release/sqllog2db &amp;&amp; test -f .planning/phases/76-async-migration/76-01-SUMMARY.md &amp;&amp; grep -q "SC Status Matrix" .planning/phases/76-async-migration/76-01-SUMMARY.md &amp;&amp; grep -q "tokio = " Cargo.toml &amp;&amp; grep -q "rt-multi-thread" Cargo.toml &amp;&amp; grep -q "features = \[\"async\"\]" Cargo.toml &amp;&amp; grep -q "tokio::main" src/main.rs</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test` 退出码 0，最终汇总行包含 `test result: ok.` 且 `0 failed`
    - `cargo clippy --all-targets -- -D warnings` 退出码 0，输出含 `Finished` 行，不含任何 `warning:` 或 `error:` 行
    - `cargo build --release` 退出码 0，`target/release/sqllog2db` 存在
    - `grep -nE "^tokio = " Cargo.toml` 命中且匹配行同时包含 `"rt-multi-thread"` 与 `"macros"`
    - `grep -nE "^dm-database-parser-sqllog = " Cargo.toml` 命中且匹配行包含 `features = ["async"]`
    - `grep -c "tokio::main" src/main.rs` ≥ 1
    - `grep -cE "Handle::current|block_on" src/cli/run/parallel.rs` ≥ 2
    - `grep -cE "block_in_place" src/cli/run/prescan.rs` ≥ 1
    - 文件 `.planning/phases/76-async-migration/76-01-SUMMARY.md` 存在且包含 "SC Status Matrix"、"Verification Log" 两个 H2 段标题
    - 若 `sqllogs/` 不存在：SUMMARY "SC Status Matrix" 中 SC-2 行写为 `SKIPPED`；若存在：SC-2 行写为 `PASS` 或 `FAIL` 且附带 throughput 数值
  </acceptance_criteria>
  <done>5 条 SC（SC-1/3/4/5 PASS，SC-2 PASS 或 SKIPPED）已落入 SUMMARY；任何 FAIL 已写入 Blockers 段落并由用户决定是否阻塞 Task 02。</done>
</task>

<task type="auto">
  <name>Task 02: 翻转 ROADMAP / REQUIREMENTS / STATE 三份文档的完成标记</name>
  <files>.planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/STATE.md</files>
  <read_first>
    - .planning/ROADMAP.md（行 5、行 244–253、行 1002–1012、Progress 表 Phase 76 行：行 927）
    - .planning/REQUIREMENTS.md（行 29 ASYNC-01 checkbox、行 51–59 Traceability、行 67–69 末尾元数据）
    - .planning/STATE.md（全文 47 行）
    - .planning/phases/76-async-migration/76-01-SUMMARY.md（确认 Task 01 已 PASS 后方可执行本 Task）
    - .planning/phases/76-async-migration/76-PATTERNS.md（"文档更新格式"段落，提供完成标记的目标字符串）
  </read_first>
  <action>
    本任务用 Edit 工具修改 3 份 planning 文档，将 Phase 76 / ASYNC-01 的状态全部翻转为完成，并与 v1.20 里程碑收尾对齐。仅当 Task 01 的 76-01-SUMMARY.md 中 SC-1/3/4/5 全部为 PASS、SC-2 为 PASS 或 SKIPPED 时方可执行；否则停止并要求用户先解决 Blockers。

    步骤 A（ROADMAP.md 改 3 处）—
      1. 行 5（v1.20 里程碑状态）：将 `- 🚧 **v1.20 性能全面提升** — Phases 72–76 (in progress)` 修改为 `- ✅ **v1.20 性能全面提升** — Phases 72–76 (shipped 2026-06-11)`。
      2. 行 250（v1.20 详情列表中 Phase 76 行）：将 `- [ ] **Phase 76: 异步解析路径迁移** — 切换为 dm-database-parser-sqllog async API，添加 tokio（ASYNC-01）` 修改为 `- [x] **Phase 76: 异步解析路径迁移** — 切换为 dm-database-parser-sqllog async API，添加 tokio（ASYNC-01） (completed 2026-06-11)`。注意保留前导短横线、星号与 `（ASYNC-01）` 的中文括号。
      3. Progress 表中 Phase 76 行（约第 927 行）：将 `| 76. 异步解析路径迁移 | v1.20 | Not started | - |` 修改为 `| 76. 异步解析路径迁移 | v1.20 | Complete | 2026-06-11 |`。
      4. 文件末尾元信息（约第 1016 行）：将 `*Updated: 2026-06-08 — milestone v1.20 性能全面提升 roadmap expanded to Phases 72–76 (8 requirements)*` 之后追加新行 `*Updated: 2026-06-11 — milestone v1.20 closed, Phase 76 ASYNC-01 completed*`。

    步骤 B（REQUIREMENTS.md 改 3 处）—
      1. 行 29（ASYNC-01 checkbox）：将 `- [ ] **ASYNC-01**:` 修改为 `- [x] **ASYNC-01**:`，需求描述文本保持原样不动。
      2. Traceability 表 ASYNC-01 行（第 59 行）：将 `| ASYNC-01 | Phase 76 | Pending |` 修改为 `| ASYNC-01 | Phase 76 | Done |`。
      3. 文件末尾的"Last updated"行（约第 68 行）：将 `*Last updated: 2026-06-08 — roadmap Phase 72–76 assigned (8/8 requirements mapped)*` 替换为 `*Last updated: 2026-06-11 — Phase 76 ASYNC-01 completed; v1.20 milestone closed*`。

    步骤 C（STATE.md 改 5 处，全文 47 行精确替换）—
      1. 第 5 行 `status:` 字段：保持 `status: completed`（v1.20 仍为 completed），无需修改。
      2. 第 6 行 `last_updated:`：将 `"2026-06-11T12:32:06.513Z"` 更新为运行本 Task 时的 ISO-8601 UTC 时间戳（格式 `"2026-06-11THH:MM:SS.000Z"`，用 `date -u +"%Y-%m-%dT%H:%M:%S.000Z"` 取当前时间）。
      3. 第 7 行 `last_activity: 2026-06-11` 保持不变。
      4. 第 9 行 `completed_phases: 26` 修改为 `completed_phases: 27`（Phase 76 完成 +1）。
      5. 第 13 行 `percent: 68` 修改为 `percent: 71`（27/38 = 71.05%，向下取整）。
      6. 第 27 行 `Phase: 75` 修改为 `Phase: 76`。
      7. 第 28 行 `Plan: Not started` 修改为 `Plan: 76-01 (verified + docs flipped)`。
      8. 第 29 行 `Status: Milestone complete` 保持不变（v1.20 仍为 milestone complete）。
      9. 第 30 行 `Last activity: 2026-06-11` 保持不变。
     10. 第 31 行 `Resume: .planning/phases/74-memory-alloc/74-CONTEXT.md` 修改为 `Resume: .planning/phases/76-async-migration/76-CONTEXT.md`。

    步骤 D（一致性自检）— Edit 完成后运行：
      - `grep -n "Phase 76" .planning/ROADMAP.md | head -20`（确认行 5、250、927 三处已翻转）
      - `grep -n "ASYNC-01" .planning/REQUIREMENTS.md`（确认 checkbox 与 Traceability 两处已翻转）
      - `cat .planning/STATE.md`（确认 7 处字段更新到位）
      若任一处仍含 `[ ]` 未翻转或 `Pending` 未变为 `Done`，立刻继续 Edit 修正。

    步骤 E（更新 76-01-SUMMARY.md）— 在 SUMMARY 中追加 "Docs Flipped" 段落，列出三份文档与本任务实际修改的行号，便于追溯。

    禁止：不得修改 v1.20 之外的里程碑标记；不得修改本 phase 之外的 Phase 状态；不得新增 Out of Scope 条目；不得在 Progress 表 Phase 76 行写入 v1.21 信息。
  </action>
  <verify>
    <automated>grep -c "\[x\] \*\*Phase 76:" .planning/ROADMAP.md | grep -q "^1$" &amp;&amp; grep -c "shipped 2026-06-11" .planning/ROADMAP.md | awk '$1 &gt;= 1 {exit 0} {exit 1}' &amp;&amp; grep -q "76. 异步解析路径迁移 | v1.20 | Complete | 2026-06-11" .planning/ROADMAP.md &amp;&amp; grep -q "\[x\] \*\*ASYNC-01\*\*" .planning/REQUIREMENTS.md &amp;&amp; grep -q "| ASYNC-01 | Phase 76 | Done |" .planning/REQUIREMENTS.md &amp;&amp; grep -q "^Phase: 76$" .planning/STATE.md &amp;&amp; grep -q "76-async-migration/76-CONTEXT.md" .planning/STATE.md &amp;&amp; grep -q "completed_phases: 27" .planning/STATE.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "\[x\] \*\*Phase 76:" .planning/ROADMAP.md` 等于 1（Phase 76 列表行已翻转）
    - `grep -c "\[ \] \*\*Phase 76:" .planning/ROADMAP.md` 等于 0（不再存在未完成标记）
    - `grep -q "76. 异步解析路径迁移 | v1.20 | Complete | 2026-06-11" .planning/ROADMAP.md` 成功（Progress 表已更新）
    - `grep -q "shipped 2026-06-11" .planning/ROADMAP.md` 成功（v1.20 行已从 in progress → shipped）
    - `grep -c "\[x\] \*\*ASYNC-01\*\*" .planning/REQUIREMENTS.md` 等于 1
    - `grep -c "\[ \] \*\*ASYNC-01\*\*" .planning/REQUIREMENTS.md` 等于 0
    - `grep -q "| ASYNC-01 | Phase 76 | Done |" .planning/REQUIREMENTS.md` 成功
    - `grep -q "^Phase: 76$" .planning/STATE.md` 成功
    - `grep -q "Resume: .planning/phases/76-async-migration/76-CONTEXT.md" .planning/STATE.md` 成功
    - `grep -q "completed_phases: 27" .planning/STATE.md` 成功
    - `grep -q "percent: 71" .planning/STATE.md` 成功
    - 76-01-SUMMARY.md 末尾出现 "Docs Flipped" 段落且列出 3 个文件路径
  </acceptance_criteria>
  <done>三份文档完整翻转，自检 grep 全部通过；用户可在 ROADMAP、REQUIREMENTS、STATE 三处看到 Phase 76 / ASYNC-01 完成状态一致；v1.20 里程碑标记为 shipped。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 无新增 | 本 Plan 仅运行只读验收命令与修改 planning 文档，不引入新的 untrusted input、网络调用、外部进程或依赖安装 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-76-01 | Tampering | .planning/{ROADMAP,REQUIREMENTS,STATE}.md | accept | 仅修改 phase 76 / ASYNC-01 相关行；Task 02 acceptance_criteria 用 grep 精确锚定目标行，禁止误改其他 phase 状态；git diff 在提交前可人工 review |
| T-76-02 | Information Disclosure | cargo test 输出 | accept | 测试不涉及 secret；输出仅记录 PASS/FAIL 摘要至 SUMMARY，不外发 |
| T-76-03 | Denial of Service | cargo bench (SC-2) 长时间运行 | mitigate | 仅在 `sqllogs/` 存在时运行；预期 wall time < 60s（CSV bench 单测目标）；缺失目录时显式 SKIP |
| T-76-04 | Repudiation | 验收命令的输出未留痕 | mitigate | 全部命令的退出码与关键输出行必须写入 76-01-SUMMARY.md "Verification Log"，可追溯到 commit 65c24fd 的代码状态 |
</threat_model>

<verification>
- 在 Plan 收尾前运行：`cargo test && cargo clippy --all-targets -- -D warnings && cargo fmt --check`
  （fmt --check 作为 CLAUDE.md 全局质量门禁，本 phase 未改动源码，预期直接通过）
- 在 Plan 收尾前运行：`git status` 确认仅 3 个 planning 文档与 1 个新增 SUMMARY 进入暂存区，不含意外的源码 diff
</verification>

<success_criteria>
- ROADMAP.md Phase 76 行以 `[x]` 开头并标注 `(completed 2026-06-11)`；v1.20 里程碑行以 ✅ 开头并标注 `shipped 2026-06-11`
- REQUIREMENTS.md ASYNC-01 checkbox 为 `[x]`，Traceability 表中 ASYNC-01 行第三列为 `Done`
- STATE.md `Phase: 76`、`completed_phases: 27`、`percent: 71`、`Resume: .planning/phases/76-async-migration/76-CONTEXT.md`
- 76-01-SUMMARY.md 存在，包含 SC-1/3/4/5 PASS 与 SC-2 PASS|SKIPPED 状态矩阵
- 全量 `cargo test` 与 `cargo clippy --all-targets -- -D warnings` 通过（源码未改，预期与研究阶段一致）
</success_criteria>

<output>
Create `.planning/phases/76-async-migration/76-01-SUMMARY.md` when done.

SUMMARY 必须至少包含以下 H2 段：
- Verification Log（步骤 A–F 的命令、退出码、关键输出行）
- SC Status Matrix（SC-1/2/3/4/5 的 PASS/SKIPPED/FAIL 表）
- Docs Flipped（ROADMAP/REQUIREMENTS/STATE 三份文件实际修改的行号）
- Blockers（若 Task 01 任一 SC 为 FAIL；正常路径下为空）
- Notes（SC-2 SKIPPED 原因、二进制体积差异、其他观察）
</output>
