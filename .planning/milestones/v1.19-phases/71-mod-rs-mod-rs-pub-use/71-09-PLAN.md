---
phase: 71-mod-rs-mod-rs-pub-use
plan: 09
type: execute
wave: 3
depends_on: []
files_modified:
  - src/cli/run/mod.rs
  - src/cli/run/orchestrator.rs
  - src/cli/run/input.rs
  - src/cli/run/sequential.rs
  - src/cli/run/summary.rs
  - src/cli/run/error_log.rs
autonomous: true
requirements: []
must_haves:
  truths:
    - "src/cli/run/mod.rs 仅含 mod 声明与 pub use 重导出"
    - "crate::cli::run::handle_run 公开签名不变（被 main.rs 与 watch/mod.rs 调用）"
    - "全部 7 个原私有辅助函数行为不变（resolve_input_files / merge_trxid_prescan / make_progress_bar / run_sequential / run_file_loop / print_run_summary / write_error_log）"
    - "cargo test 通过（含 tests/integration.rs 全部 run 子命令测试 + src/cli/run/tests.rs 单元测试）"
    - "cargo clippy --all-targets -- -D warnings 通过"
  artifacts:
    - path: "src/cli/run/mod.rs"
      provides: "module declarations + pub use only"
    - path: "src/cli/run/orchestrator.rs"
      provides: "handle_run 主编排函数"
      contains: "pub fn handle_run"
    - path: "src/cli/run/input.rs"
      provides: "resolve_input_files + merge_trxid_prescan + make_progress_bar 三个辅助函数"
      contains: "pub(super) fn resolve_input_files"
    - path: "src/cli/run/sequential.rs"
      provides: "run_sequential + run_file_loop 顺序导出路径"
      contains: "pub(super) fn run_sequential"
    - path: "src/cli/run/summary.rs"
      provides: "print_run_summary 摘要输出"
      contains: "pub(super) fn print_run_summary"
    - path: "src/cli/run/error_log.rs"
      provides: "write_error_log 错误日志落盘"
      contains: "pub(super) fn write_error_log"
  key_links:
    - from: "src/cli/run/mod.rs"
      to: "orchestrator / input / sequential / summary / error_log"
      via: "mod 声明 + pub use"
      pattern: "pub use orchestrator::handle_run"
    - from: "src/main.rs / src/cli/watch/mod.rs"
      to: "crate::cli::run::handle_run"
      via: "调用入口"
      pattern: "cli::run::handle_run"
---

<objective>
重构 src/cli/run/mod.rs（476 行）：将主函数 `handle_run` + 7 个原私有辅助函数按职责拆到 5 个新文件：orchestrator.rs（主入口）、input.rs（输入解析与 prescan 合并）、sequential.rs（顺序路径）、summary.rs（摘要输出）、error_log.rs（错误日志），mod.rs 仅保留 mod 声明与 pub use。现有 collector/filter_processor/parallel/prescan/processor/sqlite_parallel 6 个子模块文件不动。

Purpose: cli/run 是主调度核心，按职责切分后每文件 ≤ 100 行，handle_run 编排逻辑独立可读。
Output: 6 个文件（mod.rs 极简 + 5 个新职责文件），cargo test/clippy 全绿。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md

<interfaces>
<!-- 必须保持的公开 API（main.rs + watch/mod.rs 调用） -->

公开：
- `pub fn handle_run(cfg: &Config, quiet: bool, verbose: bool, interrupted: &Arc<AtomicBool>, jobs_override: Option<usize>) -> Result<ErrorStats>`

被 handle_run 内部调用：
- `mod collector;` (不动)
- `mod filter_processor;` → `use filter_processor::build_pipeline;`
- `mod parallel;` → `use parallel::process_csv_parallel;`
- `mod prescan;` → `use prescan::scan_for_trxids_by_transaction_filters;`
- `mod processor;` → `use processor::process_log_file;`
- `mod sqlite_parallel;` → `use sqlite_parallel::process_sqlite_parallel;`
- `#[cfg(test)] mod tests;` (src/cli/run/tests.rs，不动)

7 个原私有辅助函数（mod.rs 中）：
1. `fn resolve_input_files(cfg) -> Result<(Vec<PathBuf>, bool)>`
2. `fn merge_trxid_prescan(cfg, log_files, jobs, is_stdin_pipe, quiet) -> Result<Option<Config>>`
3. `fn make_progress_bar(show_progress, total_files) -> Option<ProgressBar>`
4. `fn run_sequential(...)` — 顺序导出主体
5. `fn run_file_loop(...)` — 逐文件循环
6. `fn print_run_summary(...)` — 摘要输出
7. `fn write_error_log(cfg, stats)` — 错误日志落盘

外部使用：
- src/main.rs Run 分支调用 `crate::cli::run::handle_run(...)`
- src/cli/watch/mod.rs trigger_full_file / run_incremental_handle_run 调用 `crate::cli::run::handle_run(...)`
- src/cli/run/tests.rs `use super::*;` 访问内部 fn —— 拆分后需调整 tests.rs 内的 use 路径（如果 tests.rs 访问了 resolve_input_files 等），把 super:: 改成 super::input::resolve_input_files 等
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 拆分 cli/run/mod.rs 主体（input + sequential + summary + error_log）</name>
  <files>src/cli/run/input.rs, src/cli/run/sequential.rs, src/cli/run/summary.rs, src/cli/run/error_log.rs</files>
  <read_first>
    - src/cli/run/mod.rs（全文 476 行）
    - src/cli/run/tests.rs（grep 'use super' 找内部依赖项，列出 tests 访问的所有原私有 fn 名称）
    - src/cli/run/processor.rs（grep 内部公开签名）
    - src/cli/run/filter_processor.rs（确认 build_pipeline 签名）
    - src/cli/run/prescan.rs（确认 scan_for_trxids_by_transaction_filters 签名）
    - src/cli/run/parallel.rs（确认 process_csv_parallel 签名）
    - src/cli/run/sqlite_parallel.rs（确认 process_sqlite_parallel 签名）
  </read_first>
  <action>
    Step 1: 创建 `src/cli/run/input.rs`：
      - 移入 mod.rs 第 154-238 行的 3 个函数：
        - `pub(super) fn resolve_input_files(cfg: &Config) -> Result<(Vec<PathBuf>, bool)>`
        - `pub(super) fn merge_trxid_prescan(cfg, log_files, jobs, is_stdin_pipe, quiet) -> Result<Option<Config>>`
        - `pub(super) fn make_progress_bar(show_progress: bool, total_files: usize) -> Option<ProgressBar>`
      - 顶部 use：
        ```rust
        use crate::config::Config;
        use crate::error::Result;
        use crate::parser::SqllogParser;
        use indicatif::{ProgressBar, ProgressStyle};
        use log::{info, warn};
        use std::io::IsTerminal;
        use std::path::PathBuf;
        use super::prescan::scan_for_trxids_by_transaction_filters;
        ```

    Step 2: 创建 `src/cli/run/sequential.rs`：
      - 移入 mod.rs 第 241-337 行的 2 个函数：
        - `pub(super) fn run_sequential(...) -> Result<(Vec<(PathBuf, usize)>, ErrorStats)>`
        - `fn run_file_loop(...) -> Result<(Vec<(PathBuf, usize)>, ErrorStats)>`
      - 保留 `#[allow(clippy::too_many_arguments)]` 与 `#[allow(clippy::fn_params_excessive_bools)]` 属性
      - 顶部 use：
        ```rust
        use crate::config::Config;
        use crate::error::{Error, ErrorStats, Result};
        use crate::exporter::ExporterManager;
        use indicatif::ProgressBar;
        use log::info;
        use std::path::PathBuf;
        use std::sync::Arc;
        use std::sync::atomic::{AtomicBool, Ordering};
        use super::processor::process_log_file;
        ```

    Step 3: 创建 `src/cli/run/summary.rs`：
      - 移入 mod.rs 第 339-421 行：
        - `pub(super) fn print_run_summary(quiet, verbose, use_parallel, elapsed, processed_files, total_records, skipped_files, run_stats)`
      - 保留 `#[allow(clippy::fn_params_excessive_bools)]` 与 `#[allow(clippy::cast_precision_loss)]` 内联属性
      - 顶部 use：
        ```rust
        use crate::error::ErrorStats;
        use std::path::PathBuf;
        ```

    Step 4: 创建 `src/cli/run/error_log.rs`：
      - 移入 mod.rs 第 423-473 行：
        - `pub(super) fn write_error_log(cfg: &crate::config::Config, stats: &ErrorStats)`
      - 顶部 use：
        ```rust
        use crate::error::ErrorStats;
        use std::io::Write;
        ```
  </action>
  <verify>
    <automated>cargo build</automated>
    <grep-check>test -f src/cli/run/input.rs && test -f src/cli/run/sequential.rs && test -f src/cli/run/summary.rs && test -f src/cli/run/error_log.rs && echo "OK: 4 个新文件已创建"</grep-check>
  </verify>
  <acceptance_criteria>
    - 4 个新文件已创建（input.rs / sequential.rs / summary.rs / error_log.rs）
    - cargo build 通过（即使 mod.rs 暂未改也应通过，因为新文件未被声明，编译器不会读取）
    - 文件内容包含所有迁移的函数（grep `fn (resolve_input_files|merge_trxid_prescan|make_progress_bar|run_sequential|run_file_loop|print_run_summary|write_error_log)` 应能在新文件中找到）
  </acceptance_criteria>
  <done>
    4 个新文件创建完成，包含原私有 fn 拷贝（mod.rs 暂时保留这些函数，下个 task 再清理）。cargo build 仍通过。
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: 创建 orchestrator.rs（handle_run 主函数）并改写 mod.rs 为骨架</name>
  <files>src/cli/run/mod.rs, src/cli/run/orchestrator.rs, src/cli/run/tests.rs</files>
  <read_first>
    - src/cli/run/mod.rs（含本 plan 完成 Task 1 后的状态）
    - src/cli/run/input.rs / sequential.rs / summary.rs / error_log.rs（Task 1 创建）
    - src/cli/run/tests.rs（确认本 plan Task 1 列出的 fn 在 tests 中如何被引用）
  </read_first>
  <action>
    Step 1: 创建 `src/cli/run/orchestrator.rs`：
      - 移入 mod.rs 第 1-152 行的：
        - 所有顶部 use（保留）
        - `pub fn handle_run(cfg, quiet, verbose, interrupted, jobs_override) -> Result<ErrorStats>` 完整函数体
      - 调整内部调用为：
        - `resolve_input_files` → 直接调用（来自 `use super::input::resolve_input_files;`）
        - `merge_trxid_prescan` → 同上 `use super::input::merge_trxid_prescan;`
        - `make_progress_bar` → 同上
        - `build_pipeline` → `use super::filter_processor::build_pipeline;`
        - `process_csv_parallel` → `use super::parallel::process_csv_parallel;`
        - `process_sqlite_parallel` → `use super::sqlite_parallel::process_sqlite_parallel;`
        - `run_sequential` → `use super::sequential::run_sequential;`
        - `print_run_summary` → `use super::summary::print_run_summary;`
        - `write_error_log` → `use super::error_log::write_error_log;`
      - 顶部 use 整合：
        ```rust
        use crate::config::Config;
        use crate::error::{Error, ErrorStats, Result};
        use log::info;
        use std::path::PathBuf;
        use std::sync::Arc;
        use std::sync::atomic::{AtomicBool, Ordering};
        use std::time::Instant;
        use super::error_log::write_error_log;
        use super::filter_processor::build_pipeline;
        use super::input::{make_progress_bar, merge_trxid_prescan, resolve_input_files};
        use super::parallel::process_csv_parallel;
        use super::sequential::run_sequential;
        use super::sqlite_parallel::process_sqlite_parallel;
        use super::summary::print_run_summary;
        ```

    Step 2: 改写 `src/cli/run/mod.rs` 为：
      ```rust
      //! Run 子命令：主编排在 orchestrator.rs，各阶段子模块独立成文件。

      mod collector;
      mod error_log;
      mod filter_processor;
      mod input;
      mod orchestrator;
      mod parallel;
      mod prescan;
      mod processor;
      mod sequential;
      mod sqlite_parallel;
      mod summary;

      #[cfg(test)]
      mod tests;

      pub use orchestrator::handle_run;
      ```
      仅注释、mod 声明、pub use。

    Step 3: 更新 `src/cli/run/tests.rs` 内的 use 路径（如果 tests 直接引用了原私有 fn）：
      - 若 tests.rs 中有 `use super::*;` 且实际调用了 resolve_input_files / merge_trxid_prescan 等 → 改为：
        ```rust
        use super::input::{resolve_input_files, merge_trxid_prescan, make_progress_bar};
        use super::sequential::run_sequential;
        use super::summary::print_run_summary;
        use super::error_log::write_error_log;
        use super::orchestrator::handle_run;
        ```
      - 若 tests.rs 主要测试 collector / processor 等子模块，未直接引用上述 7 个 fn，则只需把 `use super::*;` 替换为具体子模块路径（如 `use super::orchestrator::handle_run;`）

    Step 4: 运行完整质量门禁：
      - `cargo build`
      - `cargo clippy --all-targets -- -D warnings`
      - `cargo test`（确认 tests/integration.rs 的 run 子命令端到端测试 + src/cli/run/tests.rs 全部通过）
      - `cargo fmt`

    Step 5: 端到端冒烟验证：
      ```bash
      cargo run -- init -o /tmp/_71_smoke.toml --force
      mkdir -p /tmp/_71_smoke_logs
      echo '2024-01-15 10:00:00.123 (EP[0] sess:0x0001 thrd:456 user:SYSDBA trxid:1001 stmt:select_001 appname:test ip:127.0.0.1) [SEL] select * from t1. EXECTIME: 5(ms) ROWCOUNT: 10(rows) EXEC_ID: 1.' > /tmp/_71_smoke_logs/test.log
      sed -i.bak 's|inputs = .*|inputs = ["/tmp/_71_smoke_logs"]|' /tmp/_71_smoke.toml
      cargo run -- run -c /tmp/_71_smoke.toml
      ```
      应退出码 0 且生成 CSV 输出
  </action>
  <verify>
    <automated>cargo clippy --all-targets -- -D warnings && cargo test</automated>
    <grep-check>grep -v '^\s*//' src/cli/run/mod.rs | grep -vE '^\s*$' | grep -E '\bfn \w+|\bstruct \w+|\bimpl ' && echo "FAIL: mod.rs 仍含实现" || echo "OK: mod.rs 仅声明"</grep-check>
  </verify>
  <acceptance_criteria>
    - cargo clippy --all-targets -- -D warnings 退出码 0
    - cargo test 退出码 0
    - src/cli/run/mod.rs 过滤注释/空行后只含 `mod`/`pub use`/属性宏（#[cfg(test)]），无 `fn`/`struct`/`impl`
    - crate::cli::run::handle_run 签名不变，main.rs 与 watch/mod.rs 调用无需修改
    - 7 个原私有辅助函数迁移到 input.rs / sequential.rs / summary.rs / error_log.rs
    - handle_run 主体迁移到 orchestrator.rs
    - tests.rs 内的 use 路径已修正，所有原 tests 全部通过
    - 端到端冒烟：cargo run -- run -c <config> 退出码 0
  </acceptance_criteria>
  <done>
    mod.rs 简化到 ≤ 25 行（含注释），handle_run 在 orchestrator.rs，7 个原私有 fn 按职责落地 4 个文件，三道质量门禁与端到端冒烟通过。
  </done>
</task>

</tasks>

<verification>
- cargo clippy --all-targets -- -D warnings 通过
- cargo test 通过（含 tests/integration.rs 中所有 run 路径端到端测试）
- src/cli/run/mod.rs grep -E '\bfn |\bstruct |\bimpl ' 无匹配
- cargo run -- run -c <临时配置> 端到端运行成功
- src/cli/run/{collector,filter_processor,parallel,prescan,processor,sqlite_parallel}.rs 内容不变
</verification>

<success_criteria>
- mod.rs 仅含 mod 声明与 pub use（≤ 25 行）
- handle_run 在 orchestrator.rs
- 7 个原私有 fn 按职责落地 4 个文件
- 6 个原已有子模块文件不动
- 全套质量门禁 + 端到端冒烟通过
</success_criteria>

<output>
Create `.planning/phases/71-mod-rs-mod-rs-pub-use/71-09-SUMMARY.md` when done
</output>
