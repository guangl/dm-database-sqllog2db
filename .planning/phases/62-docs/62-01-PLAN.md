---
phase: 62-docs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/cli/init.rs
autonomous: true
requirements:
  - DOC-03
must_haves:
  truths:
    - "sqllog2db init -o /tmp/test.toml --force 生成的文件中，[filter.include] 节下 7 个示例字段（users/ips/sessions/threads/statements/apps/tags）每一行末尾都包含 `# <英文描述>` 行内注释"
    - "[filter.exclude] 节下相同 7 个字段每一行末尾都包含 `# <英文描述>` 行内注释，描述措辞使用 `to exclude` 而非 `to include`"
    - "[filter.indicators] 节下 3 个字段（exec_ids/min_runtime_ms/min_row_count）每行末尾都包含 `# <英文描述>` 行内注释"
    - "[filter.sql] 节下 2 个字段（includes/excludes）每行末尾都包含 `# <英文描述>` 行内注释"
    - "[filter.include] 节下额外 3 个字段（start_ts/end_ts/trxids）每行末尾都包含 `# <英文描述>` 行内注释"
    - "全部 9 个 test_init_* 测试用例继续通过（结构断言不被破坏，无中文混入）"
  artifacts:
    - path: "src/cli/init.rs"
      provides: "更新后的 CONFIG_TEMPLATE_EN 字符串常量"
      contains: "# users = [\"SYSDBA\"]"
  key_links:
    - from: "src/cli/init.rs CONFIG_TEMPLATE_EN"
      to: "sqllog2db init -o <path> 生成的文件"
      via: "handle_init 写入 path"
      pattern: "CONFIG_TEMPLATE_EN"
---

<objective>
为 `src/cli/init.rs::CONFIG_TEMPLATE_EN` 字符串常量中 `[filter.include]`、`[filter.exclude]`、`[filter.indicators]`、`[filter.sql]` 四个子节下的所有当前缺少行内注释的字段补全英文行内注释，使得 `sqllog2db init` 生成的配置模板每一个示例字段都有用途说明。

Purpose: 实现 DOC-03（Phase 62 Success Criteria #3）——"`sqllog2db init -o /tmp/test.toml` 生成的文件中，`[stats]` 节的 `from`/`to` 字段和 `[filters]` 各子字段均有行内注释，无任何字段缺少注释"。

Output: 修改后的 `src/cli/init.rs`，模板字符串中 22 个示例字段（include 10 个 + exclude 7 个 + indicators 3 个 + sql 2 个）均带英文行内注释，注释风格与现有 `[stats]` 节一致（`# field = val   # English description`）。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/62-docs/62-CONTEXT.md
@.planning/phases/62-docs/62-RESEARCH.md
@.planning/phases/62-docs/62-PATTERNS.md

<interfaces>
<!-- Phase 47 Plan 02 在 CONFIG_TEMPLATE_EN 中确立的行内注释格式（src/cli/init.rs:124-128，[stats] 节） -->
<!-- 必须沿用相同风格：`# field = val   # English description with capitalized first word, no trailing period` -->

[stats] 节当前已有的注释格式样板：
```toml
[stats]
# from = "2024-01-01"   # Start of time range. Formats: "YYYY-MM-DD" or "YYYY-MM-DD HH:MM:SS"
# to   = "2024-01-31"   # End of time range. Same formats as from.
# top  = 20             # Default top-N. CLI --top overrides this value.
```

[exporter.csv] / [exporter.sqlite] 节现有注释格式样板：
```toml
# CSV output file path
file = "outputs/sqllog.csv"
# Drop and recreate the file before writing (true/false)
overwrite = true
```

不能破坏的现有测试断言（tests/integration.rs）：
- `contains("Append to existing CSV file instead of overwriting")` — test_init_template_has_csv_append_comment
- `contains("CSV output file path")` — test_init_template_has_csv_append_comment
- `contains("SQLite database file path")` — test_init_template_has_sqlite_field_comments
- `contains("Table name to write records into")` — test_init_template_has_sqlite_field_comments
- `contains("ASCII identifiers only")` — test_init_template_has_sqlite_field_comments
- `contains("Drop and recreate the table")` — test_init_template_has_sqlite_field_comments
- `contains("Append rows to existing table")` — test_init_template_has_sqlite_field_comments
- `contains("[filter.include]")` / `contains("[filter.exclude]")` / `contains("[filter.indicators]")` / `contains("[filter.sql]")` — test_init_generates_new_nested_format
- `contains("# from = \"2024")` / `contains("# to")` / `contains("# top")` / `contains("YYYY-MM-DD HH:MM:SS")` — test_init_template_contains_stats_section
- `!content.contains("日志路径")` — test_handle_init_en_template（禁止中文）
- `contains("[sqllog]")` / `contains("SQL log path")` — test_handle_init_en_template
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 为 [filter.include] 与 [filter.exclude] 节下全部 17 个示例字段补全英文行内注释</name>

  <read_first>
    - src/cli/init.rs（待修改文件本身——必读，确认 CONFIG_TEMPLATE_EN 当前文本与第 89–110 行字段位置）
    - .planning/phases/62-docs/62-PATTERNS.md（已给出目标格式 src/cli/init.rs 章节，参照其示例措辞）
    - .planning/phases/62-docs/62-CONTEXT.md（D-04 锁定注释格式 `# field = val  # Description`；D-05 锁定修改 init.rs 而非 config.toml）
    - tests/integration.rs:160-235（test_handle_init_en_template、test_init_template_has_csv_append_comment、test_init_template_has_sqlite_field_comments——确认现有断言不能被破坏）
  </read_first>

  <behavior>
    - 行为 1：修改后 `cargo run -- init -o /tmp/p62.toml --force` 生成的文件中，`[filter.include]` 节下每行注释掉的字段都有形如 `# users = ["SYSDBA"]   # Exact-match list of usernames to include` 的行内注释（每个字段独占一行，`#` 后紧跟示例 key=value，再以两个或更多空格分隔后接 `# English description`）
    - 行为 2：`[filter.include]` 节下 10 个字段全部补注（包括已有示例的 users/ips/sessions/threads/statements/apps/tags + 已有示例但缺注释的 start_ts/end_ts/trxids）；其中 `start_ts`/`end_ts` 注释明确说明 "Inclusive lower bound of record timestamp (filter retains records with ts >= start_ts)" / "Inclusive upper bound of record timestamp (filter retains records with ts <= end_ts)"；`trxids` 注释说明 "Exact-match list of transaction IDs to include"
    - 行为 3：`[filter.exclude]` 节下 7 个字段全部补注，措辞与 include 镜像，将 "to include" 改为 "to exclude"
    - 行为 4：注释全部使用英文（test_handle_init_en_template 仍通过）；首字母大写、句末不加句号（与 [stats] 节风格一致）
    - 行为 5：保留所有现有的节标题（`[filter.include]`、`[filter.exclude]`）、节说明注释（`# --- Include filters (record-level, AND semantics...) ---` 等）、`enable = false` 等已有内容不动
  </behavior>

  <action>
    在 `src/cli/init.rs` 的 `CONFIG_TEMPLATE_EN` 字符串常量内（第 63–152 行的 raw string `r#"..."#`），定位到 `[filter.include]`（约第 89 行起）与 `[filter.exclude]`（约第 103 行起）两个子节。

    对 `[filter.include]` 节中以下 10 个被 `#` 注释掉的字段示例，将原本仅有的 `# key = value` 单行改造为 `# key = value   # <英文描述>` 形式（在 `=` 与值之间或值之后插入足够空格使描述列对齐，至少 2 个空格分隔示例与描述）。各字段对应英文描述如下：

    - `users`：`Exact-match list of usernames to include`
    - `ips`：`Exact-match list of client IP addresses to include`
    - `sessions`：`Exact-match list of session IDs (hex strings) to include`
    - `threads`：`Exact-match list of thread IDs to include`
    - `statements`：`Statement types to include (INS/UPD/DEL/SEL/SET/OTH/ORA)`
    - `apps`：`Exact-match list of application names to include`
    - `tags`：`Exact-match list of record tags to include (e.g. [SEL], [INS])`
    - `start_ts`：`Inclusive lower bound of record timestamp (format: YYYY-MM-DD HH:MM:SS)`
    - `end_ts`：`Inclusive upper bound of record timestamp (format: YYYY-MM-DD HH:MM:SS)`
    - `trxids`：`Exact-match list of transaction IDs to include`

    对 `[filter.exclude]` 节中以下 7 个被 `#` 注释掉的字段示例，以同样方式补注，描述措辞镜像 include 节但将 `to include` 改为 `to exclude`：

    - `users`：`Exact-match list of usernames to exclude`
    - `ips`：`Exact-match list of client IP addresses to exclude`
    - `sessions`：`Exact-match list of session IDs (hex strings) to exclude`
    - `threads`：`Exact-match list of thread IDs to exclude`
    - `statements`：`Statement types to exclude (INS/UPD/DEL/SEL/SET/OTH/ORA)`
    - `apps`：`Exact-match list of application names to exclude`
    - `tags`：`Exact-match list of record tags to exclude`

    保持以下内容完全不变：
    - 节级注释（如 `# --- Include filters (record-level, AND semantics: every configured field must match) ---`、`# Metadata fields use exact string matching.`）
    - 节标题 `[filter.include]`、`[filter.exclude]`
    - 字段示例值（`["SYSDBA"]` 等保持原样，不要修改示例值，只追加行内注释）
    - `[filter]`、`[filter.indicators]`、`[filter.sql]`、`[stats]`、`[exporter.csv]`、`[exporter.sqlite]` 等其他节本任务不动（[filter.indicators]/[filter.sql] 由 Task 2 处理）

    执行 D-04（注释格式 `# field = val  # Description`）与 D-05（修改 init.rs 源头而非 config.toml）。注释一律使用英文以保持 test_handle_init_en_template 通过（断言禁止中文如 "日志路径"）。
  </action>

  <verify>
    <automated>cd /Users/guang/Projects/sqllog2db && cargo test test_init --quiet 2>&1 | tail -20</automated>
    <automated>cd /Users/guang/Projects/sqllog2db && cargo run --quiet -- init -o /tmp/p62-task1.toml --force 2>/dev/null && awk '/^\[filter\.include\]/,/^\[filter\.exclude\]/' /tmp/p62-task1.toml | grep -v '^#---' | grep '^# [a-z]' | grep -vc '#.*[A-Za-z]'</automated>
    <!-- 第二个 verify：[filter.include] 节下以 `# <字段名>` 开头的行，剥离首段后必须含第二个 `#` 引导的英文描述。期望输出 0（没有缺注释行）。 -->
  </verify>

  <acceptance_criteria>
    - `cargo test test_init` 全部通过（无失败、无新增警告）
    - `cargo run -- init -o /tmp/p62-task1.toml --force` 退出码 0
    - `grep -c "to include" /tmp/p62-task1.toml` 输出 ≥ 8（include 节 7 个 + trxids 描述 + start_ts/end_ts 不含此短语但计入总注释数）
    - `grep -c "to exclude" /tmp/p62-task1.toml` 输出 ≥ 6（exclude 节 7 个字段，但 statements 描述不含 to exclude 短语而是 "Statement types to exclude"，计 ≥ 6 即可）
    - `grep -E "^# (users|ips|sessions|threads|statements|apps|tags|start_ts|end_ts|trxids) = " /tmp/p62-task1.toml | grep -vc " # "` 输出 0（每一行示例字段都必须含 ` # ` 行内注释分隔符）
    - 文件中不存在中文字符（`LC_ALL=C grep -P '[^\x00-\x7F]' /tmp/p62-task1.toml | wc -l` 输出 0）
  </acceptance_criteria>

  <done>
    `[filter.include]` 节 10 个示例字段与 `[filter.exclude]` 节 7 个示例字段全部带英文行内注释；`cargo test test_init` 全部通过；`sqllog2db init` 生成文件经 grep 验证每行都有 ` # ` 分隔符引导的描述；文件全英文，无中文混入。
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: 为 [filter.indicators] 与 [filter.sql] 节下 5 个示例字段补全英文行内注释</name>

  <read_first>
    - src/cli/init.rs（Task 1 完成后的最新版本——必读，确认 [filter.indicators]/[filter.sql] 字段位置约第 113–121 行）
    - .planning/phases/62-docs/62-PATTERNS.md（行内注释格式样板）
    - .planning/phases/62-docs/62-CONTEXT.md（D-04 注释格式约束）
    - tests/integration.rs:497-560（test_init_generates_new_nested_format、test_init_generated_en_template_passes_validate——确认 indicators/sql 节标题与字段语义断言不被破坏）
  </read_first>

  <behavior>
    - 行为 1：`[filter.indicators]` 节下 3 个被 `#` 注释掉的字段示例（exec_ids、min_runtime_ms、min_row_count）每行末尾都有 ` # <英文描述>` 形式的行内注释
    - 行为 2：`[filter.sql]` 节下 2 个被 `#` 注释掉的字段示例（includes、excludes）每行末尾都有 ` # <英文描述>` 形式的行内注释
    - 行为 3：注释文案明确字段语义：indicators 是事务级、需要 pre-scan；sql includes/excludes 也是事务级，匹配 SQL 文本子串
    - 行为 4：保留两个节的现有节级注释（`# --- Indicator filters (transaction-level...) ---` 与 `# --- SQL filters (transaction-level...) ---`）与节标题不变
    - 行为 5：生成文件经 `sqllog2db validate -c <file>` 校验仍通过（test_init_generated_en_template_passes_validate）
  </behavior>

  <action>
    在 `src/cli/init.rs` 的 `CONFIG_TEMPLATE_EN` 字符串常量内，定位到 `[filter.indicators]`（约第 113 行起）与 `[filter.sql]`（约第 119 行起）两个子节。

    对 `[filter.indicators]` 节中以下 3 个被 `#` 注释掉的字段示例，将原本仅有的 `# key = value` 单行改造为 `# key = value   # <英文描述>` 形式（与 Task 1 风格一致，至少 2 个空格分隔示例与描述）：

    - `exec_ids`：`Transaction-level: retain whole transaction if any record's exec_id matches`
    - `min_runtime_ms`：`Transaction-level: retain whole transaction if any statement's runtime (ms) >= threshold`
    - `min_row_count`：`Transaction-level: retain whole transaction if any statement's row_count >= threshold`

    对 `[filter.sql]` 节中以下 2 个被 `#` 注释掉的字段示例，补注：

    - `includes`：`Transaction-level: retain whole transaction if any SQL text contains any substring listed`
    - `excludes`：`Transaction-level: drop whole transaction if any SQL text contains any substring listed`

    保持以下内容完全不变：
    - 节级注释（`# --- Indicator filters (transaction-level: match retains the whole transaction; requires pre-scan) ---`、`# --- SQL filters (transaction-level: match retains the whole transaction; requires pre-scan) ---`）
    - 节标题 `[filter.indicators]`、`[filter.sql]`
    - 字段示例值（`[257809109, 257809110]`、`1000`、`100`、`["FROM USER_TABLES", "DELETE FROM"]`、`["SELECT 1", "DUAL"]` 等示例值保持原样）
    - 其他节（[filter.include]/[filter.exclude] 由 Task 1 处理；[stats]/[exporter.*] 不动）

    执行 D-04 行内注释格式约束。注释一律英文。
  </action>

  <verify>
    <automated>cd /Users/guang/Projects/sqllog2db && cargo test test_init --quiet 2>&1 | tail -10</automated>
    <automated>cd /Users/guang/Projects/sqllog2db && cargo run --quiet -- init -o /tmp/p62-task2.toml --force 2>/dev/null && awk '/^\[filter\.indicators\]/,/^\[filter\.sql\]/' /tmp/p62-task2.toml | grep -E "^# (exec_ids|min_runtime_ms|min_row_count) = " | grep -vc " # "</automated>
    <automated>cd /Users/guang/Projects/sqllog2db && awk '/^\[filter\.sql\]/,/^\[stats\]/' /tmp/p62-task2.toml | grep -E "^# (includes|excludes) = " | grep -vc " # "</automated>
    <!-- 后两个 verify：indicators/sql 节下示例字段如缺 ` # ` 行内注释分隔符则输出 > 0；期望均为 0。 -->
  </verify>

  <acceptance_criteria>
    - `cargo test test_init` 全部通过（包括 test_init_generates_new_nested_format、test_init_generated_en_template_passes_validate）
    - `cargo run -- init -o /tmp/p62-task2.toml --force` 生成的文件中：
      - `grep -E "^# (exec_ids|min_runtime_ms|min_row_count) = .* # [A-Z]" /tmp/p62-task2.toml | wc -l` 输出 = 3
      - `grep -E "^# (includes|excludes) = .* # [A-Z]" /tmp/p62-task2.toml | wc -l` 输出 = 2
    - `cargo run -- validate -c /tmp/p62-task2.toml` 退出码 0（生成的模板仍能通过 validate）
    - 文件中 [filter.indicators] 与 [filter.sql] 节内无中文字符
  </acceptance_criteria>

  <done>
    `[filter.indicators]` 节 3 个字段与 `[filter.sql]` 节 2 个字段全部带英文行内注释；`cargo test test_init` 全部通过；生成的模板仍能通过 `sqllog2db validate`；至此整个 [filter.*] 子树共 22 个示例字段（include 10 + exclude 7 + indicators 3 + sql 2）均有行内注释，满足 Phase 62 Success Criteria #3。
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: 全套质量门禁与 Phase 62 Success Criteria #4 验证</name>

  <read_first>
    - src/cli/init.rs（Task 1 + Task 2 完成后的最终版本——必读，目视确认全部 22 处行内注释存在）
    - .planning/ROADMAP.md（Phase 62 节，确认 Success Criteria #3 与 #4）
    - CLAUDE.md（项目质量门禁要求：clippy 必须无警告、cargo test、cargo fmt）
  </read_first>

  <behavior>
    - 行为 1：`cargo clippy --all-targets -- -D warnings` 退出码 0（无任何警告）
    - 行为 2：`cargo test` 完整通过（不仅 test_init，所有测试都通过——证明模板修改未引发回归）
    - 行为 3：`cargo fmt --check` 退出码 0（init.rs 字符串修改不影响代码格式）
    - 行为 4：Phase 47 引入的 init 模板注释存在性断言（test_init_template_has_csv_append_comment、test_init_template_has_sqlite_field_comments、test_init_template_contains_stats_section）仍全部通过（验证 Phase 62 Success Criteria #4）
  </behavior>

  <action>
    执行项目三道质量门禁（CLAUDE.md 规定的标准流程）：

    1. 运行 `cargo fmt`（自动格式化），然后 `cargo fmt --check` 验证无差异。
    2. 运行 `cargo clippy --all-targets -- -D warnings` 验证无任何警告。
    3. 运行 `cargo test` 验证全套测试通过。

    若任一门禁失败，定位失败测试名与文件：行号，回到 Task 1 或 Task 2 调整 CONFIG_TEMPLATE_EN 文本（最常见原因是误删了被 `contains(...)` 断言的字符串片段，或注释中混入了中文字符触发 test_handle_init_en_template 失败）。

    单独验证 Phase 62 Success Criteria #4（Phase 47 注释断言）通过：执行 `cargo test test_init_template_has_csv_append_comment test_init_template_has_sqlite_field_comments test_init_template_contains_stats_section --quiet` 并确认输出含 `test result: ok. 3 passed`。

    无需修改源代码，仅运行门禁；如出现失败，记录到 SUMMARY 并修复后重新跑全套。
  </action>

  <verify>
    <automated>cd /Users/guang/Projects/sqllog2db && cargo fmt --check</automated>
    <automated>cd /Users/guang/Projects/sqllog2db && cargo clippy --all-targets -- -D warnings 2>&1 | tail -5</automated>
    <automated>cd /Users/guang/Projects/sqllog2db && cargo test 2>&1 | tail -10</automated>
    <automated>cd /Users/guang/Projects/sqllog2db && cargo test test_init_template_has_csv_append_comment test_init_template_has_sqlite_field_comments test_init_template_contains_stats_section --quiet 2>&1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - `cargo fmt --check` 退出码 0
    - `cargo clippy --all-targets -- -D warnings` 退出码 0，stderr 无 `warning:` 行
    - `cargo test` 输出末尾包含 `test result: ok` 与 `0 failed`
    - Phase 47 三个注释断言测试单独跑通过：输出含 `3 passed; 0 failed`
    - 最终 `sqllog2db init -o /tmp/p62-final.toml --force` 生成的文件中，所有 22 个 `[filter.*]` 子节下的示例字段（10+7+3+2）每行都带 ` # ` 引导的英文描述（可用 `awk '/^\[filter\./,/^\[stats\]/' /tmp/p62-final.toml | grep -E "^# [a-z_]+ = " | grep -vc " # [A-Z]"` 输出 0 验证）
  </acceptance_criteria>

  <done>
    三道质量门禁（fmt/clippy/test）全部绿灯；Phase 47 注释存在性断言（Phase 62 Success Criteria #4）通过；DOC-03 完整交付——[filter.*] 子树下 22 个示例字段全部有英文行内注释，sqllog2db init 生成的模板每个示例字段均自带说明。
  </done>
</task>

</tasks>

<verification>
- `cargo fmt --check` 退出码 0
- `cargo clippy --all-targets -- -D warnings` 退出码 0
- `cargo test` 全部通过
- `sqllog2db init -o /tmp/p62-final.toml --force` 生成文件经 `awk '/^\[filter\./,/^\[stats\]/' | grep -E "^# [a-z_]+ = "` 输出的每一行都含 ` # ` 行内注释分隔符（至少 22 行字段）
- Phase 47 引入的注释断言（test_init_template_has_csv_append_comment、test_init_template_has_sqlite_field_comments、test_init_template_contains_stats_section）仍全部通过
</verification>

<success_criteria>
- ROADMAP Phase 62 Success Criteria #3 达成：`[stats]` 节 from/to 字段（已有）与 `[filters]` 各子字段（本 Plan 新增 22 个）均有行内注释
- ROADMAP Phase 62 Success Criteria #4 达成：Phase 47 新增的注释存在性断言仍全部通过
- DOC-03 requirement 完成
- CLAUDE.md 三道质量门禁（clippy 无警告 + test + fmt）全绿
</success_criteria>

<output>
Create `.planning/phases/62-docs/62-01-SUMMARY.md` when done
</output>
