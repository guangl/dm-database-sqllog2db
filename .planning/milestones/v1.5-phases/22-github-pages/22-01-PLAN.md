---
phase: 22-github-pages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - site/book.toml
  - site/src/SUMMARY.md
  - site/theme/custom.css
  - .github/workflows/pages.yml
  - Cargo.toml
autonomous: true
requirements:
  - PAGES-02
  - SUPP-06

must_haves:
  truths:
    - "用户推送 site/** 变更后，GitHub Actions 自动构建 mdBook 并部署到 gh-pages 分支"
    - "Cargo.toml 的 documentation 字段指向 https://guangl.github.io/sqllog2db/"
    - "site/ 目录包含有效的 mdBook 配置（book.toml、SUMMARY.md、theme CSS）"
  artifacts:
    - path: "site/book.toml"
      provides: "mdBook 构建配置（标题、主题、输出路径）"
      contains: "[book] title = sqllog2db"
    - path: "site/src/SUMMARY.md"
      provides: "mdBook 页面结构（单页导航）"
      contains: "index.md"
    - path: "site/theme/custom.css"
      provides: "自定义品牌色调和排版"
      contains: ".brand-color"
    - path: ".github/workflows/pages.yml"
      provides: "GitHub Actions 自动构建部署流水线"
      contains: "peaceiris/actions-gh-pages"
    - path: "Cargo.toml"
      provides: "documentation 字段指向 Pages URL"
      contains: 'documentation = "https://guangl.github.io/sqllog2db/"'
  key_links:
    - from: ".github/workflows/pages.yml"
      to: "site/book.toml"
      via: "mdbook build site"
      pattern: "mdbook build"
    - from: ".github/workflows/pages.yml"
      to: "gh-pages branch"
      via: "peaceiris/actions-gh-pages"
      pattern: "actions-gh-pages"
    - from: "Cargo.toml"
      to: "https://guangl.github.io/sqllog2db/"
      via: "documentation field"
      pattern: "documentation"
---

<objective>
建立 mdBook 单页落地页的基础设施（目录结构、配置、主题），并创建 GitHub Actions 自动部署流水线。更新 Cargo.toml 的 documentation 字段指向 Pages URL。

Purpose: 为 Phase 22-02 的落地页内容提供构建和部署的底层能力。mdBook 是 Rust 原生 SSG，零 Node.js 依赖。
Output:
  - site/book.toml — mdBook 构建配置
  - site/src/SUMMARY.md — 单页导航结构
  - site/theme/custom.css — 自定义品牌 CSS
  - .github/workflows/pages.yml — 推送 site/** 时自动构建部署
  - Cargo.toml documentation 字段更新
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
<interfaces>
<!-- GitHub Actions workflow pattern (from existing ci.yaml) -->
From .github/workflows/ci.yaml:
- uses: actions/checkout@v6
- uses: dtolnay/rust-toolchain@stable
- uses: Swatinem/rust-cache@v2
- uses: taiki-e/install-action@v2 (for tool installation)
- Env vars pattern: CARGO_TERM_COLOR: always

From .github/workflows/release.yaml:
- permissions: contents: write (for gh-pages deployment)
- uses: softprops/action-gh-release@v3

<!-- Cargo.toml current state (line 7-9) -->
From Cargo.toml:
  description = "高性能 CLI 工具..."
  homepage = "https://github.com/guangl/sqllog2db"
  repository = "https://github.com/guangl/sqllog2db"
  <!-- No documentation field currently exists -->
</interfaces>

@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/22-github-pages/22-CONTEXT.md

# Context files for deployment
@.github/workflows/ci.yaml (pattern reference)
@Cargo.toml (current state)
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建 mdBook 目录结构和配置文件</name>
  <files>
    site/book.toml
    site/src/SUMMARY.md
    site/theme/custom.css
  </files>
  <read_first>
    @.planning/phases/22-github-pages/22-CONTEXT.md (D-04: site/ directory, D-05: default theme + custom CSS)
  </read_first>
  <action>
    创建以下文件和目录：

    1. site/book.toml — mdBook 构建配置：
       - [book] title = "sqllog2db", authors = ["guangl"], description 与 Cargo.toml 一致（"高性能 CLI 工具：流式解析达梦数据库 SQL 日志并导出到 CSV/JSONL/SQLite"）, language = "en"
       - [output.html] default-theme = "ayu", preferred-dark-theme = "ayu", 不包含 no-section-label（保持默认）, git-repository-url = "https://github.com/guangl/sqllog2db", edit-url-template 指向 site/src/{path}
       - [output.html.fold] enable = true, level = 1 — 使 SVG Gallery 的 details 标签可折叠
       - [output.html.playground] editor = false, runnable = false
       - [output.html] additional-css = ["theme/custom.css"] — 引用自定义样式文件
       - [build] create-missing = false（如果 index.md 不在时构建失败，这是预期的）

    2. site/src/SUMMARY.md — 单页导航，仅包含一个章节：
       ```
       # Summary
       [sqllog2db](index.md)
       ```

    3. site/theme/custom.css — 自定义 CSS：
       - 使用 mdBook 的 CSS 变量（--md-* 前缀或 .ayu 主题类）覆盖品牌色
       - 主题色使用蓝色系（主色 #2563eb / blue-600），强调色使用青色（#06b6d4 / cyan-500）
       - 调整正文排版：font-size 16px, line-height 1.7, 代码块圆角 6px
       - Hero 区域（页首大标题）样式：加大标题字号（h1 2.5rem），增加上边距（padding-top: 2rem）
       - 性能表格样式：交替行背景色、边框圆角
       - SVG Gallery 的 details/summary 样式：summary 加粗、鼠标指针为手形、折叠时透明度回调
       - 链接颜色使用主色 #2563eb，鼠标悬停时加深至 #1d4ed8
       - 不修改 mdBook 的默认布局结构（侧栏、页脚等使用默认样式）

    mdBook 目录结构为：
    ```
    site/
      book.toml
      src/
        SUMMARY.md
        index.md  (由 22-02-PLAN.md 创建)
      theme/
        custom.css
    ```

    注意：site/src/index.md 由 22-02-PLAN.md 的 Task 2 创建。本任务不创建 index.md。
  </action>
  <acceptance_criteria>
    - site/book.toml 存在，且包含 [book] title = "sqllog2db" 和 [output.html] additional-css
    - site/src/SUMMARY.md 存在，且内容包含 "index.md"
    - site/theme/custom.css 存在，且包含颜色和排版相关的 CSS 规则（色值 #2563eb）
    - 目录结构符合 site/book.toml + site/src/ + site/theme/ 布局
  </acceptance_criteria>
  <verify>
    <automated>test -f site/book.toml && grep -q 'title.*sqllog2db' site/book.toml && test -f site/src/SUMMARY.md && grep -q 'index.md' site/src/SUMMARY.md && test -f site/theme/custom.css && grep -q '#2563eb\|#06b6d4' site/theme/custom.css</automated>
  </verify>
  <done>site/ 目录包含完整的 mdBook 项目骨架（book.toml + SUMMARY.md + custom.css），可供 mdbook build 使用</done>
</task>

<task type="auto">
  <name>Task 2: 创建 GitHub Actions 部署流水线并更新 Cargo.toml documentation 字段</name>
  <files>
    .github/workflows/pages.yml
    Cargo.toml
  </files>
  <read_first>
    @.github/workflows/ci.yaml (workflow 风格参考 — checkout@v6、env 变量模式、job 结构)
    @Cargo.toml (当前 state — 读取第 1-15 行的 [package] 区域，确认当前无 documentation 字段)
    @.planning/phases/22-github-pages/22-CONTEXT.md (D-07: 推送 site/** 时触发, D-08: 无需手动触发, D-10: Cargo.toml documentation 字段)
  </read_first>
  <action>
    创建 .github/workflows/pages.yml 和修改 Cargo.toml：

    1. .github/workflows/pages.yml：
       - name: "Pages"
       - on.push.branches: [main], on.push.paths: ["site/**"] — 推送 site/** 变更时触发（D-07）
       - 不包含 workflow_dispatch（D-08：无需手动触发）
       - env: CARGO_TERM_COLOR: always
       - 单个 job:
         - name: "Deploy"
         - runs-on: ubuntu-latest
         - permissions.contents: write（用于 gh-pages 分支推送）
         - steps:
           - uses: actions/checkout@v6
           - name: "Install mdBook"
             uses: peaceiris/actions-mdbook@v2
             with.mdbook-version: "latest"
           - name: "Build"
             run: mdbook build site
           - name: "Deploy to GitHub Pages"
             uses: peaceiris/actions-gh-pages@v4
             with.github_token: ${{ secrets.GITHUB_TOKEN }}（使用默认的 GITHUB_TOKEN）
             with.publish_dir: site/book
             with.publish_branch: gh-pages（默认值，可显式指定）
             with.force_orphan: true（确保 gh-pages 分支包含干净的构建产物）

    2. Cargo.toml — 在第 7 行（description）和第 8 行（homepage）之间添加：
       ```
       documentation = "https://guangl.github.io/sqllog2db/"
       ```
       保持字段按字母顺序排列（description → documentation → homepage）。
       这是唯一的变化，不修改其他任何字段。
  </action>
  <acceptance_criteria>
    - .github/workflows/pages.yml 存在
    - pages.yml 的 on.push.paths 包含 "site/**"
    - pages.yml 使用 peaceiris/actions-mdbook 和 peaceiris/actions-gh-pages
    - pages.yml 不包含 workflow_dispatch
    - Cargo.toml 包含 "documentation = \"https://guangl.github.io/sqllog2db/\""
    - Cargo.toml 其他字段未发生变化
  </acceptance_criteria>
  <verify>
    <automated>test -f .github/workflows/pages.yml && grep -q 'site/\*\*' .github/workflows/pages.yml && grep -q 'peaceiris/actions-gh-pages' .github/workflows/pages.yml && grep -q 'documentation.*guangl.github.io/sqllog2db' Cargo.toml && ! grep -q 'workflow_dispatch' .github/workflows/pages.yml</automated>
  </verify>
  <done>推送 site/** 到 main 时自动触发 Pages 部署；Cargo.toml 的 documentation 字段指向已规划的 GitHub Pages URL</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Phase 22 是纯文档阶段，无代码变更。不存在运行时信任边界。第三方 GitHub Action（peaceiris/actions-gh-pages）在 CI 执行环境中处理部署令牌。

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-01 | Tampering | .github/workflows/pages.yml | accept | workflow 只被 site/** 变更触发，推送者需有仓库写入权限；peaceiris/actions-gh-pages 是 GitHub 市场经过验证的 Action，SHA 固定非必要 |
| T-22-02 | Information Disclosure | GITHUB_TOKEN 在 pages.yml | mitigate | 使用默认 secrets.GITHUB_TOKEN（权限最小化，仅在 workflow 生命周期内有效），不添加自定义 secrets |
</threat_model>

<verification>
- Cargo.toml 变更后运行 `cargo check` 确认语法正确
- `cargo clippy --all-targets -- -D warnings` 通过（Cargo.toml 变更不应触发 lint 变更）
</verification>

<success_criteria>
1. site/ 目录结构完整（book.toml + SUMMARY.md + custom.css），可供 mdBook 构建
2. pages.yml 推送到 main + site/** 变更时正确触发
3. Cargo.toml documentation 字段指向 https://guangl.github.io/sqllog2db/
</success_criteria>

<output>
Create `.planning/phases/22-github-pages/22-01-SUMMARY.md` when done
</output>
