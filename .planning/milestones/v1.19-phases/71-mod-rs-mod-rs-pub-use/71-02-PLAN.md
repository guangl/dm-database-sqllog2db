---
phase: 71-mod-rs-mod-rs-pub-use
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pipeline/filters/mod.rs
  - src/pipeline/filters/feature_ops.rs
  - src/pipeline/filters/indicator_ops.rs
  - src/pipeline/filters/sql_ops.rs
  - src/pipeline/filters/tests.rs
autonomous: true
requirements: []
must_haves:
  truths:
    - "src/pipeline/filters/mod.rs 仅含 mod 声明与 pub use 重导出"
    - "FiltersFeature / IndicatorFilters / SqlFilters / TrxidSet 在原路径下仍可访问"
    - "cargo test 全部通过，原 18+ 单元测试不丢失"
    - "cargo clippy --all-targets -- -D warnings 通过"
  artifacts:
    - path: "src/pipeline/filters/mod.rs"
      provides: "module declarations + pub use re-exports only"
    - path: "src/pipeline/filters/feature_ops.rs"
      provides: "FiltersFeature 的 impl 块（has_transaction_filters, merge_found_trxids, has_filters）"
      contains: "impl FiltersFeature"
    - path: "src/pipeline/filters/indicator_ops.rs"
      provides: "IndicatorFilters 的 impl 块（has_filters, matches）"
      contains: "impl IndicatorFilters"
    - path: "src/pipeline/filters/sql_ops.rs"
      provides: "SqlFilters 的 impl 块（has_filters）"
      contains: "impl SqlFilters"
    - path: "src/pipeline/filters/tests.rs"
      provides: "全部 18+ 单元测试"
      contains: "#[test]"
  key_links:
    - from: "src/pipeline/filters/mod.rs"
      to: "feature_ops/indicator_ops/sql_ops"
      via: "mod feature_ops; mod indicator_ops; mod sql_ops;"
      pattern: "mod (feature_ops|indicator_ops|sql_ops)"
    - from: "src/pipeline/mod.rs"
      to: "crate::pipeline::filters::FiltersFeature"
      via: "pub use re-export"
      pattern: "pub use filters::FiltersFeature"
---

<objective>
重构 src/pipeline/filters/mod.rs（246 行）：将三个 impl 块（FiltersFeature、IndicatorFilters、SqlFilters）拆分到 feature_ops.rs/indicator_ops.rs/sql_ops.rs，#[cfg(test)] mod tests 块拆分到 tests.rs，mod.rs 仅保留模块声明与 pub use 重导出。

Purpose: 让过滤器各类型的方法实现就近独立成文件，避免 mod.rs 成为"什么都装"的文件。
Output: 5 个文件，cargo test/clippy 全绿，公开 API 不变。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md

<interfaces>
<!-- 必须保持的公开 API -->

外部（tests/integration.rs 和 crate 其他模块）调用：
- `crate::pipeline::filters::FiltersFeature`（已 pub use 到 pipeline::FiltersFeature）
- `crate::pipeline::filters::IndicatorFilters`（pub）
- `crate::pipeline::filters::SqlFilters`（pub）
- `crate::pipeline::filters::TrxidSet`（pub(crate)）

被 cli/run/mod.rs 调用的方法：
- `FiltersFeature::has_transaction_filters() -> bool`（pub(crate)）
- `FiltersFeature::merge_found_trxids(Vec<String>)`（pub(crate)）

被 #[cfg(test)] / tests/integration.rs 调用：
- `FiltersFeature::has_filters() -> bool`（#[cfg(test)] pub）
- `IndicatorFilters::has_filters() -> bool`（pub(crate)）
- `IndicatorFilters::matches(...)`（#[cfg(test)] pub）
- `SqlFilters::has_filters() -> bool`（pub(crate)）

子模块声明（保持不变）：
- `mod serde_helpers;`（私有）
- `pub mod types;`（公开）
- `pub(crate) use serde_helpers::TrxidSet;`

types::* 已经存在并提供 struct 定义，本次重构不动 types.rs 和 serde_helpers.rs。
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 拆分 pipeline/filters/mod.rs 到 4 个新文件</name>
  <files>src/pipeline/filters/mod.rs, src/pipeline/filters/feature_ops.rs, src/pipeline/filters/indicator_ops.rs, src/pipeline/filters/sql_ops.rs, src/pipeline/filters/tests.rs</files>
  <read_first>
    - src/pipeline/filters/mod.rs（全文 246 行）
    - src/pipeline/filters/types.rs（确认 struct 字段名）
    - src/pipeline/filters/serde_helpers.rs（确认 TrxidSet 定义）
    - src/pipeline/mod.rs 第 1-3 行（确认 pub use FiltersFeature 路径）
    - src/cli/run/mod.rs（grep 'filter' 找调用点，确认 has_transaction_filters 与 merge_found_trxids 怎么被调用）
  </read_first>
  <action>
    Step 1: 创建 `src/pipeline/filters/feature_ops.rs`：
      - 移入 mod.rs 第 15-56 行的：
        - `#[cfg(test)] impl FiltersFeature { fn has_filters() }`（注意 cfg gate）
        - `impl FiltersFeature { has_transaction_filters, merge_found_trxids }`（非 cfg gate）
      - 顶部 use：`use super::types::FiltersFeature; use super::serde_helpers::TrxidSet;`

    Step 2: 创建 `src/pipeline/filters/indicator_ops.rs`：
      - 移入 mod.rs 第 58-89 行的 `impl IndicatorFilters { has_filters, matches }`
      - 注意 matches 是 `#[cfg(test)] #[must_use] pub fn`，保留 cfg
      - 顶部 use：`use super::types::IndicatorFilters;`

    Step 3: 创建 `src/pipeline/filters/sql_ops.rs`：
      - 移入 mod.rs 第 91-97 行的 `impl SqlFilters { has_filters }`
      - 顶部 use：`use super::types::SqlFilters;`

    Step 4: 创建 `src/pipeline/filters/tests.rs`：
      - 移入 mod.rs 第 99-246 行 `#[cfg(test)] mod tests { ... }` 内的全部内容（不嵌套 mod tests）
      - 文件顶部 use：`use super::types::{ExcludeFilters, IncludeFilters, IndicatorFilters, SqlFilters, FiltersFeature};` + 视实际情况补充
      - 保留全部测试：make_feature helper 及 18 个 #[test]（test_has_filters_disabled_returns_false ... test_indicator_no_filters_always_false）

    Step 5: 改写 `src/pipeline/filters/mod.rs` 为：
      ```rust
      //! Filters 模块：过滤器类型、impl 方法、test 套件分文件组织。

      mod feature_ops;
      mod indicator_ops;
      mod serde_helpers;
      mod sql_ops;
      pub mod types;

      #[cfg(test)]
      mod tests;

      pub(crate) use serde_helpers::TrxidSet;
      // pub: required by tests/integration.rs
      pub use types::{FiltersFeature, IndicatorFilters, SqlFilters};
      ```
      仅这些行（注释 + mod 声明 + pub use）。

    Step 6: 运行验证序列：
      - `cargo build` 通过
      - `cargo clippy --all-targets -- -D warnings` 通过
      - `cargo test --lib pipeline::filters` 通过
      - `cargo test` 全套通过（含 tests/integration.rs）
      - `cargo fmt`
  </action>
  <verify>
    <automated>cargo clippy --all-targets -- -D warnings && cargo test</automated>
    <grep-check>grep -v '^\s*//' src/pipeline/filters/mod.rs | grep -vE '^\s*$' | grep -E '\bfn \w+|\bstruct \w+|\bimpl \w+' && echo "FAIL: mod.rs 仍含实现" || echo "OK: mod.rs 仅声明"</grep-check>
  </verify>
  <acceptance_criteria>
    - cargo clippy --all-targets -- -D warnings 退出码 0
    - cargo test 退出码 0
    - src/pipeline/filters/mod.rs 过滤注释/空行后，所有非空行的关键字只包括 `mod`/`pub use`/`pub(crate) use`/属性宏（#[cfg(test)]），无 `fn`/`struct`/`impl`
    - FiltersFeature / IndicatorFilters / SqlFilters / TrxidSet 在 crate::pipeline::filters 命名空间下仍可访问（pub use types::* 保留）
    - 全部 18+ 单元测试仍在 tests.rs 中运行通过
  </acceptance_criteria>
  <done>
    mod.rs 简化到 ≤ 15 行（含注释），三个 ops.rs + tests.rs 完整承接实现与测试，质量门禁全绿。
  </done>
</task>

</tasks>

<verification>
- cargo clippy --all-targets -- -D warnings 通过
- cargo test 通过（含 tests/integration.rs 中所有使用 FiltersFeature 的集成测试）
- src/pipeline/filters/mod.rs grep -E '\bfn |\bstruct |\bimpl ' 无匹配
</verification>

<success_criteria>
- mod.rs 仅含 mod 声明与 pub use（≤ 15 行）
- 三个 impl 块按类型分到 feature_ops/indicator_ops/sql_ops
- #[cfg(test)] mod tests 块移至 tests.rs
- 全套 cargo 质量门禁通过
</success_criteria>

<output>
Create `.planning/phases/71-mod-rs-mod-rs-pub-use/71-02-SUMMARY.md` when done
</output>
