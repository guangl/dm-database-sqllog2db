---
phase: 71-mod-rs-mod-rs-pub-use
plan: 10
type: execute
wave: 3
depends_on: []
files_modified:
  - src/cli/watch/mod.rs
  - src/cli/watch/handler.rs
  - src/cli/watch/state.rs
  - src/cli/watch/watcher.rs
  - src/cli/watch/event.rs
  - src/cli/watch/trigger_full.rs
  - src/cli/watch/trigger_incremental.rs
  - src/cli/watch/dirs.rs
  - src/cli/watch/status.rs
  - src/cli/watch/append.rs
  - src/cli/watch/debounce.rs
  - src/cli/watch/tests.rs
autonomous: true
requirements: []
must_haves:
  truths:
    - "src/cli/watch/mod.rs 仅含 mod 声明与 pub use 重导出"
    - "crate::cli::watch::handle_watch 公开签名不变（main.rs 调用入口）"
    - "WatchLoopState struct + new + trigger_count + total_stats + file_offsets 方法全部可达"
    - "collect_watch_dirs / trigger_full_file / trigger_incremental / read_bytes_to_tempfile / format_elapsed_hms 公开 API 保持（被集成测试调用）"
    - "offsets 子模块路径保持 pub(super) 不变"
    - "全部 14 个原单元测试 + 3 个 WATCH-07/08/09 集成测试全部通过"
    - "cargo clippy --all-targets -- -D warnings 通过"
  artifacts:
    - path: "src/cli/watch/mod.rs"
      provides: "module declarations + pub use only"
    - path: "src/cli/watch/handler.rs"
      provides: "handle_watch 主入口 + run_watch_loop + maybe_refresh_status"
      contains: "pub fn handle_watch"
    - path: "src/cli/watch/state.rs"
      provides: "WatchLoopState struct + impl + 两个常量 DEBOUNCE_WINDOW / STATUS_REFRESH_INTERVAL"
      contains: "pub struct WatchLoopState"
    - path: "src/cli/watch/watcher.rs"
      provides: "create_watcher 函数"
      contains: "fn create_watcher"
    - path: "src/cli/watch/event.rs"
      provides: "handle_event 函数（按 EventKind 路由）"
      contains: "fn handle_event"
    - path: "src/cli/watch/trigger_full.rs"
      provides: "trigger_full_file + record_offset_after_trigger + update_status_bar"
      contains: "pub fn trigger_full_file"
    - path: "src/cli/watch/trigger_incremental.rs"
      provides: "trigger_incremental + resolve_incremental_offset + read_bytes_to_tempfile + run_incremental_handle_run + build_incremental_cfg"
      contains: "pub fn trigger_incremental"
    - path: "src/cli/watch/dirs.rs"
      provides: "collect_watch_dirs + format_paths_display"
      contains: "pub fn collect_watch_dirs"
    - path: "src/cli/watch/status.rs"
      provides: "build_progress_bar + render_active_status + refresh_active_status + update_status_bar(?) + format_elapsed_hms + print_final_summary"
      contains: "pub fn format_elapsed_hms"
    - path: "src/cli/watch/append.rs"
      provides: "force_append_for_watch_trigger（WATCH-07/08 实现核心）"
      contains: "fn force_append_for_watch_trigger"
    - path: "src/cli/watch/debounce.rs"
      provides: "should_trigger（防抖逻辑）"
      contains: "fn should_trigger"
    - path: "src/cli/watch/tests.rs"
      provides: "原 14 个单元测试 + 3 个 WATCH-07/08/09 集成测试"
      contains: "#[test]"
  key_links:
    - from: "src/cli/watch/mod.rs"
      to: "handler / state / watcher / event / trigger_* / dirs / status / append / debounce"
      via: "mod 声明 + pub use"
      pattern: "pub use handler::handle_watch"
    - from: "src/main.rs"
      to: "crate::cli::watch::handle_watch"
      via: "调用入口"
      pattern: "cli::watch::handle_watch"
    - from: "tests/watch_incremental.rs"
      to: "trigger_full_file / trigger_incremental / WatchLoopState"
      via: "pub use re-exports"
      pattern: "watch::(trigger_full_file|trigger_incremental|WatchLoopState)"
---

<objective>
重构 src/cli/watch/mod.rs（998 行）：按职责将其内部 25+ 个函数 + WatchLoopState + 常量拆到 10 个新文件，#[cfg(test)] mod tests 块拆到 tests.rs，mod.rs 仅保留 mod 声明与 pub use 重导出。offsets.rs 子模块文件不动。

Purpose: watch/mod.rs 是项目中最大的文件，按 watch 流程的阶段（启动 → watcher 创建 → 事件循环 → 路由 → 全量触发 → 增量触发 → 状态展示 → 防抖 → 强制 append → 摘要）切分后每文件 < 120 行，可读性巨幅提升。
Output: 11 个文件（mod.rs 极简 + 10 个职责文件 + tests.rs），cargo test/clippy 全绿，集成测试无回归。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md

<interfaces>
<!-- 必须保持的公开 API -->

公开（被 main.rs 调用）：
- `pub fn handle_watch(cfg: &Config, quiet: bool, verbose: bool, interrupted: &Arc<AtomicBool>) -> Result<()>`

公开（被 tests/watch_incremental.rs 集成测试调用）：
- `pub struct WatchLoopState`
- `pub fn WatchLoopState::new(init_offsets, sqlite_db_url) -> Self`
- `pub fn WatchLoopState::trigger_count() -> u64`
- `pub fn WatchLoopState::total_stats() -> &ErrorStats`
- `pub fn WatchLoopState::file_offsets() -> &HashMap<PathBuf, u64>`（#[allow(dead_code)] 保留）
- `pub fn collect_watch_dirs(inputs: &[String]) -> Vec<PathBuf>`
- `pub fn trigger_full_file(path, cfg, quiet, verbose, interrupted, state, pb)`
- `pub fn trigger_incremental(path, cfg, quiet, verbose, interrupted, state, pb)`
- `pub fn format_elapsed_hms(elapsed: Duration) -> String`

pub(super)（mod.rs 自己引用 offsets）：
- `pub(super) mod offsets;` — 保持不动

pub(super)（被同模块兄弟文件访问，本 plan 创建）：
- `pub(super) fn read_bytes_to_tempfile(path, start_offset) -> std::io::Result<NamedTempFile>` （tests 中调用）

常量（位于 state.rs 顶部）：
- `pub(super) const DEBOUNCE_WINDOW: Duration = Duration::from_millis(500);`
- `pub(super) const STATUS_REFRESH_INTERVAL: Duration = Duration::from_millis(200);`

WatchLoopState 字段：
```rust
pub struct WatchLoopState {
    pub(super) last_trigger_at: Option<Instant>,
    pub(super) last_status_refresh: Instant,
    pub(super) debounce_map: HashMap<PathBuf, Instant>,
    pub(super) total_stats: ErrorStats,
    pub(super) trigger_count: u64,
    pub(super) file_offsets: HashMap<PathBuf, u64>,
    pub(super) sqlite_db_url: Option<String>,
}
```
注意：原字段是私有的，但被 handler.rs / event.rs / trigger_*.rs / status.rs 等多个兄弟文件读写 → 所有字段必须升级为 `pub(super)`，以保持原行为且让兄弟文件可访问。

外部依赖（grep tests/watch_incremental.rs 确认使用路径）：
```rust
use sqllog2db::cli::watch::{trigger_full_file, trigger_incremental, WatchLoopState};
```
这些路径通过 mod.rs 的 pub use 必须可达。
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 拆分基础设施层（state + debounce + dirs + append）</name>
  <files>src/cli/watch/state.rs, src/cli/watch/debounce.rs, src/cli/watch/dirs.rs, src/cli/watch/append.rs</files>
  <read_first>
    - src/cli/watch/mod.rs（全文 998 行，重点 1-150 行 + 215-265 行 + 521-573 行）
    - src/cli/watch/offsets.rs（确认 ensure_offset_table/load_offsets/save_offset 签名）
    - tests/watch_incremental.rs（确认对外公开 API 调用）
  </read_first>
  <action>
    Step 1: 创建 `src/cli/watch/state.rs`：
      - 移入 mod.rs 第 20-27 行的两个常量：
        ```rust
        pub(super) const DEBOUNCE_WINDOW: Duration = Duration::from_millis(500);
        pub(super) const STATUS_REFRESH_INTERVAL: Duration = Duration::from_millis(200);
        ```
        （含原注释）
      - 移入 mod.rs 第 103-149 行的：
        - `pub struct WatchLoopState { ... }` （7 个字段全部加 `pub(super)`）
        - `impl WatchLoopState { new, trigger_count, total_stats, file_offsets }`
      - 顶部 use：
        ```rust
        use crate::error::ErrorStats;
        use std::collections::HashMap;
        use std::path::PathBuf;
        use std::time::{Duration, Instant};
        ```

    Step 2: 创建 `src/cli/watch/debounce.rs`：
      - 移入 mod.rs 第 549-565 行的：
        - `pub(super) fn should_trigger(path: &Path, map: &mut HashMap<PathBuf, Instant>, now: Instant, window: Duration) -> bool`
      - 顶部 use：
        ```rust
        use std::collections::HashMap;
        use std::path::{Path, PathBuf};
        use std::time::{Duration, Instant};
        ```

    Step 3: 创建 `src/cli/watch/dirs.rs`：
      - 移入 mod.rs 第 215-262 行的：
        - `pub fn collect_watch_dirs(inputs: &[String]) -> Vec<PathBuf>` （保持 pub）
        - `pub(super) fn format_paths_display(dirs: &[PathBuf]) -> String`
      - 顶部 use：
        ```rust
        use std::path::{Path, PathBuf};
        ```

    Step 4: 创建 `src/cli/watch/append.rs`：
      - 移入 mod.rs 第 520-534 行的：
        - `pub(super) fn force_append_for_watch_trigger(cfg: &mut Config)` （WATCH-07/08 实现核心）
      - 顶部 use：
        ```rust
        use crate::config::Config;
        ```
  </action>
  <verify>
    <automated>test -f src/cli/watch/state.rs && test -f src/cli/watch/debounce.rs && test -f src/cli/watch/dirs.rs && test -f src/cli/watch/append.rs && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - 4 个新文件已创建
    - state.rs 含 WatchLoopState + 2 个常量；debounce.rs 含 should_trigger；dirs.rs 含 collect_watch_dirs + format_paths_display；append.rs 含 force_append_for_watch_trigger
    - 此 task 后 mod.rs 仍是原状（暂未改），编译可不通过（因 mod.rs 的同名 fn/struct 会与新文件冲突 → 暂不声明 mod 即可避免）
  </acceptance_criteria>
  <done>
    4 个基础设施文件创建完成，包含原私有 fn/struct 的拷贝，可见性按要求调整为 pub(super)/pub。
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: 拆分流程层（watcher + event + trigger_full + trigger_incremental + status）</name>
  <files>src/cli/watch/watcher.rs, src/cli/watch/event.rs, src/cli/watch/trigger_full.rs, src/cli/watch/trigger_incremental.rs, src/cli/watch/status.rs</files>
  <read_first>
    - src/cli/watch/mod.rs（重点 80-100 行 + 263-296 行 + 298-379 行 + 381-518 行 + 197-211 行 + 567-618 行）
    - 上一步创建的 state.rs / dirs.rs / append.rs / debounce.rs
  </read_first>
  <action>
    Step 1: 创建 `src/cli/watch/watcher.rs`：
      - 移入 mod.rs 第 80-100 行的：
        - `pub(super) fn create_watcher(watch_dirs: &[PathBuf]) -> Result<(Receiver<notify::Result<notify::Event>>, RecommendedWatcher)>`
      - 顶部 use：
        ```rust
        use crate::error::{Error, Result};
        use notify::{Config as NotifyConfig, RecommendedWatcher, RecursiveMode, Watcher};
        use std::path::PathBuf;
        use std::sync::mpsc::Receiver;
        ```

    Step 2: 创建 `src/cli/watch/event.rs`：
      - 移入 mod.rs 第 264-296 行的：
        - `pub(super) fn handle_event(event, cfg, quiet, verbose, interrupted, state, pb)`
      - 顶部 use：
        ```rust
        use super::debounce::should_trigger;
        use super::state::{WatchLoopState, DEBOUNCE_WINDOW};
        use super::trigger_full::trigger_full_file;
        use super::trigger_incremental::trigger_incremental;
        use crate::config::Config;
        use indicatif::ProgressBar;
        use notify::{EventKind, event::{DataChange, ModifyKind}};
        use std::sync::Arc;
        use std::sync::atomic::AtomicBool;
        use std::time::Instant;
        ```

    Step 3: 创建 `src/cli/watch/trigger_full.rs`：
      - 移入 mod.rs 第 298-359 行的：
        - `pub fn trigger_full_file(path, cfg, quiet, verbose, interrupted, state, pb)` （保持 pub）
        - `fn record_offset_after_trigger(path, state)` （pub(super) for 兄弟文件不需要 → 保留 fn 私有，因为只在 trigger_full.rs 内使用）
      - 同步移入 mod.rs 第 361-379 行的：
        - `fn update_status_bar(path, state, pb, last_elapsed)`（被 trigger_full 和 trigger_incremental 都调用 → 改为 pub(super)）
      - 顶部 use：
        ```rust
        use super::append::force_append_for_watch_trigger;
        use super::offsets;
        use super::state::WatchLoopState;
        use super::status::render_active_status;
        use crate::config::Config;
        use indicatif::ProgressBar;
        use log::warn;
        use std::path::Path;
        use std::sync::Arc;
        use std::sync::atomic::{AtomicBool, Ordering};
        use std::time::{Duration, Instant};
        ```

    Step 4: 创建 `src/cli/watch/trigger_incremental.rs`：
      - 移入 mod.rs 第 381-543 行的：
        - `pub fn trigger_incremental(path, cfg, quiet, verbose, interrupted, state, pb)` （保持 pub）
        - `fn resolve_incremental_offset(canonical_path, new_size, state) -> Option<u64>`
        - `pub(super) fn read_bytes_to_tempfile(path, start_offset) -> std::io::Result<NamedTempFile>`
        - `fn run_incremental_handle_run(original_path, canonical_path, tmp_file, new_size, cfg, quiet, verbose, interrupted, state, pb)`
        - `fn build_incremental_cfg(cfg: &Config, tmp_file: &NamedTempFile) -> Config`
      - 注意：`update_status_bar` 已迁移到 trigger_full.rs 并改为 pub(super)，此处通过 `use super::trigger_full::update_status_bar;` 引用
      - 顶部 use：
        ```rust
        use super::append::force_append_for_watch_trigger;
        use super::offsets;
        use super::state::WatchLoopState;
        use super::trigger_full::update_status_bar;
        use crate::config::Config;
        use indicatif::ProgressBar;
        use log::warn;
        use std::path::Path;
        use std::sync::Arc;
        use std::sync::atomic::{AtomicBool, Ordering};
        use std::time::Instant;
        ```

    Step 5: 创建 `src/cli/watch/status.rs`：
      - 移入 mod.rs 第 197-211 行 + 567-618 行：
        - `pub(super) fn build_progress_bar(watch_dirs: &[PathBuf]) -> ProgressBar`
        - `pub(super) fn render_active_status(dir, trigger_count, rows, elapsed) -> String`
        - `pub(super) fn refresh_active_status(pb, watch_dirs, trigger_count, rows, last_trigger_at)`
        - `pub fn format_elapsed_hms(elapsed: Duration) -> String` （保持 pub）
        - `pub(super) fn print_final_summary(start: &Instant, trigger_count: u64, rows: usize, quiet: bool)`
      - 顶部 use：
        ```rust
        use super::dirs::format_paths_display;
        use indicatif::{HumanDuration, ProgressBar, ProgressDrawTarget, ProgressStyle};
        use std::path::PathBuf;
        use std::time::{Duration, Instant};
        ```
  </action>
  <verify>
    <automated>test -f src/cli/watch/watcher.rs && test -f src/cli/watch/event.rs && test -f src/cli/watch/trigger_full.rs && test -f src/cli/watch/trigger_incremental.rs && test -f src/cli/watch/status.rs && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - 5 个新文件已创建
    - 公开签名（trigger_full_file / trigger_incremental / read_bytes_to_tempfile / format_elapsed_hms）保持
    - update_status_bar 在 trigger_full.rs 中暴露为 pub(super)，供 trigger_incremental 调用
  </acceptance_criteria>
  <done>
    5 个流程文件创建完成，包含全部流程层函数，可见性按要求调整。
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: 创建 handler.rs + tests.rs，改写 mod.rs 为骨架，端到端验证</name>
  <files>src/cli/watch/mod.rs, src/cli/watch/handler.rs, src/cli/watch/tests.rs</files>
  <read_first>
    - src/cli/watch/mod.rs（含已完成 Task 1+2 后的状态）
    - src/cli/watch/{state,debounce,dirs,append,watcher,event,trigger_full,trigger_incremental,status}.rs（前两步创建）
    - tests/watch_incremental.rs（确认所有 use 路径仍可达）
  </read_first>
  <action>
    Step 1: 创建 `src/cli/watch/handler.rs`：
      - 移入 mod.rs 第 30-78 行的：
        - `pub fn handle_watch(cfg, quiet, verbose, interrupted) -> Result<()>`
      - 移入 mod.rs 第 151-180 行的：
        - `fn run_watch_loop(rx, cfg, quiet, verbose, interrupted, watch_dirs, pb, state)`
      - 移入 mod.rs 第 182-195 行的：
        - `fn maybe_refresh_status(pb, watch_dirs, trigger_count, rows, last_trigger_at, last_status_refresh)`
      - 顶部 use：
        ```rust
        use super::dirs::collect_watch_dirs;
        use super::event::handle_event;
        use super::offsets;
        use super::state::{WatchLoopState, STATUS_REFRESH_INTERVAL};
        use super::status::{build_progress_bar, print_final_summary, refresh_active_status};
        use super::watcher::create_watcher;
        use crate::config::Config;
        use crate::error::{Error, Result};
        use indicatif::ProgressBar;
        use log::warn;
        use notify::RecvTimeoutError;
        use std::collections::HashMap;
        use std::path::PathBuf;
        use std::sync::Arc;
        use std::sync::atomic::{AtomicBool, Ordering};
        use std::sync::mpsc::Receiver;
        use std::time::{Duration, Instant};
        ```

    Step 2: 创建 `src/cli/watch/tests.rs`：
      - 移入 mod.rs 第 620-997 行 `#[cfg(test)] mod tests { ... }` 内的全部内容（不嵌套 mod tests）
      - 顶部 use（按 tests 实际使用调整）：
        ```rust
        use super::debounce::should_trigger;
        use super::dirs::collect_watch_dirs;
        use super::handler::handle_watch;
        use super::state::{WatchLoopState, DEBOUNCE_WINDOW};
        use super::status::{format_elapsed_hms, render_active_status};
        use super::trigger_full::trigger_full_file;
        use super::trigger_incremental::{read_bytes_to_tempfile, trigger_incremental};
        use crate::config::Config;
        use crate::error::Error;
        use std::collections::HashMap;
        use std::path::PathBuf;
        use std::sync::Arc;
        use std::sync::atomic::AtomicBool;
        use std::time::{Duration, Instant};
        ```
      - 保留全部 14 个 #[test] + 3 个 WATCH-07/08/09 #[test] + 3 个常量（DM_LOG_LINE_SEL / DM_LOG_LINE_SEL2 / DM_LOG_LINE_GARBAGE）

    Step 3: 改写 `src/cli/watch/mod.rs` 为：
      ```rust
      //! Watch 子命令：handle_watch 在 handler.rs，按 watch 流程职责分文件组织。

      pub(super) mod offsets;

      mod append;
      mod debounce;
      mod dirs;
      mod event;
      mod handler;
      mod state;
      mod status;
      mod trigger_full;
      mod trigger_incremental;
      mod watcher;

      #[cfg(test)]
      mod tests;

      pub use dirs::collect_watch_dirs;
      pub use handler::handle_watch;
      pub use state::WatchLoopState;
      pub use status::format_elapsed_hms;
      pub use trigger_full::trigger_full_file;
      pub use trigger_incremental::trigger_incremental;
      ```
      仅注释、mod 声明、pub use。

    Step 4: 运行完整质量门禁：
      - `cargo build` 通过
      - `cargo clippy --all-targets -- -D warnings` 通过
      - `cargo test --test watch_incremental` 通过（集成测试）
      - `cargo test` 全套通过
      - `cargo fmt`

    Step 5: 端到端冒烟（watch 模式）：
      ```bash
      cargo run -- init -o /tmp/_71_watch.toml --force
      mkdir -p /tmp/_71_watch_logs
      sed -i.bak 's|inputs = .*|inputs = ["/tmp/_71_watch_logs"]|' /tmp/_71_watch.toml
      # 用超时方式启动 watch，1 秒后退出验证 handle_watch 可加载
      timeout 2 cargo run -- watch -c /tmp/_71_watch.toml 2>&1 | head -3
      ```
      应观察到 watch 启动日志（不应 panic）

    可见性最终汇总（验证表）：
      | 函数/类型 | 文件 | 可见性 |
      |----|----|----|
      | handle_watch | handler.rs | pub |
      | run_watch_loop | handler.rs | fn (private) |
      | maybe_refresh_status | handler.rs | fn (private) |
      | WatchLoopState | state.rs | pub |
      | WatchLoopState 字段 | state.rs | pub(super) |
      | DEBOUNCE_WINDOW | state.rs | pub(super) |
      | STATUS_REFRESH_INTERVAL | state.rs | pub(super) |
      | should_trigger | debounce.rs | pub(super) |
      | collect_watch_dirs | dirs.rs | pub |
      | format_paths_display | dirs.rs | pub(super) |
      | force_append_for_watch_trigger | append.rs | pub(super) |
      | create_watcher | watcher.rs | pub(super) |
      | handle_event | event.rs | pub(super) |
      | trigger_full_file | trigger_full.rs | pub |
      | record_offset_after_trigger | trigger_full.rs | fn |
      | update_status_bar | trigger_full.rs | pub(super) |
      | trigger_incremental | trigger_incremental.rs | pub |
      | resolve_incremental_offset | trigger_incremental.rs | fn |
      | read_bytes_to_tempfile | trigger_incremental.rs | pub(super) |
      | run_incremental_handle_run | trigger_incremental.rs | fn |
      | build_incremental_cfg | trigger_incremental.rs | fn |
      | build_progress_bar | status.rs | pub(super) |
      | render_active_status | status.rs | pub(super) |
      | refresh_active_status | status.rs | pub(super) |
      | format_elapsed_hms | status.rs | pub |
      | print_final_summary | status.rs | pub(super) |
  </action>
  <verify>
    <automated>cargo clippy --all-targets -- -D warnings && cargo test</automated>
    <grep-check>grep -v '^\s*//' src/cli/watch/mod.rs | grep -vE '^\s*$' | grep -E '\bfn \w+|\bstruct \w+|\bimpl ' && echo "FAIL: mod.rs 仍含实现" || echo "OK: mod.rs 仅声明"</grep-check>
    <grep-check>cargo test --test watch_incremental --no-run 2>&1 | grep -E '(error|warning)' || echo "OK: watch_incremental 集成测试编译通过"</grep-check>
  </verify>
  <acceptance_criteria>
    - cargo clippy --all-targets -- -D warnings 退出码 0
    - cargo test 退出码 0（含 tests/watch_incremental.rs 全部测试）
    - src/cli/watch/mod.rs 过滤注释/空行后只含 `mod`/`pub(super) mod`/`pub use`/属性，无 `fn`/`struct`/`impl`
    - crate::cli::watch::{handle_watch, WatchLoopState, collect_watch_dirs, trigger_full_file, trigger_incremental, format_elapsed_hms} 全部可达
    - WatchLoopState 字段对兄弟模块可见（pub(super)）
    - offsets.rs 文件内容完全不变（仅 mod 声明保持 `pub(super) mod offsets;`）
    - 14+3 = 17 个原 tests 全部迁移到 tests.rs 且通过
    - 端到端冒烟：cargo run -- watch -c <config> 可启动（无 panic）
  </acceptance_criteria>
  <done>
    mod.rs 简化到 ≤ 30 行（含注释），10 个职责文件 + tests.rs 承接全部实现，offsets.rs 保持不动，三道质量门禁与冒烟通过。
  </done>
</task>

</tasks>

<verification>
- cargo clippy --all-targets -- -D warnings 通过
- cargo test 通过（含 tests/watch_incremental.rs）
- src/cli/watch/mod.rs grep -E '\bfn |\bstruct |\bimpl ' 无匹配
- src/cli/watch/offsets.rs 内容完全不变
- cargo run -- watch -c <config>（带 timeout）可启动
</verification>

<success_criteria>
- mod.rs 仅含 mod 声明与 pub use（≤ 30 行）
- 10 个职责文件按 watch 流程阶段承接全部实现
- offsets.rs 保持不动
- 全部 17 个 tests 迁移完成且通过
- 全套质量门禁 + 端到端冒烟通过
</success_criteria>

<output>
Create `.planning/phases/71-mod-rs-mod-rs-pub-use/71-10-SUMMARY.md` when done
</output>
