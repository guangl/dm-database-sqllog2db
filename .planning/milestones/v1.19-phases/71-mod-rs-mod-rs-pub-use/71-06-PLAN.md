---
phase: 71-mod-rs-mod-rs-pub-use
plan: 06
type: execute
wave: 2
depends_on: []
files_modified:
  - src/exporter/mod.rs
  - src/exporter/api.rs
  - src/exporter/kind.rs
  - src/exporter/manager.rs
  - src/exporter/stats.rs
  - src/exporter/util.rs
autonomous: true
requirements: []
must_haves:
  truths:
    - "src/exporter/mod.rs 仅含 mod 声明与 pub use 重导出"
    - "Exporter trait / ExportStats / ExporterManager / ExporterKind / CsvExporter / SqliteExporter / strip_ip_prefix / f32_ms_to_i64 / ensure_parent_dir 在原可见性下可达"
    - "cargo test 通过，包含 src/exporter/tests.rs 全部测试"
    - "cargo clippy --all-targets -- -D warnings 通过"
  artifacts:
    - path: "src/exporter/mod.rs"
      provides: "module declarations + pub use only"
    - path: "src/exporter/api.rs"
      provides: "Exporter trait 定义（含全部默认方法）"
      contains: "pub trait Exporter"
    - path: "src/exporter/stats.rs"
      provides: "ExportStats struct + impl"
      contains: "pub struct ExportStats"
    - path: "src/exporter/kind.rs"
      provides: "ExporterKind enum 包装 + 方法"
      contains: "pub(crate) enum ExporterKind"
    - path: "src/exporter/manager.rs"
      provides: "ExporterManager struct + impl + Debug impl"
      contains: "pub(crate) struct ExporterManager"
    - path: "src/exporter/util.rs"
      provides: "strip_ip_prefix / f32_ms_to_i64 / ensure_parent_dir 共享工具函数"
      contains: "pub(super) fn strip_ip_prefix"
  key_links:
    - from: "src/exporter/mod.rs"
      to: "api / stats / kind / manager / util"
      via: "mod 声明 + pub use"
      pattern: "pub use (api|stats|kind|manager)"
    - from: "src/exporter/csv/mod.rs"
      to: "super::{ExportStats, Exporter, ensure_parent_dir}"
      via: "原引用路径必须保持"
      pattern: "use super::"
    - from: "src/exporter/sqlite/mod.rs"
      to: "super::{ExportStats, Exporter}"
      via: "原引用路径必须保持"
      pattern: "use super::"
---

<objective>
重构 src/exporter/mod.rs（310 行）：将其内容拆分为 5 个职责清晰的文件：api.rs（Exporter trait）/ stats.rs（ExportStats）/ kind.rs（ExporterKind enum）/ manager.rs（ExporterManager）/ util.rs（共享 helper），mod.rs 仅保留 mod 声明与 pub use。csv/ 与 sqlite/ 两个子模块本 plan 不动（plan 07/08 处理）。

Purpose: exporter 是热路径核心模块，按类型职责拆开后每文件 < 100 行，可读性大幅提升。
Output: 6 个新/改文件，cargo test/clippy 全绿，原始 use super::* 路径全部保持兼容。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md

<interfaces>
<!-- 必须保持的可见性与路径 -->

公开：
- `pub trait Exporter`（含默认方法 export_one_normalized / export_one_preparsed / stats_snapshot）
- `pub struct ExportStats { exported, skipped, failed, flush_operations, last_flush_size }` 含 new / record_success / total

pub(crate)：
- `enum ExporterKind { Csv(CsvExporter), Sqlite(SqliteExporter) }`
- `struct ExporterManager { exporter: ExporterKind }`
- `ExporterManager::from_csv / from_config / csv_include_performance_metrics / initialize / set_sqlite_wal_mode / export_one_preparsed / finalize / name / log_stats`

pub(super)（只在 exporter 子树中使用）：
- `fn strip_ip_prefix(ip: &str) -> &str`

pub(crate)：
- `fn f32_ms_to_i64(ms: f32) -> i64`
- `fn ensure_parent_dir(path: &Path) -> io::Result<()>`

子模块（保持不动）：
- `pub mod csv;`
- `pub(crate) mod projection;`
- `pub mod sqlite;`
- `pub(crate) use csv::CsvExporter;`
- `pub(crate) use sqlite::SqliteExporter;`

被 csv/mod.rs 引用：
```rust
use super::ensure_parent_dir;
use super::{ExportStats, Exporter};
```

被 sqlite/mod.rs 引用：
```rust
use super::{ExportStats, Exporter};
```

被 cli/run 内部模块引用：
```rust
crate::exporter::ExporterManager  // 通过 pub(crate) use
```

被 writer.rs / projection.rs 引用 strip_ip_prefix / f32_ms_to_i64（grep 验证后再确定可见性是否需要调整）

测试文件 src/exporter/tests.rs（独立文件，本 plan 不动其内容，但需让其顶部 use 路径仍能 resolve）
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 拆分 exporter/mod.rs 到 5 个新文件</name>
  <files>src/exporter/mod.rs, src/exporter/api.rs, src/exporter/kind.rs, src/exporter/manager.rs, src/exporter/stats.rs, src/exporter/util.rs</files>
  <read_first>
    - src/exporter/mod.rs（全文 310 行）
    - src/exporter/csv/mod.rs 第 1-15 行（确认 use super 路径）
    - src/exporter/sqlite/mod.rs 第 1-12 行（确认 use super 路径）
    - src/exporter/tests.rs（确认本 plan 不动的测试文件 use 路径，预期 use super::*; use crate::exporter::*;）
    - src/exporter/projection.rs（grep 'super::' 找内部引用，确认 strip_ip_prefix / f32_ms_to_i64 是否被使用）
    - src/exporter/csv/writer.rs（grep 'strip_ip_prefix\|f32_ms_to_i64\|ensure_parent_dir'）
    - src/exporter/sqlite/write.rs（grep 同上）
    - src/cli/run/mod.rs 第 3 行（确认 ExporterManager 引用路径）
  </read_first>
  <action>
    Step 1: 创建 `src/exporter/stats.rs`：
      - 移入 mod.rs 第 103-127 行的：
        ```rust
        /// 导出统计
        #[derive(Debug, Default, Clone, Copy)]
        pub struct ExportStats { exported, skipped, failed, flush_operations, last_flush_size }

        impl ExportStats {
            pub fn new() -> Self { ... }
            pub fn record_success(&mut self) { ... }
            pub fn total(&self) -> usize { ... }
        }
        ```
      - 无顶部 use 需求（纯定义）

    Step 2: 创建 `src/exporter/api.rs`：
      - 移入 mod.rs 第 12-42 行的 `pub trait Exporter`（含全部默认方法）
      - 顶部 use：
        ```rust
        use super::stats::ExportStats;
        use crate::error::Result;
        use dm_database_parser_sqllog::Sqllog;
        ```

    Step 3: 创建 `src/exporter/kind.rs`：
      - 移入 mod.rs 第 44-101 行的：
        - `#[derive(Debug)] pub(crate) enum ExporterKind { Csv(CsvExporter), Sqlite(SqliteExporter) }`
        - `impl ExporterKind { kind_name, csv_include_performance_metrics, initialize, export_one_preparsed, finalize, stats_snapshot }`
      - 顶部 use：
        ```rust
        use super::api::Exporter;
        use super::stats::ExportStats;
        use super::{CsvExporter, SqliteExporter};
        use crate::error::Result;
        use dm_database_parser_sqllog::Sqllog;
        ```
        说明：CsvExporter / SqliteExporter 通过 mod.rs 中保留的 `pub(crate) use csv::CsvExporter;` 与 `pub(crate) use sqlite::SqliteExporter;` 已经在 exporter 命名空间下，所以 `super::CsvExporter` 仍可访问。

    Step 4: 创建 `src/exporter/manager.rs`：
      - 移入 mod.rs 第 129-255 行的：
        - `pub(crate) struct ExporterManager { exporter: ExporterKind }`
        - `impl std::fmt::Debug for ExporterManager`
        - `impl ExporterManager { from_csv, from_config, csv_include_performance_metrics, initialize, set_sqlite_wal_mode, export_one_preparsed, finalize, name, log_stats }`
      - 顶部 use：
        ```rust
        use super::kind::ExporterKind;
        use super::stats::ExportStats;
        use super::{CsvExporter, SqliteExporter};
        use crate::config::Config;
        use crate::error::{ConfigError, Error, Result};
        use dm_database_parser_sqllog::Sqllog;
        use log::info;
        ```

    Step 5: 创建 `src/exporter/util.rs`：
      - 移入 mod.rs 第 257-307 行的：
        - `pub(super) fn strip_ip_prefix(ip: &str) -> &str`
        - `pub(crate) fn f32_ms_to_i64(ms: f32) -> i64`
        - `pub(crate) fn ensure_parent_dir(path: &std::path::Path) -> std::io::Result<()>`
      - 顶部无 use 需求（用了完整路径）

    Step 6: 改写 `src/exporter/mod.rs` 为：
      ```rust
      //! Exporter 模块：导出器 trait、统计、kind 包装、管理器、共享工具。

      pub mod csv;
      pub(crate) mod projection;
      pub mod sqlite;

      mod api;
      mod kind;
      mod manager;
      mod stats;
      mod util;

      #[cfg(test)]
      mod tests;

      pub use api::Exporter;
      pub use stats::ExportStats;

      pub(crate) use csv::CsvExporter;
      pub(crate) use sqlite::SqliteExporter;
      pub(crate) use kind::ExporterKind;
      pub(crate) use manager::ExporterManager;
      pub(crate) use util::{ensure_parent_dir, f32_ms_to_i64};
      pub(super) use util::strip_ip_prefix;
      ```
      仅注释、mod 声明、pub use。

      **重要可见性细节：**
      - csv/mod.rs 用 `use super::ensure_parent_dir;` — 通过 mod.rs 的 `pub(crate) use util::ensure_parent_dir;` 可见
      - csv/mod.rs 用 `use super::{ExportStats, Exporter};` — 通过 mod.rs 的 `pub use api::Exporter;` 和 `pub use stats::ExportStats;` 可见
      - sqlite/mod.rs 同理
      - exporter/projection.rs / csv/writer.rs / sqlite/write.rs 若用 strip_ip_prefix → 路径仍是 `super::strip_ip_prefix`（exporter 模块内）；用 f32_ms_to_i64 同理

      **如果 grep 发现 strip_ip_prefix 只在 exporter 模块内使用：** 保留 `pub(super)` 不变；mod.rs 用 `pub(super) use util::strip_ip_prefix;` 让子模块可访问。
      **如果 grep 发现 strip_ip_prefix 在 exporter 之外被使用：** 暴露级别提升为 pub(crate)，mod.rs 改为 `pub(crate) use`。

    Step 7: 运行质量门禁：
      - `cargo build`
      - `cargo clippy --all-targets -- -D warnings`
      - `cargo test`
      - `cargo fmt`
  </action>
  <verify>
    <automated>cargo clippy --all-targets -- -D warnings && cargo test</automated>
    <grep-check>grep -v '^\s*//' src/exporter/mod.rs | grep -vE '^\s*$' | grep -E '\bfn \w+|\bstruct \w+|\bimpl |\btrait |\benum ' && echo "FAIL: mod.rs 仍含实现" || echo "OK: mod.rs 仅声明"</grep-check>
    <grep-check>grep -r 'use super::ensure_parent_dir\|use super::{ExportStats\|use super::Exporter\|use super::{Export' src/exporter/csv src/exporter/sqlite && echo "OK: 子模块 super 路径仍解析"</grep-check>
  </verify>
  <acceptance_criteria>
    - cargo clippy --all-targets -- -D warnings 退出码 0
    - cargo test 退出码 0
    - src/exporter/mod.rs 过滤注释/空行后只含 `mod`/`pub mod`/`pub use`/`pub(crate) use`/`pub(super) use`/属性，无 `fn`/`struct`/`impl`/`trait`/`enum`
    - csv/mod.rs 与 sqlite/mod.rs 顶部 `use super::*;` 路径无需修改即可解析
    - tests/integration.rs 中所有 export 相关测试通过
    - ExporterManager / ExporterKind / ExportStats / Exporter trait 在 crate::exporter 命名空间可达
  </acceptance_criteria>
  <done>
    mod.rs 简化到 ≤ 30 行（含注释），5 个新文件按职责承接全部实现，质量门禁全绿。
  </done>
</task>

</tasks>

<verification>
- cargo clippy --all-targets -- -D warnings 通过
- cargo test 通过
- src/exporter/mod.rs grep -E '\bfn |\bstruct |\bimpl |\btrait |\benum ' 无匹配
- src/exporter/csv/mod.rs 与 sqlite/mod.rs 文件内容不变（diff 应只显示 exporter/mod.rs 与新文件）
</verification>

<success_criteria>
- mod.rs 仅含 mod 声明与 pub use（≤ 30 行）
- api/kind/manager/stats/util 5 个文件按职责承接全部实现
- csv/ 与 sqlite/ 子模块代码与可见性完全不变
- 全套 cargo 质量门禁通过
</success_criteria>

<output>
Create `.planning/phases/71-mod-rs-mod-rs-pub-use/71-06-SUMMARY.md` when done
</output>
