---
phase: 44-hotpath
plan: 02
type: execute
wave: 2
depends_on:
  - 44-01
files_modified:
  - src/pipeline/normalizer.rs
  - src/exporter/csv/mod.rs
  - CLAUDE.md
autonomous: true
requirements:
  - PERF-01
  - PERF-02

must_haves:
  truths:
    - "ParamBuffer 类型别名定义为 HashMap<(String, String), Arc<Vec<ParamValue>>>"
    - "compute_normalized 中 buffer.insert 用 Arc::new(params) 包装"
    - "compute_normalized 中 buffer.get(&key)?.clone() 文字不变，但语义为 Arc 引用计数复制"
    - "apply_params_into 调用点签名未变（依赖 Arc<Vec<T>> Deref → &[T] 自动转换）"
    - "src/exporter/csv/mod.rs 的 BufWriter::with_capacity 从 2*1024*1024 提升到 16*1024*1024"
    - "CLAUDE.md 的 16MB BufWriter 描述与代码一致"
    - "compute_normalized 函数体行数未增加（保持 ≤ 64 行）"
    - "未引入新的 unsafe 代码"
    - "cargo test 全部通过，无功能回归"
    - "cargo clippy --all-targets -- -D warnings 通过"
    - "cargo fmt --check 通过"
  artifacts:
    - path: "src/pipeline/normalizer.rs"
      provides: "Arc-based ParamBuffer 消除 Vec clone 热点"
      contains: "Arc<Vec<ParamValue>>"
    - path: "src/exporter/csv/mod.rs"
      provides: "16MB BufWriter 减少 write syscall"
      contains: "16 * 1024 * 1024"
    - path: "CLAUDE.md"
      provides: "BufWriter 容量描述与代码一致"
      contains: "16MB"
  key_links:
    - from: "src/pipeline/normalizer.rs ParamBuffer"
      to: "std::sync::Arc"
      via: "use std::sync::Arc"
      pattern: "use std::sync::Arc"
    - from: "src/pipeline/normalizer.rs compute_normalized buffer.insert"
      to: "Arc::new"
      via: "Arc::new(params) 包装"
      pattern: "Arc::new\\(params\\)"
    - from: "src/exporter/csv/mod.rs"
      to: "BufWriter 容量常量"
      via: "with_capacity(16 * 1024 * 1024, file)"
      pattern: "16 \\* 1024 \\* 1024"
---

<objective>
实施 Phase 44 热点优化（D-05）：将 `ParamBuffer` 的 value 类型从 `Vec<ParamValue>` 改为 `Arc<Vec<ParamValue>>`，消除 H-3 中每条匹配 DML 记录的深拷贝开销（per RESEARCH.md 第 154-166 行）；同时将 CSV BufWriter 容量从 2MB 提升到 16MB（H-4，per RESEARCH.md 第 168-176 行）并使 CLAUDE.md 描述与代码一致。

Purpose: H-3 是 RESEARCH 识别的最高优先级热点（P1），Arc clone 仅是原子引用计数操作（O(1)），相比 Vec<ParamValue> 深拷贝（每个 String 都要堆分配）有数量级差距。H-1/H-2 的 key clone 受 HashMap borrow 限制（per Pitfall 3）无法消除——本 plan 接受现状不强求。
Output: 修改两个源文件 + 同步 CLAUDE.md 文档，全部质量门禁通过。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-hotpath/44-CONTEXT.md
@.planning/phases/44-hotpath/44-RESEARCH.md
@.planning/phases/44-hotpath/44-PATTERNS.md
@.planning/phases/44-hotpath/44-01-SUMMARY.md

@src/pipeline/normalizer.rs
@src/exporter/csv/mod.rs
@CLAUDE.md

<interfaces>
<!-- 关键现有类型与签名 — 修改这些时必须保持外部 API 不变 -->

src/pipeline/normalizer.rs 第 8 行（待修改）：
```rust
pub type ParamBuffer = HashMap<(String, String), Vec<ParamValue>>;
```

src/pipeline/normalizer.rs 第 11-19 行（保持不变，仅引用）：
```rust
#[derive(Debug, Clone)]
pub enum ParamValue {
    Quoted(String),
    Bare(String),
    Null,
}
```

src/pipeline/normalizer.rs 第 189 行（apply_params_into 签名 — 不修改）：
```rust
fn apply_params_into(sql: &str, params: &[ParamValue], colon_style: bool, out: &mut Vec<u8>) {
```

src/pipeline/normalizer.rs 第 340-345 行（compute_normalized 签名 — 不修改）：
```rust
record: &dm_database_parser_sqllog::Sqllog,
pm_sql: &str,
buffer: &mut ParamBuffer,
placeholder_override: Option<bool>,
scratch: &'a mut Vec<u8>,
```

src/cli/run/processor.rs 第 69-73 行（fast-path 保护 — 不可破坏）：
```rust
let ns = if do_normalize && (!params_buffer.is_empty() || record.tag.is_none()) {
    crate::pipeline::compute_normalized(...)
} else {
    None
};
```
HashMap::is_empty() 与 value 类型无关，Arc 改造后仍正确。

src/exporter/csv/mod.rs 第 124 行（待修改）：
```rust
let mut writer = BufWriter::with_capacity(2 * 1024 * 1024, file);
```

ParamBuffer 在项目内的所有引用（grep 已确认 — 仅 normalizer.rs 内部定义并被 processor.rs 通过 &mut 借用，未在公开 API 暴露 Vec 的具体类型，因此类型改造是源代码内部的实现细节）。
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: ParamBuffer 改为 Arc<Vec<ParamValue>> 消除 H-3 深拷贝</name>
  <read_first>
    - src/pipeline/normalizer.rs（整文件 — 重点第 1-20 行 type 定义、第 340-400 行 compute_normalized 函数）
    - src/pipeline/mod.rs（grep ParamBuffer 的导出与引用）
    - src/cli/run/processor.rs（grep params_buffer 的所有使用点 — 确认 fast-path is_empty 仍生效）
    - .planning/phases/44-hotpath/44-PATTERNS.md（第 28-99 行：normalizer.rs 修改的精确模式与 Arc Deref 说明）
    - .planning/phases/44-hotpath/44-RESEARCH.md（第 154-166 行：H-3 优化方案；第 316-325 行：Pitfall 4 Arc Deref）
  </read_first>
  <files>src/pipeline/normalizer.rs</files>
  <action>
  实施 H-3 优化：将 ParamBuffer 的 value 类型从 Vec<ParamValue> 改为 Arc<Vec<ParamValue>>。具体修改三处：

  1. 文件顶部 use 段（在 `use std::collections::HashMap;` 行之后）添加：

     use std::sync::Arc;

  2. 第 8 行类型别名修改（doc 注释保留不变）：

     原：
     pub type ParamBuffer = HashMap<(String, String), Vec<ParamValue>>;
     改为：
     pub type ParamBuffer = HashMap<(String, String), Arc<Vec<ParamValue>>>;

  3. 第 350 行 insert 调用，用 Arc::new 包装 params：

     原：
     buffer.insert((record.sess_id.clone(), record.statement.clone()), params);
     改为：
     buffer.insert((record.sess_id.clone(), record.statement.clone()), Arc::new(params));

  4. 第 367-369 行的 key clone + buffer.get(&key)?.clone() 文字保持不变：
     - key 仍由 (String, String) 组成（HashMap borrow 限制无法消除 — Pitfall 3）
     - buffer.get(&key)?.clone() 文字相同，但语义从"深拷贝 Vec"变为"复制 Arc 引用计数"（O(1) 原子操作）
     - 第 386 行 apply_params_into(pm_sql, &params, colon_style, scratch) 也保持不变（&Arc<Vec<T>> 自动 Deref 为 &[T]，per Pitfall 4）

  约束：
  - 不引入新 unsafe 代码（D-06）— Arc 是 safe Rust
  - 不修改 ParamValue 枚举本身（保持现有 Quoted/Bare/Null 三个变体）
  - 不修改 apply_params_into 签名（保持 &[ParamValue]）
  - 不修改 compute_normalized 签名（buffer: &mut ParamBuffer 仍生效，类型别名透明替换）
  - 不修改 parse_params 函数（仍返回 Option<Vec<ParamValue>>，由 Arc::new 在 insert 处包装）
  - compute_normalized 函数体不增加行数（仅第 350 行包了 Arc::new — 净增 0 行）
  - 不要尝试消除 H-1/H-2 的 key clone — 这是已识别但不在本 plan 范围的优化（受 Pitfall 3 限制）

  完成后必须验证：
  - cargo build 通过
  - cargo test 全部通过（D-07，包括 normalizer 模块的所有单测和 integration test）
  - cargo clippy --all-targets -- -D warnings 通过
  - grep -c '^[[:space:]]*unsafe' src/pipeline/normalizer.rs 返回 0
  </action>
  <verify>
    <automated>grep -F 'pub type ParamBuffer = HashMap<(String, String), Arc<Vec<ParamValue>>>' src/pipeline/normalizer.rs && grep -F 'use std::sync::Arc;' src/pipeline/normalizer.rs && grep -F 'Arc::new(params)' src/pipeline/normalizer.rs && cargo test --lib pipeline::normalizer 2>&1 | tail -5 && cargo test 2>&1 | tail -10 | grep -E 'test result: ok' && cargo clippy --all-targets -- -D warnings 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^use std::sync::Arc;$' src/pipeline/normalizer.rs` 返回 1
    - `grep -F 'pub type ParamBuffer = HashMap<(String, String), Arc<Vec<ParamValue>>>;' src/pipeline/normalizer.rs` 匹配 1 行
    - `grep -c 'Arc::new(params)' src/pipeline/normalizer.rs` 返回 ≥ 1（第 350 行 insert 处）
    - `grep -c 'buffer.get(&key)?.clone()' src/pipeline/normalizer.rs` 返回 1（第 369 行文字未变）
    - `grep -c 'apply_params_into(pm_sql, &params' src/pipeline/normalizer.rs` 返回 ≥ 1（第 386 行调用未变）
    - `grep -v '^#\|^//\|^[[:space:]]*//' src/pipeline/normalizer.rs | grep -c '\bunsafe\b'` 返回 0
    - compute_normalized 函数体行数 ≤ 64（与修改前相同，无膨胀）
    - `cargo build` 退出码 0
    - `cargo test` 全部通过（包括 normalizer 模块单测、integration test、bench 编译检查）
    - `cargo clippy --all-targets -- -D warnings` 退出码 0
    - `cargo fmt --check` 退出码 0
  </acceptance_criteria>
  <done>ParamBuffer 类型改造完成，buffer.insert 包 Arc，热路径调用点文字不变但语义为 Arc clone，全部测试通过且 clippy/fmt 无警告，未引入 unsafe。</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: 提升 CSV BufWriter 容量到 16MB 并同步 CLAUDE.md</name>
  <read_first>
    - src/exporter/csv/mod.rs（第 1-150 行 — 确认 BufWriter::with_capacity 唯一调用点在第 124 行，且无其他容量常量）
    - CLAUDE.md（搜索 "16MB BufWriter" 字符串确认文档描述）
    - .planning/phases/44-hotpath/44-PATTERNS.md（第 316-324 行：BufWriter 容量常量备查；第 325 行：CLAUDE.md 与代码不一致问题）
    - .planning/phases/44-hotpath/44-RESEARCH.md（第 168-176 行：H-4 BufWriter 容量分析；第 385-389 行：state of the art — CLAUDE.md 16MB 描述为历史残留）
  </read_first>
  <files>src/exporter/csv/mod.rs, CLAUDE.md</files>
  <action>
  实施 H-4 优化：将 BufWriter 容量从 2MB 提升到 16MB（per RESEARCH.md A5 假设——预期减少 write() 系统调用频率，对 1GB+ 文件处理有收益）。同时同步 CLAUDE.md 文档（消除当前 CLAUDE.md 16MB 与代码 2MB 的不一致）。

  修改 1: src/exporter/csv/mod.rs 第 124 行：

     原：
     let mut writer = BufWriter::with_capacity(2 * 1024 * 1024, file);
     改为：
     let mut writer = BufWriter::with_capacity(16 * 1024 * 1024, file);

  保留行尾注释（如有）；不修改函数签名；不修改其他行。

  修改 2: CLAUDE.md — 确认 "16MB `BufWriter`" 描述与新代码一致。读取 CLAUDE.md，找到含 "BufWriter" 的描述行：

     - 若行内容已是 "16MB `BufWriter`" — 无需修改（代码本来就该是 16MB，本 task 修复代码侧）
     - 若有其他不一致措辞（如 "2MB"），更新为 "16MB `BufWriter`"

  注意约束：
  - 不引入新的 unsafe 代码（D-06）
  - 不破坏 CSV 输出正确性 — BufWriter 容量变化对 flush 行为透明
  - 不修改 BufWriter::flush 调用模式（如有）
  - 不修改 file_exists / append_mode 等周边逻辑
  - 16MB 是绝对值常量（16 * 1024 * 1024 = 16,777,216 字节），不要换算成其他表达式（如 1 << 24）以保持 grep 可读性

  完成后验证：
  - cargo build 通过
  - cargo test 全部通过（CSV 导出相关 integration test 必须仍正确，因为 16MB 远大于任何单条记录大小，不影响 flush 时机）
  - 通过简短 sanity check 运行（cargo run -- run -c examples/some-config 或 cargo test 中现有的 csv 单测）
  - cargo clippy 与 fmt 通过

  影响范围说明（写入 SUMMARY）：
  - 16MB 是 BufWriter 容量上限，仅当导出数据累计达到 16MB 才触发 flush 写 syscall
  - 对 1GB 文件处理：减少 syscall 次数约 8 倍（1024MB/16MB vs 1024MB/2MB）— 量化收益由 Wave 3 criterion 测量
  - 进程峰值内存：BufWriter 单实例（单输出文件）峰值占用 16MB；流式单文件模式无放大
  </action>
  <verify>
    <automated>grep -F 'BufWriter::with_capacity(16 * 1024 * 1024, file)' src/exporter/csv/mod.rs && ! grep -F 'BufWriter::with_capacity(2 * 1024 * 1024' src/exporter/csv/mod.rs && grep -F '16MB' CLAUDE.md && cargo test 2>&1 | tail -10 | grep -E 'test result: ok' && cargo clippy --all-targets -- -D warnings && cargo fmt --check</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'BufWriter::with_capacity(16 \* 1024 \* 1024, file)' src/exporter/csv/mod.rs` 返回 1
    - `grep -c 'BufWriter::with_capacity(2 \* 1024 \* 1024' src/exporter/csv/mod.rs` 返回 0（旧 2MB 完全替换）
    - CLAUDE.md 包含 "16MB" 字面字符串（描述 BufWriter 容量）
    - CLAUDE.md 不再包含 "2MB BufWriter" 或类似不一致描述（grep 验证）
    - `cargo build` 退出码 0
    - `cargo test` 全部通过（特别是 csv 模块相关测试）
    - `cargo clippy --all-targets -- -D warnings` 退出码 0
    - `cargo fmt --check` 退出码 0
    - 函数行数未增加（仅常量替换，行数应严格不变）
  </acceptance_criteria>
  <done>BufWriter 容量提升到 16MB，CLAUDE.md 描述与代码一致，所有测试通过，质量门禁绿灯。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 内部 API | ParamBuffer 是 pub type，但仅在 crate 内部 normalizer.rs 与 processor.rs 之间流转，无外部消费者 |
| 文件 I/O | BufWriter 写 CSV 文件，容量变化仅影响 syscall 频率，输出字节流不变 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-04 | Tampering | ParamBuffer Arc 改造破坏并发安全 | accept | 当前架构为单线程（CLAUDE.md 第 38 行 "Single-threaded streaming"），Arc 在单线程下与 Rc 等价，无数据竞争风险；Phase 45 引入并行时再评估 |
| T-44-05 | Information Disclosure | BufWriter 16MB 缓冲增大进程内存占用 | accept | 单实例 +14MB 占用对 1GB+ 文件场景可忽略；不引入 swap 压力或 OOM 风险 |
| T-44-06 | Denial of Service | apply_params_into 与 Arc Deref 误用导致 panic | mitigate | acceptance criteria 强制全套 cargo test 通过；现有 integration test 覆盖 PARAMS+DML 配对场景 |
| T-44-07 | Tampering | unsafe 代码意外引入 | mitigate | acceptance criteria 显式 grep 验证 unsafe 计数为 0；D-06 锁定 |

注：纯应用层性能优化，无新 I/O 边界、无新外部输入、无用户 API 变更。
</threat_model>

<verification>
- `cargo build` 通过
- `cargo build --release` 通过
- `cargo test` 全部通过（涵盖 normalizer 单测、integration test、csv 模块测试）
- `cargo clippy --all-targets -- -D warnings` 通过
- `cargo fmt --check` 通过
- `grep -v '^#\|//' src/pipeline/normalizer.rs | grep -c '\bunsafe\b'` 返回 0
- ParamBuffer 类型改造未破坏 processor.rs 的 fast-path 检查（params_buffer.is_empty() 仍正确）
- BufWriter 容量从 2MB 提升到 16MB，CLAUDE.md 描述与代码一致
</verification>

<success_criteria>
1. ParamBuffer 类型别名为 HashMap<(String, String), Arc<Vec<ParamValue>>>
2. compute_normalized 第 350 行 insert 用 Arc::new 包装 params
3. compute_normalized 第 369、386 行调用点文字未变（依赖 Arc Deref 透明转换）
4. compute_normalized 函数体行数未增加（保持 ≤ 64 行）
5. src/exporter/csv/mod.rs 第 124 行 BufWriter::with_capacity 使用 16MB
6. CLAUDE.md 描述与代码一致
7. 未引入新 unsafe 代码（D-06）
8. cargo test 全部通过（D-07，无功能回归）
9. 全套质量门禁通过（build/test/clippy/fmt）
</success_criteria>

<output>
Create `.planning/phases/44-hotpath/44-02-SUMMARY.md` when done, recording:
- normalizer.rs 修改的精确 diff（3 处 change：use 语句、type 别名、Arc::new 包装）
- csv/mod.rs BufWriter 容量变更行号与新常量
- CLAUDE.md 描述更新（如有）
- cargo test 通过的测试数（test result: ok. N passed）
- compute_normalized 函数体最终行数确认（≤ 64）
</output>
