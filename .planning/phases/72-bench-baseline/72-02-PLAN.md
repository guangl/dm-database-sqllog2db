---
phase: 72-bench-baseline
plan: 02
type: execute
wave: 2
depends_on:
  - 72-01
files_modified:
  - benches/BENCHMARKS.md
  - benches/baselines/csv_export
  - benches/baselines/csv_format_only
  - benches/baselines/filters
  - benches/baselines/parser_throughput
  - benches/baselines/sqlite_export
  - benches/baselines/sqlite_single_row
autonomous: true
requirements:
  - BENCH-02
must_haves:
  truths:
    - "开发者运行 `CRITERION_HOME=benches/baselines cargo bench -- --save-baseline v1.20` 后 4 个 bench 文件全部成功存档"
    - "benches/baselines/ 下的合成 benchmark group 均出现 v1.20 子目录与 benchmark.json"
    - "开发者运行 `CRITERION_HOME=benches/baselines cargo bench -- --baseline v1.20` 可加载 v1.20 baseline 并产出对比结论"
    - "BENCHMARKS.md 的 \"How to compare against this baseline\" 段落出现 v1.20 命令示例（per D-06）"
    - "Phase 72 段落 Criterion 半部分补全：存档命令 + 对比命令 + 已知 skip 说明 + 结论 checklist 翻转为已完成"
  artifacts:
    - path: "benches/baselines/csv_export/1000/v1.20/benchmark.json"
      provides: "criterion v1.20 baseline 数据（csv_export 1000 size）"
      contains: "v1.20"
    - path: "benches/baselines/parser_throughput/1000/v1.20/benchmark.json"
      provides: "criterion v1.20 baseline 数据（parser_throughput 1000 size）"
    - path: "benches/baselines/sqlite_export/1000/v1.20/benchmark.json"
      provides: "criterion v1.20 baseline 数据（sqlite_export 1000 size）"
    - path: "benches/baselines/filters"
      provides: "v1.20 子目录覆盖所有 filter 场景"
    - path: "benches/BENCHMARKS.md"
      provides: "How to compare 段落新增 v1.20 命令 + Phase 72 段落 Criterion 半部分"
      contains: "--save-baseline v1.20"
  key_links:
    - from: "How to compare against this baseline 段落"
      to: "v1.20 对比命令"
      via: "bash 代码块"
      pattern: "--baseline v1.20"
    - from: "Phase 72 段落 ### Criterion v1.20 Baseline 存档"
      to: "benches/baselines/ 下的 v1.20 目录"
      via: "存档命令引用"
      pattern: "--save-baseline v1.20"
---

<objective>
完成 v1.20 里程碑 Criterion benchmark baseline 存档（BENCH-02）：用 `CRITERION_HOME=benches/baselines cargo bench -- --save-baseline v1.20` 把全部 4 个 bench 文件的结果写入 `benches/baselines/<group>/<id>/v1.20/`，更新 BENCHMARKS.md 的"How to compare against this baseline"段落补充 v1.20 对比命令（per D-06），并把 72-01 留下的 Phase 72 段落 Criterion 半部分补全。

Purpose: 让 Phase 73–76 的性能优化可以通过 `--baseline v1.20` 自动获得 improved/regressed 判定。
Output: 新增 `benches/baselines/<group>/<id>/v1.20/` JSON 数据 + BENCHMARKS.md 两处定向编辑。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/72-bench-baseline/72-CONTEXT.md
@.planning/phases/72-bench-baseline/72-RESEARCH.md
@.planning/phases/72-bench-baseline/72-PATTERNS.md
@.planning/phases/72-bench-baseline/72-01-PLAN.md
@benches/BENCHMARKS.md
@benches/baselines/.gitignore
@Cargo.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: 运行 criterion --save-baseline v1.20 并提交 baseline 文件</name>
  <files>
    benches/baselines/csv_export/**/v1.20/*
    benches/baselines/csv_format_only/**/v1.20/*
    benches/baselines/filters/**/v1.20/*
    benches/baselines/parser_throughput/**/v1.20/*
    benches/baselines/sqlite_export/**/v1.20/*
    benches/baselines/sqlite_single_row/**/v1.20/*
  </files>
  <read_first>
    - .planning/phases/72-bench-baseline/72-CONTEXT.md (D-04/D-05/D-07 锁定环境变量、命令、提交范围)
    - .planning/phases/72-bench-baseline/72-RESEARCH.md §Common Pitfalls Pitfall 2/3/4 (CRITERION_HOME 必填 / real-file 自动 skip 是正常 / git add 必须显式覆盖 baselines/)
    - .planning/phases/72-bench-baseline/72-PATTERNS.md §"benches/baselines/*/v1.20/ (data archive)" 目录树
    - benches/baselines/.gitignore (确认 v1.20 不在排除范围)
    - Cargo.toml `[[bench]]` 段（确认 bench 注册项为 bench_csv / bench_sqlite / bench_filters / bench_parser）
  </read_first>
  <action>
    Step A — 启动 baseline 存档命令（per D-04/D-05 锁定形式）：
      `CRITERION_HOME=benches/baselines cargo bench -- --save-baseline v1.20`
    必须把环境变量直接前置于命令（不要 `export` 全局），与历史 BENCHMARKS.md "How to compare" 段落写法一致。命令会顺序运行 4 个 bench 文件（bench_csv、bench_sqlite、bench_filters、bench_parser）；criterion 会在 `benches/baselines/<group>/<id>/v1.20/` 下生成 `benchmark.json`、`estimates.json`、`sample.json`、`tukey.json` 四个文件。

    Step B — 处理已知 skip 信息（per RESEARCH Pitfall 3）：
    `csv_export_real` 与 `sqlite_export_real` 依赖 `sqllogs/` 目录（538MB×2，不在 repo 中），bench 代码内部会自动 skip 并打印类似 "sqllogs/ not found, skipping ..." 的提示。这是正常信息，不计为失败，不需要构造 sqllogs/。把这两个 group 缺失 v1.20 子目录的事实记入 Task 2 待写文本。

    Step C — 验证 baseline 已写入（必须全部为真才能进入 Step D）：
      - `ls benches/baselines/csv_export/1000/v1.20/benchmark.json` 存在
      - `ls benches/baselines/csv_export/10000/v1.20/benchmark.json` 存在
      - `ls benches/baselines/csv_export/50000/v1.20/benchmark.json` 存在
      - `ls benches/baselines/sqlite_export/1000/v1.20/benchmark.json` 存在
      - `ls benches/baselines/filters/no_pipeline/v1.20/benchmark.json` 存在
      - `ls benches/baselines/parser_throughput/1000/v1.20/benchmark.json` 存在
      - `ls benches/baselines/sqlite_single_row/1000/v1.20/benchmark.json` 存在（若该 group 在 bench_sqlite 中注册）
    若上述任一不存在但相应 group 未被 skip，必须重跑 Step A 排查 CRITERION_HOME 是否生效（Pitfall 2）。

    Step D — 验证 `--baseline v1.20` 可加载（per D-06 示例命令冒烟）：
      `CRITERION_HOME=benches/baselines cargo bench --bench bench_filters -- --baseline v1.20 --warm-up-time 1 --measurement-time 1 -- no_pipeline`
      仅作冒烟，不需要采集结果；criterion 不抛出 "no such baseline" 错误即视为通过。

    Step E — 显式 `git add benches/baselines/`（per Pitfall 4），确认 `git status --short benches/baselines/` 只列出新增 v1.20 路径下的 JSON（不带 `**/new/` 或 `**/report/`），无遗漏也无误入排除目录。

    本 Task 不修改 BENCHMARKS.md，文档更新由 Task 2 完成。
  </action>
  <verify>
    <automated>test -f benches/baselines/csv_export/1000/v1.20/benchmark.json && test -f benches/baselines/sqlite_export/1000/v1.20/benchmark.json && test -f benches/baselines/filters/no_pipeline/v1.20/benchmark.json && test -f benches/baselines/parser_throughput/1000/v1.20/benchmark.json && git status --short benches/baselines/ | grep -v "/new/\|/report/" | grep -c "v1.20" | awk '$1>0{exit 0} {exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - `CRITERION_HOME=benches/baselines cargo bench -- --save-baseline v1.20` 退出码 0。
    - `find benches/baselines -type d -name v1.20 | wc -l` ≥ 7（覆盖 csv_export 三个 size + sqlite_export 至少 1 size + filters 主要场景 + parser_throughput 三个 size + sqlite_single_row）。
    - 上述 v1.20 目录中每个均含 `benchmark.json` 与 `estimates.json` 两个文件（criterion 标准产物）。
    - `csv_export_real` 与 `sqlite_export_real` 没有 v1.20 子目录是允许的（自动 skip，per Pitfall 3）。
    - `git status --short benches/baselines/` 中所有新增条目路径含 `/v1.20/`，无 `/new/` 或 `/report/` 路径误入 staging（per .gitignore 期望）。
    - `CRITERION_HOME=benches/baselines cargo bench --bench bench_filters -- --baseline v1.20 --warm-up-time 1 --measurement-time 1 -- no_pipeline` 退出码 0，stdout 中不含 "no baseline" 或 "baseline does not exist" 字样。
  </acceptance_criteria>
  <done>
    全部 4 个 bench 文件的 v1.20 baseline JSON 已生成并被 git 追踪；`--baseline v1.20` 冒烟通过；real-file group 自动 skip 已确认且不影响验收。
  </done>
</task>

<task type="auto">
  <name>Task 2: 更新 BENCHMARKS.md 的 How-to-compare 段落 + 补全 Phase 72 Criterion 半部分</name>
  <files>
    benches/BENCHMARKS.md
  </files>
  <read_first>
    - benches/BENCHMARKS.md lines 25–40 (How to compare against this baseline 段落，需在 line 36 的 v1.0 save-baseline 示例下方追加 v1.20 命令)
    - benches/BENCHMARKS.md (查找 `## Phase 72 — 基准体系完善（v1.20）` 段落位置，由 Plan 72-01 创建，定位 `### Criterion v1.20 Baseline 存档（BENCH-02）` 小节，将该小节占位文本替换为正式内容；同时翻转 `### 结论` 中 BENCH-02 checklist)
    - .planning/phases/72-bench-baseline/72-CONTEXT.md (D-02/D-06/D-08/D-09)
    - .planning/phases/72-bench-baseline/72-PATTERNS.md §"How to compare against this baseline 段落"
  </read_first>
  <action>
    本 Task 对 `benches/BENCHMARKS.md` 进行两处定向编辑，禁止改动其他行。

    编辑 A — "How to compare against this baseline" 段落追加 v1.20 命令（per D-06）：
    在 line 36 当前 `CRITERION_HOME=benches/baselines cargo bench --bench bench_csv -- --save-baseline phase4` 之后、line 36 所在 bash 代码块的关闭 ``` 之前，追加两行：
    ```
    # v1.20 baseline（已由 Phase 72 写入）
    CRITERION_HOME=benches/baselines cargo bench -- --baseline v1.20
    ```
    上面是要追加的内容（含中文注释行 + 命令行两行，处于既有 bash 代码块内部）。除此两行外，line 25–38 的所有其他文本保持完全不变。

    编辑 B — Phase 72 段落 Criterion 半部分扩写：
    定位 Plan 72-01 创建的 `### Criterion v1.20 Baseline 存档（BENCH-02）` 小节，其当前占位为单行"详见 Plan 72-02 ..."。把该占位整段替换为：

    1. 一段引言句子，说明 baseline 已存档至 `benches/baselines/`，覆盖范围为 4 个 bench 文件（bench_csv、bench_sqlite、bench_filters、bench_parser）的全部合成场景；`csv_export_real` 与 `sqlite_export_real` 因 `sqllogs/` 不在 repo 内自动 skip（与 Phase 4 处理方式一致，per Pitfall 3）。
    2. 一个 bash 代码块，标注"# 存档命令（已执行）"，原文：`CRITERION_HOME=benches/baselines cargo bench -- --save-baseline v1.20`
    3. 紧跟一个 bash 代码块，标注"# 后续版本对比命令"，原文：`CRITERION_HOME=benches/baselines cargo bench -- --baseline v1.20`
    4. 一段结论句子：'criterion 输出将标注 "Performance has improved" / "Performance has regressed" / "No change in performance detected"。'

    然后定位同一段落的 `### 结论` checklist：将 `- [ ] BENCH-02 criterion v1.20 baseline 存档（由 Plan 72-02 完成）` 改为 `- [x] BENCH-02 criterion v1.20 baseline 存档至 \`benches/baselines/\`，覆盖 4 个 bench 文件全部合成场景`。

    禁止：
    - 修改 line 1–24 与 line 39–722 中除"How to compare"段落 v1.20 追加之外的任何字符
    - 修改历史段落（Phase 4/5/6/9/10/42/44/56），per D-08
    - 修改 Plan 72-01 创建的 `### CLI 冷启动基线（hyperfine, BENCH-01）` 小节内容
    - 使用 emoji
  </action>
  <verify>
    <automated>grep -c "CRITERION_HOME=benches/baselines cargo bench -- --baseline v1.20" benches/BENCHMARKS.md | awk '$1>=2{exit 0} {exit 1}' && grep -q "CRITERION_HOME=benches/baselines cargo bench -- --save-baseline v1.20" benches/BENCHMARKS.md && grep -q "^- \[x\] BENCH-02 criterion v1.20 baseline 存档" benches/BENCHMARKS.md && grep -q "^- \[x\] BENCH-01 hyperfine 冷启动数值已记录" benches/BENCHMARKS.md && cargo clippy --all-targets -- -D warnings && cargo test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "CRITERION_HOME=benches/baselines cargo bench -- --baseline v1.20" benches/BENCHMARKS.md` ≥ 2（一次在 How-to-compare 段落、一次在 Phase 72 段落 Criterion 小节）。
    - `grep -c "CRITERION_HOME=benches/baselines cargo bench -- --save-baseline v1.20" benches/BENCHMARKS.md` ≥ 1（在 Phase 72 段落 Criterion 小节）。
    - `grep -q "^- \[x\] BENCH-02 criterion v1.20 baseline 存档"` 成功（结论 checklist 已翻转为已完成）。
    - `grep -q "^- \[x\] BENCH-01 hyperfine 冷启动数值已记录"` 成功（72-01 写入的 BENCH-01 checklist 未被破坏）。
    - `grep -q "^## Phase 9 — CLI 冷启动基线（PERF-11）" benches/BENCHMARKS.md` 成功且 `git diff benches/BENCHMARKS.md` 显示 Phase 9 段落零修改（per D-08）。
    - `git diff benches/BENCHMARKS.md` 的改动行数集中在两个区域：line 25–38 范围内追加 ≤ 5 行；Phase 72 段落 Criterion 小节扩写 + 结论 checklist 翻转。
    - `cargo clippy --all-targets -- -D warnings` 退出码 0、无 warning。
    - `cargo test` 全部通过。
    - `cargo fmt --check` 退出码 0（无源码改动，作为兜底）。
  </acceptance_criteria>
  <done>
    How-to-compare 段落含 v1.20 对比命令；Phase 72 段落 Criterion 小节齐备（存档命令 + 对比命令 + skip 说明 + 结论翻转）；clippy / test / fmt 三道质量门禁全部通过；历史段落字节级保持不变。
  </done>
</task>

</tasks>

<verification>
- benches/baselines/ 下出现 v1.20 子目录覆盖 4 个 bench 文件的全部合成场景
- BENCHMARKS.md "How to compare" 段落含 v1.20 命令示例
- BENCHMARKS.md Phase 72 段落 Criterion 小节补全 + 结论 checklist 翻转为完成
- `CRITERION_HOME=benches/baselines cargo bench -- --baseline v1.20` 可加载 baseline
- cargo clippy/test/fmt 全绿
</verification>

<success_criteria>
- BENCH-02 验收：criterion v1.20 baseline JSON 已纳入 repo，后续 phase 可通过 `--baseline v1.20` 自动对比
- D-06 兑现：How-to-compare 段落出现 v1.20 命令
- D-08 守住：line 25–38 之外、Phase 72 段落之外的内容字节级不变
- Phase 72 整体 GOAL（建立 v1.20 基准体系）达成，BENCH-01/BENCH-02 同时收尾
</success_criteria>

<output>
Create `.planning/phases/72-bench-baseline/72-02-SUMMARY.md` when done
</output>
