---
phase: 34-audit-gap-closure
plan: 02
type: execute
wave: 2
depends_on: [34-01]
files_modified:
  - .planning/phases/30-remove-template-analysis/30-VERIFICATION.md
autonomous: true
requirements: [RM-05, RM-08]
user_setup: []
must_haves:
  truths:
    - "Phase 30 有 VERIFICATION.md 文件，验证模板分析及关联功能已完全移除"
    - "审计缺口 INT-01（normalize_template 死代码）、INT-02（[template] 静默接受）、INT-03（FileError::ReadFailed TODO）均已关闭"
    - "RM-05（移除模板分析）和 RM-08（项目结构清理）确认满足"
  artifacts:
    - path: ".planning/phases/30-remove-template-analysis/30-VERIFICATION.md"
      provides: "Phase 30 正式验证报告"
      min_lines: 40
    - path: "src/pipeline/normalizer.rs"
      provides: "确认 normalize_template 已移除"
      not_contains: "normalize_template"
  key_links:
    - from: "30-VERIFICATION.md"
      to: "30-01/02/03-SUMMARY.md"
      via: "基于 SUMMARY 的 must_haves 与审计缺口对照"
    - from: "34-02-PLAN.md"
      to: "34-01-PLAN.md"
      via: "depends_on — 代码修改完成后才能验证"
---

<objective>
创建 Phase 30 VERIFICATION.md 并确认所有审计缺口已关闭

Purpose: 关闭审计缺口 INT-01、INT-02、INT-03，补全 Phase 30 缺失的 VERIFICATION.md，确认 RM-05 和 RM-08 完全满足。
Output:
- Phase 30 的 VERIFICATION.md（基于 30-01/02/03-SUMMARY.md）
- 审计缺口全部关闭的确认报告
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/guang/Projects/sqllog2db/.planning/ROADMAP.md
@/Users/guang/Projects/sqllog2db/.planning/STATE.md
@/Users/guang/Projects/sqllog2db/.planning/phases/34-audit-gap-closure/34-CONTEXT.md
@/Users/guang/Projects/sqllog2db/.planning/v1.7-MILESTONE-AUDIT.md

<interfaces>
Phase 30 已完成工作的总结文件（VERIFICATION.md 的内容来源）:
- /Users/guang/Projects/sqllog2db/.planning/phases/30-remove-template-analysis/30-01-SUMMARY.md (配置层清理)
- /Users/guang/Projects/sqllog2db/.planning/phases/30-remove-template-analysis/30-02-SUMMARY.md (运行时代码清理)  
- /Users/guang/Projects/sqllog2db/.planning/phases/30-remove-template-analysis/30-03-SUMMARY.md (测试清理+编译验证)
- /Users/guang/Projects/sqllog2db/.planning/phases/30-remove-template-analysis/30-VALIDATION.md (Nyquist 验证策略)

参考 Phase 32 VERIFICATION.md 的格式和结构:
- /Users/guang/Projects/sqllog2db/.planning/phases/32-cleanup-project-structure/32-VERIFICATION.md

审计缺口描述（需要确认关闭的项目）:
  INT-01: normalize_template 死代码 (normalizer.rs:462)
  INT-02: [template] 配置段被静默接受
  INT-03: FileError::ReadFailed TODO 注释 (error.rs:59)

INT-02 的修复由 Plan 34-01 完成，本 plan 应确认：
- normalize_template 是否已从 normalizer.rs 移除：使用 grep 验证
- FileError::ReadFailed 是否已从 error.rs 移除：使用 grep 验证
- [template] 是否被拒绝：使用 toml::from_str + validate 测试验证
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 创建 Phase 30 VERIFICATION.md</name>
  <read_first>
    - /Users/guang/Projects/sqllog2db/.planning/phases/30-remove-template-analysis/30-01-SUMMARY.md
    - /Users/guang/Projects/sqllog2db/.planning/phases/30-remove-template-analysis/30-02-SUMMARY.md
    - /Users/guang/Projects/sqllog2db/.planning/phases/30-remove-template-analysis/30-03-SUMMARY.md
    - /Users/guang/Projects/sqllog2db/.planning/phases/32-cleanup-project-structure/32-VERIFICATION.md (参考格式)
    - /Users/guang/Projects/sqllog2db/.planning/ROADMAP.md (Phase 30 的 must_haves 定义)
  </read_first>
  <files>
    - .planning/phases/30-remove-template-analysis/30-VERIFICATION.md
  </files>
  <action>
    创建 Phase 30 的 VERIFICATION.md。格式和结构参照 32-VERIFICATION.md。
    VERIFICATION.md 必须包含:
    1. frontmatter: phase编号(30), 验证时间, status(passed如果全部满足否则partial), 得分
    2. Goal Achievement 章节，列出可观测 truth 并逐一验证:
       Truth 1: aggregator.rs, template_reporter.rs, companion.rs 已删除
       Truth 2: hdrhistogram 依赖从 Cargo.toml 中删除
       Truth 3: [template] 和 [template.report] 配置段从 Config 移出
       Truth 4: 运行 sqllog2db run 时不再生成模板报告文件
       Truth 5: 核心 CSV/SQLite 导出热循环不受影响，pipeline.is_empty() 快路径零开销
       Truth 6: normalizer.rs 中 normalize_template 死代码已移除（利用当前代码库的实际状态验证）
    3. Required Artifacts 表格，列出所有关键文件及其验证状态
    4. Key Link Verification 表格
    5. 审计缺口对照表: 列出 INT-01, INT-02, INT-03 的当前状态（全部标记已关闭/已修复）
    6. Requirements Coverage: RM-05 标记 SATISFIED，引用所有 truth 作为证据

    注意:
    - INT-02（[template] 拒绝）由 Plan 34-01 修复。在本 VERIFICATION.md 中标注为"参见 Phase 34-01 修复"
    - Truth 6（normalize_template 已移除）由 WR-05 修复，但 VERIFICATION.md 应验证当前代码库确实不包含该函数
    - 所有证据必须基于当前代码库的实际 grep/检查结果，不能仅引用 SUMMARY.md
  </action>
  <acceptance_criteria>
    - 文件 `.planning/phases/30-remove-template-analysis/30-VERIFICATION.md` 已创建
    - 包含 frontmatter（phase, status, score 等字段）
    - 包含 Goal Achievement 表格，每条 truth 有状态和证据
    - 包含 Requirements Coverage 表格，RM-05 标记 SATISFIED
    - 包含审计缺口（INT-01/02/03）对照表
    - 内容基于 SUMMARY.md + 当前代码库实际验证结果
  </acceptance_criteria>
  <verify>
    <automated>grep -c "RM-05" .planning/phases/30-remove-template-analysis/30-VERIFICATION.md && grep -c "SATISFIED" .planning/phases/30-remove-template-analysis/30-VERIFICATION.md && grep -c "INT-01\|INT-02\|INT-03" .planning/phases/30-remove-template-analysis/30-VERIFICATION.md</automated>
  </verify>
  <done>Phase 30 VERIFICATION.md 创建完成，包含缺口对照表</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: 验证所有审计缺口已关闭</name>
  <read_first>
    - /Users/guang/Projects/sqllog2db/src/pipeline/normalizer.rs (grep 确认无 normalize_template)
    - /Users/guang/Projects/sqllog2db/src/error.rs (grep 确认无 ReadFailed)
    - /Users/guang/Projects/sqllog2db/src/config/mod.rs (grep 确认有 template_deprecated 字段, 需 Plan 34-01 已执行)
  </read_first>
  <files>
    - (无需修改文件)
  </files>
  <action>
    逐一验证三个审计缺口当前状态。使用 grep + 代码检查确认:
    
    1. **INT-01 — normalize_template 死代码:**
       - `grep -rn 'normalize_template' src/` 应返回空
       - 检查 src/pipeline/normalizer.rs 无该函数定义
    
    2. **INT-02 — [template] 被静默接受:**
       - `grep -rn 'template_deprecated' src/config/mod.rs` 应返回非空（Plan 34-01 已添加该字段）
       - 创建一个简短的临时测试验证: `[template]` 配置反序列化后 validate() 返回包含 "\"已废弃\"" 的错误
       - 使用 cargo test 中已有的 test_validate_rejects_template_section 验证（该测试由 Plan 34-01 添加）
    
    3. **INT-03 — FileError::ReadFailed TODO:**
       - `grep -rn 'ReadFailed' src/error.rs` 应返回空
       - 确认 src/error.rs 中 FileError 枚举只有 AlreadyExists, WriteFailed, CreateDirectoryFailed 三个变体
    
    记录每个缺口的验证结果到 SUMMARY.md 中。
  </action>
  <acceptance_criteria>
    - INT-01: `grep -rn 'normalize_template' src/` 返回空
    - INT-02: `cargo test -- config::validate::tests::test_validate_rejects_template_section` 通过（返回 OK），证明 [template] 被拒绝
    - INT-03: `grep 'ReadFailed' src/error.rs` 返回空
  </acceptance_criteria>
  <verify>
    <automated>grep -rn 'normalize_template' src/ 2>/dev/null; test $? -eq 1 && echo "INT-01: PASS"</automated>
    <automated>grep 'ReadFailed' src/error.rs 2>/dev/null; test $? -eq 1 && echo "INT-03: PASS"</automated>
    <automated>cargo test -- config::validate::tests::test_validate_rejects_template_section 2>&1 | tail -5</automated>
  </verify>
  <done>三个审计缺口全部确认关闭，验证结果记录在 SUMMARY.md</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: 全链路验证 — build + test + clippy + fmt + RM-05/RM-08 确认</name>
  <read_first>
    - (无需读取额外文件)
  </read_first>
  <files>
    - (无需修改文件)
  </files>
  <action>
    运行完整的全链路验证:
    1. `cargo build --release` — 确认 release 编译成功
    2. `cargo test` — 确认所有测试通过
    3. `cargo clippy --all-targets -- -D warnings` — 确认零警告
    4. `cargo fmt --check` — 确认格式合规
    
    然后创建确认报告输出到 SUMMARY.md，说明:
    - RM-05 已满足: 基于 Phase 30 VERIFICATION.md + INT-01/INT-02 已关闭
    - RM-08 已满足: 基于 Phase 32 VERIFICATION.md + INT-02/INT-03 已关闭
    - 所有审计缺口（INT-01/02/03）已关闭
  </action>
  <acceptance_criteria>
    - cargo build --release: 通过
    - cargo test: 全部通过
    - cargo clippy --all-targets -- -D warnings: 无警告
    - cargo fmt --check: 通过
    - RM-05 和 RM-08 在 SUMMARY.md 中标记为 SATISFIED
  </acceptance_criteria>
  <verify>
    <automated>cargo build --release && cargo test && cargo clippy --all-targets -- -D warnings && cargo fmt --check</automated>
  </verify>
  <done>全链路验证通过，RM-05/RM-08 确认满足，所有审计缺口关闭</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 创建的文件 | 仅 .planning 下文档文件，不影响运行时代码 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-02 | Tampering | 文档文件 | accept | VERIFICATION.md 仅记录验证结果，不影响代码行为 |
| T-34-SC | Tampering | npm/pip/cargo installs | accept | 本次 plan 不修改 Cargo.toml，无新依赖引入 |
</threat_model>

<verification>
执行顺序: Task 1 → Task 2 → Task 3（串行，Task 2 需要 Task 1 完成 VERIFICATION.md 作为缺口对照基础）

证明 RM-05 和 RM-08 满足的关键检查:
- RM-05: Phase 30 VERIFICATION.md + 代码中无 normalize_template + [template] 被拒绝
- RM-08: Phase 32 VERIFICATION.md (9/9 已验证) + [template] 拒绝已修复 + FileError ReadFailed 已清理
</verification>

<success_criteria>
- Phase 30 VERIFICATION.md 创建完成（D-09, D-10）
- 三个审计缺口全部确认关闭（INT-01/02/03）
- `cargo build --release` + `cargo test` + `cargo clippy --all-targets -- -D warnings` + `cargo fmt --check` 全部通过（D-13）
- RM-05 和 RM-08 确认满足（D-12）
</success_criteria>

<output>
创建 `.planning/phases/34-audit-gap-closure/34-02-SUMMARY.md`
</output>
