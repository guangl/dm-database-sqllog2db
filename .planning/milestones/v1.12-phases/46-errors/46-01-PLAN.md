---
phase: 46-errors
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main.rs
  - src/error.rs
autonomous: true
requirements:
  - ERROR-01
  - ERROR-02
must_haves:
  truths:
    - "所有非中断致命错误打印到 stderr 时第二行以 'hint:' 前缀展示修复建议（替代旧的 'Suggestion:' 前缀）(D-01)"
    - "Error::Io 变体的 suggestion() 返回非空通用提示，使其满足 ERROR-02 hint 覆盖 (D-03)"
    - "TOML 解析错误（ConfigError::ParseFailed）依赖 toml::from_str 自带字段/行号信息，本 phase 不实现 serde path 提取 (D-02)"
    - "现有错误变体的字段名/原因（thiserror Display 文本）保持不变 — ERROR-01 由原有 Display 文本继续承担（依据 D-04 决策）"
    - "现有 13 项单元测试（exit code / fatal / severity / suggestion / verbosity / load_config）仍全部通过"
    - "cargo clippy --all-targets -- -D warnings 与 cargo test 全部绿灯"
  artifacts:
    - path: src/main.rs
      provides: "错误打印路径输出 'hint:' 前缀，并新增 hint 前缀回归测试"
      contains: "eprintln!(\"  hint: {suggestion}\")"
    - path: src/error.rs
      provides: "Error::Io 变体的 suggestion() match arm 维持非空通用提示"
      contains: "Error::Io(_) => \"Check filesystem permissions and disk space.\""
  key_links:
    - from: src/main.rs
      to: src/error.rs::Error::suggestion
      via: "main() 错误分支调用 e.suggestion() 并以 'hint:' 前缀打印"
      pattern: "hint: \\{suggestion\\}"
    - from: src/error.rs
      to: stderr
      via: "main.rs 致命错误分支调用 severity + Display + suggestion 输出 [SEVERITY] / hint 两行"
      pattern: "Error::Io\\(_\\) => \"Check"
---

<objective>
统一所有错误展示前缀：将 `src/main.rs` 致命错误分支的第二行从 `"  Suggestion: {suggestion}"` 改为 `"  hint: {suggestion}"`，并确认 `Error::Io` 变体的 `suggestion()` 返回的通用 hint 文本满足 ERROR-02 覆盖要求。

Purpose: 解决 ERROR-01 (展示出错字段名/原因) 和 ERROR-02 (附带可操作 hint)。
- **ERROR-01 覆盖依据（per D-04）：** 字段名/原因已通过既有 thiserror Display 文本承担——例如 `ConfigError::InvalidValue` 的 Display 格式为 `"Invalid configuration value {field} = '{value}': {reason}"`，已包含字段名与原因。CONTEXT.md D-04 明确将此情况文档化为范围决策：ROADMAP Phase 46 Success Criteria #1 中提及的 `expected string, got integer` 仅为说明性示例而非精确格式合同，本 phase 不修改任何子错误的 thiserror Display 文本。
- **ERROR-02 覆盖范围（per D-01/D-03）：** 本 phase 完成 hint 前缀统一（`Suggestion:` → `hint:`）与 Io 变体 hint 校验。

Output: 修订后的 main.rs 与 error.rs，外加针对 hint 前缀与 Io hint 的回归测试。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/46-errors/46-CONTEXT.md
@src/error.rs
@src/main.rs
@CLAUDE.md

<interfaces>
<!-- 从 src/error.rs 提取的核心契约，executor 直接使用，无需再次探索代码库 -->

From src/error.rs (already in scope):
- `pub enum Error { Config(ConfigError), File(FileError), Parser(ParserError), Export(ExportError), Io(io::Error), Interrupted }`
- `impl Error { pub fn severity(&self) -> ErrorSeverity; pub fn suggestion(&self) -> &str; pub fn is_fatal(&self) -> bool; }`
- `pub enum ErrorSeverity { Warning, Error, Critical }` with `Display` 输出 "WARNING" / "ERROR" / "CRITICAL"
- 所有子错误枚举（ConfigError/FileError/ParserError/ExportError）的 thiserror Display 已包含 path/field/reason — 本 phase 不修改 Display 文本（依据 D-04）
- `Error::Io(_)` 当前 suggestion() 已返回 `"Check filesystem permissions and disk space."`（非空），D-03 任务为验证其内容符合"通用提示"标准

From src/main.rs (current error print path, lines 75-86):
- Match arm `Err(e) =>` 分支调用 `e.severity()` 得到前缀
- 调用 `e.suggestion()` 取得 hint 文本
- 第二行 eprintln! 当前格式：`"  Suggestion: {suggestion}"`（保留前导两空格缩进）
- `Error::Interrupted` 走独立的 EXIT_INTERRUPTED 分支，不进入打印 hint 路径
</interfaces>

<existing_tests>
src/main.rs 现有 13 个单元测试（含 test_error_suggestion_for_config_not_found / test_error_suggestion_for_export_write_failed 等），全部基于 e.suggestion() 返回值，无任何测试断言依赖字面量 "Suggestion:"，所以前缀替换不会破坏现有测试。
</existing_tests>

</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 替换错误展示前缀为 hint 并补全 Io hint 回归测试</name>
  <files>src/main.rs, src/error.rs</files>
  <read_first>
    - src/main.rs (完整读取，需要定位 main() 的错误打印分支与现有 #[cfg(test)] mod tests 块的所有测试函数)
    - src/error.rs (完整读取，确认 Error::Io match arm 在 suggestion() 中的当前文本和位置)
    - .planning/phases/46-errors/46-CONTEXT.md (D-01/D-02/D-03/D-04 决策，特别是 hint 文本与格式)
  </read_first>
  <behavior>
    - 单元测试 test_error_io_suggestion_non_empty：对 `Error::Io(io::Error::other("disk full"))` 调用 `suggestion()`，返回值非空且包含 "filesystem" 子串（验证 D-03）
    - 单元测试 test_error_print_format_uses_hint_prefix：构造一个 `Error::Export(ExportError::WriteFailed{...})`，调用 `format_error_output(&e)` 辅助函数（详见 action），断言输出文本第二行匹配正则 `^  hint: ` 且不再含 "Suggestion:"（验证 D-01 的前缀替换）
    - 既有 13 项测试继续全部通过（不修改任何既有测试断言）
  </behavior>
  <action>
    在 src/main.rs 中：
    1. 将第 83 行 `eprintln!("  Suggestion: {suggestion}");` 替换为 `eprintln!("  hint: {suggestion}");`（保留两个前导空格缩进与 newline，仅替换 "Suggestion" 为小写 "hint"，对应 D-01）。
    2. 在 main() 错误打印路径外提取一个 `fn format_error_output(e: &Error) -> String` 辅助函数（不超过 40 行），它返回最终用于打印的多行字符串（首行 `[{sev}] {e}`，若 suggestion 非空再追加 `\n  hint: {suggestion}`），并把 main() 的 `Err(e)` 分支改为调用该函数 + `eprintln!("{}", format_error_output(&e))`。此重构是为了让 hint 前缀可被单元测试直接断言而无需子进程捕获 stderr。Interrupted 分支保持独立逻辑。
    3. 在 src/main.rs `#[cfg(test)] mod tests` 内新增两个测试：
       - `test_error_io_suggestion_non_empty`：构造 `Error::Io(std::io::Error::other("disk full"))`，断言 `e.suggestion()` 非空且包含 "filesystem"。
       - `test_error_print_format_uses_hint_prefix`：构造 `Error::Export(ExportError::WriteFailed { path: "/tmp/out.csv".into(), reason: "disk full".into() })`，调用 `format_error_output(&e)`，断言返回字符串包含 `"\n  hint: "`、不含 `"Suggestion:"`，且首行以 `"[ERROR]"` 开头（WriteFailed 的 severity 为 Error）。

    在 src/error.rs 中：
    4. 校验第 156 行 `Error::Io(_) => "Check filesystem permissions and disk space."` 文本与 CONTEXT.md D-03 完全一致（已一致则保持不变，无需新增代码）。注释也不需要变更。如发现该 arm 与 D-03 文本不一致，则替换为 D-03 指定文本。
    5. 不修改任何其他 match arm，不修改任何 thiserror Display 文本，不新增 Error 变体（CONTEXT.md domain 边界与 D-04 决策明确限制）。
    6. 不修改 src/main.rs 第 80 行 `eprintln!("[{sev}] {e}")` 的格式（保留 `[SEVERITY]` 前缀，对应 D-01）。

    遵循 CLAUDE.md 规范：函数 < 40 行（format_error_output 仅 5-8 行）、变量名描述性（使用 `formatted` / `lines` 而非单字母）、commit message 采用 conventional commit 格式。
  </action>
  <verify>
    <automated>cargo test --quiet 2>&1 | tail -20 && cargo clippy --all-targets -- -D warnings 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - 源码断言：`grep -n 'hint: {suggestion}' src/main.rs` 输出 1 行（替换后的 eprintln 行）
    - 源码断言：`grep -nc 'Suggestion:' src/main.rs` 输出 `0`（无残留旧前缀）
    - 源码断言：`grep -n 'fn format_error_output' src/main.rs` 输出 1 行（辅助函数已抽取）
    - 源码断言：`grep -n 'Error::Io(_) => "Check filesystem permissions and disk space."' src/error.rs` 输出 1 行（D-03 文本就位）
    - 测试断言：`cargo test test_error_io_suggestion_non_empty --quiet` 退出码为 0
    - 测试断言：`cargo test test_error_print_format_uses_hint_prefix --quiet` 退出码为 0
    - 测试断言：`cargo test --quiet` 全部测试通过（含原 13 项 + 新增 2 项 = 至少 15 项 main.rs 单元测试），退出码 0
    - 质量门禁：`cargo clippy --all-targets -- -D warnings` 退出码 0，无任何 warning 行
    - 质量门禁：`cargo fmt --check` 退出码 0
  </acceptance_criteria>
  <done>
    main.rs 错误打印路径输出 "hint:" 前缀；format_error_output 辅助函数存在且被 main() 调用；Error::Io 的 suggestion() 文本符合 D-03；新增 2 项单元测试通过；全套质量门禁绿灯。
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 端到端 CLI 验证 hint 输出并固化 release build 门禁</name>
  <files>tests/integration.rs</files>
  <read_first>
    - tests/integration.rs (完整读取，了解既有集成测试的 assert_cmd 风格、temp dir 用法、CliCommand 调用模式)
    - src/main.rs (确认 Task 1 已完成的 format_error_output 调用路径及退出码常量 EXIT_FATAL=2)
  </read_first>
  <behavior>
    - 集成测试 test_cli_error_uses_hint_prefix 的预期 stderr 断言行为：
      - **退出码：** 进程以 `EXIT_FATAL = 2` 退出（通过 `assert_cmd::Command::assert().code(2)` 或同等断言验证）。
      - **stderr 包含 `"[CRITICAL]"` 字面前缀：** 证实 D-01 的严重级别前缀被保留并写入实际 stderr 流。
      - **stderr 包含 `"  hint: "` 字面前缀（含两个前导空格）：** 证实 Task 1 的 `format_error_output` 重构产物经由 `eprintln!` 实际写入 stderr，覆盖端到端路径（而非仅内存中的字符串）。
      - **stderr 不包含 `"Suggestion:"` 字面字符串：** 证实旧前缀彻底从打印路径消失，无任何遗漏分支仍输出旧前缀。
      - **stderr 包含 `"Configuration error"` 字面文本：** 间接证实 thiserror Display 文本经 ConfigError::ParseFailed 路径正确写入（覆盖 ERROR-01 + D-04 的"既有 Display 承担字段/原因"假设）。
    - 触发路径：通过临时文件写入无效 TOML 内容（如 `not valid toml ][[[`），让 `load_config` 返回 `Error::Config(ConfigError::ParseFailed { .. })`，进入 main() 的 fatal 分支。
    - 断言方式：使用 `predicates::str::contains("...")` 与 `.not()` 组合，或同等的字符串包含/排除检查。
  </behavior>
  <action>
    在 tests/integration.rs 末尾新增一个集成测试 `test_cli_error_uses_hint_prefix`：
    1. 使用 `assert_cmd::Command::cargo_bin("sqllog2db")` 调用 `run -c /nonexistent/definitely-missing-config.toml`（一个保证不存在的路径）。
       - 注意：根据 src/main.rs::load_config 当前行为，"配置文件不存在"会回落到 Config::default() 而不会致命退出。要触发致命错误打印，使用一个会被 load_config 当作 Error::Config(非 NotFound) 的场景：写入一个无效 TOML 内容到 temp 文件（如 `not valid toml ][[[`），然后 `run -c <temp_path>`，load_config 会传递 ParseFailed 错误，main() 致命分支打印 `[CRITICAL] Configuration error: ...` + `  hint: Check TOML syntax in the configuration file.`。
    2. 断言：进程退出码 = 2 (EXIT_FATAL)；stderr 包含字面量字符串 `"  hint: "`；stderr 包含 `"[CRITICAL]"` 前缀；stderr 不包含字面量字符串 `"Suggestion:"`；stderr 包含 `"Configuration error"` 字面文本（间接验证 ERROR-01 字段/原因经 Display 输出）。
    3. 使用 `tempfile::TempDir::new()` 创建临时目录写入 bad.toml（与 main.rs 既有单元测试 test_load_config_invalid_toml_returns_error 同套路）。
    4. 整体测试函数 < 40 行，遵循既有集成测试命名/风格。

    本任务不修改 src/，仅补充 tests/integration.rs 端到端验证，覆盖 main() 的 eprintln 实际写入 stderr 路径（Task 1 单元测试只断言 format_error_output 返回值的内存表示）。
  </action>
  <verify>
    <automated>cargo test --test integration test_cli_error_uses_hint_prefix --quiet 2>&1 | tail -10 && cargo build --release 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - 测试存在：`grep -n 'fn test_cli_error_uses_hint_prefix' tests/integration.rs` 输出 1 行
    - 测试通过：`cargo test --test integration test_cli_error_uses_hint_prefix --quiet` 退出码 0
    - 全套测试：`cargo test --quiet` 退出码 0（含所有单元测试 + 所有集成测试）
    - Release build：`cargo build --release` 退出码 0，无 deprecated/warning 行
    - 质量门禁：`cargo clippy --all-targets -- -D warnings` 退出码 0
    - 质量门禁：`cargo fmt --check` 退出码 0
    - 行为断言：集成测试代码体内通过 `predicates::str::contains("  hint: ")` 与 `predicates::str::contains("Suggestion:").not()` 显式断言 stderr 内容（或同等效果的字符串包含检查）
  </acceptance_criteria>
  <done>
    端到端集成测试 test_cli_error_uses_hint_prefix 通过，证明 CLI 实际 stderr 输出包含 "  hint: " 前缀且不再包含 "Suggestion:"；release build 与全套质量门禁绿灯，Phase 46 实现完整。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 用户配置文件 (config.toml) → 进程 | 用户提供的 TOML 内容进入 toml::from_str，错误路径输出到 stderr |
| 进程 → stderr | 错误信息（含 path / reason 字段）经 eprintln! 写入 stderr，可能被脚本/管道捕获 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-01 | Information Disclosure | format_error_output / eprintln!(stderr) | accept | hint 文本为编译期静态字符串，不含任何运行时秘密；Display 文本最多包含用户自己提供的路径/字段名，无新增泄露面 |
| T-46-02 | Tampering | Error::Io 的 hint 文本 | mitigate | 通过 acceptance_criteria 中的 `grep -n 'Error::Io(_) => "Check filesystem permissions and disk space."'` 源码断言锁定文本，防止后续误改 |
| T-46-03 | Denial of Service | format_error_output 函数 | mitigate | 函数纯字符串拼接，无递归/无 I/O，常数时间；单元测试覆盖 Export 变体，长度有界 |
</threat_model>

<verification>
ERROR-01 (字段名/原因展示)：通过既有 thiserror Display 文本承担（如 `ConfigError::InvalidValue` 已 Display 为 `Invalid configuration value {field} = '{value}': {reason}`），本 phase 通过保留 `[{sev}] {e}` 打印第一行间接验证 — Task 2 集成测试 stderr 包含 `[CRITICAL] Configuration error: Failed to parse configuration file ...` 实证字段/原因可见。覆盖范围决策已由 CONTEXT.md D-04 文档化。

ERROR-02 (hint 修复建议)：通过 `hint:` 前缀统一 + Error::Io hint 文本非空验证。两项任务的 acceptance_criteria 联合证明每条非中断致命错误都能输出 hint 行。
</verification>

<success_criteria>
- src/main.rs 错误打印路径输出 `  hint: {suggestion}`，旧前缀 `Suggestion:` 完全消失
- src/error.rs Error::Io 的 suggestion() 文本与 D-03 一致：`"Check filesystem permissions and disk space."`
- 新增 2 项单元测试 + 1 项集成测试全部通过
- 现有 13 项单元测试 + 既有集成测试全部继续通过，零回归
- `cargo build --release` + `cargo test` + `cargo clippy --all-targets -- -D warnings` + `cargo fmt --check` 全套绿灯
- ROADMAP Phase 46 Success Criteria 1-4 全部满足（其中 Criteria #1 的"具体格式示例 expected string, got integer"按 D-04 决策由既有 Display 文本承担）
</success_criteria>

<output>
Create `.planning/phases/46-errors/46-01-SUMMARY.md` when done
</output>
