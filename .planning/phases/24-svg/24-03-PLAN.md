---
phase: 24-svg
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/quickstart.md
  - docs/config-reference.md
autonomous: true
requirements: [I18N-03, I18N-04]

must_haves:
  truths:
    - 用户打开 docs/quickstart.md 看到的是全中文的快速入门指南
    - 用户打开 docs/config-reference.md 看到的是全中文的配置参考文档
    - 两篇文档中的所有配置示例、命令、路径示例保持原样
  artifacts:
    - path: "docs/quickstart.md"
      provides: "快速入门指南（中文）"
      min_lines: 200
    - path: "docs/config-reference.md"
      provides: "配置参考文档（中文）"
      min_lines: 200
  key_links:
    - from: "docs/quickstart.md"
      to: "README.md"
      via: "相对路径链接"
      pattern: "\\.\\./README\\.md"
    - from: "docs/config-reference.md"
      to: "docs/quickstart.md"
      via: "相对路径链接"
      pattern: "quickstart\\.md"
---

<objective>
将 docs/quickstart.md 和 docs/config-reference.md 两篇技术文档翻译为全中文。

Purpose: 补全 docs/ 目录下两份核心用户文档的中文化，使中文 DBA 能完整阅读配置指南和参考手册。
Output: 全中文的 docs/quickstart.md 和 docs/config-reference.md。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@docs/quickstart.md
@docs/config-reference.md

## 术语一致性约定

与 Plan 01（24-01-PLAN.md）共用相同的术语翻译表。关键对照：

| 英文 | 中文 |
|------|------|
| Dameng / DM Database | 达梦数据库 |
| pipeline | 处理管道 |
| exporter | 导出器 |
| filter / filtering | 过滤器 / 过滤 |
| template | 模板 |
| chart | 图表 |
| record | 记录 |
| parsing / parser | 解析 / 解析器 |
| CLI | 命令行 |
| config / configuration | 配置 / 配置项 |
| streaming | 流式 |
| normalization / fingerprint | 归一化 / 指纹 |
| aggregation | 聚合 |
| latency | 延迟 |
| throughput | 吞吐量 |
| binary | 二进制文件 |
| dependency | 依赖 |
| validation / validate | 验证 |
| troubleshoot / troubleshooting | 故障排查 |

## 需要执行的具体翻译规则

1. 所有正文、标题、列表、表格、说明文字翻译为中文
2. 代码块、命令（bash/cargo/sqllog2db/to/sqlite3）、TOML 配置示例保持原样
3. 链接目标和文件名（README.md、config-reference.md 等）保持原样
4. 环境变量名（SQLLOG2DB_CONFIG、NO_COLOR 等）保持原样
5. 包名、crates.io 相关信息保持原样
6. 英文括号内说明翻译为中文
7. 表格标题行翻译为中文
8. 保持 Markdown 格式结构完整
</context>

<interfaces>
<!-- docs/quickstart.md 当前结构概览 -->
<!-- 约309行，包含：
 1. # QuickStart Guide（标题）+ 简介段落
 2. ## Environment Preparation（安装步骤）
 3. ## Scenario 1: Export SQL Logs to CSV（5个步骤 + 故障排查）
 4. ## Scenario 2: Export to SQLite Database（3个步骤）
 5. ## Scenario 3: Per-File Statistics and Slow-Query Analysis（2个步骤）
 6. ## Scenario 4: SQL Template Aggregation and Chart Generation（4个步骤）
 7. ## Troubleshooting（5个常见问题）

docs/config-reference.md 当前结构概览 -->
<!-- 约260行，包含：
 1. # Configuration Reference（标题 + 简介）
 2. ## [sqllog]（输入配置）
 3. ## [logging]（日志配置）
 4. ## [filter.include] and [filter.exclude]（过滤配置）
 5. ## [template]（模板配置）
 6. ## [charts]（图表配置）
 7. ## [exporter.csv]（CSV 导出配置）
 8. ## [exporter.sqlite]（SQLite 导出配置）
 9. ## [features.replace_parameters]（遗留配置）
 10. ## Appendix: Config Behavior Notes（6个子节）
-->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 翻译 docs/quickstart.md 为中文</name>
  <files>docs/quickstart.md</files>
  <read_first>docs/quickstart.md</read_first>
  <action>
    将 docs/quickstart.md 全部翻译为中文。具体事项：

    **1. 章节标题翻译：**
    - "QuickStart Guide" → 快速入门指南
    - "Environment Preparation" → 环境准备
    - "Scenario 1: Export SQL Logs to CSV" → 场景一：导出 SQL 日志到 CSV
    - "Scenario 2: Export to SQLite Database" → 场景二：导出到 SQLite 数据库
    - "Scenario 3: Per-File Statistics and Slow-Query Analysis" → 场景三：按文件统计与慢查询分析
    - "Scenario 4: SQL Template Aggregation and Chart Generation" → 场景四：SQL 模板聚合与图表生成
    - "Troubleshooting" → 故障排查

    **2. 内容翻译：**
    - 简介段落 "This guide walks you through..." 翻译为中文介绍
    - 所有步骤说明文字翻译为中文
    - 预期输出（Expected output）的说明文字翻译为中文
    - 故障排查段落全部翻译为中文
    - 表格标题行翻译为中文

    **3. 保留的内容：**
    - 所有 bash 代码块内容（curl、cargo、sqllog2db、sqlite3 命令）
    - TOML 配置示例
    - 预期输出中的原始数据内容（如 "HIHIS|1748411" 等统计结果行）
    - 链接目标路径（config-reference.md、../README.md 等）
    - "`--group-by`" 等 CLI flag 保持英文

    **4. 内联英文保持：**
    以下内容保持英文不变：
    - CLI flag 名称（--top-slow, --group-by, --from, --to, --limit 等）
    - 配置字段名（USERNAME, APPGROUP, IP_ADDRESS, STMT_TYPE 等）
    - 文件名（config.toml, output/sqllog.csv 等）
    - bash 命令中出现的所有参数
    - 英文缩写保留：CSV、SQLite、SVG、TOML、NVMe、SSD、DBA、SIMD

    **5. 错误信息翻译：**
    配置验证错误信息（如 "Config validation failed: sqllog.path"）保留原始错误消息，但说明文字改为中文。
  </action>
  <verify>
    <automated>
      # 中文化检查：标题改为中文
      if ! grep -q '快速入门指南' docs/quickstart.md; then echo "FAIL: Chinese title not found"; fi
      if ! grep -q '场景一' docs/quickstart.md; then echo "FAIL: Scenario headers not translated"; fi
      if ! grep -q '故障排查' docs/quickstart.md; then echo "FAIL: Troubleshooting not translated"; fi

      # 代码块保留
      if ! grep -q 'cargo install dm-database-sqllog2db' docs/quickstart.md; then echo "FAIL: Code blocks modified"; fi

      # 英文缩写保留
      if ! grep -q 'CSV' docs/quickstart.md; then echo "FAIL: CSV acronym removed"; fi
    </automated>
  </verify>
  <acceptance_criteria>
    - docs/quickstart.md 标题为 "快速入门指南"
    - 四个场景标题翻译为中文
    - 故障排查标题翻译为"故障排查"
    - 所有 bash 代码块保持原样
    - 所有 TOML 配置示例保持原样
    - 所有预期输出数据保持原样
    - 所有 CLI flag 名称保持英文原样
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: 翻译 docs/config-reference.md 为中文</name>
  <files>docs/config-reference.md</files>
  <read_first>docs/config-reference.md</read_first>
  <action>
    将 docs/config-reference.md 全部翻译为中文。具体事项：

    **1. 章节标题翻译：**
    - "Configuration Reference" → 配置参考
    - "[sqllog]" 等节标题保持符号原样（方括号和英文名不翻译），但紧跟的描述文字翻译为中文
    - "Appendix: Config Behavior Notes" → 附录：配置行为说明
    - "Exporter Priority" → 导出器优先级
    - "Pipeline Fast Path" → 处理管道快速路径
    - "Config Validation" → 配置验证
    - "CLI Overrides" → 命令行覆盖
    - "CLI Commands" → 命令行子命令
    - "Field Order" → 字段顺序
    - "Environment Variables" → 环境变量

    **2. 内容翻译：**
    - 文档简介段落翻译为中文
    - 每个配置节的说明文字翻译为中文
    - 表格中的 Description 列翻译为中文
    - Notes 段落翻译为中文
    - 附录部分全部翻译为中文
    - CLI Commands 段落中的工具描述翻译为中文

    **3. 保留的内容：**
    - 所有 TOML 配置块（代码块中的 [sqllog]、[logging] 等）
    - 所有命令（sqllog2db validate、sqllog2db run 等）
    - 表格中的字段名（path、pattern、level、file 等）和类型（String、bool、usize）
    - 默认值（如 "info"、"null"、"false"）
    - 所有 CLI flag 名称
    - 环境变量名（SQLLOG2DB_CONFIG、NO_COLOR、RUST_LOG）
    - 链接目标文件名（quickstart.md 等）

    **4. 内联英文保持：**
    - 配置字段名、类型名保持英文
    - 技术缩写保持英文：CSV、SQLite、TOML、SVG、PRAGMA、hdrhistogram、SIMD、NVMe
    - 文件路径和示例值保持英文
    - serde、BufWriter、itoa 等 Rust 相关名词保持英文
  </action>
  <verify>
    <automated>
      # 中文化检查
      if ! grep -q '## 配置参考' docs/config-reference.md && ! grep -q '配置参考' docs/config-reference.md; then echo "FAIL: Title not translated"; fi
      if ! grep -q '导出器优先级' docs/config-reference.md; then echo "FAIL: Exporter Priority not translated"; fi
      if ! grep -q '处理管道快速路径' docs/config-reference.md; then echo "FAIL: Pipeline Fast Path not translated"; fi
      if ! grep -q '环境变量' docs/config-reference.md; then echo "FAIL: Environment Variables not translated"; fi

      # TOML 配置块保留
      if ! grep -q '\[sqllog\]' docs/config-reference.md; then echo "FAIL: TOML block modified"; fi
      if ! grep -q '\[exporter.csv\]' docs/config-reference.md; then echo "FAIL: CSV exporter section modified"; fi

      # 命令保留
      if ! grep -q 'sqllog2db validate' docs/config-reference.md; then echo "FAIL: Command references modified"; fi
    </automated>
  </verify>
  <acceptance_criteria>
    - docs/config-reference.md 所有正文为中文
    - 配置节标题翻译：方括号部分保持原样，描述文字为中文
    - 所有 TOML 代码块保持原样
    - 所有 sqllog2db 命令保持原样
    - 表格中的字段名、类型、默认值保持英文
    - 附录部分全部翻译为中文
    - 环境变量名保持原样
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This is a documentation-only plan with no runtime code changes. No new trust boundaries are introduced.

| Boundary | Description |
|----------|-------------|
| No code boundary | docs/quickstart.md 和 docs/config-reference.md 均为 Markdown 文档，不涉及代码执行路径 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-03 | Tampering | docs/*.md content | mitigate | 仅翻译文档内容，所有代码块和命令保持原样。不修改 src/ 目录任何文件 |
| T-24-SC | Tampering | no packages | accept | 本计划不涉及任何包管理操作 |
</threat_model>

<verification>

全自动验证步骤：

```bash
# 1. quickstart 标题已翻译
grep -q '快速入门指南' docs/quickstart.md

# 2. quickstart 场景已翻译
grep -q '场景一' docs/quickstart.md

# 3. quickstart 代码块保留
grep -q 'cargo install dm-database-sqllog2db' docs/quickstart.md

# 4. config-reference 导出器优先级已翻译
grep -q '导出器优先级' docs/config-reference.md

# 5. config-reference TOML 块保留
grep -q '\[exporter.csv\]' docs/config-reference.md

# 6. config-reference 环境变量段翻译
grep -q '环境变量' docs/config-reference.md
```

</verification>

<success_criteria>
- docs/quickstart.md 全部正文为中文
- docs/config-reference.md 全部正文为中文
- 两篇文档中的所有代码块、命令、TOML 示例、表格字段名保持原样
- 所有 Markdown 链接和内部跨文档引用保持正确
</success_criteria>
