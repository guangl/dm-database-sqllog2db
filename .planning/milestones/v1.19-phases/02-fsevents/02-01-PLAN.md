---
phase: 02-fsevents
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/watch_incremental.rs
autonomous: true
requirements: [QUAL-02, QUAL-03]
tags: [watch, integration-test, csv-append, error-log, exit-code]

must_haves:
  truths:
    - "`cargo test --test watch_incremental test_watch_07_csv_append` 绿色：两次 trigger_full_file 后 CSV 含 header + 6 行数据，header 仅出现一次"
    - "`cargo test --test watch_incremental test_watch_08_error_log_append` 绿色：两次带解析错误的触发后 error log 含至少 2 条 [ERROR] 行"
    - "`cargo test --test watch_incremental test_watch_09_exit_code_130` 绿色：interrupted=true 时 handle_watch 返回 Err(Error::Interrupted)"
    - "tests/integration.rs:2917 的 test_watch_triggers_on_new_log_file 保留 #[ignore]，函数体不变"
  artifacts:
    - path: "tests/watch_incremental.rs"
      provides: "WATCH-07/08/09 集成测试 + build_csv_config helper + INVALID_LOG_LINE 常量"
      contains: "test_watch_07_csv_append, test_watch_08_error_log_append, test_watch_09_exit_code_130, build_csv_config, INVALID_LOG_LINE"
  key_links:
    - from: "tests/watch_incremental.rs::test_watch_07_csv_append"
      to: "dm_database_sqllog2db::cli::watch::trigger_full_file"
      via: "直接调用两次（同一 cfg, 不同 log path）"
      pattern: "trigger_full_file\\(.*&cfg"
    - from: "tests/watch_incremental.rs::test_watch_09_exit_code_130"
      to: "dm_database_sqllog2db::cli::watch::handle_watch"
      via: "interrupted=Arc::new(AtomicBool::new(true)) 预设中断后调用"
      pattern: "handle_watch\\(.*interrupted"
---

<objective>
为 WATCH-07/08/09 三项功能补充集成测试，覆盖真实文件 I/O 路径（CSV exporter initialize/append、write_error_log 双分支、handle_watch 退出码 130），同时通过 trigger_full_file 间接带动 src/exporter/csv/mod.rs 的 export 路径覆盖。

Purpose: 满足 Phase 2 success criteria 第 3 条（新增 WATCH-07/08/09 集成测试），与 Phase 1 已添加的 src/cli/watch/mod.rs::tests 单元测试互补。集成测试同时为 QUAL-02 提供 csv/mod.rs 覆盖率增量。

Output: tests/watch_incremental.rs 新增 3 个 #[test] 函数 + 1 个 helper (build_csv_config) + 1 个常量 (INVALID_LOG_LINE)。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/02-fsevents/02-CONTEXT.md
@.planning/phases/02-fsevents/02-RESEARCH.md
@.planning/phases/02-fsevents/02-PATTERNS.md
@.planning/phases/01-watch/01-01-SUMMARY.md

<interfaces>
<!-- Key signatures executor needs. Extracted from src/cli/watch/mod.rs lines 30, 299, 382. -->

From src/cli/watch/mod.rs:
```
pub fn handle_watch(
    cfg: &Config,
    quiet: bool,
    verbose: bool,
    interrupted: &Arc<AtomicBool>,
) -> Result<()>

pub fn trigger_full_file(
    file: &Path,
    cfg: &Config,
    quiet: bool,
    verbose: bool,
    interrupted: &Arc<AtomicBool>,
    state: &mut WatchLoopState,
    pb: &ProgressBar,
)

pub fn trigger_incremental(
    file: &Path,
    cfg: &Config,
    quiet: bool,
    verbose: bool,
    interrupted: &Arc<AtomicBool>,
    state: &mut WatchLoopState,
    pb: &ProgressBar,
)
```

From src/config/mod.rs (re-exports at line 7, ErrorLogConfig at line 19, append_error_log at line 40 area):
```
pub use exporter::{CsvExporterConfig, ExporterConfig, SqliteExporterConfig};
pub struct ErrorLogConfig { pub file: String }
pub struct Config {
    pub sqllog: SqllogConfig,
    pub exporter: ExporterConfig,
    pub error: Option<ErrorLogConfig>,
    pub append_error_log: bool,  // #[serde(skip)], default false
    ...
}
pub struct CsvExporterConfig {
    pub file: String,
    pub overwrite: bool,
    pub append: bool,
    pub include_performance_metrics: bool,
}
```

From src/error.rs (used by WATCH-09):
```
pub enum Error { ..., Interrupted, ... }
```

Re-exports the integration test must use (verified in tests/watch_incremental.rs current file):
```
use dm_database_sqllog2db::cli::watch::{WatchLoopState, trigger_full_file, trigger_incremental};
use dm_database_sqllog2db::config::{Config, ExporterConfig, SqliteExporterConfig, SqllogConfig};
```

WATCH-09 newly required imports (verified path: handle_watch is pub at line 30, Error is pub at crate root via error module):
```
use dm_database_sqllog2db::cli::watch::handle_watch;
use dm_database_sqllog2db::error::Error;
use dm_database_sqllog2db::config::{CsvExporterConfig, ErrorLogConfig};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: WATCH-07/08/09 集成测试 + helper + 常量</name>
  <files>tests/watch_incremental.rs</files>
  <read_first>
    - tests/watch_incremental.rs (现有 WATCH-03/04 测试与 helper：write_test_log_records、build_sqlite_config、build_pb、smoke_test_helpers_compile)
    - src/cli/watch/mod.rs (查看 force_append_for_watch_trigger 与 tests::test_watch_csv_append/test_watch_error_log_append/test_handle_watch_returns_interrupted 的 Config 构造与断言模式，作为集成测试改写参考)
    - .planning/phases/02-fsevents/02-PATTERNS.md (analog 与模板已映射)
    - .planning/phases/02-fsevents/02-RESEARCH.md (Pitfall 3：force_append_for_watch_trigger 会注入 append=true，集成测试的 cfg 初值 append=false/overwrite=true 即可)
  </read_first>
  <behavior>
    - Test 1 `test_watch_07_csv_append`: 用 build_csv_config 构建 cfg（append=false, overwrite=true, include_performance_metrics=true）；写 a.log（3 条）、b.log（3 条）；连续两次 trigger_full_file（参数：file_path, &cfg, quiet=true, verbose=false, &interrupted=false, &mut state, &pb）；读 out.csv → lines.len() >= 7 且 header（lines[0]）在全文件中只出现 1 次
    - Test 2 `test_watch_08_error_log_append`: cfg 含 csv exporter（同 build_csv_config）与 error: Some(ErrorLogConfig { file: error_log_path })；a.log/b.log 内容均为 INVALID_LOG_LINE + 一条合法 SEL 行；两次 trigger_full_file 后断言 error_log_path 存在，内容按 [ERROR] 前缀过滤的行数 >= 2
    - Test 3 `test_watch_09_exit_code_130`: cfg 用 build_csv_config（log_dir=tmp.path() 目录、csv_path 任意）；interrupted=Arc::new(AtomicBool::new(true))；调用 handle_watch(&cfg, true, false, &interrupted)，断言 matches!(result, Err(Error::Interrupted))
    - 现有 4 个测试 (smoke_test_helpers_compile, test_watch_03_*, test_watch_04_*) 全部保持绿色不退化
    - 现有 helper build_sqlite_config / write_test_log_records / count_rows / build_pb 保持不变
  </behavior>
  <action>
    在 tests/watch_incremental.rs 文件末尾追加：

    1) 顶部 use 段（如已存在则跳过）扩展为包含：
       - `use dm_database_sqllog2db::cli::watch::handle_watch;`
       - `use dm_database_sqllog2db::error::Error;`
       - `use dm_database_sqllog2db::config::{CsvExporterConfig, ErrorLogConfig};`
       现有的 `WatchLoopState, trigger_full_file, trigger_incremental` 与 `Config, ExporterConfig, SqliteExporterConfig, SqllogConfig` 保持不变。

    2) 新增常量（紧邻现有 helper 段）：
       - `const INVALID_LOG_LINE: &str = "this is not a valid dm sql log line at all\n";`

    3) 新增 helper `build_csv_config(log_path: &Path, csv_path: &Path) -> Config`：
       - sqllog.inputs = vec![log_path.to_string_lossy().into_owned()]，path_deprecated=None
       - exporter.csv = Some(CsvExporterConfig { file: csv_path.to_string_lossy().into_owned(), overwrite: true, append: false, include_performance_metrics: true })
       - exporter.sqlite = None
       - 其余字段 ..Config::default()
       - 不要手动设置 append_error_log（force_append_for_watch_trigger 会注入；run 路径 default=false 保持）

    4) 新增 `test_watch_07_csv_append`：
       - TempDir、log_path_a/log_path_b/csv_path 在 tmp.path() 下
       - write_test_log_records(&log_path_a, 0, 3); write_test_log_records(&log_path_b, 3, 3)
       - cfg = build_csv_config(&log_path_a, &csv_path)（cfg.sqllog.inputs 的值不影响 trigger_full_file，trigger 用第一参数 file 路径）
       - interrupted = Arc::new(AtomicBool::new(false)); pb = build_pb(); state = WatchLoopState::new(HashMap::new(), None)
       - 调用 trigger_full_file 两次（先 a 后 b，参数顺序：file, &cfg, quiet=true, verbose=false, &interrupted, &mut state, &pb）
       - 读 csv_path 内容；lines = content.lines().collect::<Vec<_>>(); 断言 lines.len() >= 7；let header = lines[0]; header_count = lines.iter().filter(|&&l| l == header).count(); assert_eq!(header_count, 1, ...)

    5) 新增 `test_watch_08_error_log_append`：
       - TempDir、log_path_a/log_path_b/csv_path/error_log_path 在 tmp.path() 下
       - 合法行常量（function-local）：
         `let valid_line = "2025-01-15 10:30:28.001 (EP[0] sess:0x0001 user:TESTUSER trxid:1 stmt:0x1 appname:App ip:10.0.0.1) [SEL] SELECT id FROM t. EXECTIME: 5(ms) ROWCOUNT: 1(rows) EXEC_ID: 1.\n";`
       - std::fs::write(&log_path_a, format!("{INVALID_LOG_LINE}{valid_line}")).unwrap(); 同样写 b
       - cfg = build_csv_config(&log_path_a, &csv_path) 再补 `cfg.error = Some(ErrorLogConfig { file: error_log_path.to_string_lossy().into_owned() });`
       - interrupted/pb/state 同 Test 1
       - 两次 trigger_full_file（a, b）后断言：
         - error_log_path.exists()
         - let error_content = std::fs::read_to_string(&error_log_path).unwrap();
         - let error_line_count = error_content.lines().filter(|l| l.starts_with("[ERROR]")).count();
         - assert!(error_line_count >= 2, "应含至少 2 条 [ERROR]，实际 {error_line_count}\n{error_content}");

    6) 新增 `test_watch_09_exit_code_130`：
       - TempDir、csv_path 在 tmp.path() 下
       - cfg = build_csv_config(tmp.path(), &csv_path)（log path 用目录本身即可，handle_watch 不会消费 inputs[0] 内容）
       - interrupted = Arc::new(AtomicBool::new(true))
       - let result = handle_watch(&cfg, true, false, &interrupted);
       - assert!(matches!(result, Err(Error::Interrupted)), "interrupted=true 时 handle_watch 应返回 Err(Error::Interrupted)，实际: {result:?}");

    7) 不修改 tests/integration.rs:2917 的 test_watch_triggers_on_new_log_file（D-01 锁定保留 #[ignore]）。

    符合 per-decision tracing：实现自 D-08（集成测试位置与函数命名）、D-09（保留 Phase 1 单元测试）、D-01（不动 #[ignore] 测试）。
  </action>
  <verify>
    <automated>cargo test --test watch_incremental -- test_watch_0</automated>
  </verify>
  <acceptance_criteria>
    - tests/watch_incremental.rs 含 `fn test_watch_07_csv_append`、`fn test_watch_08_error_log_append`、`fn test_watch_09_exit_code_130` 三个函数定义（grep -c 'fn test_watch_0[789]_' >= 3）
    - tests/watch_incremental.rs 含 `fn build_csv_config(` 函数定义
    - tests/watch_incremental.rs 含 `const INVALID_LOG_LINE` 定义
    - tests/watch_incremental.rs 含 `use dm_database_sqllog2db::cli::watch::handle_watch;` 与 `use dm_database_sqllog2db::error::Error;` 与 `use dm_database_sqllog2db::config::{...ErrorLogConfig...};`
    - `cargo test --test watch_incremental` 全部测试（7 个：4 现有 + 3 新增）通过，退出码 0
    - `cargo test` 全套通过，无回归（含 2 个 #[ignore]）
    - `cargo clippy --all-targets -- -D warnings` 通过，无新增警告
    - `cargo fmt --check` 通过
    - tests/integration.rs:2917 处仍为 `#[ignore]`（grep -n '#\[ignore\]' tests/integration.rs | grep -c ':2917\|FSEvents' >= 1）—— 保留 D-01 锁定
  </acceptance_criteria>
  <done>
    3 个新集成测试函数 + build_csv_config helper + INVALID_LOG_LINE 常量已写入 tests/watch_incremental.rs；cargo test/clippy/fmt 三道质量门禁均通过；tests/integration.rs 的 FSEvents #[ignore] 测试未被修改。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 测试代码 → 集成测试 crate 边界 | 仅测试代码新增，不引入新生产路径；测试运行时使用 TempDir 隔离 I/O |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | tests/watch_incremental.rs 写入的临时文件 | accept | TempDir 自动清理，无持久化路径；与现有 WATCH-03/04 测试同等风险面 |
| T-02-02 | Denial of Service | TempDir 失败导致测试 panic | accept | 标准 cargo test 行为，CI 会标红失败；不影响生产 |
</threat_model>

<verification>
- 单测：`cargo test --test watch_incremental` 7 个测试全绿
- 全套：`cargo test` 全部通过（~884 测试），2 ignore 保持
- 质量门：`cargo clippy --all-targets -- -D warnings` 0 警告
- 格式：`cargo fmt --check` 通过
- D-01 防回归：tests/integration.rs:2917 的 `#[ignore]` 注解和函数体不变
</verification>

<success_criteria>
- 3 个新集成测试函数（test_watch_07_csv_append, test_watch_08_error_log_append, test_watch_09_exit_code_130）写入并通过
- 现有测试无回归
- cargo clippy/test/fmt 三道门禁绿色
- FSEvents `#[ignore]` 测试保持不变
</success_criteria>

<output>
Create `.planning/phases/02-fsevents/02-01-SUMMARY.md` when done
</output>
</content>
</invoke>