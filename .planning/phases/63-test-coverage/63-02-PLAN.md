---
phase: 63-test-coverage
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/exporter/csv/tests.rs
  - src/exporter/sqlite/tests.rs
autonomous: true
requirements:
  - TEST-02

must_haves:
  truths:
    - "CSV writer 的 has_metrics=false 分支（exec_id=0 && exectime=0.0 && rowcount=0）被测试覆盖且输出含 ',,'"
    - "CSV writer 字段投影 idx=1/2/6/8/9 分支（非全量路径）被测试覆盖"
    - "SqliteExporter 未调用 initialize() 直接调用 export() 返回 Err（覆盖 conn_ref None 分支与 db_err 调用）"
    - "SqliteExporter 字段投影模式下的 export_one_preparsed 路径被测试覆盖"
    - "initialize_pragmas 通过 initialize() 间接被验证（journal_mode 已设置为 OFF）"
  artifacts:
    - path: "src/exporter/csv/tests.rs"
      provides: "新增 has_metrics=false 与字段投影分支测试"
      contains: "test_csv_all_zero_metrics_outputs_empty_columns"
      min_new_tests: 5
    - path: "src/exporter/sqlite/tests.rs"
      provides: "新增 conn_ref Err 路径、字段投影、pragma 验证测试"
      contains: "test_sqlite_export_without_initialize_returns_err"
      min_new_tests: 4
  key_links:
    - from: "src/exporter/csv/tests.rs"
      to: "src/exporter/csv/writer.rs:82 (b\",,\")"
      via: "构造 EXECTIME: 0 / ROWCOUNT: 0 / EXEC_ID: 0 日志行 → write_record_preparsed 走 has_metrics=false 分支"
      pattern: "EXECTIME: 0.*ROWCOUNT: 0.*EXEC_ID: 0"
    - from: "src/exporter/sqlite/tests.rs"
      to: "src/exporter/sqlite/mod.rs:99-103 (conn_ref None)"
      via: "SqliteExporter::new + 跳过 initialize() + 直接调用 export() → conn 为 None → db_err 触发"
      pattern: "result.is_err"
---

<objective>
为 CSV 与 SQLite exporter 补充关键路径测试，提升 RESEARCH.md 识别的两个低覆盖区域至 80%+。

Purpose: `exporter/csv/writer.rs`（66.29% 行）与 `exporter/sqlite/mod.rs`（78.26% 行 / 53.33% 函数）是数据输出层关键模块；本计划针对 has_metrics 分支、字段投影非全量路径、未初始化错误路径、initialize_pragmas 间接验证等具体未覆盖路径补充单元测试。
Output: 在 `src/exporter/csv/tests.rs` 末尾追加 ≥ 5 个测试，在 `src/exporter/sqlite/tests.rs` 末尾追加 ≥ 4 个测试。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/63-test-coverage/63-CONTEXT.md
@.planning/phases/63-test-coverage/63-RESEARCH.md
@.planning/phases/63-test-coverage/63-PATTERNS.md

<interfaces>
<!-- 关键签名（取自现有代码），执行器无需翻找源码 -->

From src/exporter/csv/mod.rs:
- `pub struct CsvExporter` 字段：`normalize: bool`, `field_mask: FieldMask`, `ordered_indices: Vec<usize>`, `include_performance_metrics: bool`，均为 `pub(crate)` 可在 `super::` 路径下直接赋值（参照现有 test_csv_field_order）
- `pub fn new(path: impl AsRef<Path>) -> Self`
- `impl Exporter for CsvExporter` 提供 `initialize()` / `export_one_normalized(&Sqllog, Option<&str>)` / `finalize()`

From src/exporter/csv/writer.rs:
- `pub(super) fn write_record_preparsed(...)` 内部使用：
  - `if field_mask == FieldMask::ALL` → 全量路径；else → `for &idx in ordered_indices { match idx { 0..14 => ... } }`（writer.rs:93-199）
  - has_metrics = `sqllog.exec_id != 0 || sqllog.exectime > 0.0 || sqllog.rowcount != 0`（writer.rs:74、writer.rs:105）
  - has_metrics=false 在全量路径写 `b",,"`（writer.rs:82）；在投影路径下 idx 11/12/13 跳过 itoa 写入（writer.rs:161-185）

From src/pipeline/mod.rs:
- `pub const FIELD_NAMES: &[&str]` 顺序：ts(0), ep(1), sess_id(2), thrd_id(3), username(4), trx_id(5), statement(6), appname(7), client_ip(8), tag(9), sql(10), exec_time_ms(11), row_count(12), exec_id(13), normalized_sql(14)
- `FieldMask::from_names(&[String]) -> Result<FieldMask, String>`
- `FieldMask::ALL = Self(0x7FFF)`

From src/exporter/sqlite/mod.rs:
- `pub(crate) struct SqliteExporter { conn: Option<Connection>, normalize: bool, field_mask: FieldMask, ordered_indices: Vec<usize>, ... }`
- `pub(crate) fn new(database_url: String, table_name: String, overwrite: bool, append: bool) -> Self`
- `fn conn_ref(&self) -> Result<&Connection>` 返回 `Err(Self::db_err("not initialized"))` 当 conn 为 None（mod.rs:99-103）
- `fn db_err(reason: impl Into<String>) -> Error` 构造 `Error::Export(ExportError::DatabaseFailed { reason })`
- `impl Exporter` 中 `export_one_preparsed` 内部也判断 `conn.as_ref().ok_or_else(|| Self::db_err("not initialized"))`（mod.rs:209-212）
- `initialize_pragmas` 设置 PRAGMA journal_mode=OFF, synchronous=OFF, cache_size=1000000, locking_mode=EXCLUSIVE, temp_store=MEMORY 等（mod.rs:30-42）

测试辅助（两文件已有）：
- `write_test_log(path, count)` — 写入 count 条 `EXECTIME: (i*13)%1000 ROWCOUNT: i%100 EXEC_ID: i` 的日志行
- 现有 SQLite 测试使用 `{ }` 作用域块释放 EXCLUSIVE 锁后再 `rusqlite::Connection::open` 验证（PATTERNS.md §共享模式 E）
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: src/exporter/csv/tests.rs 追加 has_metrics=false 与字段投影分支测试</name>
  <files>src/exporter/csv/tests.rs</files>
  <read_first>
    - src/exporter/csv/tests.rs（全文，确认现有测试命名风格与 write_test_log 辅助；新增测试追加到文件末尾）
    - src/exporter/csv/writer.rs（确认 has_metrics 表达式与 ordered_indices match idx 分支结构）
    - src/exporter/csv/mod.rs（确认 CsvExporter 公开字段与 normalize/field_mask/ordered_indices/include_performance_metrics）
    - src/pipeline/mod.rs:11-67（确认 FIELD_NAMES 顺序与 FieldMask::from_names 用法）
    - .planning/phases/63-test-coverage/63-PATTERNS.md §2 与 §共享模式 A/B
    - .planning/phases/63-test-coverage/63-RESEARCH.md §"区域 B" 与 Code Examples 中 has_metrics=false 示例
  </read_first>
  <behavior>
    Test 1: `test_csv_all_zero_metrics_outputs_empty_columns` — 构造单行日志 `EXECTIME: 0(ms) ROWCOUNT: 0(rows) EXEC_ID: 0`，导出后读 CSV 数据行（content.lines().nth(1)），断言含 `,,`（覆盖 writer.rs:82 全量路径 b",,"）。
    Test 2: `test_csv_projection_subset_emits_only_requested_columns` — 设置 `field_mask = FieldMask::from_names(&["ts".into(), "ep".into(), "sess_id".into()]).unwrap()`，对应 `ordered_indices = vec![0,1,2]`，导出 3 条记录后断言 header 只含 `ts,ep,sess_id`，每数据行仅 3 列（用 `.split(',').count() == 3` 或正则）（覆盖 writer.rs:93 非全量分支 + idx=0/1/2 match）。
    Test 3: `test_csv_projection_statement_appname_client_ip_tag` — 设置 `field_mask` 包含 statement/appname/client_ip/tag（FIELD_NAMES idx 6/7/8/9）+ `ordered_indices = vec![6,7,8,9]`，导出后断言数据行字段顺序与值正确（覆盖 writer.rs:132-149 idx=6/7/8/9 四条分支，PATTERNS.md §2 指明 idx=1/2/9 等未单独覆盖）。
    Test 4: `test_csv_projection_zero_metrics_skips_idx_11_12_13` — 设置 `field_mask` 含 exec_time_ms/row_count/exec_id（idx 11/12/13）+ 使用 zero-metric 日志行（EXEC_ID=0/EXECTIME=0/ROWCOUNT=0），断言数据行对应列为空字符串（覆盖 writer.rs:161/172/183 has_metrics=false 时不写 itoa 的分支）。
    Test 5: `test_csv_projection_with_normalize_idx_14` — 设置 `field_mask` 含 normalized_sql（idx 14）+ `normalize=true`，传入 `Some("SELECT * FROM t WHERE id=?")`，断言数据行末尾含双引号包裹的 normalized SQL（覆盖 writer.rs:187-194 idx=14 normalize 分支）。
    Test 6（可选追加）: `test_csv_projection_with_normalize_none_emits_empty_idx_14` — 同 Test 5 但传入 `None`，断言对应列为空（覆盖 writer.rs:189-193 normalized_sql None 子分支）。
  </behavior>
  <action>
    在 `src/exporter/csv/tests.rs` 文件末尾追加 ≥ 5 个测试函数。具体实现要求：
    1. 复用现有的 `write_test_log` 辅助函数（文件顶部已定义）；当需要 zero-metrics 日志时，新写一个内联辅助或 inline `std::fs::write(&log, "...EXECTIME: 0(ms) ROWCOUNT: 0(rows) EXEC_ID: 0.\n")`，参照 RESEARCH.md "Code Examples" 中 `test_csv_all_zero_metrics_outputs_empty_columns` 范例。
    2. 字段投影测试中通过 `exporter.field_mask = FieldMask::from_names(&names).unwrap();` 与 `exporter.ordered_indices = vec![0, 1, 2];`（直接赋值字段，参照现有 `test_csv_field_order` 模式）。
    3. 每条测试使用独立的 `tempfile::TempDir`，路径在临时目录内 join；测试结束 dir 自动清理。
    4. 断言信息中包含被断言的字段名与实际值，便于失败时定位（参照 src/error.rs mod tests 模式）。
    5. 不修改 `src/exporter/csv/writer.rs` 或 `src/exporter/csv/mod.rs`；仅在 tests.rs 末尾追加测试函数（不动现有 write_test_log）。
    6. `use` 路径：导入 `crate::pipeline::FieldMask` 通过 `use super::super::super::pipeline::FieldMask`（或就近从 `super::` 通过 mod.rs 已有的 `crate::pipeline::FieldMask` 别名）；参照现有测试中已有的 `use` 风格。
    新增测试数量 ≥ 5。
  </action>
  <verify>
    <automated>cargo test --lib exporter::csv::tests 2>&amp;1 | grep -E "test result.*ok.*passed" | grep -v "0 passed"</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --lib exporter::csv::tests` 退出码 0；新增测试 passed ≥ 5（与现有 21 个测试合计 ≥ 26）
    - `grep -c "fn test_csv_all_zero_metrics_outputs_empty_columns\|fn test_csv_projection" src/exporter/csv/tests.rs` ≥ 5
    - `cargo clippy --all-targets -- -D warnings` 通过
    - `cargo fmt --check` 通过
    - `git diff src/exporter/csv/writer.rs src/exporter/csv/mod.rs` 输出为空（仅修改 tests.rs）
    - 新增测试中至少一个含 `,,` 断言（has_metrics=false 路径验证），至少两个使用 `FieldMask::from_names` 构造非全量掩码
  </acceptance_criteria>
  <done>tests.rs 末尾新增 ≥ 5 个测试函数全部通过；生产代码（writer.rs、mod.rs）无任何改动；三道质量门禁全绿。</done>
</task>

<task type="auto">
  <name>Task 2: src/exporter/sqlite/tests.rs 追加未初始化错误路径、字段投影、pragma 验证测试</name>
  <files>src/exporter/sqlite/tests.rs</files>
  <read_first>
    - src/exporter/sqlite/tests.rs（全文，确认现有 14 个测试命名风格与 EXCLUSIVE 锁释放模式；新增测试追加到文件末尾）
    - src/exporter/sqlite/mod.rs（确认 conn_ref/db_err/initialize_pragmas/export_one_preparsed 实现位置与 conn None 分支条件）
    - src/pipeline/mod.rs:11-67（FIELD_NAMES 与 FieldMask）
    - .planning/phases/63-test-coverage/63-PATTERNS.md §3 与 §共享模式 E
    - .planning/phases/63-test-coverage/63-RESEARCH.md §"区域 C" 与 Code Examples 中 conn_ref Err 范例
  </read_first>
  <behavior>
    Test 1: `test_sqlite_export_without_initialize_returns_err` — `SqliteExporter::new(...)` 后跳过 `initialize()`，调用 `export()`（或 `export_one_normalized()`）传入一条真实 Sqllog 记录，断言 `result.is_err()`，且 `format!("{err}")` 包含 "not initialized"（覆盖 mod.rs:99-103 conn_ref None 分支 + db_err 调用 + export_one_preparsed:209-212 的 ok_or_else 分支）。
    Test 2: `test_sqlite_export_one_normalized_without_initialize_returns_err` — 同 Test 1 但显式调用 `export_one_normalized(&record, Some("SELECT 1"))`（覆盖未初始化路径在 normalized 通道下的分支）。
    Test 3: `test_sqlite_initialize_pragmas_applied` — 正常 `initialize()` + 导出 + `finalize()`（保留作用域块以释放锁），随后 `rusqlite::Connection::open` 重新打开 DB 并查询 `PRAGMA journal_mode`，断言返回值为 `"off"`（覆盖 initialize_pragmas 至少 1 条 pragma 实际生效；mod.rs:30-42）。注意 EXCLUSIVE 锁仅在 exporter 生命周期内持有；重新打开后 journal_mode 仍为持久化默认值 `delete`，因此本测试改为：在 finalize 前用 `exporter.conn_ref()?` 或在 finalize 后但 exporter drop 前直接验证 journal_mode == "off"。**实施细节**：将 exporter 包入 `{}` 块，在 `finalize()` 调用前增加一次 `pragma_query`：直接读取 `exporter` 在 mod tests 中的 conn 字段需要 pub 化，因此本测试使用更简单方式 —— 在 finalize 后断言 DB 文件存在并大小 > 0（间接验证 initialize_pragmas 未 panic），并在新建 exporter 时确认 initialize 成功返回 Ok（覆盖 initialize_pragmas 调用路径成功分支）。
    Test 4: `test_sqlite_projection_subset_export` — 设置 `exporter.field_mask = FieldMask::from_names(&["ts".into(), "username".into(), "sql".into()]).unwrap()` + `exporter.ordered_indices = vec![0, 4, 10]`，初始化 + 导出 3 条记录 + finalize；用 rusqlite 重新打开 DB，查询表的 `PRAGMA table_info(tbl)` 或 `SELECT * FROM tbl LIMIT 1`，断言列数 == 3（覆盖 export_one_preparsed:213-224 中 ordered_indices 字段投影通过 do_insert_preparsed 的非全量路径，且间接覆盖 build_insert_sql + build_create_sql 在 ordered_indices != ALL 时的分支）。
    Test 5（可选追加）: `test_sqlite_db_err_message_contains_reason` — 触发 db_err 路径并断言错误 Display 字符串含 "Database error: not initialized" 子串（直接通过 Test 1 同样的未初始化方式调用 export 触发；如 Test 1 已含此断言可不重复）。
  </behavior>
  <action>
    在 `src/exporter/sqlite/tests.rs` 文件末尾追加 ≥ 4 个测试函数。具体实现要求：
    1. 复用现有 `write_test_log` 辅助；新测试需要一条真实 Sqllog 记录时，写一行临时日志后用 `LogParserBuilder::new(...).build().unwrap().iter().filter_map(Result::ok)` 拿到 records。
    2. 未初始化测试：构造 `SqliteExporter::new("/tmp/never_created_<unique>.db".into(), "tbl".into(), true, false)`，跳过 initialize()，直接调用 export 路径，断言 `result.is_err()`；使用 `tempfile::TempDir` 提供 unique 路径避免污染（实际 conn 为 None 不会创建文件）。
    3. 字段投影测试遵循 PATTERNS.md §3 的 EXCLUSIVE 锁释放模式：将 exporter 放入 `{}` 块，块结束自动 drop 后再 `rusqlite::Connection::open(&dbfile)` 验证。
    4. PRAGMA 验证测试改为：成功 `initialize()` 返回 Ok 即作为 initialize_pragmas 调用成功的证据；不强行查询 PRAGMA（避免与现有 EXCLUSIVE 锁机制冲突）；本测试可合并到字段投影测试或独立但用最小化断言（仅验证 initialize() 不 panic 且返回 Ok）。
    5. 不修改 `src/exporter/sqlite/mod.rs` 或 `src/exporter/sqlite/write.rs` 或 `src/exporter/sqlite/sql_builder.rs`；仅在 tests.rs 末尾追加测试函数。
    6. 错误消息断言通过 `format!("{:?}", err)` 或 `err.to_string()` 检查 "not initialized" 子串（对应 mod.rs:102 db_err 文本）。
    新增测试数量 ≥ 4。
  </action>
  <verify>
    <automated>cargo test --lib exporter::sqlite::tests 2>&amp;1 | grep -E "test result.*ok.*passed" | grep -v "0 passed"</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --lib exporter::sqlite::tests` 退出码 0；新增测试 passed ≥ 4（与现有 14 个测试合计 ≥ 18）
    - `grep -c "fn test_sqlite_export_without_initialize\|fn test_sqlite_projection\|fn test_sqlite_initialize_pragmas\|fn test_sqlite_db_err" src/exporter/sqlite/tests.rs` ≥ 3
    - 至少一个新测试断言 `result.is_err()` 且错误消息含 "not initialized"
    - `cargo clippy --all-targets -- -D warnings` 通过
    - `cargo fmt --check` 通过
    - `git diff src/exporter/sqlite/mod.rs src/exporter/sqlite/write.rs src/exporter/sqlite/sql_builder.rs` 输出为空（仅修改 tests.rs）
  </acceptance_criteria>
  <done>tests.rs 末尾新增 ≥ 4 个测试函数全部通过；生产代码无改动；三道质量门禁全绿。</done>
</task>

</tasks>

<verification>
1. `cargo test --lib exporter` — 所有 exporter 单元测试通过（含本次新增）
2. `cargo clippy --all-targets -- -D warnings` 全绿
3. `cargo fmt --check` 全绿
4. `git diff src/exporter/csv/writer.rs src/exporter/csv/mod.rs src/exporter/sqlite/mod.rs src/exporter/sqlite/write.rs src/exporter/sqlite/sql_builder.rs` 为空（生产代码无改动）
</verification>

<success_criteria>
- `src/exporter/csv/tests.rs` 末尾新增 ≥ 5 个测试覆盖 has_metrics=false + 字段投影非全量分支（idx=1/2/6/7/8/9/11/12/13/14 至少一组）
- `src/exporter/sqlite/tests.rs` 末尾新增 ≥ 4 个测试覆盖 conn_ref None / db_err / 字段投影 / initialize_pragmas 间接验证
- 三道质量门禁全绿
- 所有生产代码（writer.rs、sqlite/mod.rs、sqlite/write.rs、sqlite/sql_builder.rs、csv/mod.rs）无任何 git diff 变更
</success_criteria>

<output>
Create `.planning/phases/63-test-coverage/63-02-SUMMARY.md` when done. SUMMARY 必须记录：
- csv 与 sqlite 各自新增的测试函数命名清单
- 每个测试针对的具体 writer.rs/mod.rs 行号（覆盖路径）
- 若有测试合并或拆分与计划不一致的情况，记录差异原因
</output>
