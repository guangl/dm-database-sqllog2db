---
phase: 21-readme
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - CHANGELOG.md
autonomous: true
requirements:
  - DOC-05
  - DOC-06
user_setup: []

must_haves:
  truths:
    - "CHANGELOG.md contains entries for v1.0, v1.2, v1.2.1, v1.3, v1.4 (per D-11)"
    - "CHANGELOG.md uses Keep a Changelog format (Added/Changed/Fixed/Performance sections per version, per D-12)"
    - "0.x versions (0.1.0 through 0.10.7) are folded into a single summary paragraph (per D-13)"
    - "v1.0 entry includes migration note from 0.x to 1.0 (per D-14)"
    - "LICENSE file exists at repository root with Apache-2.0 content"
    - "CHANGELOG version numbers link to GitHub release tags"
  artifacts:
    - path: "CHANGELOG.md"
      provides: "Complete changelog from v0.x through v1.4"
      min_lines: 100
    - path: "LICENSE"
      provides: "Apache-2.0 license text"
      min_lines: 190
  key_links:
    - from: "CHANGELOG.md"
      to: "GitHub releases"
      via: "version link references at bottom"
      pattern: "github.com/guangl/sqllog2db/releases"
    - from: "CHANGELOG.md"
      to: "keepachangelog.com"
      via: "format reference link"
      pattern: "keepachangelog.com"
---

<objective>
补全 CHANGELOG.md 至 v1.4 版本，折叠 0.x 旧版本为摘要段落。确认 LICENSE 文件已存在。

Purpose: 当前 CHANGELOG.md 内容停留在 0.10.7 版本，缺少 v1.0、v1.2、v1.2.1、v1.3、v1.4 五个版本的全部变更记录。需按 Keep a Changelog 格式和用户决策 D-11 至 D-14 补全。LICENSE 已存在，仅需确认。
Output: 更新后的 CHANGELOG.md，保留的 LICENSE 文件
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>

@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- CHANGELOG and LICENSE are standalone files -- no code interfaces needed -->

Git tags for version mapping (per D-11):
- v1.0, v1.1 (functionality merged into v1.2 per D-11), v1.2, v1.2.1, v1.3, v1.4

Current crate version: 1.4.0
Crate name: dm-database-sqllog2db
Repository: https://github.com/guangl/sqllog2db
</interfaces>

</context>

<tasks>

<task type="auto">
  <name>Task 1: Add v1.0 through v1.4 entries to CHANGELOG.md</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - CHANGELOG.md (current file: 458 lines, ends at 0.10.7)
    - .planning/PROJECT.md (for context about what shipped in each version)
    - .planning/ROADMAP.md (phase descriptions for v1.0 Phase 2, v1.2 Phases 7-11, v1.3 Phases 12-16, v1.4 Phases 17-20)
  </read_first>
  <action>
    在 CHANGELOG.md 顶部（`## [0.10.7]` 之前）插入 v1.0 至 v1.4 版本条目。版本顺序按时间降序（最新的 v1.4 在最前）。

    每个版本条目的格式（per D-12 保持现有详细分类）：

    ```
    ## [1.4.0] - 2026-05-18

    ### Added
    - ...

    ### Changed
    - ...

    ### Fixed
    - ...

    ### Performance
    - ...

    ### Removed
    - ...
    ```

    具体变更内容从项目历史提取（用 `git log --oneline v1.3..v1.4` 查看提交记录）。根据文档和 ROADMAP.md 已知内容补充：

    **v1.4.0 - 2026-05-18** (from Phase 17-20):
    - Config restructuring: nested sub-table config model ([filter.include], [filter.exclude], [template], [charts] as top-level TOML sub-tables)
    - RawFiltersFeature intermediate struct for backward compatibility
    - Module restructuring: 5 large files split into focused modules
    - ExporterManager tightened to pub(crate)
    - DryRunExporter refactored to ExporterKind::DryRun struct variant
    - proptest property-based testing added
    - 933 tests, ~74% line coverage

    **v1.3.0 - 2026-05-17** (from Phase 12-16):
    - SQL template normalization engine (normalize_template)
    - TemplateAggregator streaming statistics accumulator (hdrhistogram-based, ~24KB/template)
    - Dual-stat output: CSV table + SQLite table via exporter
    - 4 SVG chart types: frequency bar, latency histogram, trend line, user pie (plotters SVG-only, no font/image deps)
    - Parallel CSV map-reduce merge() eliminating lock contention
    - 8 new config fields, TOML `[template]` and `[charts]` sections in config

    **v1.2.1 - 2026-05-15** (bugfix release):
    - 在 v1.2 后的小修复。运行 `git log --oneline v1.2..v1.2.1` 查看具体内容。

    **v1.2.0 - 2026-05-15** (from Phase 7-11):
    - Exclude filters (FILTER-03) integrated into CompiledMetaFilters
    - validate_and_compile() unified interface eliminating double Regex::new()
    - CLI startup optimization (binrw removal, parallel feature compilation)
    - Hot-path optimization (PERF-10 gate analysis, no blind optimization)
    - Nyquist audit remediation
    - DBUS-01/02/03 SQLite technical debt fixes
    - Cargo features removed: all functionality always compiled in
    - Perf data: CSV ~2.13M rec/s, SQLite ~1.11M rec/s

    **v1.1.0 tag exists but its functionality was merged into v1.2** (per D-11):
    - Do NOT create a separate v1.1.0 entry. Instead, mention in v1.2.0 entry that v1.1 profiling/benchmarking work laid the foundation for v1.2 performance gains.

    **v1.0.0 - 2026-04-18** (from Phase 1-2, per D-14 include migration note):
    ```
    ### Migration Note
    This is the first stable release. Upgrading from 0.x:
    - Configuration format changed from array [[exporter.*]] to single [exporter.*] sections
    - JSONL exporter removed; migrate to CSV or SQLite
    - Error logging changed from separate error log file to log::trace!() in app log
    - --set flag replaces direct config file edits for one-off overrides
    ```
    - Regular expression multi-field filtering (AND semantics + OR-veto exclude)
    - Field projection via ordered_indices Vec
    - FieldMask-based output field control
    - Initial CLI: init, validate, run, stats, digest, show-config, man, completions
    - 690+ tests

    使用 `git log --oneline` 查找每个版本的 tag 范围获取准确的 commit dates 和变更详情。

    每个版本底部的链接引用格式：
    ```
    [1.4.0]: https://github.com/guangl/sqllog2db/releases/tag/v1.4
    ```

    更新底部的版本链接列表，添加新的版本标签。
  </action>
  <verify>
    <automated>
      grep -c "^## \[1\.4\.0\]" CHANGELOG.md && 
      grep -c "^## \[1\.3\.0\]" CHANGELOG.md && 
      grep -c "^## \[1\.2\.0\]\|^## \[1\.2\.1\]" CHANGELOG.md && 
      grep -c "^## \[1\.0\.0\]" CHANGELOG.md &&
      grep -c "### Migration Note" CHANGELOG.md &&
      grep -c "\[1.4.0\]:.*releases/tag/v1.4" CHANGELOG.md
    </automated>
  </verify>
  <acceptance_criteria>
    - CHANGELOG.md contains `## [1.4.0] - 2026-05-18` entry at the top
    - CHANGELOG.md contains `## [1.3.0]` entry
    - CHANGELOG.md contains `## [1.2.1]` entry
    - CHANGELOG.md contains `## [1.2.0]` entry
    - CHANGELOG.md contains `## [1.0.0]` entry with "### Migration Note" section
    - No separate `## [1.1.0]` entry exists (per D-11)
    - Each version entry uses Added/Changed/Fixed/Performance/Removed section headers as applicable (per D-12)
    - Version links at bottom of file include [1.4.0], [1.3.0], [1.2.1], [1.2.0], [1.0.0]
    - Dates match actual release dates from git tags
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Fold 0.x versions into summary paragraph and verify LICENSE</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - CHANGELOG.md (state after Task 1, with v1.0-v1.4 entries added at top)
    - LICENSE (currently 201 lines, Apache-2.0)
  </read_first>
  <action>
    1. **折叠 0.x 版本**（per D-13）：
       - 找到 CHANGELOG.md 中 `## [0.10.7]` 到 `## [0.1.0]` 及以下所有旧版本条目
       - 移除每个 0.x 版本的详细 Added/Changed/Fixed/Removed 内容
       - 替换为一个摘要段落：
         ```
         ## [0.x] - 2025-11 to 2026-04

         The 0.x series (0.1.0 through 0.10.7) covered the initial development of sqllog2db:
         - Streaming SQL log parsing with multi-file support
         - Multi-exporter architecture (CSV, JSONL, SQLite, DuckDB, PostgreSQL, Oracle, DM)
         - Feature flags for conditional compilation ([csv], [jsonl], [sqlite], [filters])
         - CLI commands: init, validate, run, completions, man
         - GB18030 encoding support, replace_parameters SQL normalization
         - Error logging, progress bar, exit codes, graceful shutdown
         - Performance optimization: mmap + SIMD parser, itoa zero-alloc CSV formatting, batch writer, pipeline fast path
         - Architecture simplification: single-exporter mode, JSONL removal, Cargo features removal
         - See git history for full details.
         ```
       - 同时移除底部对应的 0.x 版本链接引用：删除 [0.10.7]、[0.10.5] 等所有 [0.x] 格式的行
       - 添加一个汇总的 [0.x] 链接引用指向 v0.10.7 tag
       - 确保 Keep a Changelog 格式的前言（第一段 `All notable changes...`）保留在最顶部
       - 确保 1.x 版本的链接引用保留完整

    2. **确认 LICENSE**（per D-06, DOC-06）：
       - `wc -l LICENSE` 确认存在且大于 190 行
       - `head -3 LICENSE` 确认包含 "Apache License" 和 "Version 2.0"
       - 无需修改 LICENSE 文件，仅确认

    3. **格式化检查**：
       - 运行 `cargo clippy --all-targets -- -D warnings` 确认无回归
       - 运行 `cargo test` 确认所有测试通过
       - CHANGELOG.md 不应有语法错误，确保 Markdown 格式正确
  </action>
  <verify>
    <automated>
      wc -l CHANGELOG.md &&
      grep -c "^## \[1\.4\.0\]\|^## \[1\.3\.0\]\|^## \[1\.2\.1\]\|^## \[1\.2\.0\]\|^## \[1\.0\.0\]" CHANGELOG.md &&
      grep -c "^## \[0\." CHANGELOG.md | awk '$1 == 0 {print "OK: no 0.x individual entries"; next} {print "FAIL: " $1 " individual 0.x entries remain"; exit 1}' &&
      grep -c "^## \[0\.x\]" CHANGELOG.md &&
      head -3 LICENSE | grep -c "Apache License\|Version 2.0" &&
      cargo clippy --all-targets -- -D warnings &&
      cargo test --quiet 2>&1 | tail -3
    </automated>
  </verify>
  <acceptance_criteria>
    - CHANGELOG.md has `## [0.x]` summary paragraph instead of individual 0.x entries
    - No individual `## [0.10.7]`, `## [0.10.5]`, etc. entries remain
    - No `[0.10.7]:` link references remain (replaced by single `[0.x]:` link)
    - Keep a Changelog header ("All notable changes...", format/semver links) preserved
    - v1.x entries intact and correctly formatted after 0.x folding
    - LICENSE exists at root, >= 190 lines, starts with Apache License 2.0 header
    - `cargo test` passes without regressions
    - `cargo clippy --all-targets -- -D warnings` passes
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan modifies CHANGELOG.md (documentation) and verifies LICENSE. No code changes, no network endpoints, no data processing.

| Boundary | Description |
|----------|-------------|
| None | Documentation-only changes; no new trust boundaries introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-03 | Tampering | CHANGELOG version links | accept | Links point to public GitHub releases; no sensitive data |
| T-21-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this documentation-only phase |
</threat_model>

<verification>
- CHANGELOG.md contains v1.0 through v1.4 entries in Keep a Changelog format
- 0.x entries folded into summary paragraph
- v1.0 entry includes migration note
- LICENSE file exists with Apache-2.0
- `cargo test` passes, `cargo clippy` passes
</verification>

<success_criteria>
1. CHANGELOG.md has entries for all 5 target versions: v1.0, v1.2, v1.2.1, v1.3, v1.4 (per D-11)
2. No v1.1.0 entry exists (functionality merged into v1.2, per D-11)
3. Each version entry has detailed Added/Changed/Fixed/Performance sections (per D-12)
4. 0.x versions (0.1.0-0.10.7) folded into single `## [0.x]` summary paragraph (per D-13)
5. v1.0 entry includes a "### Migration Note" explaining 0.x to 1.0 changes (per D-14)
6. Version links at bottom reference correct GitHub release tags
7. LICENSE file exists at repository root with Apache-2.0 text (201 lines)
8. `cargo test` passes, `cargo clippy --all-targets -- -D warnings` passes
</success_criteria>

<output>
Create `.planning/phases/21-readme/21-02-SUMMARY.md` when done
</output>
