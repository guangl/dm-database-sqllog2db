---
phase: 63-test-coverage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pipeline/filters/types.rs
autonomous: true
requirements:
  - TEST-01
  - TEST-02

must_haves:
  truths:
    - "运行 cargo llvm-cov 可生成 baseline HTML 报告（保存于 target/llvm-cov/html/）"
    - "src/pipeline/filters/serde_helpers.rs 中 vec_to_hashset 与 vec_to_i64_hashset 被新增测试间接调用（通过 FiltersFeature TOML 反序列化）"
    - "IncludeFilters/ExcludeFilters 的 ips/sessions/threads/apps/tags 分支均被 has_filters() 测试覆盖"
    - "FiltersFeature::from(RawFiltersFeature) 的旧格式扁平字段映射路径与混合格式路径均被新增测试覆盖"
  artifacts:
    - path: "src/pipeline/filters/types.rs"
      provides: "新增 mod tests 块（在文件末尾），通过 FilterWrapper 间接覆盖 serde_helpers"
      contains: "mod tests"
      min_new_tests: 10
  key_links:
    - from: "src/pipeline/filters/types.rs (mod tests)"
      to: "src/pipeline/filters/serde_helpers.rs"
      via: "toml::from_str::<FilterWrapper> 触发 vec_to_hashset / vec_to_i64_hashset deserialize_with"
      pattern: "vec_to_hashset|vec_to_i64_hashset"
    - from: "src/pipeline/filters/types.rs (mod tests)"
      to: "src/pipeline/filters/types.rs:146 (FiltersFeature Deserialize)"
      via: "RawFiltersFeature 中转 + From<RawFiltersFeature> 路径"
      pattern: "impl From<RawFiltersFeature>"
---

<objective>
建立 Phase 63 的覆盖率基线，并为 pipeline/filters 模块补充 TOML 反序列化与 has_filters 分支测试。

Purpose: 解决 RESEARCH.md 识别的两个零覆盖/低函数覆盖区域 —— `pipeline/filters/serde_helpers.rs`（0% 行/0% 函数）与 `pipeline/filters/types.rs`（82.21% 行/31.58% 函数）。这两个文件承载所有过滤器 TOML 反序列化与旧格式向后兼容逻辑，是配置解析关键路径。
Output: `cargo llvm-cov --html` baseline 报告 + `src/pipeline/filters/types.rs` 末尾新增 `#[cfg(test)] mod tests` 块，包含至少 10 个测试函数。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/63-test-coverage/63-CONTEXT.md
@.planning/phases/63-test-coverage/63-RESEARCH.md
@.planning/phases/63-test-coverage/63-PATTERNS.md

<interfaces>
<!-- 关键签名（取自现有代码），执行器无需再翻源码 -->

From src/pipeline/filters/serde_helpers.rs:
- `pub(crate) type TrxidSet = std::collections::HashSet<String>;`
- `pub(super) fn vec_to_hashset<'de, D>(d: D) -> Result<Option<TrxidSet>, D::Error>`
- `pub(super) fn vec_to_i64_hashset<'de, D>(d: D) -> Result<Option<HashSet<i64>>, D::Error>`

From src/pipeline/filters/types.rs:
- `pub struct IncludeFilters { users, ips, sessions, threads, statements, apps, tags, start_ts, end_ts, trxids }`
  全部字段 `Option<Vec<String>>` 或 `Option<String>`，`trxids: Option<TrxidSet>`
- `pub struct ExcludeFilters { users, ips, sessions, threads, statements, apps, tags }` 全部 `Option<Vec<String>>`
- `pub struct FiltersFeature { enable: bool, include: IncludeFilters, exclude: ExcludeFilters, indicators: IndicatorFilters, sql: SqlFilters }`
- `pub struct IndicatorFilters { exec_ids: Option<HashSet<i64>>, min_runtime_ms: Option<u32>, min_row_count: Option<u32> }`
- `impl<'de> Deserialize<'de> for FiltersFeature` 通过 `RawFiltersFeature` 中转
- `RawFiltersFeature` 同时接受新格式子表（include/exclude）与旧格式扁平字段（usernames/client_ips/sess_ids/thrd_ids/statements/appnames/tags/start_ts/end_ts/trxids/exclude_*）

RawFiltersFeature 旧格式 → IncludeFilters 字段映射（types.rs:191-203）：
- usernames → users, client_ips → ips, sess_ids → sessions, thrd_ids → threads,
- statements → statements, appnames → apps, tags → tags, start_ts → start_ts, end_ts → end_ts, trxids → trxids
- exclude_* 同理映射到 ExcludeFilters

混合格式（types.rs:167-173, 183-189）：当 `[filter.include]` 子表与旧格式扁平字段共存时，子表优先且发出 `log::warn!`；exclude 同理。
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 运行并保存 baseline 覆盖率报告</name>
  <files>无文件修改；仅生成 target/llvm-cov/html/ 报告与 baseline 文本摘要</files>
  <read_first>
    - .planning/phases/63-test-coverage/63-RESEARCH.md（包含已运行的 baseline 数字：整体 90.63% 行 / 86.05% 函数；6 个低覆盖文件清单）
    - .planning/phases/63-test-coverage/63-CONTEXT.md（D-01：cargo-llvm-cov 0.8.5 已安装，使用 `cargo llvm-cov --html`）
  </read_first>
  <action>
    执行 `cargo llvm-cov --html`（D-01）以生成本地 HTML 报告至 `target/llvm-cov/html/index.html`。
    同时执行 `cargo llvm-cov --summary-only 2>&1 | tee target/llvm-cov/baseline-summary.txt`（不在 git 中提交此文件，仅用于 Wave 2 对比）。
    若 `cargo llvm-cov` 命令报告 `target/llvm-cov` 已存在且与现有数据冲突，先执行 `cargo llvm-cov clean --workspace` 再重新运行。
    不修改任何源文件；本任务仅是数据收集与报告确认。
    在任务完成笔记中记录 baseline 整体 TOTAL 行/函数百分比，并列出实际报告中行覆盖率 < 80% 的所有文件（与 RESEARCH.md 已记录数字交叉确认）。
  </action>
  <verify>
    <automated>test -f target/llvm-cov/html/index.html &amp;&amp; test -f target/llvm-cov/baseline-summary.txt &amp;&amp; grep -E "TOTAL" target/llvm-cov/baseline-summary.txt</automated>
  </verify>
  <acceptance_criteria>
    - 文件 `target/llvm-cov/html/index.html` 存在
    - 文件 `target/llvm-cov/baseline-summary.txt` 存在且包含 `TOTAL` 行
    - baseline-summary.txt 中 TOTAL 行的行覆盖率数字（百分比形式）记录在任务完成笔记中，用于 Wave 2 对比
    - 任务完成笔记列出 baseline 中行覆盖率 < 80% 的实际文件清单（最少 3 个，至少包含 `pipeline/filters/serde_helpers.rs`、`exporter/csv/writer.rs`、`exporter/sqlite/mod.rs` 之一）
  </acceptance_criteria>
  <done>baseline HTML 与 summary 文本均生成；实际低覆盖文件清单确认与 RESEARCH.md 一致或更新 SUMMARY 记录差异。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: src/pipeline/filters/types.rs 末尾新增 mod tests，覆盖 serde_helpers + FiltersFeature::from + has_filters 分支</name>
  <files>src/pipeline/filters/types.rs</files>
  <read_first>
    - src/pipeline/filters/types.rs（全文，确认 IncludeFilters/ExcludeFilters/FiltersFeature/RawFiltersFeature/From 实现的当前形态，新增 mod tests 放在文件末尾）
    - src/pipeline/filters/serde_helpers.rs（确认 vec_to_hashset/vec_to_i64_hashset 签名）
    - src/pipeline/filters/mod.rs:93-233（参照 mod tests 结构与命名风格）
    - .planning/phases/63-test-coverage/63-PATTERNS.md §1（FilterWrapper 模式 + 旧格式映射模式 + has_filters 分支模式）
    - .planning/phases/63-test-coverage/63-RESEARCH.md §"详细低覆盖区域分析" 区域 A 与 D
  </read_first>
  <behavior>
    Test 1: `test_trxids_deserialized_to_hashset` — TOML `[filter] enable=true trxids=["TX001","TX002"]` 经 `FilterWrapper` 反序列化后，`include.trxids.unwrap()` 包含 "TX001" 与 "TX002"，len == 2（覆盖 serde_helpers::vec_to_hashset Some 分支）。
    Test 2: `test_trxids_absent_returns_none` — TOML `[filter] enable=true`（无 trxids 字段）反序列化后 `include.trxids` 为 `None`（覆盖 serde_helpers::vec_to_hashset None 分支）。
    Test 3: `test_exec_ids_deserialized_to_hashset` — TOML `[filter.indicators] exec_ids=[1,2,42]` 反序列化后 `indicators.exec_ids.unwrap()` 含 1、2、42，len == 3（覆盖 serde_helpers::vec_to_i64_hashset Some 分支）。
    Test 4: `test_exec_ids_absent_returns_none` — TOML 不含 exec_ids 时 `indicators.exec_ids` 为 `None`（覆盖 vec_to_i64_hashset None 分支）。
    Test 5: `test_legacy_flat_usernames_mapped_to_include_users` — TOML `[filter] enable=true usernames=["ALICE","BOB"]`（无 `[filter.include]` 子表）反序列化后 `include.users == Some(vec!["ALICE","BOB"])`（覆盖 FiltersFeature::from 中 raw.include.unwrap_or(...) 的 unwrap_or 分支 + usernames→users 映射）。
    Test 6: `test_legacy_flat_exclude_usernames_mapped` — TOML 旧格式 `exclude_usernames=["X"]` 反序列化后 `exclude.users == Some(vec!["X"])`（覆盖 exclude.unwrap_or 分支）。
    Test 7: `test_legacy_flat_all_include_fields_mapped` — TOML 同时包含旧格式 `client_ips=["10.0.0.1"]`、`sess_ids=["S1"]`、`thrd_ids=["T1"]`、`statements=["ST1"]`、`appnames=["A1"]`、`tags=["TG1"]`、`start_ts="2024-01-01"`、`end_ts="2024-12-31"`，断言 include 中对应字段均被正确映射（覆盖 types.rs:192-202 全部旧格式映射）。
    Test 8: `test_mixed_format_new_table_takes_priority` — TOML 同时含 `usernames=["IGNORED"]` 与 `[filter.include] users=["WINNER"]` 两段，断言 `include.users == Some(vec!["WINNER"])`（覆盖 types.rs:167-173 mixed-format 警告分支 + 新格式优先逻辑）。
    Test 9: `test_mixed_format_exclude_new_table_priority` — 类似 Test 8，但针对 exclude（覆盖 types.rs:183-189 exclude 混合格式分支）。
    Test 10: `test_include_filters_has_filters_with_ips` — 构造 `IncludeFilters { ips: Some(vec!["10.0.0.1".into()]), ..Default::default() }`，断言 `has_filters() == true`（覆盖 types.rs:35 ips 分支）。
    Test 11: `test_include_filters_has_filters_with_sessions` — 同上但 sessions 字段（覆盖 types.rs:36）。
    Test 12: `test_include_filters_has_filters_with_threads_apps_tags` — 在同一 IncludeFilters 中设置 threads/apps/tags 三字段任一非空，断言 `has_filters() == true`（覆盖 types.rs:37/38/39 三条分支；可拆为 3 个测试）。
    Test 13: `test_exclude_filters_has_filters_with_ips_sessions_threads_apps_tags` — 对 ExcludeFilters 分别测试 ips/sessions/threads/apps/tags 五个字段任一非空，has_filters() 返回 true（覆盖 types.rs:69-74 exclude 五条 has_filters 分支；可合并为参数化或拆为 5 个测试）。
  </behavior>
  <action>
    在 `src/pipeline/filters/types.rs` 文件末尾新增 `#[cfg(test)] mod tests { ... }` 块。
    在 mod tests 内：
    1. `use super::*;`
    2. 定义私有 wrapper struct：`#[derive(serde::Deserialize)] struct FilterWrapper { filter: FiltersFeature }`，用于绕过 FiltersFeature 自定义 Deserialize 的顶层 TOML 节包装要求（参照 PATTERNS.md §"共享模式 C" 与 RESEARCH.md "Pitfall 2"）。
    3. 按上方 `<behavior>` 列出的 11–13 个测试函数实现（具体行为已在 behavior 段定义）。命名前缀统一使用 `test_`，并按业务分组（trxids/exec_ids/legacy/mixed/has_filters）。
    4. 所有 TOML 字符串使用 raw string literal `r#"..."#"` 多行格式（参照 PATTERNS.md §1 中 `[filter] enable = true ...` 示例）。
    5. 不修改 IncludeFilters/ExcludeFilters/FiltersFeature/RawFiltersFeature 等任何生产结构体（D-04：仅添加测试，不动生产代码）。
    6. 不允许任何 `#[allow(...)]` 屏蔽 clippy 警告；所有断言使用 `assert!(...)` 或 `assert_eq!(...)`，断言消息包含被测字段名便于调试（参照现有 src/error.rs mod tests 风格）。
    新测试总数应 ≥ 10。Test 12 与 Test 13 若拆分则总数会更多，按拆分计算。
  </action>
  <verify>
    <automated>cargo test --lib pipeline::filters::types::tests 2>&amp;1 | grep -E "test result.*ok.*passed" | grep -v "0 passed"</automated>
  </verify>
  <acceptance_criteria>
    - 命令 `cargo test --lib pipeline::filters::types::tests` 退出码 0，"passed" 数量 ≥ 10
    - `grep -c "fn test_" src/pipeline/filters/types.rs` 返回值 ≥ 10
    - `cargo clippy --all-targets -- -D warnings` 通过，无新增警告
    - `cargo fmt --check` 通过
    - `grep -c "FilterWrapper" src/pipeline/filters/types.rs` ≥ 2（结构体定义至少出现一次，且至少一个测试使用）
    - `grep -c "toml::from_str" src/pipeline/filters/types.rs` ≥ 5（多个测试通过 TOML 反序列化触发 serde_helpers 路径）
    - 文件原有非测试内容（IncludeFilters/ExcludeFilters/FiltersFeature/RawFiltersFeature 与 impl 块）保持不变：`git diff src/pipeline/filters/types.rs` 显示新增内容仅位于文件末尾新 mod tests 块内（除测试块外无其他改动）
  </acceptance_criteria>
  <done>新增 mod tests 块至少 10 个测试函数全部通过；cargo clippy/test/fmt 三道质量门禁全绿；生产代码无修改。</done>
</task>

</tasks>

<verification>
1. `cargo test --lib pipeline::filters` — 所有 filters 单元测试通过（含 mod.rs 现有 17+ 测试与本次新增 10+ 测试）
2. `cargo clippy --all-targets -- -D warnings` — 无 warning，无新增 clippy 提示
3. `cargo fmt --check` — 格式无偏差
4. baseline 报告 `target/llvm-cov/html/index.html` 存在；`target/llvm-cov/baseline-summary.txt` 包含 TOTAL 行
</verification>

<success_criteria>
- baseline 覆盖率报告生成完成且整体覆盖率数字已记录
- `src/pipeline/filters/types.rs` 末尾新增 mod tests 块，至少 10 个测试函数
- 通过 FilterWrapper + toml::from_str 间接覆盖 serde_helpers.rs 的 vec_to_hashset 与 vec_to_i64_hashset（Some 与 None 两条分支均覆盖）
- 覆盖 FiltersFeature::from 的旧格式映射（usernames/client_ips/sess_ids/thrd_ids/statements/appnames/tags/start_ts/end_ts/exclude_*）与混合格式优先级分支
- 覆盖 IncludeFilters/ExcludeFilters has_filters() 中 ips/sessions/threads/apps/tags 字段分支
- 三道质量门禁（test/clippy/fmt）全绿
- 生产代码（types.rs 中 mod tests 之外的内容、serde_helpers.rs 整体）无任何修改
</success_criteria>

<output>
Create `.planning/phases/63-test-coverage/63-01-SUMMARY.md` when done. SUMMARY 必须记录：
- baseline TOTAL 行/函数覆盖率数字
- 本计划新增的测试函数数量与命名清单
- baseline 中行覆盖率 < 80% 的文件清单（供 Wave 2 验证回归提升）
</output>
