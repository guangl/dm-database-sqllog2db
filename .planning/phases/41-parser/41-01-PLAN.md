---
phase: 41-parser
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - Cargo.lock
autonomous: true
requirements:
  - REFACTOR-02
  - PARSER-01
tags:
  - rust
  - dependency-upgrade
  - parser

must_haves:
  truths:
    - "Cargo.toml 中 dm-database-parser-sqllog 版本号为 2.0.0"
    - "Cargo.lock 中 dm-database-parser-sqllog 解析为 2.0.0 (高于基线 1.1.0)"
    - "cargo build --release 编译成功且无任何 warning: 行 (包括 deprecated)"
    - "cargo test 全部通过,无任何测试回归"
    - "cargo clippy --all-targets -- -D warnings 通过,无新增 clippy 问题"
    - "其他可升级依赖 (autocfg/bumpalo/either/js-sys/serde_json/wasm-bindgen 系列/web-sys) 已通过 cargo update 同步到最新兼容版本"
    - "criterion 保持在 0.7.x (受 rust-version=1.85 约束,未被错误升级到 0.8.x)"
  artifacts:
    - path: "Cargo.toml"
      provides: "声明 dm-database-parser-sqllog = \"2.0.0\""
      contains: "dm-database-parser-sqllog = \"2.0.0\""
    - path: "Cargo.lock"
      provides: "Cargo.lock 锁定 dm-database-parser-sqllog 到 2.0.0 及更新后的间接依赖"
      contains: "name = \"dm-database-parser-sqllog\""
  key_links:
    - from: "Cargo.toml"
      to: "Cargo.lock"
      via: "cargo update / cargo build"
      pattern: "dm-database-parser-sqllog"
    - from: "src/cli/run/processor.rs"
      to: "dm-database-parser-sqllog 2.0.0"
      via: "use dm_database_parser_sqllog::LogParserBuilder"
      pattern: "LogParserBuilder::new"
    - from: "src/cli/run/prescan.rs"
      to: "dm-database-parser-sqllog 2.0.0"
      via: "use dm_database_parser_sqllog::LogParserBuilder"
      pattern: "LogParserBuilder::new"
    - from: "src/pipeline/mod.rs"
      to: "dm-database-parser-sqllog 2.0.0"
      via: "use dm_database_parser_sqllog::Sqllog"
      pattern: "dm_database_parser_sqllog::Sqllog"
---

<objective>
将 `dm-database-parser-sqllog` 从 1.1.0 升级到 2.0.0(major 版本,但研究确认公共 API 完全向后兼容,零代码改动),并通过 `cargo update` 同步升级其他可升级的 patch/minor 间接依赖。完成后整个项目必须满足 release 编译无任何 warning、`cargo test` 全部通过、`cargo clippy --all-targets -- -D warnings` 通过这三道质量门禁。

Purpose: 落地 v1.11 milestone 的依赖适配基线(REFACTOR-02 + PARSER-01),为 Phase 42-45 的基准测试、新 API 适配、性能优化提供干净稳定的依赖环境。

Output:
- `Cargo.toml` 中 `dm-database-parser-sqllog` 版本声明升级到 `"2.0.0"`
- `Cargo.lock` 中 `dm-database-parser-sqllog` 解析到 2.0.0,其他可升级间接依赖同步升级
- 通过 `41-01-SUMMARY.md` 记录升级前后版本对比、质量门禁运行结果、`cargo tree` 依赖变化摘要
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/41-parser/41-CONTEXT.md
@.planning/phases/41-parser/41-RESEARCH.md
@CLAUDE.md
@Cargo.toml

<interfaces>
<!-- 2.0.0 与 1.1.0 公共 API 完全一致,以下接口签名在升级后保持不变(来自 RESEARCH.md §Architecture Patterns 的全量 grep 扫描结果),执行者无需做任何代码修改。 -->
<!-- Sqllog 结构体的 14 个公共字段名 (ts/tag/ep/sess_id/thrd_id/username/trxid/statement/appname/client_ip/sql/exectime/rowcount/exec_id) 与字段类型 2.0.0 = 1.1.0,字段访问代码无需修改。 -->

dm-database-parser-sqllog 2.0.0 公共 API (与 1.1.0 兼容):
- `LogParserBuilder::new(path: impl AsRef<Path>) -> LogParserBuilder` — 构造器入口
- `LogParserBuilder::build() -> Result<LogParser, ParseError>` — 构建解析器
- `LogParser::iter() -> LogIterator` — 同步迭代器,产出 `Result<Sqllog, ParseError>`
- `Sqllog` 结构体字段 (公共): ts, tag, ep, sess_id, thrd_id, username, trxid, statement, appname, client_ip, sql, exectime, rowcount, exec_id

调用点(无需修改,仅作 grep 验证目标):
- `src/cli/run/processor.rs:5` — use 语句
- `src/cli/run/processor.rs:53` — `LogParserBuilder::new(file_path).build()`
- `src/cli/run/prescan.rs:4` — use 语句
- `src/cli/run/prescan.rs:16` — `LogParserBuilder::new(file_path).build()`
- `src/pipeline/mod.rs:8` — `use dm_database_parser_sqllog::Sqllog`
- `src/pipeline/mod.rs:311` / `:332` — pipeline 测试中的 parser 使用
- `src/pipeline/normalizer.rs:340` — `&dm_database_parser_sqllog::Sqllog` 参数
- `src/exporter/mod.rs:3` — `use dm_database_parser_sqllog::Sqllog`
- `src/exporter/sqlite/mod.rs:2` — Sqllog 引用
- `src/exporter/sqlite/write.rs:3` — Sqllog 引用
- `src/exporter/sqlite/tests.rs` — 9 处 LogParserBuilder 调用
- `src/exporter/csv/tests.rs` — 2 处 LogParserBuilder 调用
- `src/cli/run/filter_processor.rs:49, :57` — Sqllog 引用

升级约束(来自 RESEARCH.md):
- criterion 必须保持 0.7.x;0.8.x 要求 Rust 1.86,与 rust-version="1.85" 冲突
- wasip2/wasip3 锁定版本不动 (需 Rust 1.87)
- cargo update 仅升级 ^semver 兼容范围,major 升级必须先改 Cargo.toml
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 升级 dm-database-parser-sqllog 到 2.0.0 并同步 cargo update</name>
  <files>Cargo.toml, Cargo.lock</files>
  <read_first>
    - Cargo.toml (确认当前 dm-database-parser-sqllog = "1.1.0" 声明,以及完整依赖列表)
    - Cargo.lock (确认 v1.10 基线版本,后续 diff 对比)
    - .planning/phases/41-parser/41-CONTEXT.md (锁定决策 D-01..D-05)
    - .planning/phases/41-parser/41-RESEARCH.md §Standard Stack + §Common Pitfalls (升级矩阵 + criterion 0.7 锁定约束)
  </read_first>
  <action>
    实施 D-01/D-03 的依赖升级流程,操作步骤如下:

    1. 修改 Cargo.toml 第 34 行,将 `dm-database-parser-sqllog = "1.1.0"` 改为 `dm-database-parser-sqllog = "2.0.0"`。仅改这一行,不要触碰任何其他依赖声明(criterion 必须保持 "0.7",其他都让 cargo update 自动处理)。
    2. 运行 `cargo update`,允许其升级所有 ^semver 兼容的 patch/minor 间接依赖(参考 RESEARCH §Standard Stack 表格列出的 autocfg/bumpalo/either/js-sys/serde_json/wasm-bindgen 系列/web-sys 等条目)。
    3. 运行 `cargo build --release 2>&1 | tee /tmp/phase41-build.log`,确认输出中既无 `^error:` 行,也无 `^warning:` 行(包括 deprecated 警告)。这对应 D-04 的硬性门禁。
    4. 运行 `cargo test` 全套通过(参考 REFACTOR-02 验收命令),如有任何失败必须停下修复后再继续。
    5. 运行 `cargo clippy --all-targets -- -D warnings`,确认无任何 clippy 警告(参考 D-04)。
    6. 运行 `cargo fmt --check`,确保格式无漂移(沿用 v1.10 质量门禁标准)。
    7. 运行 `cargo tree -p dm-database-parser-sqllog --depth 1` 记录 2.0.0 的传递依赖树,与 RESEARCH 中提到的 1.1.0 基线 (atoi/encoding/memchr/thiserror) 做对比,供 SUMMARY 引用(对应 RESEARCH §Open Questions Q1)。

    注意事项:
    - 不要使用 FilterBuilder、AsyncLogParser、from_reader 等 2.0.0 新增 API。这些是 Phase 43 的工作(D-02)。
    - 不要修改 src/ 下任何 Rust 代码。RESEARCH 明确确认零代码改动即可编译通过(per D-02)。
    - 不要清理 prescan.rs 中关于 "v1.1.0 的 LogParser 不再实现 rayon 的 IntoParallelRefIterator" 的注释 — Task 2 单独处理注释审查,本任务专注于依赖与编译门禁。
    - 若 cargo build 报出意料外的 warning(违反 A1 假设),立即停止并将完整警告列表追加到 SUMMARY 的 Known Issues 段,然后做 D-claude-discretion 允许的最小化适配。
  </action>
  <verify>
    <automated>grep -q '^dm-database-parser-sqllog = "2.0.0"$' Cargo.toml &amp;&amp; grep -A1 '^name = "dm-database-parser-sqllog"$' Cargo.lock | grep -q '^version = "2.0.0"$' &amp;&amp; cargo build --release 2>&amp;1 | tee /tmp/phase41-build.log &amp;&amp; ! grep -E '^(warning|error):' /tmp/phase41-build.log &amp;&amp; cargo test &amp;&amp; cargo clippy --all-targets -- -D warnings &amp;&amp; cargo fmt --check</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: `Cargo.toml` 含字面行 `dm-database-parser-sqllog = "2.0.0"` (grep 精确匹配通过)
    - SOURCE: `Cargo.lock` 中 `[[package]] name = "dm-database-parser-sqllog"` 紧跟的 `version` 字段为 `"2.0.0"`
    - SOURCE: `Cargo.toml` 中 `criterion` 仍然是 `version = "0.7"`,未被错误升级 (grep 精确匹配 `criterion = { version = "0.7"`)
    - CLI: `cargo build --release` 退出码为 0,且 stderr+stdout 合并后过滤注释行 (`grep -v '^#'`) 之后 `grep -cE '^(warning|error):'` 必须为 0
    - CLI: `cargo test` 退出码为 0,全部测试 PASS,无 ignored 之外的 skipped
    - CLI: `cargo clippy --all-targets -- -D warnings` 退出码为 0
    - CLI: `cargo fmt --check` 退出码为 0
    - BEHAVIOR: `cargo tree -p dm-database-parser-sqllog --depth 1` 输出版本号 2.0.0
    - SOURCE: `src/` 目录下任何 .rs 文件相对 main 分支无变更(`git diff main -- src/ | wc -l` 为 0),验证 D-02 "无深度 API 重构" 约束
  </acceptance_criteria>
  <done>
    Cargo.toml 升级到 2.0.0、Cargo.lock 同步、release 编译零警告零错误、cargo test 全绿、clippy/fmt 门禁通过、src/ 下零 Rust 代码改动。三道质量门禁的完整命令输出可在 SUMMARY 中引用以证明 D-04 满足。
  </done>
</task>

<task type="auto">
  <name>Task 2: 审查并清理与 1.1.0 强关联的过时注释 (最小化范围)</name>
  <files>src/cli/run/prescan.rs</files>
  <read_first>
    - src/cli/run/prescan.rs (确认当前关于 v1.1.0 的注释位置和措辞)
    - .planning/phases/41-parser/41-CONTEXT.md §Claude's Discretion (允许最小化适配)
    - .planning/phases/41-parser/41-RESEARCH.md §State of the Art (注释仍然成立,可保留或简化)
  </read_first>
  <action>
    审查 `src/cli/run/prescan.rs` 中现存的关于 `dm-database-parser-sqllog` v1.1.0 行为的注释。

    具体动作:
    1. 用 `grep -n "v1.1.0\|IntoParallelRefIterator\|rayon" src/cli/run/prescan.rs` 定位所有提到旧版本号或 rayon 变通逻辑的注释。
    2. 对每条命中的注释判断:
       - 若注释固定提到 `v1.1.0` 字面值并解释为何要 collect 到 Vec 再 par_iter(根据 RESEARCH §State of the Art,2.0.0 仍然如此,事实未变),将 `v1.1.0` 字面值改写为更通用的措辞,例如把 "v1.1.0 的 LogParser 不再实现 rayon 的 IntoParallelRefIterator" 改为 "LogParser 未实现 rayon 的 IntoParallelRefIterator(需先 collect 到 Vec 再并行)"。理由:版本号锁定到 1.1.0 已不准确(当前已是 2.0.0),但 WHY 仍然有效需要保留。
       - 若注释解释的现象在 2.0.0 已不再适用(检测方式:阅读 2.0.0 changelog 后判断),整段删除。
       - 若注释与版本号无关,保持原样不动。
    3. 不要扩大改动范围:不重命名变量、不调整函数签名、不重构逻辑、不引入新 API。仅按上述规则改注释文本(per D-02)。
    4. 运行 `cargo build --release` + `cargo test` + `cargo clippy --all-targets -- -D warnings`,确认注释改动未引入任何回归(注释不会影响编译,但仍要跑一遍门禁作为防御)。

    特别约束:
    - 不要触碰 src/ 下任何其他文件 — Task 1 已确认零代码改动即可工作。
    - 若 prescan.rs 中没有任何匹配项,本任务输出 "no comment cleanup needed",依然通过验收(per D-02 "记录待深度重构的 TODO 供 Phase 43 参考",由 Phase 43 处理更深度的注释整理)。
  </action>
  <verify>
    <automated>! grep -nE '\bv1\.1\.0\b' src/cli/run/prescan.rs &amp;&amp; cargo build --release 2>&amp;1 | { ! grep -E '^(warning|error):'; } &amp;&amp; cargo test &amp;&amp; cargo clippy --all-targets -- -D warnings &amp;&amp; cargo fmt --check</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: `src/cli/run/prescan.rs` 中不再出现字面字符串 `v1.1.0` (grep -nE '\bv1\.1\.0\b' 返回退出码 1 / 无匹配)
    - SOURCE: `src/cli/run/prescan.rs` 中函数签名、`use` 语句、`LogParserBuilder::new(file_path).build()` 调用相对 Task 1 完成时保持完全一致(可通过 `git diff` 确认仅注释行变化)
    - BEHAVIOR: 该文件改动仅限于以 `//` 开头的行(comment-only diff),命令 `git diff src/cli/run/prescan.rs | grep -E '^[+-]' | grep -vE '^[+-]\s*//' | grep -vE '^(\+\+\+|---)' | wc -l` 输出为 0
    - CLI: `cargo build --release` 编译成功且 `grep -cE '^(warning|error):'` 为 0
    - CLI: `cargo test` 退出码 0
    - CLI: `cargo clippy --all-targets -- -D warnings` 退出码 0
    - CLI: `cargo fmt --check` 退出码 0
  </acceptance_criteria>
  <done>
    prescan.rs 中过时的 `v1.1.0` 版本号引用已被清理或改写,质量门禁三件套(build/test/clippy)全部通过,改动严格限制在注释范围内(comment-only diff)。如果文件中本就没有 `v1.1.0` 注释,任务以 "no cleanup needed" 结束并仍然通过门禁。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| crates.io → Cargo build cache | cargo update / cargo build 拉取新版本包,潜在供应链风险面 |
| Cargo.toml/Cargo.lock → 编译产物 | 依赖图变化影响二进制行为 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-01 | Tampering | crates.io 拉取 dm-database-parser-sqllog 2.0.0 | mitigate | 该包为本项目作者(github.com/guangl)自己发布,且 RESEARCH §Package Legitimacy Audit 已验证(cargo search + crates.io 来源核对);Cargo.lock 锁定 hash 防止后续拉取漂移 |
| T-41-02 | Tampering | cargo update 升级 10 个间接依赖 | accept | 全部为 autocfg/bumpalo/either/js-sys/serde_json/wasm-bindgen/web-sys 等知名 crate 的 patch/minor 升级,SemVer 兼容;由 Cargo.lock 锁定具体版本 |
| T-41-03 | Denial of Service | criterion 错误升级到 0.8.x 导致 Rust 1.85 项目无法编译 | mitigate | Task 1 显式约束不动 criterion;acceptance_criteria 包含 grep `criterion = { version = "0.7"` 的精确匹配验证 |
| T-41-04 | Information Disclosure | parser 2.0.0 新加 AsyncLogParser/FilterBuilder 是否引入新非可选传递依赖 | mitigate | Task 1 步骤 7 显式运行 `cargo tree -p dm-database-parser-sqllog --depth 1` 记录依赖树变化供 SUMMARY 审计 |
| T-41-SC | Tampering | npm/pip/cargo installs (Package Legitimacy Gate) | mitigate | 仅升级 1 个直接依赖 (dm-database-parser-sqllog),已在 RESEARCH §Package Legitimacy Audit 标注为 Approved (作者自有库,无 [ASSUMED]/[SUS]/[SLOP] 标记);其他升级均为 cargo update 自动处理的 patch/minor 间接依赖,无新增直接依赖,无需 blocking-human checkpoint |
</threat_model>

<verification>
## Phase-Level Checks

执行完两个任务后,在 SUMMARY 中以表格形式列出:

1. **依赖版本对比表**
   - 升级前 vs 升级后 (源自 Cargo.lock diff)
   - 至少应展示: dm-database-parser-sqllog, autocfg, bumpalo, either, js-sys, serde_json, wasm-bindgen 系列, web-sys
   - 标注 criterion 保持 0.7 不动

2. **质量门禁运行结果**
   - `cargo build --release` 完整输出(确认无 warning/error 行)
   - `cargo test` 通过测试数 / 总数
   - `cargo clippy --all-targets -- -D warnings` 通过
   - `cargo fmt --check` 通过

3. **代码改动统计**
   - `git diff --stat main -- src/ Cargo.toml Cargo.lock` 输出,确认 src/ 下仅 prescan.rs 注释行有变化(或无变化)

4. **依赖树变化摘要**
   - `cargo tree -p dm-database-parser-sqllog --depth 1` 输出对比 RESEARCH 中提到的 1.1.0 依赖 (atoi/encoding/memchr/thiserror)
</verification>

<success_criteria>
- [ ] Cargo.toml 中 `dm-database-parser-sqllog = "2.0.0"` (源断言: grep 精确匹配)
- [ ] Cargo.lock 中 `dm-database-parser-sqllog` 解析为 2.0.0 (源断言: grep 精确匹配)
- [ ] Cargo.toml 中 `criterion` 保持 0.7.x (源断言: grep 精确匹配)
- [ ] `cargo build --release` 退出 0 且零 warning/error 行 (CLI 断言)
- [ ] `cargo test` 全部通过 (CLI 断言)
- [ ] `cargo clippy --all-targets -- -D warnings` 退出 0 (CLI 断言)
- [ ] `cargo fmt --check` 退出 0 (CLI 断言)
- [ ] `src/` 下 .rs 文件改动 ≤ 注释级别 (Task 2 完成后,git diff 仅显示 prescan.rs 注释行变化或无变化)
- [ ] SUMMARY 中含有依赖版本对比表 + 质量门禁结果 + 依赖树变化摘要
</success_criteria>

<output>
Create `.planning/phases/41-parser/41-01-SUMMARY.md` when done,包含:
- Frontmatter (phase=41-parser, plan=01, requirements=[REFACTOR-02, PARSER-01], affects=[Cargo.toml, Cargo.lock, src/cli/run/prescan.rs])
- 依赖版本对比表
- 质量门禁运行结果(cargo build/test/clippy/fmt 完整输出片段)
- `cargo tree -p dm-database-parser-sqllog --depth 1` 输出
- Known Issues (若有任何意料外的 warning 或 cargo update 拒绝升级的依赖)
- 给 Phase 43 的 Notes (例如可在哪些位置利用 FilterBuilder / from_reader 删除冗余代码)
</output>
