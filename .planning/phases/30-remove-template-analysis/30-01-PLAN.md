---
phase: 30-remove-template-analysis
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pipeline/mod.rs
  - src/config/mod.rs
  - src/config/apply_one.rs
  - src/config/validate.rs
  - src/cli/init.rs
  - src/cli/show_config.rs
  - Cargo.toml
autonomous: true
requirements: [RM-05]
user_setup: []

must_haves:
  truths:
    - "TemplateConfig 和 TemplateReportConfig 结构体从 pipeline/mod.rs 中移除"
    - "Config 结构体中不再包含 template 字段"
    - "apply_one() 不再处理 template.* 覆盖键"
    - "validate_charts() 不再引用 template 字段"
    - "init 生成的模板不再包含 [template] 段"
    - "show_config 不再显示 [template] 配置段"
    - "Cargo.toml 不再包含 hdrhistogram 依赖"
    - "pipeline/mod.rs 不再声明 aggregator 和 template_reporter 模块"
  artifacts:
    - path: src/pipeline/mod.rs (MUST NOT contain "pub mod aggregator" or "TemplateConfig" or "template_report_enabled")
    - path: src/config/mod.rs (MUST NOT contain "TemplateConfig" or "pub template:")
    - path: src/config/apply_one.rs (MUST NOT contain "template." match arms)
    - path: src/config/validate.rs (MUST NOT contain "TemplateConfig" import)
    - path: src/cli/init.rs (MUST NOT contain "[template]")
    - path: src/cli/show_config.rs (MUST NOT contain "[template]" display)
    - path: Cargo.toml (MUST NOT contain "hdrhistogram")
  key_links:
    - from: src/config/mod.rs -> src/pipeline/mod.rs via "use TemplateConfig" — no longer present
    - from: src/config/validate.rs -> src/config/mod.rs via "cfg.template" — no longer present
    - from: src/cli/init.rs -> src/pipeline/mod.rs via "[template]" template text — removed
---

<objective>
移除模板分析相关配置层代码（RM-05：配置清理）

**Purpose**: 从 Config 结构体、配置覆盖、校验、init 模板、show_config 展示中删除所有 `[template]` 和 `[template.report]` 相关代码。同时从 pipeline/mod.rs 删除模块声明和辅助函数，从 Cargo.toml 删除 hdrhistogram 依赖。

**Output**: 配置层不再包含 template 相关代码，cargo build 编译通过。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/30-remove-template-analysis/30-RESEARCH.md

<source_refs>
当前代码库中需要修改的文件映射（基于 research doc 的行号分析）：

### src/pipeline/mod.rs
- line 12: `pub mod aggregator;`
- lines 13-15: `pub(crate) use aggregator::{ChartEntry, TemplateAggregator, TemplateStats};`
- line 17: `pub(crate) mod template_reporter;`
- lines 129-131: `fn default_top_n() -> usize { 10 }`
- lines 133-166: `TemplateConfig` + `TemplateReportConfig` 结构体
- lines 168-198: `template_report_enabled()` + `derive_template_report_paths()` 辅助函数
- lines 411-433: `test_template_config_default` 等 3 个配置测试

### src/config/mod.rs
- line 15: `TemplateConfig,` 从 use 导入列表
- line 38: `pub template: Option<TemplateConfig>,`
- line 281: `assert!(cfg.template.is_none());`（位于 test_config_has_5_top_level_optional_fields）

### src/config/apply_one.rs
- lines 123-146: 4 个 template.* 匹配 arm
- lines 293-321: 3 个 test_apply_one_template_* 测试

### src/config/validate.rs
- line 135: `TemplateConfig,` 从 use 导入列表
- lines 102-128: `validate_charts()` 方法中第 104 行引用 `self.template`

### src/cli/init.rs
- lines 84-94 (ZH): `[template]` 配置段（约 11 行含注释）
- lines 199-209 (EN): `[template]` 段（约 11 行含注释）

### src/cli/show_config.rs
- lines 194-217: `if let Some(ta) = &cfg.template { ... }` 完整显示块

### Cargo.toml
- line 60: `hdrhistogram = "7.5.4"`
</source_refs>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 清理 pipeline/mod.rs — 删除模块声明、类型定义、辅助函数和测试</name>
  <files>src/pipeline/mod.rs</files>
  <read_first>
    src/pipeline/mod.rs (line 12 — `pub mod aggregator;`)
    src/pipeline/mod.rs (lines 13-15 — re-exports)
    src/pipeline/mod.rs (line 17 — `pub(crate) mod template_reporter;`)
    src/pipeline/mod.rs (lines 129-198 — `default_top_n`, `TemplateConfig`, `TemplateReportConfig`, `template_report_enabled`, `derive_template_report_paths`)
    src/pipeline/mod.rs (lines 411-433 — template tests)
  </read_first>
  <action>
    依次执行以下删除操作：

    Step 1: 删除模块声明和 re-export
    - 删除 line 12: `pub mod aggregator;`
    - 删除 lines 13-15（3行）：`pub(crate) use aggregator::ChartEntry;`, `pub(crate) use aggregator::TemplateAggregator;`, `pub(crate) use aggregator::TemplateStats;`
    - 删除 line 17（原第 17 行）：`pub(crate) mod template_reporter;`

    Step 2: 删除辅助函数和类型定义
    - 删除 lines 129-131: `fn default_top_n() -> usize { 10 }`
    - 删除 lines 133-166: 整个 `TemplateConfig` 结构体 + `TemplateReportConfig` 结构体 + 它们的 `impl Default`
    - 删除 lines 168-198: 整个 `template_report_enabled()` 和 `derive_template_report_paths()` 函数

    Step 3: 删除模板相关测试
    - 删除 lines 411-433（共 3 个测试）：
      - `test_template_config_default`
      - `test_template_config_deserialize_with_report`
      - `test_template_config_deserialize_empty_is_default`

    注意：`pub(crate) use fingerprint::normalize_template;`（当前行 10）**保留不动** — 该 re-export 是 `fingerprint.rs` 的一部分，Phase 30 的 processor.rs 热循环中删除了唯一调用点，但函数定义和 re-export 由后续阶段（Phase 29 或 Phase 32）清理。

    每次删除后运行 `cargo build` 验证编译。
  </action>
  <verify>
    <automated>
      grep -c 'pub mod aggregator' src/pipeline/mod.rs; echo "expect 0"
      grep -c 'TemplateConfig' src/pipeline/mod.rs; echo "expect 0"
      grep -c 'template_report_enabled' src/pipeline/mod.rs; echo "expect 0"
      grep -c 'default_top_n' src/pipeline/mod.rs; echo "expect 0"
      cargo build 2>&1 | tail -3
    </automated>
  </verify>
  <done>pipeline/mod.rs 中 aggregator/template_reporter 模块声明、TemplateConfig/ReportConfig 类型、辅助函数和相关测试已全部移除，cargo build 编译通过。</done>
</task>

<task type="auto">
  <name>Task 2: 清理 config 层 — mod.rs、apply_one.rs、validate.rs</name>
  <files>
    src/config/mod.rs
    src/config/apply_one.rs
    src/config/validate.rs
  </files>
  <read_first>
    src/config/mod.rs (line 15 — `TemplateConfig,` in use import; line 38 — `pub template: Option<TemplateConfig>,`)
    src/config/apply_one.rs (lines 123-146 — 4 个 template.* match arms; lines 293-321 — 3 个 template 测试)
    src/config/validate.rs (line 135 — `TemplateConfig,` in use import; lines 102-128 — `validate_charts()` 方法)
  </read_first>
  <action>
    依次执行：

    **Step 1: 修改 src/config/mod.rs**
    - 从 `use crate::pipeline::{ChartsConfig, FiltersFeature, NormalizeConfig, OutputConfig, TemplateConfig,}` 中删除 `TemplateConfig,`
    - 删除 line: `pub template: Option<TemplateConfig>,`
    - 修改 `test_config_has_5_top_level_optional_fields` 测试：注释 "确保 5 个" 改为 "确保 4 个"；删除 `assert!(cfg.template.is_none());`

    **Step 2: 修改 src/config/apply_one.rs**
    - 删除 lines 123-146（4 个 template.* match arms）：
      - `"template.enable" => { ... }`
      - `"template.report.enabled" => { ... }`
      - `"template.report.csv_report_path" => { ... }`
      - `"template.report.sqlite_report_path" => { ... }`
    - 删除测试（lines 293-321）：
      - `test_apply_one_template_enable`
      - `test_apply_one_template_report_csv_path`
      - `test_apply_one_template_report_sqlite_path`

    **Step 3: 修改 src/config/validate.rs**
    - 从 `use crate::pipeline::{ChartsConfig, NormalizeConfig, OutputConfig, TemplateConfig,}`（line 135）中删除 `TemplateConfig,`
    - 修改 `validate_charts()` 方法（lines 102-128）：第 104 行 `let template_enabled = self.template.as_ref().is_some_and(|t| t.enable);` 引用了即将被删除的 `self.template`。**删除 `validate_charts()` 方法中的 template_enabled 检查**（lines 103-111）— 即删除 `let template_enabled` 声明和 `if !template_enabled { return Err(...) }` 块。
      - 修改后 `validate_charts()` 只保留 output_dir 和 top_n 的校验（lines 112-125）。charts 本身将由 Phase 28 后续清理。

    每次删除后运行 `cargo build` 验证编译。
  </action>
  <verify>
    <automated>
      grep -c 'TemplateConfig' src/config/mod.rs; echo "expect 0"
      grep -c '"template\.' src/config/apply_one.rs; echo "expect 0"
      grep -c 'TemplateConfig' src/config/validate.rs; echo "expect 0"
      grep -c 'template_enabled' src/config/validate.rs; echo "expect 0"
      cargo build 2>&1 | tail -3
    </automated>
  </verify>
  <done>Config 结构体、apply_one 覆盖、validate 校验中的 template 相关代码已全部移除，cargo build 编译通过。</done>
</task>

<task type="auto">
  <name>Task 3: 清理 CLI 侧和 Cargo.toml — init.rs、show_config.rs、hdrhistogram</name>
  <files>
    src/cli/init.rs
    src/cli/show_config.rs
    Cargo.toml
  </files>
  <read_first>
    src/cli/init.rs (lines 84-94 ZH template — `[template]` 段; lines 199-209 EN template — `[template]` 段)
    src/cli/show_config.rs (lines 194-217 — `if let Some(ta) = &cfg.template` 整个显示块)
    Cargo.toml (line 60 — `hdrhistogram = "7.5.4"`)
  </read_first>
  <action>
    依次执行：

    **Step 1: 修改 src/cli/init.rs**
    从 `CONFIG_TEMPLATE_ZH` 常量中删除以下 11 行（当前 lines 84-94）：
    ```
    [template]
    # SQL 模板归一化（v1.4 新增顶层配置）
    # 启用后对 sql_text 执行注释去除、IN 列表折叠、关键字大写、空白折叠四项变换，生成稳定的模板 key
    # 默认 false（不影响热循环性能）
    enable = false

    # 模板报告独立输出（可选配置，跟随 template.enable 自动启用）
    # [template.report]
    # enabled = true
    # csv_report_path = ""                # 留空 = 自动派生 (out.csv → out_templates.csv)
    # sqlite_report_path = ""             # 留空 = 自动派生 (out.db → out_templates.db)
    ```
    从 `CONFIG_TEMPLATE_EN` 常量中删除对应的 11 行英文版本。

    注意：确认删除前后中英文模板的 [replace_parameters] 段后紧跟 [filter] 段，中间无空行以外的间隙。

    **Step 2: 修改 src/cli/show_config.rs**
    删除 lines 194-217：整个 `if let Some(ta) = &cfg.template { ... }` 块，包括：
    - `if let Some(ta) = &cfg.template {`
    - `println!("{}", color::cyan("[template]"));`
    - `kv("enable", &ta.enable.to_string(), None, diff);`
    - `if let Some(report) = &ta.report { ... }` 子块
    - `println!();`（闭合空行）

    **Step 3: 修改 Cargo.toml**
    删除 line 60: `hdrhistogram = "7.5.4"`

    注意：删除后还需运行 `cargo update` 以清理 Cargo.lock 中的 hdrhistogram 条目。但 Plan 03 会做完整编译验证，此处可暂不执行 update。

    每步完成后运行 `cargo build` 验证。
  </action>
  <verify>
    <automated>
      grep -c '\[template\]' src/cli/init.rs; echo "expect 0"
      grep -c 'cfg.template' src/cli/show_config.rs; echo "expect 0"
      grep -c 'hdrhistogram' Cargo.toml; echo "expect 0"
      cargo build 2>&1 | tail -3
    </automated>
  </verify>
  <done>init 模板、show_config 展示中的 [template] 相关代码已移除，hdrhistogram 依赖已从 Cargo.toml 删除，cargo build 编译通过。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

本计划为移除操作，不引入新代码或新攻击面。仅确保配置层无残留 template 引用。

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-01 | Tampering | Config removal | mitigate | `cargo build` 在每步后验证编译无残留引用 |
| T-30-02 | Tampering | hdrhistogram removal | mitigate | `cargo build` 验证无缺失符号（hdrhistogram 仅被 aggregator.rs 使用，移除模块声明后编译通过即确认安全） |
</threat_model>

<verification>
1. `cargo build` 编译成功
2. `grep -c 'TemplateConfig' src/pipeline/mod.rs` 返回 0
3. `grep -c 'TemplateConfig' src/config/mod.rs` 返回 0
4. `grep -c '"template\.' src/config/apply_one.rs` 返回 0
5. `grep -c '\[template\]' src/cli/init.rs` 返回 0
6. `grep -c 'cfg.template' src/cli/show_config.rs` 返回 0
7. `grep 'hdrhistogram' Cargo.toml` 无匹配
</verification>

<success_criteria>
- pipeline/mod.rs 中 aggregator/template_reporter 模块声明、TemplateConfig/ReportConfig、辅助函数已移除
- config/mod.rs、apply_one.rs、validate.rs 中所有 template 引用已移除
- init 模板和 show_config 中无 [template] 段
- hdrhistogram 从 Cargo.toml 移除
- `cargo build` 编译通过
</success_criteria>
</task>
