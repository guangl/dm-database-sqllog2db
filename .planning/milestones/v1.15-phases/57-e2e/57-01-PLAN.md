---
phase: 57-e2e
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/stats/config.rs
  - tests/integration.rs
autonomous: true
requirements:
  - TEST-03
user_setup: []

must_haves:
  truths:
    - "validate_stats_time_range 在 from > to 时返回 Err(Error::Config(ConfigError::InvalidValue))，错误信息 reason 字段包含字符串 'must be <=' 以及具体的 from 与 to 值"
    - "现有 validate_stats_time_range 调用路径（Config::validate 与 run_stats）在 from > to 时均拒绝运行（无需新增调用点，仅扩充函数体）"
    - "stats CLI 命令 `sqllog2db stats -c <cfg> --from 2024-01-31 --to 2024-01-01` 退出码非零（EXIT_FATAL=2），stderr 包含 'stats.from' 字面量"
    - "stats CLI 在 from == to 边界情况下仍正常退出 0（不破坏已有 test_stats_from_to_filters_to_single_day 行为）"
  artifacts:
    - path: "src/stats/config.rs"
      provides: "validate_stats_time_range 新增 from ≤ to 跨字段比较分支"
      contains: "must be <="
    - path: "tests/integration.rs"
      provides: "stats from > to 边界条件 e2e 测试 + 单元测试覆盖"
      contains: "test_cli_stats_rejects_from_after_to"
  key_links:
    - from: "src/stats/config.rs:validate_stats_time_range"
      to: "src/error.rs:ConfigError::InvalidValue"
      via: "返回 Err(Error::Config(ConfigError::InvalidValue { field, value, reason }))"
      pattern: "Error::Config\\(ConfigError::InvalidValue"
    - from: "tests/integration.rs:test_cli_stats_rejects_from_after_to"
      to: "stats 子命令二进制"
      via: "assert_cmd::Command::cargo_bin(\"sqllog2db\")"
      pattern: "Command::cargo_bin\\(\"sqllog2db\"\\)"
---

<objective>
为 stats `--from`/`--to` 添加跨字段顺序校验（from ≤ to），并补全对应的 CLI 边界条件集成测试。

Purpose: TEST-03 要求 stats 子命令在 from > to 时给出明确错误并非零退出，但当前 `validate_stats_time_range` 仅做单字段格式校验，没有跨字段顺序检查；TEST-03 的此条边界场景因此无法测试。本 plan 既是代码改动（先决条件），又是测试落地。

Output:
- `src/stats/config.rs:validate_stats_time_range` 末尾新增 from ≤ to 比较分支（YYYY-MM-DD 字典序 == 日期序，纯字符串比较，零依赖）
- `src/stats/config.rs` 单元测试新增 3 个测试覆盖：from > to 报错、from == to 通过、单端配置（仅 from 或仅 to）通过
- `tests/integration.rs` 新增 1 个 e2e 测试 `test_cli_stats_rejects_from_after_to` 覆盖 CLI 路径
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/57-e2e/57-CONTEXT.md
@.planning/phases/57-e2e/57-RESEARCH.md
@.planning/phases/57-e2e/57-PATTERNS.md

<interfaces>
<!-- 关键签名：执行者无需自行探索 -->

From src/error.rs (lines 184–189):
```rust
#[error("Invalid configuration value {field} = '{value}': {reason}")]
InvalidValue {
    field: String,
    value: String,
    reason: String,
},
```

From src/stats/config.rs:17–37 (现有 validate_stats_time_range 完整结构):
```rust
pub fn validate_stats_time_range(stats: &StatsConfig) -> crate::error::Result<()> {
    if let Some(from) = &stats.from {
        validate_time_str(from).map_err(|reason| {
            Error::Config(ConfigError::InvalidValue {
                field: "stats.from".to_string(),
                value: from.clone(),
                reason,
            })
        })?;
    }
    if let Some(to) = &stats.to {
        validate_time_str(to).map_err(|reason| {
            Error::Config(ConfigError::InvalidValue {
                field: "stats.to".to_string(),
                value: to.clone(),
                reason,
            })
        })?;
    }
    Ok(())
}
```

已有调用路径（无需修改，自然继承新检查）:
- `src/config/validate.rs:15` — `Config::validate` 中调用
- `src/stats/mod.rs:23` — `run_stats` 中调用（CLI --from/--to 合并到 cfg.stats 之后）

从 tests/integration.rs:1297–1316 已有的 `make_stats_config_file` helper（本 plan e2e 测试直接复用）:
```rust
fn make_stats_config_file(dir: &std::path::Path) -> std::path::PathBuf {
    let cfg_path = dir.join("stats_cfg.toml");
    // 写入 input.log + 完整 [sqllog]/[exporter.csv]/[logging] config
    cfg_path
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 在 validate_stats_time_range 增加 from ≤ to 跨字段检查并补充单元测试</name>
  <files>src/stats/config.rs</files>
  <read_first>
    - src/stats/config.rs（完整文件——必读：现有 validate_stats_time_range 函数体在第 17–37 行，新增逻辑插入在第二个 if let 块之后、第 36 行 `Ok(())` 之前；现有单元测试模块在第 115 行起，新增测试追加到模块末尾的 `}` 前）
    - src/error.rs（必读第 184–189 行：ConfigError::InvalidValue 变体的字段顺序与签名，本任务构造 Err 必须精确匹配）
    - .planning/phases/57-e2e/57-PATTERNS.md（必读 "validate_stats_time_range 跨字段检查 (D-01 / D-02)" 段落——已给出完整 Rust 代码块，照实落地）
  </read_first>
  <behavior>
    - Test 1: `validate_stats_time_range` 在 from="2024-01-31" / to="2024-01-01" 时返回 Err；错误的 ConfigError::InvalidValue 的 reason 字段包含字符串 "must be <="，field 字段为 "stats.from"，value 字段为 "2024-01-31"
    - Test 2: from="2024-01-15" / to="2024-01-15" 时返回 Ok（边界等值通过）
    - Test 3: 仅设置 from（to=None）时返回 Ok（只校验单字段格式，不触发跨字段比较）；仅设置 to（from=None）时同理返回 Ok
    - Test 4: from="2024-01-01" / to="2024-01-31" 时返回 Ok（顺序正确）
    - 现有 16 个单元测试（validate_time_str + StatsConfig::default 等）保持全部通过，无回归
  </behavior>
  <action>
    在 src/stats/config.rs 的 `validate_stats_time_range` 函数体中（第二个 `if let Some(to)` 块之后、`Ok(())` 之前）追加 from ≤ to 跨字段比较块，逐字采用 57-PATTERNS.md 给出的代码片段：使用 `if let (Some(from), Some(to)) = (&stats.from, &stats.to)` 解构两字段；当 `from.as_str() > to.as_str()` 时返回 `Err(Error::Config(ConfigError::InvalidValue { field: "stats.from".to_string(), value: from.clone(), reason: format!("stats.from ({from}) must be <= stats.to ({to})") }))`。

    实现要点：
    - 使用 `from.as_str() > to.as_str()` 进行字符串字典序比较——YYYY-MM-DD 格式下字典序 == 日期序（per D-01）；同时支持 19 字符 "YYYY-MM-DD HH:MM:SS" 格式（同样字典序合法）
    - 错误信息 reason 必须为 `format!("stats.from ({from}) must be <= stats.to ({to})")`（per D-02，参见 RESEARCH.md "Code Examples" 段；reason 字符串需精确包含 "must be <=" 字面，因为 e2e 测试将断言此子串）
    - field 用 `"stats.from".to_string()`、value 用 `from.clone()`——按 D-02 锁定错误信息聚焦在 stats.from 字段；不要改成 stats.to
    - 不要新增任何 use 语句（Error 与 ConfigError 已在文件顶部 use）；不要修改函数签名；不要修改前两个 if let 块

    在 `#[cfg(test)] mod tests` 模块的末尾、模块闭合 `}` 之前，追加 4 个新单元测试函数：
    - `test_validate_stats_time_range_rejects_from_after_to`：构造 StatsConfig { from: Some("2024-01-31".into()), to: Some("2024-01-01".into()), top: None }，调用 `validate_stats_time_range`，断言返回 Err；解构 Err 为 `Error::Config(ConfigError::InvalidValue { field, value, reason })`，断言 `field == "stats.from"`、`value == "2024-01-31"`、`reason.contains("must be <=")` 且 `reason.contains("2024-01-31")` 且 `reason.contains("2024-01-01")`
    - `test_validate_stats_time_range_accepts_equal_from_to`：from="2024-01-15"、to="2024-01-15"，断言返回 Ok
    - `test_validate_stats_time_range_accepts_from_only`：from=Some、to=None，断言返回 Ok；同步追加一个 from=None、to=Some 的对称断言（合在同一 #[test] 内）
    - `test_validate_stats_time_range_accepts_ordered`：from="2024-01-01"、to="2024-01-31"，断言返回 Ok

    所有单元测试遵循现有模块的风格（已 `use super::*;`，直接使用 StatsConfig 与 validate_stats_time_range，无需额外 use）；测试函数命名前缀统一为 `test_validate_stats_time_range_`。
  </action>
  <verify>
    <automated>cargo test stats::config::tests -- --nocapture 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - 命令 `cargo test stats::config::tests` 退出码 0，输出包含 4 个新测试名称（test_validate_stats_time_range_rejects_from_after_to / accepts_equal_from_to / accepts_from_only / accepts_ordered）且全部 "ok"
    - 命令 `grep -c 'must be <=' src/stats/config.rs` 输出 ≥ 2（一次在生产代码 reason format!，一次在测试断言；具体次数可超过 2）
    - 命令 `grep -q 'if let (Some(from), Some(to)) = (&stats.from, &stats.to)' src/stats/config.rs` 退出 0（跨字段解构存在）
    - 命令 `grep -q 'from.as_str() > to.as_str()' src/stats/config.rs` 退出 0（字符串字典序比较存在）
    - `cargo clippy --all-targets -- -D warnings` 退出 0（无新增 clippy 警告）
    - 现有的 16 个 stats::config::tests 单元测试名称（test_validate_time_str_* + test_stats_config_*）均在测试输出中显示 "ok"，无 ignored / FAILED
  </acceptance_criteria>
  <done>
    `validate_stats_time_range` 在 from > to 时返回 Err 且错误信息符合 D-02 格式；4 个新单元测试通过；16 个旧测试不回归；clippy 全绿。
  </done>
</task>

<task type="auto">
  <name>Task 2: 新增 e2e 集成测试 test_cli_stats_rejects_from_after_to 覆盖 CLI 路径</name>
  <files>tests/integration.rs</files>
  <read_first>
    - tests/integration.rs（必读第 1297–1316 行：`make_stats_config_file` helper 签名与返回路径——本测试直接调用，无需新建 helper；必读第 1818–1834 行：`test_cli_stats_runtime_rejects_bad_cli_from_format` 是本测试的结构模板；必读第 1938–1940 行：文件末尾是 `}` + 换行，新测试追加到末尾函数之后）
    - .planning/phases/57-e2e/57-PATTERNS.md（必读 "TEST-03 — stats from > to 边界条件" 段落，给出新测试与模板的差异）
    - .planning/phases/57-e2e/57-CONTEXT.md（必读 D-01/D-02：错误信息包含字段名 "stats.from" 与具体值 "2024-01-31"——assert_cmd 断言必须匹配这些字面字符串）
  </read_first>
  <action>
    在 tests/integration.rs 末尾追加新的集成测试 `test_cli_stats_rejects_from_after_to`，结构高度复刻第 1818 行的 `test_cli_stats_runtime_rejects_bad_cli_from_format`：

    - 函数签名：`#[test] fn test_cli_stats_rejects_from_after_to()`
    - 函数体头部：`use assert_cmd::Command;` 和 `use predicates::str::contains;`（按现有测试惯例放在函数体内部 use，prediates 模块同已有测试一致）
    - 创建临时目录：`let dir = tempfile::tempdir().unwrap();`
    - 复用 helper：`let cfg_path = make_stats_config_file(dir.path());`（已存在于第 1297 行，已含 valid 的 input.log 与 [exporter.csv] 配置，可直接产生 stats 命令所需的有效配置）
    - 构造命令：`Command::cargo_bin("sqllog2db").unwrap()`，链式调用 `.args(["stats", "-c"])`、`.arg(&cfg_path)`、`.args(["--from", "2024-01-31", "--to", "2024-01-01"])`、`.assert().failure()`
    - stderr 断言（链式 .stderr 多次）：
      * `.stderr(contains("stats.from"))` —— 校验错误信息包含字段名
      * `.stderr(contains("must be <="))` —— 校验错误信息包含 D-02 锁定的核心字符串
      * 同时追加一个 `.stderr(contains("2024-01-31"))` 断言 reason 中嵌入了具体 from 值

    紧贴在新测试函数前加一行 doc 注释：`/// TEST-03 (Phase 57): stats CLI 在 --from 晚于 --to 时退出非零，stderr 包含字段名与 "must be <=" 文案（D-01/D-02）。`

    不要：
    - 新建任何 helper 函数（直接复用 `make_stats_config_file`）
    - 改动现有 helper 或现有测试
    - 引入 chrono / time 等额外 crate
    - 在 stderr 断言里使用 `.or(...)`（CONTEXT.md D-02 明确锁定错误格式，无需 OR 兜底）
  </action>
  <verify>
    <automated>cargo test --test integration test_cli_stats_rejects_from_after_to 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --test integration test_cli_stats_rejects_from_after_to` 退出码 0，输出包含 `test test_cli_stats_rejects_from_after_to ... ok`
    - `grep -c 'fn test_cli_stats_rejects_from_after_to' tests/integration.rs` 输出 1
    - `grep -A 20 'fn test_cli_stats_rejects_from_after_to' tests/integration.rs | grep -c 'contains("stats.from")'` 输出 ≥ 1
    - `grep -A 20 'fn test_cli_stats_rejects_from_after_to' tests/integration.rs | grep -c 'contains("must be <=")'` 输出 ≥ 1
    - `grep -A 20 'fn test_cli_stats_rejects_from_after_to' tests/integration.rs | grep -c 'make_stats_config_file'` 输出 ≥ 1（复用 helper，未新建）
    - `cargo test --test integration` 全套退出码 0（无回归；现有 test_cli_stats_runtime_rejects_bad_cli_from_format / test_stats_from_to_filters_to_single_day 等仍通过）
    - `cargo clippy --all-targets -- -D warnings` 退出 0
  </acceptance_criteria>
  <done>
    新测试通过；测试函数体精确包含三条 stderr 字符串断言（stats.from / must be <= / 2024-01-31）；现有 integration 测试无回归；clippy 全绿。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI 用户 → stats 子命令（参数 --from/--to） | 用户输入的字符串可能包含非预期格式、SQL 注入子串等，但本 phase 仅做格式与顺序校验，不进入 SQL 执行路径 |
| tests/integration.rs → assert_cmd 启动的真实二进制 | 测试通过 cargo_bin 启动隔离的 sqllog2db 子进程，stderr/stdout 是测试断言的唯一来源；测试在 tempfile 中运行，无外部状态泄漏 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-57-01 | Tampering | `validate_stats_time_range` 跨字段比较（new code） | mitigate | 字符串比较仅作用于 ASCII YYYY-MM-DD/Datetime 格式，调用前已通过 `validate_time_str` 强制 ASCII + 数字 + 分隔符校验，恶意 Unicode/控制字符无法绕过比较逻辑 |
| T-57-02 | Information Disclosure | ConfigError::InvalidValue reason 字段输出到 stderr | accept | reason 仅包含用户已输入的 from/to 字符串，未泄漏内部状态；遵循现有 ConfigError 显示规则，无新增攻击面 |
| T-57-03 | Denial of Service | 集成测试通过 assert_cmd 启动子进程 | accept | 单条测试 ~1s，使用 tempfile 隔离，Drop 时自动清理；新测试 1 条不影响 CI 时间预算 |
| T-57-SC | Tampering | npm/pip/cargo installs | mitigate | 本 plan 不引入任何新 crate（assert_cmd / predicates / tempfile / rusqlite 已在 Cargo.toml dev-dependencies 中），无 supply-chain 暴露面 |
</threat_model>

<verification>
- `cargo test stats::config::tests` 全部通过（含 4 个新单元测试 + 16 个原有）
- `cargo test --test integration test_cli_stats_rejects_from_after_to` 通过
- `cargo test --test integration` 全套通过，特别确认 test_stats_from_to_filters_to_single_day（from==to 边界）仍 ok
- `cargo clippy --all-targets -- -D warnings` 通过
- `cargo fmt --check` 通过
</verification>

<success_criteria>
- TEST-03 三个分支均有自动化覆盖：
  * 无效格式（已有 test_cli_stats_runtime_rejects_bad_cli_from_format）
  * from == to 边界（已有 test_stats_from_to_filters_to_single_day）
  * from > to（新增 test_cli_stats_rejects_from_after_to + 4 个单元测试）
- `validate_stats_time_range` 在所有调用点（Config::validate / run_stats）自动继承新检查，无需修改调用方
- 错误信息符合 D-02 锁定格式：`stats.from (2024-01-31) must be <= stats.to (2024-01-01)`
</success_criteria>

<output>
Create `.planning/phases/57-e2e/57-01-SUMMARY.md` when done
</output>
