---
phase: 02-fsevents
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/cli/run/tests.rs
autonomous: false
requirements: [QUAL-02, QUAL-03]
tags: [coverage, collector, unit-test, llvm-cov]

must_haves:
  truths:
    - "`cargo llvm-cov --summary-only` 输出 TOTAL 行的 Line % 列 >= 92.00%"
    - "`cargo test test_collector_invalid_path_returns_error` 绿色"
    - "`cargo test test_collector_parse_error_accumulation` 绿色"
    - "`cargo test test_collector_not_needed_filtering` 绿色"
    - "`cargo test test_collector_filtered_params_normalize` 绿色"
    - "QUAL-03 决策可在 02-RESEARCH.md 与 02-CONTEXT.md 中追溯（D-01/D-02 书面依据），无需新增文件"
  artifacts:
    - path: "src/cli/run/tests.rs"
      provides: "collector.rs Group 1+2+3+4 全分支单元测试"
      contains: "test_collector_invalid_path_returns_error, test_collector_parse_error_accumulation, test_collector_not_needed_filtering, test_collector_filtered_params_normalize"
  key_links:
    - from: "src/cli/run/tests.rs::test_collector_invalid_path_returns_error"
      to: "super::collector::collect_log_file"
      via: "传入不存在路径触发 ParserError::InvalidPath"
      pattern: "collector::collect_log_file\\(.*nonexistent"
    - from: "src/cli/run/tests.rs::test_collector_parse_error_accumulation"
      to: "super::collector::collect_log_file"
      via: "写入无效行的临时文件触发 ParseError::InvalidFormat 分支"
      pattern: "stats\\.parse_error_records"
    - from: "src/cli/run/tests.rs::test_collector_not_needed_filtering"
      to: "super::collector::process_record (via collect_log_file)"
      via: "Pipeline 含 AlwaysFail 处理器 + DML 记录（tag.is_some()） → Group 3 !needs_processing 分支"
      pattern: "filtered_out"
    - from: "src/cli/run/tests.rs::test_collector_filtered_params_normalize"
      to: "super::collector::process_record (via collect_log_file)"
      via: "Pipeline 含 AlwaysFail 处理器 + PARAMS 记录（tag.is_none()） + do_normalize=true → Group 4 filtered PARAMS else 分支"
      pattern: "filtered_out"
---

<objective>
为 src/cli/run/collector.rs 补充 4 个单元测试覆盖：Group 1 InvalidPath（lines 26-34）、Group 2 parse error 累积循环（lines 41-63）、Group 3 !needs_processing 过滤分支（lines 92-93）、Group 4 被过滤的 PARAMS else 分支（lines 109-119），将整体行覆盖率从 91.41% 推至 ≥92%。在 Wave 1（02-01）已加入的 WATCH-07/08/09 集成测试间接带动 src/exporter/csv/mod.rs 覆盖率基础上，本计划补足剩余缺口。最后通过人工 checkpoint 跑 `cargo llvm-cov --summary-only` 确认覆盖率达标。

Purpose: 满足 QUAL-02 success criteria 第 1 条（整体行覆盖率 ≥ 92%）。collector.rs 是 D-04 主要目标文件（48 行 uncovered），路径明确可控。Group 1+2 ≈ 22 行，Group 3+4 ≈ 13 行，合计 ≈ 35 行 collector 增量 + 集成测试带动 csv/mod.rs ≈ 7-15 行 = ≈ 42-50 行，确定性高于仅靠 Group 1+2（29-37 行，存在缺口风险）。

Output: src/cli/run/tests.rs 末尾追加 4 个 #[test] 函数；最后 1 个 checkpoint 任务由人工执行 llvm-cov 并粘贴 TOTAL 行。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/02-fsevents/02-CONTEXT.md
@.planning/phases/02-fsevents/02-RESEARCH.md
@.planning/phases/02-fsevents/02-PATTERNS.md
@.planning/phases/02-fsevents/02-01-PLAN.md

<interfaces>
<!-- collector.rs signature (verified at src/cli/run/collector.rs lines 18-24) -->

From src/cli/run/collector.rs:
```
pub(super) fn collect_log_file(
    file: &Path,
    pipeline: &Pipeline,
    do_normalize: bool,
    placeholder_override: Option<bool>,
    interrupted: &Arc<AtomicBool>,
) -> Result<(Vec<(Sqllog, Option<String>)>, ErrorStats)>
```

Error path triggered when LogParserBuilder fails (line 28-34):
```
Error::Parser(ParserError::InvalidPath { path, reason, line_number })
```

From src/error.rs (verified by RESEARCH.md and Phase 1 tests):
```
pub enum ParserError {
    InvalidPath { path: PathBuf, reason: String, line_number: Option<u64> },
    ...
}
pub enum Error {
    Parser(ParserError),
    ...
}
pub struct ErrorStats {
    pub parse_errors: u64,
    pub parse_error_records: Vec<ParseErrorRecord>,
    pub total_errors: u64,
    pub filtered_out: u64,
    pub by_type: HashMap<ErrorKind, u64>,
    pub records_exported: u64,
    ...
}
impl ErrorStats {
    pub fn parse_error_count(&self) -> u64 { ... }
}
```

Pipeline public API (verified at src/pipeline/mod.rs lines 163-184):
```
pub trait LogProcessor: Send + Sync + std::fmt::Debug {
    fn process(&self, record: &Sqllog) -> bool;
}

impl Pipeline {
    pub fn new() -> Self
    pub fn add(&mut self, processor: Box<dyn LogProcessor>)
    pub fn is_empty(&self) -> bool
    pub fn run_with_meta(&self, record: &Sqllog) -> bool
}
impl Default for Pipeline { ... }   // 等价 Pipeline::new()
```

Reference pattern for inline AlwaysFail (verified at src/pipeline/mod.rs lines 322-328 in tests):
```
#[derive(Debug)]
struct AlwaysFail;
impl LogProcessor for AlwaysFail {
    fn process(&self, _: &dm_database_parser_sqllog::Sqllog) -> bool {
        false  // 所有记录被过滤
    }
}
```

tests.rs 顶部已有 `use super::*;` 与 `use crate::config::Config;`（verified lines 1-2），故 super::collector::collect_log_file 直接可见（collector.rs 是 cli/run 的子模块；pub(super) 对 run 模块内可见，包含 tests 子模块）。
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: collector.rs 全分支（Group 1+2+3+4）单元测试</name>
  <files>src/cli/run/tests.rs</files>
  <read_first>
    - src/cli/run/tests.rs (现有 5 个测试 + 2 个 Gap 测试 + 5 个 progress/error log 测试的导入与结构；末尾追加位置)
    - src/cli/run/collector.rs (行 18-77 collect_log_file 全文 + lines 79-120 process_record；明确 InvalidPath 触发条件、parse_error_records 字段操作、Group 3 在 line 91-93、Group 4 在 line 109-119)
    - src/cli/run/mod.rs (查看 mod 是否声明 mod collector; 与 tests 子模块对 collector 的可见性)
    - src/pipeline/mod.rs (lines 199-213 + 318-328 的 AlwaysPass/AlwaysFail inline LogProcessor 实现模板)
    - .planning/phases/02-fsevents/02-RESEARCH.md (Pitfall 1/2，Code Examples 中的两个测试模板；Open Question 1 Group 3/4 触发条件)
    - .planning/phases/02-fsevents/02-PATTERNS.md (Group 1/2/3/4 测试模板)
  </read_first>
  <behavior>
    - Test A `test_collector_invalid_path_returns_error` (Group 1，~6 行)：
      - 输入：不存在的绝对路径 "/nonexistent/absolutely/not/there.log"
      - 期望返回：Err，且匹配 Error::Parser(ParserError::InvalidPath { .. })
    - Test B `test_collector_parse_error_accumulation` (Group 2，~16 行)：
      - 输入：临时目录下的 bad.log，内容为多条无时间戳前缀的非法文本
      - 期望返回：Ok((rows, stats))，rows 为空；stats.parse_error_count() > 0；stats.parse_error_records 非空
    - Test C `test_collector_not_needed_filtering` (Group 3，~2 行 + 共享路径)：
      - 输入：含 1 条合法 SEL 行（tag.is_some()）的 .log；Pipeline 添加 AlwaysFail processor；do_normalize=false
      - 期望：(rows, stats) = collect_log_file 返回 Ok；rows 为空；stats.filtered_out >= 1（!needs_processing 分支命中）
    - Test D `test_collector_filtered_params_normalize` (Group 4，~11 行)：
      - 输入：含 1 条 PARAMS 行（tag.is_none()）+ 可选 DML 行的 .log；Pipeline 添加 AlwaysFail processor；do_normalize=true
      - 期望：(rows, stats) 返回 Ok；rows 不包含被过滤的 PARAMS 行；stats.filtered_out >= 1（else 分支命中，compute_normalized 被调用更新 params_buf）
    - 现有所有测试保持绿色不退化
  </behavior>
  <action>
    在 src/cli/run/tests.rs 末尾（紧随 test_run_summary 之后）追加 4 个 #[test] 函数，使用 super::*; 已隐式包含的 collector 子模块：

    1) `test_collector_invalid_path_returns_error`（Group 1）：
       - 函数内 use：
         - `use crate::pipeline::Pipeline;`
         - `use std::sync::Arc;`
         - `use std::sync::atomic::AtomicBool;`
       - let pipeline = Pipeline::new();
       - let interrupted = Arc::new(AtomicBool::new(false));
       - let result = super::collector::collect_log_file(
           std::path::Path::new("/nonexistent/absolutely/not/there.log"),
           &pipeline,
           false,
           None,
           &interrupted,
         );
       - assert!(result.is_err(), "不存在路径应返回 Err，实际: {result:?}");
       - assert!(matches!(result.unwrap_err(), crate::error::Error::Parser(crate::error::ParserError::InvalidPath { .. })), "应匹配 ParserError::InvalidPath");

    2) `test_collector_parse_error_accumulation`（Group 2）：
       - 函数内 use 同上 + `use tempfile::TempDir;`
       - let dir = TempDir::new().unwrap();
       - let log_path = dir.path().join("bad.log");
       - std::fs::write(&log_path, "not a valid log line\nalso invalid\n").unwrap();
       - let pipeline = Pipeline::new();
       - let interrupted = Arc::new(AtomicBool::new(false));
       - let (rows, stats) = super::collector::collect_log_file(&log_path, &pipeline, false, None, &interrupted).expect("collect_log_file 应不返回 Err");
       - assert!(rows.is_empty(), "全部非法行应不产生记录，实际 rows.len()={}", rows.len());
       - assert!(stats.parse_error_count() > 0, "应记录至少 1 条解析错误，实际 parse_error_count={}", stats.parse_error_count());
       - assert!(!stats.parse_error_records.is_empty(), "parse_error_records 应非空");

    3) `test_collector_not_needed_filtering`（Group 3，!needs_processing 分支）：
       - 函数内 use 同上 + `use tempfile::TempDir;` + `use crate::pipeline::LogProcessor;`
       - 函数内定义 inline processor（模板参考 src/pipeline/mod.rs:318-328）：
         ```
         #[derive(Debug)]
         struct AlwaysFail;
         impl LogProcessor for AlwaysFail {
             fn process(&self, _: &dm_database_parser_sqllog::Sqllog) -> bool { false }
         }
         ```
       - let dir = TempDir::new().unwrap();
       - let log_path = dir.path().join("dml.log");
       - 写入 1 条合法 SEL 行（tag = Some("SEL")，确保 record.tag.is_some()）：
         `let valid_dml = "2025-01-15 10:30:28.001 (EP[0] sess:0x0001 user:TESTUSER trxid:1 stmt:0x1 appname:App ip:10.0.0.1) [SEL] SELECT id FROM t. EXECTIME: 5(ms) ROWCOUNT: 1(rows) EXEC_ID: 1.\n";`
         `std::fs::write(&log_path, valid_dml).unwrap();`
       - let mut pipeline = Pipeline::new();
       - pipeline.add(Box::new(AlwaysFail));
       - assert!(!pipeline.is_empty(), "AlwaysFail 添加后 pipeline 非空");
       - let interrupted = Arc::new(AtomicBool::new(false));
       - let (rows, stats) = super::collector::collect_log_file(&log_path, &pipeline, false, None, &interrupted).expect("collect_log_file 应 Ok");
       - assert!(rows.is_empty(), "AlwaysFail 过滤所有记录，rows 应为空，实际 {}", rows.len());
       - assert!(stats.filtered_out >= 1, "应至少有 1 条记录被过滤，实际 filtered_out={}", stats.filtered_out);
       - 备注：此分支触发条件 passes=false（AlwaysFail）+ do_normalize=false → needs_processing = false || (false && ...) = false → 命中 line 91-93 `if !needs_processing { filtered_out += 1; return; }`

    4) `test_collector_filtered_params_normalize`（Group 4，filtered PARAMS else 分支）：
       - 函数内 use 同 Group 3 + 复用 AlwaysFail（或在 Group 3 测试外部 mod 级别提取；为隔离性，可在本测试内部再次定义）
       - 为避免重复 inline 结构体，建议提取 AlwaysFail 到测试模块文件作用域（src/cli/run/tests.rs 内 mod 顶层 #[derive(Debug)] struct AlwaysFail; impl LogProcessor for AlwaysFail { ... }），Group 3 + Group 4 共享。如果对模块作用域改动顾虑大，则在 Group 4 内重新定义同名 inline 结构体（Rust 允许块作用域内屏蔽外部同名类型）。
       - let dir = TempDir::new().unwrap();
       - let log_path = dir.path().join("params.log");
       - 写入 1 条 PARAMS 行（tag = None，即 record.tag.is_none()）。PARAMS 行格式参照解析器对 PARAMS 行的识别约定（无 [SEL]/[UPD]/[INS]/[DEL] 标签）。从 Phase 1 watch/mod.rs::tests 或 dm-database-parser-sqllog 的固件中复制一条已知可解析的 PARAMS 行。占位写入示例：
         `let params_line = "2025-01-15 10:30:28.001 (EP[0] sess:0x0001 user:TESTUSER trxid:1 stmt:0x1 appname:App ip:10.0.0.1) PARAMS(SEQNO=1) #1(SQL_TYPE_VARCHAR2_DEF):  TESTVALUE.\n";`
         若执行时该样本无法解析为 PARAMS（tag.is_some() 或 ParseError），则改为读取 dm-database-parser-sqllog crate 仓库 testdata 中的 PARAMS 固件（通过 cargo metadata 定位），或参照 src/pipeline/normalizer/tests.rs 中已有的 PARAMS 测试样本拷贝。
       - std::fs::write(&log_path, params_line).unwrap();
       - let mut pipeline = Pipeline::new();
       - pipeline.add(Box::new(AlwaysFail));
       - let interrupted = Arc::new(AtomicBool::new(false));
       - let (rows, stats) = super::collector::collect_log_file(&log_path, &pipeline, true /* do_normalize */, None, &interrupted).expect("collect_log_file 应 Ok");
       - assert!(rows.is_empty(), "AlwaysFail 过滤 PARAMS 记录，rows 应为空，实际 {}", rows.len());
       - assert!(stats.filtered_out >= 1, "应至少有 1 条记录被过滤，实际 filtered_out={}", stats.filtered_out);
       - 备注：此分支触发条件 passes=false + do_normalize=true + record.tag.is_none() → needs_processing = false || (true && true) = true → 跳过 line 91-93，走 if passes { ... } else { ... } 中 else 分支（line 109-119），调用 compute_normalized 更新 params_buf 但不 push 到 rows。
       - 若 PARAMS 行无法构造（解析器要求严格），**不使用 `#[ignore]`**。备选策略按优先级：
         (a) 从 src/pipeline/normalizer/tests.rs 的 #[test] 函数中提取已知 PARAMS 固件行作为 params_line 样本；
         (b) 若 normalizer/tests.rs 无合用样本，从 dm-database-parser-sqllog crate 的 testdata 目录中定位可被解析为 tag=None 的行，通过 `cargo metadata --no-deps` 定位 crate 源路径；
         (c) 若以上均不可行，替换为覆盖 src/cli/run/filter_processor.rs 的未覆盖路径（D-05 次要目标），以确保覆盖增量总量不减少，在 Task 2 checkpoint 中说明替换原因。

    实现自 D-04（collector.rs 主要目标，Group 1+2+3+4 全分支）；不修改 collector.rs 本身、不调整 mod 可见性（pub(super) 在同 run 模块的 tests 子模块中已可见）。Group 3+4 解决 checker 提出的 "Group 1+2 22 行 + 集成测试 7-15 行 = 29-37 行远低于 70 行缺口" 问题，新增 ~13 行 collector 路径覆盖，使 collector 增量达 ~35 行 + csv/mod.rs ~7-15 行 = ~42-50 行，确定性达到 92% 门槛。
  </action>
  <verify>
    <automated>cargo test --lib -- test_collector_</automated>
  </verify>
  <acceptance_criteria>
    - src/cli/run/tests.rs 含 4 个函数定义：`fn test_collector_invalid_path_returns_error`、`fn test_collector_parse_error_accumulation`、`fn test_collector_not_needed_filtering`、`fn test_collector_filtered_params_normalize`（grep -c 'fn test_collector_' >= 4）
    - 4 个测试均调用 `super::collector::collect_log_file(` (grep -c 'super::collector::collect_log_file' >= 4)
    - 4 个测试均通过 `cargo test test_collector_`
    - `cargo test` 全套通过，无回归
    - `cargo clippy --all-targets -- -D warnings` 通过
    - `cargo fmt --check` 通过
    - src/cli/run/collector.rs 函数签名不变（grep -c 'pub(super) fn collect_log_file' == 1）
    - 若 Group 4 PARAMS 样本无法触发 record.tag.is_none() + 解析成功，必须在 Task 2 checkpoint 反馈中说明并改用 fallback（标 `#[ignore]` 或选用其他 LogProcessor 构造方式），不可静默删除测试
  </acceptance_criteria>
  <done>
    src/cli/run/tests.rs 含 4 个新测试函数（Group 1+2+3+4），cargo test/clippy/fmt 三道质量门禁均通过；collector.rs 文件本身未被改动。
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 人工验证 cargo llvm-cov 总覆盖率 ≥ 92%</name>
  <what-built>
    Wave 1（02-01）已添加 3 个 WATCH-07/08/09 集成测试间接带动 src/exporter/csv/mod.rs 的 export 路径；本计划 Task 1 已为 src/cli/run/collector.rs 补充 4 个单元测试覆盖 Group 1+2+3+4 全分支（~35 行）。
    叠加预期覆盖增量为：collector.rs ~35 行 + csv/mod.rs ~7-15 行 ≈ 42-50 行。如仍未达 70 行缺口（92.00% 门槛），按 D-05 次要文件兜底（filter_processor.rs / sqlite/mod.rs）。
  </what-built>
  <how-to-verify>
    人工执行以下步骤并向 Claude 反馈结果：

    1) 运行覆盖率工具（项目根目录）：
       `cargo llvm-cov --summary-only`

    2) 在输出中找到末尾 `TOTAL` 行（格式：`TOTAL ... Lines: NNNN/NNNN MM.MM%`），记录 Line % 数值。

    3) 判断：
       - 若 TOTAL Line % ≥ 92.00% → 输入 `approved` 完成 checkpoint，QUAL-02 达标。
       - 若 TOTAL Line % 在 91.50%–91.99% 之间 → 输入 `补救：D-05`，Claude 将启动子任务追加 src/cli/run/filter_processor.rs 或 src/exporter/sqlite/mod.rs 测试（02-CONTEXT.md D-05 次要目标），然后重新跑 llvm-cov。
       - 若 TOTAL Line % < 91.50% → 输入 `补救：扩展 D-05`，Claude 将同时追加 filter_processor.rs 与 sqlite/mod.rs 测试。

    4) 同时确认：
       - `cargo test` 全套通过（含 Wave 1 三个新测试 + Task 1 四个新测试 + 现有 ~889 测试）
       - `cargo clippy --all-targets -- -D warnings` 通过
       - tests/integration.rs:2917 `#[ignore]` 注解仍存在（QUAL-03 D-01 防回归）
  </how-to-verify>
  <resume-signal>
    输入 `approved`（覆盖率达标）/ `补救：D-05`（92% 差 0.01–0.50%，按 D-05 次要目标补救）/ `补救：扩展 D-05`（差 ≥0.50%，按双文件补救）/ 描述其他问题。
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 测试代码 → 单元测试模块 | 仅追加测试代码到 src/cli/run/tests.rs，访问 pub(super) 函数；TempDir 隔离 I/O |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Tampering | tests.rs 中写入的临时 bad.log / dml.log / params.log | accept | TempDir 自动清理；与现有 collector 路径测试同等风险面 |
| T-02-04 | Information Disclosure | 测试代码意外暴露解析器内部结构 | accept | 仅使用已 pub 的 ErrorStats 字段（parse_error_records、parse_error_count、filtered_out）与 pub LogProcessor trait，不引入内部反射 |
</threat_model>

<verification>
- 单测：`cargo test test_collector_` 4 个新测试绿
- 全套：`cargo test` 全部通过
- 覆盖率：`cargo llvm-cov --summary-only` 输出 TOTAL Line % ≥ 92.00%（Task 2 checkpoint 人工确认）
- 质量门：`cargo clippy --all-targets -- -D warnings` 0 警告 + `cargo fmt --check` 通过
- QUAL-03 防回归：tests/integration.rs:2917 `#[ignore]` 注解保持
</verification>

<success_criteria>
- 4 个新单元测试函数 (test_collector_invalid_path_returns_error, test_collector_parse_error_accumulation, test_collector_not_needed_filtering, test_collector_filtered_params_normalize) 通过
- cargo llvm-cov TOTAL Line % ≥ 92.00%
- cargo clippy/test/fmt 三道门禁绿色
- FSEvents `#[ignore]` 测试保持不变（D-01）
- 若 Task 2 checkpoint 反馈差额，按 D-05 路径补救后复测
</success_criteria>

<output>
Create `.planning/phases/02-fsevents/02-02-SUMMARY.md` when done（包含 llvm-cov TOTAL Line % 最终数值）
</output>
</content>
</invoke>