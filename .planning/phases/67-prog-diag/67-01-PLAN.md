---
phase: 67-prog-diag
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/cli/run/mod.rs
  - src/cli/run/processor.rs
  - src/cli/run/tests.rs
autonomous: true
requirements: [PROG-01, PROG-02]
must_haves:
  truths:
    - "多文件运行时进度条 {pos}/{len} 字段显示 [N/M] 文件计数器，N 随每文件完成自增"
    - "进度条 message 含 records/sec（>=10k 显示 'Xk rec/s'，否则 'X rec/s'）"
    - "ETA 字段由 indicatif 基于 inc(1) 速率自动渲染（template 含 '| eta {eta}'）"
    - "tick_progress 不再调用 pb.inc(1024)；pb.inc(1) 仅在 log_file_result 触发一次"
    - "非 TTY 环境进度条自动隐藏（indicatif ProgressDrawTarget 默认行为）"
  artifacts:
    - path: "src/cli/run/mod.rs"
      provides: "make_progress_bar(show_progress, total_files) -> Option<ProgressBar>"
      contains: "ProgressBar::new(total_files as u64)"
    - path: "src/cli/run/processor.rs"
      provides: "tick_progress 接收 file_start + file_name，pb.set_message 嵌入 records/sec"
      contains: "rec_per_s"
    - path: "src/cli/run/tests.rs"
      provides: "test_progress_bar_template 单元测试"
      contains: "test_progress_bar_template"
  key_links:
    - from: "handle_run (src/cli/run/mod.rs:62)"
      to: "make_progress_bar"
      via: "新签名传入 log_files.len()"
      pattern: "make_progress_bar\\(show_progress, log_files\\.len\\(\\)\\)"
    - from: "process_log_file (src/cli/run/processor.rs)"
      to: "tick_progress / log_file_result"
      via: "file_start 传入 tick；log_file_result 末尾 pb.inc(1)"
      pattern: "pb\\.inc\\(1\\)"
---

<objective>
PROG-01/PROG-02：将顺序路径进度条由 spinner 升级为固定长度文件计数器 + ETA + records/sec。

Purpose: 多文件运行时用户实时看到 [N/M]、records/sec、ETA。
Output: make_progress_bar 新签名、tick_progress 新参数、log_file_result 文件计数推进、test_progress_bar_template 单元测试。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/67-prog-diag/67-CONTEXT.md
@.planning/phases/67-prog-diag/67-RESEARCH.md
@.planning/phases/67-prog-diag/67-PATTERNS.md
@src/cli/run/mod.rs
@src/cli/run/processor.rs
@src/cli/run/tests.rs

<interfaces>
当前 make_progress_bar 签名（src/cli/run/mod.rs:205）：
  fn make_progress_bar(show_progress: bool) -> Option<ProgressBar>

目标签名（D-01/D-06）：
  fn make_progress_bar(show_progress: bool, total_files: usize) -> Option<ProgressBar>

当前 tick_progress 签名（src/cli/run/processor.rs:155）：
  fn tick_progress(pb: Option<&ProgressBar>, records_in_file: usize, interrupted: &Arc<AtomicBool>) -> bool

目标签名（D-04）：
  fn tick_progress(pb: Option<&ProgressBar>, records_in_file: usize, file_start: Instant, file_name: &str, interrupted: &Arc<AtomicBool>) -> bool

process_log_file 内已有 file_start: Instant（line 186）和 file_name: String（line 187），直接以 &file_name 传入。
tick_progress 唯一调用点在 process_log_file:208；需同时更新 setup_progress_bar 输出（已含 file_index/total_files/file_name，不变）。
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — 进度条模板单元测试桩（RED）</name>
  <files>src/cli/run/tests.rs</files>
  <read_first>
    - src/cli/run/tests.rs（现有 #[cfg(test)] mod tests 块和 use super::*; 模式）
    - src/cli/run/mod.rs:205-218（make_progress_bar 现有实现）
    - .planning/phases/67-prog-diag/67-VALIDATION.md（Wave 0 条目）
  </read_first>
  <behavior>
    - Test 1: test_progress_bar_template — 调用 make_progress_bar(true, 3)，断言返回 Some(pb)；pb.length() == Some(3)；pb.position() == 0；模板设置不 panic。
    - Test 2: test_progress_bar_disabled — 调用 make_progress_bar(false, 3)，断言返回 None。
  </behavior>
  <action>
    在 src/cli/run/tests.rs 末尾追加两个 #[test] 函数 test_progress_bar_template 与 test_progress_bar_disabled。调用 super::make_progress_bar（私有，同模块可见）。模板生效需要先在 Task 2 修改函数签名为接收 total_files: usize；本任务先以新签名书写测试调用，确保编译失败 → 标志 RED。使用 indicatif 0.18 ProgressBar API（length() 返回 Option<u64>，position() 返回 u64）。per D-01/D-02/D-06。
  </action>
  <verify>
    <automated>cargo test --lib cli::run::tests::test_progress_bar_template 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - 文件 src/cli/run/tests.rs 包含字符串 "fn test_progress_bar_template"
    - 文件 src/cli/run/tests.rs 包含字符串 "fn test_progress_bar_disabled"
    - 两个测试以 make_progress_bar(true, 3) / make_progress_bar(false, 3) 形式调用（搜索字面量 "make_progress_bar(true, 3)" 与 "make_progress_bar(false, 3)" 命中）
    - cargo test 命令编译失败或测试 RED（因 make_progress_bar 旧签名只接收一个参数）
  </acceptance_criteria>
  <done>测试函数已写入；cargo build 报 "expected 1 argument, found 2"（确认 RED）。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 升级 make_progress_bar + tick_progress + log_file_result（GREEN）</name>
  <files>src/cli/run/mod.rs, src/cli/run/processor.rs</files>
  <read_first>
    - src/cli/run/mod.rs:29-135（handle_run 主体，含 line 62 调用点）
    - src/cli/run/mod.rs:204-218（make_progress_bar 当前实现）
    - src/cli/run/processor.rs:121-169（log_file_result 与 tick_progress）
    - src/cli/run/processor.rs:175-226（process_log_file，确认 file_start/file_name 作用域）
    - .planning/phases/67-prog-diag/67-RESEARCH.md Pattern 1 与 Pattern 2
  </read_first>
  <action>
    步骤 A — src/cli/run/mod.rs:
      1) make_progress_bar 新签名：fn make_progress_bar(show_progress: bool, total_files: usize) -> Option<ProgressBar>
      2) 函数体改为 ProgressBar::new(total_files as u64)；template 字符串：
         "{spinner:.cyan} [{pos}/{len}] {wide_msg} | eta {eta}"
         set_style 失败回退 ProgressStyle::default_bar()；tick_chars 与 enable_steady_tick(80ms) 保留（per D-02）。
      3) handle_run 第 62 行调用点改为：let pb = make_progress_bar(show_progress, log_files.len());
    步骤 B — src/cli/run/processor.rs:
      4) tick_progress 新签名：fn tick_progress(pb: Option<&ProgressBar>, records_in_file: usize, file_start: std::time::Instant, file_name: &str, interrupted: &Arc<AtomicBool>) -> bool
      5) 删除 pb.inc(1024) 调用（per D-03）。在 records_in_file.trailing_zeros() >= 10 分支内改为计算 elapsed = file_start.elapsed().as_secs_f64()；rec_per_s = records_in_file as f64 / elapsed.max(1e-9)；speed_label 按 D-04 + Claude's Discretion：rec_per_s >= 10_000.0 时 format!("{:.0}k rec/s", rec_per_s / 1000.0)；否则 format!("{:.0} rec/s", rec_per_s)；调用 pb.set_message(format!("{file_name} | {speed_label}"))。
      6) process_log_file 中 tick_progress 唯一调用点（line ~208）改为传入 file_start, &file_name。
      7) log_file_result 函数末尾（show_progress && pb.is_some() 分支内 set_message 之后）追加 pb.inc(1)（per D-03）。
    步骤 C：cargo fmt + cargo clippy --all-targets -- -D warnings + cargo test。
    注意：保留 #[derive(Default)] 思路（本任务不改 ErrorStats）；并行路径不传 pb（现状不变）。
  </action>
  <verify>
    <automated>cargo clippy --all-targets -- -D warnings 2>&1 | tail -10 && cargo test --lib cli::run::tests::test_progress_bar 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - grep -nE 'pub fn make_progress_bar|fn make_progress_bar' src/cli/run/mod.rs 显示签名包含 "total_files: usize"
    - grep -n 'ProgressBar::new(' src/cli/run/mod.rs 命中 "ProgressBar::new(total_files as u64)"
    - grep -n '\\[{pos}/{len}\\]' src/cli/run/mod.rs 命中模板字符串
    - grep -nc 'pb.inc(1024)' src/cli/run/processor.rs 输出 0
    - grep -n 'pb.inc(1)' src/cli/run/processor.rs 命中至少 1 处（位于 log_file_result）
    - grep -n 'fn tick_progress' src/cli/run/processor.rs 显示签名含 "file_start" 与 "file_name"
    - grep -n 'make_progress_bar(show_progress' src/cli/run/mod.rs 命中 "make_progress_bar(show_progress, log_files.len())"
    - cargo clippy --all-targets -- -D warnings 通过
    - cargo test --lib cli::run::tests::test_progress_bar_template 与 test_progress_bar_disabled 全部 GREEN
  </acceptance_criteria>
  <done>PROG-01/PROG-02 行为可观察：[N/M] + ETA + records/sec；现有 cli/run 测试无回归；clippy 通过。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| TTY → stderr | 进度条直接写 stderr；无 untrusted input 进入此渲染层 |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-67-01 | Denial of Service | tick_progress 高频字符串格式化 | accept | 1024 节奏现状已限制频率；改动只新增 format!("{} | {}"), 无 unbounded 分配 |
| T-67-02 | Information Disclosure | file_name 写入 stderr | accept | file_name 来源为用户配置的本地路径；非敏感数据，与现状一致 |
</threat_model>

<verification>
- cargo fmt --check
- cargo clippy --all-targets -- -D warnings
- cargo test --lib cli::run 全绿
- 手工：cargo run -- run -c sqllogs/example.toml 观察 [N/M] + ETA（manual; VALIDATION.md "Manual-Only Verifications"）
</verification>

<success_criteria>
- ROADMAP SC1 + SC2（[2/5] 文件计数器 + records/sec + ETA）行为可观察
- 非 TTY 时进度条隐藏（indicatif 默认行为，无 ANSI 序列）
</success_criteria>

<output>
完成后创建 .planning/phases/67-prog-diag/67-01-SUMMARY.md
</output>
