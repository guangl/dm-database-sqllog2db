---
phase: 32-cleanup-project-structure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib.rs
  - src/main.rs
  - src/cli/mod.rs
  - src/pipeline/mod.rs
  - src/config/mod.rs
  - src/config/validate.rs
  - Cargo.toml
autonomous: true
requirements: [RM-08]
must_haves:
  truths:
    - "src/lib.rs 中不再有 charts 或 resume 模块声明"
    - "src/main.rs 中不再有 charts 或 resume 模块声明"
    - "src/cli/mod.rs 中不再有 digest、stats、update 模块声明"
    - "src/pipeline/mod.rs 中不再有 fingerprint、aggregator、template_reporter 模块声明、ChartsConfig、TemplateConfig、TemplateReportConfig 及其相关函数和测试"
    - "Config 结构体中不再有 resume、template、charts 字段"
    - "src/config/validate.rs 中不再有 validate_charts() 方法及其测试"
    - "Cargo.toml 中不再有 charts/resume/template/stats/digest/update/completions 相关的未用依赖"
    - "cargo build 编译成功"
  artifacts:
    - path: "src/lib.rs"
      provides: "Stale mod declarations removed"
    - path: "src/main.rs"
      provides: "Stale mod declarations removed"
    - path: "src/cli/mod.rs"
      provides: "Stale mod declarations removed"
    - path: "src/pipeline/mod.rs"
      provides: "ChartsConfig/TemplateConfig/TemplateReportConfig/fingerprint/aggregator/template_reporter 模块及相关代码清理完毕"
    - path: "src/config/mod.rs"
      provides: "resume/template/charts Config 字段和导入删除, PIPELINE_MIGRATION_HINT 引用精简"
    - path: "src/config/validate.rs"
      provides: "validate_charts() 方法和 12 个测试删除"
    - path: "Cargo.toml"
      provides: "clap_complete, self_update, clap_mangen, serde_json, ryu, hdrhistogram, plotters 依赖删除"
---

<objective>
清理前 4 个移除阶段（Phases 28-31）在模块声明层和配置层遗留的结构性残留。

**Purpose:** 移除已删除功能模块在 crate 根模块、子模块声明文件、Config 结构体、以及 Cargo.toml 中的死引用，使代码骨架干净可编译。

**Output:** 7 个源文件编辑 + Cargo.toml 依赖修剪，`cargo build` 编译通过。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-cleanup-project-structure/32-RESEARCH.md

<interfaces>
<!-- Plan 01 对 Plan 02/03 的契约：Config 结构体字段已删除，pipeline 中 ChartsConfig/TemplateConfig 等类型已移除 -->
<!-- Plan 02 无需处理 charts/resume/template 的 Config 字段 -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 删除 src/lib.rs、src/main.rs、src/cli/mod.rs 的 stale mod 声明</name>
  <files>src/lib.rs, src/main.rs, src/cli/mod.rs</files>
  <action>
    1. **src/lib.rs** (10 行, ~216 bytes): 删除第 1 行 `pub(crate) mod charts;` 和第 11 行 `pub(crate) mod resume;`。保留其余 10 行不变。
    2. **src/main.rs** (约 15 行头部): 删除 `mod charts;` 和 `mod resume;` 两行。保留 `use mimalloc::MiMalloc;`, `#[global_allocator]`, `mod cli;`, `mod color;`, `mod config;`, `mod error;`, `mod exporter;`, `mod lang;`, `mod logging;`, `mod parser;`, `mod pipeline;` 不变。
    3. **src/cli/mod.rs** (9 行, ~146 bytes): 删除第 1 行 `pub mod digest;`、第 7 行 `pub mod stats;`、第 8 行 `pub mod update;`。保留 `pub mod init;`, `pub mod opts;`, `pub mod preflight;`, `pub mod run;`, `pub mod show_config;`, `pub mod validate;` 不变。
  </action>
  <verify>
    <automated>cargo build 2>&1 | head -20</automated>
  </verify>
  <done>
    - src/lib.rs: 10 行 → 8 行，无 charts 或 resume 模块声明
    - src/main.rs: 无 charts 或 resume 模块声明
    - src/cli/mod.rs: 6 行，仅含 init/opts/preflight/run/show_config/validate
    - cargo build 编译成功
  </done>
</task>

<task type="auto">
  <name>Task 2: 删除 src/pipeline/mod.rs 中已移除模块的类型、函数和测试</name>
  <files>src/pipeline/mod.rs</files>
  <action>
    删除以下所有已移除模块相关的代码段（全文件名引用，不用行号，因为 Phases 28-31 后行号可能偏移）:

    1. **模块声明和 use re-export:**
       - 删除 `pub mod fingerprint;` 及紧随的 `fingerprint::fingerprint` 和 `fingerprint::normalize_template` 两行 re-export
       - 删除 `pub mod aggregator;` 及紧随的 `aggregator::ChartEntry`、`aggregator::TemplateAggregator`、`aggregator::TemplateStats` 三行 re-export
       - 删除 `pub(crate) mod template_reporter;`

    2. **Struct 定义:**
       - 删除完整的 `TemplateConfig` 结构体定义（包含 `#[serde(default)]`、`pub enabled: bool`、`pub report: Option<TemplateReportConfig>` 等字段，约 10 行）
       - 删除完整的 `TemplateReportConfig` 结构体定义（包含 `pub path: Option<String>`、`pub format: ReportFormat` 等字段，约 22 行）
       - 删除完整的 `ChartsConfig` 结构体定义（包含 `pub output_dir: Option<String>`、`pub top_n: Option<usize>`、`pub trend: Option<TrendConfig>`、`pub show_user: Option<bool>` 等字段，约 34 行）

    3. **函数:**
       - 删除 `template_report_enabled()` 函数定义（约 8 行，返回 bool，检查 TemplateReportConfig.path）
       - 删除 `derive_template_report_paths()` 函数定义（约 22 行，返回 `(Option<String>, Option<String>)`）

    4. **测试:**
       - 删除 `#[cfg(test)]` 模块中所有 charts/template 相关测试函数（约 8 个测试）:
         `test_charts_config_default_values`、`test_charts_config_deserialize_only_output_dir`、`test_charts_config_deserialize_full`、`test_template_config_default`、`test_template_config_deserialize_with_report`、`test_template_config_deserialize_empty_is_default`、`test_charts_config_deserialize_trend_user_flags`、`test_charts_config_new_fields_default_true`
       - 确认上述测试使用的 helper 类型（如 `charts_config()`、`template_config()`、`TrendConfig` 关联）一起删除

    5. **其他清理:**
       - 确认 test fixtures 中不包含已删除类型的引用
       - 确保 `#[cfg(test)] mod tests { ... }` 区块语法仍然正确（删除内容后无空测试模块）
  </action>
  <verify>
    <automated>cargo build 2>&1 | head -20</automated>
  </verify>
  <done>
    - src/pipeline/mod.rs 中无 fingerprint、aggregator、template_reporter 模块声明
    - 无  ChartsConfig、TemplateConfig、TemplateReportConfig 类型定义
    - 无  template_report_enabled()、derive_template_report_paths() 函数
    - 无  charts/template 相关的测试代码
    - cargo build 编译成功
  </done>
</task>

<task type="auto">
  <name>Task 3: 删除 Config 残留字段 + validate_charts() + Cargo.toml 未用依赖 + PIPELINE_MIGRATION_HINT</name>
  <files>src/config/mod.rs, src/config/validate.rs, Cargo.toml</files>
  <action>
    **A. src/config/mod.rs:**
    1. 删除第 4 行 `pub mod resume;` 和第 10 行 `pub use resume::ResumeConfig;`
    2. 删除 `use crate::pipeline::` 导入中的 `ChartsConfig` 和 `TemplateConfig`，仅保留 `FiltersFeature, NormalizeConfig, OutputConfig`
    3. 删除 Config 结构体中的 `pub resume: ResumeConfig,`、`pub template: Option<TemplateConfig>,`、`pub charts: Option<ChartsConfig>,` 三个字段
    4. 更新 `PIPELINE_MIGRATION_HINT` 常量：删除 `[pipeline.template_analysis] → [template]` 行、删除 `[pipeline.charts] → [charts]` 行（若存在），仅保留与 filter/normalize/output 字段相关的迁移提示
    5. 更新 `test_config_has_5_top_level_optional_fields` 测试：将原断言 5 个字段的计数改为断言 3 个字段（`filter`, `replace_parameters`, `output`），或改为断言存在特定字段而非计数

    **B. src/config/validate.rs:**
    1. 删除 `self.validate_charts()?;` 调用（两处：约 line 18 和 line 65）
    2. 删除完整的 `fn validate_charts(&self) -> Result<()>` 方法定义（约 line 102-128）
    3. 删除 `use crate::pipeline::{` 导入中的 `ChartsConfig` 和 `TemplateConfig`，仅保留 `NormalizeConfig, OutputConfig`
    4. 删除所有 validate_charts 的测试函数（约 8 个测试，分布在 lines 452-582）:
       `test_validate_charts_requires_template_analysis`
       `test_validate_charts_with_template_analysis_enabled_passes`
       `test_validate_and_compile_charts_requires_template_analysis_enabled`
       `test_validate_charts_empty_output_dir_is_rejected`
       `test_validate_charts_whitespace_output_dir_is_rejected`
       `test_validate_charts_top_n_zero_is_rejected`
       `test_validate_and_compile_charts_empty_output_dir_is_rejected`
       `test_validate_and_compile_charts_top_n_zero_is_rejected`
    5. 确认这些测试中使用的 `TemplateConfig::default()`、`ChartsConfig` 等引用不再存在

    **C. Cargo.toml:**
    1. 删除以下 7 个依赖（整行，含可能的多行配置）:
       - `clap_complete = "4.6"` (line 41)
       - `self_update` 及其 features 配置块 (lines 45-49)
       - `clap_mangen = "0.3"` (line 53)
       - `serde_json = "1.0.149"` (line 55)
       - `ryu = "1"` (line 56)
       - `hdrhistogram = "7.5.4"` (line 60)
       - `plotters` 及其 features 配置块 (lines 61-65)
    2. 确认删除后 `[dependencies]` 段无空行或畸形语法
  </action>
  <verify>
    <automated>cargo build 2>&1 | head -20</automated>
  </verify>
  <done>
    - Config 结构体中无 resume/template/charts 字段
    - validate.rs 中无 validate_charts() 方法
    - PIPELINE_MIGRATION_HINT 仅包含有效的迁移映射
    - Cargo.toml 中无 clap_complete/self_update/clap_mangen/serde_json/ryu/hdrhistogram/plotters 依赖
    - cargo build 编译成功
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Phase 32 仅做代码结构清理，不涉及运行时信任边界变更。

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-01 | Tampering | Cargo.toml | mitigate | 删除依赖后运行 `cargo build` 确认编译通过 |
</threat_model>

<verification>
```bash
cargo build 2>&1
```
预期：编译成功，无错误。
</verification>

<success_criteria>
- [ ] `cargo build` 编译成功，无错误
- [ ] 所有已删除功能模块的 mod 声明在 lib.rs/main.rs/cli/mod.rs 中已移除
- [ ] pipeline/mod.rs 无 charts/template/fingerprint/aggregator 残留
- [ ] Config 结构体无 resume/template/charts 字段
- [ ] validate.rs 无 validate_charts() 方法
- [ ] Cargo.toml 无已移除功能的依赖
</success_criteria>

<output>
创建 `.planning/phases/32-cleanup-project-structure/32-01-SUMMARY.md` 记录实际删除的代码行数和编译结果。
</output>
